;/******/ (() => { // webpackBootstrap
/******/ 	"use strict";
/******/ 	var __webpack_modules__ = ({

/***/ "./source/admin/App.tsx"
/*!******************************!*\
  !*** ./source/admin/App.tsx ***!
  \******************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var react_router_dom__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! react-router-dom */ "./node_modules/react-router/dist/development/chunk-UVKPFVEO.mjs");
/* harmony import */ var _watermark_Watermark__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./watermark/Watermark */ "./source/admin/watermark/Watermark.tsx");
/* harmony import */ var _settings_Settings__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./settings/Settings */ "./source/admin/settings/Settings.tsx");
/* harmony import */ var _files_Files__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ./files/Files */ "./source/admin/files/Files.tsx");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! react/jsx-runtime */ "react/jsx-runtime");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_4___default = /*#__PURE__*/__webpack_require__.n(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_4__);

const App = () => {
  return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_4__.jsxs)(react_router_dom__WEBPACK_IMPORTED_MODULE_0__.Routes, {
    children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_4__.jsx)(react_router_dom__WEBPACK_IMPORTED_MODULE_0__.Route, {
      path: "*",
      element: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_4__.jsx)(react_router_dom__WEBPACK_IMPORTED_MODULE_0__.Navigate, {
        to: "/files/all",
        replace: true
      })
    }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_4__.jsx)(react_router_dom__WEBPACK_IMPORTED_MODULE_0__.Route, {
      path: "/files/:menuKey/:dynamicKey?",
      element: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_4__.jsx)(_files_Files__WEBPACK_IMPORTED_MODULE_3__["default"], {})
    }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_4__.jsx)(react_router_dom__WEBPACK_IMPORTED_MODULE_0__.Route, {
      path: "/settings/:menuKey",
      element: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_4__.jsx)(_settings_Settings__WEBPACK_IMPORTED_MODULE_2__["default"], {})
    }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_4__.jsx)(react_router_dom__WEBPACK_IMPORTED_MODULE_0__.Route, {
      path: "/watermark/:menuKey",
      element: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_4__.jsx)(_watermark_Watermark__WEBPACK_IMPORTED_MODULE_1__["default"], {})
    })]
  });
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (App);

/***/ },

/***/ "./source/admin/Main.tsx"
/*!*******************************!*\
  !*** ./source/admin/Main.tsx ***!
  \*******************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _components_alert_Alert__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ~/components/alert/Alert */ "./source/components/alert/Alert.tsx");
/* harmony import */ var react_router_dom__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! react-router-dom */ "./node_modules/react-router/dist/development/chunk-UVKPFVEO.mjs");
/* harmony import */ var _routes_MainRoute__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ~/routes/MainRoute */ "./source/routes/MainRoute.tsx");
/* harmony import */ var react_redux__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! react-redux */ "./node_modules/react-redux/dist/react-redux.mjs");
/* harmony import */ var _redux_store__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ~/redux/store */ "./source/redux/store.ts");
/* harmony import */ var _App__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ./App */ "./source/admin/App.tsx");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! react/jsx-runtime */ "react/jsx-runtime");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_6___default = /*#__PURE__*/__webpack_require__.n(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_6__);

const Main = () => {
  return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_6__.jsx)(react_router_dom__WEBPACK_IMPORTED_MODULE_1__.HashRouter, {
    children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_6__.jsx)(react_redux__WEBPACK_IMPORTED_MODULE_3__.Provider, {
      store: _redux_store__WEBPACK_IMPORTED_MODULE_4__.store,
      children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_6__.jsx)(_components_alert_Alert__WEBPACK_IMPORTED_MODULE_0__.CustomAlertProvider, {
        children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_6__.jsx)(_routes_MainRoute__WEBPACK_IMPORTED_MODULE_2__["default"], {
          children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_6__.jsx)(_App__WEBPACK_IMPORTED_MODULE_5__["default"], {})
        })
      })
    })
  });
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (Main);

/***/ },

/***/ "./source/admin/files/Files.tsx"
/*!**************************************!*\
  !*** ./source/admin/files/Files.tsx ***!
  \**************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _components_status_PremiumCard__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ~/components/status/PremiumCard */ "./source/components/status/PremiumCard.tsx");
/* harmony import */ var react_router_dom__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! react-router-dom */ "./node_modules/react-router/dist/development/chunk-UVKPFVEO.mjs");
/* harmony import */ var _hooks_useLocalStorage__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ~/hooks/useLocalStorage */ "./source/hooks/useLocalStorage.ts");
/* harmony import */ var _components_FilesContainer__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ./components/FilesContainer */ "./source/admin/files/components/FilesContainer.tsx");
/* harmony import */ var _components_toggleTheme__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ~/components/toggleTheme */ "./source/components/toggleTheme/index.ts");
/* harmony import */ var _components_status_Upgrade__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ~/components/status/Upgrade */ "./source/components/status/Upgrade.tsx");
/* harmony import */ var _constants_menus__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! ~/constants/menus */ "./source/constants/menus.ts");
/* harmony import */ var _wordpress_element__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! @wordpress/element */ "@wordpress/element");
/* harmony import */ var _wordpress_element__WEBPACK_IMPORTED_MODULE_7___default = /*#__PURE__*/__webpack_require__.n(_wordpress_element__WEBPACK_IMPORTED_MODULE_7__);
/* harmony import */ var _redux_hooks__WEBPACK_IMPORTED_MODULE_8__ = __webpack_require__(/*! ~/redux/hooks */ "./source/redux/hooks.ts");
/* harmony import */ var _components_sidebar__WEBPACK_IMPORTED_MODULE_9__ = __webpack_require__(/*! ~/components/sidebar */ "./source/components/sidebar/index.ts");
/* harmony import */ var _components_layout__WEBPACK_IMPORTED_MODULE_10__ = __webpack_require__(/*! ~/components/layout */ "./source/components/layout/index.ts");
/* harmony import */ var _components_topbar__WEBPACK_IMPORTED_MODULE_11__ = __webpack_require__(/*! ~/components/topbar */ "./source/components/topbar/index.ts");
/* harmony import */ var _components_Search__WEBPACK_IMPORTED_MODULE_12__ = __webpack_require__(/*! ./components/Search */ "./source/admin/files/components/Search.tsx");
/* harmony import */ var _components_Header__WEBPACK_IMPORTED_MODULE_13__ = __webpack_require__(/*! ./components/Header */ "./source/admin/files/components/Header.tsx");
/* harmony import */ var _components_button__WEBPACK_IMPORTED_MODULE_14__ = __webpack_require__(/*! ~/components/button */ "./source/components/button/index.ts");
/* harmony import */ var _redux_api_file__WEBPACK_IMPORTED_MODULE_15__ = __webpack_require__(/*! ~/redux/api/file */ "./source/redux/api/file.ts");
/* harmony import */ var _hooks_useFile__WEBPACK_IMPORTED_MODULE_16__ = __webpack_require__(/*! ./hooks/useFile */ "./source/admin/files/hooks/useFile.ts");
/* harmony import */ var _wordpress_i18n__WEBPACK_IMPORTED_MODULE_17__ = __webpack_require__(/*! @wordpress/i18n */ "@wordpress/i18n");
/* harmony import */ var _wordpress_i18n__WEBPACK_IMPORTED_MODULE_17___default = /*#__PURE__*/__webpack_require__.n(_wordpress_i18n__WEBPACK_IMPORTED_MODULE_17__);
/* harmony import */ var _components_icon__WEBPACK_IMPORTED_MODULE_18__ = __webpack_require__(/*! ~/components/icon */ "./source/components/icon/index.ts");
/* harmony import */ var clsx__WEBPACK_IMPORTED_MODULE_19__ = __webpack_require__(/*! clsx */ "./node_modules/clsx/dist/clsx.mjs");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_20__ = __webpack_require__(/*! react/jsx-runtime */ "react/jsx-runtime");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_20___default = /*#__PURE__*/__webpack_require__.n(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_20__);

const Files = () => {
  const [theme, setTheme] = (0,_hooks_useLocalStorage__WEBPACK_IMPORTED_MODULE_2__.useLocalStorage)("pnpnm-theme-status", "light");
  const {
    setFile,
    count,
    loading
  } = (0,_hooks_useFile__WEBPACK_IMPORTED_MODULE_16__["default"])();
  const {
    menuKey,
    dynamicKey
  } = (0,react_router_dom__WEBPACK_IMPORTED_MODULE_1__.useParams)();
  const navigate = (0,react_router_dom__WEBPACK_IMPORTED_MODULE_1__.useNavigate)();
  const dispatch = (0,_redux_hooks__WEBPACK_IMPORTED_MODULE_8__.useAppDispatch)();
  const enableTrash = pnpnm?.settings?.general?.files?.moveToTrash ?? false;
  (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_7__.useEffect)(() => {
    if (menuKey === "trash" && !enableTrash) {
      navigate("/files/all");
    }
  }, [menuKey, enableTrash]);
  const handleMenuClick = key => {
    navigate(`/files/${key}`);
  };
  const search = /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_20__.jsx)(_components_Search__WEBPACK_IMPORTED_MODULE_12__["default"], {});
  const refresh = /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_20__.jsxs)(_components_button__WEBPACK_IMPORTED_MODULE_14__["default"], {
    variant: "primary",
    onClick: () => {
      if (loading) return;
      setFile("loading", true);
      setFile("query.page", 1);
      dispatch(_redux_api_file__WEBPACK_IMPORTED_MODULE_15__.file.util.invalidateTags(["Files"]));
    },
    children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_20__.jsx)(_components_icon__WEBPACK_IMPORTED_MODULE_18__["default"], {
      name: "refresh",
      color: "pure",
      fontSize: "lg",
      className: (0,clsx__WEBPACK_IMPORTED_MODULE_19__["default"])(loading && "loading")
    }), (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_17__.__)("Refresh", "advanced-media-library")]
  });
  const themeButton = /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_20__.jsx)(_components_toggleTheme__WEBPACK_IMPORTED_MODULE_4__["default"], {
    theme: theme,
    setTheme: setTheme
  });
  return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_20__.jsxs)(_components_layout__WEBPACK_IMPORTED_MODULE_10__["default"], {
    children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_20__.jsxs)(_components_sidebar__WEBPACK_IMPORTED_MODULE_9__["default"], {
      minWidth: 320,
      children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_20__.jsx)(_components_sidebar__WEBPACK_IMPORTED_MODULE_9__["default"].Menu, {
        children: _constants_menus__WEBPACK_IMPORTED_MODULE_6__.FILES_MENUS?.map(({
          key,
          title,
          icon
        }, index) => /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_20__.jsx)(_components_sidebar__WEBPACK_IMPORTED_MODULE_9__["default"].Item, {
          title: key === "dynamic" && dynamicKey ? `Dynamic Files (${dynamicKey})` : title,
          icon: icon,
          count: count[key] ?? 0,
          active: key === menuKey,
          onClick: () => handleMenuClick(key)
        }, key ?? index))
      }), !pnpnm.isPro && /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_20__.jsx)(_components_status_PremiumCard__WEBPACK_IMPORTED_MODULE_0__["default"], {
        compact: true,
        style: {
          marginTop: 10
        }
      }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_20__.jsxs)(_components_sidebar__WEBPACK_IMPORTED_MODULE_9__["default"].Bottom, {
        trash: true,
        trashCount: count?.trash,
        trashActive: menuKey === "trash",
        trashClick: () => {
        },
        disabledTrash: !enableTrash,
        children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_20__.jsx)(_components_sidebar__WEBPACK_IMPORTED_MODULE_9__["default"].Trash, {
          active: menuKey === "trash",
          count: count?.trash,
          onClick: () => {
          },
          disabled: !enableTrash
        }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_20__.jsx)(_components_sidebar__WEBPACK_IMPORTED_MODULE_9__["default"].HelpCenter, {})]
      })]
    }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_20__.jsxs)(_components_layout__WEBPACK_IMPORTED_MODULE_10__["default"].Wrapper, {
      children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_20__.jsx)(_components_topbar__WEBPACK_IMPORTED_MODULE_11__["default"], {
        leftContents: [search],
        rightContents: [themeButton, refresh]
      }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_20__.jsx)(_components_layout__WEBPACK_IMPORTED_MODULE_10__["default"].Content, {
        children: pnpnm?.isPro ? /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_20__.jsxs)(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_20__.Fragment, {
          children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_20__.jsx)(_components_Header__WEBPACK_IMPORTED_MODULE_13__["default"], {}), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_20__.jsx)(_components_FilesContainer__WEBPACK_IMPORTED_MODULE_3__["default"], {})]
        }) : ["all", "uncategorized"].includes(menuKey) ? /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_20__.jsxs)(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_20__.Fragment, {
          children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_20__.jsx)(_components_Header__WEBPACK_IMPORTED_MODULE_13__["default"], {}), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_20__.jsx)(_components_FilesContainer__WEBPACK_IMPORTED_MODULE_3__["default"], {})]
        }) : /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_20__.jsx)(_components_status_Upgrade__WEBPACK_IMPORTED_MODULE_5__["default"], {})
      })]
    })]
  });
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (Files);

/***/ },

/***/ "./source/admin/files/components/Delete.tsx"
/*!**************************************************!*\
  !*** ./source/admin/files/components/Delete.tsx ***!
  \**************************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   DeleteFile: () => (/* binding */ DeleteFile),
/* harmony export */   useDeleteFile: () => (/* binding */ useDeleteFile)
/* harmony export */ });
/* harmony import */ var _wordpress_element__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @wordpress/element */ "@wordpress/element");
/* harmony import */ var _wordpress_element__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(_wordpress_element__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var _redux_functions_updateFilesCaches__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ~/redux/functions/updateFilesCaches */ "./source/redux/functions/updateFilesCaches.ts");
/* harmony import */ var _components_alert_Alert__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ~/components/alert/Alert */ "./source/components/alert/Alert.tsx");
/* harmony import */ var _redux_api_media__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ~/redux/api/media */ "./source/redux/api/media.ts");
/* harmony import */ var _redux_features_file_file__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ~/redux/features/file/file */ "./source/redux/features/file/file.ts");
/* harmony import */ var _components_inlineStack__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ~/components/inlineStack */ "./source/components/inlineStack/index.ts");
/* harmony import */ var _components_blockStack__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! ~/components/blockStack */ "./source/components/blockStack/index.ts");
/* harmony import */ var _redux_hooks__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! ~/redux/hooks */ "./source/redux/hooks.ts");
/* harmony import */ var _wordpress_i18n__WEBPACK_IMPORTED_MODULE_8__ = __webpack_require__(/*! @wordpress/i18n */ "@wordpress/i18n");
/* harmony import */ var _wordpress_i18n__WEBPACK_IMPORTED_MODULE_8___default = /*#__PURE__*/__webpack_require__.n(_wordpress_i18n__WEBPACK_IMPORTED_MODULE_8__);
/* harmony import */ var _hooks_useSettings__WEBPACK_IMPORTED_MODULE_9__ = __webpack_require__(/*! ~/hooks/useSettings */ "./source/hooks/useSettings.ts");
/* harmony import */ var react_router_dom__WEBPACK_IMPORTED_MODULE_10__ = __webpack_require__(/*! react-router-dom */ "./node_modules/react-router/dist/development/chunk-UVKPFVEO.mjs");
/* harmony import */ var _components_button__WEBPACK_IMPORTED_MODULE_11__ = __webpack_require__(/*! ~/components/button */ "./source/components/button/index.ts");
/* harmony import */ var _hooks_useFile__WEBPACK_IMPORTED_MODULE_12__ = __webpack_require__(/*! ../hooks/useFile */ "./source/admin/files/hooks/useFile.ts");
/* harmony import */ var _components_card__WEBPACK_IMPORTED_MODULE_13__ = __webpack_require__(/*! ~/components/card */ "./source/components/card/index.ts");
/* harmony import */ var _components_text__WEBPACK_IMPORTED_MODULE_14__ = __webpack_require__(/*! ~/components/text */ "./source/components/text/index.ts");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_15__ = __webpack_require__(/*! react/jsx-runtime */ "react/jsx-runtime");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_15___default = /*#__PURE__*/__webpack_require__.n(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_15__);

const DURATION = 5;
const DeleteFile = ({
  ids,
  onClose
}) => {
  const {
    data
  } = (0,_hooks_useSettings__WEBPACK_IMPORTED_MODULE_9__["default"])();
  const [loading, setLoading] = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useState)(false);
  const {
    setFile,
    count,
    selectedFiles,
    bulkSelect
  } = (0,_hooks_useFile__WEBPACK_IMPORTED_MODULE_12__["default"])();
  const [deleteFileMutation] = (0,_redux_api_media__WEBPACK_IMPORTED_MODULE_3__.useDeleteFileMutation)();
  const dispatch = (0,_redux_hooks__WEBPACK_IMPORTED_MODULE_7__.useAppDispatch)();
  const {
    showAlert
  } = (0,_components_alert_Alert__WEBPACK_IMPORTED_MODULE_2__.useCustomAlert)();
  const {
    dynamicKey
  } = (0,react_router_dom__WEBPACK_IMPORTED_MODULE_10__.useParams)();
  const enableUndo = data?.advanced?.action?.undoActions ?? false;
  const radius = 15;
  const circumference = 2 * Math.PI * radius;
  const multiple = bulkSelect && (ids?.length ?? 0) > 1;
  const performActualDelete = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useCallback)(async () => {
    setLoading(true);
    try {
      const response = await deleteFileMutation({
        ids: ids ?? selectedFiles?.map(f => f?.id) ?? []
      }).unwrap();
      setFile("hiddenFileIds", []);
      if (!enableUndo && bulkSelect) {
        setFile("bulkSelect", false);
        setFile("selectedFiles", []);
      }
      const {
        deleted,
        allFiles,
        uncategorized,
        dynamicFolders,
        unused,
        trash
      } = response?.data || {};
      dispatch((0,_redux_features_file_file__WEBPACK_IMPORTED_MODULE_4__.deleteFile)(deleted ?? []));
      const dynamicCount = dynamicKey && dynamicFolders ? dynamicFolders[dynamicKey] ?? 0 : Object.keys(dynamicFolders ?? {}).length;
      setFile("count", {
        ...count,
        all: allFiles ?? 0,
        uncategorized: uncategorized ?? 0,
        dynamic: dynamicCount,
        unused: unused ?? 0,
        trash: trash ?? 0
      });
      (0,_redux_functions_updateFilesCaches__WEBPACK_IMPORTED_MODULE_1__["default"])(deleted ?? [], {
        allFiles,
        uncategorized,
        unused,
        trash,
        dynamicFolders
      });
      showAlert({
        toast: true,
        type: "success",
        text: response?.message || (multiple ? (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_8__.__)("Files deleted successfully", "ninja-media") : (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_8__.__)("File deleted successfully", "ninja-media")),
        timer: 3000,
        timerProgressBar: true,
        showConfirmButton: false
      });
    } catch (error) {
      showAlert({
        toast: true,
        type: "error",
        text: error?.data?.message || (multiple ? (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_8__.__)("Failed to delete files", "ninja-media") : (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_8__.__)("Failed to delete file", "ninja-media")),
        timer: 3000,
        timerProgressBar: true,
        showConfirmButton: false
      });
    } finally {
      setLoading(false);
    }
  }, [ids, multiple, deleteFileMutation, showAlert]);
  const UndoUI = ({
    multiple,
    onUndo,
    onDeleteNow,
    progress,
    timeLeft,
    radius,
    circumference
  }) => /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_15__.jsxs)("div", {
    className: "pnpnm-undo-popup",
    children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_15__.jsxs)(_components_inlineStack__WEBPACK_IMPORTED_MODULE_5__["default"], {
      align: "between",
      gap: 10,
      children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_15__.jsxs)(_components_inlineStack__WEBPACK_IMPORTED_MODULE_5__["default"], {
        gap: 10,
        children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_15__.jsxs)("div", {
          className: "pnpnm-undo-popup__circle",
          children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_15__.jsxs)("svg", {
            viewBox: "0 0 36 36",
            children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_15__.jsx)("circle", {
              className: "pnpnm-undo-popup__circle-bg",
              cx: "18",
              cy: "18",
              r: radius
            }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_15__.jsx)("circle", {
              className: "pnpnm-undo-popup__circle-progress",
              cx: "18",
              cy: "18",
              r: radius,
              strokeDasharray: circumference,
              strokeDashoffset: circumference - circumference * progress / 100
            })]
          }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_15__.jsx)("span", {
            children: timeLeft
          })]
        }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_15__.jsx)(_components_text__WEBPACK_IMPORTED_MODULE_14__["default"], {
          size: "sm",
          children: multiple ? (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_8__.__)("Files will be deleted…", "ninja-media") : (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_8__.__)("File will be deleted…", "ninja-media")
        })]
      }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_15__.jsxs)(_components_inlineStack__WEBPACK_IMPORTED_MODULE_5__["default"], {
        gap: 10,
        wrap: false,
        align: "end",
        children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_15__.jsx)(_components_button__WEBPACK_IMPORTED_MODULE_11__["default"], {
          variant: "secondary",
          size: "extrasmall",
          startIcon: "undo",
          onClick: onUndo,
          children: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_8__.__)("Undo", "ninja-media")
        }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_15__.jsx)(_components_button__WEBPACK_IMPORTED_MODULE_11__["default"], {
          variant: "secondary",
          size: "extrasmall",
          startIcon: "delete_history",
          onClick: onDeleteNow,
          children: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_8__.__)("Delete", "ninja-media")
        })]
      })]
    }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_15__.jsx)("div", {
      className: "pnpnm-undo-popup__bottom-bar",
      style: {
        width: `${progress}%`
      }
    })]
  });
  const showUndoUI = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useCallback)(() => {
    const container = document.createElement("div");
    container.id = "pnpnm-undo-fixed";
    container.className = "pnpnm-top-level-wrapper";
    document.body.appendChild(container);
    let root = null;
    const UndoWrapper = () => {
      const [progress, setProgress] = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useState)(100);
      const rafRef = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useRef)(null);
      const startTimeRef = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useRef)(null);
      const didUndoRef = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useRef)(false);
      const stopTimer = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useCallback)(() => {
        if (rafRef.current) cancelAnimationFrame(rafRef.current);
      }, []);
      const startTimer = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useCallback)(() => {
        stopTimer();
        didUndoRef.current = false;
        startTimeRef.current = performance.now();
        const tick = now => {
          const elapsed = (now - startTimeRef.current) / 1000;
          const remaining = Math.max(0, 1 - elapsed / DURATION);
          setProgress(remaining * 100);
          if (remaining > 0) {
            rafRef.current = requestAnimationFrame(tick);
          } else if (!didUndoRef.current) {
            cleanup();
            performActualDelete();
          }
        };
        rafRef.current = requestAnimationFrame(tick);
      }, [stopTimer, performActualDelete]);
      const handleUndo = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useCallback)(() => {
        didUndoRef.current = true;
        stopTimer();
        cleanup();
        setFile("hiddenFileIds", []);
      }, [stopTimer, setFile]);
      const cleanup = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useCallback)(() => {
        stopTimer();
        if (container.parentNode) document.body.removeChild(container);
        if (root) root.unmount();
      }, [stopTimer]);
      const handleDeleteNow = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useCallback)(() => {
        didUndoRef.current = true;
        stopTimer();
        cleanup();
        performActualDelete();
      }, [stopTimer, cleanup, performActualDelete]);
      (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useEffect)(() => {
        startTimer();
        return cleanup;
      }, [startTimer, cleanup]);
      return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_15__.jsx)(UndoUI, {
        multiple: multiple,
        onUndo: handleUndo,
        onDeleteNow: handleDeleteNow,
        progress: progress,
        timeLeft: Math.ceil(progress / 100 * DURATION),
        radius: radius,
        circumference: circumference
      });
    };
    root = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.createRoot)(container);
    root.render(/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_15__.jsx)(UndoWrapper, {}));
  }, [performActualDelete, multiple, radius, circumference]);
  const triggerDelete = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useCallback)(() => {
    if (!enableUndo) {
      performActualDelete();
      onClose();
      return;
    }
    setFile("hiddenFileIds", ids ?? selectedFiles?.map(f => f?.id) ?? []);
    if (bulkSelect) {
      setFile("bulkSelect", false);
      setFile("selectedFiles", []);
    }
    onClose();
    showUndoUI();
  }, [enableUndo, ids, dispatch, onClose, performActualDelete, showUndoUI]);
  return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_15__.jsxs)(_components_blockStack__WEBPACK_IMPORTED_MODULE_6__["default"], {
    gap: 20,
    children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_15__.jsx)(_components_card__WEBPACK_IMPORTED_MODULE_13__["default"], {
      padding: 10,
      background: "errorextralight",
      border: "errorlight",
      rounded: "md",
      style: {
        width: "60px",
        height: "60px"
      },
      className: "text-error",
      children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_15__.jsxs)("svg", {
        width: "100%",
        height: "100%",
        viewBox: "0 0 24 24",
        fill: "none",
        children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_15__.jsx)("circle", {
          cx: "12",
          cy: "12",
          r: "10",
          stroke: "currentColor",
          strokeWidth: "2"
        }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_15__.jsx)("path", {
          d: "M15 9l-6 6M9 9l6 6",
          stroke: "currentColor",
          strokeWidth: "2",
          strokeLinecap: "round"
        })]
      })
    }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_15__.jsxs)(_components_blockStack__WEBPACK_IMPORTED_MODULE_6__["default"], {
      gap: 10,
      children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_15__.jsx)(_components_text__WEBPACK_IMPORTED_MODULE_14__["default"], {
        size: "xl",
        weight: "semibold",
        children: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_8__.__)("Delete", "ninja-media")
      }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_15__.jsx)(_components_text__WEBPACK_IMPORTED_MODULE_14__["default"], {
        color: "descgray",
        children: multiple ? (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_8__.sprintf)((0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_8__.__)("Are you sure you want to delete these %d files?", "ninja-media"), ids?.length ?? 0) : (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_8__.__)("Are you sure you want to delete this file?", "ninja-media")
      })]
    }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_15__.jsxs)(_components_inlineStack__WEBPACK_IMPORTED_MODULE_5__["default"], {
      align: "end",
      gap: 10,
      children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_15__.jsx)(_components_button__WEBPACK_IMPORTED_MODULE_11__["default"], {
        variant: "secondary",
        size: "small",
        startIcon: "cancel",
        onClick: onClose,
        disabled: loading,
        children: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_8__.__)("Cancel", "ninja-media")
      }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_15__.jsx)(_components_button__WEBPACK_IMPORTED_MODULE_11__["default"], {
        variant: "error",
        size: "small",
        startIcon: "delete",
        onClick: triggerDelete,
        loading: loading,
        children: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_8__.__)("Delete", "ninja-media")
      })]
    })]
  });
};
const useDeleteFile = () => {
  const {
    showAlert,
    closeAlert
  } = (0,_components_alert_Alert__WEBPACK_IMPORTED_MODULE_2__.useCustomAlert)();
  const openDeleteFile = ids => {
    showAlert({
      id: "delete-file-modal",
      type: "info",
      showIcon: false,
      showConfirmButton: false,
      allowEscapeKey: false,
      width: "450px",
      height: "fit-content",
      html: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_15__.jsx)(DeleteFile, {
        ids: ids,
        onClose: () => closeAlert("delete-file-modal")
      })
    });
  };
  return {
    openDeleteFile
  };
};

/***/ },

/***/ "./source/admin/files/components/DynamicFiles__premium_only.tsx"
/*!**********************************************************************!*\
  !*** ./source/admin/files/components/DynamicFiles__premium_only.tsx ***!
  \**********************************************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _wordpress_element__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @wordpress/element */ "@wordpress/element");
/* harmony import */ var _wordpress_element__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(_wordpress_element__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var _components_skeletonLoader__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ~/components/skeletonLoader */ "./source/components/skeletonLoader/index.ts");
/* harmony import */ var _components_blockStack__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ~/components/blockStack */ "./source/components/blockStack/index.ts");
/* harmony import */ var _components_gridStack__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ~/components/gridStack */ "./source/components/gridStack/index.ts");
/* harmony import */ var react_router_dom__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! react-router-dom */ "./node_modules/react-router/dist/development/chunk-UVKPFVEO.mjs");
/* harmony import */ var _hooks_useFile__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ../hooks/useFile */ "./source/admin/files/hooks/useFile.ts");
/* harmony import */ var _components_card__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! ~/components/card */ "./source/components/card/index.ts");
/* harmony import */ var _components_icon__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! ~/components/icon */ "./source/components/icon/index.ts");
/* harmony import */ var _components_text__WEBPACK_IMPORTED_MODULE_8__ = __webpack_require__(/*! ~/components/text */ "./source/components/text/index.ts");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_9__ = __webpack_require__(/*! react/jsx-runtime */ "react/jsx-runtime");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_9___default = /*#__PURE__*/__webpack_require__.n(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_9__);

const DynamicFiles = ({
  loading
}) => {
  const {
    dynamicFolders
  } = (0,_hooks_useFile__WEBPACK_IMPORTED_MODULE_5__["default"])();
  const navigate = (0,react_router_dom__WEBPACK_IMPORTED_MODULE_4__.useNavigate)();
  const containerRef = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useRef)(null);
  (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useLayoutEffect)(() => {
    const cards = containerRef.current?.querySelectorAll(".pnpnm-dynamic-card");
    if (!cards) return;
    const parentRect = containerRef.current.getBoundingClientRect();
    const centerX = parentRect.left + parentRect.width / 2;
    const centerY = parentRect.top + parentRect.height / 2;
    const transforms = [];
    cards.forEach(card => {
      const rect = card.getBoundingClientRect();
      const dx = centerX - (rect.left + rect.width / 2);
      const dy = centerY - (rect.top + rect.height / 2);
      transforms.push({
        el: card,
        dx,
        dy
      });
    });
    transforms.forEach(({
      el,
      dx,
      dy
    }) => {
      el.style.transform = `translate(${dx}px, ${dy}px) scale(0.6)`;
      el.style.opacity = "0";
    });
    requestAnimationFrame(() => {
      transforms.forEach(({
        el
      }, i) => {
        el.style.transition = "transform 0.6s cubic-bezier(0.22, 1, 0.36, 1), opacity 0.6s";
        el.style.transform = "translate(0, 0) scale(1)";
        el.style.opacity = "1";
        el.style.transitionDelay = `${i * 0.08}s`;
      });
    });
  }, [dynamicFolders]);
  const handleFolderClick = key => {
    navigate(`/files/dynamic/${key}`);
  };
  if (!loading && Object.keys(dynamicFolders).length === 0) {
    return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_9__.jsxs)(_components_blockStack__WEBPACK_IMPORTED_MODULE_2__["default"], {
      align: "center",
      inlineAlign: "center",
      gap: 10,
      style: {
        marginTop: "50px"
      },
      children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_9__.jsx)(_components_text__WEBPACK_IMPORTED_MODULE_8__["default"], {
        size: "xl",
        weight: "semibold",
        align: "center",
        children: "No dynamic folders found"
      }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_9__.jsxs)(_components_text__WEBPACK_IMPORTED_MODULE_8__["default"], {
        size: "sm",
        align: "center",
        children: ["Dynamic folders are automatically created based on file extensions. Once you have files ", /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_9__.jsx)("br", {}), " with different extensions, dynamic folders will appear here."]
      })]
    });
  }
  return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_9__.jsx)(_components_gridStack__WEBPACK_IMPORTED_MODULE_3__["default"], {
    ref: containerRef,
    gap: 15,
    columns: "auto-fill",
    min: "400px",
    marginTop: 20,
    children: loading ? Array.from({
      length: 6
    }).map((_, index) => /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_9__.jsx)(_components_card__WEBPACK_IMPORTED_MODULE_6__["default"], {
      padding: 0,
      background: "transparent",
      flex: true,
      direction: "col",
      align: "center",
      blockAlign: "center",
      gap: 15,
      style: {
        height: "200px"
      },
      children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_9__.jsx)(_components_skeletonLoader__WEBPACK_IMPORTED_MODULE_1__["default"], {
        width: "100%",
        height: "100%",
        rounded: "lg"
      })
    }, index)) : Object.entries(dynamicFolders).map(([key, count], index) => /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_9__.jsxs)(_components_card__WEBPACK_IMPORTED_MODULE_6__["default"], {
      background: "white",
      flex: true,
      direction: "col",
      align: "center",
      blockAlign: "center",
      gap: 15,
      style: {
        position: "relative",
        height: "200px",
        cursor: "pointer"
      },
      className: "pnpnm-dynamic-card",
      onClick: () => handleFolderClick(key),
      children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_9__.jsx)(_components_icon__WEBPACK_IMPORTED_MODULE_7__["default"], {
        name: "image",
        color: "primary",
        style: {
          fontSize: "35px"
        }
      }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_9__.jsx)(_components_text__WEBPACK_IMPORTED_MODULE_8__["default"], {
        color: "primary",
        size: "xl",
        children: key
      }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_9__.jsx)(_components_text__WEBPACK_IMPORTED_MODULE_8__["default"], {
        color: "primary",
        size: "lg",
        style: {
          position: "absolute",
          top: "15px",
          right: "15px"
        },
        children: count
      })]
    }, key ?? index))
  });
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (DynamicFiles);

/***/ },

/***/ "./source/admin/files/components/File.tsx"
/*!************************************************!*\
  !*** ./source/admin/files/components/File.tsx ***!
  \************************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _components_contextMenu_ContextMenu__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ~/components/contextMenu/ContextMenu */ "./source/components/contextMenu/ContextMenu.tsx");
/* harmony import */ var _utils_functions__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ~/utils/functions */ "./source/utils/functions.ts");
/* harmony import */ var _components_inlineStack__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ~/components/inlineStack */ "./source/components/inlineStack/index.ts");
/* harmony import */ var _components_blockStack__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ~/components/blockStack */ "./source/components/blockStack/index.ts");
/* harmony import */ var _FileDetails__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ./FileDetails */ "./source/admin/files/components/FileDetails.tsx");
/* harmony import */ var _components_checkbox__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ~/components/checkbox */ "./source/components/checkbox/index.ts");
/* harmony import */ var _components_avatar__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! ~/components/avatar */ "./source/components/avatar/index.ts");
/* harmony import */ var _hooks_useFile__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! ../hooks/useFile */ "./source/admin/files/hooks/useFile.ts");
/* harmony import */ var _components_card__WEBPACK_IMPORTED_MODULE_8__ = __webpack_require__(/*! ~/components/card */ "./source/components/card/index.ts");
/* harmony import */ var _components_icon__WEBPACK_IMPORTED_MODULE_9__ = __webpack_require__(/*! ~/components/icon */ "./source/components/icon/index.ts");
/* harmony import */ var _components_text__WEBPACK_IMPORTED_MODULE_10__ = __webpack_require__(/*! ~/components/text */ "./source/components/text/index.ts");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_11__ = __webpack_require__(/*! react/jsx-runtime */ "react/jsx-runtime");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_11___default = /*#__PURE__*/__webpack_require__.n(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_11__);

const File = ({
  file
}) => {
  const {
    setFile,
    selectedFiles,
    hiddenFileIds,
    bulkSelect
  } = (0,_hooks_useFile__WEBPACK_IMPORTED_MODULE_7__["default"])();
  const {
    openViewDetails
  } = (0,_FileDetails__WEBPACK_IMPORTED_MODULE_4__.useViewDetails)();
  const {
    show
  } = (0,_components_contextMenu_ContextMenu__WEBPACK_IMPORTED_MODULE_0__.useContextMenu)();
  const {
    id,
    name,
    url,
    thumbnailUrl,
    extension,
    size,
    updatedAt
  } = file || {};
  const isSelected = selectedFiles?.find(f => f?.id === id);
  const handleSelect = () => {
    if (!bulkSelect) {
      setFile("detailsFile", file);
      openViewDetails();
      return;
    }
    const isExist = selectedFiles?.find(f => f?.id === id);
    const newSelectedFiles = isExist ? selectedFiles?.filter(f => f?.id !== id) : [...(selectedFiles ?? []), file];
    setFile("selectedFiles", newSelectedFiles);
  };
  if (hiddenFileIds?.includes(id)) return null;
  return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_11__.jsxs)(_components_card__WEBPACK_IMPORTED_MODULE_8__["default"], {
    "data-file-id": id,
    padding: 10,
    background: "white",
    rounded: "md",
    flex: true,
    direction: "col",
    gap: 10,
    style: {
      cursor: "pointer",
      opacity: bulkSelect ? isSelected ? 1 : 0.5 : 1,
      transition: "opacity 0.2s ease"
    },
    className: "pnpnm-fade-in",
    onClick: handleSelect,
    onDoubleClick: () => {
      setFile("detailsFile", file);
      openViewDetails();
    },
    onContextMenu: event => {
      event.preventDefault();
      show("file-menu", event, {
        files: bulkSelect ? selectedFiles?.length > 0 ? selectedFiles : [file] : [file]
      });
    },
    children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_11__.jsxs)(_components_blockStack__WEBPACK_IMPORTED_MODULE_3__["default"], {
      style: {
        position: "relative",
        height: "150px"
      },
      className: "bg-extralight rounded-sm border border-solid border-light",
      children: [bulkSelect && /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_11__.jsx)(_components_checkbox__WEBPACK_IMPORTED_MODULE_5__["default"], {
        rounded: "sm",
        style: {
          position: "absolute",
          top: 10,
          left: 10
        },
        checked: !!isSelected,
        onChange: handleSelect
      }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_11__.jsx)(_components_avatar__WEBPACK_IMPORTED_MODULE_6__["default"], {
        src: `${thumbnailUrl || url}?v=${updatedAt}`,
        alt: name,
        width: "100%",
        height: "100%",
        rounded: "sm",
        showSpinner: true
      })]
    }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_11__.jsxs)(_components_blockStack__WEBPACK_IMPORTED_MODULE_3__["default"], {
      gap: 5,
      children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_11__.jsx)(_components_text__WEBPACK_IMPORTED_MODULE_10__["default"], {
        size: "sm",
        wrap: false,
        ellipsis: true,
        children: name
      }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_11__.jsxs)(_components_inlineStack__WEBPACK_IMPORTED_MODULE_2__["default"], {
        align: "between",
        gap: 10,
        wrap: false,
        children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_11__.jsxs)(_components_inlineStack__WEBPACK_IMPORTED_MODULE_2__["default"], {
          gap: 10,
          wrap: false,
          style: {
            minWidth: 0
          },
          children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_11__.jsx)(_components_text__WEBPACK_IMPORTED_MODULE_10__["default"], {
            color: "descgray",
            size: "xs",
            textTransform: "uppercase",
            wrap: false,
            children: extension
          }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_11__.jsx)(_components_text__WEBPACK_IMPORTED_MODULE_10__["default"], {
            color: "descgray",
            size: "xs",
            wrap: false,
            ellipsis: true,
            style: {
              minWidth: 0
            },
            children: (0,_utils_functions__WEBPACK_IMPORTED_MODULE_1__.formatFileSize)(size || 0)
          })]
        }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_11__.jsx)(_components_icon__WEBPACK_IMPORTED_MODULE_9__["default"], {
          name: "more_vert",
          color: "descgray",
          style: {
            cursor: "pointer"
          },
          onClick: event => {
            event.preventDefault();
            event.stopPropagation();
            show("file-menu", event, {
              files: bulkSelect ? selectedFiles?.length > 0 ? selectedFiles : [file] : [file]
            });
          }
        })]
      })]
    })]
  });
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (File);

/***/ },

/***/ "./source/admin/files/components/FileContext.tsx"
/*!*******************************************************!*\
  !*** ./source/admin/files/components/FileContext.tsx ***!
  \*******************************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _media_library_context_FileContext__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ~/media-library/context/FileContext */ "./source/media-library/context/FileContext.tsx");
/* harmony import */ var _components_contextMenu_ContextMenu__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ~/components/contextMenu/ContextMenu */ "./source/components/contextMenu/ContextMenu.tsx");
/* harmony import */ var _hooks_useSettings__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ~/hooks/useSettings */ "./source/hooks/useSettings.ts");
/* harmony import */ var react_router_dom__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! react-router-dom */ "./node_modules/react-router/dist/development/chunk-UVKPFVEO.mjs");
/* harmony import */ var _components_status__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ~/components/status */ "./source/components/status/index.ts");
/* harmony import */ var _hooks_useFile__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ../hooks/useFile */ "./source/admin/files/hooks/useFile.ts");
/* harmony import */ var _components_icon__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! ~/components/icon */ "./source/components/icon/index.ts");
/* harmony import */ var _wordpress_i18n__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! @wordpress/i18n */ "@wordpress/i18n");
/* harmony import */ var _wordpress_i18n__WEBPACK_IMPORTED_MODULE_7___default = /*#__PURE__*/__webpack_require__.n(_wordpress_i18n__WEBPACK_IMPORTED_MODULE_7__);
/* harmony import */ var _components_card__WEBPACK_IMPORTED_MODULE_8__ = __webpack_require__(/*! ~/components/card */ "./source/components/card/index.ts");
/* harmony import */ var _components_text__WEBPACK_IMPORTED_MODULE_9__ = __webpack_require__(/*! ~/components/text */ "./source/components/text/index.ts");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_10__ = __webpack_require__(/*! react/jsx-runtime */ "react/jsx-runtime");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_10___default = /*#__PURE__*/__webpack_require__.n(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_10__);

const FileContext = ({
  onMenuClick
}) => {
  const {
    data
  } = (0,_hooks_useSettings__WEBPACK_IMPORTED_MODULE_2__["default"])();
  const {
    bulkSelect
  } = (0,_hooks_useFile__WEBPACK_IMPORTED_MODULE_5__["default"])();
  const {
    menuKey
  } = (0,react_router_dom__WEBPACK_IMPORTED_MODULE_3__.useParams)();
  const enableTrash = data?.general?.files?.moveToTrash ?? false;

  return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_10__.jsx)(_components_contextMenu_ContextMenu__WEBPACK_IMPORTED_MODULE_1__.Menu, {
    id: "file-menu",
    style: {
      width: "220px"
    },
    children: ({
      props
    }) => {
      const FILTERED_MENUS = _media_library_context_FileContext__WEBPACK_IMPORTED_MODULE_0__.FILE_CONTEXT_MENU_LISTS?.filter(item => {
        if (item?.key === "open") {
          return menuKey === "trash" || bulkSelect ? false : true;
        } else if (item?.key === "view") {
          return bulkSelect ? false : true;
        } else if (item?.key === "get") {
          return bulkSelect ? false : true;
        } else if (item?.key === "edit") {
          if (menuKey === "trash" || bulkSelect) return false;
          const ext = (props?.files?.[0]?.extension || "").toLowerCase();
          return ["jpg", "jpeg", "png", "gif", "webp"].includes(ext);
        } else if (item?.key === "download") {
          return bulkSelect ? false : true;
        } else if (item?.key === "duplicate") {
          return menuKey === "trash" || bulkSelect ? false : true;
        } else if (item?.key === "replace") {
          return menuKey === "trash" || bulkSelect ? false : true;
        } else if (item?.key === "favorite") {
          return menuKey === "trash" || bulkSelect ? false : props?.files[0]?.isFavorite ? false : true;
        } else if (item?.key === "unfavorite") {
          return menuKey === "trash" || bulkSelect ? false : props?.files[0]?.isFavorite ? true : false;
        } else if (item?.key === "apply") {
          // removed by dead control flow

        } else if (item?.key === "remove") {
          // removed by dead control flow

        } else if (item?.key === "trash") {
          return menuKey === "trash" ? false : enableTrash ? true : false;
        } else if (item?.key === "restore") {
          return menuKey === "trash" ? true : false;
        } else if (item?.key === "delete") {
          return !enableTrash ? true : menuKey === "trash" ? true : false;
        }
      });
      return FILTERED_MENUS?.map(({
        key,
        title,
        icon,
        className,
        statusProps
      }, index) => {
        return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_10__.jsxs)("div", {
          children: [["trash", "delete"].includes(key) && /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_10__.jsx)(_components_contextMenu_ContextMenu__WEBPACK_IMPORTED_MODULE_1__.Separator, {}), ["trash", "delete"].includes(key) ? /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_10__.jsxs)(_components_card__WEBPACK_IMPORTED_MODULE_8__["default"], {
            statusProps: {
              isPro: key === "trash",
              size: "extrasmall",
              top: 7.5,
              right: 7
            },
            margin: key === "trash" ? "5px 5px 5px 5px" : "8px 5px 5px 5px",
            padding: 5,
            background: "errorextralight",
            border: "errorextralight",
            rounded: "sm",
            flex: true,
            direction: "row",
            blockAlign: "center",
            gap: 7,
            style: {
              cursor: "pointer",
              width: "calc(100% - 10px)"
            },
            className: "hover-errorlight",
            onClick: () => onMenuClick({
              key,
              files: props?.files
            }),
            children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_10__.jsx)(_components_icon__WEBPACK_IMPORTED_MODULE_6__["default"], {
              name: icon,
              fontSize: "lg",
              color: "error"
            }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_10__.jsx)(_components_text__WEBPACK_IMPORTED_MODULE_9__["default"], {
              color: "error",
              size: "sm",
              children: key === "delete" && menuKey === "trash" ? (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_7__.__)("Delete Permanently", "ninja-media") : title
            })]
          }) : /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_10__.jsx)(_components_status__WEBPACK_IMPORTED_MODULE_4__["default"], {
            ...statusProps,
            size: "extrasmall",
            placement: "right-center",
            right: 5,
            children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_10__.jsxs)(_components_contextMenu_ContextMenu__WEBPACK_IMPORTED_MODULE_1__.Item, {
              className: className,
              onClick: () => onMenuClick({
                key,
                files: props?.files
              }),
              children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_10__.jsx)(_components_icon__WEBPACK_IMPORTED_MODULE_6__["default"], {
                name: icon
              }), title]
            })
          })]
        }, key ?? index);
      });
    }
  });
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (FileContext);

/***/ },

/***/ "./source/admin/files/components/FileDetails.tsx"
/*!*******************************************************!*\
  !*** ./source/admin/files/components/FileDetails.tsx ***!
  \*******************************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   useViewDetails: () => (/* binding */ useViewDetails)
/* harmony export */ });
/* harmony import */ var _wordpress_element__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @wordpress/element */ "@wordpress/element");
/* harmony import */ var _wordpress_element__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(_wordpress_element__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var _components_alert_Alert__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ~/components/alert/Alert */ "./source/components/alert/Alert.tsx");
/* harmony import */ var _redux_api_media__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ~/redux/api/media */ "./source/redux/api/media.ts");
/* harmony import */ var _hooks_useFileActions__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ../hooks/useFileActions */ "./source/admin/files/hooks/useFileActions.ts");
/* harmony import */ var _utils_functions__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ~/utils/functions */ "./source/utils/functions.ts");
/* harmony import */ var _components_inlineStack__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ~/components/inlineStack */ "./source/components/inlineStack/index.ts");
/* harmony import */ var _components_blockStack__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! ~/components/blockStack */ "./source/components/blockStack/index.ts");
/* harmony import */ var _components_iconButton__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! ~/components/iconButton */ "./source/components/iconButton/index.ts");
/* harmony import */ var _components_tooltip__WEBPACK_IMPORTED_MODULE_8__ = __webpack_require__(/*! ~/components/tooltip */ "./source/components/tooltip/index.ts");
/* harmony import */ var _Delete__WEBPACK_IMPORTED_MODULE_9__ = __webpack_require__(/*! ./Delete */ "./source/admin/files/components/Delete.tsx");
/* harmony import */ var _components_avatar__WEBPACK_IMPORTED_MODULE_10__ = __webpack_require__(/*! ~/components/avatar */ "./source/components/avatar/index.ts");
/* harmony import */ var _components_button__WEBPACK_IMPORTED_MODULE_11__ = __webpack_require__(/*! ~/components/button */ "./source/components/button/index.ts");
/* harmony import */ var _components_input__WEBPACK_IMPORTED_MODULE_12__ = __webpack_require__(/*! ~/components/input */ "./source/components/input/index.ts");
/* harmony import */ var _hooks_useFile__WEBPACK_IMPORTED_MODULE_13__ = __webpack_require__(/*! ../hooks/useFile */ "./source/admin/files/hooks/useFile.ts");
/* harmony import */ var _wordpress_i18n__WEBPACK_IMPORTED_MODULE_14__ = __webpack_require__(/*! @wordpress/i18n */ "@wordpress/i18n");
/* harmony import */ var _wordpress_i18n__WEBPACK_IMPORTED_MODULE_14___default = /*#__PURE__*/__webpack_require__.n(_wordpress_i18n__WEBPACK_IMPORTED_MODULE_14__);
/* harmony import */ var _components_card__WEBPACK_IMPORTED_MODULE_15__ = __webpack_require__(/*! ~/components/card */ "./source/components/card/index.ts");
/* harmony import */ var _components_text__WEBPACK_IMPORTED_MODULE_16__ = __webpack_require__(/*! ~/components/text */ "./source/components/text/index.ts");
/* harmony import */ var _components_icon__WEBPACK_IMPORTED_MODULE_17__ = __webpack_require__(/*! ~/components/icon */ "./source/components/icon/index.ts");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_18__ = __webpack_require__(/*! react/jsx-runtime */ "react/jsx-runtime");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_18___default = /*#__PURE__*/__webpack_require__.n(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_18__);

const FileDetails = ({
  onClose
}) => {
  const {
    setFile,
    detailsFile
  } = (0,_hooks_useFile__WEBPACK_IMPORTED_MODULE_13__["default"])();
  const {
    getFileLink,
  } = (0,_hooks_useFileActions__WEBPACK_IMPORTED_MODULE_3__["default"])();
  const {
    openDeleteFile
  } = (0,_Delete__WEBPACK_IMPORTED_MODULE_9__.useDeleteFile)();
  const [updateMetadata, {
    isLoading: isSaving
  }] = (0,_redux_api_media__WEBPACK_IMPORTED_MODULE_2__.useUpdateFileMetadataMutation)();
  const {
    id,
    name,
    url,
    extension,
    size,
    location,
    createdAt,
    updatedAt,
    alt,
    caption,
    description
  } = detailsFile || {};
  const [metaTitle, setMetaTitle] = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useState)(name ?? "");
  const [metaAlt, setMetaAlt] = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useState)(alt ?? "");
  const [metaCaption, setMetaCaption] = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useState)(caption ?? "");
  const [metaDesc, setMetaDesc] = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useState)(description ?? "");
  const [saveStatus, setSaveStatus] = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useState)("idle");
  (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useEffect)(() => {
    setMetaTitle(name ?? "");
    setMetaAlt(alt ?? "");
    setMetaCaption(caption ?? "");
    setMetaDesc(description ?? "");
    setSaveStatus("idle");
  }, [id]);
  const handleClose = () => {
    setFile("detailsFile", null);
    onClose();
  };
  const handleSave = async () => {
    if (!id) return;
    setSaveStatus("idle");
    try {
      await updateMetadata({
        id,
        title: metaTitle,
        alt: metaAlt,
        caption: metaCaption,
        description: metaDesc
      }).unwrap();
      setSaveStatus("success");
      setTimeout(() => setSaveStatus("idle"), 2500);
    } catch {
      setSaveStatus("error");
    }
  };
  const openImageEditor = () => {
    const editorUrl = `${pnpnm?.siteUrl ?? ""}/wp-admin/post.php?post=${id}&action=edit&open_pnpnm_editor=${id}`;
    window.open(editorUrl, "_blank");
  };
  const isImage = ["jpg", "jpeg", "png", "gif", "webp", "avif"].includes((extension ?? "").toLowerCase());
  return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_18__.jsxs)(_components_card__WEBPACK_IMPORTED_MODULE_15__["default"], {
    padding: 0,
    background: "white",
    borderStyle: "none",
    rounded: "md",
    children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_18__.jsxs)(_components_inlineStack__WEBPACK_IMPORTED_MODULE_5__["default"], {
      align: "between",
      gap: 10,
      children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_18__.jsxs)(_components_text__WEBPACK_IMPORTED_MODULE_16__["default"], {
        size: "sm",
        children: [(0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_14__.__)("Media Details", "ninja-media"), " ", id]
      }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_18__.jsx)(_components_iconButton__WEBPACK_IMPORTED_MODULE_7__["default"], {
        variant: "error",
        size: "microsmall",
        name: "close",
        style: {
          borderRadius: "5px"
        },
        onClick: handleClose
      })]
    }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_18__.jsx)(_components_card__WEBPACK_IMPORTED_MODULE_15__["default"], {
      marginTop: 15,
      padding: 0,
      background: "extralight",
      rounded: "md",
      style: {
        height: "220px"
      },
      children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_18__.jsx)(_components_avatar__WEBPACK_IMPORTED_MODULE_10__["default"], {
        src: `${url}?v=${updatedAt}`,
        alt: name,
        width: "100%",
        height: "100%",
        rounded: "md",
        objectFit: "contain",
        showSpinner: true
      })
    }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_18__.jsxs)(_components_inlineStack__WEBPACK_IMPORTED_MODULE_5__["default"], {
      gap: 20,
      wrap: false,
      style: {
        marginTop: 10
      },
      children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_18__.jsxs)(_components_blockStack__WEBPACK_IMPORTED_MODULE_6__["default"], {
        gap: 5,
        children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_18__.jsx)(_components_text__WEBPACK_IMPORTED_MODULE_16__["default"], {
          size: "sm",
          children: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_14__.__)("Name:", "ninja-media")
        }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_18__.jsx)(_components_text__WEBPACK_IMPORTED_MODULE_16__["default"], {
          size: "sm",
          children: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_14__.__)("Type:", "ninja-media")
        }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_18__.jsx)(_components_text__WEBPACK_IMPORTED_MODULE_16__["default"], {
          size: "sm",
          children: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_14__.__)("Size:", "ninja-media")
        }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_18__.jsx)(_components_text__WEBPACK_IMPORTED_MODULE_16__["default"], {
          size: "sm",
          children: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_14__.__)("Date:", "ninja-media")
        })]
      }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_18__.jsxs)(_components_blockStack__WEBPACK_IMPORTED_MODULE_6__["default"], {
        gap: 5,
        style: {
          minWidth: 0
        },
        children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_18__.jsx)(_components_text__WEBPACK_IMPORTED_MODULE_16__["default"], {
          color: "descgray",
          size: "sm",
          wrap: false,
          ellipsis: true,
          style: {
            minWidth: 0
          },
          children: name
        }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_18__.jsx)(_components_text__WEBPACK_IMPORTED_MODULE_16__["default"], {
          color: "descgray",
          size: "sm",
          children: extension
        }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_18__.jsx)(_components_text__WEBPACK_IMPORTED_MODULE_16__["default"], {
          color: "descgray",
          size: "sm",
          children: (0,_utils_functions__WEBPACK_IMPORTED_MODULE_4__.formatFileSize)(size ?? 0)
        }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_18__.jsx)(_components_text__WEBPACK_IMPORTED_MODULE_16__["default"], {
          color: "descgray",
          size: "sm",
          children: createdAt
        })]
      })]
    }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_18__.jsxs)(_components_blockStack__WEBPACK_IMPORTED_MODULE_6__["default"], {
      marginTop: 15,
      gap: location?.length === 0 ? 0 : 8,
      children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_18__.jsx)(_components_text__WEBPACK_IMPORTED_MODULE_16__["default"], {
        size: "sm",
        children: location?.length === 0 ? (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_14__.__)("Location not Found", "ninja-media") : (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_14__.__)("Location:", "ninja-media")
      }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_18__.jsx)(_components_blockStack__WEBPACK_IMPORTED_MODULE_6__["default"], {
        gap: 5,
        style: {
          marginLeft: 10
        },
        children: location?.map(({
          name: locName,
          url: locUrl
        }, index) => /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_18__.jsxs)(_components_inlineStack__WEBPACK_IMPORTED_MODULE_5__["default"], {
          gap: 10,
          wrap: false,
          children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_18__.jsxs)(_components_text__WEBPACK_IMPORTED_MODULE_16__["default"], {
            color: "descgray",
            size: "sm",
            wrap: false,
            children: [index + 1, "."]
          }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_18__.jsx)(_components_text__WEBPACK_IMPORTED_MODULE_16__["default"], {
            color: "descgray",
            size: "sm",
            wrap: false,
            children: locName
          }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_18__.jsx)(_components_icon__WEBPACK_IMPORTED_MODULE_17__["default"], {
            name: "open_in_new",
            color: "primary",
            style: {
              cursor: "pointer"
            },
            onClick: () => window.open(locUrl, "_blank")
          })]
        }, index))
      })]
    }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_18__.jsxs)(_components_blockStack__WEBPACK_IMPORTED_MODULE_6__["default"], {
      marginTop: 18,
      gap: 10,
      children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_18__.jsx)(_components_text__WEBPACK_IMPORTED_MODULE_16__["default"], {
        size: "sm",
        style: {
          fontWeight: 600
        },
        children: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_14__.__)("Metadata", "ninja-media")
      }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_18__.jsx)(_components_input__WEBPACK_IMPORTED_MODULE_12__["default"], {
        label: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_14__.__)("Title", "ninja-media"),
        value: metaTitle,
        size: "small",
        placeholder: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_14__.__)("Image title", "ninja-media"),
        onChange: v => setMetaTitle(String(v))
      }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_18__.jsx)(_components_input__WEBPACK_IMPORTED_MODULE_12__["default"], {
        label: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_14__.__)("Alt Text", "ninja-media"),
        value: metaAlt,
        size: "small",
        placeholder: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_14__.__)("Describe the image for accessibility", "ninja-media"),
        onChange: v => setMetaAlt(String(v))
      }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_18__.jsx)(_components_input__WEBPACK_IMPORTED_MODULE_12__["default"], {
        label: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_14__.__)("Caption", "ninja-media"),
        value: metaCaption,
        size: "small",
        placeholder: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_14__.__)("Short caption shown below the image", "ninja-media"),
        onChange: v => setMetaCaption(String(v))
      }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_18__.jsxs)("div", {
        style: {
          display: "flex",
          flexDirection: "column",
          gap: 4
        },
        children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_18__.jsx)("label", {
          style: {
            fontSize: 13,
            fontWeight: 500
          },
          children: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_14__.__)("Description", "ninja-media")
        }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_18__.jsx)("textarea", {
          rows: 3,
          value: metaDesc,
          placeholder: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_14__.__)("Longer description of the image", "ninja-media"),
          onChange: e => setMetaDesc(e.target.value),
          style: {
            width: "100%",
            boxSizing: "border-box",
            padding: "6px 10px",
            fontSize: 13,
            borderRadius: 4,
            border: "1px solid #dcdcde",
            resize: "vertical",
            fontFamily: "inherit"
          }
        })]
      }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_18__.jsxs)(_components_inlineStack__WEBPACK_IMPORTED_MODULE_5__["default"], {
        align: "between",
        gap: 10,
        children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_18__.jsx)(_components_button__WEBPACK_IMPORTED_MODULE_11__["default"], {
          variant: "primary",
          size: "small",
          loading: isSaving,
          onClick: handleSave,
          children: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_14__.__)("Save Metadata", "ninja-media")
        }), saveStatus === "success" && /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_18__.jsx)(_components_text__WEBPACK_IMPORTED_MODULE_16__["default"], {
          size: "sm",
          color: "success",
          children: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_14__.__)("✓ Saved", "ninja-media")
        }), saveStatus === "error" && /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_18__.jsx)(_components_text__WEBPACK_IMPORTED_MODULE_16__["default"], {
          size: "sm",
          color: "error",
          children: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_14__.__)("Save failed", "ninja-media")
        })]
      })]
    }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_18__.jsxs)(_components_inlineStack__WEBPACK_IMPORTED_MODULE_5__["default"], {
      marginTop: 20,
      gap: 15,
      align: "end",
      children: [isImage && /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_18__.jsx)(_components_tooltip__WEBPACK_IMPORTED_MODULE_8__["default"], {
        title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_14__.__)("Open in Image Editor", "ninja-media"),
        arrow: true,
        wrap: "no-wrap",
        children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_18__.jsx)(_components_icon__WEBPACK_IMPORTED_MODULE_17__["default"], {
          name: "edit",
          color: "primary",
          fontSize: "xl",
          style: {
            cursor: "pointer"
          },
          onClick: openImageEditor
        })
      }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_18__.jsx)(_components_tooltip__WEBPACK_IMPORTED_MODULE_8__["default"], {
        title: pnpnm?.isPro ? (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_14__.__)("Download", "ninja-media") : (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_14__.__)("Premium Only", "ninja-media"),
        arrow: true,
        wrap: "no-wrap",
        children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_18__.jsx)(_components_icon__WEBPACK_IMPORTED_MODULE_17__["default"], {
          name: "download",
          color: "primary",
          fontSize: "2xl",
          style: {
            cursor: "pointer"
          },
          onClick: () => {
          }
        })
      }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_18__.jsx)(_components_tooltip__WEBPACK_IMPORTED_MODULE_8__["default"], {
        title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_14__.__)("Copy URL", "ninja-media"),
        arrow: true,
        wrap: "no-wrap",
        children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_18__.jsx)(_components_icon__WEBPACK_IMPORTED_MODULE_17__["default"], {
          name: "link",
          color: "primary",
          fontSize: "2xl",
          style: {
            cursor: "pointer"
          },
          onClick: () => getFileLink(detailsFile)
        })
      }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_18__.jsx)(_components_tooltip__WEBPACK_IMPORTED_MODULE_8__["default"], {
        title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_14__.__)("Open in Media Library", "ninja-media"),
        arrow: true,
        wrap: "no-wrap",
        children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_18__.jsx)(_components_icon__WEBPACK_IMPORTED_MODULE_17__["default"], {
          name: "open_in_new",
          color: "primary",
          fontSize: "xl",
          style: {
            cursor: "pointer"
          },
          onClick: () => window.open(`${pnpnm?.siteUrl}/wp-admin/upload.php?item=${id}`, "_blank")
        })
      }), detailsFile?.location?.length === 0 && /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_18__.jsx)(_components_tooltip__WEBPACK_IMPORTED_MODULE_8__["default"], {
        title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_14__.__)("Delete", "ninja-media"),
        arrow: true,
        wrap: "no-wrap",
        children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_18__.jsx)(_components_icon__WEBPACK_IMPORTED_MODULE_17__["default"], {
          name: "delete",
          color: "error",
          fontSize: "xl",
          style: {
            cursor: "pointer"
          },
          onClick: () => openDeleteFile([Number(id)])
        })
      })]
    })]
  });
};
const useViewDetails = () => {
  const {
    showAlert,
    closeAlert
  } = (0,_components_alert_Alert__WEBPACK_IMPORTED_MODULE_1__.useCustomAlert)();
  const openViewDetails = () => {
    showAlert({
      id: "view-details-modal",
      type: "info",
      showIcon: false,
      showConfirmButton: false,
      allowEscapeKey: false,
      width: "600px",
      height: "fit-content",
      html: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_18__.jsx)(FileDetails, {
        onClose: () => closeAlert("view-details-modal")
      })
    });
  };
  return {
    openViewDetails
  };
};

/***/ },

/***/ "./source/admin/files/components/FileLists.tsx"
/*!*****************************************************!*\
  !*** ./source/admin/files/components/FileLists.tsx ***!
  \*****************************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _components_contextMenu_ContextMenu__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ~/components/contextMenu/ContextMenu */ "./source/components/contextMenu/ContextMenu.tsx");
/* harmony import */ var _components_skeletonLoader__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ~/components/skeletonLoader */ "./source/components/skeletonLoader/index.ts");
/* harmony import */ var _utils_functions__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ~/utils/functions */ "./source/utils/functions.ts");
/* harmony import */ var _components_inlineStack__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ~/components/inlineStack */ "./source/components/inlineStack/index.ts");
/* harmony import */ var _FileDetails__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ./FileDetails */ "./source/admin/files/components/FileDetails.tsx");
/* harmony import */ var _hooks_useSettings__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ~/hooks/useSettings */ "./source/hooks/useSettings.ts");
/* harmony import */ var react_router_dom__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! react-router-dom */ "./node_modules/react-router/dist/development/chunk-UVKPFVEO.mjs");
/* harmony import */ var _components_checkbox__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! ~/components/checkbox */ "./source/components/checkbox/index.ts");
/* harmony import */ var _FileLocations__WEBPACK_IMPORTED_MODULE_8__ = __webpack_require__(/*! ./FileLocations */ "./source/admin/files/components/FileLocations.tsx");
/* harmony import */ var _components_avatar__WEBPACK_IMPORTED_MODULE_9__ = __webpack_require__(/*! ~/components/avatar */ "./source/components/avatar/index.ts");
/* harmony import */ var _Delete__WEBPACK_IMPORTED_MODULE_10__ = __webpack_require__(/*! ./Delete */ "./source/admin/files/components/Delete.tsx");
/* harmony import */ var _components_button__WEBPACK_IMPORTED_MODULE_11__ = __webpack_require__(/*! ~/components/button */ "./source/components/button/index.ts");
/* harmony import */ var _hooks_useFile__WEBPACK_IMPORTED_MODULE_12__ = __webpack_require__(/*! ../hooks/useFile */ "./source/admin/files/hooks/useFile.ts");
/* harmony import */ var _wordpress_i18n__WEBPACK_IMPORTED_MODULE_13__ = __webpack_require__(/*! @wordpress/i18n */ "@wordpress/i18n");
/* harmony import */ var _wordpress_i18n__WEBPACK_IMPORTED_MODULE_13___default = /*#__PURE__*/__webpack_require__.n(_wordpress_i18n__WEBPACK_IMPORTED_MODULE_13__);
/* harmony import */ var _components_card__WEBPACK_IMPORTED_MODULE_14__ = __webpack_require__(/*! ~/components/card */ "./source/components/card/index.ts");
/* harmony import */ var _components_text__WEBPACK_IMPORTED_MODULE_15__ = __webpack_require__(/*! ~/components/text */ "./source/components/text/index.ts");
/* harmony import */ var clsx__WEBPACK_IMPORTED_MODULE_16__ = __webpack_require__(/*! clsx */ "./node_modules/clsx/dist/clsx.mjs");
/* harmony import */ var _hooks_useFileActions__WEBPACK_IMPORTED_MODULE_17__ = __webpack_require__(/*! ../hooks/useFileActions */ "./source/admin/files/hooks/useFileActions.ts");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_18__ = __webpack_require__(/*! react/jsx-runtime */ "react/jsx-runtime");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_18___default = /*#__PURE__*/__webpack_require__.n(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_18__);

const FileLists = ({
  loading,
  loadMore
}) => {
  const {
    data
  } = (0,_hooks_useSettings__WEBPACK_IMPORTED_MODULE_5__["default"])();
  const {
    setFile,
    files,
    selectedFiles,
    hiddenFileIds,
    query,
    bulkSelect
  } = (0,_hooks_useFile__WEBPACK_IMPORTED_MODULE_12__["default"])();
  const HEADER = [{
    key: "NAME",
    label: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_13__.__)("NAME", "ninja-media")
  }, {
    key: "TYPE",
    label: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_13__.__)("TYPE", "ninja-media")
  }, {
    key: "SIZE",
    label: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_13__.__)("SIZE", "ninja-media")
  }, {
    key: "LOCATION",
    label: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_13__.__)("LOCATION", "ninja-media")
  }, {
    key: "DATE",
    label: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_13__.__)("DATE", "ninja-media")
  }, {
    key: "ACTION",
    label: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_13__.__)("ACTION", "ninja-media")
  }];
  const FLEX_VALUES = ["5", "1.3", "1.3", "1.3", "1.5", "1.3"];

  const {
    openViewDetails
  } = (0,_FileDetails__WEBPACK_IMPORTED_MODULE_4__.useViewDetails)();
  const {
    openDeleteFile
  } = (0,_Delete__WEBPACK_IMPORTED_MODULE_10__.useDeleteFile)();
  const {
    menuKey
  } = (0,react_router_dom__WEBPACK_IMPORTED_MODULE_6__.useParams)();
  const {
    show
  } = (0,_components_contextMenu_ContextMenu__WEBPACK_IMPORTED_MODULE_0__.useContextMenu)();
  const handleSelect = file => {
    if (!bulkSelect) {
      setFile("detailsFile", file);
      openViewDetails();
      return;
    }
    const isExist = selectedFiles?.find(f => f?.id === file?.id);
    const newSelectedFiles = isExist ? selectedFiles?.filter(f => f?.id !== file?.id) : [...(selectedFiles ?? []), file];
    setFile("selectedFiles", newSelectedFiles);
  };
  const enableTrash = data?.general?.files?.moveToTrash ?? false;
  if (!loading && files?.length === 0) return null;
  return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_18__.jsx)("div", {
    children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_18__.jsxs)(_components_card__WEBPACK_IMPORTED_MODULE_14__["default"], {
      padding: 0,
      background: "white",
      rounded: "md",
      children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_18__.jsx)(_components_inlineStack__WEBPACK_IMPORTED_MODULE_3__["default"], {
        className: (0,clsx__WEBPACK_IMPORTED_MODULE_16__["default"])("pnpnm-file-list-header", (files?.length > 0 || loading) && "pnpnm-file-list-header--active"),
        children: HEADER?.map(({
          key,
          label
        }, index) => /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_18__.jsx)(_components_inlineStack__WEBPACK_IMPORTED_MODULE_3__["default"], {
          gap: 10,
          align: key === "NAME" ? "start" : key === "ACTION" ? "end" : "center",
          style: {
            flex: FLEX_VALUES[index],
            minWidth: 0
          },
          children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_18__.jsx)(_components_text__WEBPACK_IMPORTED_MODULE_15__["default"], {
            color: "descgray",
            size: "sm",
            wrap: false,
            ellipsis: true,
            children: label
          })
        }, key))
      }), loading ? /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_18__.jsx)(_components_skeletonLoader__WEBPACK_IMPORTED_MODULE_1__["default"].SkeletonFile, {
        variant: "list",
        length: query?.perPage
      }) : files?.map((file, index) => {
        const isSelected = selectedFiles?.find(f => f?.id === file?.id);
        const ids = bulkSelect ? selectedFiles?.length > 0 ? selectedFiles?.map(f => f?.id) ?? [] : [file?.id] : [file?.id];
        if (hiddenFileIds?.includes(file?.id)) return null;
        return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_18__.jsxs)(_components_inlineStack__WEBPACK_IMPORTED_MODULE_3__["default"], {
          "data-file-id": file?.id,
          style: {
            cursor: "pointer",
            opacity: bulkSelect ? isSelected ? 1 : 0.5 : 1
          },
          className: "pnpnm-file-list-item",
          onClick: () => handleSelect(file),
          onDoubleClick: () => {
            setFile("detailsFile", file);
            openViewDetails();
          },
          onContextMenu: e => {
            e.preventDefault();
            show("file-menu", e, {
              files: bulkSelect ? selectedFiles?.length > 0 ? selectedFiles : [file] : [file]
            });
          },
          children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_18__.jsxs)(_components_inlineStack__WEBPACK_IMPORTED_MODULE_3__["default"], {
            gap: 10,
            wrap: false,
            style: {
              flex: FLEX_VALUES[0],
              minWidth: 0
            },
            children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_18__.jsxs)(_components_card__WEBPACK_IMPORTED_MODULE_14__["default"], {
              padding: 0,
              rounded: "sm",
              style: {
                position: "relative",
                width: "50px",
                height: "40px",
                flexShrink: 0
              },
              children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_18__.jsx)(_components_avatar__WEBPACK_IMPORTED_MODULE_9__["default"], {
                src: `${file?.url}?v=${file?.updatedAt}`,
                alt: file?.name,
                width: "100%",
                height: "100%",
                rounded: "sm",
                showSpinner: true
              }), bulkSelect && /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_18__.jsx)(_components_checkbox__WEBPACK_IMPORTED_MODULE_7__["default"], {
                rounded: "sm",
                checked: !!isSelected,
                style: {
                  position: "absolute",
                  top: "50%",
                  left: "50%",
                  transform: "translate(-50%, -50%)"
                },
                onChange: () => handleSelect(file)
              })]
            }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_18__.jsx)(_components_text__WEBPACK_IMPORTED_MODULE_15__["default"], {
              size: "sm",
              wrap: false,
              ellipsis: true,
              style: {
                minWidth: 0
              },
              children: file?.name
            })]
          }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_18__.jsx)(_components_text__WEBPACK_IMPORTED_MODULE_15__["default"], {
            color: "descgray",
            size: "xs",
            textTransform: "uppercase",
            align: "center",
            wrap: false,
            style: {
              flex: FLEX_VALUES[1]
            },
            children: file?.extension
          }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_18__.jsx)(_components_text__WEBPACK_IMPORTED_MODULE_15__["default"], {
            color: "descgray",
            size: "xs",
            align: "center",
            wrap: false,
            ellipsis: true,
            style: {
              flex: FLEX_VALUES[2]
            },
            children: (0,_utils_functions__WEBPACK_IMPORTED_MODULE_2__.formatFileSize)(file?.size || 0)
          }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_18__.jsx)(_FileLocations__WEBPACK_IMPORTED_MODULE_8__["default"], {
            FLEX_VALUES: FLEX_VALUES,
            location: file?.location
          }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_18__.jsx)(_components_text__WEBPACK_IMPORTED_MODULE_15__["default"], {
            color: "descgray",
            size: "xs",
            align: "center",
            wrap: false,
            ellipsis: true,
            style: {
              flex: FLEX_VALUES[4]
            },
            children: file?.createdAt
          }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_18__.jsx)(_components_inlineStack__WEBPACK_IMPORTED_MODULE_3__["default"], {
            align: "end",
            style: {
              flex: FLEX_VALUES[5]
            },
            children: enableTrash && menuKey !== "trash" ? /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_18__.jsx)(_components_button__WEBPACK_IMPORTED_MODULE_11__["default"], {
              variant: "error",
              size: "supersmall",
              startIcon: "recycling",
              statusProps: {
                isPro: true,
                size: "extrasmall",
                placement: "right-center",
                right: 1
              },
              onClick: e => {
                e.stopPropagation();
              },
              children: "Trash"
            }) : /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_18__.jsx)(_components_button__WEBPACK_IMPORTED_MODULE_11__["default"], {
              variant: "error",
              size: "supersmall",
              startIcon: "delete",
              onClick: e => {
                e.stopPropagation();
                openDeleteFile(ids);
              },
              children: "Delete"
            })
          })]
        }, file?.id ?? index);
      }), loadMore && /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_18__.jsx)(_components_skeletonLoader__WEBPACK_IMPORTED_MODULE_1__["default"].SkeletonFile, {
        variant: "list",
        length: query?.perPage
      })]
    })
  });
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (FileLists);

/***/ },

/***/ "./source/admin/files/components/FileLocations.tsx"
/*!*********************************************************!*\
  !*** ./source/admin/files/components/FileLocations.tsx ***!
  \*********************************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _components_inlineStack__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ~/components/inlineStack */ "./source/components/inlineStack/index.ts");
/* harmony import */ var _components_blockStack__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ~/components/blockStack */ "./source/components/blockStack/index.ts");
/* harmony import */ var _components_tooltip__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ~/components/tooltip */ "./source/components/tooltip/index.ts");
/* harmony import */ var _components_card__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ~/components/card */ "./source/components/card/index.ts");
/* harmony import */ var _components_text__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ~/components/text */ "./source/components/text/index.ts");
/* harmony import */ var _components_icon__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ~/components/icon */ "./source/components/icon/index.ts");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! react/jsx-runtime */ "react/jsx-runtime");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_6___default = /*#__PURE__*/__webpack_require__.n(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_6__);

const FileLocations = ({
  FLEX_VALUES,
  location
}) => {
  const component = /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_6__.jsx)(_components_blockStack__WEBPACK_IMPORTED_MODULE_1__["default"], {
    padding: 5,
    gap: 10,
    children: location?.map(({
      name,
      url
    }, index) => /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_6__.jsxs)(_components_inlineStack__WEBPACK_IMPORTED_MODULE_0__["default"], {
      gap: 10,
      wrap: false,
      children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_6__.jsxs)(_components_text__WEBPACK_IMPORTED_MODULE_4__["default"], {
        size: "sm",
        wrap: false,
        children: [index + 1, "."]
      }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_6__.jsx)(_components_text__WEBPACK_IMPORTED_MODULE_4__["default"], {
        size: "sm",
        wrap: false,
        children: name
      }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_6__.jsx)(_components_icon__WEBPACK_IMPORTED_MODULE_5__["default"], {
        name: "open_in_new",
        color: "primary",
        style: {
          cursor: "pointer"
        },
        onClick: () => window.open(url, "_blank")
      })]
    }, index))
  });
  const notFound = /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_6__.jsx)(_components_blockStack__WEBPACK_IMPORTED_MODULE_1__["default"], {
    padding: 5,
    children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_6__.jsx)(_components_text__WEBPACK_IMPORTED_MODULE_4__["default"], {
      size: "sm",
      wrap: false,
      children: "Locations not Found"
    })
  });
  return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_6__.jsx)(_components_blockStack__WEBPACK_IMPORTED_MODULE_1__["default"], {
    inlineAlign: "center",
    style: {
      flex: FLEX_VALUES[3]
    },
    children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_6__.jsx)(_components_tooltip__WEBPACK_IMPORTED_MODULE_2__["default"], {
      arrow: true,
      placement: "bottom",
      background: "white",
      shadow: true,
      style: {
        width: "fit-content"
      },
      component: location?.length > 0 ? component : notFound,
      children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_6__.jsx)(_components_card__WEBPACK_IMPORTED_MODULE_3__["default"], {
        padding: 5,
        rounded: "md",
        flex: true,
        align: "center",
        blockAlign: "center",
        style: {
          minWidth: "40px",
          width: "fit-content",
          height: "40px",
          aspectRatio: "1 / 1"
        },
        children: location?.length ?? 0
      })
    })
  });
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (FileLocations);

/***/ },

/***/ "./source/admin/files/components/FilesContainer.tsx"
/*!**********************************************************!*\
  !*** ./source/admin/files/components/FilesContainer.tsx ***!
  \**********************************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _redux_api_file__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ~/redux/api/file */ "./source/redux/api/file.ts");
/* harmony import */ var _wordpress_element__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! @wordpress/element */ "@wordpress/element");
/* harmony import */ var _wordpress_element__WEBPACK_IMPORTED_MODULE_1___default = /*#__PURE__*/__webpack_require__.n(_wordpress_element__WEBPACK_IMPORTED_MODULE_1__);
/* harmony import */ var _components_contextMenu_ContextMenu__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ~/components/contextMenu/ContextMenu */ "./source/components/contextMenu/ContextMenu.tsx");
/* harmony import */ var _redux_features_file_file__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ~/redux/features/file/file */ "./source/redux/features/file/file.ts");
/* harmony import */ var _hooks_useInfiniteScroll__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ../hooks/useInfiniteScroll */ "./source/admin/files/hooks/useInfiniteScroll.ts");
/* harmony import */ var _components_skeletonLoader__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ~/components/skeletonLoader */ "./source/components/skeletonLoader/index.ts");
/* harmony import */ var _hooks_useDragSelect__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! ../hooks/useDragSelect */ "./source/admin/files/hooks/useDragSelect.ts");
/* harmony import */ var _hooks_useFileContext__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! ../hooks/useFileContext */ "./source/admin/files/hooks/useFileContext.tsx");
/* harmony import */ var _components_inlineStack__WEBPACK_IMPORTED_MODULE_8__ = __webpack_require__(/*! ~/components/inlineStack */ "./source/components/inlineStack/index.ts");
/* harmony import */ var _constants_files__WEBPACK_IMPORTED_MODULE_9__ = __webpack_require__(/*! ~/constants/files */ "./source/constants/files.ts");
/* harmony import */ var _components_blockStack__WEBPACK_IMPORTED_MODULE_10__ = __webpack_require__(/*! ~/components/blockStack */ "./source/components/blockStack/index.ts");
/* harmony import */ var _redux_hooks__WEBPACK_IMPORTED_MODULE_11__ = __webpack_require__(/*! ~/redux/hooks */ "./source/redux/hooks.ts");
/* harmony import */ var _utils_icons__WEBPACK_IMPORTED_MODULE_12__ = __webpack_require__(/*! ~/utils/icons */ "./source/utils/icons.ts");
/* harmony import */ var _components_selectBox__WEBPACK_IMPORTED_MODULE_13__ = __webpack_require__(/*! ~/components/selectBox */ "./source/components/selectBox/index.ts");
/* harmony import */ var _components_gridStack__WEBPACK_IMPORTED_MODULE_14__ = __webpack_require__(/*! ~/components/gridStack */ "./source/components/gridStack/index.ts");
/* harmony import */ var react_router_dom__WEBPACK_IMPORTED_MODULE_15__ = __webpack_require__(/*! react-router-dom */ "./node_modules/react-router/dist/development/chunk-UVKPFVEO.mjs");
/* harmony import */ var _FileContext__WEBPACK_IMPORTED_MODULE_16__ = __webpack_require__(/*! ./FileContext */ "./source/admin/files/components/FileContext.tsx");
/* harmony import */ var _hooks_useFile__WEBPACK_IMPORTED_MODULE_17__ = __webpack_require__(/*! ../hooks/useFile */ "./source/admin/files/hooks/useFile.ts");
/* harmony import */ var _Pagination__WEBPACK_IMPORTED_MODULE_18__ = __webpack_require__(/*! ./Pagination */ "./source/admin/files/components/Pagination.tsx");
/* harmony import */ var _components_text__WEBPACK_IMPORTED_MODULE_19__ = __webpack_require__(/*! ~/components/text */ "./source/components/text/index.ts");
/* harmony import */ var _FileLists__WEBPACK_IMPORTED_MODULE_20__ = __webpack_require__(/*! ./FileLists */ "./source/admin/files/components/FileLists.tsx");
/* harmony import */ var _File__WEBPACK_IMPORTED_MODULE_21__ = __webpack_require__(/*! ./File */ "./source/admin/files/components/File.tsx");
/* harmony import */ var _DynamicFiles_premium_only__WEBPACK_IMPORTED_MODULE_22__ = __webpack_require__(/*! ./DynamicFiles__premium_only */ "./source/admin/files/components/DynamicFiles__premium_only.tsx");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_23__ = __webpack_require__(/*! react/jsx-runtime */ "react/jsx-runtime");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_23___default = /*#__PURE__*/__webpack_require__.n(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_23__);

const FilesContainer = () => {
  const {
    setFile,
    view,
    loadingType,
    files,
    selectedFiles,
    query,
    hasMore,
    bulkSelect
  } = (0,_hooks_useFile__WEBPACK_IMPORTED_MODULE_17__["default"])();
  const {
    menuKey,
    dynamicKey
  } = (0,react_router_dom__WEBPACK_IMPORTED_MODULE_15__.useParams)();
  const isFirstLoad = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_1__.useRef)(true);
  const dispatch = (0,_redux_hooks__WEBPACK_IMPORTED_MODULE_11__.useAppDispatch)();
  const finalQuery = {
    ...query,
    type: menuKey,
    extension: dynamicKey
  };
  (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_1__.useEffect)(() => {
    if (isFirstLoad.current && loadingType === "pagination") {
      isFirstLoad.current = false;
      return;
    }
    dispatch((0,_redux_features_file_file__WEBPACK_IMPORTED_MODULE_3__.setQuery)({
      page: 1,
      search: ""
    }));
    setFile("selectedFiles", []);
  }, [menuKey, dynamicKey, loadingType]);
  const {
    data: fetchedFiles,
    isLoading,
    isFetching
  } = (0,_redux_api_file__WEBPACK_IMPORTED_MODULE_0__.useGetFilesQuery)(finalQuery, {
    skip: !["all", "uncategorized"].includes(menuKey) && !pnpnm?.isPro
  });
  const handleFileContext = (0,_hooks_useFileContext__WEBPACK_IMPORTED_MODULE_7__["default"])();
  const containerRef = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_1__.useRef)(null);
  const {
    allFiles,
    uncategorized,
    dynamicFolders,
    favorites,
    used,
    unused,
    trash,
    files: responseFiles,
    page: currentPage,
    totalPages
  } = fetchedFiles?.data || {};
  const isFirstPage = query.page === 1;
  const isInfinite = loadingType === "infinite";
  const isPagination = loadingType === "pagination";
  (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_1__.useEffect)(() => {
    if (!responseFiles) return;
    if (isFirstPage || isPagination) {
      setFile("files", responseFiles);
    } else {
      dispatch((0,_redux_features_file_file__WEBPACK_IMPORTED_MODULE_3__.appendFiles)(responseFiles));
    }
    const dynamicCount = dynamicKey && dynamicFolders ? dynamicFolders[dynamicKey] ?? 0 : Object.keys(dynamicFolders ?? {}).length;
    setFile("count", {
      all: allFiles ?? 0,
      uncategorized: uncategorized ?? 0,
      dynamic: dynamicCount,
      favorites: favorites ?? 0,
      used: used ?? 0,
      unused: unused ?? 0,
      trash: trash ?? 0
    });
    setFile("dynamicFolders", dynamicFolders ?? {});
    setFile("totalPages", totalPages ?? 1);
    setFile("hasMore", query?.page < (totalPages ?? 1));
  }, [fetchedFiles]);
  (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_1__.useEffect)(() => {
    if (!isFetching) {
      setFile("loading", false);
    }
  }, [isFetching]);
  const handleLoadMore = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_1__.useCallback)(() => {
    dispatch((0,_redux_features_file_file__WEBPACK_IMPORTED_MODULE_3__.setQuery)({
      page: query.page + 1
    }));
  }, [query.page]);
  const {
    sentinelRef
  } = (0,_hooks_useInfiniteScroll__WEBPACK_IMPORTED_MODULE_4__.useInfiniteScroll)({
    hasMore,
    isFetching,
    onLoadMore: handleLoadMore,
    enabled: isInfinite
  });
  const handleDragSelect = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_1__.useCallback)(draggedFiles => {
    const existingIds = new Set(selectedFiles.map(f => String(f.id)));
    const merged = [...selectedFiles, ...draggedFiles.filter(f => !existingIds.has(String(f.id)))];
    setFile("selectedFiles", merged);
  }, [selectedFiles]);
  const {
    dragBox,
    handleMouseDown
  } = (0,_hooks_useDragSelect__WEBPACK_IMPORTED_MODULE_6__.useDragSelect)({
    containerRef,
    files,
    onSelect: handleDragSelect,
    isEnabled: bulkSelect
  });
  const showInitialLoading = (isLoading || isFetching) && (isFirstPage || isPagination);
  const showLoadMoreSkeleton = isFetching && !isFirstPage && !isPagination;

  return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_23__.jsxs)(_components_contextMenu_ContextMenu__WEBPACK_IMPORTED_MODULE_2__.MenuProvider, {
    children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_23__.jsxs)("div", {
      ref: containerRef,
      style: {
        position: "relative",
        marginTop: 20,
        userSelect: "none"
      },
      onMouseDown: handleMouseDown,
      children: [view === "grid" ? /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_23__.jsxs)(_components_gridStack__WEBPACK_IMPORTED_MODULE_14__["default"], {
        gap: 15,
        columns: "auto-fill",
        min: "180px",
        className: "flex-1",
        children: [showInitialLoading ? /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_23__.jsx)(_components_skeletonLoader__WEBPACK_IMPORTED_MODULE_5__["default"].SkeletonFile, {
          length: query?.perPage
        }) : files?.map(file => /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_23__.jsx)(_File__WEBPACK_IMPORTED_MODULE_21__["default"], {
          file: file
        }, file?.id)), showLoadMoreSkeleton && /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_23__.jsx)(_components_skeletonLoader__WEBPACK_IMPORTED_MODULE_5__["default"].SkeletonFile, {
          length: query?.perPage
        })]
      }) : /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_23__.jsx)(_FileLists__WEBPACK_IMPORTED_MODULE_20__["default"], {
        loading: showInitialLoading,
        loadMore: showLoadMoreSkeleton
      }), !isLoading && !isFetching && files.length === 0 && /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_23__.jsxs)(_components_blockStack__WEBPACK_IMPORTED_MODULE_10__["default"], {
        align: "center",
        inlineAlign: "center",
        gap: 10,
        style: {
          marginTop: "50px"
        },
        children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_23__.jsx)("img", {
          src: _utils_icons__WEBPACK_IMPORTED_MODULE_12__.iconNotFound,
          alt: ""
        }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_23__.jsx)(_components_text__WEBPACK_IMPORTED_MODULE_19__["default"], {
          size: "xl",
          weight: "semibold",
          align: "center",
          children: "No media found"
        }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_23__.jsx)(_components_text__WEBPACK_IMPORTED_MODULE_19__["default"], {
          size: "sm",
          align: "center",
          children: "Try adjusting your search or filter or refreshing to find what you're looking for."
        })]
      }), isInfinite && hasMore && /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_23__.jsx)("div", {
        ref: sentinelRef,
        style: {
          height: "20px",
          marginTop: "-20px"
        }
      }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_23__.jsx)(_FileContext__WEBPACK_IMPORTED_MODULE_16__["default"], {
        onMenuClick: handleFileContext
      }), dragBox && /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_23__.jsx)("div", {
        style: {
          left: dragBox.x,
          top: dragBox.y,
          width: dragBox.width,
          height: dragBox.height
        },
        className: "pnpnm-drag-box"
      })]
    }), isPagination && !isLoading && files.length > 0 && /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_23__.jsx)(_components_blockStack__WEBPACK_IMPORTED_MODULE_10__["default"], {
      marginTop: 20,
      children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_23__.jsxs)(_components_inlineStack__WEBPACK_IMPORTED_MODULE_8__["default"], {
        gap: 5,
        align: "center",
        children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_23__.jsx)(_Pagination__WEBPACK_IMPORTED_MODULE_18__["default"], {
          currentPage: currentPage || 1,
          totalPages: totalPages || 1,
          onPageChange: p => dispatch((0,_redux_features_file_file__WEBPACK_IMPORTED_MODULE_3__.setQuery)({
            page: p
          }))
        }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_23__.jsx)(_components_selectBox__WEBPACK_IMPORTED_MODULE_13__["default"], {
          placement: "top",
          options: _constants_files__WEBPACK_IMPORTED_MODULE_9__.PAGE_OPTIONS,
          value: [query?.perPage?.toString() || "80"],
          onChange: value => dispatch((0,_redux_features_file_file__WEBPACK_IMPORTED_MODULE_3__.setQuery)({
            perPage: Number(value[0])
          }))
        })]
      })
    })]
  });
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (FilesContainer);

/***/ },

/***/ "./source/admin/files/components/Header.tsx"
/*!**************************************************!*\
  !*** ./source/admin/files/components/Header.tsx ***!
  \**************************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var react_router_dom__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! react-router-dom */ "./node_modules/react-router/dist/development/chunk-UVKPFVEO.mjs");
/* harmony import */ var _shared_topbar_Sorting__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ~/shared/topbar/Sorting */ "./source/shared/topbar/Sorting.tsx");
/* harmony import */ var _redux_features_file_file__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ~/redux/features/file/file */ "./source/redux/features/file/file.ts");
/* harmony import */ var _hooks_useFileActions__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ../hooks/useFileActions */ "./source/admin/files/hooks/useFileActions.ts");
/* harmony import */ var _components_inlineStack__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ~/components/inlineStack */ "./source/components/inlineStack/index.ts");
/* harmony import */ var _redux_hooks__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ~/redux/hooks */ "./source/redux/hooks.ts");
/* harmony import */ var _components_selectBox__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! ~/components/selectBox */ "./source/components/selectBox/index.ts");
/* harmony import */ var _wordpress_i18n__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! @wordpress/i18n */ "@wordpress/i18n");
/* harmony import */ var _wordpress_i18n__WEBPACK_IMPORTED_MODULE_7___default = /*#__PURE__*/__webpack_require__.n(_wordpress_i18n__WEBPACK_IMPORTED_MODULE_7__);
/* harmony import */ var _hooks_useSettings__WEBPACK_IMPORTED_MODULE_8__ = __webpack_require__(/*! ~/hooks/useSettings */ "./source/hooks/useSettings.ts");
/* harmony import */ var _components_checkbox__WEBPACK_IMPORTED_MODULE_9__ = __webpack_require__(/*! ~/components/checkbox */ "./source/components/checkbox/index.ts");
/* harmony import */ var _Delete__WEBPACK_IMPORTED_MODULE_10__ = __webpack_require__(/*! ./Delete */ "./source/admin/files/components/Delete.tsx");
/* harmony import */ var _components_button__WEBPACK_IMPORTED_MODULE_11__ = __webpack_require__(/*! ~/components/button */ "./source/components/button/index.ts");
/* harmony import */ var _hooks_useFile__WEBPACK_IMPORTED_MODULE_12__ = __webpack_require__(/*! ../hooks/useFile */ "./source/admin/files/hooks/useFile.ts");
/* harmony import */ var _components_text__WEBPACK_IMPORTED_MODULE_13__ = __webpack_require__(/*! ~/components/text */ "./source/components/text/index.ts");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_14__ = __webpack_require__(/*! react/jsx-runtime */ "react/jsx-runtime");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_14___default = /*#__PURE__*/__webpack_require__.n(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_14__);

const Header = () => {
  const {
    data
  } = (0,_hooks_useSettings__WEBPACK_IMPORTED_MODULE_8__["default"])();
  const {
    setFile,
    view,
    loadingType,
    files,
    selectedFiles,
    query,
    bulkSelect
  } = (0,_hooks_useFile__WEBPACK_IMPORTED_MODULE_12__["default"])();
  const {
    menuKey,
    dynamicKey
  } = (0,react_router_dom__WEBPACK_IMPORTED_MODULE_0__.useParams)();
  const {
    restoreFile,
  } = (0,_hooks_useFileActions__WEBPACK_IMPORTED_MODULE_3__["default"])();
  const {
    openDeleteFile
  } = (0,_Delete__WEBPACK_IMPORTED_MODULE_10__.useDeleteFile)();
  const navigate = (0,react_router_dom__WEBPACK_IMPORTED_MODULE_0__.useNavigate)();
  const dispatch = (0,_redux_hooks__WEBPACK_IMPORTED_MODULE_5__.useAppDispatch)();
  const isAllSelected = selectedFiles?.length === files?.length;
  const handleBulkSelect = () => {
    if (bulkSelect) {
      setFile("selectedFiles", []);
    }
    setFile("bulkSelect", !bulkSelect);
  };
  const handleAllSelect = () => {
    if (selectedFiles?.length === files?.length) {
      setFile("selectedFiles", []);
    } else {
      setFile("selectedFiles", files);
    }
  };
  const handleReSelect = () => {
    navigate(`/files/dynamic`);
  };
  const handleView = () => {
    setFile("view", view === "grid" ? "list" : "grid");
  };
  const handleLoadingType = () => {
    setFile("loadingType", loadingType === "infinite" ? "pagination" : "infinite");
    if (loadingType === "infinite") {
      dispatch((0,_redux_features_file_file__WEBPACK_IMPORTED_MODULE_2__.setQuery)({
        page: 1
      }));
    }
  };
  const handleQuery = q => {
    dispatch((0,_redux_features_file_file__WEBPACK_IMPORTED_MODULE_2__.setQuery)(q));
  };
  const handleTrash = () => {
  };
  const handleRestore = () => {
    const ids = selectedFiles?.map(f => f?.id) ?? [];
    restoreFile(ids);
  };
  const handleDelete = () => {
    const ids = selectedFiles?.map(f => f?.id) ?? [];
    openDeleteFile(ids);
  };
  const enableTrash = data?.general?.files?.moveToTrash ?? false;
  const showTrash = selectedFiles?.length > 0 && enableTrash && menuKey !== "trash" && (menuKey !== "dynamic" || dynamicKey);
  const showRestore = menuKey === "trash" && selectedFiles?.length > 0;
  const showDelete = selectedFiles?.length > 0 && (menuKey === "trash" ? true : !enableTrash) && (menuKey !== "dynamic" || dynamicKey);
  return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_14__.jsxs)(_components_inlineStack__WEBPACK_IMPORTED_MODULE_4__["default"], {
    align: "between",
    gap: 10,
    children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_14__.jsxs)(_components_inlineStack__WEBPACK_IMPORTED_MODULE_4__["default"], {
      gap: 10,
      children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_14__.jsx)(_components_selectBox__WEBPACK_IMPORTED_MODULE_6__["default"], {
        prefix: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_14__.jsx)(_components_text__WEBPACK_IMPORTED_MODULE_13__["default"], {
          size: "sm",
          children: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_7__.__)("Sort by:", "ninja-media")
        }),
        options: _shared_topbar_Sorting__WEBPACK_IMPORTED_MODULE_1__.FOLDER_OPTIONS,
        value: [query?.orderBy],
        onChange: value => handleQuery({
          orderBy: value[0]
        })
      }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_14__.jsx)(_components_button__WEBPACK_IMPORTED_MODULE_11__["default"], {
        variant: "outlined",
        startIcon: query?.order === "ASC" ? "arrow_upward" : "arrow_downward",
        onClick: () => handleQuery({
          order: query?.order === "ASC" ? "DESC" : "ASC"
        }),
        children: query?.order === "ASC" ? (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_7__.__)("Ascending", "ninja-media") : (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_7__.__)("Descending", "ninja-media")
      }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_14__.jsx)(_components_button__WEBPACK_IMPORTED_MODULE_11__["default"], {
        variant: "outlined",
        startIcon: loadingType === "infinite" ? "page_control" : "arrow_circle_down",
        onClick: handleLoadingType,
        children: loadingType === "infinite" ? (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_7__.__)("Pagination", "ninja-media") : (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_7__.__)("Infinite Scroll", "ninja-media")
      }), showTrash && /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_14__.jsx)(_components_button__WEBPACK_IMPORTED_MODULE_11__["default"], {
        variant: "error",
        startIcon: "recycling",
        onClick: handleTrash,
        children: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_7__.__)("Trash", "ninja-media")
      }), showRestore && /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_14__.jsx)(_components_button__WEBPACK_IMPORTED_MODULE_11__["default"], {
        variant: "primary",
        startIcon: "restore",
        onClick: handleRestore,
        children: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_7__.__)("Restore", "ninja-media")
      }), showDelete && /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_14__.jsx)(_components_button__WEBPACK_IMPORTED_MODULE_11__["default"], {
        variant: "error",
        startIcon: "delete",
        onClick: handleDelete,
        children: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_7__.__)("Delete", "ninja-media")
      }), menuKey === "dynamic" && dynamicKey && /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_14__.jsx)(_components_button__WEBPACK_IMPORTED_MODULE_11__["default"], {
        variant: "primary",
        startIcon: "left_click",
        onClick: handleReSelect,
        children: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_7__.__)("Re Select", "ninja-media")
      })]
    }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_14__.jsxs)(_components_inlineStack__WEBPACK_IMPORTED_MODULE_4__["default"], {
      gap: 10,
      children: [bulkSelect && /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_14__.jsxs)(_components_button__WEBPACK_IMPORTED_MODULE_11__["default"], {
        variant: "outlined",
        onClick: handleAllSelect,
        children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_14__.jsx)(_components_checkbox__WEBPACK_IMPORTED_MODULE_9__["default"], {
          size: "small",
          rounded: "sm",
          style: {
            marginRight: 4
          },
          checked: isAllSelected,
          onChange: handleAllSelect
        }), isAllSelected ? (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_7__.__)("Deselect All", "ninja-media") : (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_7__.__)("Select All", "ninja-media")]
      }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_14__.jsxs)(_components_button__WEBPACK_IMPORTED_MODULE_11__["default"], {
        variant: "outlined",
        onClick: handleBulkSelect,
        children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_14__.jsx)(_components_checkbox__WEBPACK_IMPORTED_MODULE_9__["default"], {
          size: "small",
          rounded: "sm",
          style: {
            marginRight: 4
          },
          checked: bulkSelect,
          onChange: handleBulkSelect
        }), bulkSelect ? (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_7__.sprintf)((0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_7__.__)("Selected %d", "ninja-media"), selectedFiles?.length) : (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_7__.__)("Bulk Select", "ninja-media")]
      }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_14__.jsx)(_components_button__WEBPACK_IMPORTED_MODULE_11__["default"], {
        variant: "outlined",
        startIcon: view === "grid" ? "dehaze" : "grid_view",
        onClick: handleView,
        children: view === "grid" ? (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_7__.__)("List view", "ninja-media") : (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_7__.__)("Grid view", "ninja-media")
      })]
    })]
  });
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (Header);

/***/ },

/***/ "./source/admin/files/components/Pagination.tsx"
/*!******************************************************!*\
  !*** ./source/admin/files/components/Pagination.tsx ***!
  \******************************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _components_inlineStack__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ~/components/inlineStack */ "./source/components/inlineStack/index.ts");
/* harmony import */ var _components_iconButton__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ~/components/iconButton */ "./source/components/iconButton/index.ts");
/* harmony import */ var _components_dropdown__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ~/components/dropdown */ "./source/components/dropdown/index.ts");
/* harmony import */ var _components_button__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ~/components/button */ "./source/components/button/index.ts");
/* harmony import */ var _components_icon__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ~/components/icon */ "./source/components/icon/index.ts");
/* harmony import */ var _components_card__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ~/components/card */ "./source/components/card/index.ts");
/* harmony import */ var _wordpress_i18n__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! @wordpress/i18n */ "@wordpress/i18n");
/* harmony import */ var _wordpress_i18n__WEBPACK_IMPORTED_MODULE_6___default = /*#__PURE__*/__webpack_require__.n(_wordpress_i18n__WEBPACK_IMPORTED_MODULE_6__);
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! react/jsx-runtime */ "react/jsx-runtime");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_7___default = /*#__PURE__*/__webpack_require__.n(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_7__);

const MAX_VISIBLE = 4;
const Pagination = ({
  variant = "large",
  currentPage,
  totalPages,
  onPageChange,
  className = ""
}) => {
  if (totalPages <= 1) return null;
  const DOTS = "...";
  const getPaginationRange = (current, total) => {
    if (total <= MAX_VISIBLE) {
      return Array.from({
        length: total
      }, (_, i) => i + 1);
    }
    let start = current - 1;
    let end = current + 2;
    if (start < 1) {
      start = 1;
      end = MAX_VISIBLE;
    }
    if (end > total) {
      end = total;
      start = total - MAX_VISIBLE + 1;
    }
    const pages = Array.from({
      length: end - start + 1
    }, (_, i) => start + i);
    const result = [];
    if (pages[0] > 1) {
      result.push(DOTS);
    }
    result.push(...pages);
    if (pages[pages.length - 1] < total) {
      result.push(DOTS);
    }
    return result;
  };
  const range = getPaginationRange(currentPage, totalPages);
  const visibleNumbers = range.filter(r => r !== DOTS);
  const hiddenPages = Array.from({
    length: totalPages
  }, (_, i) => i + 1).filter(p => !visibleNumbers.includes(p));
  const firstVisible = visibleNumbers[0];
  const leftHidden = hiddenPages.filter(p => p < firstVisible);
  const lastVisible = visibleNumbers[visibleNumbers.length - 1];
  const rightHidden = hiddenPages.filter(p => p > lastVisible);
  return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_7__.jsxs)(_components_inlineStack__WEBPACK_IMPORTED_MODULE_0__["default"], {
    gap: 5,
    align: "center",
    blockAlign: "center",
    className: className,
    children: [variant === "large" ? /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_7__.jsx)(_components_button__WEBPACK_IMPORTED_MODULE_3__["default"], {
      variant: "outlined",
      disabled: currentPage === 1,
      onClick: () => onPageChange(currentPage - 1),
      children: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_6__.__)("Prev", "ninja-media")
    }) : /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_7__.jsx)(_components_iconButton__WEBPACK_IMPORTED_MODULE_1__["default"], {
      variant: "outlined",
      size: "small",
      name: "chevron_backward",
      disabled: currentPage === 1,
      onClick: () => onPageChange(currentPage - 1)
    }), range.map((page, index) => page === DOTS ? /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_7__.jsxs)(_components_dropdown__WEBPACK_IMPORTED_MODULE_2__["default"], {
      children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_7__.jsx)(_components_dropdown__WEBPACK_IMPORTED_MODULE_2__["default"].Trigger, {
        children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_7__.jsx)(_components_icon__WEBPACK_IMPORTED_MODULE_4__["default"], {
          name: "more_horiz",
          fontSize: "xl",
          fontWeight: "semibold",
          style: {
            cursor: "pointer",
            userSelect: "none"
          }
        })
      }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_7__.jsx)(_components_dropdown__WEBPACK_IMPORTED_MODULE_2__["default"].Content, {
        position: {
          top: "170%",
          left: 0
        },
        style: {
          display: "flex",
          flexDirection: "column",
          gap: "4px",
          maxHeight: "300px",
          overflowY: "auto",
          scrollbarWidth: "none"
        },
        children: (index === 0 ? leftHidden : rightHidden).map(p => /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_7__.jsx)(_components_card__WEBPACK_IMPORTED_MODULE_5__["default"], {
          padding: 5,
          rounded: "sm",
          flex: true,
          align: "center",
          blockAlign: "center",
          background: p === currentPage ? "primary" : "extralight",
          style: {
            cursor: "pointer",
            minWidth: "32px",
            height: "32px"
          },
          className: p === currentPage ? "text-white" : "text-black",
          onClick: () => onPageChange(p),
          children: p
        }, p))
      })]
    }, `dots-${index}`) : /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_7__.jsx)(_components_button__WEBPACK_IMPORTED_MODULE_3__["default"], {
      variant: page === currentPage ? "primary" : "outlined",
      size: variant === "small" ? "small" : "medium",
      onClick: () => onPageChange(Number(page)),
      children: page
    }, page)), variant === "large" ? /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_7__.jsx)(_components_button__WEBPACK_IMPORTED_MODULE_3__["default"], {
      variant: "outlined",
      disabled: currentPage === totalPages,
      onClick: () => onPageChange(currentPage + 1),
      children: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_6__.__)("Next", "ninja-media")
    }) : /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_7__.jsx)(_components_iconButton__WEBPACK_IMPORTED_MODULE_1__["default"], {
      variant: "outlined",
      size: "small",
      name: "chevron_forward",
      disabled: currentPage === totalPages,
      onClick: () => onPageChange(currentPage + 1)
    })]
  });
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (Pagination);

/***/ },

/***/ "./source/admin/files/components/Search.tsx"
/*!**************************************************!*\
  !*** ./source/admin/files/components/Search.tsx ***!
  \**************************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _redux_features_file_file__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ~/redux/features/file/file */ "./source/redux/features/file/file.ts");
/* harmony import */ var _redux_hooks__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ~/redux/hooks */ "./source/redux/hooks.ts");
/* harmony import */ var _hooks_useDebounce__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ~/hooks/useDebounce */ "./source/hooks/useDebounce.ts");
/* harmony import */ var _wordpress_element__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! @wordpress/element */ "@wordpress/element");
/* harmony import */ var _wordpress_element__WEBPACK_IMPORTED_MODULE_3___default = /*#__PURE__*/__webpack_require__.n(_wordpress_element__WEBPACK_IMPORTED_MODULE_3__);
/* harmony import */ var _components_input__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ~/components/input */ "./source/components/input/index.ts");
/* harmony import */ var _hooks_useFile__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ../hooks/useFile */ "./source/admin/files/hooks/useFile.ts");
/* harmony import */ var _wordpress_i18n__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! @wordpress/i18n */ "@wordpress/i18n");
/* harmony import */ var _wordpress_i18n__WEBPACK_IMPORTED_MODULE_6___default = /*#__PURE__*/__webpack_require__.n(_wordpress_i18n__WEBPACK_IMPORTED_MODULE_6__);
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! react/jsx-runtime */ "react/jsx-runtime");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_7___default = /*#__PURE__*/__webpack_require__.n(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_7__);

const Search = () => {
  const {
    query
  } = (0,_hooks_useFile__WEBPACK_IMPORTED_MODULE_5__["default"])();
  const [searchTerm, setSearchTerm] = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_3__.useState)(query?.search || "");
  const dispatch = (0,_redux_hooks__WEBPACK_IMPORTED_MODULE_1__.useAppDispatch)();
  (0,_hooks_useDebounce__WEBPACK_IMPORTED_MODULE_2__["default"])(() => {
    if (!searchTerm || searchTerm.trim() === "" || searchTerm.length < 2) {
      if (query?.search) dispatch((0,_redux_features_file_file__WEBPACK_IMPORTED_MODULE_0__.setQuery)({
        search: ""
      }));
      return;
    }
    dispatch((0,_redux_features_file_file__WEBPACK_IMPORTED_MODULE_0__.setQuery)({
      search: String(searchTerm)
    }));
  }, [searchTerm], 300);
  return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_7__.jsx)(_components_input__WEBPACK_IMPORTED_MODULE_4__["default"], {
    searchIcon: true,
    placeholder: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_6__.__)("Search media...", "ninja-media"),
    value: searchTerm,
    onChange: value => setSearchTerm(value)
  });
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (Search);

/***/ },

/***/ "./source/admin/files/hooks/useDragSelect.ts"
/*!***************************************************!*\
  !*** ./source/admin/files/hooks/useDragSelect.ts ***!
  \***************************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   useDragSelect: () => (/* binding */ useDragSelect)
/* harmony export */ });
/* harmony import */ var _wordpress_element__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @wordpress/element */ "@wordpress/element");
/* harmony import */ var _wordpress_element__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(_wordpress_element__WEBPACK_IMPORTED_MODULE_0__);

const useDragSelect = ({
  containerRef,
  files,
  onSelect,
  isEnabled
}) => {
  const [dragBox, setDragBox] = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useState)(null);
  const startPoint = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useRef)(null);
  const dragBoxRef = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useRef)(null);
  const handleMouseDown = e => {
    if (!isEnabled || !containerRef.current) return;
    const target = e.target;
    if (target.closest("button, input, a, label")) return;
    if (e.button !== 0) return;
    e.preventDefault();
    const rect = containerRef.current.getBoundingClientRect();
    startPoint.current = {
      x: e.clientX - rect.left,
      y: e.clientY - rect.top
    };
    const box = {
      x: startPoint.current.x,
      y: startPoint.current.y,
      width: 0,
      height: 0
    };
    dragBoxRef.current = box;
    setDragBox(box);
  };
  const handleMouseMove = e => {
    if (!startPoint.current || !containerRef.current) return;
    const rect = containerRef.current.getBoundingClientRect();
    const currentX = Math.min(Math.max(e.clientX - rect.left, 0), rect.width);
    const currentY = Math.min(Math.max(e.clientY - rect.top, 0), rect.height);
    const box = {
      x: Math.min(startPoint.current.x, currentX),
      y: Math.min(startPoint.current.y, currentY),
      width: Math.abs(currentX - startPoint.current.x),
      height: Math.abs(currentY - startPoint.current.y)
    };
    dragBoxRef.current = box;
    setDragBox(box);
  };
  const handleMouseUp = e => {
    if (!startPoint.current || !containerRef.current) {
      setDragBox(null);
      dragBoxRef.current = null;
      return;
    }
    const rect = containerRef.current.getBoundingClientRect();
    const currentX = Math.min(Math.max(e.clientX - rect.left, 0), rect.width);
    const currentY = Math.min(Math.max(e.clientY - rect.top, 0), rect.height);
    const dragDistance = Math.sqrt((currentX - startPoint.current.x) ** 2 + (currentY - startPoint.current.y) ** 2);
    if (dragDistance < 5) {
      setDragBox(null);
      dragBoxRef.current = null;
      startPoint.current = null;
      return;
    }
    const currentDragBox = dragBoxRef.current;
    const selected = [];
    files.forEach(file => {
      const el = document.querySelector(`[data-file-id="${file.id}"]`);
      if (!el || !currentDragBox) return;
      const elRect = el.getBoundingClientRect();
      const overlap = currentDragBox.x + rect.left < elRect.right && currentDragBox.x + currentDragBox.width + rect.left > elRect.left && currentDragBox.y + rect.top < elRect.bottom && currentDragBox.y + currentDragBox.height + rect.top > elRect.top;
      if (overlap) selected.push(file);
    });
    onSelect(selected);
    startPoint.current = null;
    dragBoxRef.current = null;
    setDragBox(null);
  };
  (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useEffect)(() => {
    if (!isEnabled) {
      setDragBox(null);
      dragBoxRef.current = null;
      startPoint.current = null;
      return;
    }
    window.addEventListener("mousemove", handleMouseMove);
    window.addEventListener("mouseup", handleMouseUp);
    return () => {
      window.removeEventListener("mousemove", handleMouseMove);
      window.removeEventListener("mouseup", handleMouseUp);
    };
  }, [isEnabled, files, onSelect]);
  return {
    dragBox,
    handleMouseDown
  };
};

/***/ },

/***/ "./source/admin/files/hooks/useFile.ts"
/*!*********************************************!*\
  !*** ./source/admin/files/hooks/useFile.ts ***!
  \*********************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _redux_features_file_file__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ~/redux/features/file/file */ "./source/redux/features/file/file.ts");
/* harmony import */ var _redux_hooks__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ~/redux/hooks */ "./source/redux/hooks.ts");
/* harmony import */ var _wordpress_element__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! @wordpress/element */ "@wordpress/element");
/* harmony import */ var _wordpress_element__WEBPACK_IMPORTED_MODULE_2___default = /*#__PURE__*/__webpack_require__.n(_wordpress_element__WEBPACK_IMPORTED_MODULE_2__);

const useFile = () => {
  const file = (0,_redux_hooks__WEBPACK_IMPORTED_MODULE_1__.useAppSelector)(_redux_features_file_file__WEBPACK_IMPORTED_MODULE_0__.selectFile);
  const dispatch = (0,_redux_hooks__WEBPACK_IMPORTED_MODULE_1__.useAppDispatch)();
  const setFile = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_2__.useCallback)((path, value) => {
    dispatch((0,_redux_features_file_file__WEBPACK_IMPORTED_MODULE_0__.updateFileState)({
      path,
      value
    }));
  }, []);
  return {
    ...file,
    setFile
  };
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (useFile);

/***/ },

/***/ "./source/admin/files/hooks/useFileActions.ts"
/*!****************************************************!*\
  !*** ./source/admin/files/hooks/useFileActions.ts ***!
  \****************************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _redux_functions_updateFilesCaches__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ~/redux/functions/updateFilesCaches */ "./source/redux/functions/updateFilesCaches.ts");
/* harmony import */ var _components_alert_Alert__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ~/components/alert/Alert */ "./source/components/alert/Alert.tsx");
/* harmony import */ var _redux_hooks__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ~/redux/hooks */ "./source/redux/hooks.ts");
/* harmony import */ var _wordpress_i18n__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! @wordpress/i18n */ "@wordpress/i18n");
/* harmony import */ var _wordpress_i18n__WEBPACK_IMPORTED_MODULE_3___default = /*#__PURE__*/__webpack_require__.n(_wordpress_i18n__WEBPACK_IMPORTED_MODULE_3__);
/* harmony import */ var react_router_dom__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! react-router-dom */ "./node_modules/react-router/dist/development/chunk-UVKPFVEO.mjs");
/* harmony import */ var _useFile__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ./useFile */ "./source/admin/files/hooks/useFile.ts");
/* harmony import */ var _redux_api_media__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! ~/redux/api/media */ "./source/redux/api/media.ts");
/* harmony import */ var _redux_features_file_file__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! ~/redux/features/file/file */ "./source/redux/features/file/file.ts");
/* harmony import */ var _redux_api_watermark_premium_only__WEBPACK_IMPORTED_MODULE_8__ = __webpack_require__(/*! ~/redux/api/watermark__premium_only */ "./source/redux/api/watermark__premium_only.ts");

const useFileActions = () => {
  const {
    setFile,
    count,
    bulkSelect
  } = (0,_useFile__WEBPACK_IMPORTED_MODULE_5__["default"])();
  const [restoreFileMutation] = (0,_redux_api_media__WEBPACK_IMPORTED_MODULE_6__.useRestoreFileMutation)();

  const {
    dynamicKey
  } = (0,react_router_dom__WEBPACK_IMPORTED_MODULE_4__.useParams)();
  const dispatch = (0,_redux_hooks__WEBPACK_IMPORTED_MODULE_2__.useAppDispatch)();
  const {
    showAlert
  } = (0,_components_alert_Alert__WEBPACK_IMPORTED_MODULE_1__.useCustomAlert)();
  const resetState = () => {
    if (bulkSelect) {
      setFile("selectedFiles", []);
      setFile("bulkSelect", false);
    }
  };
  const getFileLink = file => {
    const url = file?.url;
    if (!url) return;
    navigator.clipboard.writeText(url);
    showAlert({
      toast: true,
      type: "success",
      text: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_3__.__)("File URL copied to clipboard", "ninja-media"),
      timer: 3000,
      timerProgressBar: true,
      showConfirmButton: false
    });
  };
  const restoreFile = async ids => {
    showAlert({
      type: "info",
      title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_3__.__)("Confirm Restore", "ninja-media"),
      text: ids.length > 1 ? (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_3__.__)("Are you sure you want to restore these files from trash?", "ninja-media") : (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_3__.__)("Are you sure you want to restore this file from trash?", "ninja-media"),
      showCancelButton: true,
      confirmButtonText: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_3__.__)("Restore", "ninja-media"),
      onConfirm: async () => {
        try {
          const response = await restoreFileMutation({
            ids
          }).unwrap();
          if (bulkSelect) {
            resetState();
          }
          const {
            restored,
            allFiles,
            uncategorized,
            dynamicFolders,
            unused,
            trash
          } = response?.data || {};
          dispatch((0,_redux_features_file_file__WEBPACK_IMPORTED_MODULE_7__.deleteFile)(restored ?? []));
          const dynamicCount = dynamicKey && dynamicFolders ? dynamicFolders[dynamicKey] ?? 0 : Object.keys(dynamicFolders ?? {}).length;
          setFile("count", {
            ...count,
            all: allFiles ?? 0,
            uncategorized: uncategorized ?? 0,
            dynamic: dynamicCount,
            unused: unused ?? 0,
            trash: trash ?? 0
          });
          (0,_redux_functions_updateFilesCaches__WEBPACK_IMPORTED_MODULE_0__["default"])(restored ?? [], {
            allFiles,
            uncategorized,
            unused,
            trash,
            dynamicFolders
          });
          showAlert({
            toast: true,
            type: "success",
            text: response?.message || (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_3__.sprintf)((0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_3__.__)("%d file(s) restored successfully", "ninja-media"), ids.length),
            timer: 3000,
            timerProgressBar: true,
            showConfirmButton: false
          });
        } catch (error) {
          showAlert({
            toast: true,
            type: "error",
            text: error?.data?.message || (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_3__.__)("Failed to restore files", "ninja-media"),
            timer: 3000,
            timerProgressBar: true,
            showConfirmButton: false
          });
        }
      }
    });
  };

  return {
    getFileLink,
    restoreFile,
  };
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (useFileActions);

/***/ },

/***/ "./source/admin/files/hooks/useFileContext.tsx"
/*!*****************************************************!*\
  !*** ./source/admin/files/hooks/useFileContext.tsx ***!
  \*****************************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _components_FileDetails__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../components/FileDetails */ "./source/admin/files/components/FileDetails.tsx");
/* harmony import */ var _components_alert_Alert__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ~/components/alert/Alert */ "./source/components/alert/Alert.tsx");
/* harmony import */ var _components_Delete__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../components/Delete */ "./source/admin/files/components/Delete.tsx");
/* harmony import */ var _useFileActions__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ./useFileActions */ "./source/admin/files/hooks/useFileActions.ts");
/* harmony import */ var _useFile__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ./useFile */ "./source/admin/files/hooks/useFile.ts");
/* harmony import */ var _redux_hooks__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ~/redux/hooks */ "./source/redux/hooks.ts");
/* harmony import */ var _redux_features_file_file__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! ~/redux/features/file/file */ "./source/redux/features/file/file.ts");
/* harmony import */ var _shared_hooks_useReplaceMedia_premium_only__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! ~/shared/hooks/useReplaceMedia__premium_only */ "./source/shared/hooks/useReplaceMedia__premium_only.ts");

const useFileContext = () => {
  const {
    setFile,
    detailsFile
  } = (0,_useFile__WEBPACK_IMPORTED_MODULE_4__["default"])();
  const dispatch = (0,_redux_hooks__WEBPACK_IMPORTED_MODULE_5__.useAppDispatch)();
  const {
    getFileLink,
    restoreFile,
  } = (0,_useFileActions__WEBPACK_IMPORTED_MODULE_3__["default"])();
  const {
    openViewDetails
  } = (0,_components_FileDetails__WEBPACK_IMPORTED_MODULE_0__.useViewDetails)();
  const {
    showAlert
  } = (0,_components_alert_Alert__WEBPACK_IMPORTED_MODULE_1__.useCustomAlert)();

  const {
    openDeleteFile
  } = (0,_components_Delete__WEBPACK_IMPORTED_MODULE_2__.useDeleteFile)();
  const handleFileContext = ({
    key,
    files
  }) => {
    const ids = files?.map(f => f?.id) ?? [];
    switch (key) {
      case "open":
        window.open(`${pnpnm?.siteUrl}/wp-admin/upload.php?item=${ids[0]}`, "_blank");
        return;
      case "view":
        setFile("detailsFile", files[0]);
        openViewDetails();
        return;
      case "get":
        getFileLink(files[0]);
        return;
      case "edit":
        window.open(`${pnpnm?.siteUrl}/wp-admin/post.php?post=${ids[0]}&action=edit&open_pnpnm_editor=${ids[0]}`, "_blank");
        return;
      case "download":
        return;
      case "duplicate":
        return;
      case "replace":
        return;
      case "favorite":
        return;
      case "unfavorite":
        return;
      case "apply":
        return;
      case "remove":
        return;
      case "trash":
        return;
      case "restore":
        restoreFile(ids);
        return;
      case "delete":
        openDeleteFile(ids);
        return;
      default:
        {
          const exhaustiveCheck = key;
          return exhaustiveCheck;
        }
    }
  };
  return handleFileContext;
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (useFileContext);

/***/ },

/***/ "./source/admin/files/hooks/useInfiniteScroll.ts"
/*!*******************************************************!*\
  !*** ./source/admin/files/hooks/useInfiniteScroll.ts ***!
  \*******************************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   useInfiniteScroll: () => (/* binding */ useInfiniteScroll)
/* harmony export */ });
/* harmony import */ var _wordpress_element__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @wordpress/element */ "@wordpress/element");
/* harmony import */ var _wordpress_element__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(_wordpress_element__WEBPACK_IMPORTED_MODULE_0__);

const useInfiniteScroll = ({
  hasMore,
  isFetching,
  onLoadMore,
  enabled = true
}) => {
  const sentinelRef = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useRef)(null);
  const observerRef = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useRef)(null);
  (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useEffect)(() => {
    if (!enabled) return;
    if (observerRef.current) observerRef.current.disconnect();
    observerRef.current = new IntersectionObserver(entries => {
      if (entries[0].isIntersecting && hasMore && !isFetching) {
        onLoadMore();
      }
    }, {
      threshold: 0.1
    });
    if (sentinelRef.current) {
      observerRef.current.observe(sentinelRef.current);
    }
    return () => observerRef.current?.disconnect();
  }, [hasMore, isFetching, enabled, onLoadMore]);
  return {
    sentinelRef
  };
};

/***/ },

/***/ "./source/admin/settings/Settings.tsx"
/*!********************************************!*\
  !*** ./source/admin/settings/Settings.tsx ***!
  \********************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _components_status_PremiumCard__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ~/components/status/PremiumCard */ "./source/components/status/PremiumCard.tsx");
/* harmony import */ var _hooks_useLocalStorage__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ~/hooks/useLocalStorage */ "./source/hooks/useLocalStorage.ts");
/* harmony import */ var react_router_dom__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! react-router-dom */ "./node_modules/react-router/dist/development/chunk-UVKPFVEO.mjs");
/* harmony import */ var _constants_menus__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ~/constants/menus */ "./source/constants/menus.ts");
/* harmony import */ var _components_toggleTheme__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ~/components/toggleTheme */ "./source/components/toggleTheme/index.ts");
/* harmony import */ var _components_inlineStack__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ~/components/inlineStack */ "./source/components/inlineStack/index.ts");
/* harmony import */ var _pages_advanced_Advanced__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! ./pages/advanced/Advanced */ "./source/admin/settings/pages/advanced/Advanced.tsx");
/* harmony import */ var _components_iconButton__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! ~/components/iconButton */ "./source/components/iconButton/index.ts");
/* harmony import */ var _components_blockStack__WEBPACK_IMPORTED_MODULE_8__ = __webpack_require__(/*! ~/components/blockStack */ "./source/components/blockStack/index.ts");
/* harmony import */ var _wordpress_element__WEBPACK_IMPORTED_MODULE_9__ = __webpack_require__(/*! @wordpress/element */ "@wordpress/element");
/* harmony import */ var _wordpress_element__WEBPACK_IMPORTED_MODULE_9___default = /*#__PURE__*/__webpack_require__.n(_wordpress_element__WEBPACK_IMPORTED_MODULE_9__);
/* harmony import */ var _pages_display_Display__WEBPACK_IMPORTED_MODULE_10__ = __webpack_require__(/*! ./pages/display/Display */ "./source/admin/settings/pages/display/Display.tsx");
/* harmony import */ var _pages_general_General__WEBPACK_IMPORTED_MODULE_11__ = __webpack_require__(/*! ./pages/general/General */ "./source/admin/settings/pages/general/General.tsx");
/* harmony import */ var _hooks_useSettings__WEBPACK_IMPORTED_MODULE_12__ = __webpack_require__(/*! ~/hooks/useSettings */ "./source/hooks/useSettings.ts");
/* harmony import */ var _components_sidebar__WEBPACK_IMPORTED_MODULE_13__ = __webpack_require__(/*! ~/components/sidebar */ "./source/components/sidebar/index.ts");
/* harmony import */ var _components_button__WEBPACK_IMPORTED_MODULE_14__ = __webpack_require__(/*! ~/components/button */ "./source/components/button/index.ts");
/* harmony import */ var _components_topbar__WEBPACK_IMPORTED_MODULE_15__ = __webpack_require__(/*! ~/components/topbar */ "./source/components/topbar/index.ts");
/* harmony import */ var _components_layout__WEBPACK_IMPORTED_MODULE_16__ = __webpack_require__(/*! ~/components/layout */ "./source/components/layout/index.ts");
/* harmony import */ var _pages_tools_Tools__WEBPACK_IMPORTED_MODULE_17__ = __webpack_require__(/*! ./pages/tools/Tools */ "./source/admin/settings/pages/tools/Tools.tsx");
/* harmony import */ var _components_text__WEBPACK_IMPORTED_MODULE_18__ = __webpack_require__(/*! ~/components/text */ "./source/components/text/index.ts");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_19__ = __webpack_require__(/*! react/jsx-runtime */ "react/jsx-runtime");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_19___default = /*#__PURE__*/__webpack_require__.n(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_19__);

const Settings = () => {
  const {
    data,
    isDataChanged,
    saveSettings
  } = (0,_hooks_useSettings__WEBPACK_IMPORTED_MODULE_12__["default"])();
  const [theme, setTheme] = (0,_hooks_useLocalStorage__WEBPACK_IMPORTED_MODULE_1__.useLocalStorage)("pnpnm-theme-status", "light");
  const {
    menuKey
  } = (0,react_router_dom__WEBPACK_IMPORTED_MODULE_2__.useParams)();
  const navigate = (0,react_router_dom__WEBPACK_IMPORTED_MODULE_2__.useNavigate)();
  (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_9__.useEffect)(() => {
    if (data?.tools?.autoSave && isDataChanged) {
      saveSettings();
    }
  }, [data?.tools?.autoSave, isDataChanged]);
  const handleMenuClick = key => {
    navigate(`/settings/${key}`);
  };
  const topbarTitle = /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_19__.jsxs)(_components_inlineStack__WEBPACK_IMPORTED_MODULE_5__["default"], {
    gap: 10,
    children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_19__.jsx)(_components_iconButton__WEBPACK_IMPORTED_MODULE_7__["default"], {
      variant: "secondary",
      rounded: "md",
      color: "primary",
      name: _constants_menus__WEBPACK_IMPORTED_MODULE_3__.SETTINGS_MENUS?.find(({
        key
      }) => key === menuKey)?.icon
    }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_19__.jsx)(_components_text__WEBPACK_IMPORTED_MODULE_18__["default"], {
      color: "primary",
      size: "xl",
      weight: "medium",
      children: _constants_menus__WEBPACK_IMPORTED_MODULE_3__.SETTINGS_MENUS?.find(({
        key
      }) => key === menuKey)?.title
    })]
  });
  const themeButton = /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_19__.jsx)(_components_toggleTheme__WEBPACK_IMPORTED_MODULE_4__["default"], {
    theme: theme,
    setTheme: setTheme
  });
  const save = /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_19__.jsx)(_components_button__WEBPACK_IMPORTED_MODULE_14__["default"], {
    variant: "primary",
    startIcon: "check",
    onClick: () => saveSettings(),
    disabled: !isDataChanged,
    children: "Save Settings"
  });
  const renderPage = () => {
    const pageComponents = {
      general: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_19__.jsx)(_pages_general_General__WEBPACK_IMPORTED_MODULE_11__["default"], {}),
      display: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_19__.jsx)(_pages_display_Display__WEBPACK_IMPORTED_MODULE_10__["default"], {}),
      advanced: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_19__.jsx)(_pages_advanced_Advanced__WEBPACK_IMPORTED_MODULE_6__["default"], {}),
      tools: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_19__.jsx)(_pages_tools_Tools__WEBPACK_IMPORTED_MODULE_17__["default"], {})
    };
    return pageComponents[menuKey] || /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_19__.jsx)(_pages_general_General__WEBPACK_IMPORTED_MODULE_11__["default"], {});
  };
  return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_19__.jsxs)(_components_layout__WEBPACK_IMPORTED_MODULE_16__["default"], {
    children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_19__.jsxs)(_components_sidebar__WEBPACK_IMPORTED_MODULE_13__["default"], {
      minWidth: 300,
      children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_19__.jsx)(_components_sidebar__WEBPACK_IMPORTED_MODULE_13__["default"].Menu, {
        children: _constants_menus__WEBPACK_IMPORTED_MODULE_3__.SETTINGS_MENUS?.map(({
          key,
          title,
          icon
        }, index) => /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_19__.jsx)(_components_sidebar__WEBPACK_IMPORTED_MODULE_13__["default"].Item, {
          title: title,
          icon: icon,
          active: key === menuKey,
          onClick: () => handleMenuClick(key)
        }, key ?? index))
      }), !pnpnm.isPro && /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_19__.jsx)(_components_blockStack__WEBPACK_IMPORTED_MODULE_8__["default"], {
        align: "center",
        inlineAlign: "center",
        className: "h-full",
        children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_19__.jsx)(_components_status_PremiumCard__WEBPACK_IMPORTED_MODULE_0__["default"], {})
      }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_19__.jsx)(_components_sidebar__WEBPACK_IMPORTED_MODULE_13__["default"].Bottom, {
        children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_19__.jsx)(_components_sidebar__WEBPACK_IMPORTED_MODULE_13__["default"].HelpCenter, {})
      })]
    }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_19__.jsxs)(_components_layout__WEBPACK_IMPORTED_MODULE_16__["default"].Wrapper, {
      children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_19__.jsx)(_components_topbar__WEBPACK_IMPORTED_MODULE_15__["default"], {
        leftContents: [topbarTitle],
        rightContents: [themeButton, save]
      }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_19__.jsx)(_components_layout__WEBPACK_IMPORTED_MODULE_16__["default"].Content, {
        children: renderPage()
      })]
    })]
  });
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (Settings);

/***/ },

/***/ "./source/admin/settings/pages/advanced/Advanced.tsx"
/*!***********************************************************!*\
  !*** ./source/admin/settings/pages/advanced/Advanced.tsx ***!
  \***********************************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _components_ThumbnailGenerator__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./components/ThumbnailGenerator */ "./source/admin/settings/pages/advanced/components/ThumbnailGenerator.tsx");
/* harmony import */ var _components_pageContainer__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ~/components/pageContainer */ "./source/components/pageContainer/index.ts");
/* harmony import */ var _components_settingsField__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ~/components/settingsField */ "./source/components/settingsField/index.ts");
/* harmony import */ var _hooks_useSettings__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ~/hooks/useSettings */ "./source/hooks/useSettings.ts");
/* harmony import */ var _components_switcher__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ~/components/switcher */ "./source/components/switcher/index.ts");
/* harmony import */ var _components_field__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ~/components/field */ "./source/components/field/index.ts");
/* harmony import */ var _components_input__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! ~/components/input */ "./source/components/input/index.ts");
/* harmony import */ var _wordpress_i18n__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! @wordpress/i18n */ "@wordpress/i18n");
/* harmony import */ var _wordpress_i18n__WEBPACK_IMPORTED_MODULE_7___default = /*#__PURE__*/__webpack_require__.n(_wordpress_i18n__WEBPACK_IMPORTED_MODULE_7__);
/* harmony import */ var _constants_docs__WEBPACK_IMPORTED_MODULE_8__ = __webpack_require__(/*! ~/constants/docs */ "./source/constants/docs.ts");
/* harmony import */ var _components_status__WEBPACK_IMPORTED_MODULE_9__ = __webpack_require__(/*! ~/components/status */ "./source/components/status/index.ts");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_10__ = __webpack_require__(/*! react/jsx-runtime */ "react/jsx-runtime");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_10___default = /*#__PURE__*/__webpack_require__.n(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_10__);

const Advanced = () => {
  const {
    data,
    setSettings
  } = (0,_hooks_useSettings__WEBPACK_IMPORTED_MODULE_3__["default"])();
  const {
    action,
    organization,
    imageProcessing
  } = data?.advanced || {};

  const {
    contextMenu,
    undoActions
  } = action || {};
  const {
    dynamicFolders,
    uncategorized,
    favorites,
    used,
    unused
  } = organization || {};
  const {
    convertWebp,
    thumbnailGenerator,
    defaultFeaturedImage
  } = imageProcessing || {};
  return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_10__.jsxs)(_components_pageContainer__WEBPACK_IMPORTED_MODULE_1__["default"], {
    children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_10__.jsxs)(_components_field__WEBPACK_IMPORTED_MODULE_5__["default"], {
      title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_7__.__)("Actions", "ninja-media"),
      description: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_7__.__)("Control interactive behaviours such as the context menu and undo prompts.", "ninja-media"),
      docLink: _constants_docs__WEBPACK_IMPORTED_MODULE_8__["default"]?.documentation?.main,
      children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_10__.jsx)(_components_settingsField__WEBPACK_IMPORTED_MODULE_2__["default"], {
        background: "extralight",
        description: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_7__.__)("Enable or disable the context menu in the media library.", "ninja-media"),
        action: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_10__.jsx)(_components_switcher__WEBPACK_IMPORTED_MODULE_4__["default"], {
          title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_7__.__)("Enable context menu", "ninja-media"),
          checked: contextMenu,
          onChange: () => setSettings("advanced.action.contextMenu", !contextMenu)
        })
      }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_10__.jsx)(_components_settingsField__WEBPACK_IMPORTED_MODULE_2__["default"], {
        background: "extralight",
        description: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_7__.__)("Show a temporary undo prompt after deleting a folder, giving you a few seconds to reverse the action.", "ninja-media"),
        action: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_10__.jsx)(_components_switcher__WEBPACK_IMPORTED_MODULE_4__["default"], {
          title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_7__.__)("Undo after folder delete", "ninja-media"),
          checked: undoActions,
          onChange: () => setSettings("advanced.action.undoActions", !undoActions)
        })
      })]
    }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_10__.jsxs)(_components_field__WEBPACK_IMPORTED_MODULE_5__["default"], {
      title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_7__.__)("Organization", "ninja-media"),
      description: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_7__.__)("Set up dynamic folders, uncategorized grouping, and automatic upload sorting to keep your library tidy.", "ninja-media"),
      docLink: _constants_docs__WEBPACK_IMPORTED_MODULE_8__["default"]?.documentation?.main,
      children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_10__.jsx)(_components_settingsField__WEBPACK_IMPORTED_MODULE_2__["default"], {
        statusProps: {
          isPro: true,
          ownUi: false
        },
        background: "extralight",
        description: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_7__.__)("Add smart virtual folders that group media automatically by type, date, or other criteria — no manual sorting needed.", "ninja-media"),
        action: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_10__.jsx)(_components_switcher__WEBPACK_IMPORTED_MODULE_4__["default"], {
          title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_7__.__)("Dynamic folders", "ninja-media"),
          isPro: true,
          checked: dynamicFolders,
          onChange: () => {
          }
        })
      }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_10__.jsx)(_components_settingsField__WEBPACK_IMPORTED_MODULE_2__["default"], {
        background: "extralight",
        description: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_7__.__)("Collect all files that haven't been assigned to any folder into a dedicated Uncategorized group.", "ninja-media"),
        action: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_10__.jsx)(_components_switcher__WEBPACK_IMPORTED_MODULE_4__["default"], {
          title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_7__.__)("Group uncategorized files", "ninja-media"),
          checked: uncategorized,
          onChange: () => setSettings("advanced.organization.uncategorized", !uncategorized)
        })
      }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_10__.jsx)(_components_settingsField__WEBPACK_IMPORTED_MODULE_2__["default"], {
        statusProps: {
          isPro: true,
          ownUi: false
        },
        background: "extralight",
        description: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_7__.__)("Mark your favorite media files for quick access and easy organization.", "ninja-media"),
        action: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_10__.jsx)(_components_switcher__WEBPACK_IMPORTED_MODULE_4__["default"], {
          title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_7__.__)("Mark as favorite", "ninja-media"),
          isPro: true,
          checked: favorites,
          onChange: () => {
          }
        })
      }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_10__.jsx)(_components_settingsField__WEBPACK_IMPORTED_MODULE_2__["default"], {
        statusProps: {
          isPro: true,
          ownUi: false
        },
        background: "extralight",
        description: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_7__.__)("Identify media files that are no longer attached to any post or page and collect them into a Used Files folder.", "ninja-media"),
        action: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_10__.jsx)(_components_switcher__WEBPACK_IMPORTED_MODULE_4__["default"], {
          title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_7__.__)("Used files", "ninja-media"),
          isPro: true,
          checked: used,
          onChange: () => {
          }
        })
      }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_10__.jsx)(_components_settingsField__WEBPACK_IMPORTED_MODULE_2__["default"], {
        statusProps: {
          isPro: true,
          ownUi: false
        },
        background: "extralight",
        description: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_7__.__)("Identify media files that are no longer attached to any post or page and collect them into an Unused Files folder.", "ninja-media"),
        action: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_10__.jsx)(_components_switcher__WEBPACK_IMPORTED_MODULE_4__["default"], {
          title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_7__.__)("Unused files", "ninja-media"),
          isPro: true,
          checked: unused,
          onChange: () => {
          }
        })
      })]
    }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_10__.jsxs)(_components_field__WEBPACK_IMPORTED_MODULE_5__["default"], {
      title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_7__.__)("Image Processing", "ninja-media"),
      description: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_7__.__)("Control how images are processed when uploaded to the media library.", "ninja-media"),
      docLink: _constants_docs__WEBPACK_IMPORTED_MODULE_8__["default"]?.documentation?.main,
      children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_10__.jsx)(_components_settingsField__WEBPACK_IMPORTED_MODULE_2__["default"], {
        statusProps: {
          isPro: true,
          ownUi: false
        },
        background: "extralight",
        description: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_7__.__)("Automatically convert uploaded images to WebP format to reduce file size and improve page load speed.", "ninja-media"),
        action: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_10__.jsx)(_components_switcher__WEBPACK_IMPORTED_MODULE_4__["default"], {
          title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_7__.__)("Convert to WebP on upload", "ninja-media"),
          isPro: true,
          checked: convertWebp,
          onChange: () => {
          }
        })
      }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_10__.jsx)(_components_settingsField__WEBPACK_IMPORTED_MODULE_2__["default"], {
        background: "extralight",
        description: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_7__.__)("Automatically generate all registered thumbnail sizes when a new image is uploaded.", "ninja-media"),
        action: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_10__.jsx)(_components_switcher__WEBPACK_IMPORTED_MODULE_4__["default"], {
          id: "thumbnailGenerator",
          title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_7__.__)("Thumbnail generator", "ninja-media"),
          checked: thumbnailGenerator,
          onChange: () => setSettings("advanced.imageProcessing.thumbnailGenerator", !thumbnailGenerator)
        }),
        children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_10__.jsx)(_components_settingsField__WEBPACK_IMPORTED_MODULE_2__["default"].SubField, {
          depend: !thumbnailGenerator,
          dependOn: "thumbnailGenerator",
          title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_7__.__)("Regenerate Existing Thumbnails", "ninja-media"),
          description: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_7__.__)("Regenerate all thumbnail sizes for images already in your media library. Large libraries may take a moment — processing happens in small batches.", "ninja-media"),
          secondaryAction: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_10__.jsx)(_components_ThumbnailGenerator__WEBPACK_IMPORTED_MODULE_0__["default"], {})
        })
      }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_10__.jsx)(_components_settingsField__WEBPACK_IMPORTED_MODULE_2__["default"], {
        background: "extralight",
        description: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_10__.jsxs)(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_10__.Fragment, {
          children: [(0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_7__.__)("Show a fallback image on any post or page that has no featured image set. Choose the image in ", "ninja-media"), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_10__.jsx)("a", {
            href: pnpnm.ajaxUrl.replace("admin-ajax.php", "options-media.php"),
            target: "_blank",
            rel: "noreferrer",
            style: {
              color: "var(--pnpnm-primary)"
            },
            children: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_7__.__)("WP Settings → Media", "ninja-media")
          }), "."]
        }),
        action: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_10__.jsx)(_components_switcher__WEBPACK_IMPORTED_MODULE_4__["default"], {
          title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_7__.__)("Default featured image", "ninja-media"),
          checked: defaultFeaturedImage,
          onChange: () => setSettings("advanced.imageProcessing.defaultFeaturedImage", !defaultFeaturedImage)
        })
      })]
    }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_10__.jsx)(_components_field__WEBPACK_IMPORTED_MODULE_5__["default"], {
      title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_7__.__)("CDN", "ninja-media"),
      description: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_7__.__)("Serve plugin assets through a Content Delivery Network for faster load times worldwide.", "ninja-media"),
      docLink: _constants_docs__WEBPACK_IMPORTED_MODULE_8__["default"]?.documentation?.main,
      children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_10__.jsx)(_components_settingsField__WEBPACK_IMPORTED_MODULE_2__["default"], {
        statusProps: {
          isPro: true,
          ownUi: false
        },
        background: "extralight",
        title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_7__.__)("CDN Base URL", "ninja-media"),
        description: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_7__.__)("Enter your CDN base URL. All plugin assets will be served through this URL instead of your server.", "ninja-media"),
        secondaryAction: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_10__.jsx)(_components_status__WEBPACK_IMPORTED_MODULE_9__["default"], {
          isPro: true,
          ownUi: false,
          proTooltipDisabled: true,
          style: {
            width: "100%",
            maxWidth: "380px"
          },
          children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_10__.jsx)(_components_input__WEBPACK_IMPORTED_MODULE_6__["default"], {
            type: "text",
            size: "small",
            fullWidth: true,
            placeholder: "https://cdn.example.com",
            value: cdnBaseUrl,
            disabled: !pnpnm?.isPro,
            onChange: value => {
            }
          })
        })
      })
    })]
  });
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (Advanced);

/***/ },

/***/ "./source/admin/settings/pages/advanced/components/ThumbnailGenerator.tsx"
/*!********************************************************************************!*\
  !*** ./source/admin/settings/pages/advanced/components/ThumbnailGenerator.tsx ***!
  \********************************************************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _wordpress_element__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @wordpress/element */ "@wordpress/element");
/* harmony import */ var _wordpress_element__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(_wordpress_element__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var _components_inlineStack__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ~/components/inlineStack */ "./source/components/inlineStack/index.ts");
/* harmony import */ var _wordpress_i18n__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! @wordpress/i18n */ "@wordpress/i18n");
/* harmony import */ var _wordpress_i18n__WEBPACK_IMPORTED_MODULE_2___default = /*#__PURE__*/__webpack_require__.n(_wordpress_i18n__WEBPACK_IMPORTED_MODULE_2__);
/* harmony import */ var _components_button__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ~/components/button */ "./source/components/button/index.ts");
/* harmony import */ var _components_icon__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ~/components/icon */ "./source/components/icon/index.ts");
/* harmony import */ var _components_text__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ~/components/text */ "./source/components/text/index.ts");
/* harmony import */ var _redux_api_settings__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! ~/redux/api/settings */ "./source/redux/api/settings.ts");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! react/jsx-runtime */ "react/jsx-runtime");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_7___default = /*#__PURE__*/__webpack_require__.n(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_7__);

const BATCH_SIZE = 5;
const ThumbnailGenerator = () => {
  const [isGenerating, setIsGenerating] = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useState)(false);
  const [processed, setProcessed] = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useState)(0);
  const [total, setTotal] = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useState)(null);
  const [isComplete, setIsComplete] = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useState)(false);
  const [hasError, setHasError] = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useState)(false);
  const {
    data: countData,
    refetch: refetchCount
  } = (0,_redux_api_settings__WEBPACK_IMPORTED_MODULE_6__.useGetThumbnailCountQuery)();
  const [generateThumbnails] = (0,_redux_api_settings__WEBPACK_IMPORTED_MODULE_6__.useGenerateThumbnailsMutation)();
  const runBatch = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useCallback)(async (offset, grandTotal) => {
    try {
      const result = await generateThumbnails({
        offset,
        batch_size: BATCH_SIZE
      }).unwrap();
      const newOffset = result?.data?.offset ?? 0;
      setProcessed(newOffset);
      if (result?.data?.is_complete) {
        setIsGenerating(false);
        setIsComplete(true);
        return;
      }
      await runBatch(newOffset, grandTotal);
    } catch {
      setIsGenerating(false);
      setHasError(true);
    }
  }, [generateThumbnails]);
  const handleGenerate = async () => {
    setIsGenerating(true);
    setIsComplete(false);
    setHasError(false);
    setProcessed(0);
    try {
      const countResult = await refetchCount();
      const imageTotal = countResult.data?.data?.total ?? 0;
      setTotal(imageTotal);
      if (imageTotal === 0) {
        setIsGenerating(false);
        setIsComplete(true);
        return;
      }
      await runBatch(0, imageTotal);
    } catch {
      setIsGenerating(false);
      setHasError(true);
    }
  };
  const handleReset = () => {
    setIsComplete(false);
    setHasError(false);
    setProcessed(0);
    setTotal(null);
  };
  const imageTotal = total ?? countData?.data?.total ?? 0;
  const renderStatus = () => {
    if (hasError) {
      return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_7__.jsxs)(_components_inlineStack__WEBPACK_IMPORTED_MODULE_1__["default"], {
        gap: 6,
        align: "center",
        children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_7__.jsx)(_components_icon__WEBPACK_IMPORTED_MODULE_4__["default"], {
          name: "error",
          color: "error",
          fontSize: "lg"
        }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_7__.jsx)(_components_text__WEBPACK_IMPORTED_MODULE_5__["default"], {
          size: "sm",
          color: "error",
          children: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_2__.__)("Generation failed. Please try again.", "ninja-media")
        })]
      });
    }
    if (isComplete) {
      return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_7__.jsxs)(_components_inlineStack__WEBPACK_IMPORTED_MODULE_1__["default"], {
        gap: 6,
        align: "center",
        children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_7__.jsx)(_components_icon__WEBPACK_IMPORTED_MODULE_4__["default"], {
          name: "check_circle",
          color: "primary",
          fontSize: "lg"
        }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_7__.jsx)(_components_text__WEBPACK_IMPORTED_MODULE_5__["default"], {
          size: "sm",
          color: "primary",
          children: imageTotal === 0 ? (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_2__.__)("No images found.", "ninja-media") : (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_2__.sprintf)((0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_2__.__)("Done! %d image(s) processed.", "ninja-media"), imageTotal)
        })]
      });
    }
    if (isGenerating) {
      return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_7__.jsxs)(_components_inlineStack__WEBPACK_IMPORTED_MODULE_1__["default"], {
        gap: 6,
        align: "center",
        children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_7__.jsx)(_components_icon__WEBPACK_IMPORTED_MODULE_4__["default"], {
          name: "progress_activity",
          className: "loading",
          fontSize: "lg"
        }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_7__.jsx)(_components_text__WEBPACK_IMPORTED_MODULE_5__["default"], {
          size: "sm",
          children: imageTotal > 0 ? (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_2__.sprintf)((0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_2__.__)("Generating… %1$d / %2$d", "ninja-media"), processed, imageTotal) : (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_2__.__)("Preparing…", "ninja-media")
        })]
      });
    }
    return null;
  };
  return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_7__.jsxs)(_components_inlineStack__WEBPACK_IMPORTED_MODULE_1__["default"], {
    gap: 12,
    align: "center",
    children: [isComplete || hasError ? /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_7__.jsx)(_components_button__WEBPACK_IMPORTED_MODULE_3__["default"], {
      variant: "secondary",
      size: "small",
      startIcon: "autorenew",
      onClick: handleReset,
      children: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_2__.__)("Reset", "ninja-media")
    }) : /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_7__.jsx)(_components_button__WEBPACK_IMPORTED_MODULE_3__["default"], {
      variant: "primary",
      size: "small",
      startIcon: "auto_fix_high",
      loading: isGenerating,
      disabled: isGenerating,
      onClick: handleGenerate,
      children: isGenerating ? (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_2__.__)("Generating…", "ninja-media") : (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_2__.__)("Generate Thumbnails", "ninja-media")
    }), renderStatus()]
  });
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (ThumbnailGenerator);

/***/ },

/***/ "./source/admin/settings/pages/display/Display.tsx"
/*!*********************************************************!*\
  !*** ./source/admin/settings/pages/display/Display.tsx ***!
  \*********************************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _utils_icons__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ~/utils/icons */ "./source/utils/icons.ts");
/* harmony import */ var _components_svgIcon_SvgIcon__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ~/components/svgIcon/SvgIcon */ "./source/components/svgIcon/SvgIcon.tsx");
/* harmony import */ var _wordpress_element__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! @wordpress/element */ "@wordpress/element");
/* harmony import */ var _wordpress_element__WEBPACK_IMPORTED_MODULE_2___default = /*#__PURE__*/__webpack_require__.n(_wordpress_element__WEBPACK_IMPORTED_MODULE_2__);
/* harmony import */ var _components_settingsField__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ~/components/settingsField */ "./source/components/settingsField/index.ts");
/* harmony import */ var _components_pageContainer__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ~/components/pageContainer */ "./source/components/pageContainer/index.ts");
/* harmony import */ var _components_inlineStack__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ~/components/inlineStack */ "./source/components/inlineStack/index.ts");
/* harmony import */ var _components_colorPicker__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! ~/components/colorPicker */ "./source/components/colorPicker/index.ts");
/* harmony import */ var _components_description__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! ~/components/description */ "./source/components/description/index.ts");
/* harmony import */ var _components_blockStack__WEBPACK_IMPORTED_MODULE_8__ = __webpack_require__(/*! ~/components/blockStack */ "./source/components/blockStack/index.ts");
/* harmony import */ var _hooks_useDebounce__WEBPACK_IMPORTED_MODULE_9__ = __webpack_require__(/*! ~/hooks/useDebounce */ "./source/hooks/useDebounce.ts");
/* harmony import */ var _hooks_useSettings__WEBPACK_IMPORTED_MODULE_10__ = __webpack_require__(/*! ~/hooks/useSettings */ "./source/hooks/useSettings.ts");
/* harmony import */ var _components_switcher__WEBPACK_IMPORTED_MODULE_11__ = __webpack_require__(/*! ~/components/switcher */ "./source/components/switcher/index.ts");
/* harmony import */ var _components_status__WEBPACK_IMPORTED_MODULE_12__ = __webpack_require__(/*! ~/components/status */ "./source/components/status/index.ts");
/* harmony import */ var _components_slider__WEBPACK_IMPORTED_MODULE_13__ = __webpack_require__(/*! ~/components/slider */ "./source/components/slider/index.ts");
/* harmony import */ var _components_field__WEBPACK_IMPORTED_MODULE_14__ = __webpack_require__(/*! ~/components/field */ "./source/components/field/index.ts");
/* harmony import */ var _wordpress_i18n__WEBPACK_IMPORTED_MODULE_15__ = __webpack_require__(/*! @wordpress/i18n */ "@wordpress/i18n");
/* harmony import */ var _wordpress_i18n__WEBPACK_IMPORTED_MODULE_15___default = /*#__PURE__*/__webpack_require__.n(_wordpress_i18n__WEBPACK_IMPORTED_MODULE_15__);
/* harmony import */ var _components_text__WEBPACK_IMPORTED_MODULE_16__ = __webpack_require__(/*! ~/components/text */ "./source/components/text/index.ts");
/* harmony import */ var _components_card__WEBPACK_IMPORTED_MODULE_17__ = __webpack_require__(/*! ~/components/card */ "./source/components/card/index.ts");
/* harmony import */ var _constants_docs__WEBPACK_IMPORTED_MODULE_18__ = __webpack_require__(/*! ~/constants/docs */ "./source/constants/docs.ts");
/* harmony import */ var clsx__WEBPACK_IMPORTED_MODULE_19__ = __webpack_require__(/*! clsx */ "./node_modules/clsx/dist/clsx.mjs");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_20__ = __webpack_require__(/*! react/jsx-runtime */ "react/jsx-runtime");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_20___default = /*#__PURE__*/__webpack_require__.n(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_20__);

const Display = () => {
  const {
    data,
    setSettings
  } = (0,_hooks_useSettings__WEBPACK_IMPORTED_MODULE_10__["default"])();
  const {
    theme: themeValue,
    settings
  } = data?.display || {};
  const {
    theme,
    color: colorValue,
    firstTime
  } = themeValue || {};
  const {
    perPage,
    detailsHover,
    breadcrumbNavigation,
    lightbox
  } = settings || {};
  const [color, setColor] = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_2__.useState)(colorValue || "#4D49FC");
  (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_2__.useEffect)(() => {
    if (colorValue) {
      setColor(colorValue);
    }
  }, [colorValue]);
  (0,_hooks_useDebounce__WEBPACK_IMPORTED_MODULE_9__["default"])(() => {
    setSettings("display.theme.color", color);
    const root = document.documentElement;
    if (root && colorValue) {
      root.style.setProperty("--pnpnm-primary", color);
    }
  }, [color], 800);
  return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_20__.jsxs)(_components_pageContainer__WEBPACK_IMPORTED_MODULE_4__["default"], {
    children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_20__.jsxs)(_components_field__WEBPACK_IMPORTED_MODULE_14__["default"], {
      title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_15__.__)("Theme", "ninja-media"),
      description: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_15__.__)("Choose the visual style and accent color for the media library interface.", "ninja-media"),
      docLink: _constants_docs__WEBPACK_IMPORTED_MODULE_18__["default"]?.documentation?.main,
      children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_20__.jsxs)(_components_settingsField__WEBPACK_IMPORTED_MODULE_3__["default"], {
        background: "extralight",
        title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_15__.__)("Select Theme", "ninja-media"),
        children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_20__.jsx)(_components_description__WEBPACK_IMPORTED_MODULE_7__["default"], {
          text: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_15__.__)("Choose a visual style for the folder sidebar and media browser.", "ninja-media")
        }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_20__.jsx)(_components_inlineStack__WEBPACK_IMPORTED_MODULE_5__["default"], {
          gap: 20,
          children: THEMES?.map(({
            key,
            title,
            icon,
            statusProps
          }, index) => /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_20__.jsxs)(_components_card__WEBPACK_IMPORTED_MODULE_17__["default"], {
            statusProps: {
              ...statusProps,
              ownUi: false,
              widthFull: false,
              size: "small"
            },
            padding: 5,
            background: "white",
            rounded: "md",
            style: {
              position: "relative",
              width: "235px",
              height: "200px",
              cursor: "pointer"
            },
            className: (0,clsx__WEBPACK_IMPORTED_MODULE_19__["default"])("pnpnm-theme-preview", theme === key && "pnpnm-theme-preview--active"),
            onClick: () => {
            },
            children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_20__.jsx)(_components_card__WEBPACK_IMPORTED_MODULE_17__["default"], {
              padding: 0,
              background: "white",
              rounded: "sm",
              style: {
                width: "220px",
                height: "160px",
                display: "flex",
                alignItems: "center",
                justifyContent: "center"
              },
              className: "pnpnm-theme-preview__icon",
              children: icon
            }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_20__.jsxs)(_components_inlineStack__WEBPACK_IMPORTED_MODULE_5__["default"], {
              align: "center",
              blockAlign: "end",
              gap: 10,
              wrap: false,
              style: {
                position: "absolute",
                bottom: 0,
                left: 0,
                right: 0,
                padding: 8
              },
              children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_20__.jsx)(_components_text__WEBPACK_IMPORTED_MODULE_16__["default"], {
                color: theme === key ? "primary" : "descgray",
                size: "sm",
                children: title
              }), statusProps?.isPro && /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_20__.jsx)(_components_status__WEBPACK_IMPORTED_MODULE_12__["default"].Pro, {
                tooltipDisabled: true
              })]
            })]
          }, key ?? index))
        })]
      }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_20__.jsxs)(_components_settingsField__WEBPACK_IMPORTED_MODULE_3__["default"], {
        background: "extralight",
        title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_15__.__)("Media Library Color", "ninja-media"),
        children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_20__.jsx)(_components_description__WEBPACK_IMPORTED_MODULE_7__["default"], {
          text: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_15__.__)("Set the primary accent color used throughout the media library interface.", "ninja-media")
        }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_20__.jsxs)(_components_blockStack__WEBPACK_IMPORTED_MODULE_8__["default"], {
          gap: 5,
          children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_20__.jsx)(_components_colorPicker__WEBPACK_IMPORTED_MODULE_6__["default"], {
            defaultColor: "#4D49FC",
            selectedColor: color,
            onChange: c => {
              setColor(c);
              if (firstTime) {
                setSettings("display.theme.firstTime", false);
              }
            }
          }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_20__.jsx)(_components_colorPicker__WEBPACK_IMPORTED_MODULE_6__["default"].ColorBox, {
            colors: ["#4d49fc", "#2271B1", "#00ac47", "#dd115c", "#0048BC", "#001941", "#000E25", "#0072A8", "#007AFF", "#22BC00", "#6800C8"],
            selectedColor: color,
            onSelect: c => {
              setColor(c);
              if (firstTime) {
                setSettings("display.theme.firstTime", false);
              }
            }
          })]
        })]
      })]
    }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_20__.jsxs)(_components_field__WEBPACK_IMPORTED_MODULE_14__["default"], {
      title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_15__.__)("View Settings", "ninja-media"),
      description: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_15__.__)("Adjust how many items appear per page, thumbnail sizes, and hover preview behavior.", "ninja-media"),
      docLink: _constants_docs__WEBPACK_IMPORTED_MODULE_18__["default"]?.documentation?.main,
      children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_20__.jsx)(_components_settingsField__WEBPACK_IMPORTED_MODULE_3__["default"], {
        background: "extralight",
        title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_15__.__)("Items per page", "ninja-media"),
        children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_20__.jsx)(_components_slider__WEBPACK_IMPORTED_MODULE_13__["default"], {
          min: 20,
          max: 500,
          defaultValue: 80,
          reset: true,
          value: perPage || 80,
          onChange: value => setSettings("display.settings.perPage", Number(value))
        })
      }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_20__.jsx)(_components_settingsField__WEBPACK_IMPORTED_MODULE_3__["default"], {
        statusProps: {
          isPro: true,
          ownUi: false
        },
        background: "extralight",
        description: "Show file name, type, and size as a tooltip when hovering over a media item.",
        action: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_20__.jsx)(_components_switcher__WEBPACK_IMPORTED_MODULE_11__["default"], {
          title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_15__.__)("Show media details on hover", "ninja-media"),
          isPro: true,
          checked: detailsHover,
          onChange: () => {
          }
        })
      }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_20__.jsx)(_components_settingsField__WEBPACK_IMPORTED_MODULE_3__["default"], {
        background: "extralight",
        description: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_15__.__)("Show a clickable folder trail above the media grid so you can navigate back up through nested folders.", "ninja-media"),
        action: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_20__.jsx)(_components_switcher__WEBPACK_IMPORTED_MODULE_11__["default"], {
          title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_15__.__)("Breadcrumb navigation", "ninja-media"),
          checked: breadcrumbNavigation,
          onChange: () => setSettings("display.settings.breadcrumbNavigation", !breadcrumbNavigation)
        })
      }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_20__.jsx)(_components_settingsField__WEBPACK_IMPORTED_MODULE_3__["default"], {
        background: "extralight",
        description: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_15__.__)("Open images in a full-screen lightbox viewer with prev/next navigation and keyboard support.", "ninja-media"),
        action: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_20__.jsx)(_components_switcher__WEBPACK_IMPORTED_MODULE_11__["default"], {
          title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_15__.__)("Lightbox", "ninja-media"),
          checked: lightbox ?? false,
          onChange: () => setSettings("display.settings.lightbox", !(lightbox ?? false))
        })
      })]
    })]
  });
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (Display);
const THEMES = [{
  key: "default",
  title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_15__.__)("Default", "ninja-media"),
  icon: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_20__.jsx)(_components_svgIcon_SvgIcon__WEBPACK_IMPORTED_MODULE_1__["default"], {
    src: _utils_icons__WEBPACK_IMPORTED_MODULE_0__.iconThemeDefault
  })
}, {
  key: "bold",
  title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_15__.__)("Bold", "ninja-media"),
  icon: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_20__.jsx)(_components_svgIcon_SvgIcon__WEBPACK_IMPORTED_MODULE_1__["default"], {
    src: _utils_icons__WEBPACK_IMPORTED_MODULE_0__.iconThemeBold
  }),
  statusProps: {
    isPro: true
  }
}, {
  key: "plugininja",
  title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_15__.__)("Plugininja", "ninja-media"),
  icon: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_20__.jsx)(_components_svgIcon_SvgIcon__WEBPACK_IMPORTED_MODULE_1__["default"], {
    src: _utils_icons__WEBPACK_IMPORTED_MODULE_0__.iconThemePlugininja
  }),
  statusProps: {
    isPro: true
  }
}, {
  key: "awesome",
  title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_15__.__)("Awesome", "ninja-media"),
  icon: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_20__.jsx)(_components_svgIcon_SvgIcon__WEBPACK_IMPORTED_MODULE_1__["default"], {
    src: _utils_icons__WEBPACK_IMPORTED_MODULE_0__.iconThemeAwesome
  }),
  statusProps: {
    isPro: true
  }
}];

/***/ },

/***/ "./source/admin/settings/pages/general/General.tsx"
/*!*********************************************************!*\
  !*** ./source/admin/settings/pages/general/General.tsx ***!
  \*********************************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _components_pageContainer__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ~/components/pageContainer */ "./source/components/pageContainer/index.ts");
/* harmony import */ var _components_settingsField__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ~/components/settingsField */ "./source/components/settingsField/index.ts");
/* harmony import */ var _components_inlineStack__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ~/components/inlineStack */ "./source/components/inlineStack/index.ts");
/* harmony import */ var _hooks_useSettings__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ~/hooks/useSettings */ "./source/hooks/useSettings.ts");
/* harmony import */ var _components_checkbox__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ~/components/checkbox */ "./source/components/checkbox/index.ts");
/* harmony import */ var _components_switcher__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ~/components/switcher */ "./source/components/switcher/index.ts");
/* harmony import */ var _components_field__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! ~/components/field */ "./source/components/field/index.ts");
/* harmony import */ var _components_input__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! ~/components/input */ "./source/components/input/index.ts");
/* harmony import */ var _components_text__WEBPACK_IMPORTED_MODULE_8__ = __webpack_require__(/*! ~/components/text */ "./source/components/text/index.ts");
/* harmony import */ var _wordpress_i18n__WEBPACK_IMPORTED_MODULE_9__ = __webpack_require__(/*! @wordpress/i18n */ "@wordpress/i18n");
/* harmony import */ var _wordpress_i18n__WEBPACK_IMPORTED_MODULE_9___default = /*#__PURE__*/__webpack_require__.n(_wordpress_i18n__WEBPACK_IMPORTED_MODULE_9__);
/* harmony import */ var _constants_docs__WEBPACK_IMPORTED_MODULE_10__ = __webpack_require__(/*! ~/constants/docs */ "./source/constants/docs.ts");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_11__ = __webpack_require__(/*! react/jsx-runtime */ "react/jsx-runtime");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_11___default = /*#__PURE__*/__webpack_require__.n(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_11__);

const General = () => {
  const {
    data,
    setSettings
  } = (0,_hooks_useSettings__WEBPACK_IMPORTED_MODULE_3__["default"])();
  const {
    folder,
    files,
    svgSupport
  } = data?.general || {};
  const {
    postTypeFolders,
    forceSorting,
    showFolders,
    showFolderId,
    showCount,
    treeConnector
  } = folder || {};
  const {
    bulkSelection,
    replaceMedia,
    duplicateMedia,
    moveToTrash,
    controlUploadSize,
    uploadSize,
    controlBigImageSize,
    bigImageSize
  } = files || {};
  const {
    uploadSupport,
    sanitization
  } = svgSupport || {};

  return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_11__.jsxs)(_components_pageContainer__WEBPACK_IMPORTED_MODULE_0__["default"], {
    children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_11__.jsxs)(_components_field__WEBPACK_IMPORTED_MODULE_6__["default"], {
      title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_9__.__)("Folder", "ninja-media"),
      description: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_9__.__)("Control how folders are created, displayed, and linked to your post types.", "ninja-media"),
      docLink: _constants_docs__WEBPACK_IMPORTED_MODULE_10__["default"]?.documentation?.main,
      children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_11__.jsx)(_components_settingsField__WEBPACK_IMPORTED_MODULE_1__["default"], {
        background: "extralight",
        description: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_9__.__)("Automatically places new folders in order instead of at the bottom.", "ninja-media"),
        action: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_11__.jsx)(_components_switcher__WEBPACK_IMPORTED_MODULE_5__["default"], {
          title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_9__.__)("Force sorting when adding new folders", "ninja-media"),
          checked: forceSorting,
          onChange: () => setSettings("general.folder.forceSorting", !forceSorting)
        })
      }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_11__.jsx)(_components_settingsField__WEBPACK_IMPORTED_MODULE_1__["default"], {
        background: "extralight",
        description: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_9__.__)("Displays your media folders directly in the WordPress admin sidebar.", "ninja-media"),
        action: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_11__.jsx)(_components_switcher__WEBPACK_IMPORTED_MODULE_5__["default"], {
          title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_9__.__)("Show folders in admin menu", "ninja-media"),
          checked: showFolders,
          onChange: () => setSettings("general.folder.showFolders", !showFolders)
        })
      }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_11__.jsx)(_components_settingsField__WEBPACK_IMPORTED_MODULE_1__["default"], {
        statusProps: {
          isPro: true,
          ownUi: false
        },
        background: "extralight",
        description: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_9__.__)("Displays the folder ID in the file details which can be useful for dynamic folders to identify the folder structure.", "ninja-media"),
        action: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_11__.jsx)(_components_switcher__WEBPACK_IMPORTED_MODULE_5__["default"], {
          title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_9__.__)("Show folder ID", "ninja-media"),
          isPro: true,
          checked: showFolderId,
          onChange: () => {
          }
        })
      }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_11__.jsx)(_components_settingsField__WEBPACK_IMPORTED_MODULE_1__["default"], {
        background: "extralight",
        description: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_9__.__)("Displays the count in each folder, which can be useful for quickly assessing the number of items in a folder.", "ninja-media"),
        action: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_11__.jsx)(_components_switcher__WEBPACK_IMPORTED_MODULE_5__["default"], {
          title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_9__.__)("Show count in folders", "ninja-media"),
          checked: showCount,
          onChange: () => setSettings("general.folder.showCount", !showCount)
        })
      }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_11__.jsx)(_components_settingsField__WEBPACK_IMPORTED_MODULE_1__["default"], {
        background: "extralight",
        description: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_9__.__)("A UI element that connects parent and child nodes in a tree structure using lines or arrows.", "ninja-media"),
        action: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_11__.jsx)(_components_switcher__WEBPACK_IMPORTED_MODULE_5__["default"], {
          title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_9__.__)("Tree Connector", "ninja-media"),
          checked: treeConnector,
          onChange: () => setSettings("general.folder.treeConnector", !treeConnector)
        })
      }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_11__.jsx)(_components_settingsField__WEBPACK_IMPORTED_MODULE_1__["default"], {
        statusProps: {
          isPro: true,
          ownUi: false
        },
        background: "extralight",
        title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_9__.__)("Post Type folders", "ninja-media"),
        description: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_9__.__)("Automatically creates folders for each post type in the media library to keep your media organized based on the content types you use.", "ninja-media"),
        children: pnpnm?.supportedPostTypes?.map((key, index) => /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_11__.jsxs)(_components_inlineStack__WEBPACK_IMPORTED_MODULE_2__["default"], {
          gap: 10,
          style: {
            cursor: "pointer",
            userSelect: "none"
          },
          onClick: () => {
          },
          children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_11__.jsx)(_components_checkbox__WEBPACK_IMPORTED_MODULE_4__["default"], {
            size: "small",
            checked: postTypeFolders?.includes(key),
            onChange: () => {
            }
          }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_11__.jsx)(_components_text__WEBPACK_IMPORTED_MODULE_8__["default"], {
            size: "sm",
            children: key
          })]
        }, key ?? index))
      })]
    }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_11__.jsxs)(_components_field__WEBPACK_IMPORTED_MODULE_6__["default"], {
      title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_9__.__)("Files", "ninja-media"),
      description: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_9__.__)("Configure file management options including upload limits, media replacement, and deletion behaviour.", "ninja-media"),
      docLink: _constants_docs__WEBPACK_IMPORTED_MODULE_10__["default"]?.documentation?.main,
      children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_11__.jsx)(_components_settingsField__WEBPACK_IMPORTED_MODULE_1__["default"], {
        background: "extralight",
        description: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_9__.__)("Select multiple files at once and drag them into any folder in a single action.", "ninja-media"),
        action: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_11__.jsx)(_components_switcher__WEBPACK_IMPORTED_MODULE_5__["default"], {
          title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_9__.__)("Bulk selection and bulk drag", "ninja-media"),
          checked: bulkSelection,
          onChange: () => setSettings("general.files.bulkSelection", !bulkSelection)
        })
      }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_11__.jsx)(_components_settingsField__WEBPACK_IMPORTED_MODULE_1__["default"], {
        statusProps: {
          isPro: true,
          ownUi: false
        },
        background: "extralight",
        description: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_9__.__)("Swap any existing media file with a new version while preserving all post references and attachment IDs.", "ninja-media"),
        action: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_11__.jsx)(_components_switcher__WEBPACK_IMPORTED_MODULE_5__["default"], {
          title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_9__.__)("Replace Media", "ninja-media"),
          isPro: true,
          checked: replaceMedia,
          onChange: () => {
          }
        })
      }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_11__.jsx)(_components_settingsField__WEBPACK_IMPORTED_MODULE_1__["default"], {
        statusProps: {
          isPro: true,
          ownUi: false
        },
        background: "extralight",
        description: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_9__.__)("Create an exact copy of any media file directly from the media library.", "ninja-media"),
        action: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_11__.jsx)(_components_switcher__WEBPACK_IMPORTED_MODULE_5__["default"], {
          title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_9__.__)("Duplicate Media", "ninja-media"),
          isPro: true,
          checked: duplicateMedia,
          onChange: () => {
          }
        })
      }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_11__.jsx)(_components_settingsField__WEBPACK_IMPORTED_MODULE_1__["default"], {
        statusProps: {
          isPro: true,
          ownUi: false
        },
        background: "extralight",
        description: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_9__.__)("Send deleted files to a trash bin so you can review or restore them before they are permanently removed.", "ninja-media"),
        action: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_11__.jsx)(_components_switcher__WEBPACK_IMPORTED_MODULE_5__["default"], {
          title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_9__.__)("Move files to trash before deleting", "ninja-media"),
          isPro: true,
          checked: moveToTrash,
          onChange: () => {
          }
        })
      }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_11__.jsx)(_components_settingsField__WEBPACK_IMPORTED_MODULE_1__["default"], {
        background: "extralight",
        description: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_9__.__)("Control the maximum upload file size for the media library. Current server limit: %s MB.", "ninja-media").replace("%s", String(pnpnm.maxUploadSize)),
        action: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_11__.jsx)(_components_switcher__WEBPACK_IMPORTED_MODULE_5__["default"], {
          id: "controlUploadSize",
          title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_9__.__)("Control maximum upload file size", "ninja-media"),
          checked: controlUploadSize,
          onChange: () => setSettings("general.files.controlUploadSize", !controlUploadSize)
        }),
        children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_11__.jsx)(_components_settingsField__WEBPACK_IMPORTED_MODULE_1__["default"].SubField, {
          background: "white",
          title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_9__.__)("Maximum upload size", "ninja-media"),
          description: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_9__.__)("Set a custom upload size limit in MB. The server allows up to %s MB.", "ninja-media").replace("%s", String(pnpnm.maxUploadSize)),
          depend: !controlUploadSize,
          dependOn: "controlUploadSize",
          secondaryAction: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_11__.jsx)(_components_input__WEBPACK_IMPORTED_MODULE_7__["default"], {
            type: "number",
            size: "small",
            fullWidth: false,
            customWidth: "150px",
            suffix: "MB",
            value: uploadSize || "",
            debounce: true,
            debounceTime: 1000,
            onChange: value => setSettings("general.files.uploadSize", Number(value))
          })
        })
      }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_11__.jsx)(_components_settingsField__WEBPACK_IMPORTED_MODULE_1__["default"], {
        statusProps: {
          isPro: true,
          ownUi: false
        },
        background: "extralight",
        description: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_9__.__)("Control WordPress's built-in large image resizing. When enabled, uploaded images exceeding the pixel threshold you set will be automatically scaled down on upload.", "ninja-media"),
        action: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_11__.jsx)(_components_switcher__WEBPACK_IMPORTED_MODULE_5__["default"], {
          id: "controlBigImageSize",
          title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_9__.__)("Scale down large images on upload", "ninja-media"),
          isPro: true,
          checked: controlBigImageSize,
          onChange: () => {
          }
        }),
        children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_11__.jsx)(_components_settingsField__WEBPACK_IMPORTED_MODULE_1__["default"].SubField, {
          background: "white",
          title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_9__.__)("Big image threshold", "ninja-media"),
          description: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_9__.__)("Images with a width or height exceeding this value (in pixels) will be scaled down. WordPress default is 2560.", "ninja-media"),
          depend: !controlBigImageSize,
          dependOn: "controlBigImageSize",
          secondaryAction: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_11__.jsx)(_components_input__WEBPACK_IMPORTED_MODULE_7__["default"], {
            type: "number",
            size: "small",
            fullWidth: false,
            customWidth: "150px",
            suffix: "px",
            value: bigImageSize || "",
            onChange: value => {
            }
          })
        })
      })]
    }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_11__.jsxs)(_components_field__WEBPACK_IMPORTED_MODULE_6__["default"], {
      title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_9__.__)("SVG Support", "ninja-media"),
      description: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_9__.__)("Allow SVG files to be uploaded and automatically sanitized to keep your site secure.", "ninja-media"),
      docLink: _constants_docs__WEBPACK_IMPORTED_MODULE_10__["default"]?.documentation?.main,
      children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_11__.jsx)(_components_settingsField__WEBPACK_IMPORTED_MODULE_1__["default"], {
        background: "extralight",
        description: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_9__.__)("Support for uploading SVG files to the media library. Please note that enabling this option can pose security risks if you upload SVG files from untrusted sources.", "ninja-media"),
        action: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_11__.jsx)(_components_switcher__WEBPACK_IMPORTED_MODULE_5__["default"], {
          title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_9__.__)("SVG upload support", "ninja-media"),
          checked: uploadSupport,
          onChange: () => setSettings("general.svgSupport.uploadSupport", !uploadSupport)
        })
      }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_11__.jsx)(_components_settingsField__WEBPACK_IMPORTED_MODULE_1__["default"], {
        background: "extralight",
        description: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_9__.__)("Automatically clean uploaded SVG files to strip potentially harmful code before they are saved.", "ninja-media"),
        action: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_11__.jsx)(_components_switcher__WEBPACK_IMPORTED_MODULE_5__["default"], {
          title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_9__.__)("Built-in SVG sanitization", "ninja-media"),
          checked: sanitization,
          onChange: () => setSettings("general.svgSupport.sanitization", !sanitization)
        })
      })]
    })]
  });
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (General);

/***/ },

/***/ "./source/admin/settings/pages/tools/Tools.tsx"
/*!*****************************************************!*\
  !*** ./source/admin/settings/pages/tools/Tools.tsx ***!
  \*****************************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _redux_features_settings_settings__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ~/redux/features/settings/settings */ "./source/redux/features/settings/settings.ts");
/* harmony import */ var _components_alert_Alert__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ~/components/alert/Alert */ "./source/components/alert/Alert.tsx");
/* harmony import */ var _components_pageContainer__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ~/components/pageContainer */ "./source/components/pageContainer/index.ts");
/* harmony import */ var _components_settingsField__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ~/components/settingsField */ "./source/components/settingsField/index.ts");
/* harmony import */ var react_router_dom__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! react-router-dom */ "./node_modules/react-router/dist/development/chunk-UVKPFVEO.mjs");
/* harmony import */ var _redux_hooks__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ~/redux/hooks */ "./source/redux/hooks.ts");
/* harmony import */ var _wordpress_i18n__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! @wordpress/i18n */ "@wordpress/i18n");
/* harmony import */ var _wordpress_i18n__WEBPACK_IMPORTED_MODULE_6___default = /*#__PURE__*/__webpack_require__.n(_wordpress_i18n__WEBPACK_IMPORTED_MODULE_6__);
/* harmony import */ var _hooks_useSettings__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! ~/hooks/useSettings */ "./source/hooks/useSettings.ts");
/* harmony import */ var _components_switcher__WEBPACK_IMPORTED_MODULE_8__ = __webpack_require__(/*! ~/components/switcher */ "./source/components/switcher/index.ts");
/* harmony import */ var _components_button__WEBPACK_IMPORTED_MODULE_9__ = __webpack_require__(/*! ~/components/button */ "./source/components/button/index.ts");
/* harmony import */ var _components_field__WEBPACK_IMPORTED_MODULE_10__ = __webpack_require__(/*! ~/components/field */ "./source/components/field/index.ts");
/* harmony import */ var _constants_docs__WEBPACK_IMPORTED_MODULE_11__ = __webpack_require__(/*! ~/constants/docs */ "./source/constants/docs.ts");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_12__ = __webpack_require__(/*! react/jsx-runtime */ "react/jsx-runtime");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_12___default = /*#__PURE__*/__webpack_require__.n(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_12__);

const Tools = () => {
  const {
    defaultData,
    data,
    setSettings,
    saveSettings
  } = (0,_hooks_useSettings__WEBPACK_IMPORTED_MODULE_7__["default"])();
  const dispatch = (0,_redux_hooks__WEBPACK_IMPORTED_MODULE_5__.useAppDispatch)();
  const {
    showAlert
  } = (0,_components_alert_Alert__WEBPACK_IMPORTED_MODULE_1__.useCustomAlert)();
  const navigate = (0,react_router_dom__WEBPACK_IMPORTED_MODULE_4__.useNavigate)();
  const {
    autoSave,
    deleteOnUninstall
  } = data?.tools || {};
  const handleExport = () => {
    const dataString = "data:text/json;charset=utf-8," + encodeURIComponent(JSON.stringify(data, null, 2));
    const downloadAnchorNode = document.createElement("a");
    downloadAnchorNode.setAttribute("href", dataString);
    downloadAnchorNode.setAttribute("download", `advanced_media_library_${new Date().getTime()}.json`);
    document.body.appendChild(downloadAnchorNode);
    downloadAnchorNode.click();
    downloadAnchorNode.remove();
    showAlert({
      toast: true,
      type: "success",
      text: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_6__.__)("Data exported successfully!", "ninja-media"),
      timer: 3000,
      timerProgressBar: true,
      showConfirmButton: false
    });
  };
  const handleImport = () => {
    const importInput = document.getElementById("pnpnm-import-input");
    importInput?.click();
  };
  const handleImportData = async event => {
    const file = event.target.files?.[0];
    if (!file) {
      console.error((0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_6__.__)("No file selected for import", "ninja-media"));
      return;
    }
    const reader = new FileReader();
    reader.onload = async e => {
      try {
        const result = e.target?.result;
        if (typeof result !== "string") {
          throw new Error("Invalid file content");
        }
        const importedData = JSON.parse(result);
        if (importedData) {
          saveSettings(importedData);
          dispatch((0,_redux_features_settings_settings__WEBPACK_IMPORTED_MODULE_0__.settingsInit)({
            defaultData: defaultData,
            data: importedData
          }));
        }
      } catch (error) {
        console.error((0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_6__.__)("Failed to import data:", "ninja-media"), error);
      }
    };
    reader.onerror = error => {
      console.error((0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_6__.__)("Error reading file:", "ninja-media"), error);
    };
    reader.readAsText(file);
    event.target.value = "";
  };
  const handleReset = () => {
    showAlert({
      type: "error",
      title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_6__.__)("Reset Settings", "ninja-media"),
      text: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_6__.__)("Are you sure you want to reset all settings?", "ninja-media"),
      showCancelButton: true,
      confirmButtonText: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_6__.__)("Reset", "ninja-media"),
      onConfirm: async () => {
        try {
          if (!defaultData) return;
          await saveSettings(defaultData);
          dispatch((0,_redux_features_settings_settings__WEBPACK_IMPORTED_MODULE_0__.settingsInit)({
            defaultData: defaultData,
            data: defaultData
          }));
          const color = defaultData?.display?.theme?.color || "#2271b1";
          const root = document.documentElement;
          if (root && color) {
            root.style.setProperty("--pnpnm-primary", color);
          }
          navigate("/settings/accounts");
        } catch (error) {
          showAlert({
            toast: true,
            type: "error",
            text: error?.data?.message || (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_6__.__)("Failed to reset settings!", "ninja-media"),
            timer: 3000,
            timerProgressBar: true,
            showConfirmButton: false
          });
        }
      }
    });
  };
  return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_12__.jsxs)(_components_pageContainer__WEBPACK_IMPORTED_MODULE_2__["default"], {
    children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_12__.jsx)(_components_field__WEBPACK_IMPORTED_MODULE_10__["default"], {
      title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_6__.__)("Cleanup Tools", "ninja-media"),
      description: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_6__.__)("Manage what happens to plugin data when the plugin is deactivated or uninstalled.", "ninja-media"),
      docLink: _constants_docs__WEBPACK_IMPORTED_MODULE_11__["default"]?.documentation?.main,
      children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_12__.jsx)(_components_settingsField__WEBPACK_IMPORTED_MODULE_3__["default"], {
        background: "extralight",
        description: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_6__.__)("Permanently remove all plugin data — folders, settings, and database tables — when the plugin is uninstalled.", "ninja-media"),
        action: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_12__.jsx)(_components_switcher__WEBPACK_IMPORTED_MODULE_8__["default"], {
          title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_6__.__)("Delete plugin data on uninstall", "ninja-media"),
          checked: deleteOnUninstall,
          onChange: () => setSettings("tools.deleteOnUninstall", !deleteOnUninstall)
        })
      })
    }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_12__.jsxs)(_components_field__WEBPACK_IMPORTED_MODULE_10__["default"], {
      title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_6__.__)("Backup & Restore", "ninja-media"),
      description: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_6__.__)("Export your settings to a file, import a saved configuration, or reset everything back to defaults.", "ninja-media"),
      docLink: _constants_docs__WEBPACK_IMPORTED_MODULE_11__["default"]?.documentation?.main,
      children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_12__.jsx)(_components_settingsField__WEBPACK_IMPORTED_MODULE_3__["default"], {
        background: "extralight",
        description: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_6__.__)("Automatically save setting changes as you make them, without needing to click the Save button.", "ninja-media"),
        action: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_12__.jsx)(_components_switcher__WEBPACK_IMPORTED_MODULE_8__["default"], {
          title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_6__.__)("Enable Auto Save", "ninja-media"),
          checked: autoSave,
          onChange: () => setSettings("tools.autoSave", !autoSave)
        })
      }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_12__.jsx)(_components_settingsField__WEBPACK_IMPORTED_MODULE_3__["default"], {
        background: "extralight",
        title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_6__.__)("Export Data:", "ninja-media"),
        description: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_6__.__)("Download all current plugin settings as a JSON file. Use this to back up your configuration or transfer it to another site.", "ninja-media"),
        secondaryAction: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_12__.jsx)(_components_button__WEBPACK_IMPORTED_MODULE_9__["default"], {
          variant: "primary",
          size: "small",
          startIcon: "output_circle",
          onClick: handleExport,
          children: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_6__.__)("Export", "ninja-media")
        })
      }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_12__.jsx)(_components_settingsField__WEBPACK_IMPORTED_MODULE_3__["default"], {
        background: "extralight",
        title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_6__.__)("Import Data:", "ninja-media"),
        description: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_6__.__)("Select the exported JSON file you would like to import. Please note that the import will replace the current data.", "ninja-media"),
        secondaryAction: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_12__.jsxs)(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_12__.Fragment, {
          children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_12__.jsx)("input", {
            id: "pnpnm-import-input",
            type: "file",
            accept: ".json",
            style: {
              display: "none"
            },
            onChange: handleImportData
          }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_12__.jsx)(_components_button__WEBPACK_IMPORTED_MODULE_9__["default"], {
            variant: "primary",
            size: "small",
            startIcon: "input_circle",
            onClick: handleImport,
            children: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_6__.__)("Import", "ninja-media")
          })]
        })
      }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_12__.jsx)(_components_settingsField__WEBPACK_IMPORTED_MODULE_3__["default"], {
        background: "extralight",
        title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_6__.__)("Reset Settings", "ninja-media"),
        description: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_6__.__)("Restore all plugin settings to their original default values. This cannot be undone.", "ninja-media"),
        secondaryAction: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_12__.jsx)(_components_button__WEBPACK_IMPORTED_MODULE_9__["default"], {
          variant: "error",
          size: "small",
          startIcon: "autorenew",
          onClick: handleReset,
          children: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_6__.__)("Reset", "ninja-media")
        })
      })]
    })]
  });
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (Tools);

/***/ },

/***/ "./source/admin/watermark/Watermark.tsx"
/*!**********************************************!*\
  !*** ./source/admin/watermark/Watermark.tsx ***!
  \**********************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _components_status_PremiumCard__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ~/components/status/PremiumCard */ "./source/components/status/PremiumCard.tsx");
/* harmony import */ var react_router_dom__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! react-router-dom */ "./node_modules/react-router/dist/development/chunk-UVKPFVEO.mjs");
/* harmony import */ var _hooks_useLocalStorage__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ~/hooks/useLocalStorage */ "./source/hooks/useLocalStorage.ts");
/* harmony import */ var _constants_menus__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ~/constants/menus */ "./source/constants/menus.ts");
/* harmony import */ var _components_toggleTheme__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ~/components/toggleTheme */ "./source/components/toggleTheme/index.ts");
/* harmony import */ var _components_inlineStack__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ~/components/inlineStack */ "./source/components/inlineStack/index.ts");
/* harmony import */ var _components_status_Upgrade__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! ~/components/status/Upgrade */ "./source/components/status/Upgrade.tsx");
/* harmony import */ var _components_blockStack__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! ~/components/blockStack */ "./source/components/blockStack/index.ts");
/* harmony import */ var _components_iconButton__WEBPACK_IMPORTED_MODULE_8__ = __webpack_require__(/*! ~/components/iconButton */ "./source/components/iconButton/index.ts");
/* harmony import */ var _components_text__WEBPACK_IMPORTED_MODULE_9__ = __webpack_require__(/*! ~/components/text */ "./source/components/text/index.ts");
/* harmony import */ var _components_sidebar__WEBPACK_IMPORTED_MODULE_10__ = __webpack_require__(/*! ~/components/sidebar */ "./source/components/sidebar/index.ts");
/* harmony import */ var _components_layout__WEBPACK_IMPORTED_MODULE_11__ = __webpack_require__(/*! ~/components/layout */ "./source/components/layout/index.ts");
/* harmony import */ var _components_topbar__WEBPACK_IMPORTED_MODULE_12__ = __webpack_require__(/*! ~/components/topbar */ "./source/components/topbar/index.ts");
/* harmony import */ var _wordpress_i18n__WEBPACK_IMPORTED_MODULE_13__ = __webpack_require__(/*! @wordpress/i18n */ "@wordpress/i18n");
/* harmony import */ var _wordpress_i18n__WEBPACK_IMPORTED_MODULE_13___default = /*#__PURE__*/__webpack_require__.n(_wordpress_i18n__WEBPACK_IMPORTED_MODULE_13__);
/* harmony import */ var _hooks_premium_only_useWatermarkActions__WEBPACK_IMPORTED_MODULE_14__ = __webpack_require__(/*! ./hooks__premium_only/useWatermarkActions */ "./source/admin/watermark/hooks__premium_only/useWatermarkActions.ts");
/* harmony import */ var _components_premium_only_Preview__WEBPACK_IMPORTED_MODULE_15__ = __webpack_require__(/*! ./components__premium_only/Preview */ "./source/admin/watermark/components__premium_only/Preview.tsx");
/* harmony import */ var _components_premium_only_Filter__WEBPACK_IMPORTED_MODULE_16__ = __webpack_require__(/*! ./components__premium_only/Filter */ "./source/admin/watermark/components__premium_only/Filter.tsx");
/* harmony import */ var _pages_premium_only_image_Image__WEBPACK_IMPORTED_MODULE_17__ = __webpack_require__(/*! ./pages__premium_only/image/Image */ "./source/admin/watermark/pages__premium_only/image/Image.tsx");
/* harmony import */ var _components_premium_only_Apply__WEBPACK_IMPORTED_MODULE_18__ = __webpack_require__(/*! ./components__premium_only/Apply */ "./source/admin/watermark/components__premium_only/Apply.tsx");
/* harmony import */ var _pages_premium_only_text_Text__WEBPACK_IMPORTED_MODULE_19__ = __webpack_require__(/*! ./pages__premium_only/text/Text */ "./source/admin/watermark/pages__premium_only/text/Text.tsx");
/* harmony import */ var _components_premium_only_Tile__WEBPACK_IMPORTED_MODULE_20__ = __webpack_require__(/*! ./components__premium_only/Tile */ "./source/admin/watermark/components__premium_only/Tile.tsx");
/* harmony import */ var _wordpress_element__WEBPACK_IMPORTED_MODULE_21__ = __webpack_require__(/*! @wordpress/element */ "@wordpress/element");
/* harmony import */ var _wordpress_element__WEBPACK_IMPORTED_MODULE_21___default = /*#__PURE__*/__webpack_require__.n(_wordpress_element__WEBPACK_IMPORTED_MODULE_21__);
/* harmony import */ var _hooks_useSettings__WEBPACK_IMPORTED_MODULE_22__ = __webpack_require__(/*! ~/hooks/useSettings */ "./source/hooks/useSettings.ts");
/* harmony import */ var _components_disabled__WEBPACK_IMPORTED_MODULE_23__ = __webpack_require__(/*! ~/components/disabled */ "./source/components/disabled/index.ts");
/* harmony import */ var _components_button__WEBPACK_IMPORTED_MODULE_24__ = __webpack_require__(/*! ~/components/button */ "./source/components/button/index.ts");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_25__ = __webpack_require__(/*! react/jsx-runtime */ "react/jsx-runtime");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_25___default = /*#__PURE__*/__webpack_require__.n(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_25__);

const Watermark = () => {
  const [theme, setTheme] = (0,_hooks_useLocalStorage__WEBPACK_IMPORTED_MODULE_2__.useLocalStorage)("pnpnm-theme-status", "light");
  const {
    menuKey
  } = (0,react_router_dom__WEBPACK_IMPORTED_MODULE_1__.useParams)();
  const navigate = (0,react_router_dom__WEBPACK_IMPORTED_MODULE_1__.useNavigate)();

  const handleMenuClick = key => {
    navigate(`/watermark/${key}`);
  };
  const topbarTitle = /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_25__.jsxs)(_components_inlineStack__WEBPACK_IMPORTED_MODULE_5__["default"], {
    gap: 10,
    children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_25__.jsx)(_components_iconButton__WEBPACK_IMPORTED_MODULE_8__["default"], {
      variant: "secondary",
      rounded: "md",
      color: "primary",
      name: _constants_menus__WEBPACK_IMPORTED_MODULE_3__.WATERMARK_MENUS?.find(({
        key
      }) => key === menuKey)?.icon
    }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_25__.jsx)(_components_text__WEBPACK_IMPORTED_MODULE_9__["default"], {
      color: "primary",
      size: "xl",
      weight: "medium",
      children: _constants_menus__WEBPACK_IMPORTED_MODULE_3__.WATERMARK_MENUS?.find(({
        key
      }) => key === menuKey)?.title
    })]
  });
  const themeButton = /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_25__.jsx)(_components_toggleTheme__WEBPACK_IMPORTED_MODULE_4__["default"], {
    theme: theme,
    setTheme: setTheme
  });
  const topbarRightContents = [themeButton];

  return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_25__.jsxs)(_components_layout__WEBPACK_IMPORTED_MODULE_11__["default"], {
    children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_25__.jsxs)(_components_sidebar__WEBPACK_IMPORTED_MODULE_10__["default"], {
      minWidth: 300,
      children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_25__.jsx)(_components_sidebar__WEBPACK_IMPORTED_MODULE_10__["default"].Menu, {
        children: _constants_menus__WEBPACK_IMPORTED_MODULE_3__.WATERMARK_MENUS?.map(({
          key,
          title,
          icon
        }, index) => /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_25__.jsx)(_components_sidebar__WEBPACK_IMPORTED_MODULE_10__["default"].Item, {
          title: title,
          icon: icon,
          active: key === menuKey,
          onClick: () => handleMenuClick(key)
        }, key ?? index))
      }), !pnpnm.isPro && /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_25__.jsx)(_components_blockStack__WEBPACK_IMPORTED_MODULE_7__["default"], {
        align: "center",
        inlineAlign: "center",
        className: "h-full",
        children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_25__.jsx)(_components_status_PremiumCard__WEBPACK_IMPORTED_MODULE_0__["default"], {})
      }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_25__.jsxs)(_components_sidebar__WEBPACK_IMPORTED_MODULE_10__["default"].Bottom, {
        children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_25__.jsx)(_components_sidebar__WEBPACK_IMPORTED_MODULE_10__["default"].HelpCenter, {}), !pnpnm.isPro && /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_25__.jsx)(_components_sidebar__WEBPACK_IMPORTED_MODULE_10__["default"].UpgradePro, {})]
      })]
    }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_25__.jsxs)(_components_layout__WEBPACK_IMPORTED_MODULE_11__["default"].Wrapper, {
      children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_25__.jsx)(_components_topbar__WEBPACK_IMPORTED_MODULE_12__["default"], {
        leftContents: [topbarTitle],
        rightContents: topbarRightContents
      }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_25__.jsxs)(_components_layout__WEBPACK_IMPORTED_MODULE_11__["default"].Content, {
        children: [!pnpnm?.isPro && /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_25__.jsx)(_components_status_Upgrade__WEBPACK_IMPORTED_MODULE_6__["default"], {}), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_25__.jsxs)(_components_disabled__WEBPACK_IMPORTED_MODULE_23__["default"], {
          depend: !enabled,
          dependOn: "enableWatermark",
          dependOnExact: true,
          style: {
            display: "flex",
            flexDirection: "row",
            alignItems: "flex-start",
            flexWrap: "nowrap",
            gap: 20
          },
          children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_25__.jsxs)(_components_blockStack__WEBPACK_IMPORTED_MODULE_7__["default"], {
            gap: 20,
            style: {
              width: "50%"
            },
            children: [renderPage(), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_25__.jsx)(_components_premium_only_Tile__WEBPACK_IMPORTED_MODULE_20__["default"], {}), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_25__.jsx)(_components_premium_only_Filter__WEBPACK_IMPORTED_MODULE_16__["default"], {}), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_25__.jsx)(_components_premium_only_Apply__WEBPACK_IMPORTED_MODULE_18__["default"], {})]
          }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_25__.jsx)(_components_premium_only_Preview__WEBPACK_IMPORTED_MODULE_15__["default"], {})]
        })]
      })]
    })]
  });
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (Watermark);

/***/ },

/***/ "./source/admin/watermark/components__premium_only/Adjust.tsx"
/*!********************************************************************!*\
  !*** ./source/admin/watermark/components__premium_only/Adjust.tsx ***!
  \********************************************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   GRID_POSITIONS: () => (/* binding */ GRID_POSITIONS),
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _components_pageContainer__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ~/components/pageContainer */ "./source/components/pageContainer/index.ts");
/* harmony import */ var _components_settingsField__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ~/components/settingsField */ "./source/components/settingsField/index.ts");
/* harmony import */ var _components_inlineStack__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ~/components/inlineStack */ "./source/components/inlineStack/index.ts");
/* harmony import */ var _components_blockStack__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ~/components/blockStack */ "./source/components/blockStack/index.ts");
/* harmony import */ var _hooks_useSettings__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ~/hooks/useSettings */ "./source/hooks/useSettings.ts");
/* harmony import */ var _components_slider__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ~/components/slider */ "./source/components/slider/index.ts");
/* harmony import */ var _components_input__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! ~/components/input */ "./source/components/input/index.ts");
/* harmony import */ var _wordpress_i18n__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! @wordpress/i18n */ "@wordpress/i18n");
/* harmony import */ var _wordpress_i18n__WEBPACK_IMPORTED_MODULE_7___default = /*#__PURE__*/__webpack_require__.n(_wordpress_i18n__WEBPACK_IMPORTED_MODULE_7__);
/* harmony import */ var _components_card__WEBPACK_IMPORTED_MODULE_8__ = __webpack_require__(/*! ~/components/card */ "./source/components/card/index.ts");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_9__ = __webpack_require__(/*! react/jsx-runtime */ "react/jsx-runtime");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_9___default = /*#__PURE__*/__webpack_require__.n(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_9__);

const Adjust = () => {
  const {
    data,
    setSettings
  } = (0,_hooks_useSettings__WEBPACK_IMPORTED_MODULE_4__["default"])();
  const {
    position,
    size,
    opacity,
    edgeMargin
  } = data?.watermark || {};
  return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_9__.jsxs)(_components_pageContainer__WEBPACK_IMPORTED_MODULE_0__["default"], {
    children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_9__.jsxs)(_components_inlineStack__WEBPACK_IMPORTED_MODULE_2__["default"], {
      gap: 20,
      wrap: false,
      blockAlign: "start",
      children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_9__.jsx)(_components_settingsField__WEBPACK_IMPORTED_MODULE_1__["default"], {
        title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_7__.__)("Position", "ninja-media"),
        children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_9__.jsx)(_components_card__WEBPACK_IMPORTED_MODULE_8__["default"], {
          padding: 10,
          rounded: "md",
          style: {
            width: "fit-content",
            display: "grid",
            gridTemplateColumns: "repeat(3, 43px)",
            gridTemplateRows: "repeat(3, 43px)",
            gap: "10px",
            padding: "10px"
          },
          children: GRID_POSITIONS?.map((pos, index) => {
            return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_9__.jsx)(_components_card__WEBPACK_IMPORTED_MODULE_8__["default"], {
              padding: 5,
              background: position === pos ? "primary" : "white",
              rounded: "md",
              flex: true,
              align: "center",
              blockAlign: "center",
              style: {
                width: 43,
                height: 43,
                cursor: "pointer"
              },
              onClick: () => setSettings("watermark.position", pos),
              children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_9__.jsx)("span", {
                style: {
                  width: 8,
                  height: 8,
                  borderRadius: "50%",
                  backgroundColor: position === pos ? "white" : "var(--pnpnm-primary)"
                }
              })
            }, pos ?? index);
          })
        })
      }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_9__.jsxs)(_components_blockStack__WEBPACK_IMPORTED_MODULE_3__["default"], {
        gap: 20,
        children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_9__.jsx)(_components_settingsField__WEBPACK_IMPORTED_MODULE_1__["default"], {
          title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_7__.__)("Size", "ninja-media"),
          children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_9__.jsx)(_components_input__WEBPACK_IMPORTED_MODULE_6__["default"], {
            type: "number",
            suffix: "%",
            size: "small",
            value: size ?? "",
            onChange: value => setSettings("watermark.size", Number(value))
          })
        }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_9__.jsx)(_components_settingsField__WEBPACK_IMPORTED_MODULE_1__["default"], {
          title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_7__.__)("Opacity", "ninja-media"),
          children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_9__.jsx)(_components_slider__WEBPACK_IMPORTED_MODULE_5__["default"], {
            value: opacity ?? 0,
            onChange: value => {
              setSettings("watermark.opacity", value);
            }
          })
        })]
      })]
    }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_9__.jsx)(_components_settingsField__WEBPACK_IMPORTED_MODULE_1__["default"], {
      fullWidth: true,
      title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_7__.__)("Edge margin", "ninja-media"),
      description: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_7__.__)("Set the margin between the watermark and the edges of the image.", "ninja-media"),
      secondaryAction: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_9__.jsx)(_components_input__WEBPACK_IMPORTED_MODULE_6__["default"], {
        type: "number",
        suffix: "px",
        size: "small",
        value: edgeMargin ?? "",
        onChange: value => setSettings("watermark.edgeMargin", Number(value)),
        fullWidth: false,
        className: "flex-1"
      })
    })]
  });
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (Adjust);
const GRID_POSITIONS = ["top-left", "top-center", "top-right", "center-left", "center", "center-right", "bottom-left", "bottom-center", "bottom-right"];

/***/ },

/***/ "./source/admin/watermark/components__premium_only/Apply.tsx"
/*!*******************************************************************!*\
  !*** ./source/admin/watermark/components__premium_only/Apply.tsx ***!
  \*******************************************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _wordpress_element__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @wordpress/element */ "@wordpress/element");
/* harmony import */ var _wordpress_element__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(_wordpress_element__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var _components_pageContainer__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ~/components/pageContainer */ "./source/components/pageContainer/index.ts");
/* harmony import */ var _components_settingsField__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ~/components/settingsField */ "./source/components/settingsField/index.ts");
/* harmony import */ var _components_inlineStack__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ~/components/inlineStack */ "./source/components/inlineStack/index.ts");
/* harmony import */ var _components_description__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ~/components/description */ "./source/components/description/index.ts");
/* harmony import */ var _wordpress_i18n__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! @wordpress/i18n */ "@wordpress/i18n");
/* harmony import */ var _wordpress_i18n__WEBPACK_IMPORTED_MODULE_5___default = /*#__PURE__*/__webpack_require__.n(_wordpress_i18n__WEBPACK_IMPORTED_MODULE_5__);
/* harmony import */ var _components_blockStack__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! ~/components/blockStack */ "./source/components/blockStack/index.ts");
/* harmony import */ var _hooks_useSettings__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! ~/hooks/useSettings */ "./source/hooks/useSettings.ts");
/* harmony import */ var _components_switcher__WEBPACK_IMPORTED_MODULE_8__ = __webpack_require__(/*! ~/components/switcher */ "./source/components/switcher/index.ts");
/* harmony import */ var _components_button__WEBPACK_IMPORTED_MODULE_9__ = __webpack_require__(/*! ~/components/button */ "./source/components/button/index.ts");
/* harmony import */ var _components_text__WEBPACK_IMPORTED_MODULE_10__ = __webpack_require__(/*! ~/components/text */ "./source/components/text/index.ts");
/* harmony import */ var _redux_api_watermark_premium_only__WEBPACK_IMPORTED_MODULE_11__ = __webpack_require__(/*! ~/redux/api/watermark__premium_only */ "./source/redux/api/watermark__premium_only.ts");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_12__ = __webpack_require__(/*! react/jsx-runtime */ "react/jsx-runtime");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_12___default = /*#__PURE__*/__webpack_require__.n(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_12__);

const BATCH_SIZE = 5;
const Apply = () => {
  const {
    data,
    setSettings,
    saveSettings
  } = (0,_hooks_useSettings__WEBPACK_IMPORTED_MODULE_7__["default"])();
  const [fetchEligibleIds] = (0,_redux_api_watermark_premium_only__WEBPACK_IMPORTED_MODULE_11__.useLazyGetEligibleIdsQuery)();
  const [applyBatch] = (0,_redux_api_watermark_premium_only__WEBPACK_IMPORTED_MODULE_11__.useApplyWatermarkBatchMutation)();
  const [restoreOriginals] = (0,_redux_api_watermark_premium_only__WEBPACK_IMPORTED_MODULE_11__.useRestoreOriginalsMutation)();
  const [bulkState, setBulkState] = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useState)({
    running: false,
    total: 0,
    processed: 0,
    success: 0,
    done: false
  });
  const [restoreState, setRestoreState] = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useState)({
    running: false,
    done: false,
    count: 0
  });
  const cancelRef = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useRef)(false);
  const {
    type,
    image,
    text,
    backup,
    applyOnUpload
  } = data?.watermark || {};
  const startBulkApply = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useCallback)(async () => {
    if (!pnpnm?.isPro || !data?.watermark?.enabled) return;
    if (bulkState.running) return;
    cancelRef.current = false;
    setBulkState({
      running: true,
      total: 0,
      processed: 0,
      success: 0,
      done: false
    });
    try {
      if (data) await saveSettings(data);
      const {
        data: eligibleData
      } = await fetchEligibleIds();
      const allIds = eligibleData?.data?.ids ?? [];
      if (allIds.length === 0) {
        setBulkState(s => ({
          ...s,
          running: false,
          done: true
        }));
        return;
      }
      setBulkState(s => ({
        ...s,
        total: allIds.length
      }));
      let processed = 0;
      let success = 0;
      for (let i = 0; i < allIds.length; i += BATCH_SIZE) {
        if (cancelRef.current) break;
        const chunk = allIds.slice(i, i + BATCH_SIZE);
        const res = await applyBatch({
          ids: chunk
        }).unwrap();
        processed += res?.data?.processed ?? chunk.length;
        success += res?.data?.successCount ?? 0;
        setBulkState(s => ({
          ...s,
          processed,
          success
        }));
      }
      setBulkState(s => ({
        ...s,
        running: false,
        done: true
      }));
    } catch {
      setBulkState(s => ({
        ...s,
        running: false,
        done: true
      }));
    }
  }, [data, bulkState.running, saveSettings, fetchEligibleIds, applyBatch]);
  const cancelBulk = () => {
    cancelRef.current = true;
    setBulkState(s => ({
      ...s,
      running: false,
      done: true
    }));
  };
  const startBulkRestore = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useCallback)(async () => {
    if (!pnpnm?.isPro || !data?.watermark?.enabled) return;
    if (restoreState.running) return;
    setRestoreState({
      running: true,
      done: false,
      count: 0
    });
    try {
      const {
        data: eligibleData
      } = await fetchEligibleIds();
      const allIds = eligibleData?.data?.ids ?? [];
      if (allIds.length === 0) {
        setRestoreState({
          running: false,
          done: true,
          count: 0
        });
        return;
      }
      let restoredCount = 0;
      for (let i = 0; i < allIds.length; i += BATCH_SIZE) {
        const chunk = allIds.slice(i, i + BATCH_SIZE);
        const res = await restoreOriginals({
          ids: chunk
        }).unwrap();
        restoredCount += res?.data?.results.filter(r => r.success).length ?? 0;
      }
      setRestoreState({
        running: false,
        done: true,
        count: restoredCount
      });
    } catch {
      setRestoreState({
        running: false,
        done: true,
        count: 0
      });
    }
  }, [restoreState.running, fetchEligibleIds, restoreOriginals]);
  const progress = bulkState.total > 0 ? Math.round(bulkState.processed / bulkState.total * 100) : 0;
  const hasWatermarkSource = type === "image" ? !!image : !!text?.content?.trim();
  return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_12__.jsxs)(_components_pageContainer__WEBPACK_IMPORTED_MODULE_1__["default"], {
    children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_12__.jsx)(_components_settingsField__WEBPACK_IMPORTED_MODULE_2__["default"], {
      description: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_5__.__)("Automatically apply the watermark to new images as they are uploaded to the media library.", "ninja-media"),
      action: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_12__.jsx)(_components_switcher__WEBPACK_IMPORTED_MODULE_8__["default"], {
        id: "watermarkApplyOnUpload",
        title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_5__.__)("Apply Watermark on Upload", "ninja-media"),
        checked: applyOnUpload ?? false,
        onChange: () => setSettings("watermark.applyOnUpload", !applyOnUpload)
      })
    }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_12__.jsx)(_components_settingsField__WEBPACK_IMPORTED_MODULE_2__["default"], {
      description: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_5__.__)("Enable this option to keep a backup of the original image before applying the watermark.", "ninja-media"),
      action: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_12__.jsx)(_components_switcher__WEBPACK_IMPORTED_MODULE_8__["default"], {
        id: "watermarkBackup",
        title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_5__.__)("Keep Original Backup", "ninja-media"),
        checked: backup,
        onChange: () => setSettings("watermark.backup", !backup)
      }),
      children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_12__.jsx)(_components_settingsField__WEBPACK_IMPORTED_MODULE_2__["default"].SubField, {
        depend: !backup,
        dependOn: "watermarkBackup",
        title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_5__.__)("Restore Original", "ninja-media"),
        description: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_5__.__)("Restore the original image from the backup. This will remove the watermark from the image.", "ninja-media"),
        secondaryAction: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_12__.jsxs)(_components_inlineStack__WEBPACK_IMPORTED_MODULE_3__["default"], {
          gap: 10,
          wrap: false,
          children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_12__.jsx)(_components_button__WEBPACK_IMPORTED_MODULE_9__["default"], {
            variant: "primary",
            size: "small",
            startIcon: "restore",
            onClick: startBulkRestore,
            loading: restoreState.running,
            children: restoreState.running ? (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_5__.__)("Restoring…", "ninja-media") : (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_5__.__)("Restore", "ninja-media")
          }), restoreState.done && /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_12__.jsx)(_components_text__WEBPACK_IMPORTED_MODULE_10__["default"], {
            color: "descgray",
            size: "sm",
            children: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_5__.sprintf)((0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_5__._n)("Restored %d image.", "Restored %d images.", restoreState.count, "ninja-media"), restoreState.count)
          })]
        })
      })
    }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_12__.jsxs)(_components_settingsField__WEBPACK_IMPORTED_MODULE_2__["default"], {
      gap: 10,
      title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_5__.__)("Apply Watermark to Existing Media", "ninja-media"),
      children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_12__.jsxs)(_components_inlineStack__WEBPACK_IMPORTED_MODULE_3__["default"], {
        gap: 10,
        wrap: false,
        children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_12__.jsx)(_components_button__WEBPACK_IMPORTED_MODULE_9__["default"], {
          variant: "warning",
          startIcon: bulkState.running ? "sync" : "play_arrow",
          className: "flex-1",
          onClick: startBulkApply,
          loading: bulkState.running,
          disabled: !hasWatermarkSource,
          children: bulkState.running ? (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_5__.sprintf)((0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_5__.__)("Applying… %d%%", "ninja-media"), progress) : (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_5__.__)("Apply Watermark", "ninja-media")
        }), bulkState.running && /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_12__.jsx)(_components_button__WEBPACK_IMPORTED_MODULE_9__["default"], {
          variant: "outlined",
          startIcon: "stop_circle",
          onClick: cancelBulk,
          children: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_5__.__)("Cancel", "ninja-media")
        })]
      }), (bulkState.running || bulkState.done) && /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_12__.jsxs)(_components_blockStack__WEBPACK_IMPORTED_MODULE_6__["default"], {
        gap: 5,
        children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_12__.jsx)("div", {
          style: {
            width: "100%",
            height: 8,
            background: "#e5e7eb",
            borderRadius: 4,
            overflow: "hidden"
          },
          children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_12__.jsx)("div", {
            style: {
              width: `${progress}%`,
              height: "100%",
              background: "var(--pnpnm-primary, #2271b1)",
              borderRadius: 4,
              transition: "width 0.3s ease"
            }
          })
        }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_12__.jsxs)(_components_text__WEBPACK_IMPORTED_MODULE_10__["default"], {
          color: "descgray",
          size: "sm",
          children: [(0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_5__.sprintf)((0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_5__.__)("%1$d / %2$d processed", "ninja-media"), bulkState.processed, bulkState.total), bulkState.done ? ` — ${(0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_5__.sprintf)((0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_5__.__)("%d succeeded", "ninja-media"), bulkState.success)}` : ""]
        })]
      }), !hasWatermarkSource && /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_12__.jsx)(_components_description__WEBPACK_IMPORTED_MODULE_4__["default"], {
        text: type === "image" ? (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_5__.__)("Please select a watermark image first.", "ninja-media") : (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_5__.__)("Please enter watermark text first.", "ninja-media")
      }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_12__.jsx)(_components_description__WEBPACK_IMPORTED_MODULE_4__["default"], {
        text: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_5__.__)("This will apply the watermark to all existing media items in the library that match the specified conditions. This action cannot be undone, so it's recommended to keep a backup of your original images.", "ninja-media")
      })]
    })]
  });
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (Apply);

/***/ },

/***/ "./source/admin/watermark/components__premium_only/Condition.tsx"
/*!***********************************************************************!*\
  !*** ./source/admin/watermark/components__premium_only/Condition.tsx ***!
  \***********************************************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   POST_TYPE_OPTIONS: () => (/* binding */ POST_TYPE_OPTIONS),
/* harmony export */   ROLE_OPTIONS: () => (/* binding */ ROLE_OPTIONS),
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _components_settingsField__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ~/components/settingsField */ "./source/components/settingsField/index.ts");
/* harmony import */ var _components_inlineStack__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ~/components/inlineStack */ "./source/components/inlineStack/index.ts");
/* harmony import */ var _components_blockStack__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ~/components/blockStack */ "./source/components/blockStack/index.ts");
/* harmony import */ var _components_selectBox__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ~/components/selectBox */ "./source/components/selectBox/index.ts");
/* harmony import */ var _hooks_useSettings__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ~/hooks/useSettings */ "./source/hooks/useSettings.ts");
/* harmony import */ var _components_switcher__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ~/components/switcher */ "./source/components/switcher/index.ts");
/* harmony import */ var _components_disabled__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! ~/components/disabled */ "./source/components/disabled/index.ts");
/* harmony import */ var _components_input__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! ~/components/input */ "./source/components/input/index.ts");
/* harmony import */ var _components_text__WEBPACK_IMPORTED_MODULE_8__ = __webpack_require__(/*! ~/components/text */ "./source/components/text/index.ts");
/* harmony import */ var _wordpress_i18n__WEBPACK_IMPORTED_MODULE_9__ = __webpack_require__(/*! @wordpress/i18n */ "@wordpress/i18n");
/* harmony import */ var _wordpress_i18n__WEBPACK_IMPORTED_MODULE_9___default = /*#__PURE__*/__webpack_require__.n(_wordpress_i18n__WEBPACK_IMPORTED_MODULE_9__);
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_10__ = __webpack_require__(/*! react/jsx-runtime */ "react/jsx-runtime");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_10___default = /*#__PURE__*/__webpack_require__.n(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_10__);

const Condition = () => {
  const {
    data,
    setSettings
  } = (0,_hooks_useSettings__WEBPACK_IMPORTED_MODULE_4__["default"])();
  const {
    enabled,
    minWidth,
    minHeight,
    minFileSize,
    skipRoles,
    postTypes
  } = data?.watermark?.conditions || {};
  return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_10__.jsx)(_components_settingsField__WEBPACK_IMPORTED_MODULE_0__["default"], {
    description: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_9__.__)("Enable conditional rules to apply the watermark based on specific criteria.", "ninja-media"),
    action: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_10__.jsx)(_components_switcher__WEBPACK_IMPORTED_MODULE_5__["default"], {
      id: "watermarkCondition",
      title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_9__.__)("Conditional Rules", "ninja-media"),
      checked: enabled,
      onChange: () => setSettings("watermark.conditions.enabled", !enabled)
    }),
    children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_10__.jsxs)(_components_disabled__WEBPACK_IMPORTED_MODULE_6__["default"], {
      depend: !enabled,
      dependOn: "watermarkCondition",
      children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_10__.jsxs)(_components_inlineStack__WEBPACK_IMPORTED_MODULE_1__["default"], {
        gap: 10,
        wrap: false,
        children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_10__.jsxs)(_components_blockStack__WEBPACK_IMPORTED_MODULE_2__["default"], {
          gap: 10,
          className: "flex-1",
          children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_10__.jsx)(_components_text__WEBPACK_IMPORTED_MODULE_8__["default"], {
            size: "sm",
            weight: "medium",
            children: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_9__.__)("Min image width", "ninja-media")
          }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_10__.jsx)(_components_input__WEBPACK_IMPORTED_MODULE_7__["default"], {
            type: "number",
            size: "small",
            suffix: "px",
            value: minWidth ?? "",
            onChange: value => setSettings("watermark.conditions.minWidth", Number(value))
          })]
        }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_10__.jsxs)(_components_blockStack__WEBPACK_IMPORTED_MODULE_2__["default"], {
          gap: 10,
          className: "flex-1",
          children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_10__.jsx)(_components_text__WEBPACK_IMPORTED_MODULE_8__["default"], {
            size: "sm",
            weight: "medium",
            children: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_9__.__)("Min image height", "ninja-media")
          }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_10__.jsx)(_components_input__WEBPACK_IMPORTED_MODULE_7__["default"], {
            type: "number",
            size: "small",
            suffix: "px",
            value: minHeight ?? "",
            onChange: value => setSettings("watermark.conditions.minHeight", Number(value))
          })]
        }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_10__.jsxs)(_components_blockStack__WEBPACK_IMPORTED_MODULE_2__["default"], {
          gap: 10,
          className: "flex-1",
          children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_10__.jsx)(_components_text__WEBPACK_IMPORTED_MODULE_8__["default"], {
            size: "sm",
            weight: "medium",
            children: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_9__.__)("Min file size", "ninja-media")
          }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_10__.jsx)(_components_input__WEBPACK_IMPORTED_MODULE_7__["default"], {
            type: "number",
            size: "small",
            suffix: "KB",
            value: minFileSize ?? "",
            onChange: value => setSettings("watermark.conditions.minFileSize", Number(value))
          })]
        })]
      }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_10__.jsxs)(_components_inlineStack__WEBPACK_IMPORTED_MODULE_1__["default"], {
        gap: 10,
        wrap: false,
        blockAlign: "start",
        children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_10__.jsxs)(_components_blockStack__WEBPACK_IMPORTED_MODULE_2__["default"], {
          gap: 10,
          className: "flex-1",
          children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_10__.jsx)(_components_text__WEBPACK_IMPORTED_MODULE_8__["default"], {
            size: "sm",
            weight: "medium",
            children: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_9__.__)("Skip for user roles", "ninja-media")
          }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_10__.jsx)(_components_selectBox__WEBPACK_IMPORTED_MODULE_3__["default"], {
            multiple: true,
            size: "small",
            options: ROLE_OPTIONS,
            value: skipRoles,
            onChange: value => setSettings("watermark.conditions.skipRoles", value)
          })]
        }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_10__.jsxs)(_components_blockStack__WEBPACK_IMPORTED_MODULE_2__["default"], {
          gap: 10,
          className: "flex-1",
          children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_10__.jsx)(_components_text__WEBPACK_IMPORTED_MODULE_8__["default"], {
            size: "sm",
            weight: "medium",
            children: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_9__.__)("Post types", "ninja-media")
          }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_10__.jsx)(_components_selectBox__WEBPACK_IMPORTED_MODULE_3__["default"], {
            multiple: true,
            size: "small",
            options: POST_TYPE_OPTIONS,
            value: postTypes,
            onChange: value => setSettings("watermark.conditions.postTypes", value)
          })]
        })]
      })]
    })
  });
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (Condition);
const ROLE_OPTIONS = [{
  name: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_9__.__)("Administrator", "ninja-media"),
  value: "administrator"
}, {
  name: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_9__.__)("Editor", "ninja-media"),
  value: "editor"
}, {
  name: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_9__.__)("Author", "ninja-media"),
  value: "author"
}, {
  name: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_9__.__)("Contributor", "ninja-media"),
  value: "contributor"
}, {
  name: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_9__.__)("Subscriber", "ninja-media"),
  value: "subscriber"
}];
const POST_TYPE_OPTIONS = [{
  name: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_9__.__)("Page", "ninja-media"),
  value: "page"
}, {
  name: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_9__.__)("Post", "ninja-media"),
  value: "post"
}, {
  name: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_9__.__)("Product (WooCommerce)", "ninja-media"),
  value: "product"
}, {
  name: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_9__.__)("Attachment", "ninja-media"),
  value: "attachment"
}];

/***/ },

/***/ "./source/admin/watermark/components__premium_only/Filter.tsx"
/*!********************************************************************!*\
  !*** ./source/admin/watermark/components__premium_only/Filter.tsx ***!
  \********************************************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   APPLY_ON_BUTTONS: () => (/* binding */ APPLY_ON_BUTTONS),
/* harmony export */   MIME_OPTIONS: () => (/* binding */ MIME_OPTIONS),
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _components_pageContainer__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ~/components/pageContainer */ "./source/components/pageContainer/index.ts");
/* harmony import */ var _components_settingsField__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ~/components/settingsField */ "./source/components/settingsField/index.ts");
/* harmony import */ var _components_description__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ~/components/description */ "./source/components/description/index.ts");
/* harmony import */ var _components_buttonGroup__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ~/components/buttonGroup */ "./source/components/buttonGroup/index.ts");
/* harmony import */ var _components_inlineStack__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ~/components/inlineStack */ "./source/components/inlineStack/index.ts");
/* harmony import */ var _components_selectBox__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ~/components/selectBox */ "./source/components/selectBox/index.ts");
/* harmony import */ var _hooks_useSettings__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! ~/hooks/useSettings */ "./source/hooks/useSettings.ts");
/* harmony import */ var _components_switcher__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! ~/components/switcher */ "./source/components/switcher/index.ts");
/* harmony import */ var _components_disabled__WEBPACK_IMPORTED_MODULE_8__ = __webpack_require__(/*! ~/components/disabled */ "./source/components/disabled/index.ts");
/* harmony import */ var _components_input__WEBPACK_IMPORTED_MODULE_9__ = __webpack_require__(/*! ~/components/input */ "./source/components/input/index.ts");
/* harmony import */ var _wordpress_i18n__WEBPACK_IMPORTED_MODULE_10__ = __webpack_require__(/*! @wordpress/i18n */ "@wordpress/i18n");
/* harmony import */ var _wordpress_i18n__WEBPACK_IMPORTED_MODULE_10___default = /*#__PURE__*/__webpack_require__.n(_wordpress_i18n__WEBPACK_IMPORTED_MODULE_10__);
/* harmony import */ var _components_text__WEBPACK_IMPORTED_MODULE_11__ = __webpack_require__(/*! ~/components/text */ "./source/components/text/index.ts");
/* harmony import */ var _Condition__WEBPACK_IMPORTED_MODULE_12__ = __webpack_require__(/*! ./Condition */ "./source/admin/watermark/components__premium_only/Condition.tsx");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_13__ = __webpack_require__(/*! react/jsx-runtime */ "react/jsx-runtime");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_13___default = /*#__PURE__*/__webpack_require__.n(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_13__);

const Filter = () => {
  const {
    data,
    setSettings
  } = (0,_hooks_useSettings__WEBPACK_IMPORTED_MODULE_6__["default"])();
  const {
    applyOn,
    allowedExtensions,
    allowAll,
    except,
    jpegQuality
  } = data?.watermark || {};
  return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_13__.jsxs)(_components_pageContainer__WEBPACK_IMPORTED_MODULE_0__["default"], {
    children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_13__.jsxs)(_components_settingsField__WEBPACK_IMPORTED_MODULE_1__["default"], {
      title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_10__.__)("Apply On", "ninja-media"),
      gap: 10,
      children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_13__.jsx)(_components_buttonGroup__WEBPACK_IMPORTED_MODULE_3__["default"], {
        background: "extralight",
        buttons: APPLY_ON_BUTTONS,
        selectedKey: applyOn || "all",
        onChange: value => setSettings("watermark.applyOn", value)
      }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_13__.jsx)(_components_description__WEBPACK_IMPORTED_MODULE_2__["default"], {
        text: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_10__.__)("Choose the image sizes that the watermark should be applied to.", "ninja-media")
      })]
    }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_13__.jsx)(_components_settingsField__WEBPACK_IMPORTED_MODULE_1__["default"], {
      title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_10__.__)("Allowed file types", "ninja-media"),
      description: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_10__.__)("Select the file types that the watermark should be applied to.", "ninja-media"),
      secondaryAction: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_13__.jsx)(_components_selectBox__WEBPACK_IMPORTED_MODULE_5__["default"], {
        multiple: true,
        size: "small",
        options: MIME_OPTIONS,
        value: allowedExtensions,
        onChange: value => setSettings("watermark.allowedExtensions", value)
      }),
      children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_13__.jsx)(_components_settingsField__WEBPACK_IMPORTED_MODULE_1__["default"].SubField, {
        action: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_13__.jsx)(_components_switcher__WEBPACK_IMPORTED_MODULE_7__["default"], {
          id: "watermarkAllowAll",
          title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_10__.__)("Allow all types", "ninja-media"),
          checked: allowAll,
          onChange: () => setSettings("watermark.allowAll", !allowAll)
        }),
        children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_13__.jsxs)(_components_disabled__WEBPACK_IMPORTED_MODULE_8__["default"], {
          gap: 10,
          depend: !allowAll,
          dependOn: "watermarkAllowAll",
          children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_13__.jsxs)(_components_inlineStack__WEBPACK_IMPORTED_MODULE_4__["default"], {
            gap: 10,
            wrap: false,
            children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_13__.jsx)(_components_text__WEBPACK_IMPORTED_MODULE_11__["default"], {
              weight: "medium",
              children: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_10__.__)("Except", "ninja-media")
            }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_13__.jsx)(_components_selectBox__WEBPACK_IMPORTED_MODULE_5__["default"], {
              multiple: true,
              size: "small",
              options: MIME_OPTIONS,
              value: except,
              onChange: value => setSettings("watermark.except", value)
            })]
          }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_13__.jsx)(_components_description__WEBPACK_IMPORTED_MODULE_2__["default"], {
            text: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_10__.__)("Select the file types that should be excluded from the watermark.", "ninja-media")
          })]
        })
      })
    }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_13__.jsx)(_Condition__WEBPACK_IMPORTED_MODULE_12__["default"], {}), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_13__.jsx)(_components_settingsField__WEBPACK_IMPORTED_MODULE_1__["default"], {
      title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_10__.__)("JPEG / WebP quality", "ninja-media"),
      description: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_10__.__)("Output compression quality for JPEG and WebP formats (1–100). Higher = better quality, larger file.", "ninja-media"),
      secondaryAction: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_13__.jsx)(_components_input__WEBPACK_IMPORTED_MODULE_9__["default"], {
        type: "number",
        size: "small",
        suffix: "%",
        fullWidth: false,
        value: jpegQuality ?? "",
        onChange: value => setSettings("watermark.jpegQuality", Number(value))
      })
    })]
  });
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (Filter);
const APPLY_ON_BUTTONS = [{
  key: "all",
  title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_10__.__)("All Sizes", "ninja-media"),
  startIcon: "grid_view"
}, {
  key: "thumbnail",
  title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_10__.__)("Thumbnail", "ninja-media"),
  startIcon: "photo_size_select_small"
}, {
  key: "medium",
  title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_10__.__)("Medium", "ninja-media"),
  startIcon: "photo_size_select_small"
}, {
  key: "large",
  title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_10__.__)("Large", "ninja-media"),
  startIcon: "photo_size_select_large"
}, {
  key: "full",
  title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_10__.__)("Full Size", "ninja-media"),
  startIcon: "image"
}];
const MIME_OPTIONS = [{
  name: "JPEG",
  value: "image/jpeg"
}, {
  name: "PNG",
  value: "image/png"
}, {
  name: "GIF",
  value: "image/gif"
}, {
  name: "WebP",
  value: "image/webp"
}];

/***/ },

/***/ "./source/admin/watermark/components__premium_only/Fonts.tsx"
/*!*******************************************************************!*\
  !*** ./source/admin/watermark/components__premium_only/Fonts.tsx ***!
  \*******************************************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _wordpress_element__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @wordpress/element */ "@wordpress/element");
/* harmony import */ var _wordpress_element__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(_wordpress_element__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var _components_settingsField__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ~/components/settingsField */ "./source/components/settingsField/index.ts");
/* harmony import */ var _components_description__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ~/components/description */ "./source/components/description/index.ts");
/* harmony import */ var _components_inlineStack__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ~/components/inlineStack */ "./source/components/inlineStack/index.ts");
/* harmony import */ var _components_blockStack__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ~/components/blockStack */ "./source/components/blockStack/index.ts");
/* harmony import */ var _hooks_useSettings__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ~/hooks/useSettings */ "./source/hooks/useSettings.ts");
/* harmony import */ var _wordpress_i18n__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! @wordpress/i18n */ "@wordpress/i18n");
/* harmony import */ var _wordpress_i18n__WEBPACK_IMPORTED_MODULE_6___default = /*#__PURE__*/__webpack_require__.n(_wordpress_i18n__WEBPACK_IMPORTED_MODULE_6__);
/* harmony import */ var _components_switcher__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! ~/components/switcher */ "./source/components/switcher/index.ts");
/* harmony import */ var _components_button__WEBPACK_IMPORTED_MODULE_8__ = __webpack_require__(/*! ~/components/button */ "./source/components/button/index.ts");
/* harmony import */ var _components_input__WEBPACK_IMPORTED_MODULE_9__ = __webpack_require__(/*! ~/components/input */ "./source/components/input/index.ts");
/* harmony import */ var _components_text__WEBPACK_IMPORTED_MODULE_10__ = __webpack_require__(/*! ~/components/text */ "./source/components/text/index.ts");
/* harmony import */ var _components_icon__WEBPACK_IMPORTED_MODULE_11__ = __webpack_require__(/*! ~/components/icon */ "./source/components/icon/index.ts");
/* harmony import */ var _redux_api_watermark_premium_only__WEBPACK_IMPORTED_MODULE_12__ = __webpack_require__(/*! ~/redux/api/watermark__premium_only */ "./source/redux/api/watermark__premium_only.ts");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_13__ = __webpack_require__(/*! react/jsx-runtime */ "react/jsx-runtime");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_13___default = /*#__PURE__*/__webpack_require__.n(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_13__);

const Fonts = () => {
  const {
    data,
    setSettings
  } = (0,_hooks_useSettings__WEBPACK_IMPORTED_MODULE_5__["default"])();
  const {
    data: fontsData
  } = (0,_redux_api_watermark_premium_only__WEBPACK_IMPORTED_MODULE_12__.useGetWatermarkFontsQuery)();
  const [uploadFont, {
    isLoading: isUploading
  }] = (0,_redux_api_watermark_premium_only__WEBPACK_IMPORTED_MODULE_12__.useUploadWatermarkFontMutation)();
  const [deleteFont, {
    isLoading: isDeleting
  }] = (0,_redux_api_watermark_premium_only__WEBPACK_IMPORTED_MODULE_12__.useDeleteWatermarkFontMutation)();
  const fileInputRef = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useRef)(null);
  const [uploadError, setUploadError] = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useState)("");
  const [uploadSuccess, setUploadSuccess] = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useState)("");
  const {
    enabled,
    name
  } = data?.watermark?.customFonts || {};
  const handleFontFileChange = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useCallback)(e => {
    const file = e.target.files?.[0];
    if (!file) return;
    if (!name?.trim()) {
      const base = file.name.replace(/\.[^.]+$/, "").replace(/[-_]/g, " ");
      setSettings("watermark.customFonts.name", base.charAt(0).toUpperCase() + base.slice(1));
    }
  }, [setSettings, name]);
  const handleFontUpload = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useCallback)(async () => {
    if (!pnpnm?.isPro || !data?.watermark?.enabled) return;
    const file = fileInputRef.current?.files?.[0];
    if (!file) {
      setUploadError((0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_6__.__)("Please select a font file first.", "ninja-media"));
      return;
    }
    setUploadError("");
    setUploadSuccess("");
    const formData = new FormData();
    formData.append("font", file);
    formData.append("name", name?.trim() || file.name.replace(/\.[^.]+$/, ""));
    try {
      const result = await uploadFont(formData).unwrap();
      setUploadSuccess((0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_6__.sprintf)((0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_6__.__)('"%s" uploaded successfully.', "ninja-media"), result?.data?.font.name ?? ""));
      setSettings("watermark.customFonts.name", "");
      if (fileInputRef.current) fileInputRef.current.value = "";
    } catch (err) {
      const msg = err?.data?.message ?? (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_6__.__)("Upload failed. Please try again.", "ninja-media");
      setUploadError(msg);
    }
  }, [uploadFont, name, setSettings]);
  const handleFontDelete = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useCallback)(async slug => {
    if (!window.confirm((0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_6__.__)("Remove this custom font?", "ninja-media"))) return;
    try {
      await deleteFont({
        slug
      }).unwrap();
    } catch {}
  }, [deleteFont]);
  return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_13__.jsx)(_components_settingsField__WEBPACK_IMPORTED_MODULE_1__["default"], {
    description: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_6__.__)("Enable custom font for the watermark text.", "ninja-media"),
    action: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_13__.jsx)(_components_switcher__WEBPACK_IMPORTED_MODULE_7__["default"], {
      id: "customFonts",
      title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_6__.__)("Custom Fonts", "ninja-media"),
      checked: enabled,
      onChange: () => setSettings("watermark.customFonts.enabled", !enabled)
    }),
    children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_13__.jsxs)(_components_settingsField__WEBPACK_IMPORTED_MODULE_1__["default"].SubField, {
      depend: !enabled,
      dependOn: "customFonts",
      children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_13__.jsxs)(_components_inlineStack__WEBPACK_IMPORTED_MODULE_3__["default"], {
        gap: 10,
        wrap: false,
        children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_13__.jsxs)(_components_blockStack__WEBPACK_IMPORTED_MODULE_4__["default"], {
          gap: 10,
          className: "flex-1",
          children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_13__.jsx)(_components_text__WEBPACK_IMPORTED_MODULE_10__["default"], {
            size: "sm",
            weight: "medium",
            children: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_6__.__)("Font Name", "ninja-media")
          }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_13__.jsx)(_components_input__WEBPACK_IMPORTED_MODULE_9__["default"], {
            size: "small",
            value: name ?? "",
            onChange: value => setSettings("watermark.customFonts.name", String(value))
          })]
        }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_13__.jsxs)(_components_blockStack__WEBPACK_IMPORTED_MODULE_4__["default"], {
          gap: 10,
          children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_13__.jsx)(_components_text__WEBPACK_IMPORTED_MODULE_10__["default"], {
            size: "sm",
            weight: "medium",
            children: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_6__.__)("Font file (.ttf / .otf / .woff / .woff2)", "ninja-media")
          }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_13__.jsxs)(_components_inlineStack__WEBPACK_IMPORTED_MODULE_3__["default"], {
            gap: 10,
            children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_13__.jsx)("input", {
              ref: fileInputRef,
              type: "file",
              accept: ".ttf,.otf,.woff,.woff2",
              style: {
                display: "none"
              },
              onChange: handleFontFileChange
            }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_13__.jsx)(_components_button__WEBPACK_IMPORTED_MODULE_8__["default"], {
              variant: "outlined",
              size: "small",
              startIcon: "upload",
              onClick: () => fileInputRef.current?.click(),
              children: fileInputRef.current?.files?.[0]?.name ?? (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_6__.__)("Choose file", "ninja-media")
            }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_13__.jsx)(_components_button__WEBPACK_IMPORTED_MODULE_8__["default"], {
              variant: "primary",
              size: "small",
              startIcon: "add",
              onClick: handleFontUpload,
              loading: isUploading,
              children: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_6__.__)("Add Font", "ninja-media")
            })]
          })]
        })]
      }), uploadError && /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_13__.jsx)(_components_text__WEBPACK_IMPORTED_MODULE_10__["default"], {
        color: "error",
        size: "sm",
        children: uploadError
      }), uploadSuccess && /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_13__.jsx)(_components_text__WEBPACK_IMPORTED_MODULE_10__["default"], {
        color: "primary",
        size: "sm",
        children: uploadSuccess
      }), (fontsData?.data?.custom ?? []).length > 0 && /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_13__.jsxs)(_components_blockStack__WEBPACK_IMPORTED_MODULE_4__["default"], {
        gap: 10,
        children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_13__.jsx)(_components_text__WEBPACK_IMPORTED_MODULE_10__["default"], {
          size: "sm",
          weight: "medium",
          children: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_6__.__)("Uploaded fonts", "ninja-media")
        }), (fontsData?.data?.custom ?? []).map((font, index) => /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_13__.jsxs)(_components_inlineStack__WEBPACK_IMPORTED_MODULE_3__["default"], {
          gap: 8,
          align: "between",
          blockAlign: "center",
          style: {
            padding: "6px 10px",
            background: "#fff",
            borderRadius: "6px",
            border: "1px solid #e5e7eb"
          },
          children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_13__.jsxs)(_components_inlineStack__WEBPACK_IMPORTED_MODULE_3__["default"], {
            gap: 10,
            blockAlign: "center",
            children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_13__.jsx)(_components_icon__WEBPACK_IMPORTED_MODULE_11__["default"], {
              name: "text_fields",
              color: "descgray",
              fontSize: "sm"
            }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_13__.jsxs)(_components_blockStack__WEBPACK_IMPORTED_MODULE_4__["default"], {
              gap: 5,
              children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_13__.jsx)(_components_text__WEBPACK_IMPORTED_MODULE_10__["default"], {
                size: "sm",
                weight: "medium",
                children: font?.name
              }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_13__.jsx)(_components_text__WEBPACK_IMPORTED_MODULE_10__["default"], {
                size: "xs",
                color: "descgray",
                children: font?.file
              })]
            })]
          }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_13__.jsx)(_components_button__WEBPACK_IMPORTED_MODULE_8__["default"], {
            variant: "outlined",
            size: "extrasmall",
            startIcon: "delete",
            disabled: isDeleting,
            onClick: () => handleFontDelete(font?.slug),
            children: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_6__.__)("Remove", "ninja-media")
          })]
        }, font?.slug ?? index))]
      }), (fontsData?.data?.custom ?? []).length === 0 && /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_13__.jsx)(_components_description__WEBPACK_IMPORTED_MODULE_2__["default"], {
        text: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_6__.__)("No custom fonts yet. Upload a TTF or OTF file above to add your own font.", "ninja-media")
      })]
    })
  });
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (Fonts);

/***/ },

/***/ "./source/admin/watermark/components__premium_only/Preview.tsx"
/*!*********************************************************************!*\
  !*** ./source/admin/watermark/components__premium_only/Preview.tsx ***!
  \*********************************************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _hooks_premium_only_useTilePreview__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../hooks__premium_only/useTilePreview */ "./source/admin/watermark/hooks__premium_only/useTilePreview.ts");
/* harmony import */ var _wordpress_element__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! @wordpress/element */ "@wordpress/element");
/* harmony import */ var _wordpress_element__WEBPACK_IMPORTED_MODULE_1___default = /*#__PURE__*/__webpack_require__.n(_wordpress_element__WEBPACK_IMPORTED_MODULE_1__);
/* harmony import */ var _components_settingsField__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ~/components/settingsField */ "./source/components/settingsField/index.ts");
/* harmony import */ var _components_blockStack__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ~/components/blockStack */ "./source/components/blockStack/index.ts");
/* harmony import */ var _wordpress_i18n__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! @wordpress/i18n */ "@wordpress/i18n");
/* harmony import */ var _wordpress_i18n__WEBPACK_IMPORTED_MODULE_4___default = /*#__PURE__*/__webpack_require__.n(_wordpress_i18n__WEBPACK_IMPORTED_MODULE_4__);
/* harmony import */ var _hooks_useSettings__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ~/hooks/useSettings */ "./source/hooks/useSettings.ts");
/* harmony import */ var react_router_dom__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! react-router-dom */ "./node_modules/react-router/dist/development/chunk-UVKPFVEO.mjs");
/* harmony import */ var _components_card__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! ~/components/card */ "./source/components/card/index.ts");
/* harmony import */ var _components_icon__WEBPACK_IMPORTED_MODULE_8__ = __webpack_require__(/*! ~/components/icon */ "./source/components/icon/index.ts");
/* harmony import */ var _Adjust__WEBPACK_IMPORTED_MODULE_9__ = __webpack_require__(/*! ./Adjust */ "./source/admin/watermark/components__premium_only/Adjust.tsx");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_10__ = __webpack_require__(/*! react/jsx-runtime */ "react/jsx-runtime");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_10___default = /*#__PURE__*/__webpack_require__.n(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_10__);

const Preview = () => {
  const {
    data
  } = (0,_hooks_useSettings__WEBPACK_IMPORTED_MODULE_5__["default"])();
  const [imageUrl, setImageUrl] = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_1__.useState)("");
  const {
    menuKey
  } = (0,react_router_dom__WEBPACK_IMPORTED_MODULE_6__.useParams)();
  const {
    type,
    text,
    image,
    position,
    size,
    opacity,
    edgeMargin,
    tile
  } = data?.watermark || {};
  const {
    content,
    fontSize,
    fontFamily,
    rotation,
    color,
    bold,
    italic
  } = text || {};
  const containerRef = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_1__.useRef)(null);
  const [containerSize, setContainerSize] = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_1__.useState)({
    w: 0,
    h: 0
  });
  (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_1__.useEffect)(() => {
    const el = containerRef.current;
    if (!el) return;
    const ro = new ResizeObserver(([entry]) => {
      setContainerSize({
        w: Math.round(entry.contentRect.width),
        h: Math.round(entry.contentRect.height)
      });
    });
    ro.observe(el);
    return () => ro.disconnect();
  }, []);
  (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_1__.useEffect)(() => {
    if (!image) {
      setImageUrl("");
      return;
    }
    const attachment = window.wp.media.attachment(image);
    attachment.fetch().then(() => {
      setImageUrl(attachment.get("url") || "");
    });
  }, [image]);
  const isTiled = !!tile?.enabled;
  const spacingX = tile?.spacingX ?? 20;
  const spacingY = tile?.spacingY ?? 20;
  const diagonal = tile?.diagonal ?? false;
  const resolvedContent = (content ?? "").replaceAll("{site_name}", "My Site").replaceAll("{site_url}", window.location.origin).replaceAll("{year}", String(new Date().getFullYear())).replaceAll("{date}", new Date().toISOString().slice(0, 10)).replaceAll("{author}", "Admin").replaceAll("{filename}", "photo");
  const tileOpts = isTiled ? type === "image" && imageUrl ? {
    type: "image",
    imageUrl,
    sizePct: size ?? 20,
    opacity: opacity ?? 100,
    spacingX,
    spacingY,
    diagonal
  } : type === "text" && content?.trim() ? {
    type: "text",
    content: resolvedContent,
    fontSize: fontSize ?? 24,
    fontFamily: fontFamily ?? "Sans Serif",
    color: color ?? "#ffffff",
    bold: bold ?? false,
    italic: italic ?? false,
    rotation: rotation ?? 0,
    opacity: opacity ?? 100,
    sizePct: size ?? 20,
    spacingX,
    spacingY,
    diagonal
  } : null : null;
  const tileDataUrl = (0,_hooks_premium_only_useTilePreview__WEBPACK_IMPORTED_MODULE_0__.useTilePreview)(tileOpts, containerSize.w, containerSize.h);
  const isText = menuKey === "text";
  const showSingleText = !isTiled && isText && !!content?.trim();
  const showSingleImage = !isTiled && !isText && !!imageUrl;
  const tileOverlayStyle = diagonal ? {
    position: "absolute",
    width: "250%",
    height: "250%",
    top: "50%",
    left: "50%",
    transform: "translate(-50%, -50%) rotate(45deg)",
    backgroundImage: `url(${tileDataUrl})`,
    backgroundRepeat: "repeat",
    pointerEvents: "none"
  } : {
    position: "absolute",
    inset: 0,
    backgroundImage: `url(${tileDataUrl})`,
    backgroundRepeat: "repeat",
    pointerEvents: "none"
  };
  const positionLabel = POSITION_LABELS[position] ?? "";
  return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_10__.jsxs)(_components_blockStack__WEBPACK_IMPORTED_MODULE_3__["default"], {
    gap: 20,
    style: {
      width: "50%",
      position: "sticky",
      top: 0
    },
    children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_10__.jsx)(_components_settingsField__WEBPACK_IMPORTED_MODULE_2__["default"], {
      title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_4__.__)("Preview", "ninja-media"),
      children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_10__.jsxs)(_components_card__WEBPACK_IMPORTED_MODULE_7__["default"], {
        background: "white",
        rounded: "md",
        style: {
          position: "relative",
          width: "100%",
          height: "260px",
          overflow: "hidden",
          backgroundImage: "linear-gradient(45deg, #f0f0f0 25%, transparent 25%), " + "linear-gradient(-45deg, #f0f0f0 25%, transparent 25%), " + "linear-gradient(45deg, transparent 75%, #f0f0f0 75%), " + "linear-gradient(-45deg, transparent 75%, #f0f0f0 75%)",
          backgroundSize: "20px 20px",
          backgroundPosition: "0 0, 0 10px, 10px -10px, -10px 0px"
        },
        children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_10__.jsx)("div", {
          ref: containerRef,
          style: {
            position: "absolute",
            inset: 0,
            display: "flex",
            alignItems: "center",
            justifyContent: "center"
          },
          children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_10__.jsx)(_components_icon__WEBPACK_IMPORTED_MODULE_8__["default"], {
            color: "descgray",
            name: "landscape",
            style: {
              fontSize: "80px",
              opacity: 0.12
            }
          })
        }), isTiled && tileDataUrl && /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_10__.jsx)("div", {
          style: tileOverlayStyle
        }), showSingleText && /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_10__.jsx)("span", {
          style: textPreviewStyle({
            fontSize,
            fontFamily,
            rotation,
            color,
            bold,
            italic,
            position,
            opacity,
            size,
            edgeMargin
          }),
          children: resolvedContent
        }), showSingleImage && /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_10__.jsx)("img", {
          src: imageUrl,
          alt: "preview",
          style: {
            ...positionStyle({
              position: position,
              size: size,
              edgeMargin: edgeMargin
            }),
            opacity: opacity / 100,
            pointerEvents: "none"
          }
        }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_10__.jsxs)("div", {
          style: {
            position: "absolute",
            top: "10px",
            right: "10px",
            background: "rgba(0,0,0,0.55)",
            color: "#fff",
            padding: "3px 8px",
            borderRadius: "4px",
            fontSize: "11px",
            lineHeight: "16px",
            pointerEvents: "none"
          },
          children: [positionLabel, " \xB7 ", opacity, "% \xB7 ", size, "%", isTiled ? ` · ${(0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_4__.__)("Tiled", "ninja-media")}` : ""]
        })]
      })
    }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_10__.jsx)(_Adjust__WEBPACK_IMPORTED_MODULE_9__["default"], {})]
  });
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (Preview);
const positionStyle = ({
  position,
  size,
  edgeMargin
}) => {
  const margin = `${edgeMargin}px`;
  const base = {
    position: "absolute",
    maxWidth: `${size}%`,
    maxHeight: `${size}%`,
    objectFit: "contain",
    transition: "all 0.25s ease"
  };
  if (position?.startsWith("top")) base.top = margin;else if (position?.startsWith("bottom")) base.bottom = margin;else {
    base.top = "50%";
    base.transform = "translateY(-50%)";
  }
  if (position?.endsWith("left")) base.left = margin;else if (position?.endsWith("right")) base.right = margin;else {
    base.left = "50%";
    base.transform = base.transform ? "translate(-50%, -50%)" : "translateX(-50%)";
  }
  return base;
};
const textPreviewStyle = ({
  fontSize,
  fontFamily,
  rotation,
  color,
  bold,
  italic,
  position,
  opacity,
  size,
  edgeMargin
}) => ({
  ...positionStyle({
    position,
    size,
    edgeMargin
  }),
  fontSize: `${Math.max(8, Math.round(fontSize * size / 100))}px`,
  fontFamily: fontFamily,
  transform: `${positionStyle({
    position,
    size,
    edgeMargin
  }).transform ?? ""} rotate(${-rotation}deg)`.trim(),
  color,
  fontWeight: bold ? "bold" : "normal",
  fontStyle: italic ? "italic" : "normal",
  opacity: opacity / 100,
  whiteSpace: "nowrap",
  pointerEvents: "none",
  lineHeight: 1.2
});
const POSITION_LABELS = {
  "top-left": (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_4__.__)("Top Left", "ninja-media"),
  "top-center": (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_4__.__)("Top Center", "ninja-media"),
  "top-right": (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_4__.__)("Top Right", "ninja-media"),
  "center-left": (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_4__.__)("Center Left", "ninja-media"),
  center: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_4__.__)("Center", "ninja-media"),
  "center-right": (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_4__.__)("Center Right", "ninja-media"),
  "bottom-left": (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_4__.__)("Bottom Left", "ninja-media"),
  "bottom-center": (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_4__.__)("Bottom Center", "ninja-media"),
  "bottom-right": (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_4__.__)("Bottom Right", "ninja-media")
};

/***/ },

/***/ "./source/admin/watermark/components__premium_only/Tile.tsx"
/*!******************************************************************!*\
  !*** ./source/admin/watermark/components__premium_only/Tile.tsx ***!
  \******************************************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _components_settingsField__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ~/components/settingsField */ "./source/components/settingsField/index.ts");
/* harmony import */ var _components_description__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ~/components/description */ "./source/components/description/index.ts");
/* harmony import */ var _components_inlineStack__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ~/components/inlineStack */ "./source/components/inlineStack/index.ts");
/* harmony import */ var _components_blockStack__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ~/components/blockStack */ "./source/components/blockStack/index.ts");
/* harmony import */ var _hooks_useSettings__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ~/hooks/useSettings */ "./source/hooks/useSettings.ts");
/* harmony import */ var _components_switcher__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ~/components/switcher */ "./source/components/switcher/index.ts");
/* harmony import */ var _components_disabled__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! ~/components/disabled */ "./source/components/disabled/index.ts");
/* harmony import */ var _components_input__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! ~/components/input */ "./source/components/input/index.ts");
/* harmony import */ var _wordpress_i18n__WEBPACK_IMPORTED_MODULE_8__ = __webpack_require__(/*! @wordpress/i18n */ "@wordpress/i18n");
/* harmony import */ var _wordpress_i18n__WEBPACK_IMPORTED_MODULE_8___default = /*#__PURE__*/__webpack_require__.n(_wordpress_i18n__WEBPACK_IMPORTED_MODULE_8__);
/* harmony import */ var _components_text__WEBPACK_IMPORTED_MODULE_9__ = __webpack_require__(/*! ~/components/text */ "./source/components/text/index.ts");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_10__ = __webpack_require__(/*! react/jsx-runtime */ "react/jsx-runtime");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_10___default = /*#__PURE__*/__webpack_require__.n(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_10__);

const Tile = () => {
  const {
    data,
    setSettings
  } = (0,_hooks_useSettings__WEBPACK_IMPORTED_MODULE_4__["default"])();
  const {
    enabled,
    spacingX,
    spacingY,
    diagonal
  } = data?.watermark?.tile || {};
  return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_10__.jsx)(_components_settingsField__WEBPACK_IMPORTED_MODULE_0__["default"], {
    description: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_8__.__)("Repeat the watermark across the entire image in a grid pattern.", "ninja-media"),
    action: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_10__.jsx)(_components_switcher__WEBPACK_IMPORTED_MODULE_5__["default"], {
      id: "watermarkTileEnabled",
      title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_8__.__)("Tiled / Repeating", "ninja-media"),
      checked: enabled,
      onChange: () => setSettings("watermark.tile.enabled", !enabled)
    }),
    children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_10__.jsxs)(_components_disabled__WEBPACK_IMPORTED_MODULE_6__["default"], {
      depend: !enabled,
      dependOn: "watermarkTileEnabled",
      children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_10__.jsxs)(_components_inlineStack__WEBPACK_IMPORTED_MODULE_2__["default"], {
        gap: 10,
        wrap: false,
        children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_10__.jsxs)(_components_blockStack__WEBPACK_IMPORTED_MODULE_3__["default"], {
          gap: 10,
          className: "flex-1",
          children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_10__.jsx)(_components_text__WEBPACK_IMPORTED_MODULE_9__["default"], {
            size: "sm",
            weight: "medium",
            children: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_8__.__)("Horizontal spacing", "ninja-media")
          }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_10__.jsx)(_components_input__WEBPACK_IMPORTED_MODULE_7__["default"], {
            type: "number",
            size: "small",
            suffix: "px",
            value: spacingX ?? "",
            onChange: value => setSettings("watermark.tile.spacingX", Math.max(0, Number(value)))
          })]
        }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_10__.jsxs)(_components_blockStack__WEBPACK_IMPORTED_MODULE_3__["default"], {
          gap: 10,
          className: "flex-1",
          children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_10__.jsx)(_components_text__WEBPACK_IMPORTED_MODULE_9__["default"], {
            size: "sm",
            weight: "medium",
            children: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_8__.__)("Vertical spacing", "ninja-media")
          }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_10__.jsx)(_components_input__WEBPACK_IMPORTED_MODULE_7__["default"], {
            type: "number",
            size: "small",
            suffix: "px",
            value: spacingY ?? "",
            onChange: value => setSettings("watermark.tile.spacingY", Math.max(0, Number(value)))
          })]
        })]
      }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_10__.jsxs)(_components_blockStack__WEBPACK_IMPORTED_MODULE_3__["default"], {
        gap: 10,
        children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_10__.jsx)(_components_switcher__WEBPACK_IMPORTED_MODULE_5__["default"], {
          title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_8__.__)("Diagonal tiling mode", "ninja-media"),
          titleSize: "sm",
          checked: diagonal,
          onChange: () => setSettings("watermark.tile.diagonal", !diagonal)
        }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_10__.jsx)(_components_description__WEBPACK_IMPORTED_MODULE_1__["default"], {
          text: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_8__.__)("Enable this option to tile the watermark diagonally across the image.", "ninja-media")
        })]
      })]
    })
  });
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (Tile);

/***/ },

/***/ "./source/admin/watermark/hooks__premium_only/useTilePreview.ts"
/*!**********************************************************************!*\
  !*** ./source/admin/watermark/hooks__premium_only/useTilePreview.ts ***!
  \**********************************************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   useTilePreview: () => (/* binding */ useTilePreview)
/* harmony export */ });
/* harmony import */ var _wordpress_element__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @wordpress/element */ "@wordpress/element");
/* harmony import */ var _wordpress_element__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(_wordpress_element__WEBPACK_IMPORTED_MODULE_0__);

const buildTileDataUrl = async (opts, containerW) => {
  let stampW = 0;
  let stampH = 0;
  let img = null;
  if (opts.type === "image") {
    img = await new Promise((res, rej) => {
      const el = new Image();
      el.crossOrigin = "anonymous";
      el.onload = () => res(el);
      el.onerror = rej;
      el.src = opts.imageUrl;
    });
    const maxPx = opts.sizePct / 100 * containerW;
    const ratio = img.naturalWidth / img.naturalHeight;
    if (ratio >= 1) {
      stampW = Math.min(maxPx, img.naturalWidth);
      stampH = stampW / ratio;
    } else {
      stampH = Math.min(maxPx, img.naturalHeight);
      stampW = stampH * ratio;
    }
  } else {
    const tmp = document.createElement("canvas");
    const tmpCtx = tmp.getContext("2d");
    const scaledFontSize = Math.max(8, Math.round(opts.fontSize * opts.sizePct / 100));
    const weight = opts.bold ? "bold" : "normal";
    const style = opts.italic ? "italic" : "normal";
    tmpCtx.font = `${style} ${weight} ${scaledFontSize}px ${opts.fontFamily}`;
    const metrics = tmpCtx.measureText(opts.content);
    stampW = Math.ceil(metrics.width) + 4;
    stampH = Math.ceil(scaledFontSize * 1.4);
  }
  if (stampW <= 0 || stampH <= 0) return null;
  const spacingX = Math.min(opts.spacingX, containerW * 0.5);
  const spacingY = Math.min(opts.spacingY, containerW * 0.5);
  const rotRad = opts.type === "text" && opts.rotation !== 0 ? -opts.rotation * Math.PI / 180 : 0;
  const needsPad = opts.type === "text" && opts.rotation !== 0;
  const diagLen = Math.ceil(Math.sqrt(stampW ** 2 + stampH ** 2));
  const sW = needsPad ? diagLen : Math.ceil(stampW);
  const sH = needsPad ? diagLen : Math.ceil(stampH);
  const stampCanvas = document.createElement("canvas");
  stampCanvas.width = sW;
  stampCanvas.height = sH;
  const sCtx = stampCanvas.getContext("2d");
  sCtx.globalAlpha = opts.opacity / 100;
  if (opts.type === "image" && img) {
    sCtx.drawImage(img, 0, 0, stampW, stampH);
  } else if (opts.type === "text") {
    const scaledFontSize = Math.max(8, Math.round(opts.fontSize * opts.sizePct / 100));
    const weight = opts.bold ? "bold" : "normal";
    const fStyle = opts.italic ? "italic" : "normal";
    sCtx.font = `${fStyle} ${weight} ${scaledFontSize}px ${opts.fontFamily}`;
    sCtx.fillStyle = opts.color;
    sCtx.textBaseline = "top";
    sCtx.save();
    sCtx.translate(sW / 2, sH / 2);
    sCtx.rotate(rotRad);
    sCtx.fillText(opts.content, -stampW / 2, -stampH / 2);
    sCtx.restore();
  }
  const cellW = Math.ceil(sW + spacingX);
  const cellH = Math.ceil(sH + spacingY);
  const tileCanvas = document.createElement("canvas");
  tileCanvas.width = cellW;
  tileCanvas.height = cellH;
  tileCanvas.getContext("2d").drawImage(stampCanvas, 0, 0);
  return tileCanvas.toDataURL("image/png");
};
const useTilePreview = (opts, containerW, containerH) => {
  const [dataUrl, setDataUrl] = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useState)(null);
  (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useEffect)(() => {
    if (!opts || containerW === 0) {
      setDataUrl(null);
      return;
    }
    if (opts.type === "text" && !opts.content.trim()) {
      setDataUrl(null);
      return;
    }
    if (opts.type === "image" && !opts.imageUrl) {
      setDataUrl(null);
      return;
    }
    let cancelled = false;
    buildTileDataUrl(opts, containerW).then(url => {
      if (!cancelled) setDataUrl(url);
    }).catch(() => {
      if (!cancelled) setDataUrl(null);
    });
    return () => {
      cancelled = true;
    };
  }, [JSON.stringify(opts), containerW, containerH]);
  return dataUrl;
};

/***/ },

/***/ "./source/admin/watermark/hooks__premium_only/useWatermarkActions.ts"
/*!***************************************************************************!*\
  !*** ./source/admin/watermark/hooks__premium_only/useWatermarkActions.ts ***!
  \***************************************************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _components_alert_Alert__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ~/components/alert/Alert */ "./source/components/alert/Alert.tsx");
/* harmony import */ var _hooks_useSettings__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ~/hooks/useSettings */ "./source/hooks/useSettings.ts");

const useWatermarkActions = () => {
  const {
    data,
    setSettings,
    saveSettings
  } = (0,_hooks_useSettings__WEBPACK_IMPORTED_MODULE_1__["default"])();
  const {
    showAlert
  } = (0,_components_alert_Alert__WEBPACK_IMPORTED_MODULE_0__.useCustomAlert)();
  const resetSettings = () => {
    showAlert({
      type: "error",
      title: "Reset",
      text: `Are you sure you want to reset the watermark settings?`,
      showCancelButton: true,
      confirmButtonText: "Reset",
      onConfirm: async () => {
        setSettings("watermark", pnpnm?.defaultSettings?.watermark || {});
        const updatedSettings = {
          ...data,
          watermark: pnpnm?.defaultSettings?.watermark || {}
        };
        await saveSettings(updatedSettings);
      }
    });
  };
  return {
    resetSettings
  };
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (useWatermarkActions);

/***/ },

/***/ "./source/admin/watermark/pages__premium_only/image/Image.tsx"
/*!********************************************************************!*\
  !*** ./source/admin/watermark/pages__premium_only/image/Image.tsx ***!
  \********************************************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _wordpress_element__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @wordpress/element */ "@wordpress/element");
/* harmony import */ var _wordpress_element__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(_wordpress_element__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var _components_pageContainer__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ~/components/pageContainer */ "./source/components/pageContainer/index.ts");
/* harmony import */ var _components_settingsField__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ~/components/settingsField */ "./source/components/settingsField/index.ts");
/* harmony import */ var _components_inlineStack__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ~/components/inlineStack */ "./source/components/inlineStack/index.ts");
/* harmony import */ var _hooks_useSettings__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ~/hooks/useSettings */ "./source/hooks/useSettings.ts");
/* harmony import */ var _components_button__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ~/components/button */ "./source/components/button/index.ts");
/* harmony import */ var _wordpress_i18n__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! @wordpress/i18n */ "@wordpress/i18n");
/* harmony import */ var _wordpress_i18n__WEBPACK_IMPORTED_MODULE_6___default = /*#__PURE__*/__webpack_require__.n(_wordpress_i18n__WEBPACK_IMPORTED_MODULE_6__);
/* harmony import */ var _components_card__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! ~/components/card */ "./source/components/card/index.ts");
/* harmony import */ var _components_text__WEBPACK_IMPORTED_MODULE_8__ = __webpack_require__(/*! ~/components/text */ "./source/components/text/index.ts");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_9__ = __webpack_require__(/*! react/jsx-runtime */ "react/jsx-runtime");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_9___default = /*#__PURE__*/__webpack_require__.n(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_9__);

const Image = () => {
  const {
    data,
    setSettings
  } = (0,_hooks_useSettings__WEBPACK_IMPORTED_MODULE_4__["default"])();
  const [imageUrl, setImageUrl] = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useState)("");
  const {
    image
  } = data?.watermark || {};
  (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useEffect)(() => {
    if (!image) {
      setImageUrl("");
      return;
    }
    const attachment = window.wp.media.attachment(image);
    attachment.fetch().then(() => {
      setImageUrl(attachment.get("url") || "");
    });
  }, [image]);
  const openMediaPicker = () => {
    if (!pnpnm?.isPro || !data?.watermark?.enabled) return;
    const wpMedia = window.wp?.media;
    if (typeof wpMedia !== "function") return;
    const frame = wpMedia({
      title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_6__.__)("Select Watermark Image", "ninja-media"),
      multiple: false,
      library: {
        type: "image"
      },
      button: {
        text: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_6__.__)("Use this image", "ninja-media")
      }
    });
    frame.on("select", () => {
      const attachment = frame.state().get("selection").first().toJSON();
      setSettings("watermark.image", attachment?.id);
    });
    frame.open();
  };
  return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_9__.jsx)(_components_pageContainer__WEBPACK_IMPORTED_MODULE_1__["default"], {
    children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_9__.jsx)(_components_settingsField__WEBPACK_IMPORTED_MODULE_2__["default"], {
      title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_6__.__)("Image", "ninja-media"),
      description: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_6__.__)("Choose the image to use as a watermark. PNG with transparency recommended.", "ninja-media"),
      children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_9__.jsxs)(_components_card__WEBPACK_IMPORTED_MODULE_7__["default"], {
        padding: 0,
        background: "extralight",
        flex: true,
        align: "center",
        blockAlign: "center",
        style: {
          position: "relative",
          width: "400px",
          height: "200px",
          overflow: "hidden"
        },
        children: [imageUrl ? /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_9__.jsx)("img", {
          src: imageUrl,
          alt: "watermark",
          style: {
            width: "100%",
            height: "100%",
            objectFit: "contain"
          }
        }) : /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_9__.jsx)(_components_text__WEBPACK_IMPORTED_MODULE_8__["default"], {
          color: "descgray",
          size: "sm",
          children: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_6__.__)("No image selected", "ninja-media")
        }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_9__.jsx)(_components_inlineStack__WEBPACK_IMPORTED_MODULE_3__["default"], {
          gap: 10,
          wrap: false,
          style: {
            position: "absolute",
            bottom: "20px",
            left: "20px"
          },
          children: image ? /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_9__.jsxs)(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_9__.Fragment, {
            children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_9__.jsx)(_components_button__WEBPACK_IMPORTED_MODULE_5__["default"], {
              variant: "error",
              size: "extrasmall",
              startIcon: "delete",
              onClick: () => setSettings("watermark.image", 0),
              children: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_6__.__)("Remove", "ninja-media")
            }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_9__.jsx)(_components_button__WEBPACK_IMPORTED_MODULE_5__["default"], {
              variant: "primary",
              size: "extrasmall",
              startIcon: "swap_horiz",
              onClick: openMediaPicker,
              children: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_6__.__)("Change", "ninja-media")
            })]
          }) : /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_9__.jsx)(_components_button__WEBPACK_IMPORTED_MODULE_5__["default"], {
            variant: "primary",
            size: "extrasmall",
            startIcon: "add_photo_alternate",
            onClick: openMediaPicker,
            children: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_6__.__)("Select Image", "ninja-media")
          })
        })]
      })
    })
  });
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (Image);

/***/ },

/***/ "./source/admin/watermark/pages__premium_only/text/Text.tsx"
/*!******************************************************************!*\
  !*** ./source/admin/watermark/pages__premium_only/text/Text.tsx ***!
  \******************************************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   FONT_OPTIONS: () => (/* binding */ FONT_OPTIONS),
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _components_premium_only_Fonts__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../../components__premium_only/Fonts */ "./source/admin/watermark/components__premium_only/Fonts.tsx");
/* harmony import */ var _components_settingsField__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ~/components/settingsField */ "./source/components/settingsField/index.ts");
/* harmony import */ var _components_pageContainer__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ~/components/pageContainer */ "./source/components/pageContainer/index.ts");
/* harmony import */ var _components_colorPicker__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ~/components/colorPicker */ "./source/components/colorPicker/index.ts");
/* harmony import */ var _components_inlineStack__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ~/components/inlineStack */ "./source/components/inlineStack/index.ts");
/* harmony import */ var _components_description__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ~/components/description */ "./source/components/description/index.ts");
/* harmony import */ var _components_blockStack__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! ~/components/blockStack */ "./source/components/blockStack/index.ts");
/* harmony import */ var _components_selectBox__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! ~/components/selectBox */ "./source/components/selectBox/index.ts");
/* harmony import */ var _hooks_useSettings__WEBPACK_IMPORTED_MODULE_8__ = __webpack_require__(/*! ~/hooks/useSettings */ "./source/hooks/useSettings.ts");
/* harmony import */ var _components_text__WEBPACK_IMPORTED_MODULE_9__ = __webpack_require__(/*! ~/components/text */ "./source/components/text/index.ts");
/* harmony import */ var _components_switcher__WEBPACK_IMPORTED_MODULE_10__ = __webpack_require__(/*! ~/components/switcher */ "./source/components/switcher/index.ts");
/* harmony import */ var _components_divider__WEBPACK_IMPORTED_MODULE_11__ = __webpack_require__(/*! ~/components/divider */ "./source/components/divider/index.ts");
/* harmony import */ var _components_input__WEBPACK_IMPORTED_MODULE_12__ = __webpack_require__(/*! ~/components/input */ "./source/components/input/index.ts");
/* harmony import */ var _wordpress_i18n__WEBPACK_IMPORTED_MODULE_13__ = __webpack_require__(/*! @wordpress/i18n */ "@wordpress/i18n");
/* harmony import */ var _wordpress_i18n__WEBPACK_IMPORTED_MODULE_13___default = /*#__PURE__*/__webpack_require__.n(_wordpress_i18n__WEBPACK_IMPORTED_MODULE_13__);
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_14__ = __webpack_require__(/*! react/jsx-runtime */ "react/jsx-runtime");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_14___default = /*#__PURE__*/__webpack_require__.n(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_14__);

const Text = () => {
  const {
    data,
    setSettings
  } = (0,_hooks_useSettings__WEBPACK_IMPORTED_MODULE_8__["default"])();
  const {
    text
  } = data?.watermark || {};
  const {
    content,
    fontSize,
    fontFamily,
    rotation,
    color,
    bold,
    italic
  } = text || {};
  return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_14__.jsxs)(_components_pageContainer__WEBPACK_IMPORTED_MODULE_2__["default"], {
    children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_14__.jsxs)(_components_settingsField__WEBPACK_IMPORTED_MODULE_1__["default"], {
      title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_13__.__)("Watermark Text", "ninja-media"),
      children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_14__.jsxs)(_components_blockStack__WEBPACK_IMPORTED_MODULE_6__["default"], {
        gap: 10,
        children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_14__.jsx)(_components_input__WEBPACK_IMPORTED_MODULE_12__["default"], {
          size: "small",
          value: content ?? "",
          onChange: value => setSettings("watermark.text.content", String(value))
        }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_14__.jsx)(_components_description__WEBPACK_IMPORTED_MODULE_5__["default"], {
          text: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_13__.__)("Supports dynamic tags: {site_name}, {site_url}, {year}, {date}, {author}, {filename}", "ninja-media")
        })]
      }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_14__.jsxs)(_components_inlineStack__WEBPACK_IMPORTED_MODULE_4__["default"], {
        gap: 10,
        wrap: false,
        children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_14__.jsxs)(_components_blockStack__WEBPACK_IMPORTED_MODULE_6__["default"], {
          gap: 10,
          className: "flex-1",
          children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_14__.jsx)(_components_text__WEBPACK_IMPORTED_MODULE_9__["default"], {
            size: "sm",
            weight: "medium",
            children: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_13__.__)("Font Size", "ninja-media")
          }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_14__.jsx)(_components_input__WEBPACK_IMPORTED_MODULE_12__["default"], {
            type: "number",
            size: "small",
            suffix: "px",
            value: fontSize ?? "",
            onChange: value => setSettings("watermark.text.fontSize", Number(value))
          })]
        }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_14__.jsxs)(_components_blockStack__WEBPACK_IMPORTED_MODULE_6__["default"], {
          gap: 10,
          className: "flex-1",
          children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_14__.jsx)(_components_text__WEBPACK_IMPORTED_MODULE_9__["default"], {
            size: "sm",
            weight: "medium",
            children: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_13__.__)("Font Family", "ninja-media")
          }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_14__.jsx)(_components_selectBox__WEBPACK_IMPORTED_MODULE_7__["default"], {
            size: "small",
            options: FONT_OPTIONS,
            value: [fontFamily || ""],
            onChange: value => setSettings("watermark.text.fontFamily", value[0])
          })]
        }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_14__.jsxs)(_components_blockStack__WEBPACK_IMPORTED_MODULE_6__["default"], {
          gap: 10,
          className: "flex-1",
          children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_14__.jsx)(_components_text__WEBPACK_IMPORTED_MODULE_9__["default"], {
            size: "sm",
            weight: "medium",
            children: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_13__.__)("Rotation", "ninja-media")
          }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_14__.jsx)(_components_input__WEBPACK_IMPORTED_MODULE_12__["default"], {
            type: "number",
            size: "small",
            suffix: "\xB0",
            value: rotation ?? "",
            onChange: value => setSettings("watermark.text.rotation", Number(value))
          })]
        })]
      }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_14__.jsxs)(_components_inlineStack__WEBPACK_IMPORTED_MODULE_4__["default"], {
        gap: 20,
        wrap: false,
        align: "between",
        blockAlign: "end",
        children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_14__.jsxs)(_components_blockStack__WEBPACK_IMPORTED_MODULE_6__["default"], {
          gap: 10,
          children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_14__.jsx)(_components_text__WEBPACK_IMPORTED_MODULE_9__["default"], {
            size: "sm",
            weight: "medium",
            children: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_13__.__)("Color", "ninja-media")
          }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_14__.jsx)(_components_colorPicker__WEBPACK_IMPORTED_MODULE_3__["default"], {
            defaultColor: "#2271B1",
            selectedColor: color ?? "#2271B1",
            onChange: color => setSettings("watermark.text.color", color)
          })]
        }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_14__.jsxs)(_components_inlineStack__WEBPACK_IMPORTED_MODULE_4__["default"], {
          gap: 10,
          children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_14__.jsx)(_components_switcher__WEBPACK_IMPORTED_MODULE_10__["default"], {
            title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_13__.__)("Bold", "ninja-media"),
            titleSize: "sm",
            checked: bold,
            onChange: value => setSettings("watermark.text.bold", value)
          }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_14__.jsx)(_components_divider__WEBPACK_IMPORTED_MODULE_11__["default"], {
            variant: "vertical",
            height: "20px"
          }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_14__.jsx)(_components_switcher__WEBPACK_IMPORTED_MODULE_10__["default"], {
            title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_13__.__)("Italic", "ninja-media"),
            titleSize: "sm",
            checked: italic,
            onChange: value => setSettings("watermark.text.italic", value)
          })]
        })]
      })]
    }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_14__.jsx)(_components_premium_only_Fonts__WEBPACK_IMPORTED_MODULE_0__["default"], {})]
  });
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (Text);
const FONT_OPTIONS = [{
  name: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_13__.__)("Sans Serif (Arial-like)", "ninja-media"),
  value: "Sans Serif"
}, {
  name: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_13__.__)("Serif (Times-like)", "ninja-media"),
  value: "Serif"
}, {
  name: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_13__.__)("Monospace (Courier-like)", "ninja-media"),
  value: "Monospace"
}, {
  name: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_13__.__)("DejaVu Sans (Unicode)", "ninja-media"),
  value: "DejaVu Sans"
}];

/***/ },

/***/ "./source/components/alert/Alert.tsx"
/*!*******************************************!*\
  !*** ./source/components/alert/Alert.tsx ***!
  \*******************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   CustomAlertProvider: () => (/* binding */ CustomAlertProvider),
/* harmony export */   useCustomAlert: () => (/* binding */ useCustomAlert)
/* harmony export */ });
/* harmony import */ var _input__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../input */ "./source/components/input/index.ts");
/* harmony import */ var clsx__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! clsx */ "./node_modules/clsx/dist/clsx.mjs");
/* harmony import */ var _wordpress_element__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! @wordpress/element */ "@wordpress/element");
/* harmony import */ var _wordpress_element__WEBPACK_IMPORTED_MODULE_2___default = /*#__PURE__*/__webpack_require__.n(_wordpress_element__WEBPACK_IMPORTED_MODULE_2__);
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! react/jsx-runtime */ "react/jsx-runtime");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3___default = /*#__PURE__*/__webpack_require__.n(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__);

const AlertContext = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_2__.createContext)(undefined);
function CustomAlertProvider({
  children
}) {
  const [alerts, setAlerts] = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_2__.useState)([]);
  const closeAlert = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_2__.useCallback)(id => {
    setAlerts(prev => prev.filter(alert => alert.id !== id));
  }, []);
  const showAlert = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_2__.useCallback)(options => {
    return new Promise(resolve => {
      const id = options.id || `alert-${Date.now()}-${Math.random()}`;
      const handleConfirm = async inputValue => {
        const alert = alerts.find(a => a.id === id);
        if (options.input && options.inputValidator) {
          const error = options.inputValidator(inputValue);
          if (error) {
            return;
          }
        }
        if (options.onConfirm) {
          setAlerts(prev => prev.map(a => a.id === id ? {
            ...a,
            isLoading: true
          } : a));
          await options.onConfirm(inputValue);
        }
        closeAlert(id);
        resolve({
          isConfirmed: true,
          value: alert?.inputValue
        });
      };
      const handleCancel = () => {
        if (options.onCancel) {
          options.onCancel();
        }
        closeAlert(id);
        resolve({
          isConfirmed: false
        });
      };
      const handleClose = () => {
        if (options.onClose) {
          options.onClose();
        }
        closeAlert(id);
        resolve({
          isConfirmed: false
        });
      };
      const newAlert = {
        ...options,
        id,
        isOpen: true,
        onConfirm: handleConfirm,
        onCancel: handleCancel,
        onClose: handleClose,
        inputValue: options.inputValue || "",
        pauseOnHover: options.pauseOnHover ?? true
      };
      setAlerts(prev => [...prev, newAlert]);
    });
  }, [alerts, closeAlert]);
  return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsxs)(AlertContext.Provider, {
    value: {
      showAlert,
      closeAlert
    },
    children: [children, /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsx)(AlertContainer, {
      alerts: alerts
    })]
  });
}
function useCustomAlert() {
  const context = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_2__.useContext)(AlertContext);
  if (!context) {
    throw new Error("useCustomAlert must be used within CustomAlertProvider");
  }
  return context;
}
function AlertContainer({
  alerts
}) {
  if (typeof window === "undefined") return null;
  return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsx)(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.Fragment, {
    children: alerts.map(alert => {
      const root = alert.rootId ? document.getElementById(alert.rootId) : document.body;
      if (!root) return null;
      return (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_2__.createPortal)(/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsx)(AlertComponent, {
        ...alert
      }, alert.id), root);
    })
  });
}
function AlertComponent(props) {
  const {
    title,
    text,
    html,
    type = "info",
    width,
    height,
    style,
    icon,
    showConfirmButton = true,
    showCancelButton = false,
    confirmButtonText = "OK",
    cancelButtonText = "Cancel",
    showIcon = true,
    timer,
    timerProgressBar = false,
    position = "center-center",
    toast = false,
    allowOutsideClick = true,
    allowEscapeKey = true,
    showCloseButton = false,
    onConfirm,
    onCancel,
    onClose,
    input,
    inputPlaceholder = "",
    inputValue: initialInputValue = "",
    inputSuffix,
    inputOptions = [],
    isLoading = false,
    pauseOnHover = true,
    id,
    confirmOnEnter = true,
    rootId
  } = props;
  const [inputValue, setInputValue] = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_2__.useState)(initialInputValue);
  const [errorMessage, setErrorMessage] = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_2__.useState)(null);
  const [isDark, setIsDark] = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_2__.useState)(false);
  const [closing, setClosing] = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_2__.useState)(false);
  const [remainingTime, setRemainingTime] = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_2__.useState)(timer || 0);
  const timerRef = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_2__.useRef)(null);
  const startTimeRef = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_2__.useRef)(0);
  const pausedElapsedRef = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_2__.useRef)(0);
  const inputRef = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_2__.useRef)(null);
  const [isPaused, setIsPaused] = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_2__.useState)(false);
  const isToastWithTimer = toast && !!timer && timer > 0;
  const shouldPauseOnHover = isToastWithTimer && pauseOnHover;
  (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_2__.useEffect)(() => {
    if (!isToastWithTimer) return;
    if (startTimeRef.current === 0) {
      startTimeRef.current = Date.now();
    }
    if (!isPaused) {
      timerRef.current = setInterval(() => {
        const now = Date.now();
        const totalElapsed = now - startTimeRef.current + pausedElapsedRef.current;
        const newRemaining = Math.max(0, timer - totalElapsed);
        setRemainingTime(newRemaining);
        if (newRemaining <= 0) {
          if (timerRef.current) clearInterval(timerRef.current);
          if (onClose) onClose();
        }
      }, 16);
    }
    return () => {
      if (timerRef.current) clearInterval(timerRef.current);
    };
  }, [isPaused, isToastWithTimer, timer, onClose]);
  const handleMouseEnter = () => {
    if (!shouldPauseOnHover) return;
    if (timerRef.current) {
      clearInterval(timerRef.current);
      timerRef.current = null;
    }
    const now = Date.now();
    pausedElapsedRef.current += now - startTimeRef.current;
    startTimeRef.current = 0;
    setIsPaused(true);
  };
  const handleMouseLeave = () => {
    if (!shouldPauseOnHover) return;
    startTimeRef.current = Date.now();
    setIsPaused(false);
  };
  (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_2__.useEffect)(() => {
    const theme_mode = localStorage.getItem("pnpnm-theme-status") || "light";
    setIsDark(theme_mode === "dark");
  }, []);
  (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_2__.useEffect)(() => {
    if (input && inputRef.current) {
      setTimeout(() => {
        inputRef.current?.focus();
      }, 0);
    }
  }, [input]);
  (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_2__.useEffect)(() => {
    if (!allowEscapeKey) return;
    const handleEscape = e => {
      e.stopPropagation();
      if (e.key === "Escape" && onClose) {
        handleCloseWithAnimation();
      }
    };
    document.addEventListener("keydown", handleEscape);
    return () => document.removeEventListener("keydown", handleEscape);
  }, [allowEscapeKey, onClose]);
  const handleCloseWithAnimation = () => {
    if (onClose) onClose();
  };
  const handleBackdropClick = e => {
    e.stopPropagation();
    if (allowOutsideClick && e.target === e.currentTarget) {
      handleCloseWithAnimation();
    }
  };
  const handleConfirmClick = async () => {
    if (props.inputValidator && inputValue) {
      const error = props.inputValidator(inputValue);
      if (error) {
        setErrorMessage(error);
        return;
      }
    }
    if (onConfirm) {
      await onConfirm(inputValue);
    }
  };
  (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_2__.useEffect)(() => {
    if (!confirmOnEnter) return;
    const handleEnter = e => {
      if (e.key === "Enter" && showConfirmButton && !isLoading) {
        e.stopPropagation();
        handleConfirmClick();
      }
    };
    document.addEventListener("keydown", handleEnter);
    return () => {
      document.removeEventListener("keydown", handleEnter);
    };
  }, [confirmOnEnter, inputValue]);
  const getPositionClass = () => {
    const positionMap = {
      "center-center": "center-center",
      "top-left": "top-left",
      "top-center": "top-center",
      "top-right": "top-right",
      "bottom-left": "bottom-left",
      "bottom-center": "bottom-center",
      "bottom-right": "bottom-right"
    };
    return positionMap[position] || "center-center";
  };
  const overlayClasses = ["pn-alert-overlay", isDark && "dark", toast && "pn-toast", getPositionClass()].filter(Boolean).join(" ");
  const containerClasses = ["pnpnm-top-level-wrapper", "pn-alert-container", isDark && "dark", toast && "pn-toast", closing && (toast ? "toast-closing" : "closing"), id].filter(Boolean).join(" ");
  const iconClasses = ["pn-alert-icon-container", `pn-${type}`, type === "question" || type === "info" ? "bg-extralight" : `bg-${type}extralight`, toast && "pn-toast"].filter(Boolean).join(" ");
  const contentClasses = ["pn-alert-content", toast && "pn-toast"].filter(Boolean).join(" ");
  const titleClasses = ["pn-alert-title", toast && "pn-toast"].filter(Boolean).join(" ");
  const messageClasses = ["pn-alert-message", toast && "pn-toast"].filter(Boolean).join(" ");
  const inputClasses = ["pn-alert-input", isDark && "dark", errorMessage && "error"].filter(Boolean).join(" ");
  const buttonsClasses = ["pn-alert-buttons", toast && "pn-toast"].filter(Boolean).join(" ");
  const confirmButtonClasses = ["pn-alert-button", "pn-alert-confirm-button", type, toast && "pn-toast"].filter(Boolean).join(" ");
  const cancelButtonClasses = ["pn-alert-button", "pn-alert-cancel-button", isDark && "dark", toast && "pn-toast"].filter(Boolean).join(" ");
  return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsx)("div", {
    className: overlayClasses,
    onClick: handleBackdropClick,
    onMouseEnter: handleMouseEnter,
    onMouseLeave: handleMouseLeave,
    children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsxs)("div", {
      style: {
        ...style,
        maxWidth: width ? width : "",
        height: height ? height : ""
      },
      className: containerClasses,
      children: [timerProgressBar && isToastWithTimer && /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsx)("div", {
        style: {
          transform: `scaleX(${remainingTime / timer})`,
          transformOrigin: "left",
          transition: isPaused ? "none" : "transform 0.05s linear",
          borderRadius: "0 0 var(--alert-border-radius) var(--alert-border-radius)"
        },
        className: (0,clsx__WEBPACK_IMPORTED_MODULE_1__["default"])("pn-alert-timer-progress", `pn-progress-${type}`)
      }), showCloseButton && /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsx)("button", {
        "aria-label": "Close",
        className: (0,clsx__WEBPACK_IMPORTED_MODULE_1__["default"])("pn-alert-close-button", isDark && "dark"),
        onClick: handleCloseWithAnimation,
        children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsx)("svg", {
          width: "20",
          height: "20",
          viewBox: "0 0 24 24",
          fill: "none",
          stroke: "currentColor",
          strokeWidth: "2",
          children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsx)("path", {
            d: "M18 6L6 18M6 6l12 12",
            strokeLinecap: "round"
          })
        })
      }), toast ? /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsxs)("div", {
        className: "pn-alert-toast-content",
        children: [showIcon && /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsx)("div", {
          className: iconClasses,
          children: getIcon({
            icon,
            type,
            toast
          })
        }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsxs)("div", {
          className: "pn-alert-toast-text",
          children: [title && /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsx)("h3", {
            className: titleClasses,
            children: title
          }), text && /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsx)("p", {
            className: messageClasses,
            children: text
          }), html && /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsx)("div", {
            className: "pn-alert-html-content",
            children: html
          })]
        })]
      }) : /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsxs)(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.Fragment, {
        children: [showIcon && /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsx)("div", {
          className: iconClasses,
          children: getIcon({
            icon,
            type,
            toast
          })
        }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsxs)("div", {
          className: contentClasses,
          children: [title && /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsx)("h2", {
            className: titleClasses,
            children: title
          }), text && /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsx)("p", {
            className: messageClasses,
            children: text
          }), html && /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsx)("div", {
            className: "pn-alert-html-content",
            children: html
          }), input && /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsxs)("div", {
            className: "pn-alert-input-container",
            children: [input === "textarea" ? /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsx)("textarea", {
              ref: inputRef,
              className: (0,clsx__WEBPACK_IMPORTED_MODULE_1__["default"])(inputClasses, "pn-alert-textarea"),
              placeholder: inputPlaceholder,
              value: inputValue,
              onChange: e => {
                setInputValue(e.target.value);
                setErrorMessage(null);
              }
            }) : input === "select" ? /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsxs)("select", {
              ref: inputRef,
              className: inputClasses,
              value: inputValue,
              onChange: e => {
                setInputValue(e.target.value);
                setErrorMessage(null);
              },
              children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsx)("option", {
                value: "",
                children: inputPlaceholder || "Select an option"
              }), inputOptions.map(opt => /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsx)("option", {
                value: opt.value,
                children: opt.label
              }, opt.value))]
            }) : /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsx)(_input__WEBPACK_IMPORTED_MODULE_0__["default"], {
              ref: inputRef,
              type: input,
              placeholder: inputPlaceholder,
              value: inputValue,
              suffix: inputSuffix && inputSuffix,
              onChange: value => {
                const convertedValue = input === "number" ? Number(value) : String(value);
                setInputValue(convertedValue);
                setErrorMessage(null);
              }
            }), errorMessage && /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsx)("div", {
              className: "pn-alert-error-text",
              children: errorMessage
            })]
          })]
        })]
      }), (showConfirmButton || showCancelButton) && /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsxs)("div", {
        className: buttonsClasses,
        children: [showCancelButton && /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsx)("button", {
          className: cancelButtonClasses,
          onClick: onCancel,
          disabled: isLoading,
          children: cancelButtonText
        }), showConfirmButton && /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsxs)("button", {
          className: confirmButtonClasses,
          onClick: handleConfirmClick,
          disabled: isLoading,
          children: [isLoading && /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsx)("span", {
            className: "pn-alert-spinner"
          }), confirmButtonText]
        })]
      })]
    })
  });
}
const getIcon = ({
  icon,
  type,
  toast
}) => {
  if (typeof icon !== "string" && icon) return icon;
  const iconSize = toast ? "24" : "40";
  const iconType = icon || type;
  switch (iconType) {
    case "success":
      return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsxs)("svg", {
        width: iconSize,
        height: iconSize,
        viewBox: "0 0 24 24",
        fill: "none",
        children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsx)("circle", {
          cx: "12",
          cy: "12",
          r: "10",
          stroke: "currentColor",
          strokeWidth: "2"
        }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsx)("path", {
          d: "M8 12.5l2.5 2.5L16 9",
          stroke: "currentColor",
          strokeWidth: "2",
          strokeLinecap: "round",
          strokeLinejoin: "round"
        })]
      });
    case "error":
      return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsxs)("svg", {
        width: iconSize,
        height: iconSize,
        viewBox: "0 0 24 24",
        fill: "none",
        children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsx)("circle", {
          cx: "12",
          cy: "12",
          r: "10",
          stroke: "currentColor",
          strokeWidth: "2"
        }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsx)("path", {
          d: "M15 9l-6 6M9 9l6 6",
          stroke: "currentColor",
          strokeWidth: "2",
          strokeLinecap: "round"
        })]
      });
    case "warning":
      return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsxs)("svg", {
        width: iconSize,
        height: iconSize,
        viewBox: "0 0 24 24",
        fill: "none",
        children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsx)("path", {
          d: "M12 2L2 20h20L12 2z",
          stroke: "currentColor",
          strokeWidth: "2",
          strokeLinejoin: "round"
        }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsx)("path", {
          d: "M12 9v4M12 17h.01",
          stroke: "currentColor",
          strokeWidth: "2",
          strokeLinecap: "round"
        })]
      });
    case "info":
      return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsxs)("svg", {
        width: iconSize,
        height: iconSize,
        viewBox: "0 0 24 24",
        fill: "none",
        children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsx)("circle", {
          cx: "12",
          cy: "12",
          r: "10",
          stroke: "currentColor",
          strokeWidth: "2"
        }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsx)("path", {
          d: "M12 16v-4M12 8h.01",
          stroke: "currentColor",
          strokeWidth: "2",
          strokeLinecap: "round"
        })]
      });
    case "question":
      return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsxs)("svg", {
        width: iconSize,
        height: iconSize,
        viewBox: "0 0 24 24",
        fill: "none",
        children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsx)("circle", {
          cx: "12",
          cy: "12",
          r: "10",
          stroke: "currentColor",
          strokeWidth: "2"
        }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsx)("path", {
          d: "M9.09 9a3 3 0 0 1 5.83 1c0 2-3 3-3 3M12 17h.01",
          stroke: "currentColor",
          strokeWidth: "2",
          strokeLinecap: "round"
        })]
      });
  }
};

/***/ },

/***/ "./source/components/avatar/Avatar.tsx"
/*!*********************************************!*\
  !*** ./source/components/avatar/Avatar.tsx ***!
  \*********************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _skeletonLoader__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../skeletonLoader */ "./source/components/skeletonLoader/index.ts");
/* harmony import */ var _wordpress_element__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! @wordpress/element */ "@wordpress/element");
/* harmony import */ var _wordpress_element__WEBPACK_IMPORTED_MODULE_1___default = /*#__PURE__*/__webpack_require__.n(_wordpress_element__WEBPACK_IMPORTED_MODULE_1__);
/* harmony import */ var _inlineStack__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../inlineStack */ "./source/components/inlineStack/index.ts");
/* harmony import */ var _blockStack__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ../blockStack */ "./source/components/blockStack/index.ts");
/* harmony import */ var _card__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ../card */ "./source/components/card/index.ts");
/* harmony import */ var clsx__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! clsx */ "./node_modules/clsx/dist/clsx.mjs");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! react/jsx-runtime */ "react/jsx-runtime");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_6___default = /*#__PURE__*/__webpack_require__.n(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_6__);

const Avatar = ({
  id,
  style,
  className,
  src,
  alt,
  width,
  height,
  rounded = "none",
  objectFit = "cover",
  referrerPolicy = "no-referrer",
  fallback,
  fallBackLimit,
  showSpinner = false
}) => {
  const [imageLoaded, setImageLoaded] = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_1__.useState)(false);
  const [imageError, setImageError] = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_1__.useState)(false);
  const getFallbackContent = () => {
    if (typeof fallback === "string") {
      return fallback.split(/\s+/).filter(word => word.length > 0).slice(0, fallBackLimit || Infinity).map(word => word[0].toUpperCase()).join("");
    }
    return fallback;
  };
  const styles = {
    width: width || "auto",
    height: height || "auto",
    ...style
  };
  const imageStyle = {
    display: imageLoaded ? "block" : "none",
    width: "100%",
    height: "100%",
    objectFit: objectFit
  };
  const shouldShowSpinner = showSpinner && !!src && !imageLoaded && !imageError;
  return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_6__.jsxs)(_blockStack__WEBPACK_IMPORTED_MODULE_3__["default"], {
    id: id,
    style: styles,
    align: "center",
    inlineAlign: "center",
    className: className,
    children: [shouldShowSpinner && /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_6__.jsx)(_blockStack__WEBPACK_IMPORTED_MODULE_3__["default"], {
      style: {
        width: "100%",
        height: "100%"
      },
      align: "center",
      inlineAlign: "center",
      children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_6__.jsx)(_skeletonLoader__WEBPACK_IMPORTED_MODULE_0__["default"], {
        width: "100%",
        height: "100%"
      })
    }), src && !imageError && /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_6__.jsx)("img", {
      src: src,
      alt: alt,
      style: {
        ...imageStyle,
        userSelect: "none"
      },
      className: (0,clsx__WEBPACK_IMPORTED_MODULE_5__["default"])(`rounded-${rounded}`),
      referrerPolicy: referrerPolicy,
      onLoad: () => setImageLoaded(true),
      onError: () => setImageError(true)
    }), (!src || imageError) && /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_6__.jsx)(_inlineStack__WEBPACK_IMPORTED_MODULE_2__["default"], {
      align: "center",
      children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_6__.jsx)(_card__WEBPACK_IMPORTED_MODULE_4__["default"], {
        padding: 0,
        style: styles,
        background: "white",
        rounded: rounded,
        flex: true,
        align: "center",
        blockAlign: "center",
        className: "text-black font-semibold",
        children: getFallbackContent()
      })
    })]
  });
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (Avatar);

/***/ },

/***/ "./source/components/avatar/index.ts"
/*!*******************************************!*\
  !*** ./source/components/avatar/index.ts ***!
  \*******************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (/* reexport safe */ _Avatar__WEBPACK_IMPORTED_MODULE_0__["default"])
/* harmony export */ });
/* harmony import */ var _Avatar__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./Avatar */ "./source/components/avatar/Avatar.tsx");

/***/ },

/***/ "./source/components/blockStack/BlockStack.tsx"
/*!*****************************************************!*\
  !*** ./source/components/blockStack/BlockStack.tsx ***!
  \*****************************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   BlockStack: () => (/* binding */ BlockStack),
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _wordpress_element__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @wordpress/element */ "@wordpress/element");
/* harmony import */ var _wordpress_element__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(_wordpress_element__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var clsx__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! clsx */ "./node_modules/clsx/dist/clsx.mjs");
/* harmony import */ var _utils_styles__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ~/utils/styles */ "./source/utils/styles.ts");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! react/jsx-runtime */ "react/jsx-runtime");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3___default = /*#__PURE__*/__webpack_require__.n(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__);

const BlockStack = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.forwardRef)(({
  id,
  style,
  className = "",
  margin,
  marginTop,
  padding,
  paddingTop,
  as: Component = "div",
  align = "start",
  inlineAlign,
  gap,
  reverseOrder = false,
  children,
  onClick,
  onContextMenu,
  onDoubleClick,
  ...rest
}, ref) => {
  const styles = {
    ...style,
    ...(0,_utils_styles__WEBPACK_IMPORTED_MODULE_2__.marginStyle)(margin),
    ...(0,_utils_styles__WEBPACK_IMPORTED_MODULE_2__.marginTopStyle)(marginTop),
    ...(0,_utils_styles__WEBPACK_IMPORTED_MODULE_2__.paddingStyle)(padding),
    ...(0,_utils_styles__WEBPACK_IMPORTED_MODULE_2__.paddingTopStyle)(paddingTop),
    ...(0,_utils_styles__WEBPACK_IMPORTED_MODULE_2__.gapStyle)(gap)
  };
  const classes = (0,clsx__WEBPACK_IMPORTED_MODULE_1__["default"])("flex", "flex-col", `justify-${align}`, inlineAlign && `items-${inlineAlign}`, reverseOrder && "flex-col-reverse", className);
  return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsx)(Component, {
    ref: ref,
    id: id,
    style: styles,
    className: classes,
    onClick: onClick,
    onContextMenu: onContextMenu,
    onDoubleClick: onDoubleClick,
    ...rest,
    children: children
  });
});
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (BlockStack);

/***/ },

/***/ "./source/components/blockStack/index.ts"
/*!***********************************************!*\
  !*** ./source/components/blockStack/index.ts ***!
  \***********************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (/* reexport safe */ _BlockStack__WEBPACK_IMPORTED_MODULE_0__["default"])
/* harmony export */ });
/* harmony import */ var _BlockStack__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./BlockStack */ "./source/components/blockStack/BlockStack.tsx");

/***/ },

/***/ "./source/components/button/Button.tsx"
/*!*********************************************!*\
  !*** ./source/components/button/Button.tsx ***!
  \*********************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _status__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../status */ "./source/components/status/index.ts");
/* harmony import */ var _icon__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../icon */ "./source/components/icon/index.ts");
/* harmony import */ var clsx__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! clsx */ "./node_modules/clsx/dist/clsx.mjs");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! react/jsx-runtime */ "react/jsx-runtime");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3___default = /*#__PURE__*/__webpack_require__.n(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__);

const Button = ({
  id,
  style,
  className = "",
  variant = "default",
  iconUrl,
  title,
  color,
  size = "medium",
  iconSize: _iconSize,
  rounded = "sm",
  textTransform = "capitalize",
  wrap = false,
  full = false,
  visible = true,
  disabled = false,
  loading = false,
  loadingIndicator,
  startIcon,
  startIconColor,
  startIconClassName,
  endIcon,
  endIconColor,
  endIconClassName,
  ariaLabel,
  role = "button",
  tabIndex,
  href,
  target,
  rel,
  preventDefault = false,
  stopPropagation = false,
  statusProps,
  children,
  onClick,
  onBlur,
  onFocus,
  onMouseEnter,
  onMouseLeave,
  onDoubleClick
}) => {
  const classes = (0,clsx__WEBPACK_IMPORTED_MODULE_2__["default"])("pn-button", `pn-button--${variant}`, `pn-button--${size}`, `rounded-${rounded}`, color && `text-${color}`, wrap ? "text-wrap" : "text-nowrap", `text-${textTransform}`, full && "w-full", disabled && "pn-button--disabled", className);
  const iconColor = ["default", "secondary", "outlined", "link"].includes(variant) ? "black" : variant === "primary" ? "pure" : variant === "warning" ? "warning" : "error";
  const iconSize = _iconSize ? _iconSize : ["large", "extralarge"].includes(size) ? "xl" : ["supersmall"].includes(size) ? "sm" : "lg";
  const iconProps = {
    color: startIcon ? startIconColor || iconColor : endIconColor || iconColor,
    fontSize: iconSize,
    fontWeight: "medium"
  };
  const content = () => {
    const renderIcon = icon => {
      if (loading) {
        return loadingIndicator ? loadingIndicator : /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsx)(_icon__WEBPACK_IMPORTED_MODULE_1__["default"], {
          name: "progress_activity",
          className: "loading",
          ...iconProps
        });
      } else if (iconUrl) {
        return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsx)("img", {
          referrerPolicy: "no-referrer",
          src: iconUrl,
          alt: "button-icon"
        });
      }
      return icon ? /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsx)(_icon__WEBPACK_IMPORTED_MODULE_1__["default"], {
        name: icon,
        ...iconProps,
        className: startIcon ? startIconClassName : endIconClassName
      }) : null;
    };
    const hasIcons = startIcon || endIcon;
    const showChildren = !(loading && !startIcon && !endIcon);
    if (!hasIcons && loading) {
      return renderIcon();
    }
    return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsxs)(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.Fragment, {
      children: [startIcon && renderIcon(startIcon), showChildren && children, endIcon && renderIcon(endIcon)]
    });
  };
  const commonProps = {
    id,
    style,
    className: classes,
    title,
    "aria-label": ariaLabel,
    role,
    tabIndex,
    onBlur,
    onFocus,
    onMouseEnter,
    onMouseLeave,
    onDoubleClick
  };
  const handleClick = e => {
    if (preventDefault) e.preventDefault();
    if (stopPropagation) e.stopPropagation();
    if (onClick && !disabled && !loading) onClick(e);
  };
  const finalStatusProps = {
    ...statusProps,
    ...(statusProps?.default && {
      size: "extrasmall",
      placement: "right-center",
      right: size === "extrasmall" ? 4 : size === "small" ? 6 : 8
    })
  };
  if (!visible) return null;
  if (href) {
    return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsx)(_status__WEBPACK_IMPORTED_MODULE_0__["default"], {
      ...finalStatusProps,
      children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsx)("a", {
        href: href,
        target: target,
        rel: rel,
        ...commonProps,
        onClick: e => {
          if (disabled || loading) return;
          handleClick(e);
        },
        children: content()
      })
    });
  }
  return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsx)(_status__WEBPACK_IMPORTED_MODULE_0__["default"], {
    widthFull: false,
    ...finalStatusProps,
    children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsx)("button", {
      type: "button",
      ...commonProps,
      onClick: e => {
        if (disabled || loading) return;
        handleClick(e);
      },
      disabled: disabled || loading,
      children: content()
    })
  });
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (Button);

/***/ },

/***/ "./source/components/button/index.ts"
/*!*******************************************!*\
  !*** ./source/components/button/index.ts ***!
  \*******************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (/* reexport safe */ _Button__WEBPACK_IMPORTED_MODULE_0__["default"])
/* harmony export */ });
/* harmony import */ var _Button__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./Button */ "./source/components/button/Button.tsx");

/***/ },

/***/ "./source/components/buttonGroup/ButtonGroup.tsx"
/*!*******************************************************!*\
  !*** ./source/components/buttonGroup/ButtonGroup.tsx ***!
  \*******************************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _button_Button__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../button/Button */ "./source/components/button/Button.tsx");
/* harmony import */ var _card__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../card */ "./source/components/card/index.ts");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! react/jsx-runtime */ "react/jsx-runtime");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_2___default = /*#__PURE__*/__webpack_require__.n(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_2__);

const ButtonGroup = props => {
  const {
    id,
    style,
    className = "",
    background = "white",
    border = "secondary",
    borderStyle = "dashed",
    rounded = "md",
    padding = 10,
    isFitContent = true,
    children,
    buttons = []
  } = props;
  const isSelectable = buttons.length > 0;
  const single = props.select !== "multiple";
  const styles = {
    ...style,
    width: isFitContent ? "fit-content" : "100%"
  };
  const getVariant = (key, variant, activeVariant) => {
    if (!isSelectable) return variant;
    if (single) {
      return props.selectedKey === key ? activeVariant : variant;
    }
    return props.selectedKey.includes(key) ? activeVariant : variant;
  };
  const handleClick = key => {
    if (!isSelectable) return;
    if (single) {
      props.onChange?.(key);
    } else {
      const keys = props.selectedKey;
      props.onChange?.(keys.includes(key) ? keys.filter(k => k !== key) : [...keys, key]);
    }
  };
  return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_2__.jsx)(_card__WEBPACK_IMPORTED_MODULE_1__["default"], {
    id: id,
    flex: true,
    gap: 8,
    padding: padding,
    background: background,
    border: border,
    borderStyle: borderStyle,
    rounded: rounded,
    style: styles,
    className: className,
    children: isSelectable ? buttons.map(({
      key,
      id,
      title,
      style,
      buttonClassName,
      variant = "outlined",
      activeVariant = "primary",
      size = "small",
      rounded,
      textTransform,
      startIcon,
      endIcon,
      statusProps
    }) => /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_2__.jsx)(_button_Button__WEBPACK_IMPORTED_MODULE_0__["default"], {
      id: id,
      style: style,
      className: buttonClassName,
      variant: getVariant(key, variant, activeVariant),
      size: size,
      rounded: rounded,
      textTransform: textTransform,
      startIcon: startIcon,
      endIcon: endIcon,
      statusProps: statusProps,
      onClick: () => handleClick(key),
      children: title
    }, key)) : children
  });
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (ButtonGroup);

/***/ },

/***/ "./source/components/buttonGroup/index.ts"
/*!************************************************!*\
  !*** ./source/components/buttonGroup/index.ts ***!
  \************************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (/* reexport safe */ _ButtonGroup__WEBPACK_IMPORTED_MODULE_0__["default"])
/* harmony export */ });
/* harmony import */ var _ButtonGroup__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./ButtonGroup */ "./source/components/buttonGroup/ButtonGroup.tsx");

/***/ },

/***/ "./source/components/card/Card.tsx"
/*!*****************************************!*\
  !*** ./source/components/card/Card.tsx ***!
  \*****************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _wordpress_element__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @wordpress/element */ "@wordpress/element");
/* harmony import */ var _wordpress_element__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(_wordpress_element__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var _status__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../status */ "./source/components/status/index.ts");
/* harmony import */ var clsx__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! clsx */ "./node_modules/clsx/dist/clsx.mjs");
/* harmony import */ var _utils_styles__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ~/utils/styles */ "./source/utils/styles.ts");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! react/jsx-runtime */ "react/jsx-runtime");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_4___default = /*#__PURE__*/__webpack_require__.n(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_4__);

const Card = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.forwardRef)(({
  id,
  style,
  className = "",
  margin,
  marginTop,
  padding = 20,
  paddingTop,
  background = "light",
  border = "secondary",
  borderStyle = "solid",
  rounded = "lg",
  widthFull = true,
  heightFull = false,
  disabled,
  flex = false,
  align,
  blockAlign,
  direction,
  gap,
  wrap,
  statusProps,
  children,
  onClick,
  onContextMenu,
  onMouseEnter,
  onMouseLeave,
  ...rest
}, ref) => {
  const styles = {
    ...style,
    ...(0,_utils_styles__WEBPACK_IMPORTED_MODULE_3__.marginStyle)(margin),
    ...(0,_utils_styles__WEBPACK_IMPORTED_MODULE_3__.marginTopStyle)(marginTop),
    ...(0,_utils_styles__WEBPACK_IMPORTED_MODULE_3__.paddingStyle)(padding),
    ...(0,_utils_styles__WEBPACK_IMPORTED_MODULE_3__.paddingTopStyle)(paddingTop),
    ...(0,_utils_styles__WEBPACK_IMPORTED_MODULE_3__.gapStyle)(gap)
  };
  const classes = (0,clsx__WEBPACK_IMPORTED_MODULE_2__["default"])(`bg-${background}`, "border", `border-${border}`, `border-${borderStyle}`, `rounded-${rounded}`, "bg-transition", widthFull && "w-full", heightFull && "h-full", disabled && "disabled", flex && "flex", flex && align && `justify-${align}`, flex && blockAlign && `items-${blockAlign}`, flex && (direction === "row" ? "flex-row" : direction === "row-reverse" ? "flex-row-reverse" : direction === "col" ? "flex-col" : direction === "col-reverse" ? "flex-col-reverse" : "flex-row"), flex && (wrap ? "flex-wrap" : "flex-nowrap"), className);
  return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_4__.jsx)(_status__WEBPACK_IMPORTED_MODULE_1__["default"], {
    ...statusProps,
    children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_4__.jsx)("div", {
      ref: ref,
      id: id,
      style: styles,
      className: classes,
      onClick: e => onClick && !disabled && onClick(e),
      onContextMenu: e => onContextMenu && !disabled && onContextMenu(e),
      onMouseEnter: e => onMouseEnter?.(e),
      onMouseLeave: e => onMouseLeave?.(e),
      ...rest,
      children: children
    })
  });
});
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (Card);

/***/ },

/***/ "./source/components/card/index.ts"
/*!*****************************************!*\
  !*** ./source/components/card/index.ts ***!
  \*****************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (/* reexport safe */ _Card__WEBPACK_IMPORTED_MODULE_0__["default"])
/* harmony export */ });
/* harmony import */ var _Card__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./Card */ "./source/components/card/Card.tsx");

/***/ },

/***/ "./source/components/checkbox/Checkbox.tsx"
/*!*************************************************!*\
  !*** ./source/components/checkbox/Checkbox.tsx ***!
  \*************************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var clsx__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! clsx */ "./node_modules/clsx/dist/clsx.mjs");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! react/jsx-runtime */ "react/jsx-runtime");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_1___default = /*#__PURE__*/__webpack_require__.n(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_1__);

const Checkbox = ({
  id,
  name,
  style,
  className = "",
  checked,
  defaultChecked = false,
  size = "medium",
  rounded = "xs",
  title,
  tabIndex,
  ariaLabel,
  readonly,
  visible = true,
  disabled = false,
  onChange
}) => {
  const classes = (0,clsx__WEBPACK_IMPORTED_MODULE_0__["default"])("pn-checkbox", `pn-checkbox--${size}`, `rounded-${rounded}`, disabled && "pn-checkbox--disabled", className);
  const handleCheck = checked => {
    if (readonly) return;
    onChange && onChange(checked);
  };
  if (!visible) return null;
  return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_1__.jsxs)("label", {
    title: title,
    style: style,
    className: classes,
    onClick: e => e.stopPropagation(),
    children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_1__.jsx)("input", {
      id: id,
      type: "checkbox",
      name: name,
      checked: checked,
      defaultChecked: defaultChecked,
      className: "pn-checkbox__input",
      onClick: e => e.stopPropagation(),
      onChange: e => onChange && handleCheck(e.target.checked),
      tabIndex: tabIndex,
      "aria-label": ariaLabel,
      "aria-readonly": readonly || undefined,
      readOnly: readonly,
      disabled: disabled
    }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_1__.jsx)("span", {
      className: "pn-checkbox__box"
    })]
  });
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (Checkbox);

/***/ },

/***/ "./source/components/checkbox/index.ts"
/*!*********************************************!*\
  !*** ./source/components/checkbox/index.ts ***!
  \*********************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (/* reexport safe */ _Checkbox__WEBPACK_IMPORTED_MODULE_0__["default"])
/* harmony export */ });
/* harmony import */ var _Checkbox__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./Checkbox */ "./source/components/checkbox/Checkbox.tsx");

/***/ },

/***/ "./source/components/colorPicker/ColorPicker.tsx"
/*!*******************************************************!*\
  !*** ./source/components/colorPicker/ColorPicker.tsx ***!
  \*******************************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _wordpress_element__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @wordpress/element */ "@wordpress/element");
/* harmony import */ var _wordpress_element__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(_wordpress_element__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var _wordpress_i18n__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! @wordpress/i18n */ "@wordpress/i18n");
/* harmony import */ var _wordpress_i18n__WEBPACK_IMPORTED_MODULE_1___default = /*#__PURE__*/__webpack_require__.n(_wordpress_i18n__WEBPACK_IMPORTED_MODULE_1__);
/* harmony import */ var _status__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../status */ "./source/components/status/index.ts");
/* harmony import */ var _button__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ../button */ "./source/components/button/index.ts");
/* harmony import */ var _icon__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ../icon */ "./source/components/icon/index.ts");
/* harmony import */ var clsx__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! clsx */ "./node_modules/clsx/dist/clsx.mjs");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! react/jsx-runtime */ "react/jsx-runtime");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_6___default = /*#__PURE__*/__webpack_require__.n(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_6__);

const formatColorInput = value => {
  let color = value.trim().toLowerCase();
  if (!color.startsWith("#") && /^[0-9a-f]{3,8}$/i.test(color)) {
    color = `#${color}`;
  }
  return color;
};
const isValidCssColor = color => {
  const {
    style
  } = new Option();
  style.color = color;
  return style.color !== "";
};
const toFullHexColor = color => {
  const hexColor = document.createElement("canvas").getContext("2d");
  if (!hexColor) return null;
  hexColor.fillStyle = "#000";
  hexColor.fillStyle = color;
  const finalColor = hexColor.fillStyle;
  return /^#[0-9a-f]{6}$/i.test(finalColor) ? finalColor : null;
};
const ColorPicker = ({
  id,
  style,
  className = "",
  defaultColor = "#000000",
  selectedColor,
  statusProps,
  onChange
}) => {
  const resolvedDefault = isValidCssColor(defaultColor) && toFullHexColor(defaultColor) ? toFullHexColor(defaultColor) : "#000000";
  const resolvedSelected = selectedColor && isValidCssColor(selectedColor) && toFullHexColor(selectedColor) ? toFullHexColor(selectedColor) : resolvedDefault;
  const [color, setColor] = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useState)(resolvedSelected);
  const [inputValue, setInputValue] = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useState)(resolvedSelected);
  (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useEffect)(() => {
    const fullHex = toFullHexColor(selectedColor || "");
    if (selectedColor && fullHex && fullHex !== color) {
      setColor(fullHex);
      setInputValue(fullHex);
    }
  }, [selectedColor, color]);
  (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useEffect)(() => {
    const formatted = formatColorInput(inputValue);
    if (isValidCssColor(formatted)) {
      const full = toFullHexColor(formatted);
      if (full) {
        setColor(full);
      }
    }
  }, [inputValue]);
  const isValid = isValidCssColor(formatColorInput(inputValue));
  const handleColorBoxChange = e => {
    if (statusProps?.isPro && !pnpnm?.isPro) return;
    setColor(e.target.value);
    setInputValue(e.target.value);
    onChange?.(e.target.value);
  };
  const handleInputChange = e => {
    if (statusProps?.isPro && !pnpnm?.isPro) return;
    setInputValue(e.target.value);
    const formatted = formatColorInput(e.target.value);
    if (isValidCssColor(formatted)) {
      const full = toFullHexColor(formatted);
      if (full) onChange?.(full);
    }
  };
  const handleClear = () => {
    if (statusProps?.isPro && !pnpnm?.isPro) return;
    setColor(resolvedDefault);
    setInputValue(resolvedDefault);
    onChange?.(resolvedDefault);
  };
  const finalStatusProps = {
    ...statusProps,
    size: "extrasmall",
    placement: "right-center",
    right: 8
  };
  return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_6__.jsxs)("div", {
    id: id,
    style: style,
    className: (0,clsx__WEBPACK_IMPORTED_MODULE_5__["default"])("pn-color-picker", className),
    children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_6__.jsxs)("div", {
      className: "pn-color-picker__wrapper",
      children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_6__.jsx)(_status__WEBPACK_IMPORTED_MODULE_2__["default"], {
        ...finalStatusProps,
        children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_6__.jsx)("input", {
          type: "color",
          value: color,
          onChange: handleColorBoxChange,
          className: "pn-color-picker__wrapper-picker-box",
          disabled: statusProps?.isPro && !pnpnm?.isPro
        })
      }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_6__.jsx)(_status__WEBPACK_IMPORTED_MODULE_2__["default"], {
        ...finalStatusProps,
        children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_6__.jsx)("input", {
          type: "text",
          value: inputValue,
          onChange: handleInputChange,
          placeholder: "Type Color",
          className: (0,clsx__WEBPACK_IMPORTED_MODULE_5__["default"])("pn-color-picker__wrapper-picker-input", !isValid && "error"),
          disabled: statusProps?.isPro && !pnpnm?.isPro
        })
      })]
    }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_6__.jsx)(_button__WEBPACK_IMPORTED_MODULE_3__["default"], {
      variant: "outlined",
      size: "small",
      onClick: handleClear,
      children: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_1__.__)("Clear", "ninja-media")
    })]
  });
};
const ColorBox = ({
  size = "small",
  colors,
  selectedColor,
  onSelect
}) => {
  return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_6__.jsx)("div", {
    className: (0,clsx__WEBPACK_IMPORTED_MODULE_5__["default"])("pn-color-box-wrapper", size === "large" && "pn-color-box-wrapper--large"),
    children: colors?.map((color, index) => {
      const hex = toFullHexColor(color);
      if (!hex) return null;
      const isActive = toFullHexColor(selectedColor) === hex;
      return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_6__.jsx)("div", {
        className: (0,clsx__WEBPACK_IMPORTED_MODULE_5__["default"])("pn-color-box", size === "large" && "pn-color-box--large", isActive && "active"),
        style: {
          backgroundColor: hex,
          color: isActive ? "#fff" : "#000"
        },
        onClick: () => onSelect(hex),
        children: isActive && /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_6__.jsx)(_icon__WEBPACK_IMPORTED_MODULE_4__["default"], {
          name: "check",
          color: "pure",
          fontSize: "lg"
        })
      }, index);
    })
  });
};
ColorPicker.ColorBox = ColorBox;
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (ColorPicker);

/***/ },

/***/ "./source/components/colorPicker/index.ts"
/*!************************************************!*\
  !*** ./source/components/colorPicker/index.ts ***!
  \************************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (/* reexport safe */ _ColorPicker__WEBPACK_IMPORTED_MODULE_0__["default"])
/* harmony export */ });
/* harmony import */ var _ColorPicker__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./ColorPicker */ "./source/components/colorPicker/ColorPicker.tsx");

/***/ },

/***/ "./source/components/contextMenu/ContextMenu.tsx"
/*!*******************************************************!*\
  !*** ./source/components/contextMenu/ContextMenu.tsx ***!
  \*******************************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   Item: () => (/* binding */ Item),
/* harmony export */   Menu: () => (/* binding */ Menu),
/* harmony export */   MenuProvider: () => (/* binding */ MenuProvider),
/* harmony export */   Separator: () => (/* binding */ Separator),
/* harmony export */   Submenu: () => (/* binding */ Submenu),
/* harmony export */   useContextMenu: () => (/* binding */ useContextMenu)
/* harmony export */ });
/* harmony import */ var _icon__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../icon */ "./source/components/icon/index.ts");
/* harmony import */ var clsx__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! clsx */ "./node_modules/clsx/dist/clsx.mjs");
/* harmony import */ var _wordpress_element__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! @wordpress/element */ "@wordpress/element");
/* harmony import */ var _wordpress_element__WEBPACK_IMPORTED_MODULE_2___default = /*#__PURE__*/__webpack_require__.n(_wordpress_element__WEBPACK_IMPORTED_MODULE_2__);
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! react/jsx-runtime */ "react/jsx-runtime");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3___default = /*#__PURE__*/__webpack_require__.n(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__);

const ContextMenuContext = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_2__.createContext)(undefined);
const useContextMenu = () => {
  const context = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_2__.useContext)(ContextMenuContext);
  if (!context) {
    throw new Error("useContextMenu must be used within a ContextMenuProvider");
  }
  return context;
};
const MenuProvider = ({
  children
}) => {
  const [activeMenu, setActiveMenu] = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_2__.useState)(null);
  const show = (id, event, props) => {
    event.preventDefault();
    setActiveMenu(prev => ({
      id,
      position: {
        x: event.clientX,
        y: event.clientY
      },
      props,
      showCount: prev && prev.id === id ? prev.showCount + 1 : 1
    }));
  };
  const hide = () => {
    setActiveMenu(null);
  };
  (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_2__.useEffect)(() => {
    const handleKeyDown = event => {
      if (event.key === "Escape") {
        hide();
      }
    };
    document.addEventListener("keydown", handleKeyDown);
    document.addEventListener("wheel", () => {
      hide();
    });
    document.addEventListener("click", () => {
      hide();
    });
    return () => {
      document.removeEventListener("keydown", handleKeyDown);
      document.removeEventListener("wheel", () => {
        hide();
      });
    };
  }, [hide]);
  return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsx)(ContextMenuContext.Provider, {
    value: {
      show,
      hide,
      activeMenu
    },
    children: children
  });
};
const Menu = ({
  id,
  style,
  className,
  children
}) => {
  const {
    activeMenu,
    hide
  } = useContextMenu();
  const menuRef = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_2__.useRef)(null);
  const [animate, setAnimate] = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_2__.useState)(false);
  (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_2__.useEffect)(() => {
    if (activeMenu?.id === id) {
      setAnimate(true);
      const timer = setTimeout(() => setAnimate(false), 200);
      return () => clearTimeout(timer);
    }
  }, [activeMenu?.showCount, id]);
  (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_2__.useEffect)(() => {
    const handleClickOutside = event => {
      if (menuRef.current && !menuRef.current.contains(event.target)) {
        hide();
      }
    };
    document.addEventListener("click", handleClickOutside);
    return () => document.removeEventListener("click", handleClickOutside);
  }, [hide]);
  if (!activeMenu || activeMenu.id !== id) return null;
  const adjustPosition = () => {
    if (!menuRef.current) return {
      x: activeMenu.position.x,
      y: activeMenu.position.y
    };
    const rect = menuRef.current.getBoundingClientRect();
    const windowWidth = window.innerWidth;
    const windowHeight = window.innerHeight;
    let x = activeMenu.position.x;
    let y = activeMenu.position.y;
    if (x + rect.width > windowWidth) x = windowWidth - rect.width - 10;
    if (y + rect.height > windowHeight) y = windowHeight - rect.height - 10;
    return {
      x,
      y
    };
  };
  const {
    x,
    y
  } = adjustPosition();
  return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsx)("div", {
    id: id,
    ref: menuRef,
    style: {
      ...style,
      top: y,
      left: x,
      position: "fixed"
    },
    className: (0,clsx__WEBPACK_IMPORTED_MODULE_1__["default"])("pn-context-menu-content", className),
    role: "menu",
    "aria-orientation": "vertical",
    children: children({
      props: activeMenu?.props
    })
  }, `menu-${id}-${activeMenu.showCount}`);
};
const Item = ({
  style,
  className = "",
  children,
  onClick,
  disabled
}) => {
  const {
    activeMenu
  } = useContextMenu();
  const handleClick = event => {
    if (disabled) return;
    onClick?.({
      event,
      props: activeMenu?.props
    });
  };
  return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsx)("div", {
    style: style,
    className: (0,clsx__WEBPACK_IMPORTED_MODULE_1__["default"])("pn-context-menu-item", disabled && "disabled", className),
    onClick: handleClick,
    role: "menuitem",
    "aria-disabled": disabled,
    tabIndex: disabled ? -1 : 0,
    children: children
  });
};
const Separator = ({
  style,
  className
}) => {
  return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsx)("div", {
    style: style,
    className: (0,clsx__WEBPACK_IMPORTED_MODULE_1__["default"])("pn-context-menu-separator", className),
    role: "separator"
  });
};
const Submenu = ({
  trigger = "hover",
  label,
  style,
  className = "",
  children,
  disabled
}) => {
  const [isOpen, setIsOpen] = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_2__.useState)(false);
  const triggerRef = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_2__.useRef)(null);
  const submenuRef = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_2__.useRef)(null);
  const [position, setPosition] = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_2__.useState)({
    x: 0,
    y: 0
  });
  const [animate, setAnimate] = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_2__.useState)(false);
  const closeTimeout = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_2__.useRef)(null);
  const openMenu = () => {
    clearTimeout(closeTimeout.current);
    if (!disabled) setIsOpen(true);
  };
  const closeMenu = () => {
    closeTimeout.current = setTimeout(() => {
      setIsOpen(false);
    }, 60000);
  };
  (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_2__.useEffect)(() => {
    if (trigger !== "click" || !isOpen) return;
    const handleOutsideClick = e => {
      if (submenuRef.current?.contains(e.target) || triggerRef.current?.contains(e.target)) return;
      setIsOpen(false);
    };
    document.addEventListener("mousedown", handleOutsideClick);
    return () => document.removeEventListener("mousedown", handleOutsideClick);
  }, [trigger, isOpen]);
  (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_2__.useEffect)(() => {
    if (isOpen && triggerRef.current && submenuRef.current) {
      const triggerRect = triggerRef.current.getBoundingClientRect();
      const submenuRect = submenuRef.current.getBoundingClientRect();
      let x = triggerRect.right;
      let y = triggerRect.top;
      const windowWidth = window.innerWidth;
      const windowHeight = window.innerHeight;
      if (x + submenuRect.width > windowWidth) {
        x = triggerRect.left - submenuRect.width;
      }
      if (y + submenuRect.height > windowHeight) {
        y = windowHeight - submenuRect.height - 10;
      }
      setPosition({
        x,
        y
      });
      setAnimate(true);
      const timer = setTimeout(() => setAnimate(false), 200);
      return () => clearTimeout(timer);
    }
  }, [isOpen]);
  (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_2__.useEffect)(() => {
    return () => clearTimeout(closeTimeout.current);
  }, []);
  return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsxs)("div", {
    className: "pn-context-menu-sub",
    onMouseEnter: trigger === "hover" ? openMenu : undefined,
    onMouseLeave: trigger === "hover" ? closeMenu : undefined,
    children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsxs)("div", {
      ref: triggerRef,
      style: style,
      className: (0,clsx__WEBPACK_IMPORTED_MODULE_1__["default"])("pn-context-menu-sub-trigger", disabled && "disabled", className),
      role: "menuitem",
      "aria-haspopup": "true",
      "aria-expanded": isOpen,
      tabIndex: disabled ? -1 : 0,
      onClick: trigger === "click" ? e => {
        e.stopPropagation();
        if (!disabled) setIsOpen(prev => !prev);
      } : undefined,
      children: [label, /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsx)(_icon__WEBPACK_IMPORTED_MODULE_0__["default"], {
        name: "arrow_right",
        fontSize: "lg"
      })]
    }), isOpen && /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsx)("div", {
      ref: submenuRef,
      style: {
        top: position.y,
        left: position.x,
        position: "fixed",
        zIndex: 9999
      },
      className: (0,clsx__WEBPACK_IMPORTED_MODULE_1__["default"])("pn-context-menu-sub-content", animate && "animate"),
      role: "menu",
      "aria-orientation": "vertical",
      onClick: e => e.stopPropagation(),
      children: children
    })]
  });
};

/***/ },

/***/ "./source/components/description/Description.tsx"
/*!*******************************************************!*\
  !*** ./source/components/description/Description.tsx ***!
  \*******************************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var clsx__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! clsx */ "./node_modules/clsx/dist/clsx.mjs");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! react/jsx-runtime */ "react/jsx-runtime");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_1___default = /*#__PURE__*/__webpack_require__.n(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_1__);

const Description = ({
  id,
  style,
  className = "",
  as: Component = "span",
  limit,
  text,
  color = "descgray",
  size = "xs",
  weight = "normal",
  textTransform = "none",
  align = "left"
}) => {
  const classes = (0,clsx__WEBPACK_IMPORTED_MODULE_0__["default"])(`text-${color}`, `text-${size}`, `font-${weight}`, `text-${textTransform}`, `text-${align}`, className);
  const truncatedText = limit && typeof text === "string" ? text.slice(0, limit) + (text.length > limit ? "..." : "") : text;
  const renderContent = content => {
    if (typeof content === "string") {
      const parts = content.split(/(\{.*?\})/g);
      return parts.map((part, index) => {
        if (/^\{.*\}$/.test(part)) {
          return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_1__.jsx)("strong", {
            children: part.replace(/[{}]/g, "")
          }, index);
        }
        return part;
      });
    }
    return content;
  };
  return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_1__.jsx)(Component, {
    id: id,
    style: style,
    className: classes,
    children: renderContent(truncatedText)
  });
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (Description);

/***/ },

/***/ "./source/components/description/index.ts"
/*!************************************************!*\
  !*** ./source/components/description/index.ts ***!
  \************************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (/* reexport safe */ _Description__WEBPACK_IMPORTED_MODULE_0__["default"])
/* harmony export */ });
/* harmony import */ var _Description__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./Description */ "./source/components/description/Description.tsx");

/***/ },

/***/ "./source/components/disabled/Disabled.tsx"
/*!*************************************************!*\
  !*** ./source/components/disabled/Disabled.tsx ***!
  \*************************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _blockStack__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../blockStack */ "./source/components/blockStack/index.ts");
/* harmony import */ var clsx__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! clsx */ "./node_modules/clsx/dist/clsx.mjs");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! react/jsx-runtime */ "react/jsx-runtime");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_2___default = /*#__PURE__*/__webpack_require__.n(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_2__);

const Disabled = ({
  id,
  style,
  className = "",
  depend = false,
  dependOn = "",
  dependOnExact = false,
  gap = 20,
  children
}) => {
  const classes = (0,clsx__WEBPACK_IMPORTED_MODULE_1__["default"])(depend && "pn-disabled", className);
  const handleClick = e => {
    const dependent = document.getElementById(dependOn);
    if (dependent && depend) {
      e.stopPropagation();
      const label = dependOnExact ? dependent : dependent.parentElement;
      if (!label) return;
      label.classList.add("pn-blink");
      setTimeout(() => {
        label.classList.remove("pn-blink");
      }, 500);
    }
  };
  return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_2__.jsx)(_blockStack__WEBPACK_IMPORTED_MODULE_0__["default"], {
    id: id,
    style: style,
    gap: gap,
    className: classes,
    onClick: handleClick,
    children: children
  });
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (Disabled);

/***/ },

/***/ "./source/components/disabled/index.ts"
/*!*********************************************!*\
  !*** ./source/components/disabled/index.ts ***!
  \*********************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (/* reexport safe */ _Disabled__WEBPACK_IMPORTED_MODULE_0__["default"])
/* harmony export */ });
/* harmony import */ var _Disabled__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./Disabled */ "./source/components/disabled/Disabled.tsx");

/***/ },

/***/ "./source/components/divider/Divider.tsx"
/*!***********************************************!*\
  !*** ./source/components/divider/Divider.tsx ***!
  \***********************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var clsx__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! clsx */ "./node_modules/clsx/dist/clsx.mjs");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! react/jsx-runtime */ "react/jsx-runtime");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_1___default = /*#__PURE__*/__webpack_require__.n(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_1__);

const Divider = ({
  id,
  style,
  className,
  border,
  borderWidth = 1,
  borderStyle = "solid",
  color = "light",
  margin,
  marginTop,
  marginBottom,
  padding,
  paddingTop,
  paddingBottom,
  variant = "horizontal",
  height = "100%",
  width = "100%"
}) => {
  const borderSide = border ?? (variant === "horizontal" ? "bottom" : "right");
  const dividerStyle = {
    ...(borderSide === "top" && {
      borderTopWidth: borderWidth,
      borderTopStyle: borderStyle
    }),
    ...(borderSide === "right" && {
      borderRightWidth: borderWidth,
      borderRightStyle: borderStyle
    }),
    ...(borderSide === "bottom" && {
      borderBottomWidth: borderWidth,
      borderBottomStyle: borderStyle
    }),
    ...(borderSide === "left" && {
      borderLeftWidth: borderWidth,
      borderLeftStyle: borderStyle
    }),
    ...(variant === "horizontal" ? {
      width
    } : {
      height
    }),
    margin,
    marginTop,
    marginBottom,
    padding,
    paddingTop,
    paddingBottom,
    ...style
  };
  return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_1__.jsx)("div", {
    id: id,
    style: dividerStyle,
    className: (0,clsx__WEBPACK_IMPORTED_MODULE_0__["default"])(`border-${color}`, className)
  });
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (Divider);

/***/ },

/***/ "./source/components/divider/index.ts"
/*!********************************************!*\
  !*** ./source/components/divider/index.ts ***!
  \********************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (/* reexport safe */ _Divider__WEBPACK_IMPORTED_MODULE_0__["default"])
/* harmony export */ });
/* harmony import */ var _Divider__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./Divider */ "./source/components/divider/Divider.tsx");

/***/ },

/***/ "./source/components/dropdown/Dropdown.tsx"
/*!*************************************************!*\
  !*** ./source/components/dropdown/Dropdown.tsx ***!
  \*************************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _inlineStack__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../inlineStack */ "./source/components/inlineStack/index.ts");
/* harmony import */ var _icon__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../icon */ "./source/components/icon/index.ts");
/* harmony import */ var _text__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../text */ "./source/components/text/index.ts");
/* harmony import */ var clsx__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! clsx */ "./node_modules/clsx/dist/clsx.mjs");
/* harmony import */ var _wordpress_element__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! @wordpress/element */ "@wordpress/element");
/* harmony import */ var _wordpress_element__WEBPACK_IMPORTED_MODULE_4___default = /*#__PURE__*/__webpack_require__.n(_wordpress_element__WEBPACK_IMPORTED_MODULE_4__);
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! react/jsx-runtime */ "react/jsx-runtime");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_5___default = /*#__PURE__*/__webpack_require__.n(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_5__);

const DropdownContext = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_4__.createContext)(undefined);
const useDropdownContext = () => {
  const context = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_4__.useContext)(DropdownContext);
  if (!context) {
    throw new Error("useDropdownContext must be used within a Dropdown component");
  }
  return context;
};
const Dropdown = ({
  children,
  style,
  className = "",
  outSideClick = true
}) => {
  const [open, setOpen] = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_4__.useState)(false);
  const wrapperRef = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_4__.useRef)(null);
  (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_4__.useEffect)(() => {
    if (!outSideClick) return;
    const handleClickOutside = event => {
      if (wrapperRef.current && !wrapperRef.current.contains(event.target)) {
        setOpen(false);
      }
    };
    document.addEventListener("mousedown", handleClickOutside);
    return () => document.removeEventListener("mousedown", handleClickOutside);
  }, []);
  return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_5__.jsx)(DropdownContext.Provider, {
    value: {
      open,
      setOpen
    },
    children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_5__.jsx)("div", {
      style: style,
      className: (0,clsx__WEBPACK_IMPORTED_MODULE_3__["default"])("pn-dropdown", className),
      ref: wrapperRef,
      children: children
    })
  });
};
const Trigger = ({
  children,
  openStatus,
  style,
  className = "",
  disabled = false
}) => {
  const {
    open,
    setOpen
  } = useDropdownContext();
  (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_4__.useEffect)(() => {
    if (openStatus !== undefined) {
      setOpen(openStatus);
    }
  }, [openStatus]);
  return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_5__.jsx)("div", {
    style: style,
    className: (0,clsx__WEBPACK_IMPORTED_MODULE_3__["default"])("pn-dropdown__trigger", className),
    onClick: e => {
      e.stopPropagation();
      if (disabled) return;
      setOpen(!open);
    },
    children: typeof children === "function" ? children({
      open,
      setOpen
    }) : children
  });
};
const TriggerArrow = ({
  style,
  arrowColor = "black",
  arrowSize = "lg"
}) => {
  const {
    open
  } = useDropdownContext();
  return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_5__.jsx)("div", {
    style: style,
    className: (0,clsx__WEBPACK_IMPORTED_MODULE_3__["default"])("pn-dropdown__trigger-arrow", {
      "pn-dropdown__trigger-arrow--active": open
    }),
    children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_5__.jsx)(_icon__WEBPACK_IMPORTED_MODULE_1__["default"], {
      name: "keyboard_arrow_down",
      color: arrowColor,
      fontSize: arrowSize,
      fontWeight: "semibold"
    })
  });
};
const Content = ({
  style,
  className = "",
  rounded = "md",
  border = false,
  borderColor = "light",
  borderStyle = "solid",
  shadow = true,
  fullWidth,
  children,
  position
}) => {
  const {
    open,
    setOpen
  } = useDropdownContext();
  const contentRef = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_4__.useRef)(null);
  (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_4__.useEffect)(() => {
    if (!open || !contentRef.current) return;
    if (position) return;
    const content = contentRef.current;
    const trigger = content.parentElement?.querySelector(".pn-dropdown__trigger");
    if (!trigger) return;
    const triggerRect = trigger.getBoundingClientRect();
    const contentRect = content.getBoundingClientRect();
    content.style.left = "0";
    content.style.right = "auto";
    content.style.top = "calc(100% + 5px)";
    content.style.bottom = "auto";
    if (triggerRect.right + contentRect.width > window.innerWidth) {
      content.style.left = "auto";
      content.style.right = "0";
    }
    if (triggerRect.bottom + contentRect.height > window.innerHeight) {
      content.style.top = "auto";
      content.style.bottom = "calc(100% + 5px)";
    }
  }, [open]);
  return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_5__.jsx)("div", {
    ref: contentRef,
    style: {
      ...style,
      ...(position || {})
    },
    className: (0,clsx__WEBPACK_IMPORTED_MODULE_3__["default"])("pn-dropdown__content", `rounded-${rounded}`, border && `border border-${borderColor} border-${borderStyle}`, className, {
      "pn-dropdown__content--active": open,
      "pn-dropdown__content--full": fullWidth,
      "pn-dropdown__content--shadow": shadow
    }),
    children: typeof children === "function" ? children({
      open,
      setOpen
    }) : children
  });
};
const MenuLabel = ({
  className = "",
  children,
  tag = "p"
}) => {
  const {
    open,
    setOpen
  } = useDropdownContext();
  return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_5__.jsx)(_text__WEBPACK_IMPORTED_MODULE_2__["default"], {
    as: tag,
    className: (0,clsx__WEBPACK_IMPORTED_MODULE_3__["default"])("pn-dropdown__menu-label", className),
    children: typeof children === "function" ? children({
      open,
      setOpen
    }) : children
  });
};
const MenuSeparator = () => {
  return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_5__.jsx)("div", {
    className: "pn-dropdown__menu-separator"
  });
};
const MenuItem = ({
  title,
  children,
  onClick,
  isActive,
  activeIcon = false,
  iconPosition = "left",
  activeBg = false,
  style,
  className = ""
}) => {
  return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_5__.jsx)("div", {
    title: title,
    style: style,
    className: (0,clsx__WEBPACK_IMPORTED_MODULE_3__["default"])("pn-dropdown__menu-item", className, {
      "pn-dropdown__menu-item--active": isActive,
      "pn-dropdown__menu-item--activeBg": activeBg
    }),
    onClick: e => {
      e.stopPropagation();
      onClick && onClick();
    },
    children: activeIcon ? /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_5__.jsxs)(_inlineStack__WEBPACK_IMPORTED_MODULE_0__["default"], {
      gap: 5,
      align: iconPosition === "left" ? "start" : "between",
      direction: iconPosition === "left" ? "row" : "row-reverse",
      wrap: false,
      className: "w-full",
      children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_5__.jsx)("div", {
        className: "pn-dropdown__menu-item-icon",
        children: isActive && /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_5__.jsx)(_icon__WEBPACK_IMPORTED_MODULE_1__["default"], {
          name: "check",
          fontSize: "md"
        })
      }), children]
    }) : children
  });
};
Dropdown.Trigger = Trigger;
Dropdown.TriggerArrow = TriggerArrow;
Dropdown.Content = Content;
Dropdown.MenuLabel = MenuLabel;
Dropdown.MenuSeparator = MenuSeparator;
Dropdown.MenuItem = MenuItem;
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (Dropdown);

/***/ },

/***/ "./source/components/dropdown/index.ts"
/*!*********************************************!*\
  !*** ./source/components/dropdown/index.ts ***!
  \*********************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (/* reexport safe */ _Dropdown__WEBPACK_IMPORTED_MODULE_0__["default"])
/* harmony export */ });
/* harmony import */ var _Dropdown__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./Dropdown */ "./source/components/dropdown/Dropdown.tsx");

/***/ },

/***/ "./source/components/field/Field.tsx"
/*!*******************************************!*\
  !*** ./source/components/field/Field.tsx ***!
  \*******************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _settingsField__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../settingsField */ "./source/components/settingsField/index.ts");
/* harmony import */ var _blockStack__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../blockStack */ "./source/components/blockStack/index.ts");
/* harmony import */ var _divider__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../divider */ "./source/components/divider/index.ts");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! react/jsx-runtime */ "react/jsx-runtime");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3___default = /*#__PURE__*/__webpack_require__.n(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__);

const Field = ({
  id,
  style,
  className,
  gap = 20,
  title,
  description,
  docLink,
  statusProps,
  children
}) => {
  return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsxs)(_settingsField__WEBPACK_IMPORTED_MODULE_0__["default"], {
    padding: 0,
    contentPadding: 20,
    gap: 0,
    title: title,
    titleSize: "xl",
    description: description,
    docLink: docLink,
    statusProps: statusProps,
    isIgnoreChildren: true,
    children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsx)(_divider__WEBPACK_IMPORTED_MODULE_2__["default"], {}), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsx)(_blockStack__WEBPACK_IMPORTED_MODULE_1__["default"], {
      id: id,
      padding: 20,
      gap: gap,
      style: style,
      className: className,
      children: children
    })]
  });
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (Field);

/***/ },

/***/ "./source/components/field/index.ts"
/*!******************************************!*\
  !*** ./source/components/field/index.ts ***!
  \******************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (/* reexport safe */ _Field__WEBPACK_IMPORTED_MODULE_0__["default"])
/* harmony export */ });
/* harmony import */ var _Field__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./Field */ "./source/components/field/Field.tsx");

/***/ },

/***/ "./source/components/gridStack/GridStack.tsx"
/*!***************************************************!*\
  !*** ./source/components/gridStack/GridStack.tsx ***!
  \***************************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var clsx__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! clsx */ "./node_modules/clsx/dist/clsx.mjs");
/* harmony import */ var _utils_styles__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ~/utils/styles */ "./source/utils/styles.ts");
/* harmony import */ var _wordpress_element__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! @wordpress/element */ "@wordpress/element");
/* harmony import */ var _wordpress_element__WEBPACK_IMPORTED_MODULE_2___default = /*#__PURE__*/__webpack_require__.n(_wordpress_element__WEBPACK_IMPORTED_MODULE_2__);
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! react/jsx-runtime */ "react/jsx-runtime");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3___default = /*#__PURE__*/__webpack_require__.n(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__);

const GridStack = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_2__.forwardRef)(({
  id,
  style,
  className = "",
  margin,
  marginTop,
  padding,
  paddingTop,
  as,
  children,
  columns = 1,
  rows,
  min = "0",
  max = "1fr",
  gap = 16,
  rounded = "none",
  align,
  blockAlign,
  templateAreas
}, ref) => {
  let gridTemplateColumns = "";
  if (columns === "auto-fit" || columns === "auto-fill") {
    gridTemplateColumns = `repeat(${columns}, minmax(${min}, ${max}))`;
  } else if (typeof columns === "number") {
    gridTemplateColumns = `repeat(${columns}, minmax(${min}, ${max}))`;
  } else {
    gridTemplateColumns = columns;
  }
  const classes = ["grid"];
  if (align) classes.push(`justify-${align}`);
  if (blockAlign) classes.push(`items-${blockAlign}`);
  if (rounded) classes.push(`rounded-${rounded}`);
  if (className) classes.push(className);
  const computedStyle = {
    gridTemplateColumns,
    ...(rows ? {
      gridTemplateRows: rows
    } : {}),
    ...(templateAreas && {
      gridTemplateAreas: templateAreas.map(r => `"${r}"`).join(" ")
    }),
    ...style,
    ...(0,_utils_styles__WEBPACK_IMPORTED_MODULE_1__.gapStyle)(gap),
    ...(0,_utils_styles__WEBPACK_IMPORTED_MODULE_1__.marginStyle)(margin),
    ...(0,_utils_styles__WEBPACK_IMPORTED_MODULE_1__.marginTopStyle)(marginTop),
    ...(0,_utils_styles__WEBPACK_IMPORTED_MODULE_1__.paddingStyle)(padding),
    ...(0,_utils_styles__WEBPACK_IMPORTED_MODULE_1__.paddingTopStyle)(paddingTop)
  };
  const enhancedChildren = _wordpress_element__WEBPACK_IMPORTED_MODULE_2__.Children.map(children, child => {
    if (!(0,_wordpress_element__WEBPACK_IMPORTED_MODULE_2__.isValidElement)(child)) return child;
    const el = child;
    const area = el.props.gridArea;
    return (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_2__.cloneElement)(el, {
      style: {
        ...(el.props.style || {}),
        ...(area ? {
          gridArea: area
        } : {})
      }
    });
  });

  // const Tag = as || "div";
  const Tag = as || "div";
  return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsx)(Tag, {
    ref: ref,
    id: id,
    style: computedStyle,
    className: (0,clsx__WEBPACK_IMPORTED_MODULE_0__["default"])(...classes),
    children: enhancedChildren
  });
});
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (GridStack);

/***/ },

/***/ "./source/components/gridStack/index.ts"
/*!**********************************************!*\
  !*** ./source/components/gridStack/index.ts ***!
  \**********************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (/* reexport safe */ _GridStack__WEBPACK_IMPORTED_MODULE_0__["default"])
/* harmony export */ });
/* harmony import */ var _GridStack__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./GridStack */ "./source/components/gridStack/GridStack.tsx");

/***/ },

/***/ "./source/components/icon/Icon.tsx"
/*!*****************************************!*\
  !*** ./source/components/icon/Icon.tsx ***!
  \*****************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var clsx__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! clsx */ "./node_modules/clsx/dist/clsx.mjs");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! react/jsx-runtime */ "react/jsx-runtime");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_1___default = /*#__PURE__*/__webpack_require__.n(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_1__);

const Icon = ({
  id,
  name,
  title,
  style,
  className = "",
  color = "black",
  fontSize = "md",
  fontWeight = "medium",
  onClick
}) => {
  const classes = (0,clsx__WEBPACK_IMPORTED_MODULE_0__["default"])(name, "pn-icon", `text-${color}`, `text-${fontSize}`, `font-${fontWeight}`, className);
  return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_1__.jsx)("span", {
    id: id,
    title: title,
    style: style,
    className: classes,
    onClick: onClick,
    children: name
  });
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (Icon);

/***/ },

/***/ "./source/components/icon/index.ts"
/*!*****************************************!*\
  !*** ./source/components/icon/index.ts ***!
  \*****************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (/* reexport safe */ _Icon__WEBPACK_IMPORTED_MODULE_0__["default"])
/* harmony export */ });
/* harmony import */ var _Icon__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./Icon */ "./source/components/icon/Icon.tsx");

/***/ },

/***/ "./source/components/iconButton/IconButton.tsx"
/*!*****************************************************!*\
  !*** ./source/components/iconButton/IconButton.tsx ***!
  \*****************************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _icon_Icon__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../icon/Icon */ "./source/components/icon/Icon.tsx");
/* harmony import */ var clsx__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! clsx */ "./node_modules/clsx/dist/clsx.mjs");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! react/jsx-runtime */ "react/jsx-runtime");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_2___default = /*#__PURE__*/__webpack_require__.n(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_2__);

const IconButton = ({
  name,
  iconUrl,
  svgIcon: SvgIcon,
  id,
  style,
  className = "",
  iconStyle,
  iconClassName,
  variant = "default",
  title,
  color,
  size = "medium",
  fontSize = "xl",
  fontWeight = "medium",
  rounded = "sm",
  border = true,
  borderColor = "transparent",
  borderStyle = "solid",
  full = false,
  visible = true,
  disabled = false,
  loading = false,
  loadingIndicator,
  ariaLabel,
  role = "button",
  tabIndex,
  href,
  target,
  rel,
  preventDefault = false,
  stopPropagation = false,
  children,
  onClick,
  onBlur,
  onFocus,
  onMouseEnter,
  onMouseLeave,
  onDoubleClick
}) => {
  const classes = (0,clsx__WEBPACK_IMPORTED_MODULE_1__["default"])("pn-icon-button", `pn-icon-button--${variant}`, `pn-icon-button--${size}`, `rounded-${rounded}`, border && `border border-${borderColor} border-${borderStyle}`, full && "w-full", disabled && "pn-icon-button--disabled", className);
  const iconColor = color ? color : ["default", "secondary", "outlined", "link"].includes(variant) ? "black" : variant === "primary" ? "white" : variant === "warning" ? "warning" : variant === "white" ? "primary" : variant === "light" ? "black" : "error";
  const content = loading ? loadingIndicator ?? /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_2__.jsx)(_icon_Icon__WEBPACK_IMPORTED_MODULE_0__["default"], {
    name: "progress_activity",
    color: color ?? iconColor,
    fontSize: fontSize,
    fontWeight: fontWeight,
    className: "loading"
  }) : (name && /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_2__.jsx)(_icon_Icon__WEBPACK_IMPORTED_MODULE_0__["default"], {
    name: name,
    color: color ?? iconColor,
    fontSize: fontSize,
    fontWeight: fontWeight,
    style: iconStyle,
    className: iconClassName
  })) ?? children ?? (iconUrl && /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_2__.jsx)("img", {
    referrerPolicy: "no-referrer",
    src: iconUrl,
    alt: "icon",
    style: {
      padding: "3px"
    }
  })) ?? (SvgIcon && /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_2__.jsx)(SvgIcon, {}));
  const commonProps = {
    id,
    style,
    className: classes,
    title,
    "aria-label": ariaLabel,
    role,
    tabIndex,
    onBlur,
    onFocus,
    onMouseEnter,
    onMouseLeave,
    onDoubleClick
  };
  const handleClick = e => {
    if (preventDefault) e.preventDefault();
    if (stopPropagation) e.stopPropagation();
    if (onClick && !disabled && !loading) onClick(e);
  };
  if (!visible) return null;
  if (href) {
    return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_2__.jsx)("a", {
      href: href,
      target: target,
      rel: rel,
      ...commonProps,
      onClick: e => {
        if (disabled || loading) return;
        handleClick(e);
      },
      children: content
    });
  }
  return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_2__.jsx)("button", {
    type: "button",
    ...commonProps,
    onClick: e => {
      if (disabled || loading) return;
      handleClick(e);
    },
    disabled: disabled || loading,
    children: content
  });
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (IconButton);

/***/ },

/***/ "./source/components/iconButton/index.ts"
/*!***********************************************!*\
  !*** ./source/components/iconButton/index.ts ***!
  \***********************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (/* reexport safe */ _IconButton__WEBPACK_IMPORTED_MODULE_0__["default"])
/* harmony export */ });
/* harmony import */ var _IconButton__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./IconButton */ "./source/components/iconButton/IconButton.tsx");

/***/ },

/***/ "./source/components/inlineStack/InlineStack.tsx"
/*!*******************************************************!*\
  !*** ./source/components/inlineStack/InlineStack.tsx ***!
  \*******************************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _wordpress_element__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @wordpress/element */ "@wordpress/element");
/* harmony import */ var _wordpress_element__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(_wordpress_element__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var clsx__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! clsx */ "./node_modules/clsx/dist/clsx.mjs");
/* harmony import */ var _utils_styles__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ~/utils/styles */ "./source/utils/styles.ts");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! react/jsx-runtime */ "react/jsx-runtime");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3___default = /*#__PURE__*/__webpack_require__.n(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__);

const InlineStack = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.forwardRef)(({
  id,
  title,
  style,
  className = "",
  margin,
  marginTop,
  padding,
  paddingTop,
  as: Component = "div",
  align = "start",
  blockAlign = "center",
  direction = "row",
  gap,
  wrap = true,
  children,
  onClick,
  onContextMenu,
  onDoubleClick,
  ...rest
}, ref) => {
  const styles = {
    ...style,
    ...(0,_utils_styles__WEBPACK_IMPORTED_MODULE_2__.marginStyle)(margin),
    ...(0,_utils_styles__WEBPACK_IMPORTED_MODULE_2__.marginTopStyle)(marginTop),
    ...(0,_utils_styles__WEBPACK_IMPORTED_MODULE_2__.paddingStyle)(padding),
    ...(0,_utils_styles__WEBPACK_IMPORTED_MODULE_2__.paddingTopStyle)(paddingTop),
    ...(0,_utils_styles__WEBPACK_IMPORTED_MODULE_2__.gapStyle)(gap)
  };
  const classes = (0,clsx__WEBPACK_IMPORTED_MODULE_1__["default"])("flex", `justify-${align}`, `items-${blockAlign}`, direction === "row" ? "flex-row" : "flex-row-reverse", wrap ? "flex-wrap" : "flex-nowrap", className);
  return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsx)(Component, {
    ref: ref,
    id: id,
    title: title,
    style: styles,
    className: classes,
    onClick: onClick,
    onContextMenu: onContextMenu,
    onDoubleClick: onDoubleClick,
    ...rest,
    children: children
  });
});
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (InlineStack);

/***/ },

/***/ "./source/components/inlineStack/index.ts"
/*!************************************************!*\
  !*** ./source/components/inlineStack/index.ts ***!
  \************************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (/* reexport safe */ _InlineStack__WEBPACK_IMPORTED_MODULE_0__["default"])
/* harmony export */ });
/* harmony import */ var _InlineStack__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./InlineStack */ "./source/components/inlineStack/InlineStack.tsx");

/***/ },

/***/ "./source/components/input/Input.tsx"
/*!*******************************************!*\
  !*** ./source/components/input/Input.tsx ***!
  \*******************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _wordpress_element__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @wordpress/element */ "@wordpress/element");
/* harmony import */ var _wordpress_element__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(_wordpress_element__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var _wordpress_i18n__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! @wordpress/i18n */ "@wordpress/i18n");
/* harmony import */ var _wordpress_i18n__WEBPACK_IMPORTED_MODULE_1___default = /*#__PURE__*/__webpack_require__.n(_wordpress_i18n__WEBPACK_IMPORTED_MODULE_1__);
/* harmony import */ var _icon__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../icon */ "./source/components/icon/index.ts");
/* harmony import */ var clsx__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! clsx */ "./node_modules/clsx/dist/clsx.mjs");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! react/jsx-runtime */ "react/jsx-runtime");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_4___default = /*#__PURE__*/__webpack_require__.n(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_4__);

const sizeClasses = {
  extrasmall: "pn-input__group--xs",
  small: "pn-input__group--sm",
  medium: "pn-input__group--md",
  large: "pn-input__group--lg",
  extralarge: "pn-input__group--xl"
};
const Input = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.forwardRef)((props, ref) => {
  const {
    id,
    name,
    style,
    inputStyle,
    className = "",
    gap = 10,
    inputClassName = "",
    type = "text",
    title,
    placeholder = (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_1__.__)("Type...", "ninja-media"),
    value: controlledValue = "",
    visible = true,
    disabled = false,
    readOnly = false,
    required = false,
    spellCheck = false,
    debounce = false,
    debounceTime = 300,
    color = "light",
    size = "medium",
    borderStyle = "solid",
    rounded = "sm",
    fullWidth = true,
    customWidth,
    label,
    labelFontSize = "md",
    labelFontWeight = "medium",
    helperText,
    error = false,
    errorText,
    prefix,
    suffix,
    searchIcon = false,
    ariaLabel,
    tabIndex,
    preventDefault = false,
    stopPropagation = false,
    onClick,
    onChange,
    onBlur,
    onKeyDown,
    ...rest
  } = props;
  const [internalValue, setInternalValue] = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useState)(controlledValue);
  const debounceRef = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useRef)(null);
  (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useEffect)(() => {
    setInternalValue(controlledValue);
  }, [controlledValue]);
  const handleChange = e => {
    const newValue = e.target.value;
    setInternalValue(newValue);
    const parsedValue = type === "number" ? Number(newValue) || 0 : newValue;
    if (!debounce) {
      onChange?.(parsedValue);
      return;
    }
    if (debounceRef.current) {
      clearTimeout(debounceRef.current);
    }
    debounceRef.current = window.setTimeout(() => {
      onChange?.(parsedValue);
    }, debounceTime);
  };
  (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useEffect)(() => {
    return () => {
      if (debounceRef.current) {
        clearTimeout(debounceRef.current);
      }
    };
  }, []);
  const handleBlur = () => {
    onBlur?.(type === "number" ? Number(internalValue) || 0 : internalValue);
  };
  if (!visible) return null;
  const wrapperStyle = {
    ...style,
    ...(gap && {
      gap
    }),
    width: fullWidth ? "100%" : customWidth
  };
  const wrapperClass = (0,clsx__WEBPACK_IMPORTED_MODULE_3__["default"])("pn-input", disabled && "pn-input--disabled", className);
  const inputGroupClass = (0,clsx__WEBPACK_IMPORTED_MODULE_3__["default"])("pn-input__group", sizeClasses[size], `rounded-${rounded}`, "border", error ? "border-error" : `border-${color}`, `border-${borderStyle}`, searchIcon && "pn-input__group--search-icon", prefix && "pn-input__group--prefix", suffix && "pn-input__group--suffix");
  return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_4__.jsxs)("div", {
    style: wrapperStyle,
    className: wrapperClass,
    children: [label && /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_4__.jsxs)("label", {
      htmlFor: id,
      className: (0,clsx__WEBPACK_IMPORTED_MODULE_3__["default"])(`text-${labelFontSize}`, `font-${labelFontWeight}`, "text-black"),
      children: [label, required && /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_4__.jsx)("span", {
        className: "pn-input__required",
        children: "*"
      })]
    }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_4__.jsxs)("div", {
      className: inputGroupClass,
      children: [(prefix || searchIcon) && /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_4__.jsx)("span", {
        className: "pn-input__group-prefix-icon",
        children: searchIcon ? /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_4__.jsx)(_icon__WEBPACK_IMPORTED_MODULE_2__["default"], {
          name: "search",
          fontSize: "lg"
        }) : prefix
      }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_4__.jsx)("input", {
        id: id,
        name: name,
        ref: ref,
        type: type,
        placeholder: placeholder,
        title: title,
        value: internalValue,
        disabled: disabled,
        readOnly: readOnly,
        required: required,
        spellCheck: spellCheck,
        "aria-label": ariaLabel,
        "aria-invalid": error,
        "aria-describedby": error ? `${id}-error` : undefined,
        tabIndex: tabIndex,
        style: inputStyle,
        className: inputClassName,
        onClick: e => {
          if (preventDefault) e.preventDefault();
          if (stopPropagation) e.stopPropagation();
          onClick?.(e);
        },
        onChange: handleChange,
        onBlur: handleBlur,
        onKeyDown: onKeyDown,
        ...rest
      }), suffix && /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_4__.jsx)("span", {
        className: "pn-input__group-suffix-icon",
        children: suffix
      })]
    }), (helperText || error && errorText) && /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_4__.jsx)("span", {
      id: error ? `${id}-error` : undefined,
      className: (0,clsx__WEBPACK_IMPORTED_MODULE_3__["default"])("pn-input__helper-text", error && "pn-input-error"),
      children: error && errorText ? errorText : helperText
    })]
  });
});
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (Input);

/***/ },

/***/ "./source/components/input/index.ts"
/*!******************************************!*\
  !*** ./source/components/input/index.ts ***!
  \******************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (/* reexport safe */ _Input__WEBPACK_IMPORTED_MODULE_0__["default"])
/* harmony export */ });
/* harmony import */ var _Input__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./Input */ "./source/components/input/Input.tsx");

/***/ },

/***/ "./source/components/layout/Layout.tsx"
/*!*********************************************!*\
  !*** ./source/components/layout/Layout.tsx ***!
  \*********************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _wordpress_element__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @wordpress/element */ "@wordpress/element");
/* harmony import */ var _wordpress_element__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(_wordpress_element__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var clsx__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! clsx */ "./node_modules/clsx/dist/clsx.mjs");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! react/jsx-runtime */ "react/jsx-runtime");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_2___default = /*#__PURE__*/__webpack_require__.n(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_2__);

const Layout = ({
  style,
  className,
  children
}) => {
  return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_2__.jsx)("div", {
    style: style,
    className: (0,clsx__WEBPACK_IMPORTED_MODULE_1__["default"])("pnpnm", className),
    children: children
  });
};
Layout.Wrapper = ({
  style,
  className,
  children
}) => {
  return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_2__.jsx)("div", {
    style: style,
    className: (0,clsx__WEBPACK_IMPORTED_MODULE_1__["default"])("pnpnm-content-wrapper", className),
    children: children
  });
};
Layout.Content = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.forwardRef)(({
  style,
  className,
  children
}, ref) => {
  const classNames = (0,clsx__WEBPACK_IMPORTED_MODULE_1__["default"])("pnpnm-content", className);
  return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_2__.jsx)("div", {
    style: style,
    className: classNames,
    ref: ref,
    children: children
  });
});
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (Layout);

/***/ },

/***/ "./source/components/layout/index.ts"
/*!*******************************************!*\
  !*** ./source/components/layout/index.ts ***!
  \*******************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (/* reexport safe */ _Layout__WEBPACK_IMPORTED_MODULE_0__["default"])
/* harmony export */ });
/* harmony import */ var _Layout__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./Layout */ "./source/components/layout/Layout.tsx");

/***/ },

/***/ "./source/components/logo/Logo.tsx"
/*!*****************************************!*\
  !*** ./source/components/logo/Logo.tsx ***!
  \*****************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! react/jsx-runtime */ "react/jsx-runtime");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__);

const Logo = () => {
  return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)("svg", {
    width: "154",
    height: "32",
    viewBox: "0 0 154 32",
    fill: "none",
    xmlns: "http://www.w3.org/2000/svg",
    className: "pnpnm-logo",
    children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)("g", {
      "clip-path": "url(#clip0_555_1089)",
      children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("mask", {
        id: "mask0_555_1089",
        style: {
          maskType: "luminance"
        },
        maskUnits: "userSpaceOnUse",
        x: "0",
        y: "0",
        width: "37",
        height: "32",
        children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("path", {
          d: "M36.4971 0H0V32H36.4971V0Z",
          fill: "white"
        })
      }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)("g", {
        mask: "url(#mask0_555_1089)",
        children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("path", {
          d: "M36.4971 6.72856C36.4971 5.4157 35.435 4.35141 34.1248 4.35141H18.7121C17.9014 4.35141 17.1468 3.93654 16.7112 3.25134L15.3437 1.10007C14.9082 0.414871 14.1536 0 13.3429 0H2.37231C1.06212 0 0 1.06428 0 2.37715V29.6228C0 30.9356 1.06212 32 2.37231 32H34.1248C35.435 32 36.4971 30.9356 36.4971 29.6228V6.72856Z",
          fill: "var(--pnpnm-hover)"
        }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("path", {
          d: "M13.3428 0C14.1536 0 14.9082 0.41489 15.3437 1.10009L16.7112 3.25133C17.1468 3.93652 17.9014 4.35141 18.7121 4.35143H34.1248C35.435 4.35143 36.4971 5.41571 36.4971 6.72858V7.27715C36.4971 5.96428 35.435 4.90001 34.1248 4.90001H18.7121C17.9014 4.89998 17.1468 4.48509 16.7112 3.79991L15.3437 1.64866C14.9082 0.963467 14.1536 0.548572 13.3428 0.548572H2.37231C1.06212 0.548572 0 1.61285 0 2.92572V2.37715C0 1.06428 1.06212 0 2.37231 0H13.3428Z",
          fill: "white",
          "fill-opacity": "0.4"
        }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("path", {
          d: "M0 9.6936C0 8.38079 1.06212 7.31651 2.37231 7.31651H14.9901C15.7707 7.31651 16.5013 6.93174 16.944 6.28755L18.2958 4.32042C18.7385 3.67621 19.4691 3.29145 20.2497 3.29145H34.1248C35.435 3.29145 36.4971 4.35574 36.4971 5.6686V29.6228C36.4971 30.9357 35.435 32 34.1248 32H2.37231C1.06212 32 0 30.9357 0 29.6228V9.6936Z",
          fill: "var(--pnpnm-primary)"
        }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("path", {
          d: "M34.1248 3.29145C35.435 3.29145 36.4971 4.35574 36.4971 5.6686V6.21717C36.4971 4.9043 35.435 3.84002 34.1248 3.84002H20.2497C19.4691 3.84004 18.7384 4.22477 18.2958 4.86895L16.944 6.83609C16.5012 7.48029 15.7707 7.86511 14.9901 7.86511H2.37231C1.06212 7.86511 0 8.92942 0 10.2422V9.69369C0 8.38082 1.06212 7.31654 2.37231 7.31654H14.9901C15.7707 7.31654 16.5012 6.93173 16.944 6.28752L18.2958 4.32038C18.7384 3.67619 19.4691 3.29147 20.2497 3.29145H34.1248Z",
          fill: "white",
          "fill-opacity": "0.4"
        }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("mask", {
          id: "mask1_555_1089",
          style: {
            maskType: "luminance"
          },
          maskUnits: "userSpaceOnUse",
          x: "3",
          y: "13",
          width: "16",
          height: "16",
          children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("path", {
            d: "M18.2486 13.7143H3.6497V28.3428H18.2486V13.7143Z",
            fill: "white"
          })
        }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)("g", {
          mask: "url(#mask1_555_1089)",
          children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("mask", {
            id: "mask2_555_1089",
            style: {
              maskType: "luminance"
            },
            maskUnits: "userSpaceOnUse",
            x: "3",
            y: "13",
            width: "16",
            height: "16",
            children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("path", {
              d: "M18.2486 13.7143H3.6497V28.3428H18.2486V13.7143Z",
              fill: "white"
            })
          }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)("g", {
            mask: "url(#mask2_555_1089)",
            children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("path", {
              d: "M7.50419 13.9548L10.0061 15.0004C11.5269 15.0598 12.8232 15.36 13.7171 15.6981C13.8477 15.7475 13.9712 15.7983 14.0868 15.85C16.5189 16.7711 18.2486 19.126 18.2486 21.8857C18.2486 25.4483 15.3664 28.3362 11.8113 28.3362C8.25603 28.3362 5.37397 25.4483 5.37397 21.8857C5.37397 20.5277 5.79278 19.2677 6.50805 18.2284C6.33051 18.0077 6.17484 17.7588 6.04551 17.5244C5.83525 17.1432 5.67548 16.7649 5.59074 16.5268L5.48999 16.2436L5.74656 16.0877C5.8667 16.0148 6.01959 15.9623 6.16187 15.9228C6.31087 15.8812 6.47752 15.846 6.64192 15.8168C6.81527 15.786 6.9919 15.7606 7.15182 15.741L6.99701 14.3355L6.92858 13.7143L7.50419 13.9548Z",
              fill: "#120426"
            }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("mask", {
              id: "mask3_555_1089",
              style: {
                maskType: "luminance"
              },
              maskUnits: "userSpaceOnUse",
              x: "5",
              y: "13",
              width: "14",
              height: "16",
              children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("path", {
                d: "M7.50419 13.9548L10.0061 15.0004C11.5269 15.0598 12.8232 15.36 13.7171 15.6981C13.8477 15.7475 13.9712 15.7983 14.0868 15.85C16.5189 16.7711 18.2486 19.126 18.2486 21.8857C18.2486 25.4483 15.3664 28.3362 11.8113 28.3362C8.25603 28.3362 5.37397 25.4483 5.37397 21.8857C5.37397 20.5277 5.79278 19.2677 6.50805 18.2284C6.33051 18.0077 6.17484 17.7588 6.04551 17.5244C5.83525 17.1432 5.67548 16.7649 5.59074 16.5268L5.48999 16.2436L5.74656 16.0877C5.8667 16.0148 6.01959 15.9623 6.16187 15.9228C6.31087 15.8812 6.47752 15.846 6.64192 15.8168C6.81527 15.786 6.9919 15.7606 7.15182 15.741L6.99701 14.3355L6.92858 13.7143L7.50419 13.9548Z",
                fill: "white"
              })
            }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("g", {
              mask: "url(#mask3_555_1089)",
              children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("path", {
                d: "M10.0062 15.0004L9.86455 15.3405L9.92561 15.366L9.99175 15.3686L10.0062 15.0004ZM14.0868 15.85L13.9368 16.1866L13.9466 16.1909L13.9567 16.1948L14.0868 15.85ZM6.50807 18.2284L6.81091 18.4376L6.96674 18.2111L6.79447 17.9971L6.50807 18.2284ZM5.49002 16.2436L5.29934 15.9284L5.04279 16.0842L5.14352 16.3673L5.49002 16.2436ZM7.15185 15.7411L7.19678 16.1068L7.55733 16.0624L7.51746 15.7005L7.15185 15.7411ZM6.9286 13.7143L7.07016 13.3741L6.49453 13.1336L6.56298 13.7548L6.9286 13.7143ZM7.50422 13.9548L7.36265 14.2951L9.86455 15.3405L10.0062 15.0004L10.1477 14.6602L7.64583 13.6147L7.50422 13.9548ZM10.0062 15.0004L9.99175 15.3686C11.475 15.4267 12.7321 15.7196 13.5873 16.043L13.7171 15.6981L13.8471 15.3532C12.9145 15.0005 11.579 14.6931 10.0205 14.6321L10.0062 15.0004ZM13.7171 15.6981L13.5873 16.043C13.7116 16.09 13.8285 16.1381 13.9368 16.1866L14.0868 15.85L14.2367 15.5133C14.1141 15.4586 13.9838 15.4051 13.8471 15.3532L13.7171 15.6981ZM14.0868 15.85L13.9567 16.1948C16.2506 17.0636 17.8807 19.2844 17.8807 21.8858H18.2486H18.6165C18.6165 18.9678 16.7875 16.4788 14.2168 15.5052L14.0868 15.85ZM18.2486 21.8858H17.8807C17.8807 25.2446 15.1634 27.9676 11.8113 27.9676V28.3361V28.7048C15.5697 28.7048 18.6165 25.6518 18.6165 21.8858H18.2486ZM11.8113 28.3361V27.9676C8.4592 27.9676 5.74183 25.2446 5.74183 21.8858H5.37399H5.00614C5.00614 25.6518 8.05291 28.7048 11.8113 28.7048V28.3361ZM5.37399 21.8858H5.74183C5.74183 20.6048 6.13662 19.4174 6.81091 18.4376L6.50807 18.2284L6.20525 18.0191C5.44899 19.118 5.00614 20.4506 5.00614 21.8858H5.37399ZM6.50807 18.2284L6.79447 17.9971C6.63459 17.7983 6.49044 17.569 6.36747 17.346L6.04553 17.5244L5.72361 17.7028C5.85924 17.9485 6.02651 18.217 6.22169 18.4597L6.50807 18.2284ZM6.04553 17.5244L6.36747 17.346C6.16754 16.9836 6.01578 16.6236 5.93723 16.4029L5.59075 16.5267L5.24426 16.6505C5.33522 16.9061 5.503 17.3029 5.72361 17.7028L6.04553 17.5244ZM5.59075 16.5267L5.93723 16.4029L5.8365 16.1198L5.49002 16.2436L5.14352 16.3673L5.24426 16.6505L5.59075 16.5267ZM5.49002 16.2436L5.68069 16.5588L5.93723 16.4029L5.74659 16.0877L5.55593 15.7724L5.29934 15.9284L5.49002 16.2436ZM5.74659 16.0877L5.93723 16.4029C6.01318 16.3568 6.12672 16.315 6.26033 16.2779L6.16189 15.9227L6.06342 15.5676C5.91251 15.6095 5.72022 15.6727 5.55593 15.7724L5.74659 16.0877ZM6.16189 15.9227L6.26033 16.2779C6.3942 16.2406 6.54855 16.2078 6.70627 16.1798L6.64195 15.8168L6.57765 15.454C6.40655 15.4844 6.22757 15.5219 6.06342 15.5676L6.16189 15.9227ZM6.64195 15.8168L6.70627 16.1798C6.87176 16.1503 7.04176 16.126 7.19678 16.1068L7.15185 15.7411L7.10696 15.3751C6.9421 15.3955 6.75881 15.4217 6.57765 15.454L6.64195 15.8168ZM7.15185 15.7411L7.51746 15.7005L7.36265 14.2951L6.99703 14.3355L6.6314 14.3759L6.78623 15.7814L7.15185 15.7411ZM6.99703 14.3355L7.36265 14.2951L7.29423 13.6739L6.9286 13.7143L6.56298 13.7548L6.6314 14.3759L6.99703 14.3355ZM6.9286 13.7143L6.78701 14.0545L7.36265 14.2951L7.50422 13.9548L7.64583 13.6147L7.07016 13.3741L6.9286 13.7143Z",
                fill: "white"
              })
            }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("path", {
              d: "M14.3402 21.2176L6.6499 18.5453C6.24527 18.8126 5.97169 19.9045 5.90273 20.4228C8.68915 21.8051 12.7002 22.6228 14.3402 22.8532L18.0416 21.9087L17.8462 20.3077L14.3402 21.2176Z",
              fill: "white"
            }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("path", {
              d: "M8.82377 22.9225C8.67662 22.6092 8.65517 22.1468 8.66283 21.9548C10.125 22.5999 13.0961 23.0761 14.3989 23.2334L17.2037 22.4732C17.2497 23.3579 16.583 24.4198 15.9393 24.7885C14.1121 25.8347 11.8508 25.1763 11.0884 24.8691C9.8698 24.3783 9.00767 23.314 8.82377 22.9225Z",
              fill: "white"
            }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("path", {
              d: "M15.9427 23.3627C16.0075 23.3485 16.0695 23.3959 16.073 23.4624C16.0758 23.5154 16.0396 23.5626 15.9876 23.5735L15.8364 23.6054V23.9922C15.8364 24.2404 15.6357 24.4415 15.3881 24.4415C15.1404 24.4415 14.9398 24.2404 14.9398 23.9922V23.7947L14.8794 23.8073C14.8193 23.82 14.7615 23.778 14.7547 23.717C14.7488 23.664 14.7839 23.615 14.8359 23.6037L15.9427 23.3627Z",
              fill: "#090015"
            }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("path", {
              d: "M15.342 24.1189C15.4182 24.1189 15.4799 24.0571 15.4799 23.9807C15.4799 23.9044 15.4182 23.8426 15.342 23.8426C15.2658 23.8426 15.204 23.9044 15.204 23.9807C15.204 24.0571 15.2658 24.1189 15.342 24.1189Z",
              fill: "white"
            }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("path", {
              d: "M11.1602 23.3627C11.0953 23.3485 11.0333 23.3959 11.0298 23.4623C11.027 23.5154 11.0633 23.5626 11.1151 23.5735L11.2663 23.6053V23.9922C11.2663 24.2403 11.467 24.4414 11.7147 24.4414C11.9622 24.4414 12.163 24.2403 12.163 23.9922V23.7945L12.2233 23.8073C12.2834 23.8199 12.3412 23.778 12.348 23.7169C12.3539 23.6639 12.3188 23.615 12.2668 23.6037L11.1602 23.3627Z",
              fill: "#090015"
            }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("path", {
              d: "M11.7539 24.2356C11.849 24.2356 11.9263 24.1582 11.9263 24.0628C11.9263 23.9673 11.849 23.89 11.7539 23.89C11.6586 23.89 11.5814 23.9673 11.5814 24.0628C11.5814 24.1582 11.6586 24.2356 11.7539 24.2356Z",
              fill: "white"
            }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("path", {
              d: "M17.7888 23.0492C16.4369 25.915 12.8305 27.6757 11.6043 27.9676C11.6043 27.9676 12.5344 27.9516 12.8573 27.8754C14.2252 27.5528 14.2023 27.5202 15.0414 27.0345C15.2648 26.9053 15.3993 26.8004 15.599 26.6448L15.6161 26.6314C16.5872 25.8748 17.2945 24.6387 17.4899 24.0282C17.5922 23.7084 17.7888 23.0492 17.7888 23.0492Z",
              fill: "#301A51"
            }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("path", {
              d: "M5.59841 19.2321L5.49808 19.6121L4.46932 20.4941L3.6497 19.7956L4.62408 18.8139L5.59841 19.2321ZM6.05977 18.2837L5.68712 19.045L3.6497 18.1719L4.10553 16.2484L6.05977 18.2837Z",
              fill: "white"
            })]
          })]
        })]
      }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("path", {
        d: "M103.129 8.00001H101.54V6.40001H99.9522V16H96.7743V0H99.9522V1.60001H101.54V3.2H103.129V4.8H104.718V3.2H106.306V1.60001H107.895V0H111.072V16H107.895V6.40001H106.306V8.00001H104.718V9.6H103.129V8.00001ZM112.674 4.8H114.263V3.2H122.206V4.8H123.795V11.2H115.851V12.8H123.795V14.4001H122.206V16H114.263V14.4001H112.674V4.8ZM115.851 8.00001H120.617V6.40001H115.851V8.00001ZM133.331 3.2V0H136.508V16H133.331V14.4001H131.742V16H126.976V14.4001H125.387V4.8H126.976V3.2H133.331ZM133.331 12.8V6.40001H128.565V12.8H133.331ZM138.101 0H141.279V3.2H138.101V0ZM138.101 4.8H141.279V16H138.101V4.8ZM144.468 14.4001H142.879V9.6H144.468V8.00001H150.823V6.40001H142.879V4.8H144.468V3.2H152.411V4.8H154V16H150.823V14.4001H149.234V16H144.468V14.4001ZM146.057 12.8H150.823V11.2H146.057V12.8Z",
        fill: "var(--pnpnm-primary)"
      }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("path", {
        d: "M46.5333 3.19965H48.0989V6.40018H49.6637V9.59982H51.2284V0H54.3588V16H49.6637V14.4002H48.0989V11.1996H46.5333V8H44.9685V16H41.8382V0H46.5333V3.19965ZM59.0627 16H55.9323V4.80035H59.0627V16ZM63.77 4.80035H65.3347V3.19965H70.0307V4.80035H71.5955V16H68.4651V6.40018H63.77V16H60.6396V3.19965H63.77V4.80035ZM77.8902 14.6667H76.3645V16H73.3124V13.3333H74.838V4H77.8902V14.6667ZM89.417 4H91.0523V16H87.782V14.2856H86.1469V16H81.2422V14.2856H79.6071V9.1432H81.2422V7.42882H87.782V5.71441H79.6071V4H81.2422V2.28559H89.417V4ZM82.8939 12.6164H87.7986V10.9019H82.8939V12.6164ZM59.0627 3.19965H55.9323V0H59.0627V3.19965ZM77.8902 2.66667H74.838V0H77.8902V2.66667Z",
        fill: "#010019"
      }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("path", {
        d: "M45.3079 28.2156H43.1129L42.7357 29.2808H41.8382L43.7163 24.06H44.712L46.5901 29.2808H45.6849L45.3079 28.2156ZM45.0665 27.518L44.2141 25.0952L43.3542 27.518H45.0665ZM53.0318 25.6203C53.0318 25.8853 52.969 26.1354 52.8433 26.3704C52.7176 26.6054 52.5164 26.798 52.2398 26.948C51.9633 27.093 51.6088 27.1655 51.1763 27.1655H50.2259V29.2808H49.3661V24.0675H51.1763C51.5786 24.0675 51.918 24.1375 52.1946 24.2775C52.4762 24.4125 52.6849 24.5976 52.8207 24.8326C52.9615 25.0676 53.0318 25.3302 53.0318 25.6203ZM51.1763 26.4679C51.5032 26.4679 51.747 26.3954 51.908 26.2503C52.0689 26.1004 52.1494 25.8903 52.1494 25.6203C52.1494 25.0501 51.825 24.7652 51.1763 24.7652H50.2259V26.4679H51.1763ZM56.5749 29.2808L55.3681 27.1955H54.7118V29.2808H53.8519V24.0675H55.6622C56.0645 24.0675 56.4039 24.1375 56.6805 24.2775C56.9621 24.4176 57.1708 24.6051 57.3065 24.8401C57.4473 25.0752 57.5178 25.3377 57.5178 25.6277C57.5178 25.9678 57.4172 26.2779 57.216 26.5579C57.0199 26.833 56.7157 27.0204 56.3034 27.1204L57.6007 29.2808H56.5749ZM54.7118 26.5129H55.6622C55.984 26.5129 56.2254 26.4329 56.3863 26.2729C56.5523 26.1128 56.6352 25.8978 56.6352 25.6277C56.6352 25.3577 56.5548 25.1476 56.3938 24.9977C56.233 24.8427 55.9891 24.7652 55.6622 24.7652H54.7118V26.5129ZM61.0088 29.3333C60.5211 29.3333 60.071 29.2208 59.6587 28.9958C59.2514 28.7658 58.927 28.4482 58.6857 28.0431C58.4493 27.6331 58.3311 27.173 58.3311 26.6629C58.3311 26.1528 58.4493 25.6953 58.6857 25.2902C58.927 24.8852 59.2514 24.5701 59.6587 24.3451C60.071 24.115 60.5211 24 61.0088 24C61.5016 24 61.9517 24.115 62.359 24.3451C62.7713 24.5701 63.0957 24.8852 63.332 25.2902C63.5683 25.6953 63.6865 26.1528 63.6865 26.6629C63.6865 27.173 63.5683 27.6331 63.332 28.0431C63.0957 28.4482 62.7713 28.7658 62.359 28.9958C61.9517 29.2208 61.5016 29.3333 61.0088 29.3333ZM61.0088 28.5908C61.3558 28.5908 61.6651 28.5132 61.9366 28.3582C62.2082 28.1981 62.4193 27.9732 62.5702 27.6831C62.7261 27.388 62.804 27.048 62.804 26.6629C62.804 26.2779 62.7261 25.9403 62.5702 25.6502C62.4193 25.3602 62.2082 25.1377 61.9366 24.9827C61.6651 24.8276 61.3558 24.7501 61.0088 24.7501C60.6618 24.7501 60.3526 24.8276 60.0811 24.9827C59.8095 25.1377 59.5959 25.3602 59.4399 25.6502C59.2891 25.9403 59.2136 26.2779 59.2136 26.6629C59.2136 27.048 59.2891 27.388 59.4399 27.6831C59.5959 27.9732 59.8095 28.1981 60.0811 28.3582C60.3526 28.5132 60.6618 28.5908 61.0088 28.5908ZM66.2448 24.0675C66.803 24.0675 67.2908 24.175 67.7081 24.39C68.1305 24.6001 68.4549 24.9052 68.6812 25.3052C68.9125 25.7003 69.0281 26.1628 69.0281 26.6929C69.0281 27.223 68.9125 27.6831 68.6812 28.0732C68.4549 28.4632 68.1305 28.7632 67.7081 28.9732C67.2908 29.1783 66.803 29.2808 66.2448 29.2808H64.5326V24.0675H66.2448ZM66.2448 28.5832C66.8583 28.5832 67.3284 28.4182 67.6553 28.0882C67.9822 27.758 68.1456 27.293 68.1456 26.6929C68.1456 26.0878 67.9822 25.6153 67.6553 25.2752C67.3284 24.9351 66.8583 24.7652 66.2448 24.7652H65.3925V28.5832H66.2448ZM70.7326 24.0675V27.3905C70.7326 27.7856 70.8357 28.0831 71.0419 28.2832C71.2531 28.4832 71.5448 28.5832 71.9168 28.5832C72.294 28.5832 72.5856 28.4832 72.7918 28.2832C73.003 28.0831 73.1086 27.7856 73.1086 27.3905V24.0675H73.9685V27.3756C73.9685 27.8006 73.8755 28.1606 73.6894 28.4557C73.5034 28.7508 73.2544 28.9708 72.9427 29.1158C72.6309 29.2608 72.2864 29.3333 71.9094 29.3333C71.5322 29.3333 71.1877 29.2608 70.876 29.1158C70.5692 28.9708 70.3254 28.7508 70.1443 28.4557C69.9633 28.1606 69.8728 27.8006 69.8728 27.3756V24.0675H70.7326ZM74.8014 26.6629C74.8014 26.1528 74.9197 25.6953 75.156 25.2902C75.3973 24.8852 75.7217 24.5701 76.129 24.3451C76.5414 24.115 76.9914 24 77.4792 24C78.0373 24 78.5326 24.1375 78.9651 24.4125C79.4026 24.6826 79.7194 25.0676 79.9155 25.5677H78.8821C78.7463 25.2927 78.5578 25.0876 78.3164 24.9526C78.0751 24.8176 77.796 24.7501 77.4792 24.7501C77.1322 24.7501 76.8229 24.8276 76.5514 24.9827C76.2799 25.1377 76.0661 25.3602 75.9103 25.6502C75.7595 25.9403 75.684 26.2779 75.684 26.6629C75.684 27.048 75.7595 27.3855 75.9103 27.6756C76.0661 27.9656 76.2799 28.1907 76.5514 28.3507C76.8229 28.5057 77.1322 28.5832 77.4792 28.5832C77.796 28.5832 78.0751 28.5157 78.3164 28.3807C78.5578 28.2457 78.7463 28.0406 78.8821 27.7656H79.9155C79.7194 28.2657 79.4026 28.6508 78.9651 28.9208C78.5326 29.1908 78.0373 29.3259 77.4792 29.3259C76.9864 29.3259 76.5363 29.2133 76.129 28.9883C75.7217 28.7582 75.3973 28.4407 75.156 28.0356C74.9197 27.6306 74.8014 27.173 74.8014 26.6629ZM84.3087 24.0675V24.7652H82.9132V29.2808H82.0533V24.7652H80.6504V24.0675H84.3087ZM89.5301 29.3333C89.0423 29.3333 88.5921 29.2208 88.1798 28.9958C87.7724 28.7658 87.4481 28.4482 87.2067 28.0431C86.9704 27.6331 86.8523 27.173 86.8523 26.6629C86.8523 26.1528 86.9704 25.6953 87.2067 25.2902C87.4481 24.8852 87.7724 24.5701 88.1798 24.3451C88.5921 24.115 89.0423 24 89.5301 24C90.0223 24 90.4728 24.115 90.8805 24.3451C91.2926 24.5701 91.6166 24.8852 91.8534 25.2902C92.0893 25.6953 92.2077 26.1528 92.2077 26.6629C92.2077 27.173 92.0893 27.6331 91.8534 28.0431C91.6166 28.4482 91.2926 28.7658 90.8805 28.9958C90.4728 29.2208 90.0223 29.3333 89.5301 29.3333ZM89.5301 28.5908C89.8772 28.5908 90.1861 28.5132 90.4576 28.3582C90.7291 28.1981 90.9401 27.9732 91.0914 27.6831C91.2472 27.388 91.3256 27.048 91.3256 26.6629C91.3256 26.2779 91.2472 25.9403 91.0914 25.6502C90.9401 25.3602 90.7291 25.1377 90.4576 24.9827C90.1861 24.8276 89.8772 24.7501 89.5301 24.7501C89.1829 24.7501 88.8738 24.8276 88.6022 24.9827C88.3307 25.1377 88.1169 25.3602 87.9611 25.6502C87.8102 25.9403 87.7348 26.2779 87.7348 26.6629C87.7348 27.048 87.8102 27.388 87.9611 27.6831C88.1169 27.9732 88.3307 28.1981 88.6022 28.3582C88.8738 28.5132 89.1829 28.5908 89.5301 28.5908ZM96.1387 24.0675V24.7652H93.9133V26.3028H95.6482V27.0004H93.9133V29.2808H93.0534V24.0675H96.1387Z",
        fill: "#323152",
        "fill-opacity": "0.54"
      }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("mask", {
        id: "mask4_555_1089",
        style: {
          maskType: "luminance"
        },
        maskUnits: "userSpaceOnUse",
        x: "100",
        y: "21",
        width: "44",
        height: "11",
        children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("path", {
          d: "M143.318 21.3333H100.59V32H143.318V21.3333Z",
          fill: "white"
        })
      }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)("g", {
        mask: "url(#mask4_555_1089)",
        children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("mask", {
          id: "mask5_555_1089",
          style: {
            maskType: "luminance"
          },
          maskUnits: "userSpaceOnUse",
          x: "100",
          y: "21",
          width: "12",
          height: "11",
          children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("path", {
            d: "M111.37 21.3333H100.59V32H111.37V21.3333Z",
            fill: "white"
          })
        }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)("g", {
          mask: "url(#mask5_555_1089)",
          children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("mask", {
            id: "mask6_555_1089",
            style: {
              maskType: "luminance"
            },
            maskUnits: "userSpaceOnUse",
            x: "100",
            y: "21",
            width: "12",
            height: "11",
            children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("path", {
              d: "M111.37 21.3333H100.59V32H111.37V21.3333Z",
              fill: "white"
            })
          }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)("g", {
            mask: "url(#mask6_555_1089)",
            children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("path", {
              d: "M103.435 21.5087L105.283 22.2711C106.406 22.3145 107.363 22.5333 108.023 22.7799C108.12 22.8159 108.211 22.8529 108.296 22.8906C110.092 23.5623 111.37 25.2794 111.37 27.2917C111.37 29.8893 109.241 31.9951 106.616 31.9951C103.991 31.9951 101.863 29.8893 101.863 27.2917C101.863 26.3015 102.172 25.3828 102.7 24.6248C102.569 24.4639 102.455 24.2824 102.358 24.1115C102.203 23.8336 102.085 23.5578 102.023 23.3841L101.948 23.1776L102.138 23.0639C102.227 23.0108 102.34 22.9725 102.445 22.9436C102.554 22.9134 102.678 22.8877 102.799 22.8664C102.927 22.8439 103.057 22.8255 103.176 22.8111L103.062 21.7863L103.011 21.3333L103.435 21.5087Z",
              fill: "#120426"
            }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("mask", {
              id: "mask7_555_1089",
              style: {
                maskType: "luminance"
              },
              maskUnits: "userSpaceOnUse",
              x: "101",
              y: "21",
              width: "11",
              height: "11",
              children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("path", {
                d: "M103.435 21.5087L105.283 22.2711C106.406 22.3145 107.363 22.5333 108.023 22.7799C108.12 22.8159 108.211 22.8529 108.296 22.8906C110.092 23.5623 111.37 25.2794 111.37 27.2917C111.37 29.8893 109.241 31.9951 106.616 31.9951C103.991 31.9951 101.863 29.8893 101.863 27.2917C101.863 26.3015 102.172 25.3828 102.7 24.6248C102.569 24.4639 102.455 24.2824 102.358 24.1115C102.203 23.8336 102.085 23.5578 102.023 23.3841L101.948 23.1776L102.138 23.0639C102.227 23.0108 102.34 22.9725 102.445 22.9436C102.554 22.9134 102.678 22.8877 102.799 22.8664C102.927 22.8439 103.057 22.8255 103.176 22.8111L103.062 21.7863L103.011 21.3333L103.435 21.5087Z",
                fill: "white"
              })
            }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("g", {
              mask: "url(#mask7_555_1089)",
              children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("path", {
                d: "M105.283 22.271L105.178 22.5191L105.224 22.5377L105.273 22.5396L105.283 22.271ZM108.296 22.8906L108.185 23.136L108.193 23.1392L108.201 23.142L108.296 22.8906ZM102.7 24.6248L102.924 24.7773L103.038 24.6123L102.912 24.4561L102.7 24.6248ZM101.948 23.1775L101.807 22.9476L101.619 23.0613L101.693 23.2678L101.948 23.1775ZM103.176 22.8111L103.208 23.0779L103.475 23.0454L103.445 22.7816L103.176 22.8111ZM103.011 21.3332L103.115 21.0852L102.69 20.9099L102.741 21.3628L103.011 21.3332ZM103.435 21.5087L103.331 21.7567L105.178 22.5191L105.283 22.271L105.388 22.0229L103.541 21.2606L103.435 21.5087ZM105.283 22.271L105.273 22.5396C106.368 22.582 107.296 22.7954 107.927 23.0313L108.023 22.7798L108.12 22.5284C107.431 22.2712 106.444 22.0469 105.293 22.0025L105.283 22.271ZM108.023 22.7798L107.927 23.0313C108.019 23.0656 108.105 23.1006 108.185 23.136L108.296 22.8906L108.407 22.6452C108.316 22.6052 108.22 22.5661 108.12 22.5284L108.023 22.7798ZM108.296 22.8906L108.201 23.142C109.894 23.7754 111.097 25.3948 111.097 27.2916H111.37H111.641C111.641 25.1639 110.291 23.3491 108.392 22.6391L108.296 22.8906ZM111.37 27.2916H111.097C111.097 29.7408 109.091 31.7263 106.616 31.7263V31.995V32.2638C109.391 32.2638 111.641 30.0377 111.641 27.2916H111.37ZM106.616 31.995V31.7263C104.141 31.7263 102.134 29.7408 102.134 27.2916H101.863H101.591C101.591 30.0377 103.841 32.2638 106.616 32.2638V31.995ZM101.863 27.2916H102.134C102.134 26.3576 102.426 25.4918 102.924 24.7773L102.7 24.6248L102.477 24.4722C101.918 25.2735 101.591 26.2452 101.591 27.2916H101.863ZM102.7 24.6248L102.912 24.4561C102.794 24.3112 102.687 24.1439 102.596 23.9814L102.358 24.1115L102.121 24.2415C102.221 24.4207 102.345 24.6165 102.488 24.7935L102.7 24.6248ZM102.358 24.1115L102.596 23.9814C102.448 23.7172 102.337 23.4548 102.278 23.2938L102.023 23.384L101.767 23.4742C101.834 23.6606 101.958 23.95 102.121 24.2415L102.358 24.1115ZM102.023 23.384L102.278 23.2938L102.204 23.0873L101.948 23.1775L101.693 23.2678L101.767 23.4742L102.023 23.384ZM101.948 23.1775L102.09 23.4074L102.278 23.2938L102.138 23.0639L101.997 22.834L101.807 22.9476L101.948 23.1775ZM102.138 23.0639L102.278 23.2938C102.334 23.2601 102.419 23.2297 102.517 23.2026L102.445 22.9436L102.372 22.6846C102.26 22.7153 102.118 22.7613 101.997 22.834L102.138 23.0639ZM102.445 22.9436L102.517 23.2026C102.616 23.1754 102.73 23.1515 102.846 23.131L102.799 22.8664L102.752 22.6018C102.625 22.624 102.493 22.6513 102.372 22.6846L102.445 22.9436ZM102.799 22.8664L102.846 23.131C102.969 23.1095 103.095 23.0917 103.208 23.0779L103.176 22.8111L103.143 22.5444C103.021 22.5591 102.885 22.5782 102.752 22.6018L102.799 22.8664ZM103.176 22.8111L103.445 22.7816L103.331 21.7567L103.062 21.7862L102.791 21.8157L102.906 22.8405L103.176 22.8111ZM103.062 21.7862L103.331 21.7567L103.281 21.3038L103.011 21.3332L102.741 21.3628L102.791 21.8157L103.062 21.7862ZM103.011 21.3332L102.906 21.5813L103.331 21.7567L103.435 21.5087L103.541 21.2606L103.115 21.0852L103.011 21.3332Z",
                fill: "#A970FF"
              })
            }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("path", {
              d: "M108.484 26.8045L102.805 24.856C102.506 25.0508 102.304 25.8471 102.253 26.2251C104.311 27.2329 107.272 27.8292 108.484 27.9972L111.216 27.3085L111.072 26.1411L108.484 26.8045Z",
              fill: "#A970FF"
            }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("path", {
              d: "M104.41 28.0476C104.302 27.8192 104.286 27.4821 104.291 27.3421C105.371 27.8124 107.565 28.1596 108.526 28.2744L110.598 27.7201C110.632 28.3651 110.139 29.1395 109.664 29.4083C108.315 30.1711 106.645 29.691 106.082 29.467C105.183 29.1092 104.546 28.3332 104.41 28.0476Z",
              fill: "white"
            }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("path", {
              d: "M109.667 28.3685C109.715 28.3583 109.76 28.3929 109.763 28.4412C109.765 28.48 109.738 28.5144 109.7 28.5224L109.588 28.5457V28.8276C109.588 29.0085 109.44 29.1552 109.257 29.1552C109.074 29.1552 108.926 29.0085 108.926 28.8276V28.6836L108.882 28.6929C108.837 28.702 108.794 28.6715 108.789 28.6269C108.785 28.5883 108.811 28.5526 108.85 28.5444L109.667 28.3685Z",
              fill: "#090015"
            }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("path", {
              d: "M109.223 28.9201C109.279 28.9201 109.325 28.8749 109.325 28.8193C109.325 28.7636 109.279 28.7185 109.223 28.7185C109.167 28.7185 109.121 28.7636 109.121 28.8193C109.121 28.8749 109.167 28.9201 109.223 28.9201Z",
              fill: "white"
            }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("path", {
              d: "M106.135 28.3685C106.087 28.3583 106.042 28.3929 106.039 28.4412C106.037 28.48 106.063 28.5144 106.102 28.5223L106.214 28.5456V28.8276C106.214 29.0085 106.361 29.1552 106.545 29.1552C106.727 29.1552 106.876 29.0085 106.876 28.8276V28.6836L106.921 28.6928C106.964 28.702 107.007 28.6715 107.012 28.6269C107.017 28.5883 106.991 28.5526 106.953 28.5444L106.135 28.3685Z",
              fill: "#090015"
            }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("path", {
              d: "M106.573 29.0052C106.644 29.0052 106.701 28.9487 106.701 28.8791C106.701 28.8095 106.644 28.7532 106.573 28.7532C106.503 28.7532 106.446 28.8095 106.446 28.8791C106.446 28.9487 106.503 29.0052 106.573 29.0052Z",
              fill: "white"
            }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("path", {
              d: "M111.03 28.14C110.032 30.2296 107.368 31.5135 106.463 31.7263C106.463 31.7263 107.15 31.7147 107.389 31.6591C108.398 31.424 108.381 31.4001 109.001 31.046C109.166 30.9517 109.265 30.8753 109.413 30.7618L109.425 30.7521C110.143 30.2004 110.665 29.299 110.809 28.8539C110.884 28.6207 111.03 28.14 111.03 28.14Z",
              fill: "#301A51"
            }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("path", {
              d: "M102.028 25.3567L101.954 25.6339L101.195 26.277L100.59 25.7676L101.309 25.0518L102.028 25.3567ZM102.369 24.6652L102.094 25.2204L100.59 24.5837L100.926 23.1811L102.369 24.6652Z",
              fill: "#A970FF"
            })]
          })]
        }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("path", {
          d: "M114.109 27.8922H115.618V25.9006H114.109V27.8922ZM114.109 25.4028H114.612V24.9049H116.12V25.4028H116.623V28.3901H116.12V28.888H114.109V29.8838H113.103V24.9049H114.109V25.4028ZM117.128 23.9091H118.134V28.888H117.128V23.9091ZM120.652 28.888H119.143V28.3901H118.641V24.9049H119.646V27.8922H121.155V24.9049H122.161V28.888H121.155V28.3901H120.652V28.888ZM122.665 28.888H125.18V27.8922H124.677V28.3901H123.168V27.8922H122.665V25.4028H123.168V24.9049H126.186V29.3859H125.683V29.8838H123.168V29.3859H122.665V28.888ZM123.671 27.3943H125.18V25.9006H123.671V27.3943ZM126.69 23.9091H127.696V24.9049H126.69V23.9091ZM126.69 25.4028H127.696V28.888H126.69V25.4028ZM129.209 25.4028H129.712V24.9049H131.22V25.4028H131.723V28.888H130.718V25.9006H129.209V28.888H128.203V24.9049H129.209V25.4028ZM132.227 23.9091H133.233V24.9049H132.227V23.9091ZM132.227 25.4028H133.233V28.888H132.227V25.4028ZM134.746 25.4028H135.249V24.9049H136.758V25.4028H137.26V28.888H136.254V25.9006H134.746V28.888H133.741V24.9049H134.746V25.4028ZM138.268 23.9091H139.274V24.9049H138.268V23.9091ZM137.765 28.888H138.268V25.4028H139.274V29.3859H138.771V29.8838H137.765V28.888ZM140.275 28.3901H139.772V26.8964H140.275V26.3986H142.287V25.9006H139.772V25.4028H140.275V24.9049H142.79V25.4028H143.293V28.888H142.287V28.3901H141.784V28.888H140.275V28.3901ZM140.778 27.8922H142.287V27.3943H140.778V27.8922Z",
          fill: "#090015"
        }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("path", {
          d: "M114.109 27.4327H115.618V25.4411H114.109V27.4327ZM114.109 24.9432H114.612V24.4453H116.12V24.9432H116.623V27.9306H116.12V28.4284H114.109V29.4243H113.103V24.4453H114.109V24.9432ZM117.128 23.4495H118.134V28.4284H117.128V23.4495ZM120.652 28.4284H119.143V27.9306H118.641V24.4453H119.646V27.4327H121.155V24.4453H122.161V28.4284H121.155V27.9306H120.652V28.4284ZM122.665 28.4284H125.18V27.4327H124.677V27.9306H123.168V27.4327H122.665V24.9432H123.168V24.4453H126.186V28.9264H125.683V29.4243H123.168V28.9264H122.665V28.4284ZM123.671 26.9348H125.18V25.4411H123.671V26.9348ZM126.69 23.4495H127.696V24.4453H126.69V23.4495ZM126.69 24.9432H127.696V28.4284H126.69V24.9432ZM129.209 24.9432H129.712V24.4453H131.22V24.9432H131.723V28.4284H130.718V25.4411H129.209V28.4284H128.203V24.4453H129.209V24.9432ZM132.227 23.4495H133.233V24.4453H132.227V23.4495ZM132.227 24.9432H133.233V28.4284H132.227V24.9432ZM134.746 24.9432H135.249V24.4453H136.758V24.9432H137.26V28.4284H136.254V25.4411H134.746V28.4284H133.741V24.4453H134.746V24.9432ZM138.268 23.4495H139.274V24.4453H138.268V23.4495ZM137.765 28.4284H138.268V24.9432H139.274V28.9264H138.771V29.4243H137.765V28.4284ZM140.275 27.9306H139.772V26.4369H140.275V25.939H142.287V25.4411H139.772V24.9432H140.275V24.4453H142.79V24.9432H143.293V28.4284H142.287V27.9306H141.784V28.4284H140.275V27.9306ZM140.778 27.4327H142.287V26.9348H140.778V27.4327Z",
          fill: "#A970FF"
        })]
      })]
    }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("defs", {
      children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("clipPath", {
        id: "clip0_555_1089",
        children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("rect", {
          width: "154",
          height: "32",
          fill: "white"
        })
      })
    })]
  });
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (Logo);

/***/ },

/***/ "./source/components/logo/index.ts"
/*!*****************************************!*\
  !*** ./source/components/logo/index.ts ***!
  \*****************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (/* reexport safe */ _Logo__WEBPACK_IMPORTED_MODULE_0__["default"])
/* harmony export */ });
/* harmony import */ var _Logo__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./Logo */ "./source/components/logo/Logo.tsx");

/***/ },

/***/ "./source/components/pageContainer/PageContainer.tsx"
/*!***********************************************************!*\
  !*** ./source/components/pageContainer/PageContainer.tsx ***!
  \***********************************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _description__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../description */ "./source/components/description/index.ts");
/* harmony import */ var _inlineStack__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../inlineStack */ "./source/components/inlineStack/index.ts");
/* harmony import */ var _blockStack__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../blockStack */ "./source/components/blockStack/index.ts");
/* harmony import */ var _wordpress_i18n__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! @wordpress/i18n */ "@wordpress/i18n");
/* harmony import */ var _wordpress_i18n__WEBPACK_IMPORTED_MODULE_3___default = /*#__PURE__*/__webpack_require__.n(_wordpress_i18n__WEBPACK_IMPORTED_MODULE_3__);
/* harmony import */ var _button__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ../button */ "./source/components/button/index.ts");
/* harmony import */ var _text__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ../text */ "./source/components/text/index.ts");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! react/jsx-runtime */ "react/jsx-runtime");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_6___default = /*#__PURE__*/__webpack_require__.n(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_6__);

const PageContainer = ({
  id,
  style,
  className,
  gap = 20,
  title,
  description,
  docLink,
  module,
  children
}) => {
  return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_6__.jsxs)(_blockStack__WEBPACK_IMPORTED_MODULE_2__["default"], {
    id: id,
    style: {
      ...style,
      ...(module ? {
        marginBottom: "80px"
      } : {})
    },
    gap: gap,
    className: className,
    children: [(title || description) && /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_6__.jsxs)(_inlineStack__WEBPACK_IMPORTED_MODULE_1__["default"], {
      gap: 5,
      align: "between",
      children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_6__.jsxs)(_blockStack__WEBPACK_IMPORTED_MODULE_2__["default"], {
        gap: 10,
        children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_6__.jsx)(_text__WEBPACK_IMPORTED_MODULE_5__["default"], {
          as: "h2",
          weight: "semibold",
          size: "lg",
          children: title
        }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_6__.jsx)(_description__WEBPACK_IMPORTED_MODULE_0__["default"], {
          text: description
        })]
      }), docLink && /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_6__.jsx)(_button__WEBPACK_IMPORTED_MODULE_4__["default"], {
        variant: "outlined",
        size: "small",
        startIcon: "info",
        href: docLink,
        target: "_blank",
        children: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_3__.__)("Documentation", "ninja-media")
      })]
    }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_6__.jsx)(_blockStack__WEBPACK_IMPORTED_MODULE_2__["default"], {
      gap: 20,
      children: children
    })]
  });
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (PageContainer);

/***/ },

/***/ "./source/components/pageContainer/index.ts"
/*!**************************************************!*\
  !*** ./source/components/pageContainer/index.ts ***!
  \**************************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (/* reexport safe */ _PageContainer__WEBPACK_IMPORTED_MODULE_0__["default"])
/* harmony export */ });
/* harmony import */ var _PageContainer__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./PageContainer */ "./source/components/pageContainer/PageContainer.tsx");

/***/ },

/***/ "./source/components/selectBox/SelectBox.tsx"
/*!***************************************************!*\
  !*** ./source/components/selectBox/SelectBox.tsx ***!
  \***************************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   SelectBox: () => (/* binding */ SelectBox),
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _wordpress_element__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @wordpress/element */ "@wordpress/element");
/* harmony import */ var _wordpress_element__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(_wordpress_element__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var _icon__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../icon */ "./source/components/icon/index.ts");
/* harmony import */ var clsx__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! clsx */ "./node_modules/clsx/dist/clsx.mjs");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! react/jsx-runtime */ "react/jsx-runtime");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3___default = /*#__PURE__*/__webpack_require__.n(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__);

const SelectBox = ({
  id,
  style,
  className = "",
  gap = 10,
  color = "light",
  size = "medium",
  background = "white",
  borderStyle = "solid",
  rounded = "sm",
  multiple = false,
  visible = true,
  disabled = false,
  optionIcon = false,
  placement = "bottom",
  prefix,
  suffix,
  label,
  labelFontSize = "md",
  helperText,
  error = false,
  errorText,
  searchable = false,
  required = false,
  readonly = false,
  waiting = false,
  loading = false,
  placeholder = "Select...",
  options = [],
  value,
  defaultValue = [],
  requiredValue = [],
  skipValues = [],
  onChange,
  onSearch,
  onClick,
  onFocus,
  onBlur
}) => {
  const [isOpen, setIsOpen] = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useState)(false);
  const [searchText, setSearchText] = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useState)("");
  const [internalValue, setInternalValue] = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useState)(value ?? defaultValue);
  const [dropdownPosition, setDropdownPosition] = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useState)("bottom");
  const containerRef = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useRef)(null);
  const controlled = value !== undefined;
  const selectedValues = controlled ? value ?? [] : internalValue;
  (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useEffect)(() => {
    if (!isOpen || !containerRef.current) return;
    const rect = containerRef.current.getBoundingClientRect();
    const ESTIMATED_DROPDOWN_HEIGHT = 240;
    const spaceBelow = window.innerHeight - rect.bottom - 16;
    const spaceAbove = rect.top - 16;
    if (placement === "auto") {
      if (spaceBelow < ESTIMATED_DROPDOWN_HEIGHT && spaceAbove > spaceBelow + 80) {
        setDropdownPosition("top");
      } else {
        setDropdownPosition("bottom");
      }
    } else {
      setDropdownPosition(placement);
    }
  }, [isOpen, placement, options.length]);
  (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useEffect)(() => {
    const handleClickOutside = e => {
      if (!containerRef.current?.contains(e.target)) {
        setIsOpen(false);
        onBlur?.();
      }
    };
    if (isOpen) document.addEventListener("mousedown", handleClickOutside);
    return () => document.removeEventListener("mousedown", handleClickOutside);
  }, [isOpen, onBlur]);
  const toggleOpen = () => {
    if (readonly || disabled) return;
    setIsOpen(prev => !prev);
    if (!isOpen) onFocus?.();
    onClick?.();
  };
  const handleSelect = val => {
    if (disabled || val === "") return;
    let next = multiple ? selectedValues.includes(val) ? selectedValues.filter(v => v !== val) : [...selectedValues, val] : [val];
    const final = next.filter(v => !skipValues.includes(v));
    if (!controlled) setInternalValue(next);
    onChange?.(final);
    if (!multiple) setIsOpen(false);
  };
  const handleRemove = (val, e) => {
    e.stopPropagation();
    if (disabled || requiredValue.includes(val)) return;
    const next = selectedValues.filter(v => v !== val);
    const final = next.filter(v => !skipValues.includes(v));
    if (!controlled) setInternalValue(next);
    onChange?.(final);
  };
  const clearAll = e => {
    e.stopPropagation();
    let final = requiredValue.length > 0 ? requiredValue : [];
    if (!controlled) setInternalValue(final);
    onChange?.(final);
  };
  if (!visible) return null;
  return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsxs)("div", {
    id: id,
    style: {
      ...style,
      gap,
      minWidth: 0
    },
    ref: containerRef,
    className: (0,clsx__WEBPACK_IMPORTED_MODULE_2__["default"])("pn-select-box", isOpen && "pn-select-box--open", className),
    children: [label && /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsxs)("label", {
      className: (0,clsx__WEBPACK_IMPORTED_MODULE_2__["default"])("pn-select-box__label", `text-${labelFontSize}`),
      children: [label, required && /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsx)("span", {
        className: "pn-select-box__required",
        children: "*"
      })]
    }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsx)(SelectTrigger, {
      color: color,
      size: size,
      background: background,
      borderStyle: borderStyle,
      rounded: rounded,
      disabled: disabled,
      waiting: waiting,
      isOpen: isOpen,
      selectedValues: selectedValues,
      options: options,
      placeholder: placeholder,
      multiple: multiple,
      optionIcon: optionIcon,
      prefix: prefix,
      suffix: suffix,
      error: error,
      onToggle: toggleOpen,
      onRemove: handleRemove,
      children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsx)(SelectDropdown, {
        isOpen: isOpen,
        position: dropdownPosition,
        searchable: searchable,
        searchText: searchText,
        setSearchText: setSearchText,
        onSearch: onSearch,
        loading: loading,
        options: options,
        selectedValues: selectedValues,
        multiple: multiple,
        optionIcon: optionIcon,
        onSelect: handleSelect,
        onClearAll: clearAll
      })
    }), (helperText || error && errorText) && /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsx)("span", {
      className: (0,clsx__WEBPACK_IMPORTED_MODULE_2__["default"])("pn-select-box__helper-text", error && "pn-select-box__helper-text--error"),
      children: error && errorText ? errorText : helperText
    })]
  });
};
const SelectTrigger = ({
  color,
  size,
  background = "white",
  borderStyle,
  rounded,
  disabled,
  waiting,
  isOpen,
  selectedValues,
  options,
  placeholder,
  multiple,
  optionIcon,
  prefix,
  suffix,
  error,
  children,
  onToggle,
  onRemove
}) => {
  const selectedOpts = options.filter(o => selectedValues.includes(o.value));
  return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsxs)("div", {
    style: {
      minWidth: 0
    },
    className: (0,clsx__WEBPACK_IMPORTED_MODULE_2__["default"])("pn-select-trigger", `bg-${background}`, "border", error ? "border-error" : `border-${color}`, `border-${borderStyle}`, `pn-select-trigger--${size}`, `rounded-${rounded}`, multiple && selectedOpts.length > 0 && "pn-select-trigger--multiple-select", disabled && "pn-select-trigger--disabled"),
    onClick: onToggle,
    children: [prefix, /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsx)("div", {
      style: {
        minWidth: 0
      },
      className: "pn-select-value",
      children: waiting ? /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsx)(_icon__WEBPACK_IMPORTED_MODULE_1__["default"], {
        name: "progress_activity",
        style: {
          position: "absolute",
          top: "10px",
          left: "10px"
        },
        className: "loading"
      }) : selectedValues.length === 0 ? /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsx)("span", {
        className: "pn-select-placeholder",
        children: placeholder
      }) : multiple ? selectedOpts.map(opt => /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsxs)("span", {
        className: (0,clsx__WEBPACK_IMPORTED_MODULE_2__["default"])("pn-select-tag", opt?.warning && "pn-select-tag--warning"),
        children: [optionIcon && opt.icon && /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsx)(_icon__WEBPACK_IMPORTED_MODULE_1__["default"], {
          name: opt.icon,
          fontSize: "lg"
        }), opt.name ?? opt.value, /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsx)("span", {
          className: "pn-select-remove",
          onClick: e => onRemove(opt.value, e),
          children: "\xD7"
        })]
      }, opt.value)) : /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsxs)(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.Fragment, {
        children: [optionIcon && selectedOpts[0]?.icon && /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsx)(_icon__WEBPACK_IMPORTED_MODULE_1__["default"], {
          name: selectedOpts[0].icon,
          fontSize: "lg"
        }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsx)("span", {
          style: {
            minWidth: 0
          },
          className: "text-ellipsis single",
          children: selectedOpts[0]?.name ?? selectedValues[0]
        })]
      })
    }), suffix, /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsx)(_icon__WEBPACK_IMPORTED_MODULE_1__["default"], {
      name: "expand_more",
      fontSize: "xl",
      className: (0,clsx__WEBPACK_IMPORTED_MODULE_2__["default"])("pn-select-arrow", isOpen && "pn-select-arrow--open")
    }), children]
  });
};
const SelectDropdown = ({
  isOpen,
  position,
  searchable,
  searchText,
  setSearchText,
  onSearch,
  loading,
  options,
  selectedValues,
  multiple,
  optionIcon,
  onSelect,
  onClearAll
}) => {
  const filtered = options.filter(o => (o.name ?? o.value).toLowerCase().includes(searchText.toLowerCase()));
  const visibleOptions = multiple ? filtered.filter(o => !selectedValues.includes(o.value)) : filtered;
  return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsx)("div", {
    className: (0,clsx__WEBPACK_IMPORTED_MODULE_2__["default"])("pn-select-dropdown", `pn-select-dropdown--${position}`, isOpen && "pn-select-dropdown--open"),
    children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsxs)("div", {
      className: "pn-select-scroll",
      children: [searchable && /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsxs)("div", {
        style: {
          position: "sticky",
          top: 0
        },
        className: "pn-select-search",
        children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsx)(_icon__WEBPACK_IMPORTED_MODULE_1__["default"], {
          name: "search",
          fontSize: "xl"
        }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsx)("input", {
          value: searchText,
          onChange: e => {
            setSearchText(e.target.value);
            onSearch?.(e.target.value);
          },
          placeholder: "Search...",
          onClick: e => e.stopPropagation()
        })]
      }), loading ? /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsxs)("div", {
        className: "pn-select-loading",
        children: ["Loading...", /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsx)(_icon__WEBPACK_IMPORTED_MODULE_1__["default"], {
          name: "progress_activity",
          className: "loading"
        })]
      }) : visibleOptions.length === 0 ? /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsx)("div", {
        className: "pn-select-not-found",
        children: "No results found"
      }) : visibleOptions.map(opt => /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsxs)("div", {
        className: (0,clsx__WEBPACK_IMPORTED_MODULE_2__["default"])("pn-select-option", selectedValues.includes(opt.value) && "pn-select-option--selected"),
        onClick: e => {
          e.stopPropagation();
          onSelect(opt.value);
        },
        children: [optionIcon && opt.icon && /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsx)(_icon__WEBPACK_IMPORTED_MODULE_1__["default"], {
          name: opt.icon,
          color: selectedValues.includes(opt.value) ? "primary" : "black",
          fontSize: "lg"
        }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsx)("span", {
          className: "pn-select-option-name",
          children: opt.name ?? opt.value
        })]
      }, opt.value)), multiple && selectedValues.length > 0 && /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsx)("div", {
        className: "pn-select-clear-all",
        onClick: e => {
          e.stopPropagation();
          onClearAll(e);
        },
        children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsx)("span", {
          className: "pn-select-clear-all-text",
          children: "Clear all"
        })
      })]
    })
  });
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (SelectBox);

/***/ },

/***/ "./source/components/selectBox/index.ts"
/*!**********************************************!*\
  !*** ./source/components/selectBox/index.ts ***!
  \**********************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (/* reexport safe */ _SelectBox__WEBPACK_IMPORTED_MODULE_0__["default"])
/* harmony export */ });
/* harmony import */ var _SelectBox__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./SelectBox */ "./source/components/selectBox/SelectBox.tsx");

/***/ },

/***/ "./source/components/settingsField/SettingsField.tsx"
/*!***********************************************************!*\
  !*** ./source/components/settingsField/SettingsField.tsx ***!
  \***********************************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _description__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../description */ "./source/components/description/index.ts");
/* harmony import */ var _inlineStack__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../inlineStack */ "./source/components/inlineStack/index.ts");
/* harmony import */ var _blockStack__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../blockStack */ "./source/components/blockStack/index.ts");
/* harmony import */ var _wordpress_i18n__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! @wordpress/i18n */ "@wordpress/i18n");
/* harmony import */ var _wordpress_i18n__WEBPACK_IMPORTED_MODULE_3___default = /*#__PURE__*/__webpack_require__.n(_wordpress_i18n__WEBPACK_IMPORTED_MODULE_3__);
/* harmony import */ var _status__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ../status */ "./source/components/status/index.ts");
/* harmony import */ var _button__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ../button */ "./source/components/button/index.ts");
/* harmony import */ var _card__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! ../card */ "./source/components/card/index.ts");
/* harmony import */ var _text__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! ../text */ "./source/components/text/index.ts");
/* harmony import */ var clsx__WEBPACK_IMPORTED_MODULE_8__ = __webpack_require__(/*! clsx */ "./node_modules/clsx/dist/clsx.mjs");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_9__ = __webpack_require__(/*! react/jsx-runtime */ "react/jsx-runtime");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_9___default = /*#__PURE__*/__webpack_require__.n(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_9__);

const SettingsField = ({
  id,
  style,
  className,
  title,
  titleSize = "md",
  description,
  docLink,
  background = "white",
  border = "light",
  borderStyle,
  rounded,
  padding = 20,
  contentPadding,
  fullWidth = false,
  gap = 20,
  children,
  isIgnoreChildren = false,
  action,
  secondaryAction,
  statusProps
}) => {
  return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_9__.jsxs)(_card__WEBPACK_IMPORTED_MODULE_6__["default"], {
    id: id,
    style: style,
    padding: padding,
    background: background,
    border: border,
    borderStyle: borderStyle,
    rounded: rounded,
    statusProps: statusProps,
    className: className,
    children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_9__.jsxs)(_inlineStack__WEBPACK_IMPORTED_MODULE_1__["default"], {
      padding: contentPadding,
      align: "between",
      gap: 10,
      style: {
        marginBottom: children && !isIgnoreChildren && (title || description || action || secondaryAction) ? 16 : 0
      },
      children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_9__.jsxs)(_blockStack__WEBPACK_IMPORTED_MODULE_2__["default"], {
        gap: 10,
        className: (0,clsx__WEBPACK_IMPORTED_MODULE_8__["default"])(fullWidth && "w-full"),
        children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_9__.jsxs)(_inlineStack__WEBPACK_IMPORTED_MODULE_1__["default"], {
          gap: 15,
          className: (0,clsx__WEBPACK_IMPORTED_MODULE_8__["default"])(fullWidth && "w-full"),
          children: [action && action, title && (statusProps?.isPro ? /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_9__.jsxs)(_inlineStack__WEBPACK_IMPORTED_MODULE_1__["default"], {
            gap: 10,
            wrap: false,
            children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_9__.jsx)(_text__WEBPACK_IMPORTED_MODULE_7__["default"], {
              as: "h4",
              size: titleSize,
              weight: "medium",
              children: title
            }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_9__.jsx)(_status__WEBPACK_IMPORTED_MODULE_4__["default"].Pro, {
              tooltipDisabled: true
            })]
          }) : /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_9__.jsx)(_text__WEBPACK_IMPORTED_MODULE_7__["default"], {
            as: "h4",
            size: titleSize,
            weight: "medium",
            children: title
          })), secondaryAction && secondaryAction]
        }), description && /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_9__.jsx)(_description__WEBPACK_IMPORTED_MODULE_0__["default"], {
          text: description
        })]
      }), docLink && /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_9__.jsx)(_button__WEBPACK_IMPORTED_MODULE_5__["default"], {
        variant: "outlined",
        size: "extrasmall",
        startIcon: "info",
        href: docLink,
        target: "_blank",
        children: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_3__.__)("Documentation", "ninja-media")
      })]
    }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_9__.jsx)(_blockStack__WEBPACK_IMPORTED_MODULE_2__["default"], {
      gap: gap,
      children: children
    })]
  });
};
const SettingsSubField = ({
  id,
  style,
  className,
  title,
  description,
  docLink,
  background = "extralight",
  border,
  borderStyle = "dashed",
  rounded = "md",
  gap = 20,
  children,
  action,
  secondaryAction,
  isIgnoreChildren,
  depend,
  dependOn = "",
  dependOnExact = false,
  statusProps
}) => {
  const classes = (0,clsx__WEBPACK_IMPORTED_MODULE_8__["default"])(className, depend && "pn-settings-field-disabled");
  const handleClick = e => {
    const dependent = document.getElementById(dependOn);
    if (dependent && depend) {
      e.stopPropagation();
      const label = dependOnExact ? dependent : dependent.parentElement;
      if (!label) return;
      label.classList.add("pn-blink");
      setTimeout(() => {
        label.classList.remove("pn-blink");
      }, 500);
    }
  };
  return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_9__.jsxs)(_card__WEBPACK_IMPORTED_MODULE_6__["default"], {
    id: id,
    style: style,
    background: background,
    border: border,
    borderStyle: borderStyle,
    rounded: rounded,
    statusProps: statusProps,
    className: classes,
    onClick: handleClick,
    children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_9__.jsxs)(_inlineStack__WEBPACK_IMPORTED_MODULE_1__["default"], {
      align: "between",
      gap: 10,
      style: {
        marginBottom: children && !isIgnoreChildren && (title || description || action || secondaryAction) ? 16 : 0
      },
      children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_9__.jsxs)(_blockStack__WEBPACK_IMPORTED_MODULE_2__["default"], {
        gap: 10,
        children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_9__.jsxs)(_inlineStack__WEBPACK_IMPORTED_MODULE_1__["default"], {
          gap: 15,
          children: [action && action, title && /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_9__.jsx)(_text__WEBPACK_IMPORTED_MODULE_7__["default"], {
            as: "h4",
            weight: "medium",
            children: title
          }), secondaryAction && secondaryAction]
        }), description && /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_9__.jsx)(_description__WEBPACK_IMPORTED_MODULE_0__["default"], {
          text: description
        })]
      }), docLink && /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_9__.jsx)(_button__WEBPACK_IMPORTED_MODULE_5__["default"], {
        variant: "outlined",
        size: "small",
        startIcon: "info",
        href: docLink,
        target: "_blank",
        children: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_3__.__)("Documentation", "ninja-media")
      })]
    }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_9__.jsx)(_blockStack__WEBPACK_IMPORTED_MODULE_2__["default"], {
      gap: gap,
      children: children
    })]
  });
};
SettingsField.SubField = SettingsSubField;
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (SettingsField);

/***/ },

/***/ "./source/components/settingsField/index.ts"
/*!**************************************************!*\
  !*** ./source/components/settingsField/index.ts ***!
  \**************************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (/* reexport safe */ _SettingsField__WEBPACK_IMPORTED_MODULE_0__["default"])
/* harmony export */ });
/* harmony import */ var _SettingsField__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./SettingsField */ "./source/components/settingsField/SettingsField.tsx");

/***/ },

/***/ "./source/components/sidebar/Sidebar.tsx"
/*!***********************************************!*\
  !*** ./source/components/sidebar/Sidebar.tsx ***!
  \***********************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _hooks_useLocalStorage__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ~/hooks/useLocalStorage */ "./source/hooks/useLocalStorage.ts");
/* harmony import */ var _inlineStack__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../inlineStack */ "./source/components/inlineStack/index.ts");
/* harmony import */ var _blockStack__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../blockStack */ "./source/components/blockStack/index.ts");
/* harmony import */ var _iconButton__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ../iconButton */ "./source/components/iconButton/index.ts");
/* harmony import */ var _wordpress_i18n__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! @wordpress/i18n */ "@wordpress/i18n");
/* harmony import */ var _wordpress_i18n__WEBPACK_IMPORTED_MODULE_4___default = /*#__PURE__*/__webpack_require__.n(_wordpress_i18n__WEBPACK_IMPORTED_MODULE_4__);
/* harmony import */ var _constants_docs__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ~/constants/docs */ "./source/constants/docs.ts");
/* harmony import */ var _tooltip__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! ../tooltip */ "./source/components/tooltip/index.ts");
/* harmony import */ var _status__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! ../status */ "./source/components/status/index.ts");
/* harmony import */ var _card__WEBPACK_IMPORTED_MODULE_8__ = __webpack_require__(/*! ../card */ "./source/components/card/index.ts");
/* harmony import */ var _text__WEBPACK_IMPORTED_MODULE_9__ = __webpack_require__(/*! ../text */ "./source/components/text/index.ts");
/* harmony import */ var _icon__WEBPACK_IMPORTED_MODULE_10__ = __webpack_require__(/*! ../icon */ "./source/components/icon/index.ts");
/* harmony import */ var _logo__WEBPACK_IMPORTED_MODULE_11__ = __webpack_require__(/*! ../logo */ "./source/components/logo/index.ts");
/* harmony import */ var clsx__WEBPACK_IMPORTED_MODULE_12__ = __webpack_require__(/*! clsx */ "./node_modules/clsx/dist/clsx.mjs");
/* harmony import */ var _wordpress_element__WEBPACK_IMPORTED_MODULE_13__ = __webpack_require__(/*! @wordpress/element */ "@wordpress/element");
/* harmony import */ var _wordpress_element__WEBPACK_IMPORTED_MODULE_13___default = /*#__PURE__*/__webpack_require__.n(_wordpress_element__WEBPACK_IMPORTED_MODULE_13__);
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_14__ = __webpack_require__(/*! react/jsx-runtime */ "react/jsx-runtime");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_14___default = /*#__PURE__*/__webpack_require__.n(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_14__);

const SidebarContext = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_13__.createContext)(undefined);
const useSidebar = () => {
  const context = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_13__.useContext)(SidebarContext);
  if (!context) throw new Error("Sidebar context missing");
  return context;
};
const Sidebar = ({
  id,
  style,
  className,
  defaultCollapsed = false,
  localStorageKey = "pnpnm-sidebar",
  minWidth = 255,
  children
}) => {
  const SIDEBAR_MIN_WIDTH = minWidth;
  const SIDEBAR_MAX_WIDTH = 1000;
  const [collapsed, setCollapsed] = (0,_hooks_useLocalStorage__WEBPACK_IMPORTED_MODULE_0__.useLocalStorage)(localStorageKey, defaultCollapsed);
  const [width, setWidth] = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_13__.useState)(SIDEBAR_MIN_WIDTH);
  const [isResizing, setIsResizing] = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_13__.useState)(false);
  const toggle = () => {
    const newState = !collapsed;
    setCollapsed(newState);
  };
  const startResize = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_13__.useCallback)(e => {
    e.preventDefault();
    setIsResizing(true);
    const startX = e.clientX;
    const startWidth = width;
    const onMouseMove = event => {
      let newWidth = startWidth + (event.clientX - startX);
      if (newWidth < SIDEBAR_MIN_WIDTH) newWidth = SIDEBAR_MIN_WIDTH;
      if (newWidth > SIDEBAR_MAX_WIDTH) newWidth = SIDEBAR_MAX_WIDTH;
      setWidth(newWidth);
    };
    const onMouseUp = () => {
      setIsResizing(false);
      document.removeEventListener("mousemove", onMouseMove);
      document.removeEventListener("mouseup", onMouseUp);
    };
    document.addEventListener("mousemove", onMouseMove);
    document.addEventListener("mouseup", onMouseUp);
  }, [width]);
  return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_14__.jsx)(SidebarContext.Provider, {
    value: {
      collapsed
    },
    children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_14__.jsxs)(_blockStack__WEBPACK_IMPORTED_MODULE_2__["default"], {
      id: id,
      style: {
        ...style,
        width: collapsed ? 92 : width,
        flexShrink: 0,
        transition: collapsed || !isResizing ? "width 0.25s ease" : "none"
      },
      className: (0,clsx__WEBPACK_IMPORTED_MODULE_12__["default"])("pn-sidebar", collapsed && "pn-sidebar--collapsed", className),
      children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_14__.jsxs)(_inlineStack__WEBPACK_IMPORTED_MODULE_1__["default"], {
        align: collapsed ? "center" : "end",
        blockAlign: "center",
        gap: 5,
        className: "pn-sidebar__header",
        children: [!collapsed && /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_14__.jsx)("a", {
          href: _constants_docs__WEBPACK_IMPORTED_MODULE_5__["default"].featuresPage,
          target: "_blank",
          rel: "noreferrer",
          children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_14__.jsx)(_logo__WEBPACK_IMPORTED_MODULE_11__["default"], {})
        }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_14__.jsxs)(_text__WEBPACK_IMPORTED_MODULE_9__["default"], {
          size: "sm",
          weight: "medium",
          children: ["v ", pnpnm?.version]
        })]
      }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_14__.jsx)(_iconButton__WEBPACK_IMPORTED_MODULE_3__["default"], {
        variant: collapsed ? "primary" : "white",
        name: "keyboard_tab_rtl",
        size: "small",
        border: true,
        borderColor: "light",
        color: collapsed ? "white" : "primary",
        fontSize: "lg",
        rounded: "md",
        className: "pn-sidebar--toggle",
        onClick: toggle
      }), children, !collapsed && /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_14__.jsx)("div", {
        className: "pn-sidebar__resizer",
        onMouseDown: startResize
      })]
    })
  });
};
Sidebar.Menu = ({
  children
}) => /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_14__.jsx)(_blockStack__WEBPACK_IMPORTED_MODULE_2__["default"], {
  gap: 6,
  className: "pn-sidebar__menu",
  children: children
});
Sidebar.Item = ({
  title,
  icon,
  count,
  statusProps,
  active = false,
  onClick
}) => {
  const {
    collapsed
  } = useSidebar();
  return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_14__.jsx)(_tooltip__WEBPACK_IMPORTED_MODULE_6__["default"], {
    title: title,
    placement: "right",
    wrap: "no-wrap",
    arrow: true,
    className: "w-full",
    disabled: !collapsed,
    children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_14__.jsx)(_card__WEBPACK_IMPORTED_MODULE_8__["default"], {
      padding: 6,
      background: active ? "light" : "white",
      rounded: "lg",
      border: active ? "secondary" : "transparent",
      statusProps: {
        ...statusProps,
        top: collapsed ? -5 : 10,
        right: collapsed ? -5 : 10,
        tooltipDisabled: collapsed
      },
      className: "pn-sidebar__menu-item",
      onClick: onClick,
      children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_14__.jsxs)(_inlineStack__WEBPACK_IMPORTED_MODULE_1__["default"], {
        gap: 10,
        wrap: false,
        align: "between",
        style: {
          overflow: collapsed ? "visible" : "hidden"
        },
        children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_14__.jsxs)(_inlineStack__WEBPACK_IMPORTED_MODULE_1__["default"], {
          gap: 10,
          wrap: false,
          children: [collapsed && typeof count === "number" && count > 0 ? /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_14__.jsxs)(_blockStack__WEBPACK_IMPORTED_MODULE_2__["default"], {
            style: {
              position: "relative",
              width: "fit-content",
              height: "fit-content"
            },
            children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_14__.jsx)(_iconButton__WEBPACK_IMPORTED_MODULE_3__["default"], {
              variant: active ? "white" : "transparent",
              size: "small",
              rounded: "md",
              name: icon,
              border: false,
              color: "primary",
              style: {
                flexShrink: 0
              },
              className: (0,clsx__WEBPACK_IMPORTED_MODULE_12__["default"])("pn-sidebar__menu-item-button", active && "pn-sidebar__menu-item-button-active")
            }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_14__.jsx)(_card__WEBPACK_IMPORTED_MODULE_8__["default"], {
              padding: 3,
              background: active ? "white" : "light",
              rounded: "sm",
              flex: true,
              align: "center",
              blockAlign: "center",
              style: {
                position: "absolute",
                top: -15,
                left: -15,
                minWidth: "25px",
                width: "fit-content",
                height: "fit-content"
              },
              className: "pnpnm-fade-in",
              children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_14__.jsx)(_text__WEBPACK_IMPORTED_MODULE_9__["default"], {
                color: "black",
                size: "sm",
                children: count
              })
            })]
          }) : /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_14__.jsx)(_iconButton__WEBPACK_IMPORTED_MODULE_3__["default"], {
            variant: active ? "white" : "transparent",
            size: "small",
            rounded: "md",
            border: false,
            name: icon,
            color: "primary",
            style: {
              flexShrink: 0
            },
            className: (0,clsx__WEBPACK_IMPORTED_MODULE_12__["default"])("pn-sidebar__menu-item-button", active && "pn-sidebar__menu-item-button-active")
          }), !collapsed && /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_14__.jsx)(_text__WEBPACK_IMPORTED_MODULE_9__["default"], {
            color: active ? "black" : "secondaryblack",
            weight: active ? "medium" : "normal",
            size: "md",
            wrap: false,
            className: "pn-sidebar__menu-item-title",
            children: title
          })]
        }), typeof count === "number" && count > 0 && !collapsed && /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_14__.jsx)(_card__WEBPACK_IMPORTED_MODULE_8__["default"], {
          padding: 3,
          background: active ? "white" : "light",
          rounded: "sm",
          flex: true,
          align: "center",
          blockAlign: "center",
          style: {
            minWidth: "25px",
            width: "fit-content",
            height: "fit-content"
          },
          children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_14__.jsx)(_text__WEBPACK_IMPORTED_MODULE_9__["default"], {
            color: "black",
            size: "sm",
            children: count
          })
        })]
      })
    })
  });
};
Sidebar.Bottom = ({
  children,
  trash = false,
  trashCount,
  trashActive = false,
  helpCenter = true,
  isPro = false,
  trashClick,
  disabledTrash = false
}) => {
  const {
    collapsed
  } = useSidebar();
  return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_14__.jsxs)(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_14__.Fragment, {
    children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_14__.jsxs)(_blockStack__WEBPACK_IMPORTED_MODULE_2__["default"], {
      align: "center",
      inlineAlign: "center",
      gap: 10,
      className: (0,clsx__WEBPACK_IMPORTED_MODULE_12__["default"])("pn-sidebar__bottom-collapsed", collapsed && "pn-sidebar__bottom-collapsed-active"),
      children: [trash && /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_14__.jsx)(_tooltip__WEBPACK_IMPORTED_MODULE_6__["default"], {
        title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_4__.__)("Trash", "ninja-media"),
        wrap: "no-wrap",
        placement: "right",
        children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_14__.jsxs)(_blockStack__WEBPACK_IMPORTED_MODULE_2__["default"], {
          style: {
            position: "relative",
            width: "fit-content",
            height: "fit-content"
          },
          children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_14__.jsx)(_status__WEBPACK_IMPORTED_MODULE_7__["default"], {
            isPro: true,
            size: "small",
            top: -10,
            right: -10,
            tooltipDisabled: collapsed,
            children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_14__.jsx)(_iconButton__WEBPACK_IMPORTED_MODULE_3__["default"], {
              variant: "error",
              name: "delete",
              rounded: "md",
              iconStyle: {
                color: trashActive ? "var(--pnpnm-white)" : undefined
              },
              style: {
                backgroundColor: trashActive ? "var(--pnpnm-error)" : undefined,
                opacity: disabledTrash ? 0.5 : 1,
                cursor: disabledTrash ? "not-allowed" : "pointer",
                pointerEvents: disabledTrash ? "none" : "auto"
              },
              onClick: () => !disabledTrash && trashClick?.()
            })
          }), typeof trashCount === "number" && trashCount > 0 && /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_14__.jsx)(_card__WEBPACK_IMPORTED_MODULE_8__["default"], {
            padding: 3,
            background: "white",
            rounded: "sm",
            border: "errorlight",
            flex: true,
            align: "center",
            blockAlign: "center",
            style: {
              position: "absolute",
              top: -15,
              left: -15,
              minWidth: "25px",
              width: "fit-content",
              height: "fit-content"
            },
            children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_14__.jsx)(_text__WEBPACK_IMPORTED_MODULE_9__["default"], {
              color: "error",
              size: "sm",
              children: trashCount
            })
          })]
        })
      }), helpCenter && /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_14__.jsx)(_tooltip__WEBPACK_IMPORTED_MODULE_6__["default"], {
        title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_4__.__)("Help Center", "ninja-media"),
        wrap: "no-wrap",
        placement: "right",
        children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_14__.jsx)(_iconButton__WEBPACK_IMPORTED_MODULE_3__["default"], {
          variant: "secondary",
          name: "contact_support",
          rounded: "md",
          onClick: () => window.open(_constants_docs__WEBPACK_IMPORTED_MODULE_5__["default"]?.supportLink, "_blank", "noreferrer")
        })
      }), !pnpnm?.isPro && isPro && /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_14__.jsx)(_tooltip__WEBPACK_IMPORTED_MODULE_6__["default"], {
        title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_4__.__)("Upgrade to Pro", "ninja-media"),
        wrap: "no-wrap",
        placement: "right",
        children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_14__.jsx)(_iconButton__WEBPACK_IMPORTED_MODULE_3__["default"], {
          variant: "secondary",
          name: "crown",
          rounded: "md",
          onClick: () => window.open(pnpnm?.upgradeUrl, "_blank", "noreferrer")
        })
      })]
    }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_14__.jsx)(_blockStack__WEBPACK_IMPORTED_MODULE_2__["default"], {
      gap: 20,
      className: "pn-sidebar__bottom pn-sidebar__fade",
      children: children
    })]
  });
};
Sidebar.Trash = ({
  active,
  count,
  onClick,
  disabled = false
}) => {
  return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_14__.jsxs)(_card__WEBPACK_IMPORTED_MODULE_8__["default"], {
    statusProps: {
      isPro: true,
      size: "small",
      placement: "right-center",
      right: 10
    },
    padding: 10,
    background: active ? "error" : "errorextralight",
    border: active ? "error" : "errorextralight",
    flex: true,
    gap: 10,
    align: "between",
    blockAlign: "center",
    style: {
      height: "48px",
      borderRadius: "10px",
      opacity: disabled ? 0.5 : 1,
      cursor: disabled ? "not-allowed" : "pointer",
      pointerEvents: disabled ? "none" : "auto",
      userSelect: "none"
    },
    className: (0,clsx__WEBPACK_IMPORTED_MODULE_12__["default"])(!active && "hover-errorlight"),
    onClick: () => !disabled && onClick?.(),
    children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_14__.jsxs)(_inlineStack__WEBPACK_IMPORTED_MODULE_1__["default"], {
      gap: 8,
      wrap: false,
      children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_14__.jsx)(_icon__WEBPACK_IMPORTED_MODULE_10__["default"], {
        name: "delete",
        color: active ? "white" : "error",
        fontSize: "xl"
      }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_14__.jsx)(_text__WEBPACK_IMPORTED_MODULE_9__["default"], {
        color: active ? "white" : "error",
        children: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_4__.__)("Trash", "ninja-media")
      })]
    }), typeof count === "number" && count > 0 && /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_14__.jsx)(_card__WEBPACK_IMPORTED_MODULE_8__["default"], {
      padding: 3,
      background: active ? "white" : "errorextralight",
      rounded: "sm",
      border: "errorlight",
      flex: true,
      align: "center",
      blockAlign: "center",
      style: {
        minWidth: "25px",
        width: "fit-content",
        height: "fit-content"
      },
      children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_14__.jsx)(_text__WEBPACK_IMPORTED_MODULE_9__["default"], {
        color: "error",
        size: "sm",
        children: count
      })
    })]
  });
};
Sidebar.HelpCenter = () => {
  return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_14__.jsxs)(_inlineStack__WEBPACK_IMPORTED_MODULE_1__["default"], {
    gap: 8,
    wrap: false,
    align: "between",
    style: {
      cursor: "pointer",
      userSelect: "none"
    },
    onClick: () => window.open(_constants_docs__WEBPACK_IMPORTED_MODULE_5__["default"].supportLink, "_blank", "noreferrer"),
    children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_14__.jsxs)(_inlineStack__WEBPACK_IMPORTED_MODULE_1__["default"], {
      gap: 10,
      wrap: false,
      children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_14__.jsx)(_icon__WEBPACK_IMPORTED_MODULE_10__["default"], {
        name: "contact_support",
        color: "black",
        fontSize: "xl"
      }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_14__.jsx)(_text__WEBPACK_IMPORTED_MODULE_9__["default"], {
        color: "black",
        children: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_4__.__)("Help Center", "ninja-media")
      })]
    }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_14__.jsx)(_icon__WEBPACK_IMPORTED_MODULE_10__["default"], {
      name: "open_in_new",
      color: "descgray",
      fontSize: "xl"
    })]
  });
};
Sidebar.UpgradePro = () => /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_14__.jsxs)(_inlineStack__WEBPACK_IMPORTED_MODULE_1__["default"], {
  gap: 8,
  wrap: false,
  style: {
    cursor: "pointer",
    userSelect: "none"
  },
  onClick: () => window.open(_constants_docs__WEBPACK_IMPORTED_MODULE_5__["default"].pricingPage, "_blank", "noreferrer"),
  children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_14__.jsx)(_icon__WEBPACK_IMPORTED_MODULE_10__["default"], {
    name: "crown",
    color: "primary",
    fontSize: "xl"
  }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_14__.jsx)(_text__WEBPACK_IMPORTED_MODULE_9__["default"], {
    color: "primary",
    children: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_4__.__)("Upgrade to Pro", "ninja-media")
  })]
});
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (Sidebar);

/***/ },

/***/ "./source/components/sidebar/index.ts"
/*!********************************************!*\
  !*** ./source/components/sidebar/index.ts ***!
  \********************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (/* reexport safe */ _Sidebar__WEBPACK_IMPORTED_MODULE_0__["default"])
/* harmony export */ });
/* harmony import */ var _Sidebar__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./Sidebar */ "./source/components/sidebar/Sidebar.tsx");

/***/ },

/***/ "./source/components/skeletonLoader/SkeletonBreadcrumb.tsx"
/*!*****************************************************************!*\
  !*** ./source/components/skeletonLoader/SkeletonBreadcrumb.tsx ***!
  \*****************************************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _wordpress_element__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @wordpress/element */ "@wordpress/element");
/* harmony import */ var _wordpress_element__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(_wordpress_element__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var _SkeletonLoader__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./SkeletonLoader */ "./source/components/skeletonLoader/SkeletonLoader.tsx");
/* harmony import */ var _inlineStack__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../inlineStack */ "./source/components/inlineStack/index.ts");
/* harmony import */ var _icon__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ../icon */ "./source/components/icon/index.ts");
/* harmony import */ var _card__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ../card */ "./source/components/card/index.ts");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! react/jsx-runtime */ "react/jsx-runtime");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_5___default = /*#__PURE__*/__webpack_require__.n(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_5__);

const SkeletonBreadcrumb = ({
  folder = false
}) => {
  const containerRef = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useRef)(null);
  const [mounted, setMounted] = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useState)(false);
  const [count, setCount] = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useState)(0);
  const [widths, setWidths] = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useState)([]);
  (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useEffect)(() => {
    const id = setTimeout(() => setMounted(true), 50);
    return () => clearTimeout(id);
  }, []);
  (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useEffect)(() => {
    const calculateItems = () => {
      if (!containerRef.current) return;
      const containerWidth = containerRef.current.offsetWidth;
      if (folder) {
        const gap = 5;
        const minItemWidth = 60;
        const possibleCount = Math.max(4, Math.floor(containerWidth / (minItemWidth + gap)));
        setCount(possibleCount);
        const newWidths = Array.from({
          length: possibleCount
        }).map(() => Math.floor(60 + Math.random() * 120));
        setWidths(newWidths);
      } else {
        const fixedWidth = 245;
        const remainingWidth = Math.max(0, containerWidth - fixedWidth);
        const gap = 5;
        const minItemWidth = 95;
        const dynamicCount = Math.max(1, Math.floor(remainingWidth / (minItemWidth + gap)));
        setCount(dynamicCount);
        const newWidths = Array.from({
          length: dynamicCount + 2
        }).map(() => Math.floor(60 + Math.random() * 120));
        setWidths(newWidths);
      }
    };
    calculateItems();
    window.addEventListener("resize", calculateItems);
    const interval = setInterval(() => {
      setWidths(prev => prev.map(() => Math.floor(60 + Math.random() * 120)));
    }, 1400);
    return () => {
      window.removeEventListener("resize", calculateItems);
      clearInterval(interval);
    };
  }, [folder]);
  if (folder) {
    return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_5__.jsx)(_inlineStack__WEBPACK_IMPORTED_MODULE_2__["default"], {
      ref: containerRef,
      gap: 5,
      wrap: false,
      style: {
        width: "100%",
        overflow: "hidden"
      },
      children: Array.from({
        length: count
      }).map((_, index) => /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_5__.jsx)(_SkeletonLoader__WEBPACK_IMPORTED_MODULE_1__["default"], {
        width: mounted ? `${widths[index] || 100}px` : "10px",
        height: "32px",
        rounded: "xs",
        style: {
          flexShrink: 0,
          transition: "width 0.9s ease-in-out",
          minWidth: "60px"
        }
      }, index))
    });
  }
  return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_5__.jsxs)(_inlineStack__WEBPACK_IMPORTED_MODULE_2__["default"], {
    ref: containerRef,
    gap: 5,
    wrap: false,
    style: {
      width: "100%",
      overflow: "hidden"
    },
    children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_5__.jsx)(_icon__WEBPACK_IMPORTED_MODULE_3__["default"], {
      name: "home",
      color: "primary",
      fontSize: "xl"
    }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_5__.jsxs)(_inlineStack__WEBPACK_IMPORTED_MODULE_2__["default"], {
      gap: 5,
      wrap: false,
      children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_5__.jsx)(_icon__WEBPACK_IMPORTED_MODULE_3__["default"], {
        name: "keyboard_arrow_right",
        color: "secondaryblack",
        fontSize: "lg"
      }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_5__.jsx)(_SkeletonLoader__WEBPACK_IMPORTED_MODULE_1__["default"], {
        width: mounted ? `${widths[0] || 95}px` : "10px",
        height: "22px",
        rounded: "sm",
        style: {
          transition: "width 0.9s ease-in-out",
          flexShrink: 0,
          minWidth: "60px"
        }
      })]
    }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_5__.jsxs)(_inlineStack__WEBPACK_IMPORTED_MODULE_2__["default"], {
      gap: 5,
      wrap: false,
      children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_5__.jsx)(_icon__WEBPACK_IMPORTED_MODULE_3__["default"], {
        name: "keyboard_arrow_right",
        color: "secondaryblack",
        fontSize: "lg"
      }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_5__.jsx)(_card__WEBPACK_IMPORTED_MODULE_4__["default"], {
        padding: 5,
        rounded: "sm",
        flex: true,
        align: "center",
        blockAlign: "center",
        style: {
          width: "32px",
          height: "28px",
          flexShrink: 0
        },
        children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_5__.jsx)(_icon__WEBPACK_IMPORTED_MODULE_3__["default"], {
          name: "more_horiz",
          color: "secondaryblack",
          fontSize: "lg"
        })
      })]
    }), Array.from({
      length: count
    }).map((_, index) => /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_5__.jsxs)(_inlineStack__WEBPACK_IMPORTED_MODULE_2__["default"], {
      gap: 5,
      wrap: false,
      children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_5__.jsx)(_icon__WEBPACK_IMPORTED_MODULE_3__["default"], {
        name: "keyboard_arrow_right",
        color: "secondaryblack",
        fontSize: "lg"
      }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_5__.jsx)(_SkeletonLoader__WEBPACK_IMPORTED_MODULE_1__["default"], {
        width: mounted ? `${widths[index + 1] || 110}px` : "10px",
        height: "22px",
        rounded: "sm",
        style: {
          transition: "width 0.9s ease-in-out",
          flexShrink: 0,
          minWidth: "60px"
        }
      })]
    }, index))]
  });
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (SkeletonBreadcrumb);

/***/ },

/***/ "./source/components/skeletonLoader/SkeletonFile.tsx"
/*!***********************************************************!*\
  !*** ./source/components/skeletonLoader/SkeletonFile.tsx ***!
  \***********************************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _wordpress_element__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @wordpress/element */ "@wordpress/element");
/* harmony import */ var _wordpress_element__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(_wordpress_element__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var _SkeletonLoader__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./SkeletonLoader */ "./source/components/skeletonLoader/SkeletonLoader.tsx");
/* harmony import */ var _inlineStack__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../inlineStack */ "./source/components/inlineStack/index.ts");
/* harmony import */ var _blockStack__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ../blockStack */ "./source/components/blockStack/index.ts");
/* harmony import */ var _card__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ../card */ "./source/components/card/index.ts");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! react/jsx-runtime */ "react/jsx-runtime");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_5___default = /*#__PURE__*/__webpack_require__.n(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_5__);

const SkeletonFile = ({
  variant = "grid",
  length = 10
}) => {
  const [mounted, setMounted] = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useState)(false);
  const [randomWidths, setRandomWidths] = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useState)([]);
  (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useEffect)(() => {
    const id = setTimeout(() => setMounted(true), 50);
    return () => clearTimeout(id);
  }, []);
  (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useEffect)(() => {
    const generateWidths = () => {
      return [...Array(length)].map(() => ({
        name: `${30 + Math.random() * 45}%`,
        info: `${30 + Math.random() * 45}%`
      }));
    };
    setRandomWidths(generateWidths());
    const interval = setInterval(() => {
      setRandomWidths(prev => prev.map(() => ({
        name: `${30 + Math.random() * 45}%`,
        info: `${30 + Math.random() * 45}%`
      })));
    }, 1400);
    return () => clearInterval(interval);
  }, [length]);
  const FLEX_VALUES = ["5", "1.3", "1.3", "1.3", "1.5", "1.3"];
  return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_5__.jsx)(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_5__.Fragment, {
    children: variant === "grid" ? randomWidths?.map(({
      name,
      info
    }, index) => /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_5__.jsxs)(_card__WEBPACK_IMPORTED_MODULE_4__["default"], {
      padding: 10,
      background: "white",
      rounded: "md",
      flex: true,
      direction: "col",
      gap: 10,
      children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_5__.jsx)(_SkeletonLoader__WEBPACK_IMPORTED_MODULE_1__["default"], {
        width: "100%",
        height: "150px",
        rounded: "sm"
      }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_5__.jsxs)(_blockStack__WEBPACK_IMPORTED_MODULE_3__["default"], {
        gap: 5,
        children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_5__.jsx)(_SkeletonLoader__WEBPACK_IMPORTED_MODULE_1__["default"], {
          width: mounted ? name : "10%",
          height: "18px",
          rounded: "sm",
          style: {
            transition: "width 1s ease-in-out"
          }
        }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_5__.jsxs)(_inlineStack__WEBPACK_IMPORTED_MODULE_2__["default"], {
          align: "between",
          gap: 5,
          wrap: false,
          children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_5__.jsxs)(_inlineStack__WEBPACK_IMPORTED_MODULE_2__["default"], {
            gap: 5,
            wrap: false,
            style: {
              width: "100%"
            },
            children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_5__.jsx)(_SkeletonLoader__WEBPACK_IMPORTED_MODULE_1__["default"], {
              width: "100%",
              height: "18px",
              rounded: "sm"
            }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_5__.jsx)(_SkeletonLoader__WEBPACK_IMPORTED_MODULE_1__["default"], {
              width: "100%",
              height: "18px",
              rounded: "sm"
            })]
          }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_5__.jsx)(_SkeletonLoader__WEBPACK_IMPORTED_MODULE_1__["default"], {
            width: "18px",
            height: "18px",
            rounded: "sm",
            style: {
              flexShrink: 0
            }
          })]
        })]
      })]
    }, index)) : /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_5__.jsx)(_card__WEBPACK_IMPORTED_MODULE_4__["default"], {
      padding: 0,
      background: "white",
      rounded: "md",
      borderStyle: "none",
      children: randomWidths?.map(({
        name,
        info
      }, index) => /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_5__.jsxs)(_inlineStack__WEBPACK_IMPORTED_MODULE_2__["default"], {
        className: "pnpnm-file-list-item",
        children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_5__.jsxs)(_inlineStack__WEBPACK_IMPORTED_MODULE_2__["default"], {
          gap: 10,
          wrap: false,
          style: {
            flex: FLEX_VALUES[0]
          },
          children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_5__.jsx)(_SkeletonLoader__WEBPACK_IMPORTED_MODULE_1__["default"], {
            width: "50px",
            height: "40px",
            rounded: "sm",
            style: {
              flexShrink: 0
            }
          }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_5__.jsx)("div", {
            style: {
              width: "100%"
            },
            children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_5__.jsx)(_SkeletonLoader__WEBPACK_IMPORTED_MODULE_1__["default"], {
              width: mounted ? name : "10%",
              height: "18px",
              rounded: "sm",
              style: {
                transition: "width 1s ease-in-out"
              }
            })
          })]
        }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_5__.jsx)("div", {
          style: {
            flex: FLEX_VALUES[1],
            width: "100%",
            display: "flex",
            justifyContent: "center"
          },
          children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_5__.jsx)(_SkeletonLoader__WEBPACK_IMPORTED_MODULE_1__["default"], {
            width: mounted ? info : "10%",
            height: "18px",
            rounded: "sm",
            style: {
              transition: "width 1s ease-in-out"
            }
          })
        }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_5__.jsx)("div", {
          style: {
            flex: FLEX_VALUES[2],
            width: "100%",
            display: "flex",
            justifyContent: "center"
          },
          children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_5__.jsx)(_SkeletonLoader__WEBPACK_IMPORTED_MODULE_1__["default"], {
            width: mounted ? info : "10%",
            height: "18px",
            rounded: "sm",
            style: {
              transition: "width 1s ease-in-out"
            }
          })
        }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_5__.jsx)(_blockStack__WEBPACK_IMPORTED_MODULE_3__["default"], {
          inlineAlign: "center",
          style: {
            flex: FLEX_VALUES[3]
          },
          children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_5__.jsx)(_SkeletonLoader__WEBPACK_IMPORTED_MODULE_1__["default"], {
            width: "40px",
            height: "40px",
            rounded: "md"
          })
        }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_5__.jsx)("div", {
          style: {
            flex: FLEX_VALUES[4],
            width: "100%",
            display: "flex",
            justifyContent: "center"
          },
          children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_5__.jsx)(_SkeletonLoader__WEBPACK_IMPORTED_MODULE_1__["default"], {
            width: mounted ? info : "10%",
            height: "18px",
            rounded: "sm",
            style: {
              transition: "width 1s ease-in-out"
            }
          })
        }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_5__.jsx)("div", {
          style: {
            flex: FLEX_VALUES[5],
            width: "100%",
            display: "flex",
            justifyContent: "flex-end"
          },
          children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_5__.jsx)(_SkeletonLoader__WEBPACK_IMPORTED_MODULE_1__["default"], {
            width: "70px",
            height: "28px",
            rounded: "sm",
            style: {
              flexShrink: 0
            }
          })
        })]
      }, index))
    })
  });
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (SkeletonFile);

/***/ },

/***/ "./source/components/skeletonLoader/SkeletonFolder.tsx"
/*!*************************************************************!*\
  !*** ./source/components/skeletonLoader/SkeletonFolder.tsx ***!
  \*************************************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _components_inlineStack__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ~/components/inlineStack */ "./source/components/inlineStack/index.ts");
/* harmony import */ var _components_blockStack__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ~/components/blockStack */ "./source/components/blockStack/index.ts");
/* harmony import */ var _SkeletonLoader__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./SkeletonLoader */ "./source/components/skeletonLoader/SkeletonLoader.tsx");
/* harmony import */ var _wordpress_element__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! @wordpress/element */ "@wordpress/element");
/* harmony import */ var _wordpress_element__WEBPACK_IMPORTED_MODULE_3___default = /*#__PURE__*/__webpack_require__.n(_wordpress_element__WEBPACK_IMPORTED_MODULE_3__);
/* harmony import */ var _components_card__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ~/components/card */ "./source/components/card/index.ts");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! react/jsx-runtime */ "react/jsx-runtime");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_5___default = /*#__PURE__*/__webpack_require__.n(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_5__);

const SkeletonFolder = ({
  theme = "default",
  style,
  className
}) => {
  const textWidth = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_3__.useMemo)(() => {
    return `${70 + Math.random() * 80}px`;
  }, []);
  return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_5__.jsxs)(_components_card__WEBPACK_IMPORTED_MODULE_4__["default"], {
    padding: 3,
    background: "white",
    rounded: "sm",
    border: "transparent",
    flex: true,
    direction: "row",
    align: "between",
    gap: 10,
    wrap: false,
    style: {
      ...style,
      minWidth: 0,
      height: "30px",
      transition: "all 0.2s ease"
    },
    className: className,
    children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_5__.jsxs)(_components_inlineStack__WEBPACK_IMPORTED_MODULE_0__["default"], {
      gap: 9,
      wrap: false,
      style: {
        marginLeft: "4px",
        minWidth: 0
      },
      children: [theme !== "default" && /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_5__.jsx)(_components_card__WEBPACK_IMPORTED_MODULE_4__["default"], {
        padding: 3,
        background: "white",
        rounded: "xs",
        border: "transparent",
        flex: true,
        align: "center",
        blockAlign: "center",
        style: {
          flexShrink: 0,
          width: "22px",
          height: "22px"
        },
        children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_5__.jsx)(_SkeletonLoader__WEBPACK_IMPORTED_MODULE_2__["default"], {
          width: "100%",
          height: "100%",
          rounded: "none"
        })
      }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_5__.jsx)(_components_blockStack__WEBPACK_IMPORTED_MODULE_1__["default"], {
        style: {
          flexShrink: 0
        },
        children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_5__.jsx)(_SkeletonLoader__WEBPACK_IMPORTED_MODULE_2__["default"], {
          width: 20,
          height: 20,
          rounded: "sm"
        })
      }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_5__.jsx)(_SkeletonLoader__WEBPACK_IMPORTED_MODULE_2__["default"], {
        width: textWidth,
        height: 12,
        rounded: "sm"
      })]
    }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_5__.jsx)(_components_card__WEBPACK_IMPORTED_MODULE_4__["default"], {
      padding: 3,
      background: "white",
      rounded: "xs",
      border: "transparent",
      flex: true,
      align: "center",
      blockAlign: "center",
      style: {
        flexShrink: 0,
        width: "22px",
        height: "22px"
      },
      children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_5__.jsx)(_SkeletonLoader__WEBPACK_IMPORTED_MODULE_2__["default"], {
        width: "100%",
        height: "100%",
        rounded: "none"
      })
    })]
  });
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (SkeletonFolder);

/***/ },

/***/ "./source/components/skeletonLoader/SkeletonLoader.tsx"
/*!*************************************************************!*\
  !*** ./source/components/skeletonLoader/SkeletonLoader.tsx ***!
  \*************************************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _SkeletonBreadcrumb__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./SkeletonBreadcrumb */ "./source/components/skeletonLoader/SkeletonBreadcrumb.tsx");
/* harmony import */ var _SkeletonFolder__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./SkeletonFolder */ "./source/components/skeletonLoader/SkeletonFolder.tsx");
/* harmony import */ var _SkeletonFile__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./SkeletonFile */ "./source/components/skeletonLoader/SkeletonFile.tsx");
/* harmony import */ var clsx__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! clsx */ "./node_modules/clsx/dist/clsx.mjs");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! react/jsx-runtime */ "react/jsx-runtime");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_4___default = /*#__PURE__*/__webpack_require__.n(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_4__);

const SkeletonLoader = ({
  id,
  style,
  className,
  width,
  height,
  animationSpeed = 1.5,
  rounded = "md"
}) => {
  const combinedStyle = {
    width,
    height,
    ...style,
    "--pnpnm-skl-animation-speed": `${animationSpeed}s`
  };
  const classes = (0,clsx__WEBPACK_IMPORTED_MODULE_3__["default"])("pnpnm-skeleton-loader", "pnpnm-skeleton-loader--loading", `rounded-${rounded}`, className);
  return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_4__.jsx)("div", {
    id: id,
    style: combinedStyle,
    className: classes
  });
};
SkeletonLoader.SkeletonFolder = _SkeletonFolder__WEBPACK_IMPORTED_MODULE_1__["default"];
SkeletonLoader.SkeletonFile = _SkeletonFile__WEBPACK_IMPORTED_MODULE_2__["default"];
SkeletonLoader.SkeletonBreadcrumb = _SkeletonBreadcrumb__WEBPACK_IMPORTED_MODULE_0__["default"];
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (SkeletonLoader);

/***/ },

/***/ "./source/components/skeletonLoader/index.ts"
/*!***************************************************!*\
  !*** ./source/components/skeletonLoader/index.ts ***!
  \***************************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (/* reexport safe */ _SkeletonLoader__WEBPACK_IMPORTED_MODULE_0__["default"])
/* harmony export */ });
/* harmony import */ var _SkeletonLoader__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./SkeletonLoader */ "./source/components/skeletonLoader/SkeletonLoader.tsx");

/***/ },

/***/ "./source/components/slider/Slider.tsx"
/*!*********************************************!*\
  !*** ./source/components/slider/Slider.tsx ***!
  \*********************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _wordpress_element__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @wordpress/element */ "@wordpress/element");
/* harmony import */ var _wordpress_element__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(_wordpress_element__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var _wordpress_i18n__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! @wordpress/i18n */ "@wordpress/i18n");
/* harmony import */ var _wordpress_i18n__WEBPACK_IMPORTED_MODULE_1___default = /*#__PURE__*/__webpack_require__.n(_wordpress_i18n__WEBPACK_IMPORTED_MODULE_1__);
/* harmony import */ var _selectBox__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../selectBox */ "./source/components/selectBox/index.ts");
/* harmony import */ var _button__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ../button */ "./source/components/button/index.ts");
/* harmony import */ var _text__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ../text */ "./source/components/text/index.ts");
/* harmony import */ var clsx__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! clsx */ "./node_modules/clsx/dist/clsx.mjs");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! react/jsx-runtime */ "react/jsx-runtime");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_6___default = /*#__PURE__*/__webpack_require__.n(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_6__);

const Slider = ({
  id,
  style,
  className = "",
  size = "small",
  min = 0,
  max = 100,
  step = 1,
  value = 50,
  defaultValue = 50,
  reset = false,
  showMark = false,
  marks = [],
  unit = false,
  unitOptions = [],
  unitValue = [],
  defaultUnit = "",
  unitPlaceholder = "Unit",
  trackDisabled = false,
  disabled = false,
  onChange
}) => {
  const [inputValue, setInputValue] = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useState)(value);
  const [isDragging, setIsDragging] = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useState)(false);
  const [tooltipLeft, setTooltipLeft] = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useState)(0);
  const [error, setError] = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useState)("");
  const trackRef = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useRef)(null);
  (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useEffect)(() => {
    setInputValue(value);
  }, [value]);
  const percent = (value - min) / (max - min) * 100;
  const updateValueFromClientX = clientX => {
    if (!trackRef.current) return;
    const rect = trackRef.current.getBoundingClientRect();
    let newPercent = (clientX - rect.left) / rect.width * 100;
    newPercent = Math.max(0, Math.min(100, newPercent));
    const newValue = Math.round((min + newPercent / 100 * (max - min)) / step) * step;
    handleOnchange(newValue, unitValue[0]);
    setError("");
  };
  const handleMouseDown = e => {
    if (disabled || trackDisabled) return;
    setIsDragging(true);
    updateValueFromClientX(e.clientX);
  };
  const handleMouseMove = e => {
    if (!isDragging || disabled || trackDisabled) return;
    updateValueFromClientX(e.clientX);
  };
  const handleMouseUp = () => {
    if (isDragging) setIsDragging(false);
  };
  (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useEffect)(() => {
    if (isDragging) {
      window.addEventListener("mousemove", handleMouseMove);
      window.addEventListener("mouseup", handleMouseUp);
    } else {
      window.removeEventListener("mousemove", handleMouseMove);
      window.removeEventListener("mouseup", handleMouseUp);
    }
    return () => {
      window.removeEventListener("mousemove", handleMouseMove);
      window.removeEventListener("mouseup", handleMouseUp);
    };
  }, [isDragging]);
  (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useEffect)(() => {
    if (trackRef.current) {
      const trackWidth = trackRef.current.offsetWidth;
      const thumbWidth = 14;
      const left = percent / 100 * (trackWidth - thumbWidth) + thumbWidth / 2;
      setTooltipLeft(left);
    }
  }, [value, percent]);
  const getMarkPosition = markValue => {
    if (!trackRef.current) return 0;
    const trackWidth = trackRef.current.offsetWidth;
    const thumbWidth = 14;
    const percent = (markValue - min) / (max - min);
    return percent * (trackWidth - thumbWidth) + thumbWidth / 2;
  };
  const handleOnchange = (newValue, newUnit) => {
    if (disabled) return;
    setError("");
    const isNewUnit = newUnit && unitValue[0] !== newUnit;
    const defaultUnitValue = unitOptions.find(option => option.value === newUnit)?.defaultValue;
    onChange(isNewUnit ? defaultUnitValue : newValue, newUnit);
  };
  const handleReset = () => {
    if (disabled) return;
    handleOnchange(defaultValue, defaultUnit);
    setError("");
  };
  return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_6__.jsxs)("div", {
    id: id,
    style: style,
    className: (0,clsx__WEBPACK_IMPORTED_MODULE_5__["default"])("pn-slider", size === "small" && "pn-slider--small", disabled && "pn-slider--disabled", className),
    children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_6__.jsxs)("div", {
      className: (0,clsx__WEBPACK_IMPORTED_MODULE_5__["default"])("pn-slider__track-container", trackDisabled && "pn-slider__track-container--disabled"),
      children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_6__.jsxs)("div", {
        ref: trackRef,
        className: "pn-slider__track-container-wrapper",
        onMouseDown: handleMouseDown,
        children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_6__.jsxs)("div", {
          className: "pn-slider__track-container-wrapper-track",
          children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_6__.jsx)("div", {
            className: "pn-slider__track-container-wrapper-track-fill",
            style: {
              width: `${percent}%`
            }
          }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_6__.jsx)("div", {
            style: {
              left: `${percent}%`
            },
            className: (0,clsx__WEBPACK_IMPORTED_MODULE_5__["default"])("pn-slider__track-container-wrapper-thumb", isDragging && "pn-slider__track-container-wrapper-thumb--active")
          })]
        }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_6__.jsx)("div", {
          className: "pn-slider__track-container-wrapper-tooltip",
          style: {
            left: `${tooltipLeft}px`
          },
          children: value
        })]
      }), showMark && /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_6__.jsx)("div", {
        className: "pn-slider-marks",
        children: marks.map(({
          name,
          value: markValue
        }, index) => {
          const markLeft = getMarkPosition(markValue);
          const isActive = value === markValue;
          const isPassed = value >= markValue;
          return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_6__.jsx)("div", {
            style: {
              position: "absolute",
              left: `${markLeft}px`,
              transform: "translateX(-50%)"
            },
            className: (0,clsx__WEBPACK_IMPORTED_MODULE_5__["default"])("pn-slider-mark", isActive && "pn-slider-mark--active", isPassed && "pn-slider-mark--passed"),
            onClick: () => {
              setError("");
              !disabled && handleOnchange(markValue, unitValue[0]);
            },
            children: name
          }, index);
        })
      })]
    }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_6__.jsxs)("div", {
      className: (0,clsx__WEBPACK_IMPORTED_MODULE_5__["default"])("pn-slider-input-container", unit ? "" : "pn-slider-input-container--no-unit"),
      children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_6__.jsx)("input", {
        type: "number",
        value: inputValue,
        step: step,
        disabled: disabled,
        className: (0,clsx__WEBPACK_IMPORTED_MODULE_5__["default"])("pn-slider-input-box"),
        onChange: e => {
          setInputValue(e.target.value);
          setError("");
          if (max < Number(e.target.value)) {
            setError((0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_1__.__)("Value exceeds maximum", "ninja-media"));
            return;
          }
          if (min > Number(e.target.value)) {
            setError((0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_1__.__)("Value is below minimum", "ninja-media"));
            return;
          }
          handleOnchange(Number(e.target.value), unitValue[0]);
        }
      }), unit && /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_6__.jsx)("div", {
        className: "pn-slider-input-divider"
      }), unit && /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_6__.jsx)(_selectBox__WEBPACK_IMPORTED_MODULE_2__["default"], {
        size: size === "small" ? "small" : "medium",
        borderStyle: "none",
        options: unitOptions,
        value: unitValue,
        placeholder: unitPlaceholder,
        onChange: unitValue => handleOnchange(value, unitValue[0]),
        disabled: disabled
      })]
    }), reset && /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_6__.jsx)(_button__WEBPACK_IMPORTED_MODULE_3__["default"], {
      variant: "error",
      size: size === "small" ? "small" : "medium",
      onClick: handleReset,
      disabled: disabled,
      children: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_1__.__)("Reset", "google-drive-integration")
    }), error && /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_6__.jsx)(_text__WEBPACK_IMPORTED_MODULE_4__["default"], {
      color: "error",
      size: "sm",
      children: error
    })]
  });
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (Slider);

/***/ },

/***/ "./source/components/slider/index.ts"
/*!*******************************************!*\
  !*** ./source/components/slider/index.ts ***!
  \*******************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (/* reexport safe */ _Slider__WEBPACK_IMPORTED_MODULE_0__["default"])
/* harmony export */ });
/* harmony import */ var _Slider__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./Slider */ "./source/components/slider/Slider.tsx");

/***/ },

/***/ "./source/components/status/PremiumCard.tsx"
/*!**************************************************!*\
  !*** ./source/components/status/PremiumCard.tsx ***!
  \**************************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _inlineStack__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../inlineStack */ "./source/components/inlineStack/index.ts");
/* harmony import */ var _blockStack__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../blockStack */ "./source/components/blockStack/index.ts");
/* harmony import */ var _constants_docs__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ~/constants/docs */ "./source/constants/docs.ts");
/* harmony import */ var _card__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ../card */ "./source/components/card/index.ts");
/* harmony import */ var _icon__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ../icon */ "./source/components/icon/index.ts");
/* harmony import */ var _text__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ../text */ "./source/components/text/index.ts");
/* harmony import */ var _logo__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! ../logo */ "./source/components/logo/index.ts");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! react/jsx-runtime */ "react/jsx-runtime");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_7___default = /*#__PURE__*/__webpack_require__.n(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_7__);

const PremiumCard = ({
  compact,
  style
}) => {
  const featuresToShow = compact ? FEATURES.slice(0, 5) : FEATURES;
  return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_7__.jsxs)(_card__WEBPACK_IMPORTED_MODULE_3__["default"], {
    padding: 15,
    flex: true,
    direction: "col",
    gap: 10,
    style: {
      position: "relative",
      overflow: "hidden",
      ...style
    },
    className: "pn-sidebar__fade",
    children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_7__.jsx)(_card__WEBPACK_IMPORTED_MODULE_3__["default"], {
      padding: "35px 10px 10px 10px",
      background: "primary",
      style: {
        width: "fit-content ",
        position: "absolute",
        top: -20,
        right: -15,
        transform: "rotate(5deg)",
        zIndex: 10,
        borderRadius: "14px"
      },
      children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_7__.jsx)(_text__WEBPACK_IMPORTED_MODULE_5__["default"], {
        color: "pure",
        size: "sm",
        style: {
          paddingRight: "15px"
        },
        children: "Click To Get Special Offer"
      })
    }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_7__.jsxs)(_blockStack__WEBPACK_IMPORTED_MODULE_1__["default"], {
      marginTop: 35,
      gap: 10,
      children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_7__.jsx)(_logo__WEBPACK_IMPORTED_MODULE_6__["default"], {}), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_7__.jsx)(_text__WEBPACK_IMPORTED_MODULE_5__["default"], {
        color: "descgray",
        children: "Unlock advanced features:"
      })]
    }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_7__.jsx)(_blockStack__WEBPACK_IMPORTED_MODULE_1__["default"], {
      marginTop: 10,
      gap: 16,
      children: featuresToShow?.map(({
        title
      }, index) => /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_7__.jsxs)(_inlineStack__WEBPACK_IMPORTED_MODULE_0__["default"], {
        gap: 10,
        wrap: false,
        children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_7__.jsx)(_icon__WEBPACK_IMPORTED_MODULE_4__["default"], {
          name: "check",
          color: "primary"
        }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_7__.jsx)(_text__WEBPACK_IMPORTED_MODULE_5__["default"], {
          color: "secondaryblack",
          size: "sm",
          children: title
        })]
      }, index))
    }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_7__.jsxs)(_card__WEBPACK_IMPORTED_MODULE_3__["default"], {
      marginTop: 10,
      padding: 10,
      background: "pro",
      rounded: "md",
      flex: true,
      align: "center",
      blockAlign: "center",
      gap: 10,
      style: {
        cursor: "pointer",
        userSelect: "none"
      },
      onClick: () => window.open(_constants_docs__WEBPACK_IMPORTED_MODULE_2__["default"]?.pricingPage, "_blank", "noopener noreferrer"),
      children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_7__.jsx)(_icon__WEBPACK_IMPORTED_MODULE_4__["default"], {
        name: "upgrade",
        color: "dark",
        fontSize: "xl"
      }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_7__.jsx)(_text__WEBPACK_IMPORTED_MODULE_5__["default"], {
        color: "dark",
        size: "sm",
        children: "Upgrade to Pro"
      })]
    })]
  });
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (PremiumCard);
const FEATURES = [{
  title: "Post Type Folder"
}, {
  title: "Dynamic Folders"
}, {
  title: "Replace Media"
}, {
  title: "Convert To WebP"
}, {
  title: "Watermark For Images"
}, {
  title: "Folder Custom Color"
}, {
  title: "3 Awesome Theme"
}, {
  title: "Show Folder ID"
}, {
  title: "Unused Files"
}];

/***/ },

/***/ "./source/components/status/Status.tsx"
/*!*********************************************!*\
  !*** ./source/components/status/Status.tsx ***!
  \*********************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _utils_utils__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ~/utils/utils */ "./source/utils/utils.ts");
/* harmony import */ var _inlineStack__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../inlineStack */ "./source/components/inlineStack/index.ts");
/* harmony import */ var _wordpress_i18n__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! @wordpress/i18n */ "@wordpress/i18n");
/* harmony import */ var _wordpress_i18n__WEBPACK_IMPORTED_MODULE_2___default = /*#__PURE__*/__webpack_require__.n(_wordpress_i18n__WEBPACK_IMPORTED_MODULE_2__);
/* harmony import */ var _tooltip__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ../tooltip */ "./source/components/tooltip/index.ts");
/* harmony import */ var _card__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ../card */ "./source/components/card/index.ts");
/* harmony import */ var _icon__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ../icon */ "./source/components/icon/index.ts");
/* harmony import */ var clsx__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! clsx */ "./node_modules/clsx/dist/clsx.mjs");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! react/jsx-runtime */ "react/jsx-runtime");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_7___default = /*#__PURE__*/__webpack_require__.n(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_7__);

const Status = ({
  id,
  style,
  className = "",
  isPro = false,
  isComingSoon = false,
  isHot = false,
  isNew = false,
  isBeta = false,
  ownUi = true,
  placement = "default",
  top = 10,
  bottom,
  left,
  right = 10,
  tooltipPlacement = "left",
  tooltipDisabled = false,
  proTooltipDisabled = false,
  size = "medium",
  widthFull = true,
  ignore = false,
  children
}) => {
  const allowFeature = isPro ? (0,_utils_utils__WEBPACK_IMPORTED_MODULE_0__.toBoolean)(pnpnm?.isPro) : true;
  const statusConfig = [{
    key: "pro",
    variant: "pro",
    title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_2__.__)("Premium Only", "ninja-media"),
    icon: "crown",
    iconColor: "dark",
    condition: !allowFeature
  }, {
    key: "comingsoon",
    variant: "warning",
    title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_2__.__)("Coming Soon", "ninja-media"),
    icon: "upcoming",
    iconColor: "pure",
    condition: isComingSoon
  }, {
    key: "hot",
    variant: "error",
    title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_2__.__)("Most Used Feature", "ninja-media"),
    icon: "local_fire_department",
    iconColor: "pure",
    condition: isHot
  }, {
    key: "new",
    variant: "new",
    title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_2__.__)("New Feature", "ninja-media"),
    icon: "campaign",
    iconColor: "primary",
    condition: isNew
  }, {
    key: "beta",
    variant: "light",
    title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_2__.__)("Beta Feature", "ninja-media"),
    icon: "running_with_errors",
    iconColor: "primary",
    condition: isBeta
  }];
  const statusList = statusConfig?.filter(status => status?.condition);
  if (ignore || !isPro && !isComingSoon && !isNew && !isHot && !isBeta) {
    return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_7__.jsx)(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_7__.Fragment, {
      children: children
    });
  }
  const parsePosition = value => value === undefined ? undefined : typeof value === "number" ? `${value}px` : value;
  let positionStyles = {};
  switch (placement) {
    case "center":
      positionStyles = {
        top: "50%",
        left: "50%",
        transform: "translate(-50%, -50%)"
      };
      break;
    case "right-center":
      positionStyles = {
        top: "50%",
        right: parsePosition(right ?? 10),
        transform: "translateY(-50%)"
      };
      break;
    case "left-center":
      positionStyles = {
        top: "50%",
        left: parsePosition(left ?? 10),
        transform: "translateY(-50%)"
      };
      break;
    case "top-center":
      positionStyles = {
        top: parsePosition(top ?? 10),
        left: "50%",
        transform: "translateX(-50%)"
      };
      break;
    case "bottom-center":
      positionStyles = {
        bottom: parsePosition(bottom ?? 10),
        left: "50%",
        transform: "translateX(-50%)"
      };
      break;
    default:
      positionStyles = {
        top: parsePosition(top),
        right: parsePosition(right),
        bottom: parsePosition(bottom),
        left: parsePosition(left)
      };
  }
  const classes = (0,clsx__WEBPACK_IMPORTED_MODULE_6__["default"])("pn-status", widthFull && "w-full", !allowFeature && !proTooltipDisabled && "pn-status--locked", className);
  const content = /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_7__.jsxs)("div", {
    id: id,
    style: {
      ...style
    },
    className: classes,
    onClick: e => {
      if (!allowFeature) {
        e.stopPropagation();
        e.preventDefault();
        return;
      }
      if (isComingSoon) {
        e.stopPropagation();
        e.preventDefault();
        return;
      }
    },
    children: [!allowFeature && !proTooltipDisabled && /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_7__.jsx)("div", {
      className: "pn-status__pro-tooltip",
      children: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_2__.__)("This feature is only available in the pro version", "ninja-media")
    }), ownUi && /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_7__.jsx)(_inlineStack__WEBPACK_IMPORTED_MODULE_1__["default"], {
      wrap: false,
      gap: size === "extrasmall" ? 5 : size === "small" ? 7 : size === "medium" ? 9 : size === "large" ? 11 : 13,
      style: positionStyles,
      className: "pn-status__items",
      children: statusList.map(({
        key,
        variant,
        title,
        icon,
        iconColor
      }) => /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_7__.jsx)(_tooltip__WEBPACK_IMPORTED_MODULE_3__["default"], {
        title: title,
        placement: tooltipPlacement,
        arrow: true,
        wrap: "no-wrap",
        disabled: tooltipDisabled,
        children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_7__.jsx)(_card__WEBPACK_IMPORTED_MODULE_4__["default"], {
          padding: 5,
          rounded: "sm",
          borderStyle: "none",
          background: variant,
          className: (0,clsx__WEBPACK_IMPORTED_MODULE_6__["default"])("pn-status__items-item", `pn-status__items-item--${size}`),
          children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_7__.jsx)(_icon__WEBPACK_IMPORTED_MODULE_5__["default"], {
            name: icon,
            color: iconColor
          })
        })
      }, key))
    }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_7__.jsx)("div", {
      className: (0,clsx__WEBPACK_IMPORTED_MODULE_6__["default"])("pn-status__content", !allowFeature || isComingSoon ? "pn-status__content--disabled" : ""),
      onClick: e => {
        if (!allowFeature) {
          e.stopPropagation();
          e.preventDefault();
          return;
        }
        if (isComingSoon) {
          e.stopPropagation();
          e.preventDefault();
          return;
        }
      },
      children: children
    })]
  });
  return content;
};
Status.Pro = ({
  title,
  tooltipDisabled = false
}) => {
  if ((0,_utils_utils__WEBPACK_IMPORTED_MODULE_0__.toBoolean)(pnpnm?.isPro)) {
    return null;
  }
  return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_7__.jsx)(_tooltip__WEBPACK_IMPORTED_MODULE_3__["default"], {
    title: title || (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_2__.__)("This feature is only available in the pro version", "ninja-media"),
    arrow: true,
    wrap: "no-wrap",
    disabled: tooltipDisabled,
    style: {
      display: "inline-flex",
      alignItems: "center",
      justifyContent: "center"
    },
    children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_7__.jsx)(_icon__WEBPACK_IMPORTED_MODULE_5__["default"], {
      name: "crown",
      color: "primary",
      fontSize: "xl"
    })
  });
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (Status);

/***/ },

/***/ "./source/components/status/Upgrade.tsx"
/*!**********************************************!*\
  !*** ./source/components/status/Upgrade.tsx ***!
  \**********************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _blockStack__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../blockStack */ "./source/components/blockStack/index.ts");
/* harmony import */ var _wordpress_i18n__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! @wordpress/i18n */ "@wordpress/i18n");
/* harmony import */ var _wordpress_i18n__WEBPACK_IMPORTED_MODULE_1___default = /*#__PURE__*/__webpack_require__.n(_wordpress_i18n__WEBPACK_IMPORTED_MODULE_1__);
/* harmony import */ var _constants_docs__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ~/constants/docs */ "./source/constants/docs.ts");
/* harmony import */ var _text__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ../text */ "./source/components/text/index.ts");
/* harmony import */ var _card__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ../card */ "./source/components/card/index.ts");
/* harmony import */ var _icon__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ../icon */ "./source/components/icon/index.ts");
/* harmony import */ var _utils_icons__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! ~/utils/icons */ "./source/utils/icons.ts");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! react/jsx-runtime */ "react/jsx-runtime");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_7___default = /*#__PURE__*/__webpack_require__.n(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_7__);

const Upgrade = () => {
  return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_7__.jsxs)(_blockStack__WEBPACK_IMPORTED_MODULE_0__["default"], {
    gap: 20,
    align: "center",
    inlineAlign: "center",
    children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_7__.jsx)(_blockStack__WEBPACK_IMPORTED_MODULE_0__["default"], {
      align: "center",
      inlineAlign: "center",
      style: {
        width: "150px",
        height: "150px"
      },
      children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_7__.jsx)("img", {
        src: _utils_icons__WEBPACK_IMPORTED_MODULE_6__.iconProIcon,
        alt: "Upgrade to Pro",
        style: {
          width: "100%",
          height: "100%"
        }
      })
    }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_7__.jsx)(_text__WEBPACK_IMPORTED_MODULE_3__["default"], {
      size: "2xl",
      weight: "semibold",
      textTransform: "uppercase",
      align: "center",
      children: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_1__.__)("Upgrade to Pro", "ninja-media")
    }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_7__.jsx)(_text__WEBPACK_IMPORTED_MODULE_3__["default"], {
      color: "descgray",
      align: "center",
      children: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_1__.__)("Upgrade to the Pro version of Ninja Media to unlock all the features and get access to premium support.", "ninja-media")
    }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_7__.jsxs)(_card__WEBPACK_IMPORTED_MODULE_4__["default"], {
      padding: "8px 13px",
      background: "primary",
      flex: true,
      gap: 7,
      style: {
        width: "fit-content",
        borderRadius: "6px",
        cursor: "pointer"
      },
      onClick: () => window.open(_constants_docs__WEBPACK_IMPORTED_MODULE_2__["default"]?.pricingPage, "_blank", "noreferrer"),
      children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_7__.jsx)(_icon__WEBPACK_IMPORTED_MODULE_5__["default"], {
        color: "pure",
        name: "crown",
        fontSize: "lg"
      }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_7__.jsx)(_text__WEBPACK_IMPORTED_MODULE_3__["default"], {
        color: "pure",
        size: "sm",
        children: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_1__.__)("Upgrade to pro", "ninja-media")
      })]
    })]
  });
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (Upgrade);

/***/ },

/***/ "./source/components/status/index.ts"
/*!*******************************************!*\
  !*** ./source/components/status/index.ts ***!
  \*******************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (/* reexport safe */ _Status__WEBPACK_IMPORTED_MODULE_0__["default"])
/* harmony export */ });
/* harmony import */ var _Status__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./Status */ "./source/components/status/Status.tsx");

/***/ },

/***/ "./source/components/svgIcon/SvgIcon.tsx"
/*!***********************************************!*\
  !*** ./source/components/svgIcon/SvgIcon.tsx ***!
  \***********************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _wordpress_element__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @wordpress/element */ "@wordpress/element");
/* harmony import */ var _wordpress_element__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(_wordpress_element__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! react/jsx-runtime */ "react/jsx-runtime");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_1___default = /*#__PURE__*/__webpack_require__.n(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_1__);

const cache = new Map();
const SvgIcon = ({
  src,
  style,
  className
}) => {
  const [svg, setSvg] = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useState)(cache.get(src) ?? "");
  (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useEffect)(() => {
    if (cache.has(src)) {
      setSvg(cache.get(src));
      return;
    }
    fetch(src).then(r => r.text()).then(content => {
      cache.set(src, content);
      setSvg(content);
    });
  }, [src]);
  return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_1__.jsx)("span", {
    className: className,
    style: {
      display: "inline-flex",
      ...style
    },
    dangerouslySetInnerHTML: {
      __html: svg
    }
  });
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (SvgIcon);

/***/ },

/***/ "./source/components/switcher/Switcher.tsx"
/*!*************************************************!*\
  !*** ./source/components/switcher/Switcher.tsx ***!
  \*************************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _inlineStack__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../inlineStack */ "./source/components/inlineStack/index.ts");
/* harmony import */ var _status__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../status */ "./source/components/status/index.ts");
/* harmony import */ var _text__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../text */ "./source/components/text/index.ts");
/* harmony import */ var clsx__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! clsx */ "./node_modules/clsx/dist/clsx.mjs");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! react/jsx-runtime */ "react/jsx-runtime");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_4___default = /*#__PURE__*/__webpack_require__.n(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_4__);

const Switcher = ({
  id = "",
  style,
  className = "",
  checked,
  title,
  titleSize = "md",
  tabIndex,
  ariaLabel,
  loading = false,
  disabled,
  isPro = false,
  onChange
}) => {
  const handleChange = isChecked => {
    if (disabled || loading) return;
    if (onChange) onChange(isChecked);
  };
  const classes = (0,clsx__WEBPACK_IMPORTED_MODULE_3__["default"])("pn-switcher", disabled && "pn-switcher--disabled", className);
  return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_4__.jsxs)("div", {
    id: id,
    style: {
      ...style,
      display: "flex",
      alignItems: "center",
      gap: "0.75rem",
      userSelect: "none"
    },
    tabIndex: tabIndex,
    "aria-label": ariaLabel,
    className: classes,
    children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_4__.jsx)("div", {
      className: (0,clsx__WEBPACK_IMPORTED_MODULE_3__["default"])("pn-switcher__toggle", checked && "pn-switcher__toggle--active", loading && "pn-switcher__toggle--loading"),
      onClick: () => handleChange(!checked),
      children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_4__.jsx)("div", {
        className: "pn-switcher__toggle-thumb"
      })
    }), title && (isPro ? /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_4__.jsxs)(_inlineStack__WEBPACK_IMPORTED_MODULE_0__["default"], {
      gap: 10,
      wrap: false,
      children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_4__.jsx)(_text__WEBPACK_IMPORTED_MODULE_2__["default"], {
        size: titleSize,
        weight: "medium",
        className: "cursor-pointer",
        onClick: () => handleChange(!checked),
        children: title
      }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_4__.jsx)(_status__WEBPACK_IMPORTED_MODULE_1__["default"].Pro, {
        tooltipDisabled: true
      })]
    }) : /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_4__.jsx)(_text__WEBPACK_IMPORTED_MODULE_2__["default"], {
      size: titleSize,
      weight: "medium",
      className: "cursor-pointer",
      onClick: () => handleChange(!checked),
      children: title
    }))]
  });
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (Switcher);

/***/ },

/***/ "./source/components/switcher/index.ts"
/*!*********************************************!*\
  !*** ./source/components/switcher/index.ts ***!
  \*********************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (/* reexport safe */ _Switcher__WEBPACK_IMPORTED_MODULE_0__["default"])
/* harmony export */ });
/* harmony import */ var _Switcher__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./Switcher */ "./source/components/switcher/Switcher.tsx");

/***/ },

/***/ "./source/components/text/Text.tsx"
/*!*****************************************!*\
  !*** ./source/components/text/Text.tsx ***!
  \*****************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var clsx__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! clsx */ "./node_modules/clsx/dist/clsx.mjs");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! react/jsx-runtime */ "react/jsx-runtime");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_1___default = /*#__PURE__*/__webpack_require__.n(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_1__);

const Text = ({
  id,
  style,
  className = "",
  as: Component = "span",
  color = "black",
  size = "md",
  textTransform = "none",
  align = "left",
  weight = "normal",
  wrap = true,
  ellipsis = false,
  ellipsisLine,
  children,
  onClick
}) => {
  const renderContent = content => {
    if (typeof content === "string") {
      const parts = content.split(/(\{.*?\})/g);
      return parts.map((part, index) => {
        if (/^\{.*\}$/.test(part)) {
          return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_1__.jsx)("strong", {
            children: part.replace(/[{}]/g, "")
          }, index);
        }
        return part;
      });
    }
    return content;
  };
  const isMultiLine = ellipsis && ellipsisLine && ellipsisLine > 1;
  const classes = (0,clsx__WEBPACK_IMPORTED_MODULE_0__["default"])(`text-${color}`, `text-${size}`, `font-${weight}`, `text-${textTransform}`, `text-${align}`, wrap ? "text-wrap" : "text-nowrap", ellipsis && "text-ellipsis", ellipsis && (isMultiLine ? "multi" : "single"), className);
  const ellipsisStyle = isMultiLine ? {
    WebkitLineClamp: ellipsisLine
  } : {};
  return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_1__.jsx)(Component, {
    id: id,
    style: {
      ...style,
      ...ellipsisStyle
    },
    className: classes,
    onClick: onClick,
    children: renderContent(children)
  });
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (Text);

/***/ },

/***/ "./source/components/text/index.ts"
/*!*****************************************!*\
  !*** ./source/components/text/index.ts ***!
  \*****************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (/* reexport safe */ _Text__WEBPACK_IMPORTED_MODULE_0__["default"])
/* harmony export */ });
/* harmony import */ var _Text__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./Text */ "./source/components/text/Text.tsx");

/***/ },

/***/ "./source/components/toggleTheme/ToggleTheme.tsx"
/*!*******************************************************!*\
  !*** ./source/components/toggleTheme/ToggleTheme.tsx ***!
  \*******************************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _wordpress_element__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @wordpress/element */ "@wordpress/element");
/* harmony import */ var _wordpress_element__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(_wordpress_element__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var _wordpress_i18n__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! @wordpress/i18n */ "@wordpress/i18n");
/* harmony import */ var _wordpress_i18n__WEBPACK_IMPORTED_MODULE_1___default = /*#__PURE__*/__webpack_require__.n(_wordpress_i18n__WEBPACK_IMPORTED_MODULE_1__);
/* harmony import */ var _button__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../button */ "./source/components/button/index.ts");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! react/jsx-runtime */ "react/jsx-runtime");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3___default = /*#__PURE__*/__webpack_require__.n(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__);

const ToggleTheme = ({
  theme,
  setTheme
}) => {
  (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useEffect)(() => {
    const root = document?.documentElement;
    root?.setAttribute("pnpnm-theme-status", theme);
  }, [theme]);
  return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_3__.jsx)(_button__WEBPACK_IMPORTED_MODULE_2__["default"], {
    variant: theme === "light" ? "outlined" : "primary",
    startIcon: theme === "light" ? "dark_mode" : "light_mode",
    onClick: () => setTheme(theme === "light" ? "dark" : "light"),
    children: theme === "light" ? (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_1__.__)("Dark Mode", "ninja-media") : (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_1__.__)("Light Mode", "ninja-media")
  });
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (ToggleTheme);

/***/ },

/***/ "./source/components/toggleTheme/index.ts"
/*!************************************************!*\
  !*** ./source/components/toggleTheme/index.ts ***!
  \************************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (/* reexport safe */ _ToggleTheme__WEBPACK_IMPORTED_MODULE_0__["default"])
/* harmony export */ });
/* harmony import */ var _ToggleTheme__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./ToggleTheme */ "./source/components/toggleTheme/ToggleTheme.tsx");

/***/ },

/***/ "./source/components/tooltip/Tooltip.tsx"
/*!***********************************************!*\
  !*** ./source/components/tooltip/Tooltip.tsx ***!
  \***********************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _wordpress_element__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @wordpress/element */ "@wordpress/element");
/* harmony import */ var _wordpress_element__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(_wordpress_element__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var clsx__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! clsx */ "./node_modules/clsx/dist/clsx.mjs");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! react/jsx-runtime */ "react/jsx-runtime");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_2___default = /*#__PURE__*/__webpack_require__.n(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_2__);

const Tooltip = ({
  title = "Tooltip",
  component,
  style,
  className,
  placement = "top",
  arrow = false,
  arrowSize = 5,
  openStyle = "fade",
  trigger = "hover",
  width = "auto",
  wrap = "wrap",
  background = "secondaryblack",
  color = "white",
  border,
  shadow = false,
  visible = true,
  disabled = false,
  children
}) => {
  const [actualPlacement, setActualPlacement] = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useState)(placement);
  const [isVisible, setIsVisible] = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useState)(false);
  const tooltipRef = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useRef)(null);
  const triggerRef = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useRef)(null);
  (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useEffect)(() => {
    const handleClickOutside = event => {
      if (trigger === "click" && tooltipRef.current && !tooltipRef.current.contains(event.target) && triggerRef.current && !triggerRef.current.contains(event.target)) {
        setIsVisible(false);
      }
    };
    document.addEventListener("mousedown", handleClickOutside);
    return () => document.removeEventListener("mousedown", handleClickOutside);
  }, [trigger]);
  (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useEffect)(() => {
    if (placement !== "auto" || !isVisible || !tooltipRef.current || !triggerRef.current) return;
    const triggerBox = triggerRef.current.getBoundingClientRect();
    const windowWidth = window.innerWidth;
    const windowHeight = window.innerHeight;
    const spaces = {
      top: triggerBox.top,
      bottom: windowHeight - triggerBox.bottom,
      left: triggerBox.left,
      right: windowWidth - triggerBox.right
    };
    const bestSide = Object.entries(spaces).sort((a, b) => b[1] - a[1])[0][0];
    setActualPlacement(bestSide);
  }, [isVisible, placement]);
  const wrapperClasses = (0,clsx__WEBPACK_IMPORTED_MODULE_1__["default"])("pn-tooltip", isVisible && "pn-tooltip--show", placement === "auto" ? `pn-tooltip--${actualPlacement}` : `pn-tooltip--${placement}`);
  const textClasses = (0,clsx__WEBPACK_IMPORTED_MODULE_1__["default"])("pn-tooltip__text", arrow && "pn-tooltip__text--has-arrow", `pn-tooltip__text--open-${openStyle}`, `pn-tooltip__text--wrap-${wrap}`, `text-${color}`, `bg-${background}`, border && `border border-solid border-${border}`, shadow && "pn-tooltip__text--shadow");
  if (disabled) return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_2__.jsx)(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_2__.Fragment, {
    children: children
  });
  return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_2__.jsxs)("div", {
    ref: triggerRef,
    style: style,
    className: (0,clsx__WEBPACK_IMPORTED_MODULE_1__["default"])(wrapperClasses, className),
    onMouseEnter: trigger === "hover" ? () => setIsVisible(visible ? true : false) : undefined,
    onMouseLeave: trigger === "hover" ? () => setIsVisible(false) : undefined,
    onClick: trigger === "click" ? () => setIsVisible(prev => visible ? !prev : false) : undefined,
    children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_2__.jsx)("div", {
      ref: tooltipRef,
      className: textClasses,
      style: {
        "--pn-tooltip-width": width,
        "--pn-tooltip-arrow-size": `${arrowSize}px`,
        "--pn-tooltip-arrow-color": `var(--pnpnm-${background})`
      },
      children: component ? component : title
    }), children]
  });
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (Tooltip);

/***/ },

/***/ "./source/components/tooltip/index.ts"
/*!********************************************!*\
  !*** ./source/components/tooltip/index.ts ***!
  \********************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (/* reexport safe */ _Tooltip__WEBPACK_IMPORTED_MODULE_0__["default"])
/* harmony export */ });
/* harmony import */ var _Tooltip__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./Tooltip */ "./source/components/tooltip/Tooltip.tsx");

/***/ },

/***/ "./source/components/topbar/Topbar.tsx"
/*!*********************************************!*\
  !*** ./source/components/topbar/Topbar.tsx ***!
  \*********************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _wordpress_element__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @wordpress/element */ "@wordpress/element");
/* harmony import */ var _wordpress_element__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(_wordpress_element__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var _utils_utils__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ~/utils/utils */ "./source/utils/utils.ts");
/* harmony import */ var _inlineStack__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../inlineStack */ "./source/components/inlineStack/index.ts");
/* harmony import */ var clsx__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! clsx */ "./node_modules/clsx/dist/clsx.mjs");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! react/jsx-runtime */ "react/jsx-runtime");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_4___default = /*#__PURE__*/__webpack_require__.n(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_4__);

const Topbar = ({
  id,
  style,
  className = "",
  padding = 20,
  leftContents = [],
  rightContents = []
}) => {
  const renderContent = (item, index) => {
    if (!(0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.isValidElement)(item)) return null;
    return (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.cloneElement)(item, {
      key: item.key ?? index
    });
  };
  return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_4__.jsxs)(_inlineStack__WEBPACK_IMPORTED_MODULE_2__["default"], {
    id: id,
    gap: 10,
    align: "between",
    style: {
      ...style,
      padding
    },
    className: (0,clsx__WEBPACK_IMPORTED_MODULE_3__["default"])("pn-topbar", className),
    children: [(0,_utils_utils__WEBPACK_IMPORTED_MODULE_1__.isValidArray)(leftContents) && /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_4__.jsx)(_inlineStack__WEBPACK_IMPORTED_MODULE_2__["default"], {
      gap: 10,
      className: "flex-1",
      children: leftContents.map(renderContent)
    }), (0,_utils_utils__WEBPACK_IMPORTED_MODULE_1__.isValidArray)(rightContents) && /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_4__.jsx)(_inlineStack__WEBPACK_IMPORTED_MODULE_2__["default"], {
      gap: 10,
      children: rightContents.map(renderContent)
    })]
  });
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (Topbar);

/***/ },

/***/ "./source/components/topbar/index.ts"
/*!*******************************************!*\
  !*** ./source/components/topbar/index.ts ***!
  \*******************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (/* reexport safe */ _Topbar__WEBPACK_IMPORTED_MODULE_0__["default"])
/* harmony export */ });
/* harmony import */ var _Topbar__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./Topbar */ "./source/components/topbar/Topbar.tsx");

/***/ },

/***/ "./source/constants/docs.ts"
/*!**********************************!*\
  !*** ./source/constants/docs.ts ***!
  \**********************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
const DOCS = {
  supportLink: "https://plugininja.com/support-portal",
  featuresPage: "https://plugininja.com/ninja-media",
  pricingPage: "https://plugininja.com/ninja-media-pricing",
  documentation: {
    main: "https://plugininja.com/docs-category/ninja-media"
  }
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (DOCS);

/***/ },

/***/ "./source/constants/files.ts"
/*!***********************************!*\
  !*** ./source/constants/files.ts ***!
  \***********************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   PAGE_OPTIONS: () => (/* binding */ PAGE_OPTIONS)
/* harmony export */ });
/* harmony import */ var _wordpress_i18n__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @wordpress/i18n */ "@wordpress/i18n");
/* harmony import */ var _wordpress_i18n__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(_wordpress_i18n__WEBPACK_IMPORTED_MODULE_0__);

const PAGE_OPTIONS = [{
  name: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_0__.__)("10/Page", "ninja-media"),
  value: "10"
}, {
  name: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_0__.__)("20/Page", "ninja-media"),
  value: "20"
}, {
  name: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_0__.__)("30/Page", "ninja-media"),
  value: "30"
}, {
  name: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_0__.__)("50/Page", "ninja-media"),
  value: "50"
}, {
  name: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_0__.__)("80/Page", "ninja-media"),
  value: "80"
}, {
  name: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_0__.__)("100/Page", "ninja-media"),
  value: "100"
}];

/***/ },

/***/ "./source/constants/menus.ts"
/*!***********************************!*\
  !*** ./source/constants/menus.ts ***!
  \***********************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   FILES_MENUS: () => (/* binding */ FILES_MENUS),
/* harmony export */   SETTINGS_MENUS: () => (/* binding */ SETTINGS_MENUS),
/* harmony export */   WATERMARK_MENUS: () => (/* binding */ WATERMARK_MENUS)
/* harmony export */ });
/* harmony import */ var _wordpress_i18n__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @wordpress/i18n */ "@wordpress/i18n");
/* harmony import */ var _wordpress_i18n__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(_wordpress_i18n__WEBPACK_IMPORTED_MODULE_0__);

const SETTINGS_MENUS = [{
  key: "general",
  title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_0__.__)("General", "ninja-media"),
  icon: "settings"
}, {
  key: "display",
  title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_0__.__)("Display", "ninja-media"),
  icon: "auto_awesome_mosaic"
}, {
  key: "advanced",
  title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_0__.__)("Advanced", "ninja-media"),
  icon: "instant_mix"
}, {
  key: "tools",
  title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_0__.__)("Tools", "ninja-media"),
  icon: "build"
}];
const FILES_MENUS = [{
  key: "all",
  title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_0__.__)("All Files", "ninja-media"),
  icon: "collections"
}, {
  key: "uncategorized",
  title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_0__.__)("Uncategorized", "ninja-media"),
  icon: "image"
}, {
  key: "dynamic",
  title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_0__.__)("Dynamic Folders", "ninja-media"),
  icon: "dynamic_feed"
}, {
  key: "favorites",
  title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_0__.__)("Favorites", "ninja-media"),
  icon: "favorite"
}, {
  key: "used",
  title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_0__.__)("Used Files", "ninja-media"),
  icon: "gallery_thumbnail"
}, {
  key: "unused",
  title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_0__.__)("Unused Files", "ninja-media"),
  icon: "dangerous"
}];
const WATERMARK_MENUS = [{
  key: "text",
  title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_0__.__)("Text", "ninja-media"),
  icon: "art_track"
}, {
  key: "image",
  title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_0__.__)("Image", "ninja-media"),
  icon: "gallery_thumbnail"
}];

/***/ },

/***/ "./source/hooks/useDebounce.ts"
/*!*************************************!*\
  !*** ./source/hooks/useDebounce.ts ***!
  \*************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (/* binding */ useDebounce)
/* harmony export */ });
/* harmony import */ var _wordpress_element__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @wordpress/element */ "@wordpress/element");
/* harmony import */ var _wordpress_element__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(_wordpress_element__WEBPACK_IMPORTED_MODULE_0__);

function useDebounce(effect, dependencies, delay) {
  const callback = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useCallback)(effect, dependencies);
  (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useEffect)(() => {
    const timeout = setTimeout(callback, delay);
    return () => clearTimeout(timeout);
  }, [callback, delay]);
}

/***/ },

/***/ "./source/hooks/useEventListener.ts"
/*!******************************************!*\
  !*** ./source/hooks/useEventListener.ts ***!
  \******************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   useEventListener: () => (/* binding */ useEventListener)
/* harmony export */ });
/* harmony import */ var _wordpress_element__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @wordpress/element */ "@wordpress/element");
/* harmony import */ var _wordpress_element__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(_wordpress_element__WEBPACK_IMPORTED_MODULE_0__);

const useEventListener = (eventName, handler, element, options) => {
  const savedHandler = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useRef)(handler);
  (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useEffect)(() => {
    savedHandler.current = handler;
  }, [handler]);
  const eventListener = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useCallback)(event => {
    if (typeof options !== "object" || !options.selector) {
      savedHandler.current(event);
      return;
    }
    const target = event.target;
    if (target && target.closest) {
      const delegateTarget = target.closest(options.selector);
      if (delegateTarget) {
        savedHandler.current(event, delegateTarget);
      }
    }
  }, [options]);
  (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useEffect)(() => {
    const targetElement = element ?? window;
    if (!targetElement || !("addEventListener" in targetElement)) {
      console.warn(`Cannot attach ${eventName} listener: Invalid target element`);
      return;
    }
    let eventOptions = options;
    if (typeof options === "object") {
      const {
        selector,
        ...standardOptions
      } = options;
      eventOptions = standardOptions;
    }
    targetElement.addEventListener(eventName, eventListener, eventOptions);
    return () => {
      targetElement.removeEventListener(eventName, eventListener, eventOptions);
    };
  }, [eventName, element, eventListener, options]);
};

/***/ },

/***/ "./source/hooks/useLocalStorage.ts"
/*!*****************************************!*\
  !*** ./source/hooks/useLocalStorage.ts ***!
  \*****************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   useLocalStorage: () => (/* binding */ useLocalStorage)
/* harmony export */ });
/* harmony import */ var _wordpress_element__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @wordpress/element */ "@wordpress/element");
/* harmony import */ var _wordpress_element__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(_wordpress_element__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var _useEventListener__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./useEventListener */ "./source/hooks/useEventListener.ts");

function useLocalStorage(key, initialValue = undefined, options = {}) {
  const {
    initializeWithValue = true,
    serializer = JSON.stringify,
    deserializer = JSON.parse,
    onError = (error, key) => console.warn(`Error with localStorage key "${key}": ${error.message}`)
  } = options;
  const isLocalStorageAvailable = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useCallback)(() => {
    return typeof window !== "undefined" && !!window.localStorage;
  }, []);
  const getInitialValue = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useCallback)(() => {
    if (typeof initialValue === "function") {
      return initialValue();
    }
    return initialValue;
  }, [initialValue]);
  const readValueFromStorage = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useCallback)(() => {
    if (!isLocalStorageAvailable()) {
      return getInitialValue();
    }
    try {
      const storedItem = window.localStorage.getItem(key);
      if (storedItem === null) {
        return getInitialValue();
      }
      return deserializer(storedItem);
    } catch (error) {
      const errorObj = error instanceof Error ? error : new Error("Unknown error");
      onError(errorObj, key);
      return getInitialValue();
    }
  }, [key, deserializer, onError, isLocalStorageAvailable, getInitialValue]);
  const [storedValue, setStoredValue] = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useState)(() => {
    if (initializeWithValue) {
      return readValueFromStorage();
    }
    return getInitialValue();
  });
  const setValue = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useCallback)(value => {
    if (!isLocalStorageAvailable()) {
      onError(new Error("localStorage not available"), key);
      return;
    }
    try {
      const newValue = typeof value === "function" ? value(storedValue) : value;
      window.localStorage.setItem(key, serializer(newValue));
      setStoredValue(newValue);
    } catch (error) {
      const errorObj = error instanceof Error ? error : new Error("Unknown error");
      onError(errorObj, key);
    }
  }, [key, storedValue, serializer, onError, isLocalStorageAvailable]);
  const removeValue = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useCallback)(() => {
    if (isLocalStorageAvailable()) {
      window.localStorage.removeItem(key);
    }
    setStoredValue(getInitialValue());
  }, [key, isLocalStorageAvailable, getInitialValue]);
  const handleStorageChange = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useCallback)(event => {
    if (event.key === key) {
      setStoredValue(readValueFromStorage());
    }
  }, [key, readValueFromStorage]);
  (0,_useEventListener__WEBPACK_IMPORTED_MODULE_1__.useEventListener)("storage", handleStorageChange, typeof window !== "undefined" ? window : null);
  return [storedValue, setValue, removeValue];
}

/***/ },

/***/ "./source/hooks/useSettings.ts"
/*!*************************************!*\
  !*** ./source/hooks/useSettings.ts ***!
  \*************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _redux_api_settings__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ~/redux/api/settings */ "./source/redux/api/settings.ts");
/* harmony import */ var _redux_hooks__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ~/redux/hooks */ "./source/redux/hooks.ts");
/* harmony import */ var _components_alert_Alert__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ~/components/alert/Alert */ "./source/components/alert/Alert.tsx");
/* harmony import */ var _wordpress_element__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! @wordpress/element */ "@wordpress/element");
/* harmony import */ var _wordpress_element__WEBPACK_IMPORTED_MODULE_3___default = /*#__PURE__*/__webpack_require__.n(_wordpress_element__WEBPACK_IMPORTED_MODULE_3__);
/* harmony import */ var _utils_functions__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ~/utils/functions */ "./source/utils/functions.ts");
/* harmony import */ var _redux_features_settings_settings__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ~/redux/features/settings/settings */ "./source/redux/features/settings/settings.ts");

const useSettings = () => {
  const {
    data,
    defaultData,
    draft
  } = (0,_redux_hooks__WEBPACK_IMPORTED_MODULE_1__.useAppSelector)(_redux_features_settings_settings__WEBPACK_IMPORTED_MODULE_5__.selectSettings);
  const [isDataChanged, setIsDataChanged] = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_3__.useState)(false);
  const [updateSettingsData] = (0,_redux_api_settings__WEBPACK_IMPORTED_MODULE_0__.useUpdateSettingsMutation)();
  const dispatch = (0,_redux_hooks__WEBPACK_IMPORTED_MODULE_1__.useAppDispatch)();
  const {
    showAlert
  } = (0,_components_alert_Alert__WEBPACK_IMPORTED_MODULE_2__.useCustomAlert)();
  (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_3__.useEffect)(() => {
    if (data && draft) {
      setIsDataChanged(!(0,_utils_functions__WEBPACK_IMPORTED_MODULE_4__.objectEqual)(data, draft));
    } else {
      setIsDataChanged(false);
    }
  }, [data, draft]);
  const saveSettings = async propsData => {
    if (!data) return;
    try {
      const response = await updateSettingsData(propsData || data).unwrap();
      dispatch((0,_redux_features_settings_settings__WEBPACK_IMPORTED_MODULE_5__.setDraft)(propsData || data));
      showAlert({
        toast: true,
        type: "success",
        text: response?.message || "Settings saved successfully",
        timer: 3000,
        timerProgressBar: true,
        showConfirmButton: false
      });
    } catch (error) {
      showAlert({
        toast: true,
        type: "error",
        text: error?.data?.message || "Failed to save settings",
        timer: 3000,
        timerProgressBar: true,
        showConfirmButton: false
      });
    }
  };
  const setSettings = (path, value) => {
    dispatch((0,_redux_features_settings_settings__WEBPACK_IMPORTED_MODULE_5__.updateSettings)({
      path,
      value
    }));
  };
  return {
    data,
    defaultData,
    draft,
    isDataChanged,
    setSettings,
    saveSettings
  };
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (useSettings);

/***/ },

/***/ "./source/media-library/context/FileContext.tsx"
/*!******************************************************!*\
  !*** ./source/media-library/context/FileContext.tsx ***!
  \******************************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   FILE_CONTEXT_MENU_LISTS: () => (/* binding */ FILE_CONTEXT_MENU_LISTS),
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _components_contextMenu_ContextMenu__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ~/components/contextMenu/ContextMenu */ "./source/components/contextMenu/ContextMenu.tsx");
/* harmony import */ var _hooks_useSettings__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ~/hooks/useSettings */ "./source/hooks/useSettings.ts");
/* harmony import */ var _components_status__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ~/components/status */ "./source/components/status/index.ts");
/* harmony import */ var _hooks_useMedia__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ../hooks/useMedia */ "./source/media-library/hooks/useMedia.ts");
/* harmony import */ var _components_icon__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ~/components/icon */ "./source/components/icon/index.ts");
/* harmony import */ var _wordpress_i18n__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! @wordpress/i18n */ "@wordpress/i18n");
/* harmony import */ var _wordpress_i18n__WEBPACK_IMPORTED_MODULE_5___default = /*#__PURE__*/__webpack_require__.n(_wordpress_i18n__WEBPACK_IMPORTED_MODULE_5__);
/* harmony import */ var _components_card__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! ~/components/card */ "./source/components/card/index.ts");
/* harmony import */ var _components_text__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! ~/components/text */ "./source/components/text/index.ts");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_8__ = __webpack_require__(/*! react/jsx-runtime */ "react/jsx-runtime");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_8___default = /*#__PURE__*/__webpack_require__.n(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_8__);

const FileContext = ({
  onMenuClick
}) => {
  const {
    data
  } = (0,_hooks_useSettings__WEBPACK_IMPORTED_MODULE_1__["default"])();
  const {
    menu
  } = (0,_hooks_useMedia__WEBPACK_IMPORTED_MODULE_3__["default"])();
  const enableTrash = data?.general?.files?.moveToTrash ?? false;

  return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_8__.jsx)(_components_contextMenu_ContextMenu__WEBPACK_IMPORTED_MODULE_0__.Menu, {
    id: "file-menu",
    style: {
      width: "220px"
    },
    children: ({
      props
    }) => {
      const FILTERED_MENUS = FILE_CONTEXT_MENU_LISTS?.filter(item => {
        if (item?.key === "open") {
          return false;
        } else if (item?.key === "view") {
          return false;
        } else if (item?.key === "get") {
          return true;
        } else if (item?.key === "edit") {
          if (menu === "trash") return false;
          return props?.attachments?.[0]?.type === "image";
        } else if (item?.key === "download") {
          return true;
        } else if (item?.key === "duplicate") {
          return menu === "trash" ? false : true;
        } else if (item?.key === "replace") {
          return menu === "trash" ? false : true;
        } else if (item?.key === "favorite") {
          if (menu === "trash") return false;
          if (props?.attachments?.length === 1) {
            return !props.attachments[0]?.isFavorite;
          }
          return true;
        } else if (item?.key === "unfavorite") {
          if (menu === "trash") return false;
          if (props?.attachments?.length === 1) {
            return !!props.attachments[0]?.isFavorite;
          }
          return false;
        } else if (item?.key === "apply") {
          return false;
        } else if (item?.key === "remove") {
          return false;
        } else if (item.key === "trash") {
          return menu === "trash" ? false : enableTrash ? true : false;
        } else if (item.key === "restore") {
          return menu === "trash" ? true : false;
        } else if (item.key === "delete") {
          return !enableTrash ? true : menu === "trash" ? true : false;
        }
      });
      return FILTERED_MENUS?.map(({
        key,
        title,
        icon,
        className,
        statusProps
      }, index) => {
        return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_8__.jsxs)("div", {
          children: [["trash", "delete"].includes(key) && /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_8__.jsx)(_components_contextMenu_ContextMenu__WEBPACK_IMPORTED_MODULE_0__.Separator, {}), ["trash", "delete"].includes(key) ? /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_8__.jsxs)(_components_card__WEBPACK_IMPORTED_MODULE_6__["default"], {
            statusProps: {
              isPro: key === "trash",
              proTooltipDisabled: true,
              size: "extrasmall",
              top: 7.5,
              right: 7,
              style: {
                height: "fit-content"
              }
            },
            margin: key === "trash" ? "5px 5px 5px 5px" : "8px 5px 5px 5px",
            padding: 5,
            background: "errorextralight",
            border: "errorextralight",
            rounded: "sm",
            flex: true,
            direction: "row",
            blockAlign: "center",
            gap: 7,
            style: {
              cursor: "pointer",
              width: "calc(100% - 10px)"
            },
            className: "hover-errorlight",
            onClick: () => onMenuClick({
              key,
              attachments: props?.attachments
            }),
            children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_8__.jsx)(_components_icon__WEBPACK_IMPORTED_MODULE_4__["default"], {
              name: icon,
              fontSize: "lg",
              color: "error"
            }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_8__.jsx)(_components_text__WEBPACK_IMPORTED_MODULE_7__["default"], {
              color: "error",
              size: "sm",
              children: key === "delete" && menu === "trash" ? (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_5__.__)("Delete Permanently", "ninja-media") : title
            })]
          }) : /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_8__.jsx)(_components_status__WEBPACK_IMPORTED_MODULE_2__["default"], {
            ...statusProps,
            size: "extrasmall",
            placement: "right-center",
            right: 5,
            proTooltipDisabled: true,
            children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_8__.jsxs)(_components_contextMenu_ContextMenu__WEBPACK_IMPORTED_MODULE_0__.Item, {
              className: className,
              onClick: () => onMenuClick({
                key,
                attachments: props?.attachments
              }),
              children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_8__.jsx)(_components_icon__WEBPACK_IMPORTED_MODULE_4__["default"], {
                name: icon
              }), title]
            })
          })]
        }, key ?? index);
      });
    }
  });
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (FileContext);
const FILE_CONTEXT_MENU_LISTS = [{
  key: "open",
  title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_5__.__)("Open in Media Library", "ninja-media"),
  icon: "photo_library"
}, {
  key: "edit",
  title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_5__.__)("Image Editor", "ninja-media"),
  icon: "edit_document"
}, {
  key: "get",
  title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_5__.__)("Get link", "ninja-media"),
  icon: "link"
}, {
  key: "view",
  title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_5__.__)("View details", "ninja-media"),
  icon: "art_track"
}, {
  key: "download",
  title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_5__.__)("Download", "ninja-media"),
  icon: "download",
  statusProps: {
    isPro: true
  }
}, {
  key: "duplicate",
  title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_5__.__)("Duplicate", "ninja-media"),
  icon: "file_copy",
  statusProps: {
    isPro: true
  }
}, {
  key: "replace",
  title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_5__.__)("Replace", "ninja-media"),
  icon: "replace_image",
  statusProps: {
    isPro: true
  }
}, {
  key: "favorite",
  title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_5__.__)("Add to favorites", "ninja-media"),
  icon: "favorite",
  statusProps: {
    isPro: true
  }
}, {
  key: "unfavorite",
  title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_5__.__)("Remove from favorites", "ninja-media"),
  icon: "heart_broken",
  statusProps: {
    isPro: true
  }
}, {
  key: "apply",
  title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_5__.__)("Apply watermark", "ninja-media"),
  icon: "water",
  statusProps: {
    isPro: true
  }
}, {
  key: "remove",
  title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_5__.__)("Remove watermark", "ninja-media"),
  icon: "block",
  statusProps: {
    isPro: true
  }
}, {
  key: "trash",
  title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_5__.__)("Trash", "ninja-media"),
  icon: "recycling",
  className: "destructive",
  statusProps: {
    isPro: true
  }
}, {
  key: "restore",
  title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_5__.__)("Restore", "ninja-media"),
  icon: "restore"
}, {
  key: "delete",
  title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_5__.__)("Delete", "ninja-media"),
  icon: "delete",
  className: "destructive"
}];

/***/ },

/***/ "./source/media-library/hooks/useMedia.ts"
/*!************************************************!*\
  !*** ./source/media-library/hooks/useMedia.ts ***!
  \************************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _redux_features_media_media__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ~/redux/features/media/media */ "./source/redux/features/media/media.ts");
/* harmony import */ var _redux_hooks__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ~/redux/hooks */ "./source/redux/hooks.ts");
/* harmony import */ var _wordpress_element__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! @wordpress/element */ "@wordpress/element");
/* harmony import */ var _wordpress_element__WEBPACK_IMPORTED_MODULE_2___default = /*#__PURE__*/__webpack_require__.n(_wordpress_element__WEBPACK_IMPORTED_MODULE_2__);

const useMedia = () => {
  const media = (0,_redux_hooks__WEBPACK_IMPORTED_MODULE_1__.useAppSelector)(_redux_features_media_media__WEBPACK_IMPORTED_MODULE_0__.selectMedia);
  const dispatch = (0,_redux_hooks__WEBPACK_IMPORTED_MODULE_1__.useAppDispatch)();
  const setMedia = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_2__.useCallback)((path, value) => {
    dispatch((0,_redux_features_media_media__WEBPACK_IMPORTED_MODULE_0__.updateMedia)({
      path,
      value
    }));
  }, []);
  return {
    ...media,
    setMedia
  };
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (useMedia);

/***/ },

/***/ "./source/redux/api/base.ts"
/*!**********************************!*\
  !*** ./source/redux/api/base.ts ***!
  \**********************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   baseApi: () => (/* binding */ baseApi),
/* harmony export */   wpBaseQuery: () => (/* binding */ wpBaseQuery)
/* harmony export */ });
/* harmony import */ var _reduxjs_toolkit_query_react__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @reduxjs/toolkit/query/react */ "./node_modules/@reduxjs/toolkit/dist/query/react/rtk-query-react.modern.mjs");
/* harmony import */ var _reduxjs_toolkit_query_react__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! @reduxjs/toolkit/query/react */ "./node_modules/@reduxjs/toolkit/dist/query/rtk-query.modern.mjs");

const rawBaseQuery = (0,_reduxjs_toolkit_query_react__WEBPACK_IMPORTED_MODULE_1__.fetchBaseQuery)({
  baseUrl: pnpnm.restUrl,
  credentials: "same-origin",
  prepareHeaders: (headers, {
    endpoint
  }) => {
    headers.set("X-WP-Nonce", pnpnm.nonce);
    const fileUploadEndpoints = ["uploadWatermarkFont", "replaceMedia"];
    if (!fileUploadEndpoints.includes(endpoint)) {
      headers.set("Content-Type", "application/json");
    }
    return headers;
  }
});
const wpBaseQuery = async (args, api, extraOptions) => {
  if (typeof args === "object" && pnpnm.isPlain && args.params && Object.keys(args.params).length > 0) {
    const qs = new URLSearchParams(Object.entries(args.params).reduce((acc, [key, value]) => {
      if (value !== undefined && value !== null && value !== "") {
        acc[key] = String(value);
      }
      return acc;
    }, {})).toString();
    if (qs) {
      args.url += pnpnm.restUrl.includes("?") ? `&${qs}` : `?${qs}`;
    }
    delete args.params;
  }
  return rawBaseQuery(args, api, extraOptions);
};
const baseApi = (0,_reduxjs_toolkit_query_react__WEBPACK_IMPORTED_MODULE_0__.createApi)({
  reducerPath: "baseApi",
  baseQuery: wpBaseQuery,
  tagTypes: ["Dashboard", "Folders", "PostFolders", "Files", "WatermarkFonts"],
  endpoints: () => ({})
});

/***/ },

/***/ "./source/redux/api/file.ts"
/*!**********************************!*\
  !*** ./source/redux/api/file.ts ***!
  \**********************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   file: () => (/* binding */ file),
/* harmony export */   useGetFilesQuery: () => (/* binding */ useGetFilesQuery)
/* harmony export */ });
/* harmony import */ var _base__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./base */ "./source/redux/api/base.ts");

const file = _base__WEBPACK_IMPORTED_MODULE_0__.baseApi.injectEndpoints({
  endpoints: builder => ({
    getFiles: builder.query({
      query: ({
        type,
        extension,
        orderBy,
        order,
        page,
        perPage,
        search
      }) => {
        const params = {
          ...(type && {
            type
          }),
          ...(type === "dynamic" && extension && {
            extension
          }),
          ...(orderBy && {
            orderBy
          }),
          ...(order && {
            order
          }),
          ...(page !== undefined && {
            page
          }),
          ...(perPage !== undefined && {
            perPage
          }),
          ...(search && {
            search
          })
        };
        return {
          url: `media-library/files`,
          method: "GET",
          params
        };
      },
      providesTags: ["Files"]
    })
  })
});
const {
  useGetFilesQuery
} = file;

/***/ },

/***/ "./source/redux/api/media.ts"
/*!***********************************!*\
  !*** ./source/redux/api/media.ts ***!
  \***********************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   media: () => (/* binding */ media),
/* harmony export */   useAssignFileMutation: () => (/* binding */ useAssignFileMutation),
/* harmony export */   useCopyFolderMutation: () => (/* binding */ useCopyFolderMutation),
/* harmony export */   useCreateFolderMutation: () => (/* binding */ useCreateFolderMutation),
/* harmony export */   useDeleteFileMutation: () => (/* binding */ useDeleteFileMutation),
/* harmony export */   useDeleteFolderMutation: () => (/* binding */ useDeleteFolderMutation),
/* harmony export */   useDownloadFolderMutation: () => (/* binding */ useDownloadFolderMutation),
/* harmony export */   useDuplicateMediaMutation: () => (/* binding */ useDuplicateMediaMutation),
/* harmony export */   useGetBreadcrumbsQuery: () => (/* binding */ useGetBreadcrumbsQuery),
/* harmony export */   useGetFoldersQuery: () => (/* binding */ useGetFoldersQuery),
/* harmony export */   useLazyGetFolderQuery: () => (/* binding */ useLazyGetFolderQuery),
/* harmony export */   useMoveFolderMutation: () => (/* binding */ useMoveFolderMutation),
/* harmony export */   useReplaceMediaMutation: () => (/* binding */ useReplaceMediaMutation),
/* harmony export */   useRestoreFileMutation: () => (/* binding */ useRestoreFileMutation),
/* harmony export */   useToggleFavoriteMutation: () => (/* binding */ useToggleFavoriteMutation),
/* harmony export */   useTrashFileMutation: () => (/* binding */ useTrashFileMutation),
/* harmony export */   useUpdateFileMetadataMutation: () => (/* binding */ useUpdateFileMetadataMutation),
/* harmony export */   useUpdateFolderMutation: () => (/* binding */ useUpdateFolderMutation)
/* harmony export */ });
/* harmony import */ var _base__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./base */ "./source/redux/api/base.ts");

const media = _base__WEBPACK_IMPORTED_MODULE_0__.baseApi.injectEndpoints({
  endpoints: builder => ({
    getFolders: builder.query({
      query: ({
        orderBy,
        order,
        search
      }) => {
        const params = {};
        if (orderBy) {
          params.orderBy = orderBy;
        }
        if (order) {
          params.order = order;
        }
        if (search) {
          params.search = search;
        }
        return {
          url: `media-library`,
          method: "GET",
          params
        };
      },
      providesTags: ["Folders"]
    }),
    getFolder: builder.query({
      query: ({
        id
      }) => {
        return {
          url: `media-library/${id}`,
          method: "GET"
        };
      }
    }),
    getBreadcrumbs: builder.query({
      query: ({
        id
      }) => {
        return {
          url: `media-library/breadcrumbs`,
          method: "GET",
          params: {
            id
          }
        };
      }
    }),
    createFolder: builder.mutation({
      query: ({
        name,
        parentId
      }) => {
        return {
          url: `media-library/folder`,
          method: "POST",
          body: {
            name,
            parentId
          }
        };
      },
      async onQueryStarted({
        parentId
      }, {
        dispatch,
        queryFulfilled,
        getState
      }) {
        try {
          const {
            data: result
          } = await queryFulfilled;
          const newFolder = result?.data;
          if (!newFolder || parentId) return;
          const state = getState();
          const {
            folderSorting,
            order,
            search
          } = state.media;
          const isForceSorting = state.settings?.data?.general?.folder?.forceSorting ?? false;
          if (isForceSorting) {
            dispatch(media.util.invalidateTags(["Folders"]));
            dispatch(media.endpoints.getFolders.initiate({
              orderBy: folderSorting?.orderBy,
              order: order
            }, {
              forceRefetch: true
            }));
          } else {
            dispatch(media.util.updateQueryData("getFolders", {
              orderBy: folderSorting?.orderBy,
              order: order,
              search: search
            }, draft => {
              if (draft?.data?.folders) {
                draft.data.folders.unshift(newFolder);
                draft.data.totalFolders = (draft.data.totalFolders ?? 0) + 1;
              }
            }));
          }
        } catch {}
      }
    }),
    updateFolder: builder.mutation({
      query: ({
        id,
        name,
        color
      }) => {
        return {
          url: `media-library/folder`,
          method: "PUT",
          body: {
            id,
            name,
            color
          }
        };
      }
    }),
    copyFolder: builder.mutation({
      query: ({
        parentId,
        id
      }) => {
        return {
          url: `media-library/folder/copy`,
          method: "POST",
          body: {
            parentId,
            ids: [id]
          }
        };
      }
    }),
    moveFolder: builder.mutation({
      query: ({
        id,
        ids
      }) => {
        return {
          url: `media-library/folder/move`,
          method: "POST",
          body: {
            parentId: id,
            ids
          }
        };
      }
    }),
    deleteFolder: builder.mutation({
      query: ({
        ids,
        isMediaDelete
      }) => {
        return {
          url: `media-library/folder`,
          method: "DELETE",
          body: {
            ids,
            isMediaDelete
          }
        };
      }
    }),
    assignFile: builder.mutation({
      query: ({
        id,
        attachments
      }) => {
        return {
          url: `media-library/folder/${id}/assign`,
          method: "POST",
          body: {
            attachments
          }
        };
      }
    }),
    restoreFile: builder.mutation({
      query: ({
        ids
      }) => {
        return {
          url: `media-library/restore`,
          method: "POST",
          body: {
            ids
          }
        };
      }
    }),
    deleteFile: builder.mutation({
      query: ({
        ids
      }) => {
        return {
          url: `media-library/delete`,
          method: "DELETE",
          body: {
            ids
          }
        };
      }
    }),
    updateFileMetadata: builder.mutation({
      query: ({
        id,
        ...body
      }) => ({
        url: `media-library/files/${id}/metadata`,
        method: "PUT",
        body
      }),
      invalidatesTags: ["Files"]
    }),
  })
});
const {
  useGetFoldersQuery,
  useLazyGetFolderQuery,
  useGetBreadcrumbsQuery,
  useCreateFolderMutation,
  useUpdateFolderMutation,
  useCopyFolderMutation,
  useMoveFolderMutation,
  useAssignFileMutation,
  useDeleteFolderMutation,
  useRestoreFileMutation,
  useDeleteFileMutation,
  useUpdateFileMetadataMutation,
} = media;

/***/ },

/***/ "./source/redux/api/settings.ts"
/*!**************************************!*\
  !*** ./source/redux/api/settings.ts ***!
  \**************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   settingsApi: () => (/* binding */ settingsApi),
/* harmony export */   useGenerateThumbnailsMutation: () => (/* binding */ useGenerateThumbnailsMutation),
/* harmony export */   useGetThumbnailCountQuery: () => (/* binding */ useGetThumbnailCountQuery),
/* harmony export */   useUpdateSettingsMutation: () => (/* binding */ useUpdateSettingsMutation)
/* harmony export */ });
/* harmony import */ var _base__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./base */ "./source/redux/api/base.ts");

const settingsApi = _base__WEBPACK_IMPORTED_MODULE_0__.baseApi.injectEndpoints({
  endpoints: builder => ({
    updateSettings: builder.mutation({
      query: data => ({
        url: "settings",
        method: "POST",
        body: {
          settings: data
        }
      })
    }),
    getThumbnailCount: builder.query({
      query: () => ({
        url: "tools/thumbnails/count",
        method: "GET"
      })
    }),
    generateThumbnails: builder.mutation({
      query: ({
        offset,
        batch_size = 5
      }) => ({
        url: "tools/thumbnails/generate",
        method: "POST",
        body: {
          offset,
          batch_size
        }
      })
    })
  })
});
const {
  useUpdateSettingsMutation,
  useGetThumbnailCountQuery,
  useGenerateThumbnailsMutation
} = settingsApi;

/***/ },

/***/ "./source/redux/api/watermark__premium_only.ts"
/*!*****************************************************!*\
  !*** ./source/redux/api/watermark__premium_only.ts ***!
  \*****************************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   useApplyWatermarkBatchMutation: () => (/* binding */ useApplyWatermarkBatchMutation),
/* harmony export */   useApplyWatermarkMutation: () => (/* binding */ useApplyWatermarkMutation),
/* harmony export */   useDeleteWatermarkFontMutation: () => (/* binding */ useDeleteWatermarkFontMutation),
/* harmony export */   useGetWatermarkCountQuery: () => (/* binding */ useGetWatermarkCountQuery),
/* harmony export */   useGetWatermarkFontsQuery: () => (/* binding */ useGetWatermarkFontsQuery),
/* harmony export */   useLazyGetEligibleIdsQuery: () => (/* binding */ useLazyGetEligibleIdsQuery),
/* harmony export */   useRestoreOriginalsMutation: () => (/* binding */ useRestoreOriginalsMutation),
/* harmony export */   useUploadWatermarkFontMutation: () => (/* binding */ useUploadWatermarkFontMutation),
/* harmony export */   watermarkApi: () => (/* binding */ watermarkApi)
/* harmony export */ });
/* harmony import */ var _base__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./base */ "./source/redux/api/base.ts");

const watermarkApi = _base__WEBPACK_IMPORTED_MODULE_0__.baseApi.injectEndpoints({
  endpoints: builder => ({
    applyWatermark: builder.mutation({
      query: body => ({
        url: "watermark/apply",
        method: "POST",
        body
      })
    }),
    applyWatermarkBatch: builder.mutation({
      query: body => ({
        url: "watermark/apply-batch",
        method: "POST",
        body
      })
    }),
    getWatermarkCount: builder.query({
      query: () => ({
        url: "watermark/count",
        method: "GET"
      })
    }),
    getEligibleIds: builder.query({
      query: () => ({
        url: "watermark/eligible-ids",
        method: "GET"
      })
    }),
    restoreOriginals: builder.mutation({
      query: body => ({
        url: "watermark/restore",
        method: "POST",
        body
      })
    }),
    getWatermarkFonts: builder.query({
      query: () => ({
        url: "watermark/fonts",
        method: "GET"
      }),
      providesTags: ["WatermarkFonts"]
    }),
    uploadWatermarkFont: builder.mutation({
      query: formData => ({
        url: "watermark/fonts/upload",
        method: "POST",
        body: formData,
        formData: true
      }),
      invalidatesTags: ["WatermarkFonts"]
    }),
    deleteWatermarkFont: builder.mutation({
      query: ({
        slug
      }) => ({
        url: "watermark/fonts",
        method: "DELETE",
        body: {
          slug
        }
      }),
      invalidatesTags: ["WatermarkFonts"]
    })
  })
});
const {
  useApplyWatermarkMutation,
  useApplyWatermarkBatchMutation,
  useGetWatermarkCountQuery,
  useLazyGetEligibleIdsQuery,
  useRestoreOriginalsMutation,
  useGetWatermarkFontsQuery,
  useUploadWatermarkFontMutation,
  useDeleteWatermarkFontMutation
} = watermarkApi;

/***/ },

/***/ "./source/redux/features/file/file.ts"
/*!********************************************!*\
  !*** ./source/redux/features/file/file.ts ***!
  \********************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   appendFiles: () => (/* binding */ appendFiles),
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__),
/* harmony export */   deleteFile: () => (/* binding */ deleteFile),
/* harmony export */   fileSlice: () => (/* binding */ fileSlice),
/* harmony export */   selectFile: () => (/* binding */ selectFile),
/* harmony export */   setQuery: () => (/* binding */ setQuery),
/* harmony export */   updateFile: () => (/* binding */ updateFile),
/* harmony export */   updateFileState: () => (/* binding */ updateFileState)
/* harmony export */ });
/* harmony import */ var _reduxjs_toolkit__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @reduxjs/toolkit */ "./node_modules/@reduxjs/toolkit/dist/redux-toolkit.modern.mjs");
/* harmony import */ var _utils_functions__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ~/utils/functions */ "./source/utils/functions.ts");

const getPersistedFileState = () => {
  if (typeof window === "undefined") return undefined;
  try {
    const raw = localStorage.getItem("fileStates");
    if (!raw) return undefined;
    const parsed = JSON.parse(raw);
    if (typeof parsed !== "object") return undefined;
    return parsed;
  } catch {
    return undefined;
  }
};
const persisted = getPersistedFileState();
const initialState = {
  view: persisted?.view ?? "grid",
  loadingType: persisted?.loadingType ?? "infinite",
  count: {
    all: 0,
    uncategorized: 0,
    dynamic: 0,
    favorites: 0,
    used: 0,
    unused: 0,
    trash: 0
  },
  files: [],
  selectedFiles: [],
  hiddenFileIds: [],
  detailsFile: null,
  query: {
    orderBy: persisted?.query?.orderBy ?? "createdAt",
    order: persisted?.query?.order ?? "DESC",
    page: persisted?.query?.page ?? 1,
    perPage: persisted?.query?.perPage ?? 80,
    search: ""
  },
  dynamicFolders: {},
  totalPages: 1,
  hasMore: false,
  bulkSelect: false,
  loading: false
};
const fileSlice = (0,_reduxjs_toolkit__WEBPACK_IMPORTED_MODULE_0__.createSlice)({
  name: "file",
  initialState,
  reducers: {
    updateFileState: (state, action) => {
      (0,_utils_functions__WEBPACK_IMPORTED_MODULE_1__.setNestedValue)(state, action.payload.path, action.payload.value);
    },
    appendFiles: (state, action) => {
      const existingIds = new Set(state.files.map(f => String(f.id)));
      const newFiles = action.payload.filter(f => !existingIds.has(String(f.id)));
      state.files = [...state.files, ...newFiles];
    },
    setQuery: (state, action) => {
      const resettingKeys = ["orderBy", "order", "search"];
      const isResetting = resettingKeys.some(key => key in action.payload);
      state.query = {
        ...state.query,
        ...action.payload,
        ...(isResetting && !("page" in action.payload) && {
          page: 1
        })
      };
      if (isResetting) {
        state.hasMore = false;
      }
    },
    updateFile: (state, action) => {
      const {
        id,
        data
      } = action.payload;
      const index = state.files.findIndex(f => String(f?.id) === String(id));
      if (index !== -1) {
        state.files[index] = {
          ...state.files[index],
          ...data
        };
      }
    },
    deleteFile: (state, action) => {
      const ids = new Set(action.payload);
      state.files = state.files.filter(f => !ids.has(f?.id));
      state.selectedFiles = state.selectedFiles.filter(f => !ids.has(f?.id));
    }
  }
});
const {
  updateFileState,
  appendFiles,
  setQuery,
  updateFile,
  deleteFile
} = fileSlice.actions;
const selectFile = state => state.file;
const fileReducer = fileSlice.reducer;
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (fileReducer);

/***/ },

/***/ "./source/redux/features/media/media.ts"
/*!**********************************************!*\
  !*** ./source/redux/features/media/media.ts ***!
  \**********************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__),
/* harmony export */   mediaSlice: () => (/* binding */ mediaSlice),
/* harmony export */   removeFolder: () => (/* binding */ removeFolder),
/* harmony export */   selectMedia: () => (/* binding */ selectMedia),
/* harmony export */   setFolders: () => (/* binding */ setFolders),
/* harmony export */   updateFolder: () => (/* binding */ updateFolder),
/* harmony export */   updateMedia: () => (/* binding */ updateMedia)
/* harmony export */ });
/* harmony import */ var _reduxjs_toolkit__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @reduxjs/toolkit */ "./node_modules/@reduxjs/toolkit/dist/redux-toolkit.modern.mjs");
/* harmony import */ var _utils_functions__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ~/utils/functions */ "./source/utils/functions.ts");

const initialState = {
  menu: "all",
  allFiles: 0,
  uncategorized: 0,
  dynamicFolders: {},
  favorites: 0,
  used: 0,
  unused: 0,
  trash: 0,
  folders: {},
  activeFolder: null,
  selectedFolders: [],
  expandedFolderIds: [],
  hiddenFolderIds: [],
  createFolder: {
    create: false,
    folder: null
  },
  renameFolder: {
    rename: false,
    folder: null
  },
  cutFolders: {
    cut: false,
    folders: []
  },
  moveFolder: {
    move: false,
    id: null
  },
  folderSorting: {
    orderBy: "name"
  },
  fileSorting: {
    orderBy: "name"
  },
  order: "ASC",
  search: "",
  bulkSelect: false,
  expandAll: false,
  loading: false
};
const mediaSlice = (0,_reduxjs_toolkit__WEBPACK_IMPORTED_MODULE_0__.createSlice)({
  name: "media",
  initialState,
  reducers: {
    updateMedia: (state, action) => {
      (0,_utils_functions__WEBPACK_IMPORTED_MODULE_1__.setNestedValue)(state, action.payload.path, action.payload.value);
    },
    setFolders: (state, action) => {
      state.folders[action.payload.id] = action.payload.children;
    },
    updateFolder: (state, action) => {
      const {
        id,
        data
      } = action.payload;
      Object.keys(state.folders).forEach(parentId => {
        const index = state.folders[parentId].findIndex(f => String(f.id) === String(id));
        if (index !== -1) {
          state.folders[parentId][index] = {
            ...state.folders[parentId][index],
            ...data
          };
        }
      });
    },
    removeFolder: (state, action) => {
      const deletedIds = new Set(action.payload.map(String));
      Object.keys(state.folders).forEach(parentId => {
        state.folders[parentId] = state.folders[parentId].filter(f => !deletedIds.has(String(f.id)));
      });
    }
  }
});
const {
  updateMedia,
  setFolders,
  updateFolder,
  removeFolder
} = mediaSlice.actions;
const selectMedia = state => state.media;
const mediaReducer = mediaSlice.reducer;
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (mediaReducer);

/***/ },

/***/ "./source/redux/features/post__premium_only/post.ts"
/*!**********************************************************!*\
  !*** ./source/redux/features/post__premium_only/post.ts ***!
  \**********************************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__),
/* harmony export */   postsSlice: () => (/* binding */ postsSlice),
/* harmony export */   removeFolder: () => (/* binding */ removeFolder),
/* harmony export */   selectPost: () => (/* binding */ selectPost),
/* harmony export */   setFolders: () => (/* binding */ setFolders),
/* harmony export */   updateFolder: () => (/* binding */ updateFolder),
/* harmony export */   updatePost: () => (/* binding */ updatePost)
/* harmony export */ });
/* harmony import */ var _reduxjs_toolkit__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @reduxjs/toolkit */ "./node_modules/@reduxjs/toolkit/dist/redux-toolkit.modern.mjs");
/* harmony import */ var _utils_functions__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ~/utils/functions */ "./source/utils/functions.ts");

const initialState = {
  menu: "all",
  allPosts: 0,
  uncategorized: 0,
  favorites: 0,
  trash: 0,
  folders: {},
  activeFolder: null,
  selectedFolders: [],
  expandedFolderIds: [],
  hiddenFolderIds: [],
  createFolder: {
    create: false,
    folder: null
  },
  renameFolder: {
    rename: false,
    folder: null
  },
  cutFolders: {
    cut: false,
    folders: []
  },
  moveFolder: {
    move: false,
    id: null
  },
  folderSorting: {
    orderBy: "name"
  },
  postSorting: {
    orderBy: "name"
  },
  order: "ASC",
  search: "",
  bulkSelect: false,
  expandAll: false,
  loading: false
};
const postsSlice = (0,_reduxjs_toolkit__WEBPACK_IMPORTED_MODULE_0__.createSlice)({
  name: "post",
  initialState,
  reducers: {
    updatePost: (state, action) => {
      (0,_utils_functions__WEBPACK_IMPORTED_MODULE_1__.setNestedValue)(state, action.payload.path, action.payload.value);
    },
    setFolders: (state, action) => {
      state.folders[action.payload.id] = action.payload.children;
    },
    updateFolder: (state, action) => {
      const {
        id,
        data
      } = action.payload;
      Object.keys(state.folders).forEach(parentId => {
        const index = state.folders[parentId].findIndex(f => String(f.id) === String(id));
        if (index !== -1) {
          state.folders[parentId][index] = {
            ...state.folders[parentId][index],
            ...data
          };
        }
      });
    },
    removeFolder: (state, action) => {
      const deletedIds = new Set(action.payload.map(String));
      Object.keys(state.folders).forEach(parentId => {
        state.folders[parentId] = state.folders[parentId].filter(f => !deletedIds.has(String(f.id)));
      });
    }
  }
});
const {
  updatePost,
  setFolders,
  updateFolder,
  removeFolder
} = postsSlice.actions;
const selectPost = state => state.post;
const postReducer = postsSlice.reducer;
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (postReducer);

/***/ },

/***/ "./source/redux/features/settings/settings.ts"
/*!****************************************************!*\
  !*** ./source/redux/features/settings/settings.ts ***!
  \****************************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__),
/* harmony export */   selectSettings: () => (/* binding */ selectSettings),
/* harmony export */   setDraft: () => (/* binding */ setDraft),
/* harmony export */   settingsInit: () => (/* binding */ settingsInit),
/* harmony export */   settingsSlice: () => (/* binding */ settingsSlice),
/* harmony export */   updateSettings: () => (/* binding */ updateSettings)
/* harmony export */ });
/* harmony import */ var _reduxjs_toolkit__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @reduxjs/toolkit */ "./node_modules/@reduxjs/toolkit/dist/redux-toolkit.modern.mjs");
/* harmony import */ var _utils_functions__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ~/utils/functions */ "./source/utils/functions.ts");

const initialState = {
  data: null,
  defaultData: null,
  draft: null
};
const settingsSlice = (0,_reduxjs_toolkit__WEBPACK_IMPORTED_MODULE_0__.createSlice)({
  name: "settings",
  initialState,
  reducers: {
    settingsInit: (state, action) => {
      state.data = action.payload.data;
      state.defaultData = action.payload.defaultData;
      state.draft = action.payload.data;
    },
    updateSettings: (state, action) => {
      if (!state.data) return;
      (0,_utils_functions__WEBPACK_IMPORTED_MODULE_1__.setNestedValue)(state.data, action.payload.path, action.payload.value);
    },
    setDraft: (state, action) => {
      state.draft = action.payload;
    }
  }
});
const {
  settingsInit,
  updateSettings,
  setDraft
} = settingsSlice.actions;
const selectSettings = state => state.settings;
const settingsReducer = settingsSlice.reducer;
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (settingsReducer);

/***/ },

/***/ "./source/redux/functions/updateFilesCaches.ts"
/*!*****************************************************!*\
  !*** ./source/redux/functions/updateFilesCaches.ts ***!
  \*****************************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _api_file__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../api/file */ "./source/redux/api/file.ts");
/* harmony import */ var _store__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../store */ "./source/redux/store.ts");

const updateFilesCaches = (removedIds, countUpdates) => {
  const state = _store__WEBPACK_IMPORTED_MODULE_1__.store.getState();
  const queries = state[_api_file__WEBPACK_IMPORTED_MODULE_0__.file.reducerPath]?.queries ?? {};
  Object.entries(queries).forEach(([, queryEntry]) => {
    if (queryEntry?.status !== "fulfilled") return;
    const endpointName = queryEntry?.endpointName;
    if (endpointName !== "getFiles") return;
    const args = queryEntry?.originalArgs;
    if (!args) return;
    _store__WEBPACK_IMPORTED_MODULE_1__.store.dispatch(_api_file__WEBPACK_IMPORTED_MODULE_0__.file.util.updateQueryData("getFiles", args, draft => {
      if (!draft?.data) return;
      const removedSet = new Set(removedIds.map(String));
      draft.data.files = draft.data.files.filter(f => !removedSet.has(String(f.id)));
      if (countUpdates.allFiles !== undefined) {
        draft.data.allFiles = countUpdates.allFiles;
      }
      if (countUpdates.uncategorized !== undefined) {
        draft.data.uncategorized = countUpdates.uncategorized;
      }
      if (countUpdates.unused !== undefined) {
        draft.data.unused = countUpdates.unused;
      }
      if (countUpdates.trash !== undefined) {
        draft.data.trash = countUpdates.trash;
      }
      if (countUpdates.dynamicFolders !== undefined) {
        draft.data.dynamicFolders = countUpdates.dynamicFolders;
      }
      const perPage = args.perPage ?? 30;
      draft.data.total = Math.max(0, draft.data.total - removedIds.length);
      draft.data.totalPages = Math.max(1, Math.ceil(draft.data.total / perPage));
    }));
  });
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (updateFilesCaches);

/***/ },

/***/ "./source/redux/hooks.ts"
/*!*******************************!*\
  !*** ./source/redux/hooks.ts ***!
  \*******************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   useAppDispatch: () => (/* binding */ useAppDispatch),
/* harmony export */   useAppSelector: () => (/* binding */ useAppSelector)
/* harmony export */ });
/* harmony import */ var react_redux__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! react-redux */ "./node_modules/react-redux/dist/react-redux.mjs");

const useAppSelector = react_redux__WEBPACK_IMPORTED_MODULE_0__.useSelector.withTypes();
const useAppDispatch = react_redux__WEBPACK_IMPORTED_MODULE_0__.useDispatch.withTypes();

/***/ },

/***/ "./source/redux/middleware/file.ts"
/*!*****************************************!*\
  !*** ./source/redux/middleware/file.ts ***!
  \*****************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   fileMiddleware: () => (/* binding */ fileMiddleware)
/* harmony export */ });
let debounceTimer = null;
const fileMiddleware = store => next => action => {
  const result = next(action);
  const actionType = action.type;
  if (!actionType?.startsWith("file/")) return result;
  if (debounceTimer) clearTimeout(debounceTimer);
  debounceTimer = setTimeout(() => {
    const state = store.getState().file;
    localStorage.setItem("fileStates", JSON.stringify({
      view: state.view,
      loadingType: state.loadingType,
      query: {
        orderBy: state.query.orderBy,
        order: state.query.order,
        page: state.query.page,
        perPage: state.query.perPage
      }
    }));
  }, 300);
  return result;
};

/***/ },

/***/ "./source/redux/store.ts"
/*!*******************************!*\
  !*** ./source/redux/store.ts ***!
  \*******************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   store: () => (/* binding */ store)
/* harmony export */ });
/* harmony import */ var _reduxjs_toolkit__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @reduxjs/toolkit */ "./node_modules/@reduxjs/toolkit/dist/redux-toolkit.modern.mjs");
/* harmony import */ var _reduxjs_toolkit__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! @reduxjs/toolkit */ "./node_modules/redux/dist/redux.mjs");
/* harmony import */ var _features_settings_settings__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./features/settings/settings */ "./source/redux/features/settings/settings.ts");
/* harmony import */ var _middleware_file__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ./middleware/file */ "./source/redux/middleware/file.ts");
/* harmony import */ var _features_media_media__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ./features/media/media */ "./source/redux/features/media/media.ts");
/* harmony import */ var _features_file_file__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ./features/file/file */ "./source/redux/features/file/file.ts");
/* harmony import */ var _api_base__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! ./api/base */ "./source/redux/api/base.ts");
/* harmony import */ var _features_post_premium_only_post__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! ./features/post__premium_only/post */ "./source/redux/features/post__premium_only/post.ts");

const rootReducer = (0,_reduxjs_toolkit__WEBPACK_IMPORTED_MODULE_1__.combineReducers)({
  [_api_base__WEBPACK_IMPORTED_MODULE_6__.baseApi.reducerPath]: _api_base__WEBPACK_IMPORTED_MODULE_6__.baseApi.reducer,
  settings: _features_settings_settings__WEBPACK_IMPORTED_MODULE_2__["default"],
  media: _features_media_media__WEBPACK_IMPORTED_MODULE_4__["default"],
  file: _features_file_file__WEBPACK_IMPORTED_MODULE_5__["default"],
});
const store = (0,_reduxjs_toolkit__WEBPACK_IMPORTED_MODULE_0__.configureStore)({
  reducer: rootReducer,
  middleware: getDefaultMiddleware => getDefaultMiddleware().concat(_middleware_file__WEBPACK_IMPORTED_MODULE_3__.fileMiddleware).concat(_api_base__WEBPACK_IMPORTED_MODULE_6__.baseApi.middleware)
});

/***/ },

/***/ "./source/routes/MainRoute.tsx"
/*!*************************************!*\
  !*** ./source/routes/MainRoute.tsx ***!
  \*************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _redux_features_settings_settings__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ~/redux/features/settings/settings */ "./source/redux/features/settings/settings.ts");
/* harmony import */ var _hooks_useLocalStorage__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ~/hooks/useLocalStorage */ "./source/hooks/useLocalStorage.ts");
/* harmony import */ var _wordpress_element__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! @wordpress/element */ "@wordpress/element");
/* harmony import */ var _wordpress_element__WEBPACK_IMPORTED_MODULE_2___default = /*#__PURE__*/__webpack_require__.n(_wordpress_element__WEBPACK_IMPORTED_MODULE_2__);
/* harmony import */ var _redux_hooks__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ~/redux/hooks */ "./source/redux/hooks.ts");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! react/jsx-runtime */ "react/jsx-runtime");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_4___default = /*#__PURE__*/__webpack_require__.n(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_4__);

const MainRoute = ({
  children
}) => {
  const [theme, setTheme] = (0,_hooks_useLocalStorage__WEBPACK_IMPORTED_MODULE_1__.useLocalStorage)("pnpnm-theme-status", "light");
  const dispatch = (0,_redux_hooks__WEBPACK_IMPORTED_MODULE_3__.useAppDispatch)();
  const defaultSettings = pnpnm?.defaultSettings || {};
  const settings = pnpnm?.settings || defaultSettings || {};
  (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_2__.useEffect)(() => {
    const root = document?.documentElement;
    root?.setAttribute("pnpnm-theme-status", theme);
  }, [theme]);
  (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_2__.useEffect)(() => {
    const updateMenu = () => {
      const currentMenu = location.hash.split("/")[1];
      const anchors = Array.from(document.querySelectorAll('a[href*="page=ninja-media#"]'));
      anchors.forEach(a => a.classList.remove("current"));
      const activeAnchor = anchors.filter(a => {
        const href = a.getAttribute("href") || "";
        return href.split("#/")[1]?.split("/")[0] === currentMenu;
      }).at(-1);
      activeAnchor?.classList.add("current");
    };
    updateMenu();
    window.addEventListener("hashchange", updateMenu);
    return () => window.removeEventListener("hashchange", updateMenu);
  }, []);
  (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_2__.useEffect)(() => {
    dispatch((0,_redux_features_settings_settings__WEBPACK_IMPORTED_MODULE_0__.settingsInit)({
      defaultData: defaultSettings,
      data: settings
    }));
  }, [defaultSettings, settings]);
  return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_4__.jsx)(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_4__.Fragment, {
    children: children
  });
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (MainRoute);

/***/ },

/***/ "./source/shared/hooks/useReplaceMedia__premium_only.ts"
/*!**************************************************************!*\
  !*** ./source/shared/hooks/useReplaceMedia__premium_only.ts ***!
  \**************************************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   refreshAttachmentPreview: () => (/* binding */ refreshAttachmentPreview),
/* harmony export */   useReplaceMedia: () => (/* binding */ useReplaceMedia)
/* harmony export */ });
/* harmony import */ var _redux_api_media__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ~/redux/api/media */ "./source/redux/api/media.ts");
/* harmony import */ var _components_alert_Alert__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ~/components/alert/Alert */ "./source/components/alert/Alert.tsx");
/* harmony import */ var _wordpress_element__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! @wordpress/element */ "@wordpress/element");
/* harmony import */ var _wordpress_element__WEBPACK_IMPORTED_MODULE_2___default = /*#__PURE__*/__webpack_require__.n(_wordpress_element__WEBPACK_IMPORTED_MODULE_2__);

const useReplaceMedia = onSuccess => {
  const [loading, setLoading] = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_2__.useState)(false);
  const [replaceMedia] = (0,_redux_api_media__WEBPACK_IMPORTED_MODULE_0__.useReplaceMediaMutation)();
  const {
    showAlert
  } = (0,_components_alert_Alert__WEBPACK_IMPORTED_MODULE_1__.useCustomAlert)();
  const replaceAttachment = async attachmentId => {
    const input = document.createElement("input");
    input.type = "file";
    input.style.display = "none";
    input.accept = "image/*,video/*,audio/*,.pdf,.doc,.docx,.xls,.xlsx";
    document.body.appendChild(input);
    input.addEventListener("change", async () => {
      const file = input.files?.[0];
      input.remove();
      if (!file) return;
      setLoading(true);
      try {
        const formData = new FormData();
        formData.append("file", file);
        const result = await replaceMedia({
          id: attachmentId,
          formData
        }).unwrap();
        const freshData = result?.data;
        refreshAttachmentPreview(attachmentId, freshData);
        if (onSuccess && freshData) {
          const url = typeof freshData.url === "string" ? freshData.url : "";
          const sizes = freshData.sizes;
          const thumbnailUrl = sizes?.medium?.url || sizes?.thumbnail?.url || url;
          onSuccess({
            id: attachmentId,
            url,
            thumbnailUrl
          });
        }
        showAlert({
          toast: true,
          type: "success",
          text: result?.message || "Media replaced successfully.",
          timer: 3000,
          timerProgressBar: true,
          showConfirmButton: false
        });
      } catch (error) {
        showAlert({
          toast: true,
          type: "error",
          text: error?.data?.message || "Failed to replace media. Please try again.",
          timer: 4000,
          timerProgressBar: true,
          showConfirmButton: false
        });
      } finally {
        setLoading(false);
      }
    });
    input.click();
  };
  return {
    replaceAttachment,
    loading
  };
};
function refreshAttachmentPreview(attachmentId, freshData) {
  const wpMedia = window.wp?.media;
  if (!wpMedia) return;
  const attachment = wpMedia.attachment(attachmentId);
  if (!attachment) return;
  const newUrl = typeof freshData.url === "string" ? freshData.url : "";
  const patch = {};
  if (newUrl) {
    patch.url = newUrl;
  }
  if (freshData.sizes && typeof freshData.sizes === "object") {
    patch.sizes = freshData.sizes;
  }
  if (Object.keys(patch).length > 0) {
    attachment.set(patch);
  }

  // Directly update every preview <img> to the new URL so the display
  // is instant regardless of whether Backbone triggers a re-render.
  if (newUrl) {
    document.querySelectorAll(".attachment-details img, .attachment-preview img, " + ".attachment-details .attachment-media-view img").forEach(img => {
      img.src = newUrl;
    });
  }
}

/***/ },

/***/ "./source/shared/topbar/Sorting.tsx"
/*!******************************************!*\
  !*** ./source/shared/topbar/Sorting.tsx ***!
  \******************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   FOLDER_OPTIONS: () => (/* binding */ FOLDER_OPTIONS),
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _components_blockStack__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ~/components/blockStack */ "./source/components/blockStack/index.ts");
/* harmony import */ var _components_iconButton__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ~/components/iconButton */ "./source/components/iconButton/index.ts");
/* harmony import */ var _components_selectBox__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ~/components/selectBox */ "./source/components/selectBox/index.ts");
/* harmony import */ var _components_dropdown__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ~/components/dropdown */ "./source/components/dropdown/index.ts");
/* harmony import */ var _wordpress_i18n__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! @wordpress/i18n */ "@wordpress/i18n");
/* harmony import */ var _wordpress_i18n__WEBPACK_IMPORTED_MODULE_4___default = /*#__PURE__*/__webpack_require__.n(_wordpress_i18n__WEBPACK_IMPORTED_MODULE_4__);
/* harmony import */ var _components_text__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ~/components/text */ "./source/components/text/index.ts");
/* harmony import */ var _components_card__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! ~/components/card */ "./source/components/card/index.ts");
/* harmony import */ var _components_icon__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! ~/components/icon */ "./source/components/icon/index.ts");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_8__ = __webpack_require__(/*! react/jsx-runtime */ "react/jsx-runtime");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_8___default = /*#__PURE__*/__webpack_require__.n(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_8__);

const Sorting = ({
  variant = "media",
  folderSorting,
  fileSorting,
  order,
  onFolderSorting,
  onFileSorting,
  onOrder,
  disabled
}) => {
  return /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_8__.jsxs)(_components_dropdown__WEBPACK_IMPORTED_MODULE_3__["default"], {
    children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_8__.jsx)(_components_dropdown__WEBPACK_IMPORTED_MODULE_3__["default"].Trigger, {
      disabled: disabled,
      children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_8__.jsx)(_components_iconButton__WEBPACK_IMPORTED_MODULE_1__["default"], {
        variant: "light",
        size: "extrasmall",
        rounded: "xs",
        name: "sort_by_alpha",
        disabled: disabled
      })
    }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_8__.jsxs)(_components_dropdown__WEBPACK_IMPORTED_MODULE_3__["default"].Content, {
      position: {
        right: 0,
        top: "115%"
      },
      style: {
        width: "180px",
        padding: "10px"
      },
      children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_8__.jsx)(_components_text__WEBPACK_IMPORTED_MODULE_5__["default"], {
        weight: "medium",
        children: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_4__.__)("Sorting", "ninja-media")
      }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_8__.jsxs)(_components_blockStack__WEBPACK_IMPORTED_MODULE_0__["default"], {
        marginTop: 15,
        gap: 7,
        children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_8__.jsx)(_components_text__WEBPACK_IMPORTED_MODULE_5__["default"], {
          color: "secondaryblack",
          size: "sm",
          children: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_4__.__)("Sort Folder", "ninja-media")
        }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_8__.jsx)(_components_selectBox__WEBPACK_IMPORTED_MODULE_2__["default"], {
          size: "extrasmall",
          background: "extralight",
          options: FOLDER_OPTIONS,
          value: [folderSorting?.orderBy],
          onChange: value => onFolderSorting({
            ...folderSorting,
            orderBy: value[0]
          })
        })]
      }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_8__.jsxs)(_components_blockStack__WEBPACK_IMPORTED_MODULE_0__["default"], {
        marginTop: 15,
        gap: 7,
        children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_8__.jsx)(_components_text__WEBPACK_IMPORTED_MODULE_5__["default"], {
          color: "secondaryblack",
          size: "sm",
          children: variant === "media" ? (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_4__.__)("Sort Files", "ninja-media") : (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_4__.__)("Sort Posts", "ninja-media")
        }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_8__.jsx)(_components_selectBox__WEBPACK_IMPORTED_MODULE_2__["default"], {
          size: "extrasmall",
          background: "extralight",
          options: FOLDER_OPTIONS,
          value: [fileSorting?.orderBy],
          onChange: value => onFileSorting({
            ...fileSorting,
            orderBy: value[0]
          })
        })]
      }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_8__.jsxs)(_components_blockStack__WEBPACK_IMPORTED_MODULE_0__["default"], {
        marginTop: 15,
        gap: 7,
        children: [/*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_8__.jsx)(_components_text__WEBPACK_IMPORTED_MODULE_5__["default"], {
          color: "secondaryblack",
          size: "sm",
          children: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_4__.__)("Order By", "ninja-media")
        }), /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_8__.jsx)(_components_card__WEBPACK_IMPORTED_MODULE_6__["default"], {
          padding: 5,
          rounded: "sm",
          flex: true,
          direction: "row",
          gap: 5,
          wrap: false,
          children: ORDER_OPTIONS?.map(({
            key,
            icon
          }, index) => /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_8__.jsx)(_components_card__WEBPACK_IMPORTED_MODULE_6__["default"], {
            padding: 5,
            background: order === key ? "primary" : "white",
            rounded: "xs",
            flex: true,
            align: "center",
            blockAlign: "center",
            style: {
              cursor: "pointer"
            },
            onClick: () => onOrder(key),
            children: /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_8__.jsx)(_components_icon__WEBPACK_IMPORTED_MODULE_7__["default"], {
              name: icon,
              color: order === key ? "white" : "black"
            })
          }, key ?? index))
        })]
      })]
    })]
  });
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (Sorting);
const FOLDER_OPTIONS = [{
  name: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_4__.__)("Name", "ninja-media"),
  value: "name"
}, {
  name: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_4__.__)("Size", "ninja-media"),
  value: "size"
}, {
  name: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_4__.__)("Created At", "ninja-media"),
  value: "createdAt"
}, {
  name: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_4__.__)("Updated At", "ninja-media"),
  value: "updatedAt"
}];
const ORDER_OPTIONS = [{
  key: "ASC",
  icon: "arrow_warm_up"
}, {
  key: "DESC",
  icon: "arrow_cool_down"
}];

/***/ },

/***/ "./source/utils/functions.ts"
/*!***********************************!*\
  !*** ./source/utils/functions.ts ***!
  \***********************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   formatFileSize: () => (/* binding */ formatFileSize),
/* harmony export */   objectEqual: () => (/* binding */ objectEqual),
/* harmony export */   setNestedValue: () => (/* binding */ setNestedValue)
/* harmony export */ });
function setNestedValue(obj, path, value) {
  if (!obj || typeof obj !== "object") return;
  const keys = path.split(".");
  let current = obj;
  for (let i = 0; i < keys.length; i++) {
    const key = keys[i];
    if (i === keys.length - 1) {
      current[key] = value;
    } else {
      if (!current[key] || typeof current[key] !== "object") {
        current[key] = {};
      }
      current = current[key];
    }
  }
}
function objectEqual(obj1, obj2) {
  if (obj1 === obj2) return true;
  if (typeof obj1 !== typeof obj2) return false;
  if (obj1 == null || obj2 == null) return obj1 === obj2;
  if (Array.isArray(obj1) && Array.isArray(obj2)) {
    if (obj1.length !== obj2.length) return false;
    const arr1 = [...obj1];
    const arr2 = [...obj2];
    return arr1.every(item1 => {
      const index = arr2.findIndex(item2 => objectEqual(item1, item2));
      if (index === -1) return false;
      arr2.splice(index, 1);
      return true;
    });
  }
  if (typeof obj1 === "object" && typeof obj2 === "object") {
    const keys1 = Object.keys(obj1);
    const keys2 = Object.keys(obj2);
    if (keys1.length !== keys2.length) return false;
    return keys1.every(key => keys2.includes(key) && objectEqual(obj1[key], obj2[key]));
  }
  return obj1 === obj2;
}
const formatFileSize = bytes => {
  if (bytes === 0) return "0 Bytes";
  const k = 1024;
  const sizes = ["Bytes", "KB", "MB", "GB", "TB", "PB", "EB", "ZB", "YB"];
  const i = Math.floor(Math.log(bytes) / Math.log(k));
  return parseFloat((bytes / Math.pow(k, i)).toFixed(2)) + " " + sizes[i];
};

/***/ },

/***/ "./source/utils/icons.ts"
/*!*******************************!*\
  !*** ./source/utils/icons.ts ***!
  \*******************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   iconFolderAwesome: () => (/* binding */ iconFolderAwesome),
/* harmony export */   iconFolderBold: () => (/* binding */ iconFolderBold),
/* harmony export */   iconFolderBoldOpen: () => (/* binding */ iconFolderBoldOpen),
/* harmony export */   iconFolderDefault: () => (/* binding */ iconFolderDefault),
/* harmony export */   iconFolderDefaultOpen: () => (/* binding */ iconFolderDefaultOpen),
/* harmony export */   iconFolderHome: () => (/* binding */ iconFolderHome),
/* harmony export */   iconFolderUncategorized: () => (/* binding */ iconFolderUncategorized),
/* harmony export */   iconNotFound: () => (/* binding */ iconNotFound),
/* harmony export */   iconProIcon: () => (/* binding */ iconProIcon),
/* harmony export */   iconThemeAwesome: () => (/* binding */ iconThemeAwesome),
/* harmony export */   iconThemeBold: () => (/* binding */ iconThemeBold),
/* harmony export */   iconThemeDefault: () => (/* binding */ iconThemeDefault),
/* harmony export */   iconThemePlugininja: () => (/* binding */ iconThemePlugininja),
/* harmony export */   iconUsers: () => (/* binding */ iconUsers)
/* harmony export */ });
const base = pnpnm.assetUrl + "/icons/";
const iconProIcon = base + "IconProIcon.svg";
const iconNotFound = base + "IconNotFound.svg";
const iconUsers = base + "IconUsers.svg";
const iconFolderAwesome = base + "IconFolderAwesome.svg";
const iconFolderBold = base + "IconFolderBold.svg";
const iconFolderBoldOpen = base + "IconFolderBoldOpen.svg";
const iconFolderDefaultOpen = base + "IconFolderDefaultOpen.svg";
const iconFolderHome = base + "IconFolderHome.svg";
const iconFolderDefault = base + "IconFolderDefault.svg";
const iconFolderUncategorized = base + "IconFolderUncategorized.svg";
const iconThemeAwesome = base + "IconThemeAwesome.svg";
const iconThemeBold = base + "IconThemeBold.svg";
const iconThemeDefault = base + "IconThemeDefault.svg";
const iconThemePlugininja = base + "IconThemePlugininja.svg";

/***/ },

/***/ "./source/utils/styles.ts"
/*!********************************!*\
  !*** ./source/utils/styles.ts ***!
  \********************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   gapStyle: () => (/* binding */ gapStyle),
/* harmony export */   marginStyle: () => (/* binding */ marginStyle),
/* harmony export */   marginTopStyle: () => (/* binding */ marginTopStyle),
/* harmony export */   paddingStyle: () => (/* binding */ paddingStyle),
/* harmony export */   paddingTopStyle: () => (/* binding */ paddingTopStyle)
/* harmony export */ });
const marginStyle = margin => margin === undefined ? {} : {
  margin: typeof margin === "number" ? `${margin}px` : margin
};
const marginTopStyle = marginTop => marginTop === undefined ? {} : {
  marginTop: typeof marginTop === "number" ? `${marginTop}px` : marginTop
};
const paddingStyle = padding => padding === undefined ? {} : {
  padding: typeof padding === "number" ? `${padding}px` : padding
};
const paddingTopStyle = paddingTop => paddingTop === undefined ? {} : {
  paddingTop: typeof paddingTop === "number" ? `${paddingTop}px` : paddingTop
};
const gapStyle = gap => gap === undefined ? {} : {
  gap: typeof gap === "number" ? `${gap}px` : gap
};

/***/ },

/***/ "./source/utils/utils.ts"
/*!*******************************!*\
  !*** ./source/utils/utils.ts ***!
  \*******************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   isValidArray: () => (/* binding */ isValidArray),
/* harmony export */   toBoolean: () => (/* binding */ toBoolean)
/* harmony export */ });
const toBoolean = value => value === "true" || value === true || value === "1";
function isValidArray(value) {
  return Array.isArray(value) && value.length > 0;
}

/***/ },

/***/ "./node_modules/use-sync-external-store/cjs/use-sync-external-store-with-selector.development.js"
/*!*******************************************************************************************************!*\
  !*** ./node_modules/use-sync-external-store/cjs/use-sync-external-store-with-selector.development.js ***!
  \*******************************************************************************************************/
(__unused_webpack_module, exports, __webpack_require__) {

/**
 * @license React
 * use-sync-external-store-with-selector.development.js
 *
 * Copyright (c) Meta Platforms, Inc. and affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */

 true &&
  (function () {
    function is(x, y) {
      return (x === y && (0 !== x || 1 / x === 1 / y)) || (x !== x && y !== y);
    }
    "undefined" !== typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ &&
      "function" ===
        typeof __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStart &&
      __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStart(Error());
    var React = __webpack_require__(/*! react */ "react"),
      objectIs = "function" === typeof Object.is ? Object.is : is,
      useSyncExternalStore = React.useSyncExternalStore,
      useRef = React.useRef,
      useEffect = React.useEffect,
      useMemo = React.useMemo,
      useDebugValue = React.useDebugValue;
    exports.useSyncExternalStoreWithSelector = function (
      subscribe,
      getSnapshot,
      getServerSnapshot,
      selector,
      isEqual
    ) {
      var instRef = useRef(null);
      if (null === instRef.current) {
        var inst = { hasValue: !1, value: null };
        instRef.current = inst;
      } else inst = instRef.current;
      instRef = useMemo(
        function () {
          function memoizedSelector(nextSnapshot) {
            if (!hasMemo) {
              hasMemo = !0;
              memoizedSnapshot = nextSnapshot;
              nextSnapshot = selector(nextSnapshot);
              if (void 0 !== isEqual && inst.hasValue) {
                var currentSelection = inst.value;
                if (isEqual(currentSelection, nextSnapshot))
                  return (memoizedSelection = currentSelection);
              }
              return (memoizedSelection = nextSnapshot);
            }
            currentSelection = memoizedSelection;
            if (objectIs(memoizedSnapshot, nextSnapshot))
              return currentSelection;
            var nextSelection = selector(nextSnapshot);
            if (void 0 !== isEqual && isEqual(currentSelection, nextSelection))
              return (memoizedSnapshot = nextSnapshot), currentSelection;
            memoizedSnapshot = nextSnapshot;
            return (memoizedSelection = nextSelection);
          }
          var hasMemo = !1,
            memoizedSnapshot,
            memoizedSelection,
            maybeGetServerSnapshot =
              void 0 === getServerSnapshot ? null : getServerSnapshot;
          return [
            function () {
              return memoizedSelector(getSnapshot());
            },
            null === maybeGetServerSnapshot
              ? void 0
              : function () {
                  return memoizedSelector(maybeGetServerSnapshot());
                }
          ];
        },
        [getSnapshot, getServerSnapshot, selector, isEqual]
      );
      var value = useSyncExternalStore(subscribe, instRef[0], instRef[1]);
      useEffect(
        function () {
          inst.hasValue = !0;
          inst.value = value;
        },
        [value]
      );
      useDebugValue(value);
      return value;
    };
    "undefined" !== typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ &&
      "function" ===
        typeof __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStop &&
      __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStop(Error());
  })();

/***/ },

/***/ "./node_modules/use-sync-external-store/with-selector.js"
/*!***************************************************************!*\
  !*** ./node_modules/use-sync-external-store/with-selector.js ***!
  \***************************************************************/
(module, __unused_webpack_exports, __webpack_require__) {

if (false) // removed by dead control flow
{} else {
  module.exports = __webpack_require__(/*! ./cjs/use-sync-external-store-with-selector.development.js */ "./node_modules/use-sync-external-store/cjs/use-sync-external-store-with-selector.development.js");
}

/***/ },

/***/ "react"
/*!************************!*\
  !*** external "React" ***!
  \************************/
(module) {

module.exports = window["React"];

/***/ },

/***/ "react/jsx-runtime"
/*!**********************************!*\
  !*** external "ReactJSXRuntime" ***!
  \**********************************/
(module) {

module.exports = window["ReactJSXRuntime"];

/***/ },

/***/ "@wordpress/element"
/*!*********************************!*\
  !*** external ["wp","element"] ***!
  \*********************************/
(module) {

module.exports = window["wp"]["element"];

/***/ },

/***/ "@wordpress/i18n"
/*!******************************!*\
  !*** external ["wp","i18n"] ***!
  \******************************/
(module) {

module.exports = window["wp"]["i18n"];

/***/ },

/***/ "./node_modules/@reduxjs/toolkit/dist/query/react/rtk-query-react.modern.mjs"
/*!***********************************************************************************!*\
  !*** ./node_modules/@reduxjs/toolkit/dist/query/react/rtk-query-react.modern.mjs ***!
  \***********************************************************************************/
(__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   ApiProvider: () => (/* binding */ ApiProvider),
/* harmony export */   NamedSchemaError: () => (/* reexport safe */ _reduxjs_toolkit_query__WEBPACK_IMPORTED_MODULE_0__.NamedSchemaError),
/* harmony export */   QueryStatus: () => (/* reexport safe */ _reduxjs_toolkit_query__WEBPACK_IMPORTED_MODULE_0__.QueryStatus),
/* harmony export */   UNINITIALIZED_VALUE: () => (/* binding */ UNINITIALIZED_VALUE),
/* harmony export */   _NEVER: () => (/* reexport safe */ _reduxjs_toolkit_query__WEBPACK_IMPORTED_MODULE_0__._NEVER),
/* harmony export */   buildCreateApi: () => (/* reexport safe */ _reduxjs_toolkit_query__WEBPACK_IMPORTED_MODULE_0__.buildCreateApi),
/* harmony export */   copyWithStructuralSharing: () => (/* reexport safe */ _reduxjs_toolkit_query__WEBPACK_IMPORTED_MODULE_0__.copyWithStructuralSharing),
/* harmony export */   coreModule: () => (/* reexport safe */ _reduxjs_toolkit_query__WEBPACK_IMPORTED_MODULE_0__.coreModule),
/* harmony export */   coreModuleName: () => (/* reexport safe */ _reduxjs_toolkit_query__WEBPACK_IMPORTED_MODULE_0__.coreModuleName),
/* harmony export */   createApi: () => (/* binding */ createApi),
/* harmony export */   defaultSerializeQueryArgs: () => (/* reexport safe */ _reduxjs_toolkit_query__WEBPACK_IMPORTED_MODULE_0__.defaultSerializeQueryArgs),
/* harmony export */   fakeBaseQuery: () => (/* reexport safe */ _reduxjs_toolkit_query__WEBPACK_IMPORTED_MODULE_0__.fakeBaseQuery),
/* harmony export */   fetchBaseQuery: () => (/* reexport safe */ _reduxjs_toolkit_query__WEBPACK_IMPORTED_MODULE_0__.fetchBaseQuery),
/* harmony export */   reactHooksModule: () => (/* binding */ reactHooksModule),
/* harmony export */   reactHooksModuleName: () => (/* binding */ reactHooksModuleName),
/* harmony export */   retry: () => (/* reexport safe */ _reduxjs_toolkit_query__WEBPACK_IMPORTED_MODULE_0__.retry),
/* harmony export */   setupListeners: () => (/* reexport safe */ _reduxjs_toolkit_query__WEBPACK_IMPORTED_MODULE_0__.setupListeners),
/* harmony export */   skipToken: () => (/* reexport safe */ _reduxjs_toolkit_query__WEBPACK_IMPORTED_MODULE_0__.skipToken)
/* harmony export */ });
/* harmony import */ var _reduxjs_toolkit_query__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @reduxjs/toolkit/query */ "./node_modules/@reduxjs/toolkit/dist/query/rtk-query.modern.mjs");
/* harmony import */ var react_redux__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! react-redux */ "./node_modules/react-redux/dist/react-redux.mjs");
/* harmony import */ var reselect__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! reselect */ "./node_modules/reselect/dist/reselect.mjs");
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! react */ "react");
/* harmony import */ var _reduxjs_toolkit__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! @reduxjs/toolkit */ "./node_modules/@reduxjs/toolkit/dist/redux-toolkit.modern.mjs");
// src/query/react/rtkqImports.ts

// src/query/react/module.ts

// src/query/utils/capitalize.ts
function capitalize(str) {
  return str.replace(str[0], str[0].toUpperCase());
}

// src/query/utils/countObjectKeys.ts
function countObjectKeys(obj) {
  let count = 0;
  for (const _key in obj) {
    count++;
  }
  return count;
}

// src/query/endpointDefinitions.ts
var ENDPOINT_QUERY = "query" /* query */;
var ENDPOINT_MUTATION = "mutation" /* mutation */;
var ENDPOINT_INFINITEQUERY = "infinitequery" /* infinitequery */;
function isQueryDefinition(e) {
  return e.type === ENDPOINT_QUERY;
}
function isMutationDefinition(e) {
  return e.type === ENDPOINT_MUTATION;
}
function isInfiniteQueryDefinition(e) {
  return e.type === ENDPOINT_INFINITEQUERY;
}

// src/query/tsHelpers.ts
function safeAssign(target, ...args) {
  return Object.assign(target, ...args);
}

// src/query/react/buildHooks.ts

// src/query/react/reactImports.ts

// src/query/react/reactReduxImports.ts

// src/query/react/constants.ts
var UNINITIALIZED_VALUE = Symbol();

// src/query/react/useSerializedStableValue.ts
function useStableQueryArgs(queryArgs) {
  const cache = (0,react__WEBPACK_IMPORTED_MODULE_3__.useRef)(queryArgs);
  const copy = (0,react__WEBPACK_IMPORTED_MODULE_3__.useMemo)(() => (0,_reduxjs_toolkit_query__WEBPACK_IMPORTED_MODULE_0__.copyWithStructuralSharing)(cache.current, queryArgs), [queryArgs]);
  (0,react__WEBPACK_IMPORTED_MODULE_3__.useEffect)(() => {
    if (cache.current !== copy) {
      cache.current = copy;
    }
  }, [copy]);
  return copy;
}

// src/query/react/useShallowStableValue.ts
function useShallowStableValue(value) {
  const cache = (0,react__WEBPACK_IMPORTED_MODULE_3__.useRef)(value);
  (0,react__WEBPACK_IMPORTED_MODULE_3__.useEffect)(() => {
    if (!(0,react_redux__WEBPACK_IMPORTED_MODULE_1__.shallowEqual)(cache.current, value)) {
      cache.current = value;
    }
  }, [value]);
  return (0,react_redux__WEBPACK_IMPORTED_MODULE_1__.shallowEqual)(cache.current, value) ? cache.current : value;
}

// src/query/react/buildHooks.ts
var canUseDOM = () => !!(typeof window !== "undefined" && typeof window.document !== "undefined" && typeof window.document.createElement !== "undefined");
var isDOM = /* @__PURE__ */ canUseDOM();
var isRunningInReactNative = () => typeof navigator !== "undefined" && navigator.product === "ReactNative";
var isReactNative = /* @__PURE__ */ isRunningInReactNative();
var getUseIsomorphicLayoutEffect = () => isDOM || isReactNative ? react__WEBPACK_IMPORTED_MODULE_3__.useLayoutEffect : react__WEBPACK_IMPORTED_MODULE_3__.useEffect;
var useIsomorphicLayoutEffect = /* @__PURE__ */ getUseIsomorphicLayoutEffect();
var noPendingQueryStateSelector = (selected) => {
  if (selected.isUninitialized) {
    return {
      ...selected,
      isUninitialized: false,
      isFetching: true,
      isLoading: selected.data !== void 0 ? false : true,
      // This is the one place where we still have to use `QueryStatus` as an enum,
      // since it's the only reference in the React package and not in the core.
      status: _reduxjs_toolkit_query__WEBPACK_IMPORTED_MODULE_0__.QueryStatus.pending
    };
  }
  return selected;
};
function pick(obj, ...keys) {
  const ret = {};
  keys.forEach((key) => {
    ret[key] = obj[key];
  });
  return ret;
}
var COMMON_HOOK_DEBUG_FIELDS = ["data", "status", "isLoading", "isSuccess", "isError", "error"];
function buildHooks({
  api,
  moduleOptions: {
    batch,
    hooks: {
      useDispatch,
      useSelector,
      useStore
    },
    unstable__sideEffectsInRender,
    createSelector
  },
  serializeQueryArgs,
  context
}) {
  const usePossiblyImmediateEffect = unstable__sideEffectsInRender ? (cb) => cb() : react__WEBPACK_IMPORTED_MODULE_3__.useEffect;
  const unsubscribePromiseRef = (ref) => ref.current?.unsubscribe?.();
  const endpointDefinitions = context.endpointDefinitions;
  return {
    buildQueryHooks,
    buildInfiniteQueryHooks,
    buildMutationHook,
    usePrefetch
  };
  function queryStatePreSelector(currentState, lastResult, queryArgs) {
    if (lastResult?.endpointName && currentState.isUninitialized) {
      const {
        endpointName
      } = lastResult;
      const endpointDefinition = endpointDefinitions[endpointName];
      if (queryArgs !== _reduxjs_toolkit_query__WEBPACK_IMPORTED_MODULE_0__.skipToken && serializeQueryArgs({
        queryArgs: lastResult.originalArgs,
        endpointDefinition,
        endpointName
      }) === serializeQueryArgs({
        queryArgs,
        endpointDefinition,
        endpointName
      })) lastResult = void 0;
    }
    let data = currentState.isSuccess ? currentState.data : lastResult?.data;
    if (data === void 0) data = currentState.data;
    const hasData = data !== void 0;
    const isFetching = currentState.isLoading;
    const isLoading = (!lastResult || lastResult.isLoading || lastResult.isUninitialized) && !hasData && isFetching;
    const isSuccess = currentState.isSuccess || hasData && (isFetching && !lastResult?.isError || currentState.isUninitialized);
    return {
      ...currentState,
      data,
      currentData: currentState.data,
      isFetching,
      isLoading,
      isSuccess
    };
  }
  function infiniteQueryStatePreSelector(currentState, lastResult, queryArgs) {
    if (lastResult?.endpointName && currentState.isUninitialized) {
      const {
        endpointName
      } = lastResult;
      const endpointDefinition = endpointDefinitions[endpointName];
      if (queryArgs !== _reduxjs_toolkit_query__WEBPACK_IMPORTED_MODULE_0__.skipToken && serializeQueryArgs({
        queryArgs: lastResult.originalArgs,
        endpointDefinition,
        endpointName
      }) === serializeQueryArgs({
        queryArgs,
        endpointDefinition,
        endpointName
      })) lastResult = void 0;
    }
    let data = currentState.isSuccess ? currentState.data : lastResult?.data;
    if (data === void 0) data = currentState.data;
    const hasData = data !== void 0;
    const isFetching = currentState.isLoading;
    const isLoading = (!lastResult || lastResult.isLoading || lastResult.isUninitialized) && !hasData && isFetching;
    const isSuccess = currentState.isSuccess || isFetching && hasData;
    return {
      ...currentState,
      data,
      currentData: currentState.data,
      isFetching,
      isLoading,
      isSuccess
    };
  }
  function usePrefetch(endpointName, defaultOptions) {
    const dispatch = useDispatch();
    const stableDefaultOptions = useShallowStableValue(defaultOptions);
    return (0,react__WEBPACK_IMPORTED_MODULE_3__.useCallback)((arg, options) => dispatch(api.util.prefetch(endpointName, arg, {
      ...stableDefaultOptions,
      ...options
    })), [endpointName, dispatch, stableDefaultOptions]);
  }
  function useQuerySubscriptionCommonImpl(endpointName, arg, {
    refetchOnReconnect,
    refetchOnFocus,
    refetchOnMountOrArgChange,
    skip = false,
    pollingInterval = 0,
    skipPollingIfUnfocused = false,
    ...rest
  } = {}) {
    const {
      initiate
    } = api.endpoints[endpointName];
    const dispatch = useDispatch();
    const subscriptionSelectorsRef = (0,react__WEBPACK_IMPORTED_MODULE_3__.useRef)(void 0);
    if (!subscriptionSelectorsRef.current) {
      const returnedValue = dispatch(api.internalActions.internal_getRTKQSubscriptions());
      if (true) {
        if (typeof returnedValue !== "object" || typeof returnedValue?.type === "string") {
          throw new Error( false ? 0 : `Warning: Middleware for RTK-Query API at reducerPath "${api.reducerPath}" has not been added to the store.
    You must add the middleware for RTK-Query to function correctly!`);
        }
      }
      subscriptionSelectorsRef.current = returnedValue;
    }
    const stableArg = useStableQueryArgs(skip ? _reduxjs_toolkit_query__WEBPACK_IMPORTED_MODULE_0__.skipToken : arg);
    const stableSubscriptionOptions = useShallowStableValue({
      refetchOnReconnect,
      refetchOnFocus,
      pollingInterval,
      skipPollingIfUnfocused
    });
    const initialPageParam = rest.initialPageParam;
    const stableInitialPageParam = useShallowStableValue(initialPageParam);
    const refetchCachedPages = rest.refetchCachedPages;
    const stableRefetchCachedPages = useShallowStableValue(refetchCachedPages);
    const promiseRef = (0,react__WEBPACK_IMPORTED_MODULE_3__.useRef)(void 0);
    let {
      queryCacheKey,
      requestId
    } = promiseRef.current || {};
    let currentRenderHasSubscription = false;
    if (queryCacheKey && requestId) {
      currentRenderHasSubscription = subscriptionSelectorsRef.current.isRequestSubscribed(queryCacheKey, requestId);
    }
    const subscriptionRemoved = !currentRenderHasSubscription && promiseRef.current !== void 0;
    usePossiblyImmediateEffect(() => {
      if (subscriptionRemoved) {
        promiseRef.current = void 0;
      }
    }, [subscriptionRemoved]);
    usePossiblyImmediateEffect(() => {
      const lastPromise = promiseRef.current;
      if (typeof process !== "undefined" && "development" === "removeMeOnCompilation") // removed by dead control flow
{}
      if (stableArg === _reduxjs_toolkit_query__WEBPACK_IMPORTED_MODULE_0__.skipToken) {
        lastPromise?.unsubscribe();
        promiseRef.current = void 0;
        return;
      }
      const lastSubscriptionOptions = promiseRef.current?.subscriptionOptions;
      if (!lastPromise || lastPromise.arg !== stableArg) {
        lastPromise?.unsubscribe();
        const promise = dispatch(initiate(stableArg, {
          subscriptionOptions: stableSubscriptionOptions,
          forceRefetch: refetchOnMountOrArgChange,
          ...isInfiniteQueryDefinition(endpointDefinitions[endpointName]) ? {
            initialPageParam: stableInitialPageParam,
            refetchCachedPages: stableRefetchCachedPages
          } : {}
        }));
        promiseRef.current = promise;
      } else if (stableSubscriptionOptions !== lastSubscriptionOptions) {
        lastPromise.updateSubscriptionOptions(stableSubscriptionOptions);
      }
    }, [dispatch, initiate, refetchOnMountOrArgChange, stableArg, stableSubscriptionOptions, subscriptionRemoved, stableInitialPageParam, stableRefetchCachedPages, endpointName]);
    return [promiseRef, dispatch, initiate, stableSubscriptionOptions];
  }
  function buildUseQueryState(endpointName, preSelector) {
    const useQueryState = (arg, {
      skip = false,
      selectFromResult
    } = {}) => {
      const {
        select
      } = api.endpoints[endpointName];
      const stableArg = useStableQueryArgs(skip ? _reduxjs_toolkit_query__WEBPACK_IMPORTED_MODULE_0__.skipToken : arg);
      const lastValue = (0,react__WEBPACK_IMPORTED_MODULE_3__.useRef)(void 0);
      const selectDefaultResult = (0,react__WEBPACK_IMPORTED_MODULE_3__.useMemo)(() => (
        // Normally ts-ignores are bad and should be avoided, but we're
        // already casting this selector to be `Selector<any>` anyway,
        // so the inconsistencies don't matter here
        // @ts-ignore
        createSelector([
          // @ts-ignore
          select(stableArg),
          (_, lastResult) => lastResult,
          (_) => stableArg
        ], preSelector, {
          memoizeOptions: {
            resultEqualityCheck: react_redux__WEBPACK_IMPORTED_MODULE_1__.shallowEqual
          }
        })
      ), [select, stableArg]);
      const querySelector = (0,react__WEBPACK_IMPORTED_MODULE_3__.useMemo)(() => selectFromResult ? createSelector([selectDefaultResult], selectFromResult, {
        devModeChecks: {
          identityFunctionCheck: "never"
        }
      }) : selectDefaultResult, [selectDefaultResult, selectFromResult]);
      const currentState = useSelector((state) => querySelector(state, lastValue.current), react_redux__WEBPACK_IMPORTED_MODULE_1__.shallowEqual);
      const store = useStore();
      const newLastValue = selectDefaultResult(store.getState(), lastValue.current);
      useIsomorphicLayoutEffect(() => {
        lastValue.current = newLastValue;
      }, [newLastValue]);
      return currentState;
    };
    return useQueryState;
  }
  function usePromiseRefUnsubscribeOnUnmount(promiseRef) {
    (0,react__WEBPACK_IMPORTED_MODULE_3__.useEffect)(() => {
      return () => {
        unsubscribePromiseRef(promiseRef);
        promiseRef.current = void 0;
      };
    }, [promiseRef]);
  }
  function refetchOrErrorIfUnmounted(promiseRef) {
    if (!promiseRef.current) throw new Error( false ? 0 : "Cannot refetch a query that has not been started yet.");
    return promiseRef.current.refetch();
  }
  function buildQueryHooks(endpointName) {
    const useQuerySubscription = (arg, options = {}) => {
      const [promiseRef] = useQuerySubscriptionCommonImpl(endpointName, arg, options);
      usePromiseRefUnsubscribeOnUnmount(promiseRef);
      return (0,react__WEBPACK_IMPORTED_MODULE_3__.useMemo)(() => ({
        /**
         * A method to manually refetch data for the query
         */
        refetch: () => refetchOrErrorIfUnmounted(promiseRef)
      }), [promiseRef]);
    };
    const useLazyQuerySubscription = ({
      refetchOnReconnect,
      refetchOnFocus,
      pollingInterval = 0,
      skipPollingIfUnfocused = false
    } = {}) => {
      const {
        initiate
      } = api.endpoints[endpointName];
      const dispatch = useDispatch();
      const [arg, setArg] = (0,react__WEBPACK_IMPORTED_MODULE_3__.useState)(UNINITIALIZED_VALUE);
      const promiseRef = (0,react__WEBPACK_IMPORTED_MODULE_3__.useRef)(void 0);
      const stableSubscriptionOptions = useShallowStableValue({
        refetchOnReconnect,
        refetchOnFocus,
        pollingInterval,
        skipPollingIfUnfocused
      });
      usePossiblyImmediateEffect(() => {
        const lastSubscriptionOptions = promiseRef.current?.subscriptionOptions;
        if (stableSubscriptionOptions !== lastSubscriptionOptions) {
          promiseRef.current?.updateSubscriptionOptions(stableSubscriptionOptions);
        }
      }, [stableSubscriptionOptions]);
      const subscriptionOptionsRef = (0,react__WEBPACK_IMPORTED_MODULE_3__.useRef)(stableSubscriptionOptions);
      usePossiblyImmediateEffect(() => {
        subscriptionOptionsRef.current = stableSubscriptionOptions;
      }, [stableSubscriptionOptions]);
      const trigger = (0,react__WEBPACK_IMPORTED_MODULE_3__.useCallback)(function(arg2, preferCacheValue = false) {
        let promise;
        batch(() => {
          unsubscribePromiseRef(promiseRef);
          promiseRef.current = promise = dispatch(initiate(arg2, {
            subscriptionOptions: subscriptionOptionsRef.current,
            forceRefetch: !preferCacheValue
          }));
          setArg(arg2);
        });
        return promise;
      }, [dispatch, initiate]);
      const reset = (0,react__WEBPACK_IMPORTED_MODULE_3__.useCallback)(() => {
        if (promiseRef.current?.queryCacheKey) {
          dispatch(api.internalActions.removeQueryResult({
            queryCacheKey: promiseRef.current?.queryCacheKey
          }));
        }
      }, [dispatch]);
      (0,react__WEBPACK_IMPORTED_MODULE_3__.useEffect)(() => {
        return () => {
          unsubscribePromiseRef(promiseRef);
        };
      }, []);
      (0,react__WEBPACK_IMPORTED_MODULE_3__.useEffect)(() => {
        if (arg !== UNINITIALIZED_VALUE && !promiseRef.current) {
          trigger(arg, true);
        }
      }, [arg, trigger]);
      return (0,react__WEBPACK_IMPORTED_MODULE_3__.useMemo)(() => [trigger, arg, {
        reset
      }], [trigger, arg, reset]);
    };
    const useQueryState = buildUseQueryState(endpointName, queryStatePreSelector);
    return {
      useQueryState,
      useQuerySubscription,
      useLazyQuerySubscription,
      useLazyQuery(options) {
        const [trigger, arg, {
          reset
        }] = useLazyQuerySubscription(options);
        const queryStateResults = useQueryState(arg, {
          ...options,
          skip: arg === UNINITIALIZED_VALUE
        });
        const info = (0,react__WEBPACK_IMPORTED_MODULE_3__.useMemo)(() => ({
          lastArg: arg
        }), [arg]);
        return (0,react__WEBPACK_IMPORTED_MODULE_3__.useMemo)(() => [trigger, {
          ...queryStateResults,
          reset
        }, info], [trigger, queryStateResults, reset, info]);
      },
      useQuery(arg, options) {
        const querySubscriptionResults = useQuerySubscription(arg, options);
        const queryStateResults = useQueryState(arg, {
          selectFromResult: arg === _reduxjs_toolkit_query__WEBPACK_IMPORTED_MODULE_0__.skipToken || options?.skip ? void 0 : noPendingQueryStateSelector,
          ...options
        });
        const debugValue = pick(queryStateResults, ...COMMON_HOOK_DEBUG_FIELDS);
        (0,react__WEBPACK_IMPORTED_MODULE_3__.useDebugValue)(debugValue);
        return (0,react__WEBPACK_IMPORTED_MODULE_3__.useMemo)(() => ({
          ...queryStateResults,
          ...querySubscriptionResults
        }), [queryStateResults, querySubscriptionResults]);
      }
    };
  }
  function buildInfiniteQueryHooks(endpointName) {
    const useInfiniteQuerySubscription = (arg, options = {}) => {
      const [promiseRef, dispatch, initiate, stableSubscriptionOptions] = useQuerySubscriptionCommonImpl(endpointName, arg, options);
      const subscriptionOptionsRef = (0,react__WEBPACK_IMPORTED_MODULE_3__.useRef)(stableSubscriptionOptions);
      usePossiblyImmediateEffect(() => {
        subscriptionOptionsRef.current = stableSubscriptionOptions;
      }, [stableSubscriptionOptions]);
      const hookRefetchCachedPages = options.refetchCachedPages;
      const stableHookRefetchCachedPages = useShallowStableValue(hookRefetchCachedPages);
      const trigger = (0,react__WEBPACK_IMPORTED_MODULE_3__.useCallback)(function(arg2, direction) {
        let promise;
        batch(() => {
          unsubscribePromiseRef(promiseRef);
          promiseRef.current = promise = dispatch(initiate(arg2, {
            subscriptionOptions: subscriptionOptionsRef.current,
            direction
          }));
        });
        return promise;
      }, [promiseRef, dispatch, initiate]);
      usePromiseRefUnsubscribeOnUnmount(promiseRef);
      const stableArg = useStableQueryArgs(options.skip ? _reduxjs_toolkit_query__WEBPACK_IMPORTED_MODULE_0__.skipToken : arg);
      const refetch = (0,react__WEBPACK_IMPORTED_MODULE_3__.useCallback)((options2) => {
        if (!promiseRef.current) throw new Error( false ? 0 : "Cannot refetch a query that has not been started yet.");
        const mergedOptions = {
          refetchCachedPages: options2?.refetchCachedPages ?? stableHookRefetchCachedPages
        };
        return promiseRef.current.refetch(mergedOptions);
      }, [promiseRef, stableHookRefetchCachedPages]);
      return (0,react__WEBPACK_IMPORTED_MODULE_3__.useMemo)(() => {
        const fetchNextPage = () => {
          return trigger(stableArg, "forward");
        };
        const fetchPreviousPage = () => {
          return trigger(stableArg, "backward");
        };
        return {
          trigger,
          /**
           * A method to manually refetch data for the query
           */
          refetch,
          fetchNextPage,
          fetchPreviousPage
        };
      }, [refetch, trigger, stableArg]);
    };
    const useInfiniteQueryState = buildUseQueryState(endpointName, infiniteQueryStatePreSelector);
    return {
      useInfiniteQueryState,
      useInfiniteQuerySubscription,
      useInfiniteQuery(arg, options) {
        const {
          refetch,
          fetchNextPage,
          fetchPreviousPage
        } = useInfiniteQuerySubscription(arg, options);
        const queryStateResults = useInfiniteQueryState(arg, {
          selectFromResult: arg === _reduxjs_toolkit_query__WEBPACK_IMPORTED_MODULE_0__.skipToken || options?.skip ? void 0 : noPendingQueryStateSelector,
          ...options
        });
        const debugValue = pick(queryStateResults, ...COMMON_HOOK_DEBUG_FIELDS, "hasNextPage", "hasPreviousPage");
        (0,react__WEBPACK_IMPORTED_MODULE_3__.useDebugValue)(debugValue);
        return (0,react__WEBPACK_IMPORTED_MODULE_3__.useMemo)(() => ({
          ...queryStateResults,
          fetchNextPage,
          fetchPreviousPage,
          refetch
        }), [queryStateResults, fetchNextPage, fetchPreviousPage, refetch]);
      }
    };
  }
  function buildMutationHook(name) {
    return ({
      selectFromResult,
      fixedCacheKey
    } = {}) => {
      const {
        select,
        initiate
      } = api.endpoints[name];
      const dispatch = useDispatch();
      const [promise, setPromise] = (0,react__WEBPACK_IMPORTED_MODULE_3__.useState)();
      (0,react__WEBPACK_IMPORTED_MODULE_3__.useEffect)(() => () => {
        if (!promise?.arg.fixedCacheKey) {
          promise?.reset();
        }
      }, [promise]);
      const triggerMutation = (0,react__WEBPACK_IMPORTED_MODULE_3__.useCallback)(function(arg) {
        const promise2 = dispatch(initiate(arg, {
          fixedCacheKey
        }));
        setPromise(promise2);
        return promise2;
      }, [dispatch, initiate, fixedCacheKey]);
      const {
        requestId
      } = promise || {};
      const selectDefaultResult = (0,react__WEBPACK_IMPORTED_MODULE_3__.useMemo)(() => select({
        fixedCacheKey,
        requestId: promise?.requestId
      }), [fixedCacheKey, promise, select]);
      const mutationSelector = (0,react__WEBPACK_IMPORTED_MODULE_3__.useMemo)(() => selectFromResult ? createSelector([selectDefaultResult], selectFromResult) : selectDefaultResult, [selectFromResult, selectDefaultResult]);
      const currentState = useSelector(mutationSelector, react_redux__WEBPACK_IMPORTED_MODULE_1__.shallowEqual);
      const originalArgs = fixedCacheKey == null ? promise?.arg.originalArgs : void 0;
      const reset = (0,react__WEBPACK_IMPORTED_MODULE_3__.useCallback)(() => {
        batch(() => {
          if (promise) {
            setPromise(void 0);
          }
          if (fixedCacheKey) {
            dispatch(api.internalActions.removeMutationResult({
              requestId,
              fixedCacheKey
            }));
          }
        });
      }, [dispatch, fixedCacheKey, promise, requestId]);
      const debugValue = pick(currentState, ...COMMON_HOOK_DEBUG_FIELDS, "endpointName");
      (0,react__WEBPACK_IMPORTED_MODULE_3__.useDebugValue)(debugValue);
      const finalState = (0,react__WEBPACK_IMPORTED_MODULE_3__.useMemo)(() => ({
        ...currentState,
        originalArgs,
        reset
      }), [currentState, originalArgs, reset]);
      return (0,react__WEBPACK_IMPORTED_MODULE_3__.useMemo)(() => [triggerMutation, finalState], [triggerMutation, finalState]);
    };
  }
}

// src/query/react/module.ts
var reactHooksModuleName = /* @__PURE__ */ Symbol();
var reactHooksModule = ({
  batch = react_redux__WEBPACK_IMPORTED_MODULE_1__.batch,
  hooks = {
    useDispatch: react_redux__WEBPACK_IMPORTED_MODULE_1__.useDispatch,
    useSelector: react_redux__WEBPACK_IMPORTED_MODULE_1__.useSelector,
    useStore: react_redux__WEBPACK_IMPORTED_MODULE_1__.useStore
  },
  createSelector = reselect__WEBPACK_IMPORTED_MODULE_2__.createSelector,
  unstable__sideEffectsInRender = false,
  ...rest
} = {}) => {
  if (true) {
    const hookNames = ["useDispatch", "useSelector", "useStore"];
    let warned = false;
    for (const hookName of hookNames) {
      if (countObjectKeys(rest) > 0) {
        if (rest[hookName]) {
          if (!warned) {
            console.warn("As of RTK 2.0, the hooks now need to be specified as one object, provided under a `hooks` key:\n`reactHooksModule({ hooks: { useDispatch, useSelector, useStore } })`");
            warned = true;
          }
        }
        hooks[hookName] = rest[hookName];
      }
      if (typeof hooks[hookName] !== "function") {
        throw new Error( false ? 0 : `When using custom hooks for context, all ${hookNames.length} hooks need to be provided: ${hookNames.join(", ")}.
Hook ${hookName} was either not provided or not a function.`);
      }
    }
  }
  return {
    name: reactHooksModuleName,
    init(api, {
      serializeQueryArgs
    }, context) {
      const anyApi = api;
      const {
        buildQueryHooks,
        buildInfiniteQueryHooks,
        buildMutationHook,
        usePrefetch
      } = buildHooks({
        api,
        moduleOptions: {
          batch,
          hooks,
          unstable__sideEffectsInRender,
          createSelector
        },
        serializeQueryArgs,
        context
      });
      safeAssign(anyApi, {
        usePrefetch
      });
      safeAssign(context, {
        batch
      });
      return {
        injectEndpoint(endpointName, definition) {
          if (isQueryDefinition(definition)) {
            const {
              useQuery,
              useLazyQuery,
              useLazyQuerySubscription,
              useQueryState,
              useQuerySubscription
            } = buildQueryHooks(endpointName);
            safeAssign(anyApi.endpoints[endpointName], {
              useQuery,
              useLazyQuery,
              useLazyQuerySubscription,
              useQueryState,
              useQuerySubscription
            });
            api[`use${capitalize(endpointName)}Query`] = useQuery;
            api[`useLazy${capitalize(endpointName)}Query`] = useLazyQuery;
          }
          if (isMutationDefinition(definition)) {
            const useMutation = buildMutationHook(endpointName);
            safeAssign(anyApi.endpoints[endpointName], {
              useMutation
            });
            api[`use${capitalize(endpointName)}Mutation`] = useMutation;
          } else if (isInfiniteQueryDefinition(definition)) {
            const {
              useInfiniteQuery,
              useInfiniteQuerySubscription,
              useInfiniteQueryState
            } = buildInfiniteQueryHooks(endpointName);
            safeAssign(anyApi.endpoints[endpointName], {
              useInfiniteQuery,
              useInfiniteQuerySubscription,
              useInfiniteQueryState
            });
            api[`use${capitalize(endpointName)}InfiniteQuery`] = useInfiniteQuery;
          }
        }
      };
    }
  };
};

// src/query/react/index.ts

// src/query/react/ApiProvider.tsx

function ApiProvider(props) {
  const context = props.context || react_redux__WEBPACK_IMPORTED_MODULE_1__.ReactReduxContext;
  const existingContext = (0,react__WEBPACK_IMPORTED_MODULE_3__.useContext)(context);
  if (existingContext) {
    throw new Error( false ? 0 : "Existing Redux context detected. If you already have a store set up, please use the traditional Redux setup.");
  }
  const [store] = react__WEBPACK_IMPORTED_MODULE_3__.useState(() => (0,_reduxjs_toolkit__WEBPACK_IMPORTED_MODULE_4__.configureStore)({
    reducer: {
      [props.api.reducerPath]: props.api.reducer
    },
    middleware: (gDM) => gDM().concat(props.api.middleware)
  }));
  (0,react__WEBPACK_IMPORTED_MODULE_3__.useEffect)(() => props.setupListeners === false ? void 0 : (0,_reduxjs_toolkit_query__WEBPACK_IMPORTED_MODULE_0__.setupListeners)(store.dispatch, props.setupListeners), [props.setupListeners, store.dispatch]);
  return /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_3__.createElement(react_redux__WEBPACK_IMPORTED_MODULE_1__.Provider, { store, context }, props.children);
}

// src/query/react/index.ts
var createApi = /* @__PURE__ */ (0,_reduxjs_toolkit_query__WEBPACK_IMPORTED_MODULE_0__.buildCreateApi)((0,_reduxjs_toolkit_query__WEBPACK_IMPORTED_MODULE_0__.coreModule)(), reactHooksModule());

//# sourceMappingURL=rtk-query-react.modern.mjs.map

/***/ },

/***/ "./node_modules/@reduxjs/toolkit/dist/query/rtk-query.modern.mjs"
/*!***********************************************************************!*\
  !*** ./node_modules/@reduxjs/toolkit/dist/query/rtk-query.modern.mjs ***!
  \***********************************************************************/
(__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   NamedSchemaError: () => (/* binding */ NamedSchemaError),
/* harmony export */   QueryStatus: () => (/* binding */ QueryStatus),
/* harmony export */   _NEVER: () => (/* binding */ _NEVER),
/* harmony export */   buildCreateApi: () => (/* binding */ buildCreateApi),
/* harmony export */   copyWithStructuralSharing: () => (/* binding */ copyWithStructuralSharing),
/* harmony export */   coreModule: () => (/* binding */ coreModule),
/* harmony export */   coreModuleName: () => (/* binding */ coreModuleName),
/* harmony export */   createApi: () => (/* binding */ createApi),
/* harmony export */   defaultSerializeQueryArgs: () => (/* binding */ defaultSerializeQueryArgs),
/* harmony export */   fakeBaseQuery: () => (/* binding */ fakeBaseQuery),
/* harmony export */   fetchBaseQuery: () => (/* binding */ fetchBaseQuery),
/* harmony export */   retry: () => (/* binding */ retry),
/* harmony export */   setupListeners: () => (/* binding */ setupListeners),
/* harmony export */   skipToken: () => (/* binding */ skipToken)
/* harmony export */ });
/* harmony import */ var _reduxjs_toolkit__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @reduxjs/toolkit */ "./node_modules/@reduxjs/toolkit/dist/redux-toolkit.modern.mjs");
/* harmony import */ var _reduxjs_toolkit__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! @reduxjs/toolkit */ "./node_modules/redux/dist/redux.mjs");
/* harmony import */ var _reduxjs_toolkit__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! immer */ "./node_modules/immer/dist/immer.mjs");
/* harmony import */ var _reduxjs_toolkit__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! reselect */ "./node_modules/reselect/dist/reselect.mjs");
/* harmony import */ var _standard_schema_utils__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! @standard-schema/utils */ "./node_modules/@standard-schema/utils/dist/index.js");
// src/query/core/apiState.ts
var QueryStatus = /* @__PURE__ */ ((QueryStatus7) => {
  QueryStatus7["uninitialized"] = "uninitialized";
  QueryStatus7["pending"] = "pending";
  QueryStatus7["fulfilled"] = "fulfilled";
  QueryStatus7["rejected"] = "rejected";
  return QueryStatus7;
})(QueryStatus || {});
var STATUS_UNINITIALIZED = "uninitialized" /* uninitialized */;
var STATUS_PENDING = "pending" /* pending */;
var STATUS_FULFILLED = "fulfilled" /* fulfilled */;
var STATUS_REJECTED = "rejected" /* rejected */;
function getRequestStatusFlags(status) {
  return {
    status,
    isUninitialized: status === STATUS_UNINITIALIZED,
    isLoading: status === STATUS_PENDING,
    isSuccess: status === STATUS_FULFILLED,
    isError: status === STATUS_REJECTED
  };
}

// src/query/core/rtkImports.ts

// src/query/utils/copyWithStructuralSharing.ts
var isPlainObject2 = _reduxjs_toolkit__WEBPACK_IMPORTED_MODULE_1__.isPlainObject;
function copyWithStructuralSharing(oldObj, newObj) {
  if (oldObj === newObj || !(isPlainObject2(oldObj) && isPlainObject2(newObj) || Array.isArray(oldObj) && Array.isArray(newObj))) {
    return newObj;
  }
  const newKeys = Object.keys(newObj);
  const oldKeys = Object.keys(oldObj);
  let isSameObject = newKeys.length === oldKeys.length;
  const mergeObj = Array.isArray(newObj) ? [] : {};
  for (const key of newKeys) {
    mergeObj[key] = copyWithStructuralSharing(oldObj[key], newObj[key]);
    if (isSameObject) isSameObject = oldObj[key] === mergeObj[key];
  }
  return isSameObject ? oldObj : mergeObj;
}

// src/query/utils/filterMap.ts
function filterMap(array, predicate, mapper) {
  return array.reduce((acc, item, i) => {
    if (predicate(item, i)) {
      acc.push(mapper(item, i));
    }
    return acc;
  }, []).flat();
}

// src/query/utils/isAbsoluteUrl.ts
function isAbsoluteUrl(url) {
  return new RegExp(`(^|:)//`).test(url);
}

// src/query/utils/isDocumentVisible.ts
function isDocumentVisible() {
  if (typeof document === "undefined") {
    return true;
  }
  return document.visibilityState !== "hidden";
}

// src/query/utils/isNotNullish.ts
function isNotNullish(v) {
  return v != null;
}
function filterNullishValues(map) {
  return [...map?.values() ?? []].filter(isNotNullish);
}

// src/query/utils/isOnline.ts
function isOnline() {
  return typeof navigator === "undefined" ? true : navigator.onLine === void 0 ? true : navigator.onLine;
}

// src/query/utils/joinUrls.ts
var withoutTrailingSlash = (url) => url.replace(/\/$/, "");
var withoutLeadingSlash = (url) => url.replace(/^\//, "");
function joinUrls(base, url) {
  if (!base) {
    return url;
  }
  if (!url) {
    return base;
  }
  if (isAbsoluteUrl(url)) {
    return url;
  }
  const delimiter = base.endsWith("/") || !url.startsWith("?") ? "/" : "";
  base = withoutTrailingSlash(base);
  url = withoutLeadingSlash(url);
  return `${base}${delimiter}${url}`;
}

// src/query/utils/getOrInsert.ts
function getOrInsertComputed(map, key, compute) {
  if (map.has(key)) return map.get(key);
  return map.set(key, compute(key)).get(key);
}
var createNewMap = () => /* @__PURE__ */ new Map();

// src/query/utils/signals.ts
var timeoutSignal = (milliseconds) => {
  const abortController = new AbortController();
  setTimeout(() => {
    const message = "signal timed out";
    const name = "TimeoutError";
    abortController.abort(
      // some environments (React Native, Node) don't have DOMException
      typeof DOMException !== "undefined" ? new DOMException(message, name) : Object.assign(new Error(message), {
        name
      })
    );
  }, milliseconds);
  return abortController.signal;
};
var anySignal = (...signals) => {
  for (const signal of signals) if (signal.aborted) return AbortSignal.abort(signal.reason);
  const abortController = new AbortController();
  for (const signal of signals) {
    signal.addEventListener("abort", () => abortController.abort(signal.reason), {
      signal: abortController.signal,
      once: true
    });
  }
  return abortController.signal;
};

// src/query/fetchBaseQuery.ts
var defaultFetchFn = (...args) => fetch(...args);
var defaultValidateStatus = (response) => response.status >= 200 && response.status <= 299;
var defaultIsJsonContentType = (headers) => (
  /*applicat*/
  /ion\/(vnd\.api\+)?json/.test(headers.get("content-type") || "")
);
function stripUndefined(obj) {
  if (!(0,_reduxjs_toolkit__WEBPACK_IMPORTED_MODULE_1__.isPlainObject)(obj)) {
    return obj;
  }
  const copy = {
    ...obj
  };
  for (const [k, v] of Object.entries(copy)) {
    if (v === void 0) delete copy[k];
  }
  return copy;
}
var isJsonifiable = (body) => typeof body === "object" && ((0,_reduxjs_toolkit__WEBPACK_IMPORTED_MODULE_1__.isPlainObject)(body) || Array.isArray(body) || typeof body.toJSON === "function");
function fetchBaseQuery({
  baseUrl,
  prepareHeaders = (x) => x,
  fetchFn = defaultFetchFn,
  paramsSerializer,
  isJsonContentType = defaultIsJsonContentType,
  jsonContentType = "application/json",
  jsonReplacer,
  timeout: defaultTimeout,
  responseHandler: globalResponseHandler,
  validateStatus: globalValidateStatus,
  ...baseFetchOptions
} = {}) {
  if (typeof fetch === "undefined" && fetchFn === defaultFetchFn) {
    console.warn("Warning: `fetch` is not available. Please supply a custom `fetchFn` property to use `fetchBaseQuery` on SSR environments.");
  }
  return async (arg, api, extraOptions) => {
    const {
      getState,
      extra,
      endpoint,
      forced,
      type
    } = api;
    let meta;
    let {
      url,
      headers = new Headers(baseFetchOptions.headers),
      params = void 0,
      responseHandler = globalResponseHandler ?? "json",
      validateStatus = globalValidateStatus ?? defaultValidateStatus,
      timeout = defaultTimeout,
      ...rest
    } = typeof arg == "string" ? {
      url: arg
    } : arg;
    let config = {
      ...baseFetchOptions,
      signal: timeout ? anySignal(api.signal, timeoutSignal(timeout)) : api.signal,
      ...rest
    };
    headers = new Headers(stripUndefined(headers));
    config.headers = await prepareHeaders(headers, {
      getState,
      arg,
      extra,
      endpoint,
      forced,
      type,
      extraOptions
    }) || headers;
    const bodyIsJsonifiable = isJsonifiable(config.body);
    if (config.body != null && !bodyIsJsonifiable && typeof config.body !== "string") {
      config.headers.delete("content-type");
    }
    if (!config.headers.has("content-type") && bodyIsJsonifiable) {
      config.headers.set("content-type", jsonContentType);
    }
    if (bodyIsJsonifiable && isJsonContentType(config.headers)) {
      config.body = JSON.stringify(config.body, jsonReplacer);
    }
    if (!config.headers.has("accept")) {
      if (responseHandler === "json") {
        config.headers.set("accept", "application/json");
      } else if (responseHandler === "text") {
        config.headers.set("accept", "text/plain, text/html, */*");
      }
    }
    if (params) {
      const divider = ~url.indexOf("?") ? "&" : "?";
      const query = paramsSerializer ? paramsSerializer(params) : new URLSearchParams(stripUndefined(params));
      url += divider + query;
    }
    url = joinUrls(baseUrl, url);
    const request = new Request(url, config);
    const requestClone = new Request(url, config);
    meta = {
      request: requestClone
    };
    let response;
    try {
      response = await fetchFn(request);
    } catch (e) {
      return {
        error: {
          status: (e instanceof Error || typeof DOMException !== "undefined" && e instanceof DOMException) && e.name === "TimeoutError" ? "TIMEOUT_ERROR" : "FETCH_ERROR",
          error: String(e)
        },
        meta
      };
    }
    const responseClone = response.clone();
    meta.response = responseClone;
    let resultData;
    let responseText = "";
    try {
      let handleResponseError;
      await Promise.all([
        handleResponse(response, responseHandler).then((r) => resultData = r, (e) => handleResponseError = e),
        // see https://github.com/node-fetch/node-fetch/issues/665#issuecomment-538995182
        // we *have* to "use up" both streams at the same time or they will stop running in node-fetch scenarios
        responseClone.text().then((r) => responseText = r, () => {
        })
      ]);
      if (handleResponseError) throw handleResponseError;
    } catch (e) {
      return {
        error: {
          status: "PARSING_ERROR",
          originalStatus: response.status,
          data: responseText,
          error: String(e)
        },
        meta
      };
    }
    return validateStatus(response, resultData) ? {
      data: resultData,
      meta
    } : {
      error: {
        status: response.status,
        data: resultData
      },
      meta
    };
  };
  async function handleResponse(response, responseHandler) {
    if (typeof responseHandler === "function") {
      return responseHandler(response);
    }
    if (responseHandler === "content-type") {
      responseHandler = isJsonContentType(response.headers) ? "json" : "text";
    }
    if (responseHandler === "json") {
      const text = await response.text();
      return text.length ? JSON.parse(text) : null;
    }
    return response.text();
  }
}

// src/query/HandledError.ts
var HandledError = class {
  constructor(value, meta = void 0) {
    this.value = value;
    this.meta = meta;
  }
};

// src/query/retry.ts
async function defaultBackoff(attempt = 0, maxRetries = 5, signal) {
  const attempts = Math.min(attempt, maxRetries);
  const timeout = ~~((Math.random() + 0.4) * (300 << attempts));
  await new Promise((resolve, reject) => {
    const timeoutId = setTimeout(() => resolve(), timeout);
    if (signal) {
      const abortHandler = () => {
        clearTimeout(timeoutId);
        reject(new Error("Aborted"));
      };
      if (signal.aborted) {
        clearTimeout(timeoutId);
        reject(new Error("Aborted"));
      } else {
        signal.addEventListener("abort", abortHandler, {
          once: true
        });
      }
    }
  });
}
function fail(error, meta) {
  throw Object.assign(new HandledError({
    error,
    meta
  }), {
    throwImmediately: true
  });
}
function failIfAborted(signal) {
  if (signal.aborted) {
    fail({
      status: "CUSTOM_ERROR",
      error: "Aborted"
    });
  }
}
var EMPTY_OPTIONS = {};
var retryWithBackoff = (baseQuery, defaultOptions) => async (args, api, extraOptions) => {
  const possibleMaxRetries = [5, (defaultOptions || EMPTY_OPTIONS).maxRetries, (extraOptions || EMPTY_OPTIONS).maxRetries].filter((x) => x !== void 0);
  const [maxRetries] = possibleMaxRetries.slice(-1);
  const defaultRetryCondition = (_, __, {
    attempt
  }) => attempt <= maxRetries;
  const options = {
    maxRetries,
    backoff: defaultBackoff,
    retryCondition: defaultRetryCondition,
    ...defaultOptions,
    ...extraOptions
  };
  let retry2 = 0;
  while (true) {
    failIfAborted(api.signal);
    try {
      const result = await baseQuery(args, api, extraOptions);
      if (result.error) {
        throw new HandledError(result);
      }
      return result;
    } catch (e) {
      retry2++;
      if (e.throwImmediately) {
        if (e instanceof HandledError) {
          return e.value;
        }
        throw e;
      }
      if (e instanceof HandledError) {
        if (!options.retryCondition(e.value.error, args, {
          attempt: retry2,
          baseQueryApi: api,
          extraOptions
        })) {
          return e.value;
        }
      } else {
        if (retry2 > options.maxRetries) {
          return {
            error: e
          };
        }
      }
      failIfAborted(api.signal);
      try {
        await options.backoff(retry2, options.maxRetries, api.signal);
      } catch (backoffError) {
        failIfAborted(api.signal);
        throw backoffError;
      }
    }
  }
};
var retry = /* @__PURE__ */ Object.assign(retryWithBackoff, {
  fail
});

// src/query/core/setupListeners.ts
var INTERNAL_PREFIX = "__rtkq/";
var ONLINE = "online";
var OFFLINE = "offline";
var FOCUS = "focus";
var FOCUSED = "focused";
var VISIBILITYCHANGE = "visibilitychange";
var onFocus = /* @__PURE__ */ (0,_reduxjs_toolkit__WEBPACK_IMPORTED_MODULE_0__.createAction)(`${INTERNAL_PREFIX}${FOCUSED}`);
var onFocusLost = /* @__PURE__ */ (0,_reduxjs_toolkit__WEBPACK_IMPORTED_MODULE_0__.createAction)(`${INTERNAL_PREFIX}un${FOCUSED}`);
var onOnline = /* @__PURE__ */ (0,_reduxjs_toolkit__WEBPACK_IMPORTED_MODULE_0__.createAction)(`${INTERNAL_PREFIX}${ONLINE}`);
var onOffline = /* @__PURE__ */ (0,_reduxjs_toolkit__WEBPACK_IMPORTED_MODULE_0__.createAction)(`${INTERNAL_PREFIX}${OFFLINE}`);
var actions = {
  onFocus,
  onFocusLost,
  onOnline,
  onOffline
};
var initialized = false;
function setupListeners(dispatch, customHandler) {
  function defaultHandler() {
    const [handleFocus, handleFocusLost, handleOnline, handleOffline] = [onFocus, onFocusLost, onOnline, onOffline].map((action) => () => dispatch(action()));
    const handleVisibilityChange = () => {
      if (window.document.visibilityState === "visible") {
        handleFocus();
      } else {
        handleFocusLost();
      }
    };
    let unsubscribe = () => {
      initialized = false;
    };
    if (!initialized) {
      if (typeof window !== "undefined" && window.addEventListener) {
        let updateListeners2 = function(add) {
          Object.entries(handlers).forEach(([event, handler]) => {
            if (add) {
              window.addEventListener(event, handler, false);
            } else {
              window.removeEventListener(event, handler);
            }
          });
        };
        var updateListeners = updateListeners2;
        const handlers = {
          [FOCUS]: handleFocus,
          [VISIBILITYCHANGE]: handleVisibilityChange,
          [ONLINE]: handleOnline,
          [OFFLINE]: handleOffline
        };
        updateListeners2(true);
        initialized = true;
        unsubscribe = () => {
          updateListeners2(false);
          initialized = false;
        };
      }
    }
    return unsubscribe;
  }
  return customHandler ? customHandler(dispatch, actions) : defaultHandler();
}

// src/query/endpointDefinitions.ts
var ENDPOINT_QUERY = "query" /* query */;
var ENDPOINT_MUTATION = "mutation" /* mutation */;
var ENDPOINT_INFINITEQUERY = "infinitequery" /* infinitequery */;
function isQueryDefinition(e) {
  return e.type === ENDPOINT_QUERY;
}
function isMutationDefinition(e) {
  return e.type === ENDPOINT_MUTATION;
}
function isInfiniteQueryDefinition(e) {
  return e.type === ENDPOINT_INFINITEQUERY;
}
function isAnyQueryDefinition(e) {
  return isQueryDefinition(e) || isInfiniteQueryDefinition(e);
}
function calculateProvidedBy(description, result, error, queryArg, meta, assertTagTypes) {
  const finalDescription = isFunction(description) ? description(result, error, queryArg, meta) : description;
  if (finalDescription) {
    return filterMap(finalDescription, isNotNullish, (tag) => assertTagTypes(expandTagDescription(tag)));
  }
  return [];
}
function isFunction(t) {
  return typeof t === "function";
}
function expandTagDescription(description) {
  return typeof description === "string" ? {
    type: description
  } : description;
}

// src/query/utils/immerImports.ts

// src/query/core/buildInitiate.ts

// src/tsHelpers.ts
function asSafePromise(promise, fallback) {
  return promise.catch(fallback);
}

// src/query/apiTypes.ts
var getEndpointDefinition = (context, endpointName) => context.endpointDefinitions[endpointName];

// src/query/core/buildInitiate.ts
var forceQueryFnSymbol = Symbol("forceQueryFn");
var isUpsertQuery = (arg) => typeof arg[forceQueryFnSymbol] === "function";
function buildInitiate({
  serializeQueryArgs,
  queryThunk,
  infiniteQueryThunk,
  mutationThunk,
  api,
  context,
  getInternalState
}) {
  const getRunningQueries = (dispatch) => getInternalState(dispatch)?.runningQueries;
  const getRunningMutations = (dispatch) => getInternalState(dispatch)?.runningMutations;
  const {
    unsubscribeQueryResult,
    removeMutationResult,
    updateSubscriptionOptions
  } = api.internalActions;
  return {
    buildInitiateQuery,
    buildInitiateInfiniteQuery,
    buildInitiateMutation,
    getRunningQueryThunk,
    getRunningMutationThunk,
    getRunningQueriesThunk,
    getRunningMutationsThunk
  };
  function getRunningQueryThunk(endpointName, queryArgs) {
    return (dispatch) => {
      const endpointDefinition = getEndpointDefinition(context, endpointName);
      const queryCacheKey = serializeQueryArgs({
        queryArgs,
        endpointDefinition,
        endpointName
      });
      return getRunningQueries(dispatch)?.get(queryCacheKey);
    };
  }
  function getRunningMutationThunk(_endpointName, fixedCacheKeyOrRequestId) {
    return (dispatch) => {
      return getRunningMutations(dispatch)?.get(fixedCacheKeyOrRequestId);
    };
  }
  function getRunningQueriesThunk() {
    return (dispatch) => filterNullishValues(getRunningQueries(dispatch));
  }
  function getRunningMutationsThunk() {
    return (dispatch) => filterNullishValues(getRunningMutations(dispatch));
  }
  function middlewareWarning(dispatch) {
    if (true) {
      if (middlewareWarning.triggered) return;
      const returnedValue = dispatch(api.internalActions.internal_getRTKQSubscriptions());
      middlewareWarning.triggered = true;
      if (typeof returnedValue !== "object" || typeof returnedValue?.type === "string") {
        throw new Error( false ? 0 : `Warning: Middleware for RTK-Query API at reducerPath "${api.reducerPath}" has not been added to the store.
You must add the middleware for RTK-Query to function correctly!`);
      }
    }
  }
  function buildInitiateAnyQuery(endpointName, endpointDefinition) {
    const queryAction = (arg, {
      subscribe = true,
      forceRefetch,
      subscriptionOptions,
      [forceQueryFnSymbol]: forceQueryFn,
      ...rest
    } = {}) => (dispatch, getState) => {
      const queryCacheKey = serializeQueryArgs({
        queryArgs: arg,
        endpointDefinition,
        endpointName
      });
      let thunk;
      const commonThunkArgs = {
        ...rest,
        type: ENDPOINT_QUERY,
        subscribe,
        forceRefetch,
        subscriptionOptions,
        endpointName,
        originalArgs: arg,
        queryCacheKey,
        [forceQueryFnSymbol]: forceQueryFn
      };
      if (isQueryDefinition(endpointDefinition)) {
        thunk = queryThunk(commonThunkArgs);
      } else {
        const {
          direction,
          initialPageParam,
          refetchCachedPages
        } = rest;
        thunk = infiniteQueryThunk({
          ...commonThunkArgs,
          // Supply these even if undefined. This helps with a field existence
          // check over in `buildSlice.ts`
          direction,
          initialPageParam,
          refetchCachedPages
        });
      }
      const selector = api.endpoints[endpointName].select(arg);
      const thunkResult = dispatch(thunk);
      const stateAfter = selector(getState());
      middlewareWarning(dispatch);
      const {
        requestId,
        abort
      } = thunkResult;
      const skippedSynchronously = stateAfter.requestId !== requestId;
      const runningQuery = getRunningQueries(dispatch)?.get(queryCacheKey);
      const selectFromState = () => selector(getState());
      const statePromise = Object.assign(forceQueryFn ? (
        // a query has been forced (upsertQueryData)
        // -> we want to resolve it once data has been written with the data that will be written
        thunkResult.then(selectFromState)
      ) : skippedSynchronously && !runningQuery ? (
        // a query has been skipped due to a condition and we do not have any currently running query
        // -> we want to resolve it immediately with the current data
        Promise.resolve(stateAfter)
      ) : (
        // query just started or one is already in flight
        // -> wait for the running query, then resolve with data from after that
        Promise.all([runningQuery, thunkResult]).then(selectFromState)
      ), {
        arg,
        requestId,
        subscriptionOptions,
        queryCacheKey,
        abort,
        async unwrap() {
          const result = await statePromise;
          if (result.isError) {
            throw result.error;
          }
          return result.data;
        },
        refetch: (options) => dispatch(queryAction(arg, {
          subscribe: false,
          forceRefetch: true,
          ...options
        })),
        unsubscribe() {
          if (subscribe) dispatch(unsubscribeQueryResult({
            queryCacheKey,
            requestId
          }));
        },
        updateSubscriptionOptions(options) {
          statePromise.subscriptionOptions = options;
          dispatch(updateSubscriptionOptions({
            endpointName,
            requestId,
            queryCacheKey,
            options
          }));
        }
      });
      if (!runningQuery && !skippedSynchronously && !forceQueryFn) {
        const runningQueries = getRunningQueries(dispatch);
        runningQueries.set(queryCacheKey, statePromise);
        statePromise.then(() => {
          runningQueries.delete(queryCacheKey);
        });
      }
      return statePromise;
    };
    return queryAction;
  }
  function buildInitiateQuery(endpointName, endpointDefinition) {
    const queryAction = buildInitiateAnyQuery(endpointName, endpointDefinition);
    return queryAction;
  }
  function buildInitiateInfiniteQuery(endpointName, endpointDefinition) {
    const infiniteQueryAction = buildInitiateAnyQuery(endpointName, endpointDefinition);
    return infiniteQueryAction;
  }
  function buildInitiateMutation(endpointName) {
    return (arg, {
      track = true,
      fixedCacheKey
    } = {}) => (dispatch, getState) => {
      const thunk = mutationThunk({
        type: "mutation",
        endpointName,
        originalArgs: arg,
        track,
        fixedCacheKey
      });
      const thunkResult = dispatch(thunk);
      middlewareWarning(dispatch);
      const {
        requestId,
        abort,
        unwrap
      } = thunkResult;
      const returnValuePromise = asSafePromise(thunkResult.unwrap().then((data) => ({
        data
      })), (error) => ({
        error
      }));
      const reset = () => {
        dispatch(removeMutationResult({
          requestId,
          fixedCacheKey
        }));
      };
      const ret = Object.assign(returnValuePromise, {
        arg: thunkResult.arg,
        requestId,
        abort,
        unwrap,
        reset
      });
      const runningMutations = getRunningMutations(dispatch);
      runningMutations.set(requestId, ret);
      ret.then(() => {
        runningMutations.delete(requestId);
      });
      if (fixedCacheKey) {
        runningMutations.set(fixedCacheKey, ret);
        ret.then(() => {
          if (runningMutations.get(fixedCacheKey) === ret) {
            runningMutations.delete(fixedCacheKey);
          }
        });
      }
      return ret;
    };
  }
}

// src/query/standardSchema.ts

var NamedSchemaError = class extends _standard_schema_utils__WEBPACK_IMPORTED_MODULE_4__.SchemaError {
  constructor(issues, value, schemaName, _bqMeta) {
    super(issues);
    this.value = value;
    this.schemaName = schemaName;
    this._bqMeta = _bqMeta;
  }
};
var shouldSkip = (skipSchemaValidation, schemaName) => Array.isArray(skipSchemaValidation) ? skipSchemaValidation.includes(schemaName) : !!skipSchemaValidation;
async function parseWithSchema(schema, data, schemaName, bqMeta) {
  const result = await schema["~standard"].validate(data);
  if (result.issues) {
    throw new NamedSchemaError(result.issues, data, schemaName, bqMeta);
  }
  return result.value;
}

// src/query/core/buildThunks.ts
function defaultTransformResponse(baseQueryReturnValue) {
  return baseQueryReturnValue;
}
var addShouldAutoBatch = (arg = {}) => {
  return {
    ...arg,
    [_reduxjs_toolkit__WEBPACK_IMPORTED_MODULE_0__.SHOULD_AUTOBATCH]: true
  };
};
function buildThunks({
  reducerPath,
  baseQuery,
  context: {
    endpointDefinitions
  },
  serializeQueryArgs,
  api,
  assertTagType,
  selectors,
  onSchemaFailure,
  catchSchemaFailure: globalCatchSchemaFailure,
  skipSchemaValidation: globalSkipSchemaValidation
}) {
  const patchQueryData = (endpointName, arg, patches, updateProvided) => (dispatch, getState) => {
    const endpointDefinition = endpointDefinitions[endpointName];
    const queryCacheKey = serializeQueryArgs({
      queryArgs: arg,
      endpointDefinition,
      endpointName
    });
    dispatch(api.internalActions.queryResultPatched({
      queryCacheKey,
      patches
    }));
    if (!updateProvided) {
      return;
    }
    const newValue = api.endpoints[endpointName].select(arg)(
      // Work around TS 4.1 mismatch
      getState()
    );
    const providedTags = calculateProvidedBy(endpointDefinition.providesTags, newValue.data, void 0, arg, {}, assertTagType);
    dispatch(api.internalActions.updateProvidedBy([{
      queryCacheKey,
      providedTags
    }]));
  };
  function addToStart(items, item, max = 0) {
    const newItems = [item, ...items];
    return max && newItems.length > max ? newItems.slice(0, -1) : newItems;
  }
  function addToEnd(items, item, max = 0) {
    const newItems = [...items, item];
    return max && newItems.length > max ? newItems.slice(1) : newItems;
  }
  const updateQueryData = (endpointName, arg, updateRecipe, updateProvided = true) => (dispatch, getState) => {
    const endpointDefinition = api.endpoints[endpointName];
    const currentState = endpointDefinition.select(arg)(
      // Work around TS 4.1 mismatch
      getState()
    );
    const ret = {
      patches: [],
      inversePatches: [],
      undo: () => dispatch(api.util.patchQueryData(endpointName, arg, ret.inversePatches, updateProvided))
    };
    if (currentState.status === STATUS_UNINITIALIZED) {
      return ret;
    }
    let newValue;
    if ("data" in currentState) {
      if ((0,_reduxjs_toolkit__WEBPACK_IMPORTED_MODULE_2__.isDraftable)(currentState.data)) {
        const [value, patches, inversePatches] = (0,_reduxjs_toolkit__WEBPACK_IMPORTED_MODULE_2__.produceWithPatches)(currentState.data, updateRecipe);
        ret.patches.push(...patches);
        ret.inversePatches.push(...inversePatches);
        newValue = value;
      } else {
        newValue = updateRecipe(currentState.data);
        ret.patches.push({
          op: "replace",
          path: [],
          value: newValue
        });
        ret.inversePatches.push({
          op: "replace",
          path: [],
          value: currentState.data
        });
      }
    }
    if (ret.patches.length === 0) {
      return ret;
    }
    dispatch(api.util.patchQueryData(endpointName, arg, ret.patches, updateProvided));
    return ret;
  };
  const upsertQueryData = (endpointName, arg, value) => (dispatch) => {
    const res = dispatch(api.endpoints[endpointName].initiate(arg, {
      subscribe: false,
      forceRefetch: true,
      [forceQueryFnSymbol]: () => ({
        data: value
      })
    }));
    return res;
  };
  const getTransformCallbackForEndpoint = (endpointDefinition, transformFieldName) => {
    return endpointDefinition.query && endpointDefinition[transformFieldName] ? endpointDefinition[transformFieldName] : defaultTransformResponse;
  };
  const executeEndpoint = async (arg, {
    signal,
    abort,
    rejectWithValue,
    fulfillWithValue,
    dispatch,
    getState,
    extra
  }) => {
    const endpointDefinition = endpointDefinitions[arg.endpointName];
    const {
      metaSchema,
      skipSchemaValidation = globalSkipSchemaValidation
    } = endpointDefinition;
    const isQuery = arg.type === ENDPOINT_QUERY;
    try {
      let transformResponse = defaultTransformResponse;
      const baseQueryApi = {
        signal,
        abort,
        dispatch,
        getState,
        extra,
        endpoint: arg.endpointName,
        type: arg.type,
        forced: isQuery ? isForcedQuery(arg, getState()) : void 0,
        queryCacheKey: isQuery ? arg.queryCacheKey : void 0
      };
      const forceQueryFn = isQuery ? arg[forceQueryFnSymbol] : void 0;
      let finalQueryReturnValue;
      const fetchPage = async (data, param, maxPages, previous) => {
        if (param == null && data.pages.length) {
          return Promise.resolve({
            data
          });
        }
        const finalQueryArg = {
          queryArg: arg.originalArgs,
          pageParam: param
        };
        const pageResponse = await executeRequest(finalQueryArg);
        const addTo = previous ? addToStart : addToEnd;
        return {
          data: {
            pages: addTo(data.pages, pageResponse.data, maxPages),
            pageParams: addTo(data.pageParams, param, maxPages)
          },
          meta: pageResponse.meta
        };
      };
      async function executeRequest(finalQueryArg) {
        let result;
        const {
          extraOptions,
          argSchema,
          rawResponseSchema,
          responseSchema
        } = endpointDefinition;
        if (argSchema && !shouldSkip(skipSchemaValidation, "arg")) {
          finalQueryArg = await parseWithSchema(
            argSchema,
            finalQueryArg,
            "argSchema",
            {}
            // we don't have a meta yet, so we can't pass it
          );
        }
        if (forceQueryFn) {
          result = forceQueryFn();
        } else if (endpointDefinition.query) {
          transformResponse = getTransformCallbackForEndpoint(endpointDefinition, "transformResponse");
          result = await baseQuery(endpointDefinition.query(finalQueryArg), baseQueryApi, extraOptions);
        } else {
          result = await endpointDefinition.queryFn(finalQueryArg, baseQueryApi, extraOptions, (arg2) => baseQuery(arg2, baseQueryApi, extraOptions));
        }
        if (typeof process !== "undefined" && "development" === "development") {
          const what = endpointDefinition.query ? "`baseQuery`" : "`queryFn`";
          let err;
          if (!result) {
            err = `${what} did not return anything.`;
          } else if (typeof result !== "object") {
            err = `${what} did not return an object.`;
          } else if (result.error && result.data) {
            err = `${what} returned an object containing both \`error\` and \`result\`.`;
          } else if (result.error === void 0 && result.data === void 0) {
            err = `${what} returned an object containing neither a valid \`error\` and \`result\`. At least one of them should not be \`undefined\``;
          } else {
            for (const key of Object.keys(result)) {
              if (key !== "error" && key !== "data" && key !== "meta") {
                err = `The object returned by ${what} has the unknown property ${key}.`;
                break;
              }
            }
          }
          if (err) {
            console.error(`Error encountered handling the endpoint ${arg.endpointName}.
                  ${err}
                  It needs to return an object with either the shape \`{ data: <value> }\` or \`{ error: <value> }\` that may contain an optional \`meta\` property.
                  Object returned was:`, result);
          }
        }
        if (result.error) throw new HandledError(result.error, result.meta);
        let {
          data
        } = result;
        if (rawResponseSchema && !shouldSkip(skipSchemaValidation, "rawResponse")) {
          data = await parseWithSchema(rawResponseSchema, result.data, "rawResponseSchema", result.meta);
        }
        let transformedResponse = await transformResponse(data, result.meta, finalQueryArg);
        if (responseSchema && !shouldSkip(skipSchemaValidation, "response")) {
          transformedResponse = await parseWithSchema(responseSchema, transformedResponse, "responseSchema", result.meta);
        }
        return {
          ...result,
          data: transformedResponse
        };
      }
      if (isQuery && "infiniteQueryOptions" in endpointDefinition) {
        const {
          infiniteQueryOptions
        } = endpointDefinition;
        const {
          maxPages = Infinity
        } = infiniteQueryOptions;
        const refetchCachedPages = arg.refetchCachedPages ?? infiniteQueryOptions.refetchCachedPages ?? true;
        let result;
        const blankData = {
          pages: [],
          pageParams: []
        };
        const cachedData = selectors.selectQueryEntry(getState(), arg.queryCacheKey)?.data;
        const isForcedQueryNeedingRefetch = (
          // arg.forceRefetch
          isForcedQuery(arg, getState()) && !arg.direction
        );
        const existingData = isForcedQueryNeedingRefetch || !cachedData ? blankData : cachedData;
        if ("direction" in arg && arg.direction && existingData.pages.length) {
          const previous = arg.direction === "backward";
          const pageParamFn = previous ? getPreviousPageParam : getNextPageParam;
          const param = pageParamFn(infiniteQueryOptions, existingData, arg.originalArgs);
          result = await fetchPage(existingData, param, maxPages, previous);
        } else {
          const {
            initialPageParam = infiniteQueryOptions.initialPageParam
          } = arg;
          const cachedPageParams = cachedData?.pageParams ?? [];
          const firstPageParam = cachedPageParams[0] ?? initialPageParam;
          const totalPages = cachedPageParams.length;
          result = await fetchPage(existingData, firstPageParam, maxPages);
          if (forceQueryFn) {
            result = {
              data: result.data.pages[0]
            };
          }
          if (refetchCachedPages) {
            for (let i = 1; i < totalPages; i++) {
              const param = getNextPageParam(infiniteQueryOptions, result.data, arg.originalArgs);
              result = await fetchPage(result.data, param, maxPages);
            }
          }
        }
        finalQueryReturnValue = result;
      } else {
        finalQueryReturnValue = await executeRequest(arg.originalArgs);
      }
      if (metaSchema && !shouldSkip(skipSchemaValidation, "meta") && finalQueryReturnValue.meta) {
        finalQueryReturnValue.meta = await parseWithSchema(metaSchema, finalQueryReturnValue.meta, "metaSchema", finalQueryReturnValue.meta);
      }
      return fulfillWithValue(finalQueryReturnValue.data, addShouldAutoBatch({
        fulfilledTimeStamp: Date.now(),
        baseQueryMeta: finalQueryReturnValue.meta
      }));
    } catch (error) {
      let caughtError = error;
      if (caughtError instanceof HandledError) {
        let transformErrorResponse = getTransformCallbackForEndpoint(endpointDefinition, "transformErrorResponse");
        const {
          rawErrorResponseSchema,
          errorResponseSchema
        } = endpointDefinition;
        let {
          value,
          meta
        } = caughtError;
        try {
          if (rawErrorResponseSchema && !shouldSkip(skipSchemaValidation, "rawErrorResponse")) {
            value = await parseWithSchema(rawErrorResponseSchema, value, "rawErrorResponseSchema", meta);
          }
          if (metaSchema && !shouldSkip(skipSchemaValidation, "meta")) {
            meta = await parseWithSchema(metaSchema, meta, "metaSchema", meta);
          }
          let transformedErrorResponse = await transformErrorResponse(value, meta, arg.originalArgs);
          if (errorResponseSchema && !shouldSkip(skipSchemaValidation, "errorResponse")) {
            transformedErrorResponse = await parseWithSchema(errorResponseSchema, transformedErrorResponse, "errorResponseSchema", meta);
          }
          return rejectWithValue(transformedErrorResponse, addShouldAutoBatch({
            baseQueryMeta: meta
          }));
        } catch (e) {
          caughtError = e;
        }
      }
      try {
        if (caughtError instanceof NamedSchemaError) {
          const info = {
            endpoint: arg.endpointName,
            arg: arg.originalArgs,
            type: arg.type,
            queryCacheKey: isQuery ? arg.queryCacheKey : void 0
          };
          endpointDefinition.onSchemaFailure?.(caughtError, info);
          onSchemaFailure?.(caughtError, info);
          const {
            catchSchemaFailure = globalCatchSchemaFailure
          } = endpointDefinition;
          if (catchSchemaFailure) {
            return rejectWithValue(catchSchemaFailure(caughtError, info), addShouldAutoBatch({
              baseQueryMeta: caughtError._bqMeta
            }));
          }
        }
      } catch (e) {
        caughtError = e;
      }
      if (typeof process !== "undefined" && "development" !== "production") {
        console.error(`An unhandled error occurred processing a request for the endpoint "${arg.endpointName}".
In the case of an unhandled error, no tags will be "provided" or "invalidated".`, caughtError);
      } else {
        console.error(caughtError);
      }
      throw caughtError;
    }
  };
  function isForcedQuery(arg, state) {
    const requestState = selectors.selectQueryEntry(state, arg.queryCacheKey);
    const baseFetchOnMountOrArgChange = selectors.selectConfig(state).refetchOnMountOrArgChange;
    const fulfilledVal = requestState?.fulfilledTimeStamp;
    const refetchVal = arg.forceRefetch ?? (arg.subscribe && baseFetchOnMountOrArgChange);
    if (refetchVal) {
      return refetchVal === true || (Number(/* @__PURE__ */ new Date()) - Number(fulfilledVal)) / 1e3 >= refetchVal;
    }
    return false;
  }
  const createQueryThunk = () => {
    const generatedQueryThunk = (0,_reduxjs_toolkit__WEBPACK_IMPORTED_MODULE_0__.createAsyncThunk)(`${reducerPath}/executeQuery`, executeEndpoint, {
      getPendingMeta({
        arg
      }) {
        const endpointDefinition = endpointDefinitions[arg.endpointName];
        return addShouldAutoBatch({
          startedTimeStamp: Date.now(),
          ...isInfiniteQueryDefinition(endpointDefinition) ? {
            direction: arg.direction
          } : {}
        });
      },
      condition(queryThunkArg, {
        getState
      }) {
        const state = getState();
        const requestState = selectors.selectQueryEntry(state, queryThunkArg.queryCacheKey);
        const fulfilledVal = requestState?.fulfilledTimeStamp;
        const currentArg = queryThunkArg.originalArgs;
        const previousArg = requestState?.originalArgs;
        const endpointDefinition = endpointDefinitions[queryThunkArg.endpointName];
        const direction = queryThunkArg.direction;
        if (isUpsertQuery(queryThunkArg)) {
          return true;
        }
        if (requestState?.status === "pending") {
          return false;
        }
        if (isForcedQuery(queryThunkArg, state)) {
          return true;
        }
        if (isQueryDefinition(endpointDefinition) && endpointDefinition?.forceRefetch?.({
          currentArg,
          previousArg,
          endpointState: requestState,
          state
        })) {
          return true;
        }
        if (fulfilledVal && !direction) {
          return false;
        }
        return true;
      },
      dispatchConditionRejection: true
    });
    return generatedQueryThunk;
  };
  const queryThunk = createQueryThunk();
  const infiniteQueryThunk = createQueryThunk();
  const mutationThunk = (0,_reduxjs_toolkit__WEBPACK_IMPORTED_MODULE_0__.createAsyncThunk)(`${reducerPath}/executeMutation`, executeEndpoint, {
    getPendingMeta() {
      return addShouldAutoBatch({
        startedTimeStamp: Date.now()
      });
    }
  });
  const hasTheForce = (options) => "force" in options;
  const hasMaxAge = (options) => "ifOlderThan" in options;
  const prefetch = (endpointName, arg, options = {}) => (dispatch, getState) => {
    const force = hasTheForce(options) && options.force;
    const maxAge = hasMaxAge(options) && options.ifOlderThan;
    const queryAction = (force2 = true) => {
      const options2 = {
        forceRefetch: force2,
        subscribe: false
      };
      return api.endpoints[endpointName].initiate(arg, options2);
    };
    const latestStateValue = api.endpoints[endpointName].select(arg)(getState());
    if (force) {
      dispatch(queryAction());
    } else if (maxAge) {
      const lastFulfilledTs = latestStateValue?.fulfilledTimeStamp;
      if (!lastFulfilledTs) {
        dispatch(queryAction());
        return;
      }
      const shouldRetrigger = (Number(/* @__PURE__ */ new Date()) - Number(new Date(lastFulfilledTs))) / 1e3 >= maxAge;
      if (shouldRetrigger) {
        dispatch(queryAction());
      }
    } else {
      dispatch(queryAction(false));
    }
  };
  function matchesEndpoint(endpointName) {
    return (action) => action?.meta?.arg?.endpointName === endpointName;
  }
  function buildMatchThunkActions(thunk, endpointName) {
    return {
      matchPending: (0,_reduxjs_toolkit__WEBPACK_IMPORTED_MODULE_0__.isAllOf)((0,_reduxjs_toolkit__WEBPACK_IMPORTED_MODULE_0__.isPending)(thunk), matchesEndpoint(endpointName)),
      matchFulfilled: (0,_reduxjs_toolkit__WEBPACK_IMPORTED_MODULE_0__.isAllOf)((0,_reduxjs_toolkit__WEBPACK_IMPORTED_MODULE_0__.isFulfilled)(thunk), matchesEndpoint(endpointName)),
      matchRejected: (0,_reduxjs_toolkit__WEBPACK_IMPORTED_MODULE_0__.isAllOf)((0,_reduxjs_toolkit__WEBPACK_IMPORTED_MODULE_0__.isRejected)(thunk), matchesEndpoint(endpointName))
    };
  }
  return {
    queryThunk,
    mutationThunk,
    infiniteQueryThunk,
    prefetch,
    updateQueryData,
    upsertQueryData,
    patchQueryData,
    buildMatchThunkActions
  };
}
function getNextPageParam(options, {
  pages,
  pageParams
}, queryArg) {
  const lastIndex = pages.length - 1;
  return options.getNextPageParam(pages[lastIndex], pages, pageParams[lastIndex], pageParams, queryArg);
}
function getPreviousPageParam(options, {
  pages,
  pageParams
}, queryArg) {
  return options.getPreviousPageParam?.(pages[0], pages, pageParams[0], pageParams, queryArg);
}
function calculateProvidedByThunk(action, type, endpointDefinitions, assertTagType) {
  return calculateProvidedBy(endpointDefinitions[action.meta.arg.endpointName][type], (0,_reduxjs_toolkit__WEBPACK_IMPORTED_MODULE_0__.isFulfilled)(action) ? action.payload : void 0, (0,_reduxjs_toolkit__WEBPACK_IMPORTED_MODULE_0__.isRejectedWithValue)(action) ? action.payload : void 0, action.meta.arg.originalArgs, "baseQueryMeta" in action.meta ? action.meta.baseQueryMeta : void 0, assertTagType);
}

// src/query/utils/getCurrent.ts
function getCurrent(value) {
  return (0,_reduxjs_toolkit__WEBPACK_IMPORTED_MODULE_2__.isDraft)(value) ? (0,_reduxjs_toolkit__WEBPACK_IMPORTED_MODULE_2__.current)(value) : value;
}

// src/query/core/buildSlice.ts
function updateQuerySubstateIfExists(state, queryCacheKey, update) {
  const substate = state[queryCacheKey];
  if (substate) {
    update(substate);
  }
}
function getMutationCacheKey(id) {
  return ("arg" in id ? id.arg.fixedCacheKey : id.fixedCacheKey) ?? id.requestId;
}
function updateMutationSubstateIfExists(state, id, update) {
  const substate = state[getMutationCacheKey(id)];
  if (substate) {
    update(substate);
  }
}
var initialState = {};
function buildSlice({
  reducerPath,
  queryThunk,
  mutationThunk,
  serializeQueryArgs,
  context: {
    endpointDefinitions: definitions,
    apiUid,
    extractRehydrationInfo,
    hasRehydrationInfo
  },
  assertTagType,
  config
}) {
  const resetApiState = (0,_reduxjs_toolkit__WEBPACK_IMPORTED_MODULE_0__.createAction)(`${reducerPath}/resetApiState`);
  function writePendingCacheEntry(draft, arg, upserting, meta) {
    draft[arg.queryCacheKey] ??= {
      status: STATUS_UNINITIALIZED,
      endpointName: arg.endpointName
    };
    updateQuerySubstateIfExists(draft, arg.queryCacheKey, (substate) => {
      substate.status = STATUS_PENDING;
      substate.requestId = upserting && substate.requestId ? (
        // for `upsertQuery` **updates**, keep the current `requestId`
        substate.requestId
      ) : (
        // for normal queries or `upsertQuery` **inserts** always update the `requestId`
        meta.requestId
      );
      if (arg.originalArgs !== void 0) {
        substate.originalArgs = arg.originalArgs;
      }
      substate.startedTimeStamp = meta.startedTimeStamp;
      const endpointDefinition = definitions[meta.arg.endpointName];
      if (isInfiniteQueryDefinition(endpointDefinition) && "direction" in arg) {
        ;
        substate.direction = arg.direction;
      }
    });
  }
  function writeFulfilledCacheEntry(draft, meta, payload, upserting) {
    updateQuerySubstateIfExists(draft, meta.arg.queryCacheKey, (substate) => {
      if (substate.requestId !== meta.requestId && !upserting) return;
      const {
        merge
      } = definitions[meta.arg.endpointName];
      substate.status = STATUS_FULFILLED;
      if (merge) {
        if (substate.data !== void 0) {
          const {
            fulfilledTimeStamp,
            arg,
            baseQueryMeta,
            requestId
          } = meta;
          let newData = (0,_reduxjs_toolkit__WEBPACK_IMPORTED_MODULE_2__.produce)(substate.data, (draftSubstateData) => {
            return merge(draftSubstateData, payload, {
              arg: arg.originalArgs,
              baseQueryMeta,
              fulfilledTimeStamp,
              requestId
            });
          });
          substate.data = newData;
        } else {
          substate.data = payload;
        }
      } else {
        substate.data = definitions[meta.arg.endpointName].structuralSharing ?? true ? copyWithStructuralSharing((0,_reduxjs_toolkit__WEBPACK_IMPORTED_MODULE_2__.isDraft)(substate.data) ? (0,_reduxjs_toolkit__WEBPACK_IMPORTED_MODULE_2__.original)(substate.data) : substate.data, payload) : payload;
      }
      delete substate.error;
      substate.fulfilledTimeStamp = meta.fulfilledTimeStamp;
    });
  }
  const querySlice = (0,_reduxjs_toolkit__WEBPACK_IMPORTED_MODULE_0__.createSlice)({
    name: `${reducerPath}/queries`,
    initialState,
    reducers: {
      removeQueryResult: {
        reducer(draft, {
          payload: {
            queryCacheKey
          }
        }) {
          delete draft[queryCacheKey];
        },
        prepare: (0,_reduxjs_toolkit__WEBPACK_IMPORTED_MODULE_0__.prepareAutoBatched)()
      },
      cacheEntriesUpserted: {
        reducer(draft, action) {
          for (const entry of action.payload) {
            const {
              queryDescription: arg,
              value
            } = entry;
            writePendingCacheEntry(draft, arg, true, {
              arg,
              requestId: action.meta.requestId,
              startedTimeStamp: action.meta.timestamp
            });
            writeFulfilledCacheEntry(
              draft,
              {
                arg,
                requestId: action.meta.requestId,
                fulfilledTimeStamp: action.meta.timestamp,
                baseQueryMeta: {}
              },
              value,
              // We know we're upserting here
              true
            );
          }
        },
        prepare: (payload) => {
          const queryDescriptions = payload.map((entry) => {
            const {
              endpointName,
              arg,
              value
            } = entry;
            const endpointDefinition = definitions[endpointName];
            const queryDescription = {
              type: ENDPOINT_QUERY,
              endpointName,
              originalArgs: entry.arg,
              queryCacheKey: serializeQueryArgs({
                queryArgs: arg,
                endpointDefinition,
                endpointName
              })
            };
            return {
              queryDescription,
              value
            };
          });
          const result = {
            payload: queryDescriptions,
            meta: {
              [_reduxjs_toolkit__WEBPACK_IMPORTED_MODULE_0__.SHOULD_AUTOBATCH]: true,
              requestId: (0,_reduxjs_toolkit__WEBPACK_IMPORTED_MODULE_0__.nanoid)(),
              timestamp: Date.now()
            }
          };
          return result;
        }
      },
      queryResultPatched: {
        reducer(draft, {
          payload: {
            queryCacheKey,
            patches
          }
        }) {
          updateQuerySubstateIfExists(draft, queryCacheKey, (substate) => {
            substate.data = (0,_reduxjs_toolkit__WEBPACK_IMPORTED_MODULE_2__.applyPatches)(substate.data, patches.concat());
          });
        },
        prepare: (0,_reduxjs_toolkit__WEBPACK_IMPORTED_MODULE_0__.prepareAutoBatched)()
      }
    },
    extraReducers(builder) {
      builder.addCase(queryThunk.pending, (draft, {
        meta,
        meta: {
          arg
        }
      }) => {
        const upserting = isUpsertQuery(arg);
        writePendingCacheEntry(draft, arg, upserting, meta);
      }).addCase(queryThunk.fulfilled, (draft, {
        meta,
        payload
      }) => {
        const upserting = isUpsertQuery(meta.arg);
        writeFulfilledCacheEntry(draft, meta, payload, upserting);
      }).addCase(queryThunk.rejected, (draft, {
        meta: {
          condition,
          arg,
          requestId
        },
        error,
        payload
      }) => {
        updateQuerySubstateIfExists(draft, arg.queryCacheKey, (substate) => {
          if (condition) {
          } else {
            if (substate.requestId !== requestId) return;
            substate.status = STATUS_REJECTED;
            substate.error = payload ?? error;
          }
        });
      }).addMatcher(hasRehydrationInfo, (draft, action) => {
        const {
          queries
        } = extractRehydrationInfo(action);
        for (const [key, entry] of Object.entries(queries)) {
          if (
            // do not rehydrate entries that were currently in flight.
            entry?.status === STATUS_FULFILLED || entry?.status === STATUS_REJECTED
          ) {
            draft[key] = entry;
          }
        }
      });
    }
  });
  const mutationSlice = (0,_reduxjs_toolkit__WEBPACK_IMPORTED_MODULE_0__.createSlice)({
    name: `${reducerPath}/mutations`,
    initialState,
    reducers: {
      removeMutationResult: {
        reducer(draft, {
          payload
        }) {
          const cacheKey = getMutationCacheKey(payload);
          if (cacheKey in draft) {
            delete draft[cacheKey];
          }
        },
        prepare: (0,_reduxjs_toolkit__WEBPACK_IMPORTED_MODULE_0__.prepareAutoBatched)()
      }
    },
    extraReducers(builder) {
      builder.addCase(mutationThunk.pending, (draft, {
        meta,
        meta: {
          requestId,
          arg,
          startedTimeStamp
        }
      }) => {
        if (!arg.track) return;
        draft[getMutationCacheKey(meta)] = {
          requestId,
          status: STATUS_PENDING,
          endpointName: arg.endpointName,
          startedTimeStamp
        };
      }).addCase(mutationThunk.fulfilled, (draft, {
        payload,
        meta
      }) => {
        if (!meta.arg.track) return;
        updateMutationSubstateIfExists(draft, meta, (substate) => {
          if (substate.requestId !== meta.requestId) return;
          substate.status = STATUS_FULFILLED;
          substate.data = payload;
          substate.fulfilledTimeStamp = meta.fulfilledTimeStamp;
        });
      }).addCase(mutationThunk.rejected, (draft, {
        payload,
        error,
        meta
      }) => {
        if (!meta.arg.track) return;
        updateMutationSubstateIfExists(draft, meta, (substate) => {
          if (substate.requestId !== meta.requestId) return;
          substate.status = STATUS_REJECTED;
          substate.error = payload ?? error;
        });
      }).addMatcher(hasRehydrationInfo, (draft, action) => {
        const {
          mutations
        } = extractRehydrationInfo(action);
        for (const [key, entry] of Object.entries(mutations)) {
          if (
            // do not rehydrate entries that were currently in flight.
            (entry?.status === STATUS_FULFILLED || entry?.status === STATUS_REJECTED) && // only rehydrate endpoints that were persisted using a `fixedCacheKey`
            key !== entry?.requestId
          ) {
            draft[key] = entry;
          }
        }
      });
    }
  });
  const initialInvalidationState = {
    tags: {},
    keys: {}
  };
  const invalidationSlice = (0,_reduxjs_toolkit__WEBPACK_IMPORTED_MODULE_0__.createSlice)({
    name: `${reducerPath}/invalidation`,
    initialState: initialInvalidationState,
    reducers: {
      updateProvidedBy: {
        reducer(draft, action) {
          for (const {
            queryCacheKey,
            providedTags
          } of action.payload) {
            removeCacheKeyFromTags(draft, queryCacheKey);
            for (const {
              type,
              id
            } of providedTags) {
              const subscribedQueries = (draft.tags[type] ??= {})[id || "__internal_without_id"] ??= [];
              const alreadySubscribed = subscribedQueries.includes(queryCacheKey);
              if (!alreadySubscribed) {
                subscribedQueries.push(queryCacheKey);
              }
            }
            draft.keys[queryCacheKey] = providedTags;
          }
        },
        prepare: (0,_reduxjs_toolkit__WEBPACK_IMPORTED_MODULE_0__.prepareAutoBatched)()
      }
    },
    extraReducers(builder) {
      builder.addCase(querySlice.actions.removeQueryResult, (draft, {
        payload: {
          queryCacheKey
        }
      }) => {
        removeCacheKeyFromTags(draft, queryCacheKey);
      }).addMatcher(hasRehydrationInfo, (draft, action) => {
        const {
          provided
        } = extractRehydrationInfo(action);
        for (const [type, incomingTags] of Object.entries(provided.tags ?? {})) {
          for (const [id, cacheKeys] of Object.entries(incomingTags)) {
            const subscribedQueries = (draft.tags[type] ??= {})[id || "__internal_without_id"] ??= [];
            for (const queryCacheKey of cacheKeys) {
              const alreadySubscribed = subscribedQueries.includes(queryCacheKey);
              if (!alreadySubscribed) {
                subscribedQueries.push(queryCacheKey);
              }
              draft.keys[queryCacheKey] = provided.keys[queryCacheKey];
            }
          }
        }
      }).addMatcher((0,_reduxjs_toolkit__WEBPACK_IMPORTED_MODULE_0__.isAnyOf)((0,_reduxjs_toolkit__WEBPACK_IMPORTED_MODULE_0__.isFulfilled)(queryThunk), (0,_reduxjs_toolkit__WEBPACK_IMPORTED_MODULE_0__.isRejectedWithValue)(queryThunk)), (draft, action) => {
        writeProvidedTagsForQueries(draft, [action]);
      }).addMatcher(querySlice.actions.cacheEntriesUpserted.match, (draft, action) => {
        const mockActions = action.payload.map(({
          queryDescription,
          value
        }) => {
          return {
            type: "UNKNOWN",
            payload: value,
            meta: {
              requestStatus: "fulfilled",
              requestId: "UNKNOWN",
              arg: queryDescription
            }
          };
        });
        writeProvidedTagsForQueries(draft, mockActions);
      });
    }
  });
  function removeCacheKeyFromTags(draft, queryCacheKey) {
    const existingTags = getCurrent(draft.keys[queryCacheKey] ?? []);
    for (const tag of existingTags) {
      const tagType = tag.type;
      const tagId = tag.id ?? "__internal_without_id";
      const tagSubscriptions = draft.tags[tagType]?.[tagId];
      if (tagSubscriptions) {
        draft.tags[tagType][tagId] = getCurrent(tagSubscriptions).filter((qc) => qc !== queryCacheKey);
      }
    }
    delete draft.keys[queryCacheKey];
  }
  function writeProvidedTagsForQueries(draft, actions3) {
    const providedByEntries = actions3.map((action) => {
      const providedTags = calculateProvidedByThunk(action, "providesTags", definitions, assertTagType);
      const {
        queryCacheKey
      } = action.meta.arg;
      return {
        queryCacheKey,
        providedTags
      };
    });
    invalidationSlice.caseReducers.updateProvidedBy(draft, invalidationSlice.actions.updateProvidedBy(providedByEntries));
  }
  const subscriptionSlice = (0,_reduxjs_toolkit__WEBPACK_IMPORTED_MODULE_0__.createSlice)({
    name: `${reducerPath}/subscriptions`,
    initialState,
    reducers: {
      updateSubscriptionOptions(d, a) {
      },
      unsubscribeQueryResult(d, a) {
      },
      internal_getRTKQSubscriptions() {
      }
    }
  });
  const internalSubscriptionsSlice = (0,_reduxjs_toolkit__WEBPACK_IMPORTED_MODULE_0__.createSlice)({
    name: `${reducerPath}/internalSubscriptions`,
    initialState,
    reducers: {
      subscriptionsUpdated: {
        reducer(state, action) {
          return (0,_reduxjs_toolkit__WEBPACK_IMPORTED_MODULE_2__.applyPatches)(state, action.payload);
        },
        prepare: (0,_reduxjs_toolkit__WEBPACK_IMPORTED_MODULE_0__.prepareAutoBatched)()
      }
    }
  });
  const configSlice = (0,_reduxjs_toolkit__WEBPACK_IMPORTED_MODULE_0__.createSlice)({
    name: `${reducerPath}/config`,
    initialState: {
      online: isOnline(),
      focused: isDocumentVisible(),
      middlewareRegistered: false,
      ...config
    },
    reducers: {
      middlewareRegistered(state, {
        payload
      }) {
        state.middlewareRegistered = state.middlewareRegistered === "conflict" || apiUid !== payload ? "conflict" : true;
      }
    },
    extraReducers: (builder) => {
      builder.addCase(onOnline, (state) => {
        state.online = true;
      }).addCase(onOffline, (state) => {
        state.online = false;
      }).addCase(onFocus, (state) => {
        state.focused = true;
      }).addCase(onFocusLost, (state) => {
        state.focused = false;
      }).addMatcher(hasRehydrationInfo, (draft) => ({
        ...draft
      }));
    }
  });
  const combinedReducer = (0,_reduxjs_toolkit__WEBPACK_IMPORTED_MODULE_1__.combineReducers)({
    queries: querySlice.reducer,
    mutations: mutationSlice.reducer,
    provided: invalidationSlice.reducer,
    subscriptions: internalSubscriptionsSlice.reducer,
    config: configSlice.reducer
  });
  const reducer = (state, action) => combinedReducer(resetApiState.match(action) ? void 0 : state, action);
  const actions2 = {
    ...configSlice.actions,
    ...querySlice.actions,
    ...subscriptionSlice.actions,
    ...internalSubscriptionsSlice.actions,
    ...mutationSlice.actions,
    ...invalidationSlice.actions,
    resetApiState
  };
  return {
    reducer,
    actions: actions2
  };
}

// src/query/core/buildSelectors.ts
var skipToken = /* @__PURE__ */ Symbol.for("RTKQ/skipToken");
var initialSubState = {
  status: STATUS_UNINITIALIZED
};
var defaultQuerySubState = /* @__PURE__ */ (0,_reduxjs_toolkit__WEBPACK_IMPORTED_MODULE_2__.produce)(initialSubState, () => {
});
var defaultMutationSubState = /* @__PURE__ */ (0,_reduxjs_toolkit__WEBPACK_IMPORTED_MODULE_2__.produce)(initialSubState, () => {
});
function buildSelectors({
  serializeQueryArgs,
  reducerPath,
  createSelector: createSelector2
}) {
  const selectSkippedQuery = (state) => defaultQuerySubState;
  const selectSkippedMutation = (state) => defaultMutationSubState;
  return {
    buildQuerySelector,
    buildInfiniteQuerySelector,
    buildMutationSelector,
    selectInvalidatedBy,
    selectCachedArgsForQuery,
    selectApiState,
    selectQueries,
    selectMutations,
    selectQueryEntry,
    selectConfig
  };
  function withRequestFlags(substate) {
    return {
      ...substate,
      ...getRequestStatusFlags(substate.status)
    };
  }
  function selectApiState(rootState) {
    const state = rootState[reducerPath];
    if (true) {
      if (!state) {
        if (selectApiState.triggered) return state;
        selectApiState.triggered = true;
        console.error(`Error: No data found at \`state.${reducerPath}\`. Did you forget to add the reducer to the store?`);
      }
    }
    return state;
  }
  function selectQueries(rootState) {
    return selectApiState(rootState)?.queries;
  }
  function selectQueryEntry(rootState, cacheKey) {
    return selectQueries(rootState)?.[cacheKey];
  }
  function selectMutations(rootState) {
    return selectApiState(rootState)?.mutations;
  }
  function selectConfig(rootState) {
    return selectApiState(rootState)?.config;
  }
  function buildAnyQuerySelector(endpointName, endpointDefinition, combiner) {
    return (queryArgs) => {
      if (queryArgs === skipToken) {
        return createSelector2(selectSkippedQuery, combiner);
      }
      const serializedArgs = serializeQueryArgs({
        queryArgs,
        endpointDefinition,
        endpointName
      });
      const selectQuerySubstate = (state) => selectQueryEntry(state, serializedArgs) ?? defaultQuerySubState;
      return createSelector2(selectQuerySubstate, combiner);
    };
  }
  function buildQuerySelector(endpointName, endpointDefinition) {
    return buildAnyQuerySelector(endpointName, endpointDefinition, withRequestFlags);
  }
  function buildInfiniteQuerySelector(endpointName, endpointDefinition) {
    const {
      infiniteQueryOptions
    } = endpointDefinition;
    function withInfiniteQueryResultFlags(substate) {
      const stateWithRequestFlags = {
        ...substate,
        ...getRequestStatusFlags(substate.status)
      };
      const {
        isLoading,
        isError,
        direction
      } = stateWithRequestFlags;
      const isForward = direction === "forward";
      const isBackward = direction === "backward";
      return {
        ...stateWithRequestFlags,
        hasNextPage: getHasNextPage(infiniteQueryOptions, stateWithRequestFlags.data, stateWithRequestFlags.originalArgs),
        hasPreviousPage: getHasPreviousPage(infiniteQueryOptions, stateWithRequestFlags.data, stateWithRequestFlags.originalArgs),
        isFetchingNextPage: isLoading && isForward,
        isFetchingPreviousPage: isLoading && isBackward,
        isFetchNextPageError: isError && isForward,
        isFetchPreviousPageError: isError && isBackward
      };
    }
    return buildAnyQuerySelector(endpointName, endpointDefinition, withInfiniteQueryResultFlags);
  }
  function buildMutationSelector() {
    return (id) => {
      let mutationId;
      if (typeof id === "object") {
        mutationId = getMutationCacheKey(id) ?? skipToken;
      } else {
        mutationId = id;
      }
      const selectMutationSubstate = (state) => selectApiState(state)?.mutations?.[mutationId] ?? defaultMutationSubState;
      const finalSelectMutationSubstate = mutationId === skipToken ? selectSkippedMutation : selectMutationSubstate;
      return createSelector2(finalSelectMutationSubstate, withRequestFlags);
    };
  }
  function selectInvalidatedBy(state, tags) {
    const apiState = state[reducerPath];
    const toInvalidate = /* @__PURE__ */ new Set();
    const finalTags = filterMap(tags, isNotNullish, expandTagDescription);
    for (const tag of finalTags) {
      const provided = apiState.provided.tags[tag.type];
      if (!provided) {
        continue;
      }
      let invalidateSubscriptions = (tag.id !== void 0 ? (
        // id given: invalidate all queries that provide this type & id
        provided[tag.id]
      ) : (
        // no id: invalidate all queries that provide this type
        Object.values(provided).flat()
      )) ?? [];
      for (const invalidate of invalidateSubscriptions) {
        toInvalidate.add(invalidate);
      }
    }
    return Array.from(toInvalidate.values()).flatMap((queryCacheKey) => {
      const querySubState = apiState.queries[queryCacheKey];
      return querySubState ? {
        queryCacheKey,
        endpointName: querySubState.endpointName,
        originalArgs: querySubState.originalArgs
      } : [];
    });
  }
  function selectCachedArgsForQuery(state, queryName) {
    return filterMap(Object.values(selectQueries(state)), (entry) => entry?.endpointName === queryName && entry.status !== STATUS_UNINITIALIZED, (entry) => entry.originalArgs);
  }
  function getHasNextPage(options, data, queryArg) {
    if (!data) return false;
    return getNextPageParam(options, data, queryArg) != null;
  }
  function getHasPreviousPage(options, data, queryArg) {
    if (!data || !options.getPreviousPageParam) return false;
    return getPreviousPageParam(options, data, queryArg) != null;
  }
}

// src/query/createApi.ts

// src/query/defaultSerializeQueryArgs.ts
var cache = WeakMap ? /* @__PURE__ */ new WeakMap() : void 0;
var defaultSerializeQueryArgs = ({
  endpointName,
  queryArgs
}) => {
  let serialized = "";
  const cached = cache?.get(queryArgs);
  if (typeof cached === "string") {
    serialized = cached;
  } else {
    const stringified = JSON.stringify(queryArgs, (key, value) => {
      value = typeof value === "bigint" ? {
        $bigint: value.toString()
      } : value;
      value = (0,_reduxjs_toolkit__WEBPACK_IMPORTED_MODULE_1__.isPlainObject)(value) ? Object.keys(value).sort().reduce((acc, key2) => {
        acc[key2] = value[key2];
        return acc;
      }, {}) : value;
      return value;
    });
    if ((0,_reduxjs_toolkit__WEBPACK_IMPORTED_MODULE_1__.isPlainObject)(queryArgs)) {
      cache?.set(queryArgs, stringified);
    }
    serialized = stringified;
  }
  return `${endpointName}(${serialized})`;
};

// src/query/createApi.ts

function buildCreateApi(...modules) {
  return function baseCreateApi(options) {
    const extractRehydrationInfo = (0,_reduxjs_toolkit__WEBPACK_IMPORTED_MODULE_3__.weakMapMemoize)((action) => options.extractRehydrationInfo?.(action, {
      reducerPath: options.reducerPath ?? "api"
    }));
    const optionsWithDefaults = {
      reducerPath: "api",
      keepUnusedDataFor: 60,
      refetchOnMountOrArgChange: false,
      refetchOnFocus: false,
      refetchOnReconnect: false,
      invalidationBehavior: "delayed",
      ...options,
      extractRehydrationInfo,
      serializeQueryArgs(queryArgsApi) {
        let finalSerializeQueryArgs = defaultSerializeQueryArgs;
        if ("serializeQueryArgs" in queryArgsApi.endpointDefinition) {
          const endpointSQA = queryArgsApi.endpointDefinition.serializeQueryArgs;
          finalSerializeQueryArgs = (queryArgsApi2) => {
            const initialResult = endpointSQA(queryArgsApi2);
            if (typeof initialResult === "string") {
              return initialResult;
            } else {
              return defaultSerializeQueryArgs({
                ...queryArgsApi2,
                queryArgs: initialResult
              });
            }
          };
        } else if (options.serializeQueryArgs) {
          finalSerializeQueryArgs = options.serializeQueryArgs;
        }
        return finalSerializeQueryArgs(queryArgsApi);
      },
      tagTypes: [...options.tagTypes || []]
    };
    const context = {
      endpointDefinitions: {},
      batch(fn) {
        fn();
      },
      apiUid: (0,_reduxjs_toolkit__WEBPACK_IMPORTED_MODULE_0__.nanoid)(),
      extractRehydrationInfo,
      hasRehydrationInfo: (0,_reduxjs_toolkit__WEBPACK_IMPORTED_MODULE_3__.weakMapMemoize)((action) => extractRehydrationInfo(action) != null)
    };
    const api = {
      injectEndpoints,
      enhanceEndpoints({
        addTagTypes,
        endpoints
      }) {
        if (addTagTypes) {
          for (const eT of addTagTypes) {
            if (!optionsWithDefaults.tagTypes.includes(eT)) {
              ;
              optionsWithDefaults.tagTypes.push(eT);
            }
          }
        }
        if (endpoints) {
          for (const [endpointName, partialDefinition] of Object.entries(endpoints)) {
            if (typeof partialDefinition === "function") {
              partialDefinition(getEndpointDefinition(context, endpointName));
            } else {
              Object.assign(getEndpointDefinition(context, endpointName) || {}, partialDefinition);
            }
          }
        }
        return api;
      }
    };
    const initializedModules = modules.map((m) => m.init(api, optionsWithDefaults, context));
    function injectEndpoints(inject) {
      const evaluatedEndpoints = inject.endpoints({
        query: (x) => ({
          ...x,
          type: ENDPOINT_QUERY
        }),
        mutation: (x) => ({
          ...x,
          type: ENDPOINT_MUTATION
        }),
        infiniteQuery: (x) => ({
          ...x,
          type: ENDPOINT_INFINITEQUERY
        })
      });
      for (const [endpointName, definition] of Object.entries(evaluatedEndpoints)) {
        if (inject.overrideExisting !== true && endpointName in context.endpointDefinitions) {
          if (inject.overrideExisting === "throw") {
            throw new Error( false ? 0 : `called \`injectEndpoints\` to override already-existing endpointName ${endpointName} without specifying \`overrideExisting: true\``);
          } else if (typeof process !== "undefined" && "development" === "development") {
            console.error(`called \`injectEndpoints\` to override already-existing endpointName ${endpointName} without specifying \`overrideExisting: true\``);
          }
          continue;
        }
        if (typeof process !== "undefined" && "development" === "development") {
          if (isInfiniteQueryDefinition(definition)) {
            const {
              infiniteQueryOptions
            } = definition;
            const {
              maxPages,
              getPreviousPageParam: getPreviousPageParam2
            } = infiniteQueryOptions;
            if (typeof maxPages === "number") {
              if (maxPages < 1) {
                throw new Error( false ? 0 : `maxPages for endpoint '${endpointName}' must be a number greater than 0`);
              }
              if (typeof getPreviousPageParam2 !== "function") {
                throw new Error( false ? 0 : `getPreviousPageParam for endpoint '${endpointName}' must be a function if maxPages is used`);
              }
            }
          }
        }
        context.endpointDefinitions[endpointName] = definition;
        for (const m of initializedModules) {
          m.injectEndpoint(endpointName, definition);
        }
      }
      return api;
    }
    return api.injectEndpoints({
      endpoints: options.endpoints
    });
  };
}

// src/query/fakeBaseQuery.ts

var _NEVER = /* @__PURE__ */ Symbol();
function fakeBaseQuery() {
  return function() {
    throw new Error( false ? 0 : "When using `fakeBaseQuery`, all queries & mutations must use the `queryFn` definition syntax.");
  };
}

// src/query/tsHelpers.ts
function assertCast(v) {
}
function safeAssign(target, ...args) {
  return Object.assign(target, ...args);
}

// src/query/core/buildMiddleware/batchActions.ts
var buildBatchedActionsHandler = ({
  api,
  queryThunk,
  internalState,
  mwApi
}) => {
  const subscriptionsPrefix = `${api.reducerPath}/subscriptions`;
  let previousSubscriptions = null;
  let updateSyncTimer = null;
  const {
    updateSubscriptionOptions,
    unsubscribeQueryResult
  } = api.internalActions;
  const actuallyMutateSubscriptions = (currentSubscriptions, action) => {
    if (updateSubscriptionOptions.match(action)) {
      const {
        queryCacheKey,
        requestId,
        options
      } = action.payload;
      const sub = currentSubscriptions.get(queryCacheKey);
      if (sub?.has(requestId)) {
        sub.set(requestId, options);
      }
      return true;
    }
    if (unsubscribeQueryResult.match(action)) {
      const {
        queryCacheKey,
        requestId
      } = action.payload;
      const sub = currentSubscriptions.get(queryCacheKey);
      if (sub) {
        sub.delete(requestId);
      }
      return true;
    }
    if (api.internalActions.removeQueryResult.match(action)) {
      currentSubscriptions.delete(action.payload.queryCacheKey);
      return true;
    }
    if (queryThunk.pending.match(action)) {
      const {
        meta: {
          arg,
          requestId
        }
      } = action;
      const substate = getOrInsertComputed(currentSubscriptions, arg.queryCacheKey, createNewMap);
      if (arg.subscribe) {
        substate.set(requestId, arg.subscriptionOptions ?? substate.get(requestId) ?? {});
      }
      return true;
    }
    let mutated = false;
    if (queryThunk.rejected.match(action)) {
      const {
        meta: {
          condition,
          arg,
          requestId
        }
      } = action;
      if (condition && arg.subscribe) {
        const substate = getOrInsertComputed(currentSubscriptions, arg.queryCacheKey, createNewMap);
        substate.set(requestId, arg.subscriptionOptions ?? substate.get(requestId) ?? {});
        mutated = true;
      }
    }
    return mutated;
  };
  const getSubscriptions = () => internalState.currentSubscriptions;
  const getSubscriptionCount = (queryCacheKey) => {
    const subscriptions = getSubscriptions();
    const subscriptionsForQueryArg = subscriptions.get(queryCacheKey);
    return subscriptionsForQueryArg?.size ?? 0;
  };
  const isRequestSubscribed = (queryCacheKey, requestId) => {
    const subscriptions = getSubscriptions();
    return !!subscriptions?.get(queryCacheKey)?.get(requestId);
  };
  const subscriptionSelectors = {
    getSubscriptions,
    getSubscriptionCount,
    isRequestSubscribed
  };
  function serializeSubscriptions(currentSubscriptions) {
    return JSON.parse(JSON.stringify(Object.fromEntries([...currentSubscriptions].map(([k, v]) => [k, Object.fromEntries(v)]))));
  }
  return (action, mwApi2) => {
    if (!previousSubscriptions) {
      previousSubscriptions = serializeSubscriptions(internalState.currentSubscriptions);
    }
    if (api.util.resetApiState.match(action)) {
      previousSubscriptions = {};
      internalState.currentSubscriptions.clear();
      updateSyncTimer = null;
      return [true, false];
    }
    if (api.internalActions.internal_getRTKQSubscriptions.match(action)) {
      return [false, subscriptionSelectors];
    }
    const didMutate = actuallyMutateSubscriptions(internalState.currentSubscriptions, action);
    let actionShouldContinue = true;
    if (false) // removed by dead control flow
{}
    if (didMutate) {
      if (!updateSyncTimer) {
        updateSyncTimer = setTimeout(() => {
          const newSubscriptions = serializeSubscriptions(internalState.currentSubscriptions);
          const [, patches] = (0,_reduxjs_toolkit__WEBPACK_IMPORTED_MODULE_2__.produceWithPatches)(previousSubscriptions, () => newSubscriptions);
          mwApi2.next(api.internalActions.subscriptionsUpdated(patches));
          previousSubscriptions = newSubscriptions;
          updateSyncTimer = null;
        }, 500);
      }
      const isSubscriptionSliceAction = typeof action.type == "string" && !!action.type.startsWith(subscriptionsPrefix);
      const isAdditionalSubscriptionAction = queryThunk.rejected.match(action) && action.meta.condition && !!action.meta.arg.subscribe;
      actionShouldContinue = !isSubscriptionSliceAction && !isAdditionalSubscriptionAction;
    }
    return [actionShouldContinue, false];
  };
};

// src/query/core/buildMiddleware/cacheCollection.ts
var THIRTY_TWO_BIT_MAX_TIMER_SECONDS = 2147483647 / 1e3 - 1;
var buildCacheCollectionHandler = ({
  reducerPath,
  api,
  queryThunk,
  context,
  internalState,
  selectors: {
    selectQueryEntry,
    selectConfig
  },
  getRunningQueryThunk,
  mwApi
}) => {
  const {
    removeQueryResult,
    unsubscribeQueryResult,
    cacheEntriesUpserted
  } = api.internalActions;
  const canTriggerUnsubscribe = (0,_reduxjs_toolkit__WEBPACK_IMPORTED_MODULE_0__.isAnyOf)(unsubscribeQueryResult.match, queryThunk.fulfilled, queryThunk.rejected, cacheEntriesUpserted.match);
  function anySubscriptionsRemainingForKey(queryCacheKey) {
    const subscriptions = internalState.currentSubscriptions.get(queryCacheKey);
    if (!subscriptions) {
      return false;
    }
    const hasSubscriptions = subscriptions.size > 0;
    return hasSubscriptions;
  }
  const currentRemovalTimeouts = {};
  function abortAllPromises(promiseMap) {
    for (const promise of promiseMap.values()) {
      promise?.abort?.();
    }
  }
  const handler = (action, mwApi2) => {
    const state = mwApi2.getState();
    const config = selectConfig(state);
    if (canTriggerUnsubscribe(action)) {
      let queryCacheKeys;
      if (cacheEntriesUpserted.match(action)) {
        queryCacheKeys = action.payload.map((entry) => entry.queryDescription.queryCacheKey);
      } else {
        const {
          queryCacheKey
        } = unsubscribeQueryResult.match(action) ? action.payload : action.meta.arg;
        queryCacheKeys = [queryCacheKey];
      }
      handleUnsubscribeMany(queryCacheKeys, mwApi2, config);
    }
    if (api.util.resetApiState.match(action)) {
      for (const [key, timeout] of Object.entries(currentRemovalTimeouts)) {
        if (timeout) clearTimeout(timeout);
        delete currentRemovalTimeouts[key];
      }
      abortAllPromises(internalState.runningQueries);
      abortAllPromises(internalState.runningMutations);
    }
    if (context.hasRehydrationInfo(action)) {
      const {
        queries
      } = context.extractRehydrationInfo(action);
      handleUnsubscribeMany(Object.keys(queries), mwApi2, config);
    }
  };
  function handleUnsubscribeMany(cacheKeys, api2, config) {
    const state = api2.getState();
    for (const queryCacheKey of cacheKeys) {
      const entry = selectQueryEntry(state, queryCacheKey);
      if (entry?.endpointName) {
        handleUnsubscribe(queryCacheKey, entry.endpointName, api2, config);
      }
    }
  }
  function handleUnsubscribe(queryCacheKey, endpointName, api2, config) {
    const endpointDefinition = getEndpointDefinition(context, endpointName);
    const keepUnusedDataFor = endpointDefinition?.keepUnusedDataFor ?? config.keepUnusedDataFor;
    if (keepUnusedDataFor === Infinity) {
      return;
    }
    const finalKeepUnusedDataFor = Math.max(0, Math.min(keepUnusedDataFor, THIRTY_TWO_BIT_MAX_TIMER_SECONDS));
    if (!anySubscriptionsRemainingForKey(queryCacheKey)) {
      const currentTimeout = currentRemovalTimeouts[queryCacheKey];
      if (currentTimeout) {
        clearTimeout(currentTimeout);
      }
      currentRemovalTimeouts[queryCacheKey] = setTimeout(() => {
        if (!anySubscriptionsRemainingForKey(queryCacheKey)) {
          const entry = selectQueryEntry(api2.getState(), queryCacheKey);
          if (entry?.endpointName) {
            const runningQuery = api2.dispatch(getRunningQueryThunk(entry.endpointName, entry.originalArgs));
            runningQuery?.abort();
          }
          api2.dispatch(removeQueryResult({
            queryCacheKey
          }));
        }
        delete currentRemovalTimeouts[queryCacheKey];
      }, finalKeepUnusedDataFor * 1e3);
    }
  }
  return handler;
};

// src/query/core/buildMiddleware/cacheLifecycle.ts
var neverResolvedError = new Error("Promise never resolved before cacheEntryRemoved.");
var buildCacheLifecycleHandler = ({
  api,
  reducerPath,
  context,
  queryThunk,
  mutationThunk,
  internalState,
  selectors: {
    selectQueryEntry,
    selectApiState
  }
}) => {
  const isQueryThunk = (0,_reduxjs_toolkit__WEBPACK_IMPORTED_MODULE_0__.isAsyncThunkAction)(queryThunk);
  const isMutationThunk = (0,_reduxjs_toolkit__WEBPACK_IMPORTED_MODULE_0__.isAsyncThunkAction)(mutationThunk);
  const isFulfilledThunk = (0,_reduxjs_toolkit__WEBPACK_IMPORTED_MODULE_0__.isFulfilled)(queryThunk, mutationThunk);
  const lifecycleMap = {};
  const {
    removeQueryResult,
    removeMutationResult,
    cacheEntriesUpserted
  } = api.internalActions;
  function resolveLifecycleEntry(cacheKey, data, meta) {
    const lifecycle = lifecycleMap[cacheKey];
    if (lifecycle?.valueResolved) {
      lifecycle.valueResolved({
        data,
        meta
      });
      delete lifecycle.valueResolved;
    }
  }
  function removeLifecycleEntry(cacheKey) {
    const lifecycle = lifecycleMap[cacheKey];
    if (lifecycle) {
      delete lifecycleMap[cacheKey];
      lifecycle.cacheEntryRemoved();
    }
  }
  function getActionMetaFields(action) {
    const {
      arg,
      requestId
    } = action.meta;
    const {
      endpointName,
      originalArgs
    } = arg;
    return [endpointName, originalArgs, requestId];
  }
  const handler = (action, mwApi, stateBefore) => {
    const cacheKey = getCacheKey(action);
    function checkForNewCacheKey(endpointName, cacheKey2, requestId, originalArgs) {
      const oldEntry = selectQueryEntry(stateBefore, cacheKey2);
      const newEntry = selectQueryEntry(mwApi.getState(), cacheKey2);
      if (!oldEntry && newEntry) {
        handleNewKey(endpointName, originalArgs, cacheKey2, mwApi, requestId);
      }
    }
    if (queryThunk.pending.match(action)) {
      const [endpointName, originalArgs, requestId] = getActionMetaFields(action);
      checkForNewCacheKey(endpointName, cacheKey, requestId, originalArgs);
    } else if (cacheEntriesUpserted.match(action)) {
      for (const {
        queryDescription,
        value
      } of action.payload) {
        const {
          endpointName,
          originalArgs,
          queryCacheKey
        } = queryDescription;
        checkForNewCacheKey(endpointName, queryCacheKey, action.meta.requestId, originalArgs);
        resolveLifecycleEntry(queryCacheKey, value, {});
      }
    } else if (mutationThunk.pending.match(action)) {
      const state = mwApi.getState()[reducerPath].mutations[cacheKey];
      if (state) {
        const [endpointName, originalArgs, requestId] = getActionMetaFields(action);
        handleNewKey(endpointName, originalArgs, cacheKey, mwApi, requestId);
      }
    } else if (isFulfilledThunk(action)) {
      resolveLifecycleEntry(cacheKey, action.payload, action.meta.baseQueryMeta);
    } else if (removeQueryResult.match(action) || removeMutationResult.match(action)) {
      removeLifecycleEntry(cacheKey);
    } else if (api.util.resetApiState.match(action)) {
      for (const cacheKey2 of Object.keys(lifecycleMap)) {
        removeLifecycleEntry(cacheKey2);
      }
    }
  };
  function getCacheKey(action) {
    if (isQueryThunk(action)) return action.meta.arg.queryCacheKey;
    if (isMutationThunk(action)) {
      return action.meta.arg.fixedCacheKey ?? action.meta.requestId;
    }
    if (removeQueryResult.match(action)) return action.payload.queryCacheKey;
    if (removeMutationResult.match(action)) return getMutationCacheKey(action.payload);
    return "";
  }
  function handleNewKey(endpointName, originalArgs, queryCacheKey, mwApi, requestId) {
    const endpointDefinition = getEndpointDefinition(context, endpointName);
    const onCacheEntryAdded = endpointDefinition?.onCacheEntryAdded;
    if (!onCacheEntryAdded) return;
    const lifecycle = {};
    const cacheEntryRemoved = new Promise((resolve) => {
      lifecycle.cacheEntryRemoved = resolve;
    });
    const cacheDataLoaded = Promise.race([new Promise((resolve) => {
      lifecycle.valueResolved = resolve;
    }), cacheEntryRemoved.then(() => {
      throw neverResolvedError;
    })]);
    cacheDataLoaded.catch(() => {
    });
    lifecycleMap[queryCacheKey] = lifecycle;
    const selector = api.endpoints[endpointName].select(isAnyQueryDefinition(endpointDefinition) ? originalArgs : queryCacheKey);
    const extra = mwApi.dispatch((_, __, extra2) => extra2);
    const lifecycleApi = {
      ...mwApi,
      getCacheEntry: () => selector(mwApi.getState()),
      requestId,
      extra,
      updateCachedData: isAnyQueryDefinition(endpointDefinition) ? (updateRecipe) => mwApi.dispatch(api.util.updateQueryData(endpointName, originalArgs, updateRecipe)) : void 0,
      cacheDataLoaded,
      cacheEntryRemoved
    };
    const runningHandler = onCacheEntryAdded(originalArgs, lifecycleApi);
    Promise.resolve(runningHandler).catch((e) => {
      if (e === neverResolvedError) return;
      throw e;
    });
  }
  return handler;
};

// src/query/core/buildMiddleware/devMiddleware.ts
var buildDevCheckHandler = ({
  api,
  context: {
    apiUid
  },
  reducerPath
}) => {
  return (action, mwApi) => {
    if (api.util.resetApiState.match(action)) {
      mwApi.dispatch(api.internalActions.middlewareRegistered(apiUid));
    }
    if (typeof process !== "undefined" && "development" === "development") {
      if (api.internalActions.middlewareRegistered.match(action) && action.payload === apiUid && mwApi.getState()[reducerPath]?.config?.middlewareRegistered === "conflict") {
        console.warn(`There is a mismatch between slice and middleware for the reducerPath "${reducerPath}".
You can only have one api per reducer path, this will lead to crashes in various situations!${reducerPath === "api" ? `
If you have multiple apis, you *have* to specify the reducerPath option when using createApi!` : ""}`);
      }
    }
  };
};

// src/query/core/buildMiddleware/invalidationByTags.ts
var buildInvalidationByTagsHandler = ({
  reducerPath,
  context,
  context: {
    endpointDefinitions
  },
  mutationThunk,
  queryThunk,
  api,
  assertTagType,
  refetchQuery,
  internalState
}) => {
  const {
    removeQueryResult
  } = api.internalActions;
  const isThunkActionWithTags = (0,_reduxjs_toolkit__WEBPACK_IMPORTED_MODULE_0__.isAnyOf)((0,_reduxjs_toolkit__WEBPACK_IMPORTED_MODULE_0__.isFulfilled)(mutationThunk), (0,_reduxjs_toolkit__WEBPACK_IMPORTED_MODULE_0__.isRejectedWithValue)(mutationThunk));
  const isQueryEnd = (0,_reduxjs_toolkit__WEBPACK_IMPORTED_MODULE_0__.isAnyOf)((0,_reduxjs_toolkit__WEBPACK_IMPORTED_MODULE_0__.isFulfilled)(queryThunk, mutationThunk), (0,_reduxjs_toolkit__WEBPACK_IMPORTED_MODULE_0__.isRejected)(queryThunk, mutationThunk));
  let pendingTagInvalidations = [];
  let pendingRequestCount = 0;
  const handler = (action, mwApi) => {
    if (queryThunk.pending.match(action) || mutationThunk.pending.match(action)) {
      pendingRequestCount++;
    }
    if (isQueryEnd(action)) {
      pendingRequestCount = Math.max(0, pendingRequestCount - 1);
    }
    if (isThunkActionWithTags(action)) {
      invalidateTags(calculateProvidedByThunk(action, "invalidatesTags", endpointDefinitions, assertTagType), mwApi);
    } else if (isQueryEnd(action)) {
      invalidateTags([], mwApi);
    } else if (api.util.invalidateTags.match(action)) {
      invalidateTags(calculateProvidedBy(action.payload, void 0, void 0, void 0, void 0, assertTagType), mwApi);
    }
  };
  function hasPendingRequests() {
    return pendingRequestCount > 0;
  }
  function invalidateTags(newTags, mwApi) {
    const rootState = mwApi.getState();
    const state = rootState[reducerPath];
    pendingTagInvalidations.push(...newTags);
    if (state.config.invalidationBehavior === "delayed" && hasPendingRequests()) {
      return;
    }
    const tags = pendingTagInvalidations;
    pendingTagInvalidations = [];
    if (tags.length === 0) return;
    const toInvalidate = api.util.selectInvalidatedBy(rootState, tags);
    context.batch(() => {
      const valuesArray = Array.from(toInvalidate.values());
      for (const {
        queryCacheKey
      } of valuesArray) {
        const querySubState = state.queries[queryCacheKey];
        const subscriptionSubState = getOrInsertComputed(internalState.currentSubscriptions, queryCacheKey, createNewMap);
        if (querySubState) {
          if (subscriptionSubState.size === 0) {
            mwApi.dispatch(removeQueryResult({
              queryCacheKey
            }));
          } else if (querySubState.status !== STATUS_UNINITIALIZED) {
            mwApi.dispatch(refetchQuery(querySubState));
          }
        }
      }
    });
  }
  return handler;
};

// src/query/core/buildMiddleware/polling.ts
var buildPollingHandler = ({
  reducerPath,
  queryThunk,
  api,
  refetchQuery,
  internalState
}) => {
  const {
    currentPolls,
    currentSubscriptions
  } = internalState;
  const pendingPollingUpdates = /* @__PURE__ */ new Set();
  let pollingUpdateTimer = null;
  const handler = (action, mwApi) => {
    if (api.internalActions.updateSubscriptionOptions.match(action) || api.internalActions.unsubscribeQueryResult.match(action)) {
      schedulePollingUpdate(action.payload.queryCacheKey, mwApi);
    }
    if (queryThunk.pending.match(action) || queryThunk.rejected.match(action) && action.meta.condition) {
      schedulePollingUpdate(action.meta.arg.queryCacheKey, mwApi);
    }
    if (queryThunk.fulfilled.match(action) || queryThunk.rejected.match(action) && !action.meta.condition) {
      startNextPoll(action.meta.arg, mwApi);
    }
    if (api.util.resetApiState.match(action)) {
      clearPolls();
      if (pollingUpdateTimer) {
        clearTimeout(pollingUpdateTimer);
        pollingUpdateTimer = null;
      }
      pendingPollingUpdates.clear();
    }
  };
  function schedulePollingUpdate(queryCacheKey, api2) {
    pendingPollingUpdates.add(queryCacheKey);
    if (!pollingUpdateTimer) {
      pollingUpdateTimer = setTimeout(() => {
        for (const key of pendingPollingUpdates) {
          updatePollingInterval({
            queryCacheKey: key
          }, api2);
        }
        pendingPollingUpdates.clear();
        pollingUpdateTimer = null;
      }, 0);
    }
  }
  function startNextPoll({
    queryCacheKey
  }, api2) {
    const state = api2.getState()[reducerPath];
    const querySubState = state.queries[queryCacheKey];
    const subscriptions = currentSubscriptions.get(queryCacheKey);
    if (!querySubState || querySubState.status === STATUS_UNINITIALIZED) return;
    const {
      lowestPollingInterval,
      skipPollingIfUnfocused
    } = findLowestPollingInterval(subscriptions);
    if (!Number.isFinite(lowestPollingInterval)) return;
    const currentPoll = currentPolls.get(queryCacheKey);
    if (currentPoll?.timeout) {
      clearTimeout(currentPoll.timeout);
      currentPoll.timeout = void 0;
    }
    const nextPollTimestamp = Date.now() + lowestPollingInterval;
    currentPolls.set(queryCacheKey, {
      nextPollTimestamp,
      pollingInterval: lowestPollingInterval,
      timeout: setTimeout(() => {
        if (state.config.focused || !skipPollingIfUnfocused) {
          api2.dispatch(refetchQuery(querySubState));
        }
        startNextPoll({
          queryCacheKey
        }, api2);
      }, lowestPollingInterval)
    });
  }
  function updatePollingInterval({
    queryCacheKey
  }, api2) {
    const state = api2.getState()[reducerPath];
    const querySubState = state.queries[queryCacheKey];
    const subscriptions = currentSubscriptions.get(queryCacheKey);
    if (!querySubState || querySubState.status === STATUS_UNINITIALIZED) {
      return;
    }
    const {
      lowestPollingInterval
    } = findLowestPollingInterval(subscriptions);
    if (false) // removed by dead control flow
{}
    if (!Number.isFinite(lowestPollingInterval)) {
      cleanupPollForKey(queryCacheKey);
      return;
    }
    const currentPoll = currentPolls.get(queryCacheKey);
    const nextPollTimestamp = Date.now() + lowestPollingInterval;
    if (!currentPoll || nextPollTimestamp < currentPoll.nextPollTimestamp) {
      startNextPoll({
        queryCacheKey
      }, api2);
    }
  }
  function cleanupPollForKey(key) {
    const existingPoll = currentPolls.get(key);
    if (existingPoll?.timeout) {
      clearTimeout(existingPoll.timeout);
    }
    currentPolls.delete(key);
  }
  function clearPolls() {
    for (const key of currentPolls.keys()) {
      cleanupPollForKey(key);
    }
  }
  function findLowestPollingInterval(subscribers = /* @__PURE__ */ new Map()) {
    let skipPollingIfUnfocused = false;
    let lowestPollingInterval = Number.POSITIVE_INFINITY;
    for (const entry of subscribers.values()) {
      if (!!entry.pollingInterval) {
        lowestPollingInterval = Math.min(entry.pollingInterval, lowestPollingInterval);
        skipPollingIfUnfocused = entry.skipPollingIfUnfocused || skipPollingIfUnfocused;
      }
    }
    return {
      lowestPollingInterval,
      skipPollingIfUnfocused
    };
  }
  return handler;
};

// src/query/core/buildMiddleware/queryLifecycle.ts
var buildQueryLifecycleHandler = ({
  api,
  context,
  queryThunk,
  mutationThunk
}) => {
  const isPendingThunk = (0,_reduxjs_toolkit__WEBPACK_IMPORTED_MODULE_0__.isPending)(queryThunk, mutationThunk);
  const isRejectedThunk = (0,_reduxjs_toolkit__WEBPACK_IMPORTED_MODULE_0__.isRejected)(queryThunk, mutationThunk);
  const isFullfilledThunk = (0,_reduxjs_toolkit__WEBPACK_IMPORTED_MODULE_0__.isFulfilled)(queryThunk, mutationThunk);
  const lifecycleMap = {};
  const handler = (action, mwApi) => {
    if (isPendingThunk(action)) {
      const {
        requestId,
        arg: {
          endpointName,
          originalArgs
        }
      } = action.meta;
      const endpointDefinition = getEndpointDefinition(context, endpointName);
      const onQueryStarted = endpointDefinition?.onQueryStarted;
      if (onQueryStarted) {
        const lifecycle = {};
        const queryFulfilled = new Promise((resolve, reject) => {
          lifecycle.resolve = resolve;
          lifecycle.reject = reject;
        });
        queryFulfilled.catch(() => {
        });
        lifecycleMap[requestId] = lifecycle;
        const selector = api.endpoints[endpointName].select(isAnyQueryDefinition(endpointDefinition) ? originalArgs : requestId);
        const extra = mwApi.dispatch((_, __, extra2) => extra2);
        const lifecycleApi = {
          ...mwApi,
          getCacheEntry: () => selector(mwApi.getState()),
          requestId,
          extra,
          updateCachedData: isAnyQueryDefinition(endpointDefinition) ? (updateRecipe) => mwApi.dispatch(api.util.updateQueryData(endpointName, originalArgs, updateRecipe)) : void 0,
          queryFulfilled
        };
        onQueryStarted(originalArgs, lifecycleApi);
      }
    } else if (isFullfilledThunk(action)) {
      const {
        requestId,
        baseQueryMeta
      } = action.meta;
      lifecycleMap[requestId]?.resolve({
        data: action.payload,
        meta: baseQueryMeta
      });
      delete lifecycleMap[requestId];
    } else if (isRejectedThunk(action)) {
      const {
        requestId,
        rejectedWithValue,
        baseQueryMeta
      } = action.meta;
      lifecycleMap[requestId]?.reject({
        error: action.payload ?? action.error,
        isUnhandledError: !rejectedWithValue,
        meta: baseQueryMeta
      });
      delete lifecycleMap[requestId];
    }
  };
  return handler;
};

// src/query/core/buildMiddleware/windowEventHandling.ts
var buildWindowEventHandler = ({
  reducerPath,
  context,
  api,
  refetchQuery,
  internalState
}) => {
  const {
    removeQueryResult
  } = api.internalActions;
  const handler = (action, mwApi) => {
    if (onFocus.match(action)) {
      refetchValidQueries(mwApi, "refetchOnFocus");
    }
    if (onOnline.match(action)) {
      refetchValidQueries(mwApi, "refetchOnReconnect");
    }
  };
  function refetchValidQueries(api2, type) {
    const state = api2.getState()[reducerPath];
    const queries = state.queries;
    const subscriptions = internalState.currentSubscriptions;
    context.batch(() => {
      for (const queryCacheKey of subscriptions.keys()) {
        const querySubState = queries[queryCacheKey];
        const subscriptionSubState = subscriptions.get(queryCacheKey);
        if (!subscriptionSubState || !querySubState) continue;
        const values = [...subscriptionSubState.values()];
        const shouldRefetch = values.some((sub) => sub[type] === true) || values.every((sub) => sub[type] === void 0) && state.config[type];
        if (shouldRefetch) {
          if (subscriptionSubState.size === 0) {
            api2.dispatch(removeQueryResult({
              queryCacheKey
            }));
          } else if (querySubState.status !== STATUS_UNINITIALIZED) {
            api2.dispatch(refetchQuery(querySubState));
          }
        }
      }
    });
  }
  return handler;
};

// src/query/core/buildMiddleware/index.ts
function buildMiddleware(input) {
  const {
    reducerPath,
    queryThunk,
    api,
    context,
    getInternalState
  } = input;
  const {
    apiUid
  } = context;
  const actions2 = {
    invalidateTags: (0,_reduxjs_toolkit__WEBPACK_IMPORTED_MODULE_0__.createAction)(`${reducerPath}/invalidateTags`)
  };
  const isThisApiSliceAction = (action) => action.type.startsWith(`${reducerPath}/`);
  const handlerBuilders = [buildDevCheckHandler, buildCacheCollectionHandler, buildInvalidationByTagsHandler, buildPollingHandler, buildCacheLifecycleHandler, buildQueryLifecycleHandler];
  const middleware = (mwApi) => {
    let initialized2 = false;
    const internalState = getInternalState(mwApi.dispatch);
    const builderArgs = {
      ...input,
      internalState,
      refetchQuery,
      isThisApiSliceAction,
      mwApi
    };
    const handlers = handlerBuilders.map((build) => build(builderArgs));
    const batchedActionsHandler = buildBatchedActionsHandler(builderArgs);
    const windowEventsHandler = buildWindowEventHandler(builderArgs);
    return (next) => {
      return (action) => {
        if (!(0,_reduxjs_toolkit__WEBPACK_IMPORTED_MODULE_1__.isAction)(action)) {
          return next(action);
        }
        if (!initialized2) {
          initialized2 = true;
          mwApi.dispatch(api.internalActions.middlewareRegistered(apiUid));
        }
        const mwApiWithNext = {
          ...mwApi,
          next
        };
        const stateBefore = mwApi.getState();
        const [actionShouldContinue, internalProbeResult] = batchedActionsHandler(action, mwApiWithNext, stateBefore);
        let res;
        if (actionShouldContinue) {
          res = next(action);
        } else {
          res = internalProbeResult;
        }
        if (!!mwApi.getState()[reducerPath]) {
          windowEventsHandler(action, mwApiWithNext, stateBefore);
          if (isThisApiSliceAction(action) || context.hasRehydrationInfo(action)) {
            for (const handler of handlers) {
              handler(action, mwApiWithNext, stateBefore);
            }
          }
        }
        return res;
      };
    };
  };
  return {
    middleware,
    actions: actions2
  };
  function refetchQuery(querySubState) {
    return input.api.endpoints[querySubState.endpointName].initiate(querySubState.originalArgs, {
      subscribe: false,
      forceRefetch: true
    });
  }
}

// src/query/core/module.ts
var coreModuleName = /* @__PURE__ */ Symbol();
var coreModule = ({
  createSelector: createSelector2 = _reduxjs_toolkit__WEBPACK_IMPORTED_MODULE_3__.createSelector
} = {}) => ({
  name: coreModuleName,
  init(api, {
    baseQuery,
    tagTypes,
    reducerPath,
    serializeQueryArgs,
    keepUnusedDataFor,
    refetchOnMountOrArgChange,
    refetchOnFocus,
    refetchOnReconnect,
    invalidationBehavior,
    onSchemaFailure,
    catchSchemaFailure,
    skipSchemaValidation
  }, context) {
    (0,_reduxjs_toolkit__WEBPACK_IMPORTED_MODULE_2__.enablePatches)();
    assertCast(serializeQueryArgs);
    const assertTagType = (tag) => {
      if (typeof process !== "undefined" && "development" === "development") {
        if (!tagTypes.includes(tag.type)) {
          console.error(`Tag type '${tag.type}' was used, but not specified in \`tagTypes\`!`);
        }
      }
      return tag;
    };
    Object.assign(api, {
      reducerPath,
      endpoints: {},
      internalActions: {
        onOnline,
        onOffline,
        onFocus,
        onFocusLost
      },
      util: {}
    });
    const selectors = buildSelectors({
      serializeQueryArgs,
      reducerPath,
      createSelector: createSelector2
    });
    const {
      selectInvalidatedBy,
      selectCachedArgsForQuery,
      buildQuerySelector,
      buildInfiniteQuerySelector,
      buildMutationSelector
    } = selectors;
    safeAssign(api.util, {
      selectInvalidatedBy,
      selectCachedArgsForQuery
    });
    const {
      queryThunk,
      infiniteQueryThunk,
      mutationThunk,
      patchQueryData,
      updateQueryData,
      upsertQueryData,
      prefetch,
      buildMatchThunkActions
    } = buildThunks({
      baseQuery,
      reducerPath,
      context,
      api,
      serializeQueryArgs,
      assertTagType,
      selectors,
      onSchemaFailure,
      catchSchemaFailure,
      skipSchemaValidation
    });
    const {
      reducer,
      actions: sliceActions
    } = buildSlice({
      context,
      queryThunk,
      infiniteQueryThunk,
      mutationThunk,
      serializeQueryArgs,
      reducerPath,
      assertTagType,
      config: {
        refetchOnFocus,
        refetchOnReconnect,
        refetchOnMountOrArgChange,
        keepUnusedDataFor,
        reducerPath,
        invalidationBehavior
      }
    });
    safeAssign(api.util, {
      patchQueryData,
      updateQueryData,
      upsertQueryData,
      prefetch,
      resetApiState: sliceActions.resetApiState,
      upsertQueryEntries: sliceActions.cacheEntriesUpserted
    });
    safeAssign(api.internalActions, sliceActions);
    const internalStateMap = /* @__PURE__ */ new WeakMap();
    const getInternalState = (dispatch) => {
      const state = getOrInsertComputed(internalStateMap, dispatch, () => ({
        currentSubscriptions: /* @__PURE__ */ new Map(),
        currentPolls: /* @__PURE__ */ new Map(),
        runningQueries: /* @__PURE__ */ new Map(),
        runningMutations: /* @__PURE__ */ new Map()
      }));
      return state;
    };
    const {
      buildInitiateQuery,
      buildInitiateInfiniteQuery,
      buildInitiateMutation,
      getRunningMutationThunk,
      getRunningMutationsThunk,
      getRunningQueriesThunk,
      getRunningQueryThunk
    } = buildInitiate({
      queryThunk,
      mutationThunk,
      infiniteQueryThunk,
      api,
      serializeQueryArgs,
      context,
      getInternalState
    });
    safeAssign(api.util, {
      getRunningMutationThunk,
      getRunningMutationsThunk,
      getRunningQueryThunk,
      getRunningQueriesThunk
    });
    const {
      middleware,
      actions: middlewareActions
    } = buildMiddleware({
      reducerPath,
      context,
      queryThunk,
      mutationThunk,
      infiniteQueryThunk,
      api,
      assertTagType,
      selectors,
      getRunningQueryThunk,
      getInternalState
    });
    safeAssign(api.util, middlewareActions);
    safeAssign(api, {
      reducer,
      middleware
    });
    return {
      name: coreModuleName,
      injectEndpoint(endpointName, definition) {
        const anyApi = api;
        const endpoint = anyApi.endpoints[endpointName] ??= {};
        if (isQueryDefinition(definition)) {
          safeAssign(endpoint, {
            name: endpointName,
            select: buildQuerySelector(endpointName, definition),
            initiate: buildInitiateQuery(endpointName, definition)
          }, buildMatchThunkActions(queryThunk, endpointName));
        }
        if (isMutationDefinition(definition)) {
          safeAssign(endpoint, {
            name: endpointName,
            select: buildMutationSelector(),
            initiate: buildInitiateMutation(endpointName)
          }, buildMatchThunkActions(mutationThunk, endpointName));
        }
        if (isInfiniteQueryDefinition(definition)) {
          safeAssign(endpoint, {
            name: endpointName,
            select: buildInfiniteQuerySelector(endpointName, definition),
            initiate: buildInitiateInfiniteQuery(endpointName, definition)
          }, buildMatchThunkActions(queryThunk, endpointName));
        }
      }
    };
  }
});

// src/query/core/index.ts
var createApi = /* @__PURE__ */ buildCreateApi(coreModule());

//# sourceMappingURL=rtk-query.modern.mjs.map

/***/ },

/***/ "./node_modules/@reduxjs/toolkit/dist/redux-toolkit.modern.mjs"
/*!*********************************************************************!*\
  !*** ./node_modules/@reduxjs/toolkit/dist/redux-toolkit.modern.mjs ***!
  \*********************************************************************/
(__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   ReducerType: () => (/* binding */ ReducerType),
/* harmony export */   SHOULD_AUTOBATCH: () => (/* binding */ SHOULD_AUTOBATCH),
/* harmony export */   TaskAbortError: () => (/* binding */ TaskAbortError),
/* harmony export */   Tuple: () => (/* binding */ Tuple),
/* harmony export */   __DO_NOT_USE__ActionTypes: () => (/* reexport safe */ redux__WEBPACK_IMPORTED_MODULE_0__.__DO_NOT_USE__ActionTypes),
/* harmony export */   addListener: () => (/* binding */ addListener),
/* harmony export */   applyMiddleware: () => (/* reexport safe */ redux__WEBPACK_IMPORTED_MODULE_0__.applyMiddleware),
/* harmony export */   asyncThunkCreator: () => (/* binding */ asyncThunkCreator),
/* harmony export */   autoBatchEnhancer: () => (/* binding */ autoBatchEnhancer),
/* harmony export */   bindActionCreators: () => (/* reexport safe */ redux__WEBPACK_IMPORTED_MODULE_0__.bindActionCreators),
/* harmony export */   buildCreateSlice: () => (/* binding */ buildCreateSlice),
/* harmony export */   clearAllListeners: () => (/* binding */ clearAllListeners),
/* harmony export */   combineReducers: () => (/* reexport safe */ redux__WEBPACK_IMPORTED_MODULE_0__.combineReducers),
/* harmony export */   combineSlices: () => (/* binding */ combineSlices),
/* harmony export */   compose: () => (/* reexport safe */ redux__WEBPACK_IMPORTED_MODULE_0__.compose),
/* harmony export */   configureStore: () => (/* binding */ configureStore),
/* harmony export */   createAction: () => (/* binding */ createAction),
/* harmony export */   createActionCreatorInvariantMiddleware: () => (/* binding */ createActionCreatorInvariantMiddleware),
/* harmony export */   createAsyncThunk: () => (/* binding */ createAsyncThunk),
/* harmony export */   createDraftSafeSelector: () => (/* binding */ createDraftSafeSelector),
/* harmony export */   createDraftSafeSelectorCreator: () => (/* binding */ createDraftSafeSelectorCreator),
/* harmony export */   createDynamicMiddleware: () => (/* binding */ createDynamicMiddleware),
/* harmony export */   createEntityAdapter: () => (/* binding */ createEntityAdapter),
/* harmony export */   createImmutableStateInvariantMiddleware: () => (/* binding */ createImmutableStateInvariantMiddleware),
/* harmony export */   createListenerMiddleware: () => (/* binding */ createListenerMiddleware),
/* harmony export */   createNextState: () => (/* reexport safe */ immer__WEBPACK_IMPORTED_MODULE_1__.produce),
/* harmony export */   createReducer: () => (/* binding */ createReducer),
/* harmony export */   createSelector: () => (/* reexport safe */ reselect__WEBPACK_IMPORTED_MODULE_2__.createSelector),
/* harmony export */   createSelectorCreator: () => (/* reexport safe */ reselect__WEBPACK_IMPORTED_MODULE_2__.createSelectorCreator),
/* harmony export */   createSerializableStateInvariantMiddleware: () => (/* binding */ createSerializableStateInvariantMiddleware),
/* harmony export */   createSlice: () => (/* binding */ createSlice),
/* harmony export */   createStore: () => (/* reexport safe */ redux__WEBPACK_IMPORTED_MODULE_0__.createStore),
/* harmony export */   current: () => (/* reexport safe */ immer__WEBPACK_IMPORTED_MODULE_1__.current),
/* harmony export */   findNonSerializableValue: () => (/* binding */ findNonSerializableValue),
/* harmony export */   formatProdErrorMessage: () => (/* binding */ formatProdErrorMessage),
/* harmony export */   freeze: () => (/* reexport safe */ immer__WEBPACK_IMPORTED_MODULE_1__.freeze),
/* harmony export */   isAction: () => (/* reexport safe */ redux__WEBPACK_IMPORTED_MODULE_0__.isAction),
/* harmony export */   isActionCreator: () => (/* binding */ isActionCreator),
/* harmony export */   isAllOf: () => (/* binding */ isAllOf),
/* harmony export */   isAnyOf: () => (/* binding */ isAnyOf),
/* harmony export */   isAsyncThunkAction: () => (/* binding */ isAsyncThunkAction),
/* harmony export */   isDraft: () => (/* reexport safe */ immer__WEBPACK_IMPORTED_MODULE_1__.isDraft),
/* harmony export */   isFluxStandardAction: () => (/* binding */ isFSA),
/* harmony export */   isFulfilled: () => (/* binding */ isFulfilled),
/* harmony export */   isImmutableDefault: () => (/* binding */ isImmutableDefault),
/* harmony export */   isPending: () => (/* binding */ isPending),
/* harmony export */   isPlain: () => (/* binding */ isPlain),
/* harmony export */   isPlainObject: () => (/* reexport safe */ redux__WEBPACK_IMPORTED_MODULE_0__.isPlainObject),
/* harmony export */   isRejected: () => (/* binding */ isRejected),
/* harmony export */   isRejectedWithValue: () => (/* binding */ isRejectedWithValue),
/* harmony export */   legacy_createStore: () => (/* reexport safe */ redux__WEBPACK_IMPORTED_MODULE_0__.legacy_createStore),
/* harmony export */   lruMemoize: () => (/* reexport safe */ reselect__WEBPACK_IMPORTED_MODULE_2__.lruMemoize),
/* harmony export */   miniSerializeError: () => (/* binding */ miniSerializeError),
/* harmony export */   nanoid: () => (/* binding */ nanoid),
/* harmony export */   original: () => (/* reexport safe */ immer__WEBPACK_IMPORTED_MODULE_1__.original),
/* harmony export */   prepareAutoBatched: () => (/* binding */ prepareAutoBatched),
/* harmony export */   removeListener: () => (/* binding */ removeListener),
/* harmony export */   unwrapResult: () => (/* binding */ unwrapResult),
/* harmony export */   weakMapMemoize: () => (/* reexport safe */ reselect__WEBPACK_IMPORTED_MODULE_2__.weakMapMemoize)
/* harmony export */ });
/* harmony import */ var redux__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! redux */ "./node_modules/redux/dist/redux.mjs");
/* harmony import */ var immer__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! immer */ "./node_modules/immer/dist/immer.mjs");
/* harmony import */ var reselect__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! reselect */ "./node_modules/reselect/dist/reselect.mjs");
/* harmony import */ var redux_thunk__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! redux-thunk */ "./node_modules/redux-thunk/dist/redux-thunk.mjs");
// src/index.ts

// src/immerImports.ts

// src/index.ts

// src/reselectImports.ts

// src/createDraftSafeSelector.ts
var createDraftSafeSelectorCreator = (...args) => {
  const createSelector2 = (0,reselect__WEBPACK_IMPORTED_MODULE_2__.createSelectorCreator)(...args);
  const createDraftSafeSelector2 = Object.assign((...args2) => {
    const selector = createSelector2(...args2);
    const wrappedSelector = (value, ...rest) => selector((0,immer__WEBPACK_IMPORTED_MODULE_1__.isDraft)(value) ? (0,immer__WEBPACK_IMPORTED_MODULE_1__.current)(value) : value, ...rest);
    Object.assign(wrappedSelector, selector);
    return wrappedSelector;
  }, {
    withTypes: () => createDraftSafeSelector2
  });
  return createDraftSafeSelector2;
};
var createDraftSafeSelector = /* @__PURE__ */ createDraftSafeSelectorCreator(reselect__WEBPACK_IMPORTED_MODULE_2__.weakMapMemoize);

// src/reduxImports.ts

// src/devtoolsExtension.ts
var composeWithDevTools = typeof window !== "undefined" && window.__REDUX_DEVTOOLS_EXTENSION_COMPOSE__ ? window.__REDUX_DEVTOOLS_EXTENSION_COMPOSE__ : function() {
  if (arguments.length === 0) return void 0;
  if (typeof arguments[0] === "object") return redux__WEBPACK_IMPORTED_MODULE_0__.compose;
  return redux__WEBPACK_IMPORTED_MODULE_0__.compose.apply(null, arguments);
};
var devToolsEnhancer = typeof window !== "undefined" && window.__REDUX_DEVTOOLS_EXTENSION__ ? window.__REDUX_DEVTOOLS_EXTENSION__ : function() {
  return function(noop3) {
    return noop3;
  };
};

// src/getDefaultMiddleware.ts

// src/tsHelpers.ts
var hasMatchFunction = (v) => {
  return v && typeof v.match === "function";
};

// src/createAction.ts
function createAction(type, prepareAction) {
  function actionCreator(...args) {
    if (prepareAction) {
      let prepared = prepareAction(...args);
      if (!prepared) {
        throw new Error( false ? 0 : "prepareAction did not return an object");
      }
      return {
        type,
        payload: prepared.payload,
        ..."meta" in prepared && {
          meta: prepared.meta
        },
        ..."error" in prepared && {
          error: prepared.error
        }
      };
    }
    return {
      type,
      payload: args[0]
    };
  }
  actionCreator.toString = () => `${type}`;
  actionCreator.type = type;
  actionCreator.match = (action) => (0,redux__WEBPACK_IMPORTED_MODULE_0__.isAction)(action) && action.type === type;
  return actionCreator;
}
function isActionCreator(action) {
  return typeof action === "function" && "type" in action && // hasMatchFunction only wants Matchers but I don't see the point in rewriting it
  hasMatchFunction(action);
}
function isFSA(action) {
  return (0,redux__WEBPACK_IMPORTED_MODULE_0__.isAction)(action) && Object.keys(action).every(isValidKey);
}
function isValidKey(key) {
  return ["type", "payload", "error", "meta"].indexOf(key) > -1;
}

// src/actionCreatorInvariantMiddleware.ts
function getMessage(type) {
  const splitType = type ? `${type}`.split("/") : [];
  const actionName = splitType[splitType.length - 1] || "actionCreator";
  return `Detected an action creator with type "${type || "unknown"}" being dispatched. 
Make sure you're calling the action creator before dispatching, i.e. \`dispatch(${actionName}())\` instead of \`dispatch(${actionName})\`. This is necessary even if the action has no payload.`;
}
function createActionCreatorInvariantMiddleware(options = {}) {
  if (false) // removed by dead control flow
{}
  const {
    isActionCreator: isActionCreator2 = isActionCreator
  } = options;
  return () => (next) => (action) => {
    if (isActionCreator2(action)) {
      console.warn(getMessage(action.type));
    }
    return next(action);
  };
}

// src/utils.ts
function getTimeMeasureUtils(maxDelay, fnName) {
  let elapsed = 0;
  return {
    measureTime(fn) {
      const started = Date.now();
      try {
        return fn();
      } finally {
        const finished = Date.now();
        elapsed += finished - started;
      }
    },
    warnIfExceeded() {
      if (elapsed > maxDelay) {
        console.warn(`${fnName} took ${elapsed}ms, which is more than the warning threshold of ${maxDelay}ms. 
If your state or actions are very large, you may want to disable the middleware as it might cause too much of a slowdown in development mode. See https://redux-toolkit.js.org/api/getDefaultMiddleware for instructions.
It is disabled in production builds, so you don't need to worry about that.`);
      }
    }
  };
}
var Tuple = class _Tuple extends Array {
  constructor(...items) {
    super(...items);
    Object.setPrototypeOf(this, _Tuple.prototype);
  }
  static get [Symbol.species]() {
    return _Tuple;
  }
  concat(...arr) {
    return super.concat.apply(this, arr);
  }
  prepend(...arr) {
    if (arr.length === 1 && Array.isArray(arr[0])) {
      return new _Tuple(...arr[0].concat(this));
    }
    return new _Tuple(...arr.concat(this));
  }
};
function freezeDraftable(val) {
  return (0,immer__WEBPACK_IMPORTED_MODULE_1__.isDraftable)(val) ? (0,immer__WEBPACK_IMPORTED_MODULE_1__.produce)(val, () => {
  }) : val;
}
function getOrInsertComputed(map, key, compute) {
  if (map.has(key)) return map.get(key);
  return map.set(key, compute(key)).get(key);
}

// src/immutableStateInvariantMiddleware.ts
function isImmutableDefault(value) {
  return typeof value !== "object" || value == null || Object.isFrozen(value);
}
function trackForMutations(isImmutable, ignoredPaths, obj) {
  const trackedProperties = trackProperties(isImmutable, ignoredPaths, obj);
  return {
    detectMutations() {
      return detectMutations(isImmutable, ignoredPaths, trackedProperties, obj);
    }
  };
}
function trackProperties(isImmutable, ignoredPaths = [], obj, path = "", checkedObjects = /* @__PURE__ */ new Set()) {
  const tracked = {
    value: obj
  };
  if (!isImmutable(obj) && !checkedObjects.has(obj)) {
    checkedObjects.add(obj);
    tracked.children = {};
    const hasIgnoredPaths = ignoredPaths.length > 0;
    for (const key in obj) {
      const nestedPath = path ? path + "." + key : key;
      if (hasIgnoredPaths) {
        const hasMatches = ignoredPaths.some((ignored) => {
          if (ignored instanceof RegExp) {
            return ignored.test(nestedPath);
          }
          return nestedPath === ignored;
        });
        if (hasMatches) {
          continue;
        }
      }
      tracked.children[key] = trackProperties(isImmutable, ignoredPaths, obj[key], nestedPath);
    }
  }
  return tracked;
}
function detectMutations(isImmutable, ignoredPaths = [], trackedProperty, obj, sameParentRef = false, path = "") {
  const prevObj = trackedProperty ? trackedProperty.value : void 0;
  const sameRef = prevObj === obj;
  if (sameParentRef && !sameRef && !Number.isNaN(obj)) {
    return {
      wasMutated: true,
      path
    };
  }
  if (isImmutable(prevObj) || isImmutable(obj)) {
    return {
      wasMutated: false
    };
  }
  const keysToDetect = {};
  for (let key in trackedProperty.children) {
    keysToDetect[key] = true;
  }
  for (let key in obj) {
    keysToDetect[key] = true;
  }
  const hasIgnoredPaths = ignoredPaths.length > 0;
  for (let key in keysToDetect) {
    const nestedPath = path ? path + "." + key : key;
    if (hasIgnoredPaths) {
      const hasMatches = ignoredPaths.some((ignored) => {
        if (ignored instanceof RegExp) {
          return ignored.test(nestedPath);
        }
        return nestedPath === ignored;
      });
      if (hasMatches) {
        continue;
      }
    }
    const result = detectMutations(isImmutable, ignoredPaths, trackedProperty.children[key], obj[key], sameRef, nestedPath);
    if (result.wasMutated) {
      return result;
    }
  }
  return {
    wasMutated: false
  };
}
function createImmutableStateInvariantMiddleware(options = {}) {
  if (false) // removed by dead control flow
{} else {
    let stringify2 = function(obj, serializer, indent, decycler) {
      return JSON.stringify(obj, getSerialize2(serializer, decycler), indent);
    }, getSerialize2 = function(serializer, decycler) {
      let stack = [], keys = [];
      if (!decycler) decycler = function(_, value) {
        if (stack[0] === value) return "[Circular ~]";
        return "[Circular ~." + keys.slice(0, stack.indexOf(value)).join(".") + "]";
      };
      return function(key, value) {
        if (stack.length > 0) {
          var thisPos = stack.indexOf(this);
          ~thisPos ? stack.splice(thisPos + 1) : stack.push(this);
          ~thisPos ? keys.splice(thisPos, Infinity, key) : keys.push(key);
          if (~stack.indexOf(value)) value = decycler.call(this, key, value);
        } else stack.push(value);
        return serializer == null ? value : serializer.call(this, key, value);
      };
    };
    var stringify = stringify2, getSerialize = getSerialize2;
    let {
      isImmutable = isImmutableDefault,
      ignoredPaths,
      warnAfter = 32
    } = options;
    const track = trackForMutations.bind(null, isImmutable, ignoredPaths);
    return ({
      getState
    }) => {
      let state = getState();
      let tracker = track(state);
      let result;
      return (next) => (action) => {
        const measureUtils = getTimeMeasureUtils(warnAfter, "ImmutableStateInvariantMiddleware");
        measureUtils.measureTime(() => {
          state = getState();
          result = tracker.detectMutations();
          tracker = track(state);
          if (result.wasMutated) {
            throw new Error( false ? 0 : `A state mutation was detected between dispatches, in the path '${result.path || ""}'.  This may cause incorrect behavior. (https://redux.js.org/style-guide/style-guide#do-not-mutate-state)`);
          }
        });
        const dispatchedAction = next(action);
        measureUtils.measureTime(() => {
          state = getState();
          result = tracker.detectMutations();
          tracker = track(state);
          if (result.wasMutated) {
            throw new Error( false ? 0 : `A state mutation was detected inside a dispatch, in the path: ${result.path || ""}. Take a look at the reducer(s) handling the action ${stringify2(action)}. (https://redux.js.org/style-guide/style-guide#do-not-mutate-state)`);
          }
        });
        measureUtils.warnIfExceeded();
        return dispatchedAction;
      };
    };
  }
}

// src/serializableStateInvariantMiddleware.ts
function isPlain(val) {
  const type = typeof val;
  return val == null || type === "string" || type === "boolean" || type === "number" || Array.isArray(val) || (0,redux__WEBPACK_IMPORTED_MODULE_0__.isPlainObject)(val);
}
function findNonSerializableValue(value, path = "", isSerializable = isPlain, getEntries, ignoredPaths = [], cache) {
  let foundNestedSerializable;
  if (!isSerializable(value)) {
    return {
      keyPath: path || "<root>",
      value
    };
  }
  if (typeof value !== "object" || value === null) {
    return false;
  }
  if (cache?.has(value)) return false;
  const entries = getEntries != null ? getEntries(value) : Object.entries(value);
  const hasIgnoredPaths = ignoredPaths.length > 0;
  for (const [key, nestedValue] of entries) {
    const nestedPath = path ? path + "." + key : key;
    if (hasIgnoredPaths) {
      const hasMatches = ignoredPaths.some((ignored) => {
        if (ignored instanceof RegExp) {
          return ignored.test(nestedPath);
        }
        return nestedPath === ignored;
      });
      if (hasMatches) {
        continue;
      }
    }
    if (!isSerializable(nestedValue)) {
      return {
        keyPath: nestedPath,
        value: nestedValue
      };
    }
    if (typeof nestedValue === "object") {
      foundNestedSerializable = findNonSerializableValue(nestedValue, nestedPath, isSerializable, getEntries, ignoredPaths, cache);
      if (foundNestedSerializable) {
        return foundNestedSerializable;
      }
    }
  }
  if (cache && isNestedFrozen(value)) cache.add(value);
  return false;
}
function isNestedFrozen(value) {
  if (!Object.isFrozen(value)) return false;
  for (const nestedValue of Object.values(value)) {
    if (typeof nestedValue !== "object" || nestedValue === null) continue;
    if (!isNestedFrozen(nestedValue)) return false;
  }
  return true;
}
function createSerializableStateInvariantMiddleware(options = {}) {
  if (false) // removed by dead control flow
{} else {
    const {
      isSerializable = isPlain,
      getEntries,
      ignoredActions = [],
      ignoredActionPaths = ["meta.arg", "meta.baseQueryMeta"],
      ignoredPaths = [],
      warnAfter = 32,
      ignoreState = false,
      ignoreActions = false,
      disableCache = false
    } = options;
    const cache = !disableCache && WeakSet ? /* @__PURE__ */ new WeakSet() : void 0;
    return (storeAPI) => (next) => (action) => {
      if (!(0,redux__WEBPACK_IMPORTED_MODULE_0__.isAction)(action)) {
        return next(action);
      }
      const result = next(action);
      const measureUtils = getTimeMeasureUtils(warnAfter, "SerializableStateInvariantMiddleware");
      if (!ignoreActions && !(ignoredActions.length && ignoredActions.indexOf(action.type) !== -1)) {
        measureUtils.measureTime(() => {
          const foundActionNonSerializableValue = findNonSerializableValue(action, "", isSerializable, getEntries, ignoredActionPaths, cache);
          if (foundActionNonSerializableValue) {
            const {
              keyPath,
              value
            } = foundActionNonSerializableValue;
            console.error(`A non-serializable value was detected in an action, in the path: \`${keyPath}\`. Value:`, value, "\nTake a look at the logic that dispatched this action: ", action, "\n(See https://redux.js.org/faq/actions#why-should-type-be-a-string-or-at-least-serializable-why-should-my-action-types-be-constants)", "\n(To allow non-serializable values see: https://redux-toolkit.js.org/usage/usage-guide#working-with-non-serializable-data)");
          }
        });
      }
      if (!ignoreState) {
        measureUtils.measureTime(() => {
          const state = storeAPI.getState();
          const foundStateNonSerializableValue = findNonSerializableValue(state, "", isSerializable, getEntries, ignoredPaths, cache);
          if (foundStateNonSerializableValue) {
            const {
              keyPath,
              value
            } = foundStateNonSerializableValue;
            console.error(`A non-serializable value was detected in the state, in the path: \`${keyPath}\`. Value:`, value, `
Take a look at the reducer(s) handling this action type: ${action.type}.
(See https://redux.js.org/faq/organizing-state#can-i-put-functions-promises-or-other-non-serializable-items-in-my-store-state)`);
          }
        });
        measureUtils.warnIfExceeded();
      }
      return result;
    };
  }
}

// src/getDefaultMiddleware.ts
function isBoolean(x) {
  return typeof x === "boolean";
}
var buildGetDefaultMiddleware = () => function getDefaultMiddleware(options) {
  const {
    thunk = true,
    immutableCheck = true,
    serializableCheck = true,
    actionCreatorCheck = true
  } = options ?? {};
  let middlewareArray = new Tuple();
  if (thunk) {
    if (isBoolean(thunk)) {
      middlewareArray.push(redux_thunk__WEBPACK_IMPORTED_MODULE_3__.thunk);
    } else {
      middlewareArray.push((0,redux_thunk__WEBPACK_IMPORTED_MODULE_3__.withExtraArgument)(thunk.extraArgument));
    }
  }
  if (true) {
    if (immutableCheck) {
      let immutableOptions = {};
      if (!isBoolean(immutableCheck)) {
        immutableOptions = immutableCheck;
      }
      middlewareArray.unshift(createImmutableStateInvariantMiddleware(immutableOptions));
    }
    if (serializableCheck) {
      let serializableOptions = {};
      if (!isBoolean(serializableCheck)) {
        serializableOptions = serializableCheck;
      }
      middlewareArray.push(createSerializableStateInvariantMiddleware(serializableOptions));
    }
    if (actionCreatorCheck) {
      let actionCreatorOptions = {};
      if (!isBoolean(actionCreatorCheck)) {
        actionCreatorOptions = actionCreatorCheck;
      }
      middlewareArray.unshift(createActionCreatorInvariantMiddleware(actionCreatorOptions));
    }
  }
  return middlewareArray;
};

// src/autoBatchEnhancer.ts
var SHOULD_AUTOBATCH = "RTK_autoBatch";
var prepareAutoBatched = () => (payload) => ({
  payload,
  meta: {
    [SHOULD_AUTOBATCH]: true
  }
});
var createQueueWithTimer = (timeout) => {
  return (notify) => {
    setTimeout(notify, timeout);
  };
};
var autoBatchEnhancer = (options = {
  type: "raf"
}) => (next) => (...args) => {
  const store = next(...args);
  let notifying = true;
  let shouldNotifyAtEndOfTick = false;
  let notificationQueued = false;
  const listeners = /* @__PURE__ */ new Set();
  const queueCallback = options.type === "tick" ? queueMicrotask : options.type === "raf" ? (
    // requestAnimationFrame won't exist in SSR environments. Fall back to a vague approximation just to keep from erroring.
    typeof window !== "undefined" && window.requestAnimationFrame ? window.requestAnimationFrame : createQueueWithTimer(10)
  ) : options.type === "callback" ? options.queueNotification : createQueueWithTimer(options.timeout);
  const notifyListeners = () => {
    notificationQueued = false;
    if (shouldNotifyAtEndOfTick) {
      shouldNotifyAtEndOfTick = false;
      listeners.forEach((l) => l());
    }
  };
  return Object.assign({}, store, {
    // Override the base `store.subscribe` method to keep original listeners
    // from running if we're delaying notifications
    subscribe(listener2) {
      const wrappedListener = () => notifying && listener2();
      const unsubscribe = store.subscribe(wrappedListener);
      listeners.add(listener2);
      return () => {
        unsubscribe();
        listeners.delete(listener2);
      };
    },
    // Override the base `store.dispatch` method so that we can check actions
    // for the `shouldAutoBatch` flag and determine if batching is active
    dispatch(action) {
      try {
        notifying = !action?.meta?.[SHOULD_AUTOBATCH];
        shouldNotifyAtEndOfTick = !notifying;
        if (shouldNotifyAtEndOfTick) {
          if (!notificationQueued) {
            notificationQueued = true;
            queueCallback(notifyListeners);
          }
        }
        return store.dispatch(action);
      } finally {
        notifying = true;
      }
    }
  });
};

// src/getDefaultEnhancers.ts
var buildGetDefaultEnhancers = (middlewareEnhancer) => function getDefaultEnhancers(options) {
  const {
    autoBatch = true
  } = options ?? {};
  let enhancerArray = new Tuple(middlewareEnhancer);
  if (autoBatch) {
    enhancerArray.push(autoBatchEnhancer(typeof autoBatch === "object" ? autoBatch : void 0));
  }
  return enhancerArray;
};

// src/configureStore.ts
function configureStore(options) {
  const getDefaultMiddleware = buildGetDefaultMiddleware();
  const {
    reducer = void 0,
    middleware,
    devTools = true,
    duplicateMiddlewareCheck = true,
    preloadedState = void 0,
    enhancers = void 0
  } = options || {};
  let rootReducer;
  if (typeof reducer === "function") {
    rootReducer = reducer;
  } else if ((0,redux__WEBPACK_IMPORTED_MODULE_0__.isPlainObject)(reducer)) {
    rootReducer = (0,redux__WEBPACK_IMPORTED_MODULE_0__.combineReducers)(reducer);
  } else {
    throw new Error( false ? 0 : "`reducer` is a required argument, and must be a function or an object of functions that can be passed to combineReducers");
  }
  if ( true && middleware && typeof middleware !== "function") {
    throw new Error( false ? 0 : "`middleware` field must be a callback");
  }
  let finalMiddleware;
  if (typeof middleware === "function") {
    finalMiddleware = middleware(getDefaultMiddleware);
    if ( true && !Array.isArray(finalMiddleware)) {
      throw new Error( false ? 0 : "when using a middleware builder function, an array of middleware must be returned");
    }
  } else {
    finalMiddleware = getDefaultMiddleware();
  }
  if ( true && finalMiddleware.some((item) => typeof item !== "function")) {
    throw new Error( false ? 0 : "each middleware provided to configureStore must be a function");
  }
  if ( true && duplicateMiddlewareCheck) {
    let middlewareReferences = /* @__PURE__ */ new Set();
    finalMiddleware.forEach((middleware2) => {
      if (middlewareReferences.has(middleware2)) {
        throw new Error( false ? 0 : "Duplicate middleware references found when creating the store. Ensure that each middleware is only included once.");
      }
      middlewareReferences.add(middleware2);
    });
  }
  let finalCompose = redux__WEBPACK_IMPORTED_MODULE_0__.compose;
  if (devTools) {
    finalCompose = composeWithDevTools({
      // Enable capture of stack traces for dispatched Redux actions
      trace: "development" !== "production",
      ...typeof devTools === "object" && devTools
    });
  }
  const middlewareEnhancer = (0,redux__WEBPACK_IMPORTED_MODULE_0__.applyMiddleware)(...finalMiddleware);
  const getDefaultEnhancers = buildGetDefaultEnhancers(middlewareEnhancer);
  if ( true && enhancers && typeof enhancers !== "function") {
    throw new Error( false ? 0 : "`enhancers` field must be a callback");
  }
  let storeEnhancers = typeof enhancers === "function" ? enhancers(getDefaultEnhancers) : getDefaultEnhancers();
  if ( true && !Array.isArray(storeEnhancers)) {
    throw new Error( false ? 0 : "`enhancers` callback must return an array");
  }
  if ( true && storeEnhancers.some((item) => typeof item !== "function")) {
    throw new Error( false ? 0 : "each enhancer provided to configureStore must be a function");
  }
  if ( true && finalMiddleware.length && !storeEnhancers.includes(middlewareEnhancer)) {
    console.error("middlewares were provided, but middleware enhancer was not included in final enhancers - make sure to call `getDefaultEnhancers`");
  }
  const composedEnhancer = finalCompose(...storeEnhancers);
  return (0,redux__WEBPACK_IMPORTED_MODULE_0__.createStore)(rootReducer, preloadedState, composedEnhancer);
}

// src/mapBuilders.ts
function executeReducerBuilderCallback(builderCallback) {
  const actionsMap = {};
  const actionMatchers = [];
  let defaultCaseReducer;
  const builder = {
    addCase(typeOrActionCreator, reducer) {
      if (true) {
        if (actionMatchers.length > 0) {
          throw new Error( false ? 0 : "`builder.addCase` should only be called before calling `builder.addMatcher`");
        }
        if (defaultCaseReducer) {
          throw new Error( false ? 0 : "`builder.addCase` should only be called before calling `builder.addDefaultCase`");
        }
      }
      const type = typeof typeOrActionCreator === "string" ? typeOrActionCreator : typeOrActionCreator.type;
      if (!type) {
        throw new Error( false ? 0 : "`builder.addCase` cannot be called with an empty action type");
      }
      if (type in actionsMap) {
        throw new Error( false ? 0 : `\`builder.addCase\` cannot be called with two reducers for the same action type '${type}'`);
      }
      actionsMap[type] = reducer;
      return builder;
    },
    addAsyncThunk(asyncThunk, reducers) {
      if (true) {
        if (defaultCaseReducer) {
          throw new Error( false ? 0 : "`builder.addAsyncThunk` should only be called before calling `builder.addDefaultCase`");
        }
      }
      if (reducers.pending) actionsMap[asyncThunk.pending.type] = reducers.pending;
      if (reducers.rejected) actionsMap[asyncThunk.rejected.type] = reducers.rejected;
      if (reducers.fulfilled) actionsMap[asyncThunk.fulfilled.type] = reducers.fulfilled;
      if (reducers.settled) actionMatchers.push({
        matcher: asyncThunk.settled,
        reducer: reducers.settled
      });
      return builder;
    },
    addMatcher(matcher, reducer) {
      if (true) {
        if (defaultCaseReducer) {
          throw new Error( false ? 0 : "`builder.addMatcher` should only be called before calling `builder.addDefaultCase`");
        }
      }
      actionMatchers.push({
        matcher,
        reducer
      });
      return builder;
    },
    addDefaultCase(reducer) {
      if (true) {
        if (defaultCaseReducer) {
          throw new Error( false ? 0 : "`builder.addDefaultCase` can only be called once");
        }
      }
      defaultCaseReducer = reducer;
      return builder;
    }
  };
  builderCallback(builder);
  return [actionsMap, actionMatchers, defaultCaseReducer];
}

// src/createReducer.ts
function isStateFunction(x) {
  return typeof x === "function";
}
function createReducer(initialState, mapOrBuilderCallback) {
  if (true) {
    if (typeof mapOrBuilderCallback === "object") {
      throw new Error( false ? 0 : "The object notation for `createReducer` has been removed. Please use the 'builder callback' notation instead: https://redux-toolkit.js.org/api/createReducer");
    }
  }
  let [actionsMap, finalActionMatchers, finalDefaultCaseReducer] = executeReducerBuilderCallback(mapOrBuilderCallback);
  let getInitialState;
  if (isStateFunction(initialState)) {
    getInitialState = () => freezeDraftable(initialState());
  } else {
    const frozenInitialState = freezeDraftable(initialState);
    getInitialState = () => frozenInitialState;
  }
  function reducer(state = getInitialState(), action) {
    let caseReducers = [actionsMap[action.type], ...finalActionMatchers.filter(({
      matcher
    }) => matcher(action)).map(({
      reducer: reducer2
    }) => reducer2)];
    if (caseReducers.filter((cr) => !!cr).length === 0) {
      caseReducers = [finalDefaultCaseReducer];
    }
    return caseReducers.reduce((previousState, caseReducer) => {
      if (caseReducer) {
        if ((0,immer__WEBPACK_IMPORTED_MODULE_1__.isDraft)(previousState)) {
          const draft = previousState;
          const result = caseReducer(draft, action);
          if (result === void 0) {
            return previousState;
          }
          return result;
        } else if (!(0,immer__WEBPACK_IMPORTED_MODULE_1__.isDraftable)(previousState)) {
          const result = caseReducer(previousState, action);
          if (result === void 0) {
            if (previousState === null) {
              return previousState;
            }
            throw Error("A case reducer on a non-draftable value must not return undefined");
          }
          return result;
        } else {
          return (0,immer__WEBPACK_IMPORTED_MODULE_1__.produce)(previousState, (draft) => {
            return caseReducer(draft, action);
          });
        }
      }
      return previousState;
    }, state);
  }
  reducer.getInitialState = getInitialState;
  return reducer;
}

// src/matchers.ts
var matches = (matcher, action) => {
  if (hasMatchFunction(matcher)) {
    return matcher.match(action);
  } else {
    return matcher(action);
  }
};
function isAnyOf(...matchers) {
  return (action) => {
    return matchers.some((matcher) => matches(matcher, action));
  };
}
function isAllOf(...matchers) {
  return (action) => {
    return matchers.every((matcher) => matches(matcher, action));
  };
}
function hasExpectedRequestMetadata(action, validStatus) {
  if (!action || !action.meta) return false;
  const hasValidRequestId = typeof action.meta.requestId === "string";
  const hasValidRequestStatus = validStatus.indexOf(action.meta.requestStatus) > -1;
  return hasValidRequestId && hasValidRequestStatus;
}
function isAsyncThunkArray(a) {
  return typeof a[0] === "function" && "pending" in a[0] && "fulfilled" in a[0] && "rejected" in a[0];
}
function isPending(...asyncThunks) {
  if (asyncThunks.length === 0) {
    return (action) => hasExpectedRequestMetadata(action, ["pending"]);
  }
  if (!isAsyncThunkArray(asyncThunks)) {
    return isPending()(asyncThunks[0]);
  }
  return isAnyOf(...asyncThunks.map((asyncThunk) => asyncThunk.pending));
}
function isRejected(...asyncThunks) {
  if (asyncThunks.length === 0) {
    return (action) => hasExpectedRequestMetadata(action, ["rejected"]);
  }
  if (!isAsyncThunkArray(asyncThunks)) {
    return isRejected()(asyncThunks[0]);
  }
  return isAnyOf(...asyncThunks.map((asyncThunk) => asyncThunk.rejected));
}
function isRejectedWithValue(...asyncThunks) {
  const hasFlag = (action) => {
    return action && action.meta && action.meta.rejectedWithValue;
  };
  if (asyncThunks.length === 0) {
    return isAllOf(isRejected(...asyncThunks), hasFlag);
  }
  if (!isAsyncThunkArray(asyncThunks)) {
    return isRejectedWithValue()(asyncThunks[0]);
  }
  return isAllOf(isRejected(...asyncThunks), hasFlag);
}
function isFulfilled(...asyncThunks) {
  if (asyncThunks.length === 0) {
    return (action) => hasExpectedRequestMetadata(action, ["fulfilled"]);
  }
  if (!isAsyncThunkArray(asyncThunks)) {
    return isFulfilled()(asyncThunks[0]);
  }
  return isAnyOf(...asyncThunks.map((asyncThunk) => asyncThunk.fulfilled));
}
function isAsyncThunkAction(...asyncThunks) {
  if (asyncThunks.length === 0) {
    return (action) => hasExpectedRequestMetadata(action, ["pending", "fulfilled", "rejected"]);
  }
  if (!isAsyncThunkArray(asyncThunks)) {
    return isAsyncThunkAction()(asyncThunks[0]);
  }
  return isAnyOf(...asyncThunks.flatMap((asyncThunk) => [asyncThunk.pending, asyncThunk.rejected, asyncThunk.fulfilled]));
}

// src/nanoid.ts
var urlAlphabet = "ModuleSymbhasOwnPr-0123456789ABCDEFGHNRVfgctiUvz_KqYTJkLxpZXIjQW";
var nanoid = (size = 21) => {
  let id = "";
  let i = size;
  while (i--) {
    id += urlAlphabet[Math.random() * 64 | 0];
  }
  return id;
};

// src/createAsyncThunk.ts
var commonProperties = ["name", "message", "stack", "code"];
var RejectWithValue = class {
  constructor(payload, meta) {
    this.payload = payload;
    this.meta = meta;
  }
  /*
  type-only property to distinguish between RejectWithValue and FulfillWithMeta
  does not exist at runtime
  */
  _type;
};
var FulfillWithMeta = class {
  constructor(payload, meta) {
    this.payload = payload;
    this.meta = meta;
  }
  /*
  type-only property to distinguish between RejectWithValue and FulfillWithMeta
  does not exist at runtime
  */
  _type;
};
var miniSerializeError = (value) => {
  if (typeof value === "object" && value !== null) {
    const simpleError = {};
    for (const property of commonProperties) {
      if (typeof value[property] === "string") {
        simpleError[property] = value[property];
      }
    }
    return simpleError;
  }
  return {
    message: String(value)
  };
};
var externalAbortMessage = "External signal was aborted";
var createAsyncThunk = /* @__PURE__ */ (() => {
  function createAsyncThunk2(typePrefix, payloadCreator, options) {
    const fulfilled = createAction(typePrefix + "/fulfilled", (payload, requestId, arg, meta) => ({
      payload,
      meta: {
        ...meta || {},
        arg,
        requestId,
        requestStatus: "fulfilled"
      }
    }));
    const pending = createAction(typePrefix + "/pending", (requestId, arg, meta) => ({
      payload: void 0,
      meta: {
        ...meta || {},
        arg,
        requestId,
        requestStatus: "pending"
      }
    }));
    const rejected = createAction(typePrefix + "/rejected", (error, requestId, arg, payload, meta) => ({
      payload,
      error: (options && options.serializeError || miniSerializeError)(error || "Rejected"),
      meta: {
        ...meta || {},
        arg,
        requestId,
        rejectedWithValue: !!payload,
        requestStatus: "rejected",
        aborted: error?.name === "AbortError",
        condition: error?.name === "ConditionError"
      }
    }));
    function actionCreator(arg, {
      signal
    } = {}) {
      return (dispatch, getState, extra) => {
        const requestId = options?.idGenerator ? options.idGenerator(arg) : nanoid();
        const abortController = new AbortController();
        let abortHandler;
        let abortReason;
        function abort(reason) {
          abortReason = reason;
          abortController.abort();
        }
        if (signal) {
          if (signal.aborted) {
            abort(externalAbortMessage);
          } else {
            signal.addEventListener("abort", () => abort(externalAbortMessage), {
              once: true
            });
          }
        }
        const promise = async function() {
          let finalAction;
          try {
            let conditionResult = options?.condition?.(arg, {
              getState,
              extra
            });
            if (isThenable(conditionResult)) {
              conditionResult = await conditionResult;
            }
            if (conditionResult === false || abortController.signal.aborted) {
              throw {
                name: "ConditionError",
                message: "Aborted due to condition callback returning false."
              };
            }
            const abortedPromise = new Promise((_, reject) => {
              abortHandler = () => {
                reject({
                  name: "AbortError",
                  message: abortReason || "Aborted"
                });
              };
              abortController.signal.addEventListener("abort", abortHandler, {
                once: true
              });
            });
            dispatch(pending(requestId, arg, options?.getPendingMeta?.({
              requestId,
              arg
            }, {
              getState,
              extra
            })));
            finalAction = await Promise.race([abortedPromise, Promise.resolve(payloadCreator(arg, {
              dispatch,
              getState,
              extra,
              requestId,
              signal: abortController.signal,
              abort,
              rejectWithValue: (value, meta) => {
                return new RejectWithValue(value, meta);
              },
              fulfillWithValue: (value, meta) => {
                return new FulfillWithMeta(value, meta);
              }
            })).then((result) => {
              if (result instanceof RejectWithValue) {
                throw result;
              }
              if (result instanceof FulfillWithMeta) {
                return fulfilled(result.payload, requestId, arg, result.meta);
              }
              return fulfilled(result, requestId, arg);
            })]);
          } catch (err) {
            finalAction = err instanceof RejectWithValue ? rejected(null, requestId, arg, err.payload, err.meta) : rejected(err, requestId, arg);
          } finally {
            if (abortHandler) {
              abortController.signal.removeEventListener("abort", abortHandler);
            }
          }
          const skipDispatch = options && !options.dispatchConditionRejection && rejected.match(finalAction) && finalAction.meta.condition;
          if (!skipDispatch) {
            dispatch(finalAction);
          }
          return finalAction;
        }();
        return Object.assign(promise, {
          abort,
          requestId,
          arg,
          unwrap() {
            return promise.then(unwrapResult);
          }
        });
      };
    }
    return Object.assign(actionCreator, {
      pending,
      rejected,
      fulfilled,
      settled: isAnyOf(rejected, fulfilled),
      typePrefix
    });
  }
  createAsyncThunk2.withTypes = () => createAsyncThunk2;
  return createAsyncThunk2;
})();
function unwrapResult(action) {
  if (action.meta && action.meta.rejectedWithValue) {
    throw action.payload;
  }
  if (action.error) {
    throw action.error;
  }
  return action.payload;
}
function isThenable(value) {
  return value !== null && typeof value === "object" && typeof value.then === "function";
}

// src/createSlice.ts
var asyncThunkSymbol = /* @__PURE__ */ Symbol.for("rtk-slice-createasyncthunk");
var asyncThunkCreator = {
  [asyncThunkSymbol]: createAsyncThunk
};
var ReducerType = /* @__PURE__ */ ((ReducerType2) => {
  ReducerType2["reducer"] = "reducer";
  ReducerType2["reducerWithPrepare"] = "reducerWithPrepare";
  ReducerType2["asyncThunk"] = "asyncThunk";
  return ReducerType2;
})(ReducerType || {});
function getType(slice, actionKey) {
  return `${slice}/${actionKey}`;
}
function buildCreateSlice({
  creators
} = {}) {
  const cAT = creators?.asyncThunk?.[asyncThunkSymbol];
  return function createSlice2(options) {
    const {
      name,
      reducerPath = name
    } = options;
    if (!name) {
      throw new Error( false ? 0 : "`name` is a required option for createSlice");
    }
    if (typeof process !== "undefined" && "development" === "development") {
      if (options.initialState === void 0) {
        console.error("You must provide an `initialState` value that is not `undefined`. You may have misspelled `initialState`");
      }
    }
    const reducers = (typeof options.reducers === "function" ? options.reducers(buildReducerCreators()) : options.reducers) || {};
    const reducerNames = Object.keys(reducers);
    const context = {
      sliceCaseReducersByName: {},
      sliceCaseReducersByType: {},
      actionCreators: {},
      sliceMatchers: []
    };
    const contextMethods = {
      addCase(typeOrActionCreator, reducer2) {
        const type = typeof typeOrActionCreator === "string" ? typeOrActionCreator : typeOrActionCreator.type;
        if (!type) {
          throw new Error( false ? 0 : "`context.addCase` cannot be called with an empty action type");
        }
        if (type in context.sliceCaseReducersByType) {
          throw new Error( false ? 0 : "`context.addCase` cannot be called with two reducers for the same action type: " + type);
        }
        context.sliceCaseReducersByType[type] = reducer2;
        return contextMethods;
      },
      addMatcher(matcher, reducer2) {
        context.sliceMatchers.push({
          matcher,
          reducer: reducer2
        });
        return contextMethods;
      },
      exposeAction(name2, actionCreator) {
        context.actionCreators[name2] = actionCreator;
        return contextMethods;
      },
      exposeCaseReducer(name2, reducer2) {
        context.sliceCaseReducersByName[name2] = reducer2;
        return contextMethods;
      }
    };
    reducerNames.forEach((reducerName) => {
      const reducerDefinition = reducers[reducerName];
      const reducerDetails = {
        reducerName,
        type: getType(name, reducerName),
        createNotation: typeof options.reducers === "function"
      };
      if (isAsyncThunkSliceReducerDefinition(reducerDefinition)) {
        handleThunkCaseReducerDefinition(reducerDetails, reducerDefinition, contextMethods, cAT);
      } else {
        handleNormalReducerDefinition(reducerDetails, reducerDefinition, contextMethods);
      }
    });
    function buildReducer() {
      if (true) {
        if (typeof options.extraReducers === "object") {
          throw new Error( false ? 0 : "The object notation for `createSlice.extraReducers` has been removed. Please use the 'builder callback' notation instead: https://redux-toolkit.js.org/api/createSlice");
        }
      }
      const [extraReducers = {}, actionMatchers = [], defaultCaseReducer = void 0] = typeof options.extraReducers === "function" ? executeReducerBuilderCallback(options.extraReducers) : [options.extraReducers];
      const finalCaseReducers = {
        ...extraReducers,
        ...context.sliceCaseReducersByType
      };
      return createReducer(options.initialState, (builder) => {
        for (let key in finalCaseReducers) {
          builder.addCase(key, finalCaseReducers[key]);
        }
        for (let sM of context.sliceMatchers) {
          builder.addMatcher(sM.matcher, sM.reducer);
        }
        for (let m of actionMatchers) {
          builder.addMatcher(m.matcher, m.reducer);
        }
        if (defaultCaseReducer) {
          builder.addDefaultCase(defaultCaseReducer);
        }
      });
    }
    const selectSelf = (state) => state;
    const injectedSelectorCache = /* @__PURE__ */ new Map();
    const injectedStateCache = /* @__PURE__ */ new WeakMap();
    let _reducer;
    function reducer(state, action) {
      if (!_reducer) _reducer = buildReducer();
      return _reducer(state, action);
    }
    function getInitialState() {
      if (!_reducer) _reducer = buildReducer();
      return _reducer.getInitialState();
    }
    function makeSelectorProps(reducerPath2, injected = false) {
      function selectSlice(state) {
        let sliceState = state[reducerPath2];
        if (typeof sliceState === "undefined") {
          if (injected) {
            sliceState = getOrInsertComputed(injectedStateCache, selectSlice, getInitialState);
          } else if (true) {
            throw new Error( false ? 0 : "selectSlice returned undefined for an uninjected slice reducer");
          }
        }
        return sliceState;
      }
      function getSelectors(selectState = selectSelf) {
        const selectorCache = getOrInsertComputed(injectedSelectorCache, injected, () => /* @__PURE__ */ new WeakMap());
        return getOrInsertComputed(selectorCache, selectState, () => {
          const map = {};
          for (const [name2, selector] of Object.entries(options.selectors ?? {})) {
            map[name2] = wrapSelector(selector, selectState, () => getOrInsertComputed(injectedStateCache, selectState, getInitialState), injected);
          }
          return map;
        });
      }
      return {
        reducerPath: reducerPath2,
        getSelectors,
        get selectors() {
          return getSelectors(selectSlice);
        },
        selectSlice
      };
    }
    const slice = {
      name,
      reducer,
      actions: context.actionCreators,
      caseReducers: context.sliceCaseReducersByName,
      getInitialState,
      ...makeSelectorProps(reducerPath),
      injectInto(injectable, {
        reducerPath: pathOpt,
        ...config
      } = {}) {
        const newReducerPath = pathOpt ?? reducerPath;
        injectable.inject({
          reducerPath: newReducerPath,
          reducer
        }, config);
        return {
          ...slice,
          ...makeSelectorProps(newReducerPath, true)
        };
      }
    };
    return slice;
  };
}
function wrapSelector(selector, selectState, getInitialState, injected) {
  function wrapper(rootState, ...args) {
    let sliceState = selectState(rootState);
    if (typeof sliceState === "undefined") {
      if (injected) {
        sliceState = getInitialState();
      } else if (true) {
        throw new Error( false ? 0 : "selectState returned undefined for an uninjected slice reducer");
      }
    }
    return selector(sliceState, ...args);
  }
  wrapper.unwrapped = selector;
  return wrapper;
}
var createSlice = /* @__PURE__ */ buildCreateSlice();
function buildReducerCreators() {
  function asyncThunk(payloadCreator, config) {
    return {
      _reducerDefinitionType: "asyncThunk" /* asyncThunk */,
      payloadCreator,
      ...config
    };
  }
  asyncThunk.withTypes = () => asyncThunk;
  return {
    reducer(caseReducer) {
      return Object.assign({
        // hack so the wrapping function has the same name as the original
        // we need to create a wrapper so the `reducerDefinitionType` is not assigned to the original
        [caseReducer.name](...args) {
          return caseReducer(...args);
        }
      }[caseReducer.name], {
        _reducerDefinitionType: "reducer" /* reducer */
      });
    },
    preparedReducer(prepare, reducer) {
      return {
        _reducerDefinitionType: "reducerWithPrepare" /* reducerWithPrepare */,
        prepare,
        reducer
      };
    },
    asyncThunk
  };
}
function handleNormalReducerDefinition({
  type,
  reducerName,
  createNotation
}, maybeReducerWithPrepare, context) {
  let caseReducer;
  let prepareCallback;
  if ("reducer" in maybeReducerWithPrepare) {
    if (createNotation && !isCaseReducerWithPrepareDefinition(maybeReducerWithPrepare)) {
      throw new Error( false ? 0 : "Please use the `create.preparedReducer` notation for prepared action creators with the `create` notation.");
    }
    caseReducer = maybeReducerWithPrepare.reducer;
    prepareCallback = maybeReducerWithPrepare.prepare;
  } else {
    caseReducer = maybeReducerWithPrepare;
  }
  context.addCase(type, caseReducer).exposeCaseReducer(reducerName, caseReducer).exposeAction(reducerName, prepareCallback ? createAction(type, prepareCallback) : createAction(type));
}
function isAsyncThunkSliceReducerDefinition(reducerDefinition) {
  return reducerDefinition._reducerDefinitionType === "asyncThunk" /* asyncThunk */;
}
function isCaseReducerWithPrepareDefinition(reducerDefinition) {
  return reducerDefinition._reducerDefinitionType === "reducerWithPrepare" /* reducerWithPrepare */;
}
function handleThunkCaseReducerDefinition({
  type,
  reducerName
}, reducerDefinition, context, cAT) {
  if (!cAT) {
    throw new Error( false ? 0 : "Cannot use `create.asyncThunk` in the built-in `createSlice`. Use `buildCreateSlice({ creators: { asyncThunk: asyncThunkCreator } })` to create a customised version of `createSlice`.");
  }
  const {
    payloadCreator,
    fulfilled,
    pending,
    rejected,
    settled,
    options
  } = reducerDefinition;
  const thunk = cAT(type, payloadCreator, options);
  context.exposeAction(reducerName, thunk);
  if (fulfilled) {
    context.addCase(thunk.fulfilled, fulfilled);
  }
  if (pending) {
    context.addCase(thunk.pending, pending);
  }
  if (rejected) {
    context.addCase(thunk.rejected, rejected);
  }
  if (settled) {
    context.addMatcher(thunk.settled, settled);
  }
  context.exposeCaseReducer(reducerName, {
    fulfilled: fulfilled || noop,
    pending: pending || noop,
    rejected: rejected || noop,
    settled: settled || noop
  });
}
function noop() {
}

// src/entities/entity_state.ts
function getInitialEntityState() {
  return {
    ids: [],
    entities: {}
  };
}
function createInitialStateFactory(stateAdapter) {
  function getInitialState(additionalState = {}, entities) {
    const state = Object.assign(getInitialEntityState(), additionalState);
    return entities ? stateAdapter.setAll(state, entities) : state;
  }
  return {
    getInitialState
  };
}

// src/entities/state_selectors.ts
function createSelectorsFactory() {
  function getSelectors(selectState, options = {}) {
    const {
      createSelector: createSelector2 = createDraftSafeSelector
    } = options;
    const selectIds = (state) => state.ids;
    const selectEntities = (state) => state.entities;
    const selectAll = createSelector2(selectIds, selectEntities, (ids, entities) => ids.map((id) => entities[id]));
    const selectId = (_, id) => id;
    const selectById = (entities, id) => entities[id];
    const selectTotal = createSelector2(selectIds, (ids) => ids.length);
    if (!selectState) {
      return {
        selectIds,
        selectEntities,
        selectAll,
        selectTotal,
        selectById: createSelector2(selectEntities, selectId, selectById)
      };
    }
    const selectGlobalizedEntities = createSelector2(selectState, selectEntities);
    return {
      selectIds: createSelector2(selectState, selectIds),
      selectEntities: selectGlobalizedEntities,
      selectAll: createSelector2(selectState, selectAll),
      selectTotal: createSelector2(selectState, selectTotal),
      selectById: createSelector2(selectGlobalizedEntities, selectId, selectById)
    };
  }
  return {
    getSelectors
  };
}

// src/entities/state_adapter.ts
var isDraftTyped = immer__WEBPACK_IMPORTED_MODULE_1__.isDraft;
function createSingleArgumentStateOperator(mutator) {
  const operator = createStateOperator((_, state) => mutator(state));
  return function operation(state) {
    return operator(state, void 0);
  };
}
function createStateOperator(mutator) {
  return function operation(state, arg) {
    function isPayloadActionArgument(arg2) {
      return isFSA(arg2);
    }
    const runMutator = (draft) => {
      if (isPayloadActionArgument(arg)) {
        mutator(arg.payload, draft);
      } else {
        mutator(arg, draft);
      }
    };
    if (isDraftTyped(state)) {
      runMutator(state);
      return state;
    }
    return (0,immer__WEBPACK_IMPORTED_MODULE_1__.produce)(state, runMutator);
  };
}

// src/entities/utils.ts
function selectIdValue(entity, selectId) {
  const key = selectId(entity);
  if ( true && key === void 0) {
    console.warn("The entity passed to the `selectId` implementation returned undefined.", "You should probably provide your own `selectId` implementation.", "The entity that was passed:", entity, "The `selectId` implementation:", selectId.toString());
  }
  return key;
}
function ensureEntitiesArray(entities) {
  if (!Array.isArray(entities)) {
    entities = Object.values(entities);
  }
  return entities;
}
function getCurrent(value) {
  return (0,immer__WEBPACK_IMPORTED_MODULE_1__.isDraft)(value) ? (0,immer__WEBPACK_IMPORTED_MODULE_1__.current)(value) : value;
}
function splitAddedUpdatedEntities(newEntities, selectId, state) {
  newEntities = ensureEntitiesArray(newEntities);
  const existingIdsArray = getCurrent(state.ids);
  const existingIds = new Set(existingIdsArray);
  const added = [];
  const addedIds = /* @__PURE__ */ new Set([]);
  const updated = [];
  for (const entity of newEntities) {
    const id = selectIdValue(entity, selectId);
    if (existingIds.has(id) || addedIds.has(id)) {
      updated.push({
        id,
        changes: entity
      });
    } else {
      addedIds.add(id);
      added.push(entity);
    }
  }
  return [added, updated, existingIdsArray];
}

// src/entities/unsorted_state_adapter.ts
function createUnsortedStateAdapter(selectId) {
  function addOneMutably(entity, state) {
    const key = selectIdValue(entity, selectId);
    if (key in state.entities) {
      return;
    }
    state.ids.push(key);
    state.entities[key] = entity;
  }
  function addManyMutably(newEntities, state) {
    newEntities = ensureEntitiesArray(newEntities);
    for (const entity of newEntities) {
      addOneMutably(entity, state);
    }
  }
  function setOneMutably(entity, state) {
    const key = selectIdValue(entity, selectId);
    if (!(key in state.entities)) {
      state.ids.push(key);
    }
    ;
    state.entities[key] = entity;
  }
  function setManyMutably(newEntities, state) {
    newEntities = ensureEntitiesArray(newEntities);
    for (const entity of newEntities) {
      setOneMutably(entity, state);
    }
  }
  function setAllMutably(newEntities, state) {
    newEntities = ensureEntitiesArray(newEntities);
    state.ids = [];
    state.entities = {};
    addManyMutably(newEntities, state);
  }
  function removeOneMutably(key, state) {
    return removeManyMutably([key], state);
  }
  function removeManyMutably(keys, state) {
    let didMutate = false;
    keys.forEach((key) => {
      if (key in state.entities) {
        delete state.entities[key];
        didMutate = true;
      }
    });
    if (didMutate) {
      state.ids = state.ids.filter((id) => id in state.entities);
    }
  }
  function removeAllMutably(state) {
    Object.assign(state, {
      ids: [],
      entities: {}
    });
  }
  function takeNewKey(keys, update, state) {
    const original3 = state.entities[update.id];
    if (original3 === void 0) {
      return false;
    }
    const updated = Object.assign({}, original3, update.changes);
    const newKey = selectIdValue(updated, selectId);
    const hasNewKey = newKey !== update.id;
    if (hasNewKey) {
      keys[update.id] = newKey;
      delete state.entities[update.id];
    }
    ;
    state.entities[newKey] = updated;
    return hasNewKey;
  }
  function updateOneMutably(update, state) {
    return updateManyMutably([update], state);
  }
  function updateManyMutably(updates, state) {
    const newKeys = {};
    const updatesPerEntity = {};
    updates.forEach((update) => {
      if (update.id in state.entities) {
        updatesPerEntity[update.id] = {
          id: update.id,
          // Spreads ignore falsy values, so this works even if there isn't
          // an existing update already at this key
          changes: {
            ...updatesPerEntity[update.id]?.changes,
            ...update.changes
          }
        };
      }
    });
    updates = Object.values(updatesPerEntity);
    const didMutateEntities = updates.length > 0;
    if (didMutateEntities) {
      const didMutateIds = updates.filter((update) => takeNewKey(newKeys, update, state)).length > 0;
      if (didMutateIds) {
        state.ids = Object.values(state.entities).map((e) => selectIdValue(e, selectId));
      }
    }
  }
  function upsertOneMutably(entity, state) {
    return upsertManyMutably([entity], state);
  }
  function upsertManyMutably(newEntities, state) {
    const [added, updated] = splitAddedUpdatedEntities(newEntities, selectId, state);
    addManyMutably(added, state);
    updateManyMutably(updated, state);
  }
  return {
    removeAll: createSingleArgumentStateOperator(removeAllMutably),
    addOne: createStateOperator(addOneMutably),
    addMany: createStateOperator(addManyMutably),
    setOne: createStateOperator(setOneMutably),
    setMany: createStateOperator(setManyMutably),
    setAll: createStateOperator(setAllMutably),
    updateOne: createStateOperator(updateOneMutably),
    updateMany: createStateOperator(updateManyMutably),
    upsertOne: createStateOperator(upsertOneMutably),
    upsertMany: createStateOperator(upsertManyMutably),
    removeOne: createStateOperator(removeOneMutably),
    removeMany: createStateOperator(removeManyMutably)
  };
}

// src/entities/sorted_state_adapter.ts
function findInsertIndex(sortedItems, item, comparisonFunction) {
  let lowIndex = 0;
  let highIndex = sortedItems.length;
  while (lowIndex < highIndex) {
    let middleIndex = lowIndex + highIndex >>> 1;
    const currentItem = sortedItems[middleIndex];
    const res = comparisonFunction(item, currentItem);
    if (res >= 0) {
      lowIndex = middleIndex + 1;
    } else {
      highIndex = middleIndex;
    }
  }
  return lowIndex;
}
function insert(sortedItems, item, comparisonFunction) {
  const insertAtIndex = findInsertIndex(sortedItems, item, comparisonFunction);
  sortedItems.splice(insertAtIndex, 0, item);
  return sortedItems;
}
function createSortedStateAdapter(selectId, comparer) {
  const {
    removeOne,
    removeMany,
    removeAll
  } = createUnsortedStateAdapter(selectId);
  function addOneMutably(entity, state) {
    return addManyMutably([entity], state);
  }
  function addManyMutably(newEntities, state, existingIds) {
    newEntities = ensureEntitiesArray(newEntities);
    const existingKeys = new Set(existingIds ?? getCurrent(state.ids));
    const addedKeys = /* @__PURE__ */ new Set();
    const models = newEntities.filter((model) => {
      const modelId = selectIdValue(model, selectId);
      const notAdded = !addedKeys.has(modelId);
      if (notAdded) addedKeys.add(modelId);
      return !existingKeys.has(modelId) && notAdded;
    });
    if (models.length !== 0) {
      mergeFunction(state, models);
    }
  }
  function setOneMutably(entity, state) {
    return setManyMutably([entity], state);
  }
  function setManyMutably(newEntities, state) {
    let deduplicatedEntities = {};
    newEntities = ensureEntitiesArray(newEntities);
    if (newEntities.length !== 0) {
      for (const item of newEntities) {
        const entityId = selectId(item);
        deduplicatedEntities[entityId] = item;
        delete state.entities[entityId];
      }
      newEntities = ensureEntitiesArray(deduplicatedEntities);
      mergeFunction(state, newEntities);
    }
  }
  function setAllMutably(newEntities, state) {
    newEntities = ensureEntitiesArray(newEntities);
    state.entities = {};
    state.ids = [];
    addManyMutably(newEntities, state, []);
  }
  function updateOneMutably(update, state) {
    return updateManyMutably([update], state);
  }
  function updateManyMutably(updates, state) {
    let appliedUpdates = false;
    let replacedIds = false;
    for (let update of updates) {
      const entity = state.entities[update.id];
      if (!entity) {
        continue;
      }
      appliedUpdates = true;
      Object.assign(entity, update.changes);
      const newId = selectId(entity);
      if (update.id !== newId) {
        replacedIds = true;
        delete state.entities[update.id];
        const oldIndex = state.ids.indexOf(update.id);
        state.ids[oldIndex] = newId;
        state.entities[newId] = entity;
      }
    }
    if (appliedUpdates) {
      mergeFunction(state, [], appliedUpdates, replacedIds);
    }
  }
  function upsertOneMutably(entity, state) {
    return upsertManyMutably([entity], state);
  }
  function upsertManyMutably(newEntities, state) {
    const [added, updated, existingIdsArray] = splitAddedUpdatedEntities(newEntities, selectId, state);
    if (added.length) {
      addManyMutably(added, state, existingIdsArray);
    }
    if (updated.length) {
      updateManyMutably(updated, state);
    }
  }
  function areArraysEqual(a, b) {
    if (a.length !== b.length) {
      return false;
    }
    for (let i = 0; i < a.length; i++) {
      if (a[i] === b[i]) {
        continue;
      }
      return false;
    }
    return true;
  }
  const mergeFunction = (state, addedItems, appliedUpdates, replacedIds) => {
    const currentEntities = getCurrent(state.entities);
    const currentIds = getCurrent(state.ids);
    const stateEntities = state.entities;
    let ids = currentIds;
    if (replacedIds) {
      ids = new Set(currentIds);
    }
    let sortedEntities = [];
    for (const id of ids) {
      const entity = currentEntities[id];
      if (entity) {
        sortedEntities.push(entity);
      }
    }
    const wasPreviouslyEmpty = sortedEntities.length === 0;
    for (const item of addedItems) {
      stateEntities[selectId(item)] = item;
      if (!wasPreviouslyEmpty) {
        insert(sortedEntities, item, comparer);
      }
    }
    if (wasPreviouslyEmpty) {
      sortedEntities = addedItems.slice().sort(comparer);
    } else if (appliedUpdates) {
      sortedEntities.sort(comparer);
    }
    const newSortedIds = sortedEntities.map(selectId);
    if (!areArraysEqual(currentIds, newSortedIds)) {
      state.ids = newSortedIds;
    }
  };
  return {
    removeOne,
    removeMany,
    removeAll,
    addOne: createStateOperator(addOneMutably),
    updateOne: createStateOperator(updateOneMutably),
    upsertOne: createStateOperator(upsertOneMutably),
    setOne: createStateOperator(setOneMutably),
    setMany: createStateOperator(setManyMutably),
    setAll: createStateOperator(setAllMutably),
    addMany: createStateOperator(addManyMutably),
    updateMany: createStateOperator(updateManyMutably),
    upsertMany: createStateOperator(upsertManyMutably)
  };
}

// src/entities/create_adapter.ts
function createEntityAdapter(options = {}) {
  const {
    selectId,
    sortComparer
  } = {
    sortComparer: false,
    selectId: (instance) => instance.id,
    ...options
  };
  const stateAdapter = sortComparer ? createSortedStateAdapter(selectId, sortComparer) : createUnsortedStateAdapter(selectId);
  const stateFactory = createInitialStateFactory(stateAdapter);
  const selectorsFactory = createSelectorsFactory();
  return {
    selectId,
    sortComparer,
    ...stateFactory,
    ...selectorsFactory,
    ...stateAdapter
  };
}

// src/listenerMiddleware/exceptions.ts
var task = "task";
var listener = "listener";
var completed = "completed";
var cancelled = "cancelled";
var taskCancelled = `task-${cancelled}`;
var taskCompleted = `task-${completed}`;
var listenerCancelled = `${listener}-${cancelled}`;
var listenerCompleted = `${listener}-${completed}`;
var TaskAbortError = class {
  constructor(code) {
    this.code = code;
    this.message = `${task} ${cancelled} (reason: ${code})`;
  }
  name = "TaskAbortError";
  message;
};

// src/listenerMiddleware/utils.ts
var assertFunction = (func, expected) => {
  if (typeof func !== "function") {
    throw new TypeError( false ? 0 : `${expected} is not a function`);
  }
};
var noop2 = () => {
};
var catchRejection = (promise, onError = noop2) => {
  promise.catch(onError);
  return promise;
};
var addAbortSignalListener = (abortSignal, callback) => {
  abortSignal.addEventListener("abort", callback, {
    once: true
  });
  return () => abortSignal.removeEventListener("abort", callback);
};

// src/listenerMiddleware/task.ts
var validateActive = (signal) => {
  if (signal.aborted) {
    throw new TaskAbortError(signal.reason);
  }
};
function raceWithSignal(signal, promise) {
  let cleanup = noop2;
  return new Promise((resolve, reject) => {
    const notifyRejection = () => reject(new TaskAbortError(signal.reason));
    if (signal.aborted) {
      notifyRejection();
      return;
    }
    cleanup = addAbortSignalListener(signal, notifyRejection);
    promise.finally(() => cleanup()).then(resolve, reject);
  }).finally(() => {
    cleanup = noop2;
  });
}
var runTask = async (task2, cleanUp) => {
  try {
    await Promise.resolve();
    const value = await task2();
    return {
      status: "ok",
      value
    };
  } catch (error) {
    return {
      status: error instanceof TaskAbortError ? "cancelled" : "rejected",
      error
    };
  } finally {
    cleanUp?.();
  }
};
var createPause = (signal) => {
  return (promise) => {
    return catchRejection(raceWithSignal(signal, promise).then((output) => {
      validateActive(signal);
      return output;
    }));
  };
};
var createDelay = (signal) => {
  const pause = createPause(signal);
  return (timeoutMs) => {
    return pause(new Promise((resolve) => setTimeout(resolve, timeoutMs)));
  };
};

// src/listenerMiddleware/index.ts
var {
  assign
} = Object;
var INTERNAL_NIL_TOKEN = {};
var alm = "listenerMiddleware";
var createFork = (parentAbortSignal, parentBlockingPromises) => {
  const linkControllers = (controller) => addAbortSignalListener(parentAbortSignal, () => controller.abort(parentAbortSignal.reason));
  return (taskExecutor, opts) => {
    assertFunction(taskExecutor, "taskExecutor");
    const childAbortController = new AbortController();
    linkControllers(childAbortController);
    const result = runTask(async () => {
      validateActive(parentAbortSignal);
      validateActive(childAbortController.signal);
      const result2 = await taskExecutor({
        pause: createPause(childAbortController.signal),
        delay: createDelay(childAbortController.signal),
        signal: childAbortController.signal
      });
      validateActive(childAbortController.signal);
      return result2;
    }, () => childAbortController.abort(taskCompleted));
    if (opts?.autoJoin) {
      parentBlockingPromises.push(result.catch(noop2));
    }
    return {
      result: createPause(parentAbortSignal)(result),
      cancel() {
        childAbortController.abort(taskCancelled);
      }
    };
  };
};
var createTakePattern = (startListening, signal) => {
  const take = async (predicate, timeout) => {
    validateActive(signal);
    let unsubscribe = () => {
    };
    const tuplePromise = new Promise((resolve, reject) => {
      let stopListening = startListening({
        predicate,
        effect: (action, listenerApi) => {
          listenerApi.unsubscribe();
          resolve([action, listenerApi.getState(), listenerApi.getOriginalState()]);
        }
      });
      unsubscribe = () => {
        stopListening();
        reject();
      };
    });
    const promises = [tuplePromise];
    if (timeout != null) {
      promises.push(new Promise((resolve) => setTimeout(resolve, timeout, null)));
    }
    try {
      const output = await raceWithSignal(signal, Promise.race(promises));
      validateActive(signal);
      return output;
    } finally {
      unsubscribe();
    }
  };
  return (predicate, timeout) => catchRejection(take(predicate, timeout));
};
var getListenerEntryPropsFrom = (options) => {
  let {
    type,
    actionCreator,
    matcher,
    predicate,
    effect
  } = options;
  if (type) {
    predicate = createAction(type).match;
  } else if (actionCreator) {
    type = actionCreator.type;
    predicate = actionCreator.match;
  } else if (matcher) {
    predicate = matcher;
  } else if (predicate) {
  } else {
    throw new Error( false ? 0 : "Creating or removing a listener requires one of the known fields for matching an action");
  }
  assertFunction(effect, "options.listener");
  return {
    predicate,
    type,
    effect
  };
};
var createListenerEntry = /* @__PURE__ */ assign((options) => {
  const {
    type,
    predicate,
    effect
  } = getListenerEntryPropsFrom(options);
  const entry = {
    id: nanoid(),
    effect,
    type,
    predicate,
    pending: /* @__PURE__ */ new Set(),
    unsubscribe: () => {
      throw new Error( false ? 0 : "Unsubscribe not initialized");
    }
  };
  return entry;
}, {
  withTypes: () => createListenerEntry
});
var findListenerEntry = (listenerMap, options) => {
  const {
    type,
    effect,
    predicate
  } = getListenerEntryPropsFrom(options);
  return Array.from(listenerMap.values()).find((entry) => {
    const matchPredicateOrType = typeof type === "string" ? entry.type === type : entry.predicate === predicate;
    return matchPredicateOrType && entry.effect === effect;
  });
};
var cancelActiveListeners = (entry) => {
  entry.pending.forEach((controller) => {
    controller.abort(listenerCancelled);
  });
};
var createClearListenerMiddleware = (listenerMap, executingListeners) => {
  return () => {
    for (const listener2 of executingListeners.keys()) {
      cancelActiveListeners(listener2);
    }
    listenerMap.clear();
  };
};
var safelyNotifyError = (errorHandler, errorToNotify, errorInfo) => {
  try {
    errorHandler(errorToNotify, errorInfo);
  } catch (errorHandlerError) {
    setTimeout(() => {
      throw errorHandlerError;
    }, 0);
  }
};
var addListener = /* @__PURE__ */ assign(/* @__PURE__ */ createAction(`${alm}/add`), {
  withTypes: () => addListener
});
var clearAllListeners = /* @__PURE__ */ createAction(`${alm}/removeAll`);
var removeListener = /* @__PURE__ */ assign(/* @__PURE__ */ createAction(`${alm}/remove`), {
  withTypes: () => removeListener
});
var defaultErrorHandler = (...args) => {
  console.error(`${alm}/error`, ...args);
};
var createListenerMiddleware = (middlewareOptions = {}) => {
  const listenerMap = /* @__PURE__ */ new Map();
  const executingListeners = /* @__PURE__ */ new Map();
  const trackExecutingListener = (entry) => {
    const count = executingListeners.get(entry) ?? 0;
    executingListeners.set(entry, count + 1);
  };
  const untrackExecutingListener = (entry) => {
    const count = executingListeners.get(entry) ?? 1;
    if (count === 1) {
      executingListeners.delete(entry);
    } else {
      executingListeners.set(entry, count - 1);
    }
  };
  const {
    extra,
    onError = defaultErrorHandler
  } = middlewareOptions;
  assertFunction(onError, "onError");
  const insertEntry = (entry) => {
    entry.unsubscribe = () => listenerMap.delete(entry.id);
    listenerMap.set(entry.id, entry);
    return (cancelOptions) => {
      entry.unsubscribe();
      if (cancelOptions?.cancelActive) {
        cancelActiveListeners(entry);
      }
    };
  };
  const startListening = (options) => {
    const entry = findListenerEntry(listenerMap, options) ?? createListenerEntry(options);
    return insertEntry(entry);
  };
  assign(startListening, {
    withTypes: () => startListening
  });
  const stopListening = (options) => {
    const entry = findListenerEntry(listenerMap, options);
    if (entry) {
      entry.unsubscribe();
      if (options.cancelActive) {
        cancelActiveListeners(entry);
      }
    }
    return !!entry;
  };
  assign(stopListening, {
    withTypes: () => stopListening
  });
  const notifyListener = async (entry, action, api, getOriginalState) => {
    const internalTaskController = new AbortController();
    const take = createTakePattern(startListening, internalTaskController.signal);
    const autoJoinPromises = [];
    try {
      entry.pending.add(internalTaskController);
      trackExecutingListener(entry);
      await Promise.resolve(entry.effect(
        action,
        // Use assign() rather than ... to avoid extra helper functions added to bundle
        assign({}, api, {
          getOriginalState,
          condition: (predicate, timeout) => take(predicate, timeout).then(Boolean),
          take,
          delay: createDelay(internalTaskController.signal),
          pause: createPause(internalTaskController.signal),
          extra,
          signal: internalTaskController.signal,
          fork: createFork(internalTaskController.signal, autoJoinPromises),
          unsubscribe: entry.unsubscribe,
          subscribe: () => {
            listenerMap.set(entry.id, entry);
          },
          cancelActiveListeners: () => {
            entry.pending.forEach((controller, _, set) => {
              if (controller !== internalTaskController) {
                controller.abort(listenerCancelled);
                set.delete(controller);
              }
            });
          },
          cancel: () => {
            internalTaskController.abort(listenerCancelled);
            entry.pending.delete(internalTaskController);
          },
          throwIfCancelled: () => {
            validateActive(internalTaskController.signal);
          }
        })
      ));
    } catch (listenerError) {
      if (!(listenerError instanceof TaskAbortError)) {
        safelyNotifyError(onError, listenerError, {
          raisedBy: "effect"
        });
      }
    } finally {
      await Promise.all(autoJoinPromises);
      internalTaskController.abort(listenerCompleted);
      untrackExecutingListener(entry);
      entry.pending.delete(internalTaskController);
    }
  };
  const clearListenerMiddleware = createClearListenerMiddleware(listenerMap, executingListeners);
  const middleware = (api) => (next) => (action) => {
    if (!(0,redux__WEBPACK_IMPORTED_MODULE_0__.isAction)(action)) {
      return next(action);
    }
    if (addListener.match(action)) {
      return startListening(action.payload);
    }
    if (clearAllListeners.match(action)) {
      clearListenerMiddleware();
      return;
    }
    if (removeListener.match(action)) {
      return stopListening(action.payload);
    }
    let originalState = api.getState();
    const getOriginalState = () => {
      if (originalState === INTERNAL_NIL_TOKEN) {
        throw new Error( false ? 0 : `${alm}: getOriginalState can only be called synchronously`);
      }
      return originalState;
    };
    let result;
    try {
      result = next(action);
      if (listenerMap.size > 0) {
        const currentState = api.getState();
        const listenerEntries = Array.from(listenerMap.values());
        for (const entry of listenerEntries) {
          let runListener = false;
          try {
            runListener = entry.predicate(action, currentState, originalState);
          } catch (predicateError) {
            runListener = false;
            safelyNotifyError(onError, predicateError, {
              raisedBy: "predicate"
            });
          }
          if (!runListener) {
            continue;
          }
          notifyListener(entry, action, api, getOriginalState);
        }
      }
    } finally {
      originalState = INTERNAL_NIL_TOKEN;
    }
    return result;
  };
  return {
    middleware,
    startListening,
    stopListening,
    clearListeners: clearListenerMiddleware
  };
};

// src/dynamicMiddleware/index.ts
var createMiddlewareEntry = (middleware) => ({
  middleware,
  applied: /* @__PURE__ */ new Map()
});
var matchInstance = (instanceId) => (action) => action?.meta?.instanceId === instanceId;
var createDynamicMiddleware = () => {
  const instanceId = nanoid();
  const middlewareMap = /* @__PURE__ */ new Map();
  const withMiddleware = Object.assign(createAction("dynamicMiddleware/add", (...middlewares) => ({
    payload: middlewares,
    meta: {
      instanceId
    }
  })), {
    withTypes: () => withMiddleware
  });
  const addMiddleware = Object.assign(function addMiddleware2(...middlewares) {
    middlewares.forEach((middleware2) => {
      getOrInsertComputed(middlewareMap, middleware2, createMiddlewareEntry);
    });
  }, {
    withTypes: () => addMiddleware
  });
  const getFinalMiddleware = (api) => {
    const appliedMiddleware = Array.from(middlewareMap.values()).map((entry) => getOrInsertComputed(entry.applied, api, entry.middleware));
    return (0,redux__WEBPACK_IMPORTED_MODULE_0__.compose)(...appliedMiddleware);
  };
  const isWithMiddleware = isAllOf(withMiddleware, matchInstance(instanceId));
  const middleware = (api) => (next) => (action) => {
    if (isWithMiddleware(action)) {
      addMiddleware(...action.payload);
      return api.dispatch;
    }
    return getFinalMiddleware(api)(next)(action);
  };
  return {
    middleware,
    addMiddleware,
    withMiddleware,
    instanceId
  };
};

// src/combineSlices.ts

var isSliceLike = (maybeSliceLike) => "reducerPath" in maybeSliceLike && typeof maybeSliceLike.reducerPath === "string";
var getReducers = (slices) => slices.flatMap((sliceOrMap) => isSliceLike(sliceOrMap) ? [[sliceOrMap.reducerPath, sliceOrMap.reducer]] : Object.entries(sliceOrMap));
var ORIGINAL_STATE = Symbol.for("rtk-state-proxy-original");
var isStateProxy = (value) => !!value && !!value[ORIGINAL_STATE];
var stateProxyMap = /* @__PURE__ */ new WeakMap();
var createStateProxy = (state, reducerMap, initialStateCache) => getOrInsertComputed(stateProxyMap, state, () => new Proxy(state, {
  get: (target, prop, receiver) => {
    if (prop === ORIGINAL_STATE) return target;
    const result = Reflect.get(target, prop, receiver);
    if (typeof result === "undefined") {
      const cached = initialStateCache[prop];
      if (typeof cached !== "undefined") return cached;
      const reducer = reducerMap[prop];
      if (reducer) {
        const reducerResult = reducer(void 0, {
          type: nanoid()
        });
        if (typeof reducerResult === "undefined") {
          throw new Error( false ? 0 : `The slice reducer for key "${prop.toString()}" returned undefined when called for selector(). If the state passed to the reducer is undefined, you must explicitly return the initial state. The initial state may not be undefined. If you don't want to set a value for this reducer, you can use null instead of undefined.`);
        }
        initialStateCache[prop] = reducerResult;
        return reducerResult;
      }
    }
    return result;
  }
}));
var original = (state) => {
  if (!isStateProxy(state)) {
    throw new Error( false ? 0 : "original must be used on state Proxy");
  }
  return state[ORIGINAL_STATE];
};
var emptyObject = {};
var noopReducer = (state = emptyObject) => state;
function combineSlices(...slices) {
  const reducerMap = Object.fromEntries(getReducers(slices));
  const getReducer = () => Object.keys(reducerMap).length ? (0,redux__WEBPACK_IMPORTED_MODULE_0__.combineReducers)(reducerMap) : noopReducer;
  let reducer = getReducer();
  function combinedReducer(state, action) {
    return reducer(state, action);
  }
  combinedReducer.withLazyLoadedSlices = () => combinedReducer;
  const initialStateCache = {};
  const inject = (slice, config = {}) => {
    const {
      reducerPath,
      reducer: reducerToInject
    } = slice;
    const currentReducer = reducerMap[reducerPath];
    if (!config.overrideExisting && currentReducer && currentReducer !== reducerToInject) {
      if (typeof process !== "undefined" && "development" === "development") {
        console.error(`called \`inject\` to override already-existing reducer ${reducerPath} without specifying \`overrideExisting: true\``);
      }
      return combinedReducer;
    }
    if (config.overrideExisting && currentReducer !== reducerToInject) {
      delete initialStateCache[reducerPath];
    }
    reducerMap[reducerPath] = reducerToInject;
    reducer = getReducer();
    return combinedReducer;
  };
  const selector = Object.assign(function makeSelector(selectorFn, selectState) {
    return function selector2(state, ...args) {
      return selectorFn(createStateProxy(selectState ? selectState(state, ...args) : state, reducerMap, initialStateCache), ...args);
    };
  }, {
    original
  });
  return Object.assign(combinedReducer, {
    inject,
    selector
  });
}

// src/formatProdErrorMessage.ts
function formatProdErrorMessage(code) {
  return `Minified Redux Toolkit error #${code}; visit https://redux-toolkit.js.org/Errors?code=${code} for the full message or use the non-minified dev environment for full errors. `;
}

//# sourceMappingURL=redux-toolkit.modern.mjs.map

/***/ },

/***/ "./node_modules/@standard-schema/utils/dist/index.js"
/*!***********************************************************!*\
  !*** ./node_modules/@standard-schema/utils/dist/index.js ***!
  \***********************************************************/
(__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   SchemaError: () => (/* binding */ SchemaError),
/* harmony export */   getDotPath: () => (/* binding */ getDotPath)
/* harmony export */ });
// src/getDotPath/getDotPath.ts
function getDotPath(issue) {
  if (issue.path?.length) {
    let dotPath = "";
    for (const item of issue.path) {
      const key = typeof item === "object" ? item.key : item;
      if (typeof key === "string" || typeof key === "number") {
        if (dotPath) {
          dotPath += `.${key}`;
        } else {
          dotPath += key;
        }
      } else {
        return null;
      }
    }
    return dotPath;
  }
  return null;
}

// src/SchemaError/SchemaError.ts
var SchemaError = class extends Error {
  /**
   * The schema issues.
   */
  issues;
  /**
   * Creates a schema error with useful information.
   *
   * @param issues The schema issues.
   */
  constructor(issues) {
    super(issues[0].message);
    this.name = "SchemaError";
    this.issues = issues;
  }
};

/***/ },

/***/ "./node_modules/clsx/dist/clsx.mjs"
/*!*****************************************!*\
  !*** ./node_modules/clsx/dist/clsx.mjs ***!
  \*****************************************/
(__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   clsx: () => (/* binding */ clsx),
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
function r(e){var t,f,n="";if("string"==typeof e||"number"==typeof e)n+=e;else if("object"==typeof e)if(Array.isArray(e)){var o=e.length;for(t=0;t<o;t++)e[t]&&(f=r(e[t]))&&(n&&(n+=" "),n+=f)}else for(f in e)e[f]&&(n&&(n+=" "),n+=f);return n}function clsx(){for(var e,t,f=0,n="",o=arguments.length;f<o;f++)(e=arguments[f])&&(t=r(e))&&(n&&(n+=" "),n+=t);return n}/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (clsx);

/***/ },

/***/ "./node_modules/immer/dist/immer.mjs"
/*!*******************************************!*\
  !*** ./node_modules/immer/dist/immer.mjs ***!
  \*******************************************/
(__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   Immer: () => (/* binding */ Immer2),
/* harmony export */   applyPatches: () => (/* binding */ applyPatches),
/* harmony export */   castDraft: () => (/* binding */ castDraft),
/* harmony export */   castImmutable: () => (/* binding */ castImmutable),
/* harmony export */   createDraft: () => (/* binding */ createDraft),
/* harmony export */   current: () => (/* binding */ current),
/* harmony export */   enableArrayMethods: () => (/* binding */ enableArrayMethods),
/* harmony export */   enableMapSet: () => (/* binding */ enableMapSet),
/* harmony export */   enablePatches: () => (/* binding */ enablePatches),
/* harmony export */   finishDraft: () => (/* binding */ finishDraft),
/* harmony export */   freeze: () => (/* binding */ freeze),
/* harmony export */   immerable: () => (/* binding */ DRAFTABLE),
/* harmony export */   isDraft: () => (/* binding */ isDraft),
/* harmony export */   isDraftable: () => (/* binding */ isDraftable),
/* harmony export */   nothing: () => (/* binding */ NOTHING),
/* harmony export */   original: () => (/* binding */ original),
/* harmony export */   produce: () => (/* binding */ produce),
/* harmony export */   produceWithPatches: () => (/* binding */ produceWithPatches),
/* harmony export */   setAutoFreeze: () => (/* binding */ setAutoFreeze),
/* harmony export */   setUseStrictIteration: () => (/* binding */ setUseStrictIteration),
/* harmony export */   setUseStrictShallowCopy: () => (/* binding */ setUseStrictShallowCopy)
/* harmony export */ });
// src/utils/env.ts
var NOTHING = Symbol.for("immer-nothing");
var DRAFTABLE = Symbol.for("immer-draftable");
var DRAFT_STATE = Symbol.for("immer-state");

// src/utils/errors.ts
var errors =  true ? [
  // All error codes, starting by 0:
  function(plugin) {
    return `The plugin for '${plugin}' has not been loaded into Immer. To enable the plugin, import and call \`enable${plugin}()\` when initializing your application.`;
  },
  function(thing) {
    return `produce can only be called on things that are draftable: plain objects, arrays, Map, Set or classes that are marked with '[immerable]: true'. Got '${thing}'`;
  },
  "This object has been frozen and should not be mutated",
  function(data) {
    return "Cannot use a proxy that has been revoked. Did you pass an object from inside an immer function to an async process? " + data;
  },
  "An immer producer returned a new value *and* modified its draft. Either return a new value *or* modify the draft.",
  "Immer forbids circular references",
  "The first or second argument to `produce` must be a function",
  "The third argument to `produce` must be a function or undefined",
  "First argument to `createDraft` must be a plain object, an array, or an immerable object",
  "First argument to `finishDraft` must be a draft returned by `createDraft`",
  function(thing) {
    return `'current' expects a draft, got: ${thing}`;
  },
  "Object.defineProperty() cannot be used on an Immer draft",
  "Object.setPrototypeOf() cannot be used on an Immer draft",
  "Immer only supports deleting array indices",
  "Immer only supports setting array indices and the 'length' property",
  function(thing) {
    return `'original' expects a draft, got: ${thing}`;
  }
  // Note: if more errors are added, the errorOffset in Patches.ts should be increased
  // See Patches.ts for additional errors
] : 0;
function die(error, ...args) {
  if (true) {
    const e = errors[error];
    const msg = isFunction(e) ? e.apply(null, args) : e;
    throw new Error(`[Immer] ${msg}`);
  }
  // removed by dead control flow

}

// src/utils/common.ts
var O = Object;
var getPrototypeOf = O.getPrototypeOf;
var CONSTRUCTOR = "constructor";
var PROTOTYPE = "prototype";
var CONFIGURABLE = "configurable";
var ENUMERABLE = "enumerable";
var WRITABLE = "writable";
var VALUE = "value";
var isDraft = (value) => !!value && !!value[DRAFT_STATE];
function isDraftable(value) {
  if (!value)
    return false;
  return isPlainObject(value) || isArray(value) || !!value[DRAFTABLE] || !!value[CONSTRUCTOR]?.[DRAFTABLE] || isMap(value) || isSet(value);
}
var objectCtorString = O[PROTOTYPE][CONSTRUCTOR].toString();
var cachedCtorStrings = /* @__PURE__ */ new WeakMap();
function isPlainObject(value) {
  if (!value || !isObjectish(value))
    return false;
  const proto = getPrototypeOf(value);
  if (proto === null || proto === O[PROTOTYPE])
    return true;
  const Ctor = O.hasOwnProperty.call(proto, CONSTRUCTOR) && proto[CONSTRUCTOR];
  if (Ctor === Object)
    return true;
  if (!isFunction(Ctor))
    return false;
  let ctorString = cachedCtorStrings.get(Ctor);
  if (ctorString === void 0) {
    ctorString = Function.toString.call(Ctor);
    cachedCtorStrings.set(Ctor, ctorString);
  }
  return ctorString === objectCtorString;
}
function original(value) {
  if (!isDraft(value))
    die(15, value);
  return value[DRAFT_STATE].base_;
}
function each(obj, iter, strict = true) {
  if (getArchtype(obj) === 0 /* Object */) {
    const keys = strict ? Reflect.ownKeys(obj) : O.keys(obj);
    keys.forEach((key) => {
      iter(key, obj[key], obj);
    });
  } else {
    obj.forEach((entry, index) => iter(index, entry, obj));
  }
}
function getArchtype(thing) {
  const state = thing[DRAFT_STATE];
  return state ? state.type_ : isArray(thing) ? 1 /* Array */ : isMap(thing) ? 2 /* Map */ : isSet(thing) ? 3 /* Set */ : 0 /* Object */;
}
var has = (thing, prop, type = getArchtype(thing)) => type === 2 /* Map */ ? thing.has(prop) : O[PROTOTYPE].hasOwnProperty.call(thing, prop);
var get = (thing, prop, type = getArchtype(thing)) => (
  // @ts-ignore
  type === 2 /* Map */ ? thing.get(prop) : thing[prop]
);
var set = (thing, propOrOldValue, value, type = getArchtype(thing)) => {
  if (type === 2 /* Map */)
    thing.set(propOrOldValue, value);
  else if (type === 3 /* Set */) {
    thing.add(value);
  } else
    thing[propOrOldValue] = value;
};
function is(x, y) {
  if (x === y) {
    return x !== 0 || 1 / x === 1 / y;
  } else {
    return x !== x && y !== y;
  }
}
var isArray = Array.isArray;
var isMap = (target) => target instanceof Map;
var isSet = (target) => target instanceof Set;
var isObjectish = (target) => typeof target === "object";
var isFunction = (target) => typeof target === "function";
var isBoolean = (target) => typeof target === "boolean";
function isArrayIndex(value) {
  const n = +value;
  return Number.isInteger(n) && String(n) === value;
}
var getProxyDraft = (value) => {
  if (!isObjectish(value))
    return null;
  return value?.[DRAFT_STATE];
};
var latest = (state) => state.copy_ || state.base_;
var getValue = (value) => {
  const proxyDraft = getProxyDraft(value);
  return proxyDraft ? proxyDraft.copy_ ?? proxyDraft.base_ : value;
};
var getFinalValue = (state) => state.modified_ ? state.copy_ : state.base_;
function shallowCopy(base, strict) {
  if (isMap(base)) {
    return new Map(base);
  }
  if (isSet(base)) {
    return new Set(base);
  }
  if (isArray(base))
    return Array[PROTOTYPE].slice.call(base);
  const isPlain = isPlainObject(base);
  if (strict === true || strict === "class_only" && !isPlain) {
    const descriptors = O.getOwnPropertyDescriptors(base);
    delete descriptors[DRAFT_STATE];
    let keys = Reflect.ownKeys(descriptors);
    for (let i = 0; i < keys.length; i++) {
      const key = keys[i];
      const desc = descriptors[key];
      if (desc[WRITABLE] === false) {
        desc[WRITABLE] = true;
        desc[CONFIGURABLE] = true;
      }
      if (desc.get || desc.set)
        descriptors[key] = {
          [CONFIGURABLE]: true,
          [WRITABLE]: true,
          // could live with !!desc.set as well here...
          [ENUMERABLE]: desc[ENUMERABLE],
          [VALUE]: base[key]
        };
    }
    return O.create(getPrototypeOf(base), descriptors);
  } else {
    const proto = getPrototypeOf(base);
    if (proto !== null && isPlain) {
      return { ...base };
    }
    const obj = O.create(proto);
    return O.assign(obj, base);
  }
}
function freeze(obj, deep = false) {
  if (isFrozen(obj) || isDraft(obj) || !isDraftable(obj))
    return obj;
  if (getArchtype(obj) > 1) {
    O.defineProperties(obj, {
      set: dontMutateMethodOverride,
      add: dontMutateMethodOverride,
      clear: dontMutateMethodOverride,
      delete: dontMutateMethodOverride
    });
  }
  O.freeze(obj);
  if (deep)
    each(
      obj,
      (_key, value) => {
        freeze(value, true);
      },
      false
    );
  return obj;
}
function dontMutateFrozenCollections() {
  die(2);
}
var dontMutateMethodOverride = {
  [VALUE]: dontMutateFrozenCollections
};
function isFrozen(obj) {
  if (obj === null || !isObjectish(obj))
    return true;
  return O.isFrozen(obj);
}

// src/utils/plugins.ts
var PluginMapSet = "MapSet";
var PluginPatches = "Patches";
var PluginArrayMethods = "ArrayMethods";
var plugins = {};
function getPlugin(pluginKey) {
  const plugin = plugins[pluginKey];
  if (!plugin) {
    die(0, pluginKey);
  }
  return plugin;
}
var isPluginLoaded = (pluginKey) => !!plugins[pluginKey];
function loadPlugin(pluginKey, implementation) {
  if (!plugins[pluginKey])
    plugins[pluginKey] = implementation;
}

// src/core/scope.ts
var currentScope;
var getCurrentScope = () => currentScope;
var createScope = (parent_, immer_) => ({
  drafts_: [],
  parent_,
  immer_,
  // Whenever the modified draft contains a draft from another scope, we
  // need to prevent auto-freezing so the unowned draft can be finalized.
  canAutoFreeze_: true,
  unfinalizedDrafts_: 0,
  handledSet_: /* @__PURE__ */ new Set(),
  processedForPatches_: /* @__PURE__ */ new Set(),
  mapSetPlugin_: isPluginLoaded(PluginMapSet) ? getPlugin(PluginMapSet) : void 0,
  arrayMethodsPlugin_: isPluginLoaded(PluginArrayMethods) ? getPlugin(PluginArrayMethods) : void 0
});
function usePatchesInScope(scope, patchListener) {
  if (patchListener) {
    scope.patchPlugin_ = getPlugin(PluginPatches);
    scope.patches_ = [];
    scope.inversePatches_ = [];
    scope.patchListener_ = patchListener;
  }
}
function revokeScope(scope) {
  leaveScope(scope);
  scope.drafts_.forEach(revokeDraft);
  scope.drafts_ = null;
}
function leaveScope(scope) {
  if (scope === currentScope) {
    currentScope = scope.parent_;
  }
}
var enterScope = (immer2) => currentScope = createScope(currentScope, immer2);
function revokeDraft(draft) {
  const state = draft[DRAFT_STATE];
  if (state.type_ === 0 /* Object */ || state.type_ === 1 /* Array */)
    state.revoke_();
  else
    state.revoked_ = true;
}

// src/core/finalize.ts
function processResult(result, scope) {
  scope.unfinalizedDrafts_ = scope.drafts_.length;
  const baseDraft = scope.drafts_[0];
  const isReplaced = result !== void 0 && result !== baseDraft;
  if (isReplaced) {
    if (baseDraft[DRAFT_STATE].modified_) {
      revokeScope(scope);
      die(4);
    }
    if (isDraftable(result)) {
      result = finalize(scope, result);
    }
    const { patchPlugin_ } = scope;
    if (patchPlugin_) {
      patchPlugin_.generateReplacementPatches_(
        baseDraft[DRAFT_STATE].base_,
        result,
        scope
      );
    }
  } else {
    result = finalize(scope, baseDraft);
  }
  maybeFreeze(scope, result, true);
  revokeScope(scope);
  if (scope.patches_) {
    scope.patchListener_(scope.patches_, scope.inversePatches_);
  }
  return result !== NOTHING ? result : void 0;
}
function finalize(rootScope, value) {
  if (isFrozen(value))
    return value;
  const state = value[DRAFT_STATE];
  if (!state) {
    const finalValue = handleValue(value, rootScope.handledSet_, rootScope);
    return finalValue;
  }
  if (!isSameScope(state, rootScope)) {
    return value;
  }
  if (!state.modified_) {
    return state.base_;
  }
  if (!state.finalized_) {
    const { callbacks_ } = state;
    if (callbacks_) {
      while (callbacks_.length > 0) {
        const callback = callbacks_.pop();
        callback(rootScope);
      }
    }
    generatePatchesAndFinalize(state, rootScope);
  }
  return state.copy_;
}
function maybeFreeze(scope, value, deep = false) {
  if (!scope.parent_ && scope.immer_.autoFreeze_ && scope.canAutoFreeze_) {
    freeze(value, deep);
  }
}
function markStateFinalized(state) {
  state.finalized_ = true;
  state.scope_.unfinalizedDrafts_--;
}
var isSameScope = (state, rootScope) => state.scope_ === rootScope;
var EMPTY_LOCATIONS_RESULT = [];
function updateDraftInParent(parent, draftValue, finalizedValue, originalKey) {
  const parentCopy = latest(parent);
  const parentType = parent.type_;
  if (originalKey !== void 0) {
    const currentValue = get(parentCopy, originalKey, parentType);
    if (currentValue === draftValue) {
      set(parentCopy, originalKey, finalizedValue, parentType);
      return;
    }
  }
  if (!parent.draftLocations_) {
    const draftLocations = parent.draftLocations_ = /* @__PURE__ */ new Map();
    each(parentCopy, (key, value) => {
      if (isDraft(value)) {
        const keys = draftLocations.get(value) || [];
        keys.push(key);
        draftLocations.set(value, keys);
      }
    });
  }
  const locations = parent.draftLocations_.get(draftValue) ?? EMPTY_LOCATIONS_RESULT;
  for (const location of locations) {
    set(parentCopy, location, finalizedValue, parentType);
  }
}
function registerChildFinalizationCallback(parent, child, key) {
  parent.callbacks_.push(function childCleanup(rootScope) {
    const state = child;
    if (!state || !isSameScope(state, rootScope)) {
      return;
    }
    rootScope.mapSetPlugin_?.fixSetContents(state);
    const finalizedValue = getFinalValue(state);
    updateDraftInParent(parent, state.draft_ ?? state, finalizedValue, key);
    generatePatchesAndFinalize(state, rootScope);
  });
}
function generatePatchesAndFinalize(state, rootScope) {
  const shouldFinalize = state.modified_ && !state.finalized_ && (state.type_ === 3 /* Set */ || state.type_ === 1 /* Array */ && state.allIndicesReassigned_ || (state.assigned_?.size ?? 0) > 0);
  if (shouldFinalize) {
    const { patchPlugin_ } = rootScope;
    if (patchPlugin_) {
      const basePath = patchPlugin_.getPath(state);
      if (basePath) {
        patchPlugin_.generatePatches_(state, basePath, rootScope);
      }
    }
    markStateFinalized(state);
  }
}
function handleCrossReference(target, key, value) {
  const { scope_ } = target;
  if (isDraft(value)) {
    const state = value[DRAFT_STATE];
    if (isSameScope(state, scope_)) {
      state.callbacks_.push(function crossReferenceCleanup() {
        prepareCopy(target);
        const finalizedValue = getFinalValue(state);
        updateDraftInParent(target, value, finalizedValue, key);
      });
    }
  } else if (isDraftable(value)) {
    target.callbacks_.push(function nestedDraftCleanup() {
      const targetCopy = latest(target);
      if (target.type_ === 3 /* Set */) {
        if (targetCopy.has(value)) {
          handleValue(value, scope_.handledSet_, scope_);
        }
      } else {
        if (get(targetCopy, key, target.type_) === value) {
          if (scope_.drafts_.length > 1 && (target.assigned_.get(key) ?? false) === true && target.copy_) {
            handleValue(
              get(target.copy_, key, target.type_),
              scope_.handledSet_,
              scope_
            );
          }
        }
      }
    });
  }
}
function handleValue(target, handledSet, rootScope) {
  if (!rootScope.immer_.autoFreeze_ && rootScope.unfinalizedDrafts_ < 1) {
    return target;
  }
  if (isDraft(target) || handledSet.has(target) || !isDraftable(target) || isFrozen(target)) {
    return target;
  }
  handledSet.add(target);
  each(target, (key, value) => {
    if (isDraft(value)) {
      const state = value[DRAFT_STATE];
      if (isSameScope(state, rootScope)) {
        const updatedValue = getFinalValue(state);
        set(target, key, updatedValue, target.type_);
        markStateFinalized(state);
      }
    } else if (isDraftable(value)) {
      handleValue(value, handledSet, rootScope);
    }
  });
  return target;
}

// src/core/proxy.ts
function createProxyProxy(base, parent) {
  const baseIsArray = isArray(base);
  const state = {
    type_: baseIsArray ? 1 /* Array */ : 0 /* Object */,
    // Track which produce call this is associated with.
    scope_: parent ? parent.scope_ : getCurrentScope(),
    // True for both shallow and deep changes.
    modified_: false,
    // Used during finalization.
    finalized_: false,
    // Track which properties have been assigned (true) or deleted (false).
    // actually instantiated in `prepareCopy()`
    assigned_: void 0,
    // The parent draft state.
    parent_: parent,
    // The base state.
    base_: base,
    // The base proxy.
    draft_: null,
    // set below
    // The base copy with any updated values.
    copy_: null,
    // Called by the `produce` function.
    revoke_: null,
    isManual_: false,
    // `callbacks` actually gets assigned in `createProxy`
    callbacks_: void 0
  };
  let target = state;
  let traps = objectTraps;
  if (baseIsArray) {
    target = [state];
    traps = arrayTraps;
  }
  const { revoke, proxy } = Proxy.revocable(target, traps);
  state.draft_ = proxy;
  state.revoke_ = revoke;
  return [proxy, state];
}
var objectTraps = {
  get(state, prop) {
    if (prop === DRAFT_STATE)
      return state;
    let arrayPlugin = state.scope_.arrayMethodsPlugin_;
    const isArrayWithStringProp = state.type_ === 1 /* Array */ && typeof prop === "string";
    if (isArrayWithStringProp) {
      if (arrayPlugin?.isArrayOperationMethod(prop)) {
        return arrayPlugin.createMethodInterceptor(state, prop);
      }
    }
    const source = latest(state);
    if (!has(source, prop, state.type_)) {
      return readPropFromProto(state, source, prop);
    }
    const value = source[prop];
    if (state.finalized_ || !isDraftable(value)) {
      return value;
    }
    if (isArrayWithStringProp && state.operationMethod && arrayPlugin?.isMutatingArrayMethod(
      state.operationMethod
    ) && isArrayIndex(prop)) {
      return value;
    }
    if (value === peek(state.base_, prop)) {
      prepareCopy(state);
      const childKey = state.type_ === 1 /* Array */ ? +prop : prop;
      const childDraft = createProxy(state.scope_, value, state, childKey);
      return state.copy_[childKey] = childDraft;
    }
    return value;
  },
  has(state, prop) {
    return prop in latest(state);
  },
  ownKeys(state) {
    return Reflect.ownKeys(latest(state));
  },
  set(state, prop, value) {
    const desc = getDescriptorFromProto(latest(state), prop);
    if (desc?.set) {
      desc.set.call(state.draft_, value);
      return true;
    }
    if (!state.modified_) {
      const current2 = peek(latest(state), prop);
      const currentState = current2?.[DRAFT_STATE];
      if (currentState && currentState.base_ === value) {
        state.copy_[prop] = value;
        state.assigned_.set(prop, false);
        return true;
      }
      if (is(value, current2) && (value !== void 0 || has(state.base_, prop, state.type_)))
        return true;
      prepareCopy(state);
      markChanged(state);
    }
    if (state.copy_[prop] === value && // special case: handle new props with value 'undefined'
    (value !== void 0 || prop in state.copy_) || // special case: NaN
    Number.isNaN(value) && Number.isNaN(state.copy_[prop]))
      return true;
    state.copy_[prop] = value;
    state.assigned_.set(prop, true);
    handleCrossReference(state, prop, value);
    return true;
  },
  deleteProperty(state, prop) {
    prepareCopy(state);
    if (peek(state.base_, prop) !== void 0 || prop in state.base_) {
      state.assigned_.set(prop, false);
      markChanged(state);
    } else {
      state.assigned_.delete(prop);
    }
    if (state.copy_) {
      delete state.copy_[prop];
    }
    return true;
  },
  // Note: We never coerce `desc.value` into an Immer draft, because we can't make
  // the same guarantee in ES5 mode.
  getOwnPropertyDescriptor(state, prop) {
    const owner = latest(state);
    const desc = Reflect.getOwnPropertyDescriptor(owner, prop);
    if (!desc)
      return desc;
    return {
      [WRITABLE]: true,
      [CONFIGURABLE]: state.type_ !== 1 /* Array */ || prop !== "length",
      [ENUMERABLE]: desc[ENUMERABLE],
      [VALUE]: owner[prop]
    };
  },
  defineProperty() {
    die(11);
  },
  getPrototypeOf(state) {
    return getPrototypeOf(state.base_);
  },
  setPrototypeOf() {
    die(12);
  }
};
var arrayTraps = {};
for (let key in objectTraps) {
  let fn = objectTraps[key];
  arrayTraps[key] = function() {
    const args = arguments;
    args[0] = args[0][0];
    return fn.apply(this, args);
  };
}
arrayTraps.deleteProperty = function(state, prop) {
  if ( true && isNaN(parseInt(prop)))
    die(13);
  return arrayTraps.set.call(this, state, prop, void 0);
};
arrayTraps.set = function(state, prop, value) {
  if ( true && prop !== "length" && isNaN(parseInt(prop)))
    die(14);
  return objectTraps.set.call(this, state[0], prop, value, state[0]);
};
function peek(draft, prop) {
  const state = draft[DRAFT_STATE];
  const source = state ? latest(state) : draft;
  return source[prop];
}
function readPropFromProto(state, source, prop) {
  const desc = getDescriptorFromProto(source, prop);
  return desc ? VALUE in desc ? desc[VALUE] : (
    // This is a very special case, if the prop is a getter defined by the
    // prototype, we should invoke it with the draft as context!
    desc.get?.call(state.draft_)
  ) : void 0;
}
function getDescriptorFromProto(source, prop) {
  if (!(prop in source))
    return void 0;
  let proto = getPrototypeOf(source);
  while (proto) {
    const desc = Object.getOwnPropertyDescriptor(proto, prop);
    if (desc)
      return desc;
    proto = getPrototypeOf(proto);
  }
  return void 0;
}
function markChanged(state) {
  if (!state.modified_) {
    state.modified_ = true;
    if (state.parent_) {
      markChanged(state.parent_);
    }
  }
}
function prepareCopy(state) {
  if (!state.copy_) {
    state.assigned_ = /* @__PURE__ */ new Map();
    state.copy_ = shallowCopy(
      state.base_,
      state.scope_.immer_.useStrictShallowCopy_
    );
  }
}

// src/core/immerClass.ts
var Immer2 = class {
  constructor(config) {
    this.autoFreeze_ = true;
    this.useStrictShallowCopy_ = false;
    this.useStrictIteration_ = false;
    /**
     * The `produce` function takes a value and a "recipe function" (whose
     * return value often depends on the base state). The recipe function is
     * free to mutate its first argument however it wants. All mutations are
     * only ever applied to a __copy__ of the base state.
     *
     * Pass only a function to create a "curried producer" which relieves you
     * from passing the recipe function every time.
     *
     * Only plain objects and arrays are made mutable. All other objects are
     * considered uncopyable.
     *
     * Note: This function is __bound__ to its `Immer` instance.
     *
     * @param {any} base - the initial state
     * @param {Function} recipe - function that receives a proxy of the base state as first argument and which can be freely modified
     * @param {Function} patchListener - optional function that will be called with all the patches produced here
     * @returns {any} a new state, or the initial state if nothing was modified
     */
    this.produce = (base, recipe, patchListener) => {
      if (isFunction(base) && !isFunction(recipe)) {
        const defaultBase = recipe;
        recipe = base;
        const self = this;
        return function curriedProduce(base2 = defaultBase, ...args) {
          return self.produce(base2, (draft) => recipe.call(this, draft, ...args));
        };
      }
      if (!isFunction(recipe))
        die(6);
      if (patchListener !== void 0 && !isFunction(patchListener))
        die(7);
      let result;
      if (isDraftable(base)) {
        const scope = enterScope(this);
        const proxy = createProxy(scope, base, void 0);
        let hasError = true;
        try {
          result = recipe(proxy);
          hasError = false;
        } finally {
          if (hasError)
            revokeScope(scope);
          else
            leaveScope(scope);
        }
        usePatchesInScope(scope, patchListener);
        return processResult(result, scope);
      } else if (!base || !isObjectish(base)) {
        result = recipe(base);
        if (result === void 0)
          result = base;
        if (result === NOTHING)
          result = void 0;
        if (this.autoFreeze_)
          freeze(result, true);
        if (patchListener) {
          const p = [];
          const ip = [];
          getPlugin(PluginPatches).generateReplacementPatches_(base, result, {
            patches_: p,
            inversePatches_: ip
          });
          patchListener(p, ip);
        }
        return result;
      } else
        die(1, base);
    };
    this.produceWithPatches = (base, recipe) => {
      if (isFunction(base)) {
        return (state, ...args) => this.produceWithPatches(state, (draft) => base(draft, ...args));
      }
      let patches, inversePatches;
      const result = this.produce(base, recipe, (p, ip) => {
        patches = p;
        inversePatches = ip;
      });
      return [result, patches, inversePatches];
    };
    if (isBoolean(config?.autoFreeze))
      this.setAutoFreeze(config.autoFreeze);
    if (isBoolean(config?.useStrictShallowCopy))
      this.setUseStrictShallowCopy(config.useStrictShallowCopy);
    if (isBoolean(config?.useStrictIteration))
      this.setUseStrictIteration(config.useStrictIteration);
  }
  createDraft(base) {
    if (!isDraftable(base))
      die(8);
    if (isDraft(base))
      base = current(base);
    const scope = enterScope(this);
    const proxy = createProxy(scope, base, void 0);
    proxy[DRAFT_STATE].isManual_ = true;
    leaveScope(scope);
    return proxy;
  }
  finishDraft(draft, patchListener) {
    const state = draft && draft[DRAFT_STATE];
    if (!state || !state.isManual_)
      die(9);
    const { scope_: scope } = state;
    usePatchesInScope(scope, patchListener);
    return processResult(void 0, scope);
  }
  /**
   * Pass true to automatically freeze all copies created by Immer.
   *
   * By default, auto-freezing is enabled.
   */
  setAutoFreeze(value) {
    this.autoFreeze_ = value;
  }
  /**
   * Pass true to enable strict shallow copy.
   *
   * By default, immer does not copy the object descriptors such as getter, setter and non-enumrable properties.
   */
  setUseStrictShallowCopy(value) {
    this.useStrictShallowCopy_ = value;
  }
  /**
   * Pass false to use faster iteration that skips non-enumerable properties
   * but still handles symbols for compatibility.
   *
   * By default, strict iteration is enabled (includes all own properties).
   */
  setUseStrictIteration(value) {
    this.useStrictIteration_ = value;
  }
  shouldUseStrictIteration() {
    return this.useStrictIteration_;
  }
  applyPatches(base, patches) {
    let i;
    for (i = patches.length - 1; i >= 0; i--) {
      const patch = patches[i];
      if (patch.path.length === 0 && patch.op === "replace") {
        base = patch.value;
        break;
      }
    }
    if (i > -1) {
      patches = patches.slice(i + 1);
    }
    const applyPatchesImpl = getPlugin(PluginPatches).applyPatches_;
    if (isDraft(base)) {
      return applyPatchesImpl(base, patches);
    }
    return this.produce(
      base,
      (draft) => applyPatchesImpl(draft, patches)
    );
  }
};
function createProxy(rootScope, value, parent, key) {
  const [draft, state] = isMap(value) ? getPlugin(PluginMapSet).proxyMap_(value, parent) : isSet(value) ? getPlugin(PluginMapSet).proxySet_(value, parent) : createProxyProxy(value, parent);
  const scope = parent?.scope_ ?? getCurrentScope();
  scope.drafts_.push(draft);
  state.callbacks_ = parent?.callbacks_ ?? [];
  state.key_ = key;
  if (parent && key !== void 0) {
    registerChildFinalizationCallback(parent, state, key);
  } else {
    state.callbacks_.push(function rootDraftCleanup(rootScope2) {
      rootScope2.mapSetPlugin_?.fixSetContents(state);
      const { patchPlugin_ } = rootScope2;
      if (state.modified_ && patchPlugin_) {
        patchPlugin_.generatePatches_(state, [], rootScope2);
      }
    });
  }
  return draft;
}

// src/core/current.ts
function current(value) {
  if (!isDraft(value))
    die(10, value);
  return currentImpl(value);
}
function currentImpl(value) {
  if (!isDraftable(value) || isFrozen(value))
    return value;
  const state = value[DRAFT_STATE];
  let copy;
  let strict = true;
  if (state) {
    if (!state.modified_)
      return state.base_;
    state.finalized_ = true;
    copy = shallowCopy(value, state.scope_.immer_.useStrictShallowCopy_);
    strict = state.scope_.immer_.shouldUseStrictIteration();
  } else {
    copy = shallowCopy(value, true);
  }
  each(
    copy,
    (key, childValue) => {
      set(copy, key, currentImpl(childValue));
    },
    strict
  );
  if (state) {
    state.finalized_ = false;
  }
  return copy;
}

// src/plugins/patches.ts
function enablePatches() {
  const errorOffset = 16;
  if (true) {
    errors.push(
      'Sets cannot have "replace" patches.',
      function(op) {
        return "Unsupported patch operation: " + op;
      },
      function(path) {
        return "Cannot apply patch, path doesn't resolve: " + path;
      },
      "Patching reserved attributes like __proto__, prototype and constructor is not allowed"
    );
  }
  function getPath(state, path = []) {
    if (state.key_ !== void 0) {
      const parentCopy = state.parent_.copy_ ?? state.parent_.base_;
      const proxyDraft = getProxyDraft(get(parentCopy, state.key_));
      const valueAtKey = get(parentCopy, state.key_);
      if (valueAtKey === void 0) {
        return null;
      }
      if (valueAtKey !== state.draft_ && valueAtKey !== state.base_ && valueAtKey !== state.copy_) {
        return null;
      }
      if (proxyDraft != null && proxyDraft.base_ !== state.base_) {
        return null;
      }
      const isSet2 = state.parent_.type_ === 3 /* Set */;
      let key;
      if (isSet2) {
        const setParent = state.parent_;
        key = Array.from(setParent.drafts_.keys()).indexOf(state.key_);
      } else {
        key = state.key_;
      }
      if (!(isSet2 && parentCopy.size > key || has(parentCopy, key))) {
        return null;
      }
      path.push(key);
    }
    if (state.parent_) {
      return getPath(state.parent_, path);
    }
    path.reverse();
    try {
      resolvePath(state.copy_, path);
    } catch (e) {
      return null;
    }
    return path;
  }
  function resolvePath(base, path) {
    let current2 = base;
    for (let i = 0; i < path.length - 1; i++) {
      const key = path[i];
      current2 = get(current2, key);
      if (!isObjectish(current2) || current2 === null) {
        throw new Error(`Cannot resolve path at '${path.join("/")}'`);
      }
    }
    return current2;
  }
  const REPLACE = "replace";
  const ADD = "add";
  const REMOVE = "remove";
  function generatePatches_(state, basePath, scope) {
    if (state.scope_.processedForPatches_.has(state)) {
      return;
    }
    state.scope_.processedForPatches_.add(state);
    const { patches_, inversePatches_ } = scope;
    switch (state.type_) {
      case 0 /* Object */:
      case 2 /* Map */:
        return generatePatchesFromAssigned(
          state,
          basePath,
          patches_,
          inversePatches_
        );
      case 1 /* Array */:
        return generateArrayPatches(
          state,
          basePath,
          patches_,
          inversePatches_
        );
      case 3 /* Set */:
        return generateSetPatches(
          state,
          basePath,
          patches_,
          inversePatches_
        );
    }
  }
  function generateArrayPatches(state, basePath, patches, inversePatches) {
    let { base_, assigned_ } = state;
    let copy_ = state.copy_;
    if (copy_.length < base_.length) {
      ;
      [base_, copy_] = [copy_, base_];
      [patches, inversePatches] = [inversePatches, patches];
    }
    const allReassigned = state.allIndicesReassigned_ === true;
    for (let i = 0; i < base_.length; i++) {
      const copiedItem = copy_[i];
      const baseItem = base_[i];
      const isAssigned = allReassigned || assigned_?.get(i.toString());
      if (isAssigned && copiedItem !== baseItem) {
        const childState = copiedItem?.[DRAFT_STATE];
        if (childState && childState.modified_) {
          continue;
        }
        const path = basePath.concat([i]);
        patches.push({
          op: REPLACE,
          path,
          // Need to maybe clone it, as it can in fact be the original value
          // due to the base/copy inversion at the start of this function
          value: clonePatchValueIfNeeded(copiedItem)
        });
        inversePatches.push({
          op: REPLACE,
          path,
          value: clonePatchValueIfNeeded(baseItem)
        });
      }
    }
    for (let i = base_.length; i < copy_.length; i++) {
      const path = basePath.concat([i]);
      patches.push({
        op: ADD,
        path,
        // Need to maybe clone it, as it can in fact be the original value
        // due to the base/copy inversion at the start of this function
        value: clonePatchValueIfNeeded(copy_[i])
      });
    }
    for (let i = copy_.length - 1; base_.length <= i; --i) {
      const path = basePath.concat([i]);
      inversePatches.push({
        op: REMOVE,
        path
      });
    }
  }
  function generatePatchesFromAssigned(state, basePath, patches, inversePatches) {
    const { base_, copy_, type_ } = state;
    each(state.assigned_, (key, assignedValue) => {
      const origValue = get(base_, key, type_);
      const value = get(copy_, key, type_);
      const op = !assignedValue ? REMOVE : has(base_, key) ? REPLACE : ADD;
      if (origValue === value && op === REPLACE)
        return;
      const path = basePath.concat(key);
      patches.push(
        op === REMOVE ? { op, path } : { op, path, value: clonePatchValueIfNeeded(value) }
      );
      inversePatches.push(
        op === ADD ? { op: REMOVE, path } : op === REMOVE ? { op: ADD, path, value: clonePatchValueIfNeeded(origValue) } : { op: REPLACE, path, value: clonePatchValueIfNeeded(origValue) }
      );
    });
  }
  function generateSetPatches(state, basePath, patches, inversePatches) {
    let { base_, copy_ } = state;
    let i = 0;
    base_.forEach((value) => {
      if (!copy_.has(value)) {
        const path = basePath.concat([i]);
        patches.push({
          op: REMOVE,
          path,
          value
        });
        inversePatches.unshift({
          op: ADD,
          path,
          value
        });
      }
      i++;
    });
    i = 0;
    copy_.forEach((value) => {
      if (!base_.has(value)) {
        const path = basePath.concat([i]);
        patches.push({
          op: ADD,
          path,
          value
        });
        inversePatches.unshift({
          op: REMOVE,
          path,
          value
        });
      }
      i++;
    });
  }
  function generateReplacementPatches_(baseValue, replacement, scope) {
    const { patches_, inversePatches_ } = scope;
    patches_.push({
      op: REPLACE,
      path: [],
      value: replacement === NOTHING ? void 0 : replacement
    });
    inversePatches_.push({
      op: REPLACE,
      path: [],
      value: baseValue
    });
  }
  function applyPatches_(draft, patches) {
    patches.forEach((patch) => {
      const { path, op } = patch;
      let base = draft;
      for (let i = 0; i < path.length - 1; i++) {
        const parentType = getArchtype(base);
        let p = path[i];
        if (typeof p !== "string" && typeof p !== "number") {
          p = "" + p;
        }
        if ((parentType === 0 /* Object */ || parentType === 1 /* Array */) && (p === "__proto__" || p === CONSTRUCTOR))
          die(errorOffset + 3);
        if (isFunction(base) && p === PROTOTYPE)
          die(errorOffset + 3);
        base = get(base, p);
        if (!isObjectish(base))
          die(errorOffset + 2, path.join("/"));
      }
      const type = getArchtype(base);
      const value = deepClonePatchValue(patch.value);
      const key = path[path.length - 1];
      switch (op) {
        case REPLACE:
          switch (type) {
            case 2 /* Map */:
              return base.set(key, value);
            case 3 /* Set */:
              die(errorOffset);
            default:
              return base[key] = value;
          }
        case ADD:
          switch (type) {
            case 1 /* Array */:
              return key === "-" ? base.push(value) : base.splice(key, 0, value);
            case 2 /* Map */:
              return base.set(key, value);
            case 3 /* Set */:
              return base.add(value);
            default:
              return base[key] = value;
          }
        case REMOVE:
          switch (type) {
            case 1 /* Array */:
              return base.splice(key, 1);
            case 2 /* Map */:
              return base.delete(key);
            case 3 /* Set */:
              return base.delete(patch.value);
            default:
              return delete base[key];
          }
        default:
          die(errorOffset + 1, op);
      }
    });
    return draft;
  }
  function deepClonePatchValue(obj) {
    if (!isDraftable(obj))
      return obj;
    if (isArray(obj))
      return obj.map(deepClonePatchValue);
    if (isMap(obj))
      return new Map(
        Array.from(obj.entries()).map(([k, v]) => [k, deepClonePatchValue(v)])
      );
    if (isSet(obj))
      return new Set(Array.from(obj).map(deepClonePatchValue));
    const cloned = Object.create(getPrototypeOf(obj));
    for (const key in obj)
      cloned[key] = deepClonePatchValue(obj[key]);
    if (has(obj, DRAFTABLE))
      cloned[DRAFTABLE] = obj[DRAFTABLE];
    return cloned;
  }
  function clonePatchValueIfNeeded(obj) {
    if (isDraft(obj)) {
      return deepClonePatchValue(obj);
    } else
      return obj;
  }
  loadPlugin(PluginPatches, {
    applyPatches_,
    generatePatches_,
    generateReplacementPatches_,
    getPath
  });
}

// src/plugins/mapset.ts
function enableMapSet() {
  class DraftMap extends Map {
    constructor(target, parent) {
      super();
      this[DRAFT_STATE] = {
        type_: 2 /* Map */,
        parent_: parent,
        scope_: parent ? parent.scope_ : getCurrentScope(),
        modified_: false,
        finalized_: false,
        copy_: void 0,
        assigned_: void 0,
        base_: target,
        draft_: this,
        isManual_: false,
        revoked_: false,
        callbacks_: []
      };
    }
    get size() {
      return latest(this[DRAFT_STATE]).size;
    }
    has(key) {
      return latest(this[DRAFT_STATE]).has(key);
    }
    set(key, value) {
      const state = this[DRAFT_STATE];
      assertUnrevoked(state);
      if (!latest(state).has(key) || latest(state).get(key) !== value) {
        prepareMapCopy(state);
        markChanged(state);
        state.assigned_.set(key, true);
        state.copy_.set(key, value);
        state.assigned_.set(key, true);
        handleCrossReference(state, key, value);
      }
      return this;
    }
    delete(key) {
      if (!this.has(key)) {
        return false;
      }
      const state = this[DRAFT_STATE];
      assertUnrevoked(state);
      prepareMapCopy(state);
      markChanged(state);
      if (state.base_.has(key)) {
        state.assigned_.set(key, false);
      } else {
        state.assigned_.delete(key);
      }
      state.copy_.delete(key);
      return true;
    }
    clear() {
      const state = this[DRAFT_STATE];
      assertUnrevoked(state);
      if (latest(state).size) {
        prepareMapCopy(state);
        markChanged(state);
        state.assigned_ = /* @__PURE__ */ new Map();
        each(state.base_, (key) => {
          state.assigned_.set(key, false);
        });
        state.copy_.clear();
      }
    }
    forEach(cb, thisArg) {
      const state = this[DRAFT_STATE];
      latest(state).forEach((_value, key, _map) => {
        cb.call(thisArg, this.get(key), key, this);
      });
    }
    get(key) {
      const state = this[DRAFT_STATE];
      assertUnrevoked(state);
      const value = latest(state).get(key);
      if (state.finalized_ || !isDraftable(value)) {
        return value;
      }
      if (value !== state.base_.get(key)) {
        return value;
      }
      const draft = createProxy(state.scope_, value, state, key);
      prepareMapCopy(state);
      state.copy_.set(key, draft);
      return draft;
    }
    keys() {
      return latest(this[DRAFT_STATE]).keys();
    }
    values() {
      const iterator = this.keys();
      return {
        [Symbol.iterator]: () => this.values(),
        next: () => {
          const r = iterator.next();
          if (r.done)
            return r;
          const value = this.get(r.value);
          return {
            done: false,
            value
          };
        }
      };
    }
    entries() {
      const iterator = this.keys();
      return {
        [Symbol.iterator]: () => this.entries(),
        next: () => {
          const r = iterator.next();
          if (r.done)
            return r;
          const value = this.get(r.value);
          return {
            done: false,
            value: [r.value, value]
          };
        }
      };
    }
    [(DRAFT_STATE, Symbol.iterator)]() {
      return this.entries();
    }
  }
  function proxyMap_(target, parent) {
    const map = new DraftMap(target, parent);
    return [map, map[DRAFT_STATE]];
  }
  function prepareMapCopy(state) {
    if (!state.copy_) {
      state.assigned_ = /* @__PURE__ */ new Map();
      state.copy_ = new Map(state.base_);
    }
  }
  class DraftSet extends Set {
    constructor(target, parent) {
      super();
      this[DRAFT_STATE] = {
        type_: 3 /* Set */,
        parent_: parent,
        scope_: parent ? parent.scope_ : getCurrentScope(),
        modified_: false,
        finalized_: false,
        copy_: void 0,
        base_: target,
        draft_: this,
        drafts_: /* @__PURE__ */ new Map(),
        revoked_: false,
        isManual_: false,
        assigned_: void 0,
        callbacks_: []
      };
    }
    get size() {
      return latest(this[DRAFT_STATE]).size;
    }
    has(value) {
      const state = this[DRAFT_STATE];
      assertUnrevoked(state);
      if (!state.copy_) {
        return state.base_.has(value);
      }
      if (state.copy_.has(value))
        return true;
      if (state.drafts_.has(value) && state.copy_.has(state.drafts_.get(value)))
        return true;
      return false;
    }
    add(value) {
      const state = this[DRAFT_STATE];
      assertUnrevoked(state);
      if (!this.has(value)) {
        prepareSetCopy(state);
        markChanged(state);
        state.copy_.add(value);
        handleCrossReference(state, value, value);
      }
      return this;
    }
    delete(value) {
      if (!this.has(value)) {
        return false;
      }
      const state = this[DRAFT_STATE];
      assertUnrevoked(state);
      prepareSetCopy(state);
      markChanged(state);
      return state.copy_.delete(value) || (state.drafts_.has(value) ? state.copy_.delete(state.drafts_.get(value)) : (
        /* istanbul ignore next */
        false
      ));
    }
    clear() {
      const state = this[DRAFT_STATE];
      assertUnrevoked(state);
      if (latest(state).size) {
        prepareSetCopy(state);
        markChanged(state);
        state.copy_.clear();
      }
    }
    values() {
      const state = this[DRAFT_STATE];
      assertUnrevoked(state);
      prepareSetCopy(state);
      return state.copy_.values();
    }
    entries() {
      const state = this[DRAFT_STATE];
      assertUnrevoked(state);
      prepareSetCopy(state);
      return state.copy_.entries();
    }
    keys() {
      return this.values();
    }
    [(DRAFT_STATE, Symbol.iterator)]() {
      return this.values();
    }
    forEach(cb, thisArg) {
      const iterator = this.values();
      let result = iterator.next();
      while (!result.done) {
        cb.call(thisArg, result.value, result.value, this);
        result = iterator.next();
      }
    }
  }
  function proxySet_(target, parent) {
    const set2 = new DraftSet(target, parent);
    return [set2, set2[DRAFT_STATE]];
  }
  function prepareSetCopy(state) {
    if (!state.copy_) {
      state.copy_ = /* @__PURE__ */ new Set();
      state.base_.forEach((value) => {
        if (isDraftable(value)) {
          const draft = createProxy(state.scope_, value, state, value);
          state.drafts_.set(value, draft);
          state.copy_.add(draft);
        } else {
          state.copy_.add(value);
        }
      });
    }
  }
  function assertUnrevoked(state) {
    if (state.revoked_)
      die(3, JSON.stringify(latest(state)));
  }
  function fixSetContents(target) {
    if (target.type_ === 3 /* Set */ && target.copy_) {
      const copy = new Set(target.copy_);
      target.copy_.clear();
      copy.forEach((value) => {
        target.copy_.add(getValue(value));
      });
    }
  }
  loadPlugin(PluginMapSet, { proxyMap_, proxySet_, fixSetContents });
}

// src/plugins/arrayMethods.ts
function enableArrayMethods() {
  const SHIFTING_METHODS = /* @__PURE__ */ new Set(["shift", "unshift"]);
  const QUEUE_METHODS = /* @__PURE__ */ new Set(["push", "pop"]);
  const RESULT_RETURNING_METHODS = /* @__PURE__ */ new Set([
    ...QUEUE_METHODS,
    ...SHIFTING_METHODS
  ]);
  const REORDERING_METHODS = /* @__PURE__ */ new Set(["reverse", "sort"]);
  const MUTATING_METHODS = /* @__PURE__ */ new Set([
    ...RESULT_RETURNING_METHODS,
    ...REORDERING_METHODS,
    "splice"
  ]);
  const FIND_METHODS = /* @__PURE__ */ new Set(["find", "findLast"]);
  const NON_MUTATING_METHODS = /* @__PURE__ */ new Set([
    "filter",
    "slice",
    "concat",
    "flat",
    ...FIND_METHODS,
    "findIndex",
    "findLastIndex",
    "some",
    "every",
    "indexOf",
    "lastIndexOf",
    "includes",
    "join",
    "toString",
    "toLocaleString"
  ]);
  function isMutatingArrayMethod(method) {
    return MUTATING_METHODS.has(method);
  }
  function isNonMutatingArrayMethod(method) {
    return NON_MUTATING_METHODS.has(method);
  }
  function isArrayOperationMethod(method) {
    return isMutatingArrayMethod(method) || isNonMutatingArrayMethod(method);
  }
  function enterOperation(state, method) {
    state.operationMethod = method;
  }
  function exitOperation(state) {
    state.operationMethod = void 0;
  }
  function executeArrayMethod(state, operation, markLength = true) {
    prepareCopy(state);
    const result = operation();
    markChanged(state);
    if (markLength)
      state.assigned_.set("length", true);
    return result;
  }
  function markAllIndicesReassigned(state) {
    state.allIndicesReassigned_ = true;
  }
  function normalizeSliceIndex(index, length) {
    if (index < 0) {
      return Math.max(length + index, 0);
    }
    return Math.min(index, length);
  }
  function handleInsertedValues(state, startIndex, values) {
    for (let i = 0; i < values.length; i++) {
      const index = startIndex + i;
      state.assigned_.set(index, true);
      handleCrossReference(state, index, values[i]);
    }
  }
  function handleSimpleOperation(state, method, args) {
    return executeArrayMethod(state, () => {
      const lengthBefore = state.copy_.length;
      const result = state.copy_[method](...args);
      if (SHIFTING_METHODS.has(method)) {
        markAllIndicesReassigned(state);
      }
      if (method === "push" && args.length > 0) {
        handleInsertedValues(state, lengthBefore, args);
      } else if (method === "unshift" && args.length > 0) {
        handleInsertedValues(state, 0, args);
      }
      return RESULT_RETURNING_METHODS.has(method) ? result : state.draft_;
    });
  }
  function handleReorderingOperation(state, method, args) {
    return executeArrayMethod(
      state,
      () => {
        ;
        state.copy_[method](...args);
        markAllIndicesReassigned(state);
        return state.draft_;
      },
      false
    );
  }
  function createMethodInterceptor(state, originalMethod) {
    return function interceptedMethod(...args) {
      const method = originalMethod;
      enterOperation(state, method);
      try {
        if (isMutatingArrayMethod(method)) {
          if (RESULT_RETURNING_METHODS.has(method)) {
            return handleSimpleOperation(state, method, args);
          }
          if (REORDERING_METHODS.has(method)) {
            return handleReorderingOperation(state, method, args);
          }
          if (method === "splice") {
            const res = executeArrayMethod(
              state,
              () => state.copy_.splice(...args)
            );
            markAllIndicesReassigned(state);
            if (args.length > 2) {
              const startIndex = normalizeSliceIndex(
                args[0] ?? 0,
                state.copy_.length
              );
              handleInsertedValues(state, startIndex, args.slice(2));
            }
            return res;
          }
        } else {
          return handleNonMutatingOperation(state, method, args);
        }
      } finally {
        exitOperation(state);
      }
    };
  }
  function handleNonMutatingOperation(state, method, args) {
    const source = latest(state);
    if (method === "filter") {
      const predicate = args[0];
      const result = [];
      for (let i = 0; i < source.length; i++) {
        if (predicate(source[i], i, source)) {
          result.push(state.draft_[i]);
        }
      }
      return result;
    }
    if (FIND_METHODS.has(method)) {
      const predicate = args[0];
      const isForward = method === "find";
      const step = isForward ? 1 : -1;
      const start = isForward ? 0 : source.length - 1;
      for (let i = start; i >= 0 && i < source.length; i += step) {
        if (predicate(source[i], i, source)) {
          return state.draft_[i];
        }
      }
      return void 0;
    }
    if (method === "slice") {
      const rawStart = args[0] ?? 0;
      const rawEnd = args[1] ?? source.length;
      const start = normalizeSliceIndex(rawStart, source.length);
      const end = normalizeSliceIndex(rawEnd, source.length);
      const result = [];
      for (let i = start; i < end; i++) {
        result.push(state.draft_[i]);
      }
      return result;
    }
    return source[method](...args);
  }
  loadPlugin(PluginArrayMethods, {
    createMethodInterceptor,
    isArrayOperationMethod,
    isMutatingArrayMethod
  });
}

// src/immer.ts
var immer = new Immer2();
var produce = immer.produce;
var produceWithPatches = /* @__PURE__ */ immer.produceWithPatches.bind(
  immer
);
var setAutoFreeze = /* @__PURE__ */ immer.setAutoFreeze.bind(immer);
var setUseStrictShallowCopy = /* @__PURE__ */ immer.setUseStrictShallowCopy.bind(
  immer
);
var setUseStrictIteration = /* @__PURE__ */ immer.setUseStrictIteration.bind(
  immer
);
var applyPatches = /* @__PURE__ */ immer.applyPatches.bind(immer);
var createDraft = /* @__PURE__ */ immer.createDraft.bind(immer);
var finishDraft = /* @__PURE__ */ immer.finishDraft.bind(immer);
var castDraft = (value) => value;
var castImmutable = (value) => value;

//# sourceMappingURL=immer.mjs.map

/***/ },

/***/ "./node_modules/react-redux/dist/react-redux.mjs"
/*!*******************************************************!*\
  !*** ./node_modules/react-redux/dist/react-redux.mjs ***!
  \*******************************************************/
(__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   Provider: () => (/* binding */ Provider_default),
/* harmony export */   ReactReduxContext: () => (/* binding */ ReactReduxContext),
/* harmony export */   batch: () => (/* binding */ batch),
/* harmony export */   connect: () => (/* binding */ connect_default),
/* harmony export */   createDispatchHook: () => (/* binding */ createDispatchHook),
/* harmony export */   createSelectorHook: () => (/* binding */ createSelectorHook),
/* harmony export */   createStoreHook: () => (/* binding */ createStoreHook),
/* harmony export */   shallowEqual: () => (/* binding */ shallowEqual),
/* harmony export */   useDispatch: () => (/* binding */ useDispatch),
/* harmony export */   useSelector: () => (/* binding */ useSelector),
/* harmony export */   useStore: () => (/* binding */ useStore)
/* harmony export */ });
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! react */ "react");
/* harmony import */ var use_sync_external_store_with_selector_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! use-sync-external-store/with-selector.js */ "./node_modules/use-sync-external-store/with-selector.js");
// src/utils/react.ts

// src/utils/react-is.ts
var IS_REACT_19 = /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.version.startsWith("19");
var REACT_ELEMENT_TYPE = /* @__PURE__ */ Symbol.for(
  IS_REACT_19 ? "react.transitional.element" : "react.element"
);
var REACT_PORTAL_TYPE = /* @__PURE__ */ Symbol.for("react.portal");
var REACT_FRAGMENT_TYPE = /* @__PURE__ */ Symbol.for("react.fragment");
var REACT_STRICT_MODE_TYPE = /* @__PURE__ */ Symbol.for("react.strict_mode");
var REACT_PROFILER_TYPE = /* @__PURE__ */ Symbol.for("react.profiler");
var REACT_CONSUMER_TYPE = /* @__PURE__ */ Symbol.for("react.consumer");
var REACT_CONTEXT_TYPE = /* @__PURE__ */ Symbol.for("react.context");
var REACT_FORWARD_REF_TYPE = /* @__PURE__ */ Symbol.for("react.forward_ref");
var REACT_SUSPENSE_TYPE = /* @__PURE__ */ Symbol.for("react.suspense");
var REACT_SUSPENSE_LIST_TYPE = /* @__PURE__ */ Symbol.for(
  "react.suspense_list"
);
var REACT_MEMO_TYPE = /* @__PURE__ */ Symbol.for("react.memo");
var REACT_LAZY_TYPE = /* @__PURE__ */ Symbol.for("react.lazy");
var REACT_OFFSCREEN_TYPE = /* @__PURE__ */ Symbol.for("react.offscreen");
var REACT_CLIENT_REFERENCE = /* @__PURE__ */ Symbol.for(
  "react.client.reference"
);
var ForwardRef = REACT_FORWARD_REF_TYPE;
var Memo = REACT_MEMO_TYPE;
function isValidElementType(type) {
  return typeof type === "string" || typeof type === "function" || type === REACT_FRAGMENT_TYPE || type === REACT_PROFILER_TYPE || type === REACT_STRICT_MODE_TYPE || type === REACT_SUSPENSE_TYPE || type === REACT_SUSPENSE_LIST_TYPE || type === REACT_OFFSCREEN_TYPE || typeof type === "object" && type !== null && (type.$$typeof === REACT_LAZY_TYPE || type.$$typeof === REACT_MEMO_TYPE || type.$$typeof === REACT_CONTEXT_TYPE || type.$$typeof === REACT_CONSUMER_TYPE || type.$$typeof === REACT_FORWARD_REF_TYPE || type.$$typeof === REACT_CLIENT_REFERENCE || type.getModuleId !== void 0) ? true : false;
}
function typeOf(object) {
  if (typeof object === "object" && object !== null) {
    const { $$typeof } = object;
    switch ($$typeof) {
      case REACT_ELEMENT_TYPE:
        switch (object = object.type, object) {
          case REACT_FRAGMENT_TYPE:
          case REACT_PROFILER_TYPE:
          case REACT_STRICT_MODE_TYPE:
          case REACT_SUSPENSE_TYPE:
          case REACT_SUSPENSE_LIST_TYPE:
            return object;
          default:
            switch (object = object && object.$$typeof, object) {
              case REACT_CONTEXT_TYPE:
              case REACT_FORWARD_REF_TYPE:
              case REACT_LAZY_TYPE:
              case REACT_MEMO_TYPE:
                return object;
              case REACT_CONSUMER_TYPE:
                return object;
              default:
                return $$typeof;
            }
        }
      case REACT_PORTAL_TYPE:
        return $$typeof;
    }
  }
}
function isContextConsumer(object) {
  return IS_REACT_19 ? typeOf(object) === REACT_CONSUMER_TYPE : typeOf(object) === REACT_CONTEXT_TYPE;
}
function isMemo(object) {
  return typeOf(object) === REACT_MEMO_TYPE;
}

// src/utils/warning.ts
function warning(message) {
  if (typeof console !== "undefined" && typeof console.error === "function") {
    console.error(message);
  }
  try {
    throw new Error(message);
  } catch (e) {
  }
}

// src/connect/verifySubselectors.ts
function verify(selector, methodName) {
  if (!selector) {
    throw new Error(`Unexpected value for ${methodName} in connect.`);
  } else if (methodName === "mapStateToProps" || methodName === "mapDispatchToProps") {
    if (!Object.prototype.hasOwnProperty.call(selector, "dependsOnOwnProps")) {
      warning(
        `The selector for ${methodName} of connect did not specify a value for dependsOnOwnProps.`
      );
    }
  }
}
function verifySubselectors(mapStateToProps, mapDispatchToProps, mergeProps) {
  verify(mapStateToProps, "mapStateToProps");
  verify(mapDispatchToProps, "mapDispatchToProps");
  verify(mergeProps, "mergeProps");
}

// src/connect/selectorFactory.ts
function pureFinalPropsSelectorFactory(mapStateToProps, mapDispatchToProps, mergeProps, dispatch, {
  areStatesEqual,
  areOwnPropsEqual,
  areStatePropsEqual
}) {
  let hasRunAtLeastOnce = false;
  let state;
  let ownProps;
  let stateProps;
  let dispatchProps;
  let mergedProps;
  function handleFirstCall(firstState, firstOwnProps) {
    state = firstState;
    ownProps = firstOwnProps;
    stateProps = mapStateToProps(state, ownProps);
    dispatchProps = mapDispatchToProps(dispatch, ownProps);
    mergedProps = mergeProps(stateProps, dispatchProps, ownProps);
    hasRunAtLeastOnce = true;
    return mergedProps;
  }
  function handleNewPropsAndNewState() {
    stateProps = mapStateToProps(state, ownProps);
    if (mapDispatchToProps.dependsOnOwnProps)
      dispatchProps = mapDispatchToProps(dispatch, ownProps);
    mergedProps = mergeProps(stateProps, dispatchProps, ownProps);
    return mergedProps;
  }
  function handleNewProps() {
    if (mapStateToProps.dependsOnOwnProps)
      stateProps = mapStateToProps(state, ownProps);
    if (mapDispatchToProps.dependsOnOwnProps)
      dispatchProps = mapDispatchToProps(dispatch, ownProps);
    mergedProps = mergeProps(stateProps, dispatchProps, ownProps);
    return mergedProps;
  }
  function handleNewState() {
    const nextStateProps = mapStateToProps(state, ownProps);
    const statePropsChanged = !areStatePropsEqual(nextStateProps, stateProps);
    stateProps = nextStateProps;
    if (statePropsChanged)
      mergedProps = mergeProps(stateProps, dispatchProps, ownProps);
    return mergedProps;
  }
  function handleSubsequentCalls(nextState, nextOwnProps) {
    const propsChanged = !areOwnPropsEqual(nextOwnProps, ownProps);
    const stateChanged = !areStatesEqual(
      nextState,
      state,
      nextOwnProps,
      ownProps
    );
    state = nextState;
    ownProps = nextOwnProps;
    if (propsChanged && stateChanged) return handleNewPropsAndNewState();
    if (propsChanged) return handleNewProps();
    if (stateChanged) return handleNewState();
    return mergedProps;
  }
  return function pureFinalPropsSelector(nextState, nextOwnProps) {
    return hasRunAtLeastOnce ? handleSubsequentCalls(nextState, nextOwnProps) : handleFirstCall(nextState, nextOwnProps);
  };
}
function finalPropsSelectorFactory(dispatch, {
  initMapStateToProps,
  initMapDispatchToProps,
  initMergeProps,
  ...options
}) {
  const mapStateToProps = initMapStateToProps(dispatch, options);
  const mapDispatchToProps = initMapDispatchToProps(dispatch, options);
  const mergeProps = initMergeProps(dispatch, options);
  if (true) {
    verifySubselectors(mapStateToProps, mapDispatchToProps, mergeProps);
  }
  return pureFinalPropsSelectorFactory(mapStateToProps, mapDispatchToProps, mergeProps, dispatch, options);
}

// src/utils/bindActionCreators.ts
function bindActionCreators(actionCreators, dispatch) {
  const boundActionCreators = {};
  for (const key in actionCreators) {
    const actionCreator = actionCreators[key];
    if (typeof actionCreator === "function") {
      boundActionCreators[key] = (...args) => dispatch(actionCreator(...args));
    }
  }
  return boundActionCreators;
}

// src/utils/isPlainObject.ts
function isPlainObject(obj) {
  if (typeof obj !== "object" || obj === null) return false;
  const proto = Object.getPrototypeOf(obj);
  if (proto === null) return true;
  let baseProto = proto;
  while (Object.getPrototypeOf(baseProto) !== null) {
    baseProto = Object.getPrototypeOf(baseProto);
  }
  return proto === baseProto;
}

// src/utils/verifyPlainObject.ts
function verifyPlainObject(value, displayName, methodName) {
  if (!isPlainObject(value)) {
    warning(
      `${methodName}() in ${displayName} must return a plain object. Instead received ${value}.`
    );
  }
}

// src/connect/wrapMapToProps.ts
function wrapMapToPropsConstant(getConstant) {
  return function initConstantSelector(dispatch) {
    const constant = getConstant(dispatch);
    function constantSelector() {
      return constant;
    }
    constantSelector.dependsOnOwnProps = false;
    return constantSelector;
  };
}
function getDependsOnOwnProps(mapToProps) {
  return mapToProps.dependsOnOwnProps ? Boolean(mapToProps.dependsOnOwnProps) : mapToProps.length !== 1;
}
function wrapMapToPropsFunc(mapToProps, methodName) {
  return function initProxySelector(dispatch, { displayName }) {
    const proxy = function mapToPropsProxy(stateOrDispatch, ownProps) {
      return proxy.dependsOnOwnProps ? proxy.mapToProps(stateOrDispatch, ownProps) : proxy.mapToProps(stateOrDispatch, void 0);
    };
    proxy.dependsOnOwnProps = true;
    proxy.mapToProps = function detectFactoryAndVerify(stateOrDispatch, ownProps) {
      proxy.mapToProps = mapToProps;
      proxy.dependsOnOwnProps = getDependsOnOwnProps(mapToProps);
      let props = proxy(stateOrDispatch, ownProps);
      if (typeof props === "function") {
        proxy.mapToProps = props;
        proxy.dependsOnOwnProps = getDependsOnOwnProps(props);
        props = proxy(stateOrDispatch, ownProps);
      }
      if (true)
        verifyPlainObject(props, displayName, methodName);
      return props;
    };
    return proxy;
  };
}

// src/connect/invalidArgFactory.ts
function createInvalidArgFactory(arg, name) {
  return (dispatch, options) => {
    throw new Error(
      `Invalid value of type ${typeof arg} for ${name} argument when connecting component ${options.wrappedComponentName}.`
    );
  };
}

// src/connect/mapDispatchToProps.ts
function mapDispatchToPropsFactory(mapDispatchToProps) {
  return mapDispatchToProps && typeof mapDispatchToProps === "object" ? wrapMapToPropsConstant(
    (dispatch) => (
      // @ts-ignore
      bindActionCreators(mapDispatchToProps, dispatch)
    )
  ) : !mapDispatchToProps ? wrapMapToPropsConstant((dispatch) => ({
    dispatch
  })) : typeof mapDispatchToProps === "function" ? (
    // @ts-ignore
    wrapMapToPropsFunc(mapDispatchToProps, "mapDispatchToProps")
  ) : createInvalidArgFactory(mapDispatchToProps, "mapDispatchToProps");
}

// src/connect/mapStateToProps.ts
function mapStateToPropsFactory(mapStateToProps) {
  return !mapStateToProps ? wrapMapToPropsConstant(() => ({})) : typeof mapStateToProps === "function" ? (
    // @ts-ignore
    wrapMapToPropsFunc(mapStateToProps, "mapStateToProps")
  ) : createInvalidArgFactory(mapStateToProps, "mapStateToProps");
}

// src/connect/mergeProps.ts
function defaultMergeProps(stateProps, dispatchProps, ownProps) {
  return { ...ownProps, ...stateProps, ...dispatchProps };
}
function wrapMergePropsFunc(mergeProps) {
  return function initMergePropsProxy(dispatch, { displayName, areMergedPropsEqual }) {
    let hasRunOnce = false;
    let mergedProps;
    return function mergePropsProxy(stateProps, dispatchProps, ownProps) {
      const nextMergedProps = mergeProps(stateProps, dispatchProps, ownProps);
      if (hasRunOnce) {
        if (!areMergedPropsEqual(nextMergedProps, mergedProps))
          mergedProps = nextMergedProps;
      } else {
        hasRunOnce = true;
        mergedProps = nextMergedProps;
        if (true)
          verifyPlainObject(mergedProps, displayName, "mergeProps");
      }
      return mergedProps;
    };
  };
}
function mergePropsFactory(mergeProps) {
  return !mergeProps ? () => defaultMergeProps : typeof mergeProps === "function" ? wrapMergePropsFunc(mergeProps) : createInvalidArgFactory(mergeProps, "mergeProps");
}

// src/utils/batch.ts
function defaultNoopBatch(callback) {
  callback();
}

// src/utils/Subscription.ts
function createListenerCollection() {
  let first = null;
  let last = null;
  return {
    clear() {
      first = null;
      last = null;
    },
    notify() {
      defaultNoopBatch(() => {
        let listener = first;
        while (listener) {
          listener.callback();
          listener = listener.next;
        }
      });
    },
    get() {
      const listeners = [];
      let listener = first;
      while (listener) {
        listeners.push(listener);
        listener = listener.next;
      }
      return listeners;
    },
    subscribe(callback) {
      let isSubscribed = true;
      const listener = last = {
        callback,
        next: null,
        prev: last
      };
      if (listener.prev) {
        listener.prev.next = listener;
      } else {
        first = listener;
      }
      return function unsubscribe() {
        if (!isSubscribed || first === null) return;
        isSubscribed = false;
        if (listener.next) {
          listener.next.prev = listener.prev;
        } else {
          last = listener.prev;
        }
        if (listener.prev) {
          listener.prev.next = listener.next;
        } else {
          first = listener.next;
        }
      };
    }
  };
}
var nullListeners = {
  notify() {
  },
  get: () => []
};
function createSubscription(store, parentSub) {
  let unsubscribe;
  let listeners = nullListeners;
  let subscriptionsAmount = 0;
  let selfSubscribed = false;
  function addNestedSub(listener) {
    trySubscribe();
    const cleanupListener = listeners.subscribe(listener);
    let removed = false;
    return () => {
      if (!removed) {
        removed = true;
        cleanupListener();
        tryUnsubscribe();
      }
    };
  }
  function notifyNestedSubs() {
    listeners.notify();
  }
  function handleChangeWrapper() {
    if (subscription.onStateChange) {
      subscription.onStateChange();
    }
  }
  function isSubscribed() {
    return selfSubscribed;
  }
  function trySubscribe() {
    subscriptionsAmount++;
    if (!unsubscribe) {
      unsubscribe = parentSub ? parentSub.addNestedSub(handleChangeWrapper) : store.subscribe(handleChangeWrapper);
      listeners = createListenerCollection();
    }
  }
  function tryUnsubscribe() {
    subscriptionsAmount--;
    if (unsubscribe && subscriptionsAmount === 0) {
      unsubscribe();
      unsubscribe = void 0;
      listeners.clear();
      listeners = nullListeners;
    }
  }
  function trySubscribeSelf() {
    if (!selfSubscribed) {
      selfSubscribed = true;
      trySubscribe();
    }
  }
  function tryUnsubscribeSelf() {
    if (selfSubscribed) {
      selfSubscribed = false;
      tryUnsubscribe();
    }
  }
  const subscription = {
    addNestedSub,
    notifyNestedSubs,
    handleChangeWrapper,
    isSubscribed,
    trySubscribe: trySubscribeSelf,
    tryUnsubscribe: tryUnsubscribeSelf,
    getListeners: () => listeners
  };
  return subscription;
}

// src/utils/useIsomorphicLayoutEffect.ts
var canUseDOM = () => !!(typeof window !== "undefined" && typeof window.document !== "undefined" && typeof window.document.createElement !== "undefined");
var isDOM = /* @__PURE__ */ canUseDOM();
var isRunningInReactNative = () => typeof navigator !== "undefined" && navigator.product === "ReactNative";
var isReactNative = /* @__PURE__ */ isRunningInReactNative();
var getUseIsomorphicLayoutEffect = () => isDOM || isReactNative ? react__WEBPACK_IMPORTED_MODULE_0__.useLayoutEffect : react__WEBPACK_IMPORTED_MODULE_0__.useEffect;
var useIsomorphicLayoutEffect = /* @__PURE__ */ getUseIsomorphicLayoutEffect();

// src/utils/shallowEqual.ts
function is(x, y) {
  if (x === y) {
    return x !== 0 || y !== 0 || 1 / x === 1 / y;
  } else {
    return x !== x && y !== y;
  }
}
function shallowEqual(objA, objB) {
  if (is(objA, objB)) return true;
  if (typeof objA !== "object" || objA === null || typeof objB !== "object" || objB === null) {
    return false;
  }
  const keysA = Object.keys(objA);
  const keysB = Object.keys(objB);
  if (keysA.length !== keysB.length) return false;
  for (let i = 0; i < keysA.length; i++) {
    if (!Object.prototype.hasOwnProperty.call(objB, keysA[i]) || !is(objA[keysA[i]], objB[keysA[i]])) {
      return false;
    }
  }
  return true;
}

// src/utils/hoistStatics.ts
var REACT_STATICS = {
  childContextTypes: true,
  contextType: true,
  contextTypes: true,
  defaultProps: true,
  displayName: true,
  getDefaultProps: true,
  getDerivedStateFromError: true,
  getDerivedStateFromProps: true,
  mixins: true,
  propTypes: true,
  type: true
};
var KNOWN_STATICS = {
  name: true,
  length: true,
  prototype: true,
  caller: true,
  callee: true,
  arguments: true,
  arity: true
};
var FORWARD_REF_STATICS = {
  $$typeof: true,
  render: true,
  defaultProps: true,
  displayName: true,
  propTypes: true
};
var MEMO_STATICS = {
  $$typeof: true,
  compare: true,
  defaultProps: true,
  displayName: true,
  propTypes: true,
  type: true
};
var TYPE_STATICS = {
  [ForwardRef]: FORWARD_REF_STATICS,
  [Memo]: MEMO_STATICS
};
function getStatics(component) {
  if (isMemo(component)) {
    return MEMO_STATICS;
  }
  return TYPE_STATICS[component["$$typeof"]] || REACT_STATICS;
}
var defineProperty = Object.defineProperty;
var getOwnPropertyNames = Object.getOwnPropertyNames;
var getOwnPropertySymbols = Object.getOwnPropertySymbols;
var getOwnPropertyDescriptor = Object.getOwnPropertyDescriptor;
var getPrototypeOf = Object.getPrototypeOf;
var objectPrototype = Object.prototype;
function hoistNonReactStatics(targetComponent, sourceComponent) {
  if (typeof sourceComponent !== "string") {
    if (objectPrototype) {
      const inheritedComponent = getPrototypeOf(sourceComponent);
      if (inheritedComponent && inheritedComponent !== objectPrototype) {
        hoistNonReactStatics(targetComponent, inheritedComponent);
      }
    }
    let keys = getOwnPropertyNames(sourceComponent);
    if (getOwnPropertySymbols) {
      keys = keys.concat(getOwnPropertySymbols(sourceComponent));
    }
    const targetStatics = getStatics(targetComponent);
    const sourceStatics = getStatics(sourceComponent);
    for (let i = 0; i < keys.length; ++i) {
      const key = keys[i];
      if (!KNOWN_STATICS[key] && !(sourceStatics && sourceStatics[key]) && !(targetStatics && targetStatics[key])) {
        const descriptor = getOwnPropertyDescriptor(sourceComponent, key);
        try {
          defineProperty(targetComponent, key, descriptor);
        } catch (e) {
        }
      }
    }
  }
  return targetComponent;
}

// src/components/Context.ts
var ContextKey = /* @__PURE__ */ Symbol.for(`react-redux-context`);
var gT = typeof globalThis !== "undefined" ? globalThis : (
  /* fall back to a per-module scope (pre-8.1 behaviour) if `globalThis` is not available */
  {}
);
function getContext() {
  if (!react__WEBPACK_IMPORTED_MODULE_0__.createContext) return {};
  const contextMap = gT[ContextKey] ??= /* @__PURE__ */ new Map();
  let realContext = contextMap.get(react__WEBPACK_IMPORTED_MODULE_0__.createContext);
  if (!realContext) {
    realContext = react__WEBPACK_IMPORTED_MODULE_0__.createContext(
      null
    );
    if (true) {
      realContext.displayName = "ReactRedux";
    }
    contextMap.set(react__WEBPACK_IMPORTED_MODULE_0__.createContext, realContext);
  }
  return realContext;
}
var ReactReduxContext = /* @__PURE__ */ getContext();

// src/components/connect.tsx
var NO_SUBSCRIPTION_ARRAY = [null, null];
var stringifyComponent = (Comp) => {
  try {
    return JSON.stringify(Comp);
  } catch (err) {
    return String(Comp);
  }
};
function useIsomorphicLayoutEffectWithArgs(effectFunc, effectArgs, dependencies) {
  useIsomorphicLayoutEffect(() => effectFunc(...effectArgs), dependencies);
}
function captureWrapperProps(lastWrapperProps, lastChildProps, renderIsScheduled, wrapperProps, childPropsFromStoreUpdate, notifyNestedSubs) {
  lastWrapperProps.current = wrapperProps;
  renderIsScheduled.current = false;
  if (childPropsFromStoreUpdate.current) {
    childPropsFromStoreUpdate.current = null;
    notifyNestedSubs();
  }
}
function subscribeUpdates(shouldHandleStateChanges, store, subscription, childPropsSelector, lastWrapperProps, lastChildProps, renderIsScheduled, isMounted, childPropsFromStoreUpdate, notifyNestedSubs, additionalSubscribeListener) {
  if (!shouldHandleStateChanges) return () => {
  };
  let didUnsubscribe = false;
  let lastThrownError = null;
  const checkForUpdates = () => {
    if (didUnsubscribe || !isMounted.current) {
      return;
    }
    const latestStoreState = store.getState();
    let newChildProps, error;
    try {
      newChildProps = childPropsSelector(
        latestStoreState,
        lastWrapperProps.current
      );
    } catch (e) {
      error = e;
      lastThrownError = e;
    }
    if (!error) {
      lastThrownError = null;
    }
    if (newChildProps === lastChildProps.current) {
      if (!renderIsScheduled.current) {
        notifyNestedSubs();
      }
    } else {
      lastChildProps.current = newChildProps;
      childPropsFromStoreUpdate.current = newChildProps;
      renderIsScheduled.current = true;
      additionalSubscribeListener();
    }
  };
  subscription.onStateChange = checkForUpdates;
  subscription.trySubscribe();
  checkForUpdates();
  const unsubscribeWrapper = () => {
    didUnsubscribe = true;
    subscription.tryUnsubscribe();
    subscription.onStateChange = null;
    if (lastThrownError) {
      throw lastThrownError;
    }
  };
  return unsubscribeWrapper;
}
function strictEqual(a, b) {
  return a === b;
}
var hasWarnedAboutDeprecatedPureOption = false;
function connect(mapStateToProps, mapDispatchToProps, mergeProps, {
  // The `pure` option has been removed, so TS doesn't like us destructuring this to check its existence.
  // @ts-ignore
  pure,
  areStatesEqual = strictEqual,
  areOwnPropsEqual = shallowEqual,
  areStatePropsEqual = shallowEqual,
  areMergedPropsEqual = shallowEqual,
  // use React's forwardRef to expose a ref of the wrapped component
  forwardRef = false,
  // the context consumer to use
  context = ReactReduxContext
} = {}) {
  if (true) {
    if (pure !== void 0 && !hasWarnedAboutDeprecatedPureOption) {
      hasWarnedAboutDeprecatedPureOption = true;
      warning(
        'The `pure` option has been removed. `connect` is now always a "pure/memoized" component'
      );
    }
  }
  const Context = context;
  const initMapStateToProps = mapStateToPropsFactory(mapStateToProps);
  const initMapDispatchToProps = mapDispatchToPropsFactory(mapDispatchToProps);
  const initMergeProps = mergePropsFactory(mergeProps);
  const shouldHandleStateChanges = Boolean(mapStateToProps);
  const wrapWithConnect = (WrappedComponent) => {
    if (true) {
      const isValid = /* @__PURE__ */ isValidElementType(WrappedComponent);
      if (!isValid)
        throw new Error(
          `You must pass a component to the function returned by connect. Instead received ${stringifyComponent(
            WrappedComponent
          )}`
        );
    }
    const wrappedComponentName = WrappedComponent.displayName || WrappedComponent.name || "Component";
    const displayName = `Connect(${wrappedComponentName})`;
    const selectorFactoryOptions = {
      shouldHandleStateChanges,
      displayName,
      wrappedComponentName,
      WrappedComponent,
      // @ts-ignore
      initMapStateToProps,
      initMapDispatchToProps,
      initMergeProps,
      areStatesEqual,
      areStatePropsEqual,
      areOwnPropsEqual,
      areMergedPropsEqual
    };
    function ConnectFunction(props) {
      const [propsContext, reactReduxForwardedRef, wrapperProps] = react__WEBPACK_IMPORTED_MODULE_0__.useMemo(() => {
        const { reactReduxForwardedRef: reactReduxForwardedRef2, ...wrapperProps2 } = props;
        return [props.context, reactReduxForwardedRef2, wrapperProps2];
      }, [props]);
      const ContextToUse = react__WEBPACK_IMPORTED_MODULE_0__.useMemo(() => {
        let ResultContext = Context;
        if (propsContext?.Consumer) {
          if (true) {
            const isValid = /* @__PURE__ */ isContextConsumer(
              // @ts-ignore
              /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement(propsContext.Consumer, null)
            );
            if (!isValid) {
              throw new Error(
                "You must pass a valid React context consumer as `props.context`"
              );
            }
            ResultContext = propsContext;
          }
        }
        return ResultContext;
      }, [propsContext, Context]);
      const contextValue = react__WEBPACK_IMPORTED_MODULE_0__.useContext(ContextToUse);
      const didStoreComeFromProps = Boolean(props.store) && Boolean(props.store.getState) && Boolean(props.store.dispatch);
      const didStoreComeFromContext = Boolean(contextValue) && Boolean(contextValue.store);
      if ( true && !didStoreComeFromProps && !didStoreComeFromContext) {
        throw new Error(
          `Could not find "store" in the context of "${displayName}". Either wrap the root component in a <Provider>, or pass a custom React context provider to <Provider> and the corresponding React context consumer to ${displayName} in connect options.`
        );
      }
      const store = didStoreComeFromProps ? props.store : contextValue.store;
      const getServerState = didStoreComeFromContext ? contextValue.getServerState : store.getState;
      const childPropsSelector = react__WEBPACK_IMPORTED_MODULE_0__.useMemo(() => {
        return finalPropsSelectorFactory(store.dispatch, selectorFactoryOptions);
      }, [store]);
      const [subscription, notifyNestedSubs] = react__WEBPACK_IMPORTED_MODULE_0__.useMemo(() => {
        if (!shouldHandleStateChanges) return NO_SUBSCRIPTION_ARRAY;
        const subscription2 = createSubscription(
          store,
          didStoreComeFromProps ? void 0 : contextValue.subscription
        );
        const notifyNestedSubs2 = subscription2.notifyNestedSubs.bind(subscription2);
        return [subscription2, notifyNestedSubs2];
      }, [store, didStoreComeFromProps, contextValue]);
      const overriddenContextValue = react__WEBPACK_IMPORTED_MODULE_0__.useMemo(() => {
        if (didStoreComeFromProps) {
          return contextValue;
        }
        return {
          ...contextValue,
          subscription
        };
      }, [didStoreComeFromProps, contextValue, subscription]);
      const lastChildProps = react__WEBPACK_IMPORTED_MODULE_0__.useRef(void 0);
      const lastWrapperProps = react__WEBPACK_IMPORTED_MODULE_0__.useRef(wrapperProps);
      const childPropsFromStoreUpdate = react__WEBPACK_IMPORTED_MODULE_0__.useRef(void 0);
      const renderIsScheduled = react__WEBPACK_IMPORTED_MODULE_0__.useRef(false);
      const isMounted = react__WEBPACK_IMPORTED_MODULE_0__.useRef(false);
      const latestSubscriptionCallbackError = react__WEBPACK_IMPORTED_MODULE_0__.useRef(
        void 0
      );
      useIsomorphicLayoutEffect(() => {
        isMounted.current = true;
        return () => {
          isMounted.current = false;
        };
      }, []);
      const actualChildPropsSelector = react__WEBPACK_IMPORTED_MODULE_0__.useMemo(() => {
        const selector = () => {
          if (childPropsFromStoreUpdate.current && wrapperProps === lastWrapperProps.current) {
            return childPropsFromStoreUpdate.current;
          }
          return childPropsSelector(store.getState(), wrapperProps);
        };
        return selector;
      }, [store, wrapperProps]);
      const subscribeForReact = react__WEBPACK_IMPORTED_MODULE_0__.useMemo(() => {
        const subscribe = (reactListener) => {
          if (!subscription) {
            return () => {
            };
          }
          return subscribeUpdates(
            shouldHandleStateChanges,
            store,
            subscription,
            // @ts-ignore
            childPropsSelector,
            lastWrapperProps,
            lastChildProps,
            renderIsScheduled,
            isMounted,
            childPropsFromStoreUpdate,
            notifyNestedSubs,
            reactListener
          );
        };
        return subscribe;
      }, [subscription]);
      useIsomorphicLayoutEffectWithArgs(captureWrapperProps, [
        lastWrapperProps,
        lastChildProps,
        renderIsScheduled,
        wrapperProps,
        childPropsFromStoreUpdate,
        notifyNestedSubs
      ]);
      let actualChildProps;
      try {
        actualChildProps = react__WEBPACK_IMPORTED_MODULE_0__.useSyncExternalStore(
          // TODO We're passing through a big wrapper that does a bunch of extra side effects besides subscribing
          subscribeForReact,
          // TODO This is incredibly hacky. We've already processed the store update and calculated new child props,
          // TODO and we're just passing that through so it triggers a re-render for us rather than relying on `uSES`.
          actualChildPropsSelector,
          getServerState ? () => childPropsSelector(getServerState(), wrapperProps) : actualChildPropsSelector
        );
      } catch (err) {
        if (latestSubscriptionCallbackError.current) {
          ;
          err.message += `
The error may be correlated with this previous error:
${latestSubscriptionCallbackError.current.stack}

`;
        }
        throw err;
      }
      useIsomorphicLayoutEffect(() => {
        latestSubscriptionCallbackError.current = void 0;
        childPropsFromStoreUpdate.current = void 0;
        lastChildProps.current = actualChildProps;
      });
      const renderedWrappedComponent = react__WEBPACK_IMPORTED_MODULE_0__.useMemo(() => {
        return (
          // @ts-ignore
          /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement(
            WrappedComponent,
            {
              ...actualChildProps,
              ref: reactReduxForwardedRef
            }
          )
        );
      }, [reactReduxForwardedRef, WrappedComponent, actualChildProps]);
      const renderedChild = react__WEBPACK_IMPORTED_MODULE_0__.useMemo(() => {
        if (shouldHandleStateChanges) {
          return /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement(ContextToUse.Provider, { value: overriddenContextValue }, renderedWrappedComponent);
        }
        return renderedWrappedComponent;
      }, [ContextToUse, renderedWrappedComponent, overriddenContextValue]);
      return renderedChild;
    }
    const _Connect = react__WEBPACK_IMPORTED_MODULE_0__.memo(ConnectFunction);
    const Connect = _Connect;
    Connect.WrappedComponent = WrappedComponent;
    Connect.displayName = ConnectFunction.displayName = displayName;
    if (forwardRef) {
      const _forwarded = react__WEBPACK_IMPORTED_MODULE_0__.forwardRef(
        function forwardConnectRef(props, ref) {
          return /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement(Connect, { ...props, reactReduxForwardedRef: ref });
        }
      );
      const forwarded = _forwarded;
      forwarded.displayName = displayName;
      forwarded.WrappedComponent = WrappedComponent;
      return /* @__PURE__ */ hoistNonReactStatics(forwarded, WrappedComponent);
    }
    return /* @__PURE__ */ hoistNonReactStatics(Connect, WrappedComponent);
  };
  return wrapWithConnect;
}
var connect_default = connect;

// src/components/Provider.tsx
function Provider(providerProps) {
  const { children, context, serverState, store } = providerProps;
  const contextValue = react__WEBPACK_IMPORTED_MODULE_0__.useMemo(() => {
    const subscription = createSubscription(store);
    const baseContextValue = {
      store,
      subscription,
      getServerState: serverState ? () => serverState : void 0
    };
    if (false) // removed by dead control flow
{} else {
      const { identityFunctionCheck = "once", stabilityCheck = "once" } = providerProps;
      return /* @__PURE__ */ Object.assign(baseContextValue, {
        stabilityCheck,
        identityFunctionCheck
      });
    }
  }, [store, serverState]);
  const previousState = react__WEBPACK_IMPORTED_MODULE_0__.useMemo(() => store.getState(), [store]);
  useIsomorphicLayoutEffect(() => {
    const { subscription } = contextValue;
    subscription.onStateChange = subscription.notifyNestedSubs;
    subscription.trySubscribe();
    if (previousState !== store.getState()) {
      subscription.notifyNestedSubs();
    }
    return () => {
      subscription.tryUnsubscribe();
      subscription.onStateChange = void 0;
    };
  }, [contextValue, previousState]);
  const Context = context || ReactReduxContext;
  return /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement(Context.Provider, { value: contextValue }, children);
}
var Provider_default = Provider;

// src/hooks/useReduxContext.ts
function createReduxContextHook(context = ReactReduxContext) {
  return function useReduxContext2() {
    const contextValue = react__WEBPACK_IMPORTED_MODULE_0__.useContext(context);
    if ( true && !contextValue) {
      throw new Error(
        "could not find react-redux context value; please ensure the component is wrapped in a <Provider>"
      );
    }
    return contextValue;
  };
}
var useReduxContext = /* @__PURE__ */ createReduxContextHook();

// src/hooks/useStore.ts
function createStoreHook(context = ReactReduxContext) {
  const useReduxContext2 = context === ReactReduxContext ? useReduxContext : (
    // @ts-ignore
    createReduxContextHook(context)
  );
  const useStore2 = () => {
    const { store } = useReduxContext2();
    return store;
  };
  Object.assign(useStore2, {
    withTypes: () => useStore2
  });
  return useStore2;
}
var useStore = /* @__PURE__ */ createStoreHook();

// src/hooks/useDispatch.ts
function createDispatchHook(context = ReactReduxContext) {
  const useStore2 = context === ReactReduxContext ? useStore : createStoreHook(context);
  const useDispatch2 = () => {
    const store = useStore2();
    return store.dispatch;
  };
  Object.assign(useDispatch2, {
    withTypes: () => useDispatch2
  });
  return useDispatch2;
}
var useDispatch = /* @__PURE__ */ createDispatchHook();

// src/hooks/useSelector.ts

var refEquality = (a, b) => a === b;
function createSelectorHook(context = ReactReduxContext) {
  const useReduxContext2 = context === ReactReduxContext ? useReduxContext : createReduxContextHook(context);
  const useSelector2 = (selector, equalityFnOrOptions = {}) => {
    const { equalityFn = refEquality } = typeof equalityFnOrOptions === "function" ? { equalityFn: equalityFnOrOptions } : equalityFnOrOptions;
    if (true) {
      if (!selector) {
        throw new Error(`You must pass a selector to useSelector`);
      }
      if (typeof selector !== "function") {
        throw new Error(`You must pass a function as a selector to useSelector`);
      }
      if (typeof equalityFn !== "function") {
        throw new Error(
          `You must pass a function as an equality function to useSelector`
        );
      }
    }
    const reduxContext = useReduxContext2();
    const { store, subscription, getServerState } = reduxContext;
    const firstRun = react__WEBPACK_IMPORTED_MODULE_0__.useRef(true);
    const wrappedSelector = react__WEBPACK_IMPORTED_MODULE_0__.useCallback(
      {
        [selector.name](state) {
          const selected = selector(state);
          if (true) {
            const { devModeChecks = {} } = typeof equalityFnOrOptions === "function" ? {} : equalityFnOrOptions;
            const { identityFunctionCheck, stabilityCheck } = reduxContext;
            const {
              identityFunctionCheck: finalIdentityFunctionCheck,
              stabilityCheck: finalStabilityCheck
            } = {
              stabilityCheck,
              identityFunctionCheck,
              ...devModeChecks
            };
            if (finalStabilityCheck === "always" || finalStabilityCheck === "once" && firstRun.current) {
              const toCompare = selector(state);
              if (!equalityFn(selected, toCompare)) {
                let stack = void 0;
                try {
                  throw new Error();
                } catch (e) {
                  ;
                  ({ stack } = e);
                }
                console.warn(
                  "Selector " + (selector.name || "unknown") + " returned a different result when called with the same parameters. This can lead to unnecessary rerenders.\nSelectors that return a new reference (such as an object or an array) should be memoized: https://redux.js.org/usage/deriving-data-selectors#optimizing-selectors-with-memoization",
                  {
                    state,
                    selected,
                    selected2: toCompare,
                    stack
                  }
                );
              }
            }
            if (finalIdentityFunctionCheck === "always" || finalIdentityFunctionCheck === "once" && firstRun.current) {
              if (selected === state) {
                let stack = void 0;
                try {
                  throw new Error();
                } catch (e) {
                  ;
                  ({ stack } = e);
                }
                console.warn(
                  "Selector " + (selector.name || "unknown") + " returned the root state when called. This can lead to unnecessary rerenders.\nSelectors that return the entire state are almost certainly a mistake, as they will cause a rerender whenever *anything* in state changes.",
                  { stack }
                );
              }
            }
            if (firstRun.current) firstRun.current = false;
          }
          return selected;
        }
      }[selector.name],
      [selector]
    );
    const selectedState = (0,use_sync_external_store_with_selector_js__WEBPACK_IMPORTED_MODULE_1__.useSyncExternalStoreWithSelector)(
      subscription.addNestedSub,
      store.getState,
      getServerState || store.getState,
      wrappedSelector,
      equalityFn
    );
    react__WEBPACK_IMPORTED_MODULE_0__.useDebugValue(selectedState);
    return selectedState;
  };
  Object.assign(useSelector2, {
    withTypes: () => useSelector2
  });
  return useSelector2;
}
var useSelector = /* @__PURE__ */ createSelectorHook();

// src/exports.ts
var batch = defaultNoopBatch;

//# sourceMappingURL=react-redux.mjs.map

/***/ },

/***/ "./node_modules/react-router/dist/development/chunk-UVKPFVEO.mjs"
/*!***********************************************************************!*\
  !*** ./node_modules/react-router/dist/development/chunk-UVKPFVEO.mjs ***!
  \***********************************************************************/
(__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) {

var react__WEBPACK_IMPORTED_MODULE_0___namespace_cache;
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   Action: () => (/* binding */ Action),
/* harmony export */   Await: () => (/* binding */ Await),
/* harmony export */   AwaitContextProvider: () => (/* binding */ AwaitContextProvider),
/* harmony export */   BrowserRouter: () => (/* binding */ BrowserRouter),
/* harmony export */   CRITICAL_CSS_DATA_ATTRIBUTE: () => (/* binding */ CRITICAL_CSS_DATA_ATTRIBUTE),
/* harmony export */   DataRouterContext: () => (/* binding */ DataRouterContext),
/* harmony export */   DataRouterStateContext: () => (/* binding */ DataRouterStateContext),
/* harmony export */   ENABLE_DEV_WARNINGS: () => (/* binding */ ENABLE_DEV_WARNINGS),
/* harmony export */   ErrorResponseImpl: () => (/* binding */ ErrorResponseImpl),
/* harmony export */   FetchersContext: () => (/* binding */ FetchersContext),
/* harmony export */   Form: () => (/* binding */ Form),
/* harmony export */   FrameworkContext: () => (/* binding */ FrameworkContext),
/* harmony export */   HashRouter: () => (/* binding */ HashRouter),
/* harmony export */   HistoryRouter: () => (/* binding */ HistoryRouter),
/* harmony export */   IDLE_BLOCKER: () => (/* binding */ IDLE_BLOCKER),
/* harmony export */   IDLE_FETCHER: () => (/* binding */ IDLE_FETCHER),
/* harmony export */   IDLE_NAVIGATION: () => (/* binding */ IDLE_NAVIGATION),
/* harmony export */   Link: () => (/* binding */ Link),
/* harmony export */   Links: () => (/* binding */ Links),
/* harmony export */   LocationContext: () => (/* binding */ LocationContext),
/* harmony export */   MemoryRouter: () => (/* binding */ MemoryRouter),
/* harmony export */   Meta: () => (/* binding */ Meta),
/* harmony export */   NO_BODY_STATUS_CODES: () => (/* binding */ NO_BODY_STATUS_CODES),
/* harmony export */   NavLink: () => (/* binding */ NavLink),
/* harmony export */   Navigate: () => (/* binding */ Navigate),
/* harmony export */   NavigationContext: () => (/* binding */ NavigationContext),
/* harmony export */   Outlet: () => (/* binding */ Outlet),
/* harmony export */   PrefetchPageLinks: () => (/* binding */ PrefetchPageLinks),
/* harmony export */   RSCRouterContext: () => (/* binding */ RSCRouterContext),
/* harmony export */   RemixErrorBoundary: () => (/* binding */ RemixErrorBoundary),
/* harmony export */   Route: () => (/* binding */ Route),
/* harmony export */   RouteContext: () => (/* binding */ RouteContext),
/* harmony export */   Router: () => (/* binding */ Router),
/* harmony export */   RouterContextProvider: () => (/* binding */ RouterContextProvider),
/* harmony export */   RouterProvider: () => (/* binding */ RouterProvider),
/* harmony export */   Routes: () => (/* binding */ Routes),
/* harmony export */   SINGLE_FETCH_REDIRECT_STATUS: () => (/* binding */ SINGLE_FETCH_REDIRECT_STATUS),
/* harmony export */   Scripts: () => (/* binding */ Scripts),
/* harmony export */   ScrollRestoration: () => (/* binding */ ScrollRestoration),
/* harmony export */   SingleFetchRedirectSymbol: () => (/* binding */ SingleFetchRedirectSymbol),
/* harmony export */   StaticRouter: () => (/* binding */ StaticRouter),
/* harmony export */   StaticRouterProvider: () => (/* binding */ StaticRouterProvider),
/* harmony export */   StreamTransfer: () => (/* binding */ StreamTransfer),
/* harmony export */   ViewTransitionContext: () => (/* binding */ ViewTransitionContext),
/* harmony export */   WithComponentProps: () => (/* binding */ WithComponentProps),
/* harmony export */   WithErrorBoundaryProps: () => (/* binding */ WithErrorBoundaryProps),
/* harmony export */   WithHydrateFallbackProps: () => (/* binding */ WithHydrateFallbackProps),
/* harmony export */   convertRoutesToDataRoutes: () => (/* binding */ convertRoutesToDataRoutes),
/* harmony export */   createBrowserHistory: () => (/* binding */ createBrowserHistory),
/* harmony export */   createBrowserRouter: () => (/* binding */ createBrowserRouter),
/* harmony export */   createClientRoutes: () => (/* binding */ createClientRoutes),
/* harmony export */   createClientRoutesWithHMRRevalidationOptOut: () => (/* binding */ createClientRoutesWithHMRRevalidationOptOut),
/* harmony export */   createContext: () => (/* binding */ createContext),
/* harmony export */   createHashHistory: () => (/* binding */ createHashHistory),
/* harmony export */   createHashRouter: () => (/* binding */ createHashRouter),
/* harmony export */   createMemoryHistory: () => (/* binding */ createMemoryHistory),
/* harmony export */   createMemoryRouter: () => (/* binding */ createMemoryRouter),
/* harmony export */   createPath: () => (/* binding */ createPath),
/* harmony export */   createRequestInit: () => (/* binding */ createRequestInit),
/* harmony export */   createRouter: () => (/* binding */ createRouter),
/* harmony export */   createRoutesFromChildren: () => (/* binding */ createRoutesFromChildren),
/* harmony export */   createRoutesFromElements: () => (/* binding */ createRoutesFromElements),
/* harmony export */   createSearchParams: () => (/* binding */ createSearchParams),
/* harmony export */   createServerRoutes: () => (/* binding */ createServerRoutes),
/* harmony export */   createStaticHandler: () => (/* binding */ createStaticHandler),
/* harmony export */   createStaticHandler2: () => (/* binding */ createStaticHandler2),
/* harmony export */   createStaticRouter: () => (/* binding */ createStaticRouter),
/* harmony export */   data: () => (/* binding */ data),
/* harmony export */   decodeRedirectErrorDigest: () => (/* binding */ decodeRedirectErrorDigest),
/* harmony export */   decodeRouteErrorResponseDigest: () => (/* binding */ decodeRouteErrorResponseDigest),
/* harmony export */   decodeViaTurboStream: () => (/* binding */ decodeViaTurboStream),
/* harmony export */   encode: () => (/* binding */ encode),
/* harmony export */   escapeHtml: () => (/* binding */ escapeHtml),
/* harmony export */   generatePath: () => (/* binding */ generatePath),
/* harmony export */   getManifestPath: () => (/* binding */ getManifestPath),
/* harmony export */   getPatchRoutesOnNavigationFunction: () => (/* binding */ getPatchRoutesOnNavigationFunction),
/* harmony export */   getSingleFetchDataStrategyImpl: () => (/* binding */ getSingleFetchDataStrategyImpl),
/* harmony export */   getStaticContextFromError: () => (/* binding */ getStaticContextFromError),
/* harmony export */   getTurboStreamSingleFetchDataStrategy: () => (/* binding */ getTurboStreamSingleFetchDataStrategy),
/* harmony export */   hydrationRouteProperties: () => (/* binding */ hydrationRouteProperties),
/* harmony export */   instrumentHandler: () => (/* binding */ instrumentHandler),
/* harmony export */   invalidProtocols: () => (/* binding */ invalidProtocols),
/* harmony export */   invariant: () => (/* binding */ invariant),
/* harmony export */   isDataWithResponseInit: () => (/* binding */ isDataWithResponseInit),
/* harmony export */   isMutationMethod: () => (/* binding */ isMutationMethod),
/* harmony export */   isRedirectResponse: () => (/* binding */ isRedirectResponse),
/* harmony export */   isRedirectStatusCode: () => (/* binding */ isRedirectStatusCode),
/* harmony export */   isResponse: () => (/* binding */ isResponse),
/* harmony export */   isRouteErrorResponse: () => (/* binding */ isRouteErrorResponse),
/* harmony export */   mapRouteProperties: () => (/* binding */ mapRouteProperties),
/* harmony export */   matchPath: () => (/* binding */ matchPath),
/* harmony export */   matchRoutes: () => (/* binding */ matchRoutes),
/* harmony export */   noActionDefinedError: () => (/* binding */ noActionDefinedError),
/* harmony export */   parsePath: () => (/* binding */ parsePath),
/* harmony export */   redirect: () => (/* binding */ redirect),
/* harmony export */   redirectDocument: () => (/* binding */ redirectDocument),
/* harmony export */   renderMatches: () => (/* binding */ renderMatches),
/* harmony export */   replace: () => (/* binding */ replace),
/* harmony export */   resolvePath: () => (/* binding */ resolvePath),
/* harmony export */   setIsHydrated: () => (/* binding */ setIsHydrated),
/* harmony export */   shouldHydrateRouteLoader: () => (/* binding */ shouldHydrateRouteLoader),
/* harmony export */   singleFetchUrl: () => (/* binding */ singleFetchUrl),
/* harmony export */   stripBasename: () => (/* binding */ stripBasename),
/* harmony export */   stripIndexParam: () => (/* binding */ stripIndexParam),
/* harmony export */   useActionData: () => (/* binding */ useActionData),
/* harmony export */   useAsyncError: () => (/* binding */ useAsyncError),
/* harmony export */   useAsyncValue: () => (/* binding */ useAsyncValue),
/* harmony export */   useBeforeUnload: () => (/* binding */ useBeforeUnload),
/* harmony export */   useBlocker: () => (/* binding */ useBlocker),
/* harmony export */   useFetcher: () => (/* binding */ useFetcher),
/* harmony export */   useFetchers: () => (/* binding */ useFetchers),
/* harmony export */   useFogOFWarDiscovery: () => (/* binding */ useFogOFWarDiscovery),
/* harmony export */   useFormAction: () => (/* binding */ useFormAction),
/* harmony export */   useHref: () => (/* binding */ useHref),
/* harmony export */   useInRouterContext: () => (/* binding */ useInRouterContext),
/* harmony export */   useLinkClickHandler: () => (/* binding */ useLinkClickHandler),
/* harmony export */   useLoaderData: () => (/* binding */ useLoaderData),
/* harmony export */   useLocation: () => (/* binding */ useLocation),
/* harmony export */   useMatch: () => (/* binding */ useMatch),
/* harmony export */   useMatches: () => (/* binding */ useMatches),
/* harmony export */   useNavigate: () => (/* binding */ useNavigate),
/* harmony export */   useNavigation: () => (/* binding */ useNavigation),
/* harmony export */   useNavigationType: () => (/* binding */ useNavigationType),
/* harmony export */   useOutlet: () => (/* binding */ useOutlet),
/* harmony export */   useOutletContext: () => (/* binding */ useOutletContext),
/* harmony export */   useParams: () => (/* binding */ useParams),
/* harmony export */   usePrompt: () => (/* binding */ usePrompt),
/* harmony export */   useResolvedPath: () => (/* binding */ useResolvedPath),
/* harmony export */   useRevalidator: () => (/* binding */ useRevalidator),
/* harmony export */   useRoute: () => (/* binding */ useRoute),
/* harmony export */   useRouteError: () => (/* binding */ useRouteError),
/* harmony export */   useRouteLoaderData: () => (/* binding */ useRouteLoaderData),
/* harmony export */   useRoutes: () => (/* binding */ useRoutes),
/* harmony export */   useScrollRestoration: () => (/* binding */ useScrollRestoration),
/* harmony export */   useSearchParams: () => (/* binding */ useSearchParams),
/* harmony export */   useSubmit: () => (/* binding */ useSubmit),
/* harmony export */   useViewTransitionState: () => (/* binding */ useViewTransitionState),
/* harmony export */   warnOnce: () => (/* binding */ warnOnce),
/* harmony export */   withComponentProps: () => (/* binding */ withComponentProps),
/* harmony export */   withErrorBoundaryProps: () => (/* binding */ withErrorBoundaryProps),
/* harmony export */   withHydrateFallbackProps: () => (/* binding */ withHydrateFallbackProps)
/* harmony export */ });
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! react */ "react");
/**
 * react-router v7.13.2
 *
 * Copyright (c) Remix Software Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE.md file in the root directory of this source tree.
 *
 * @license MIT
 */
var __typeError = (msg) => {
  throw TypeError(msg);
};
var __accessCheck = (obj, member, msg) => member.has(obj) || __typeError("Cannot " + msg);
var __privateGet = (obj, member, getter) => (__accessCheck(obj, member, "read from private field"), getter ? getter.call(obj) : member.get(obj));
var __privateAdd = (obj, member, value) => member.has(obj) ? __typeError("Cannot add the same private member more than once") : member instanceof WeakSet ? member.add(obj) : member.set(obj, value);

// lib/router/history.ts
var Action = /* @__PURE__ */ ((Action2) => {
  Action2["Pop"] = "POP";
  Action2["Push"] = "PUSH";
  Action2["Replace"] = "REPLACE";
  return Action2;
})(Action || {});
var PopStateEventType = "popstate";
function isLocation(obj) {
  return typeof obj === "object" && obj != null && "pathname" in obj && "search" in obj && "hash" in obj && "state" in obj && "key" in obj;
}
function createMemoryHistory(options = {}) {
  let { initialEntries = ["/"], initialIndex, v5Compat = false } = options;
  let entries;
  entries = initialEntries.map(
    (entry, index2) => createMemoryLocation(
      entry,
      typeof entry === "string" ? null : entry.state,
      index2 === 0 ? "default" : void 0,
      typeof entry === "string" ? void 0 : entry.unstable_mask
    )
  );
  let index = clampIndex(
    initialIndex == null ? entries.length - 1 : initialIndex
  );
  let action = "POP" /* Pop */;
  let listener = null;
  function clampIndex(n) {
    return Math.min(Math.max(n, 0), entries.length - 1);
  }
  function getCurrentLocation() {
    return entries[index];
  }
  function createMemoryLocation(to, state = null, key, unstable_mask) {
    let location = createLocation(
      entries ? getCurrentLocation().pathname : "/",
      to,
      state,
      key,
      unstable_mask
    );
    warning(
      location.pathname.charAt(0) === "/",
      `relative pathnames are not supported in memory history: ${JSON.stringify(
        to
      )}`
    );
    return location;
  }
  function createHref2(to) {
    return typeof to === "string" ? to : createPath(to);
  }
  let history = {
    get index() {
      return index;
    },
    get action() {
      return action;
    },
    get location() {
      return getCurrentLocation();
    },
    createHref: createHref2,
    createURL(to) {
      return new URL(createHref2(to), "http://localhost");
    },
    encodeLocation(to) {
      let path = typeof to === "string" ? parsePath(to) : to;
      return {
        pathname: path.pathname || "",
        search: path.search || "",
        hash: path.hash || ""
      };
    },
    push(to, state) {
      action = "PUSH" /* Push */;
      let nextLocation = isLocation(to) ? to : createMemoryLocation(to, state);
      index += 1;
      entries.splice(index, entries.length, nextLocation);
      if (v5Compat && listener) {
        listener({ action, location: nextLocation, delta: 1 });
      }
    },
    replace(to, state) {
      action = "REPLACE" /* Replace */;
      let nextLocation = isLocation(to) ? to : createMemoryLocation(to, state);
      entries[index] = nextLocation;
      if (v5Compat && listener) {
        listener({ action, location: nextLocation, delta: 0 });
      }
    },
    go(delta) {
      action = "POP" /* Pop */;
      let nextIndex = clampIndex(index + delta);
      let nextLocation = entries[nextIndex];
      index = nextIndex;
      if (listener) {
        listener({ action, location: nextLocation, delta });
      }
    },
    listen(fn) {
      listener = fn;
      return () => {
        listener = null;
      };
    }
  };
  return history;
}
function createBrowserHistory(options = {}) {
  function createBrowserLocation(window2, globalHistory) {
    let maskedLocation = globalHistory.state?.masked;
    let { pathname, search, hash } = maskedLocation || window2.location;
    return createLocation(
      "",
      { pathname, search, hash },
      // state defaults to `null` because `window.history.state` does
      globalHistory.state && globalHistory.state.usr || null,
      globalHistory.state && globalHistory.state.key || "default",
      maskedLocation ? {
        pathname: window2.location.pathname,
        search: window2.location.search,
        hash: window2.location.hash
      } : void 0
    );
  }
  function createBrowserHref(window2, to) {
    return typeof to === "string" ? to : createPath(to);
  }
  return getUrlBasedHistory(
    createBrowserLocation,
    createBrowserHref,
    null,
    options
  );
}
function createHashHistory(options = {}) {
  function createHashLocation(window2, globalHistory) {
    let {
      pathname = "/",
      search = "",
      hash = ""
    } = parsePath(window2.location.hash.substring(1));
    if (!pathname.startsWith("/") && !pathname.startsWith(".")) {
      pathname = "/" + pathname;
    }
    return createLocation(
      "",
      { pathname, search, hash },
      // state defaults to `null` because `window.history.state` does
      globalHistory.state && globalHistory.state.usr || null,
      globalHistory.state && globalHistory.state.key || "default"
    );
  }
  function createHashHref(window2, to) {
    let base = window2.document.querySelector("base");
    let href = "";
    if (base && base.getAttribute("href")) {
      let url = window2.location.href;
      let hashIndex = url.indexOf("#");
      href = hashIndex === -1 ? url : url.slice(0, hashIndex);
    }
    return href + "#" + (typeof to === "string" ? to : createPath(to));
  }
  function validateHashLocation(location, to) {
    warning(
      location.pathname.charAt(0) === "/",
      `relative pathnames are not supported in hash history.push(${JSON.stringify(
        to
      )})`
    );
  }
  return getUrlBasedHistory(
    createHashLocation,
    createHashHref,
    validateHashLocation,
    options
  );
}
function invariant(value, message) {
  if (value === false || value === null || typeof value === "undefined") {
    throw new Error(message);
  }
}
function warning(cond, message) {
  if (!cond) {
    if (typeof console !== "undefined") console.warn(message);
    try {
      throw new Error(message);
    } catch (e) {
    }
  }
}
function createKey() {
  return Math.random().toString(36).substring(2, 10);
}
function getHistoryState(location, index) {
  return {
    usr: location.state,
    key: location.key,
    idx: index,
    masked: location.unstable_mask ? {
      pathname: location.pathname,
      search: location.search,
      hash: location.hash
    } : void 0
  };
}
function createLocation(current, to, state = null, key, unstable_mask) {
  let location = {
    pathname: typeof current === "string" ? current : current.pathname,
    search: "",
    hash: "",
    ...typeof to === "string" ? parsePath(to) : to,
    state,
    // TODO: This could be cleaned up.  push/replace should probably just take
    // full Locations now and avoid the need to run through this flow at all
    // But that's a pretty big refactor to the current test suite so going to
    // keep as is for the time being and just let any incoming keys take precedence
    key: to && to.key || key || createKey(),
    unstable_mask
  };
  return location;
}
function createPath({
  pathname = "/",
  search = "",
  hash = ""
}) {
  if (search && search !== "?")
    pathname += search.charAt(0) === "?" ? search : "?" + search;
  if (hash && hash !== "#")
    pathname += hash.charAt(0) === "#" ? hash : "#" + hash;
  return pathname;
}
function parsePath(path) {
  let parsedPath = {};
  if (path) {
    let hashIndex = path.indexOf("#");
    if (hashIndex >= 0) {
      parsedPath.hash = path.substring(hashIndex);
      path = path.substring(0, hashIndex);
    }
    let searchIndex = path.indexOf("?");
    if (searchIndex >= 0) {
      parsedPath.search = path.substring(searchIndex);
      path = path.substring(0, searchIndex);
    }
    if (path) {
      parsedPath.pathname = path;
    }
  }
  return parsedPath;
}
function getUrlBasedHistory(getLocation, createHref2, validateLocation, options = {}) {
  let { window: window2 = document.defaultView, v5Compat = false } = options;
  let globalHistory = window2.history;
  let action = "POP" /* Pop */;
  let listener = null;
  let index = getIndex();
  if (index == null) {
    index = 0;
    globalHistory.replaceState({ ...globalHistory.state, idx: index }, "");
  }
  function getIndex() {
    let state = globalHistory.state || { idx: null };
    return state.idx;
  }
  function handlePop() {
    action = "POP" /* Pop */;
    let nextIndex = getIndex();
    let delta = nextIndex == null ? null : nextIndex - index;
    index = nextIndex;
    if (listener) {
      listener({ action, location: history.location, delta });
    }
  }
  function push(to, state) {
    action = "PUSH" /* Push */;
    let location = isLocation(to) ? to : createLocation(history.location, to, state);
    if (validateLocation) validateLocation(location, to);
    index = getIndex() + 1;
    let historyState = getHistoryState(location, index);
    let url = history.createHref(location.unstable_mask || location);
    try {
      globalHistory.pushState(historyState, "", url);
    } catch (error) {
      if (error instanceof DOMException && error.name === "DataCloneError") {
        throw error;
      }
      window2.location.assign(url);
    }
    if (v5Compat && listener) {
      listener({ action, location: history.location, delta: 1 });
    }
  }
  function replace2(to, state) {
    action = "REPLACE" /* Replace */;
    let location = isLocation(to) ? to : createLocation(history.location, to, state);
    if (validateLocation) validateLocation(location, to);
    index = getIndex();
    let historyState = getHistoryState(location, index);
    let url = history.createHref(location.unstable_mask || location);
    globalHistory.replaceState(historyState, "", url);
    if (v5Compat && listener) {
      listener({ action, location: history.location, delta: 0 });
    }
  }
  function createURL(to) {
    return createBrowserURLImpl(to);
  }
  let history = {
    get action() {
      return action;
    },
    get location() {
      return getLocation(window2, globalHistory);
    },
    listen(fn) {
      if (listener) {
        throw new Error("A history only accepts one active listener");
      }
      window2.addEventListener(PopStateEventType, handlePop);
      listener = fn;
      return () => {
        window2.removeEventListener(PopStateEventType, handlePop);
        listener = null;
      };
    },
    createHref(to) {
      return createHref2(window2, to);
    },
    createURL,
    encodeLocation(to) {
      let url = createURL(to);
      return {
        pathname: url.pathname,
        search: url.search,
        hash: url.hash
      };
    },
    push,
    replace: replace2,
    go(n) {
      return globalHistory.go(n);
    }
  };
  return history;
}
function createBrowserURLImpl(to, isAbsolute = false) {
  let base = "http://localhost";
  if (typeof window !== "undefined") {
    base = window.location.origin !== "null" ? window.location.origin : window.location.href;
  }
  invariant(base, "No window.location.(origin|href) available to create URL");
  let href = typeof to === "string" ? to : createPath(to);
  href = href.replace(/ $/, "%20");
  if (!isAbsolute && href.startsWith("//")) {
    href = base + href;
  }
  return new URL(href, base);
}

// lib/router/utils.ts
function createContext(defaultValue) {
  return { defaultValue };
}
var _map;
var RouterContextProvider = class {
  /**
   * Create a new `RouterContextProvider` instance
   * @param init An optional initial context map to populate the provider with
   */
  constructor(init) {
    __privateAdd(this, _map, /* @__PURE__ */ new Map());
    if (init) {
      for (let [context, value] of init) {
        this.set(context, value);
      }
    }
  }
  /**
   * Access a value from the context. If no value has been set for the context,
   * it will return the context's `defaultValue` if provided, or throw an error
   * if no `defaultValue` was set.
   * @param context The context to get the value for
   * @returns The value for the context, or the context's `defaultValue` if no
   * value was set
   */
  get(context) {
    if (__privateGet(this, _map).has(context)) {
      return __privateGet(this, _map).get(context);
    }
    if (context.defaultValue !== void 0) {
      return context.defaultValue;
    }
    throw new Error("No value found for context");
  }
  /**
   * Set a value for the context. If the context already has a value set, this
   * will overwrite it.
   *
   * @param context The context to set the value for
   * @param value The value to set for the context
   * @returns {void}
   */
  set(context, value) {
    __privateGet(this, _map).set(context, value);
  }
};
_map = new WeakMap();
var unsupportedLazyRouteObjectKeys = /* @__PURE__ */ new Set([
  "lazy",
  "caseSensitive",
  "path",
  "id",
  "index",
  "children"
]);
function isUnsupportedLazyRouteObjectKey(key) {
  return unsupportedLazyRouteObjectKeys.has(
    key
  );
}
var unsupportedLazyRouteFunctionKeys = /* @__PURE__ */ new Set([
  "lazy",
  "caseSensitive",
  "path",
  "id",
  "index",
  "middleware",
  "children"
]);
function isUnsupportedLazyRouteFunctionKey(key) {
  return unsupportedLazyRouteFunctionKeys.has(
    key
  );
}
function isIndexRoute(route) {
  return route.index === true;
}
function convertRoutesToDataRoutes(routes, mapRouteProperties2, parentPath = [], manifest = {}, allowInPlaceMutations = false) {
  return routes.map((route, index) => {
    let treePath = [...parentPath, String(index)];
    let id = typeof route.id === "string" ? route.id : treePath.join("-");
    invariant(
      route.index !== true || !route.children,
      `Cannot specify children on an index route`
    );
    invariant(
      allowInPlaceMutations || !manifest[id],
      `Found a route id collision on id "${id}".  Route id's must be globally unique within Data Router usages`
    );
    if (isIndexRoute(route)) {
      let indexRoute = {
        ...route,
        id
      };
      manifest[id] = mergeRouteUpdates(
        indexRoute,
        mapRouteProperties2(indexRoute)
      );
      return indexRoute;
    } else {
      let pathOrLayoutRoute = {
        ...route,
        id,
        children: void 0
      };
      manifest[id] = mergeRouteUpdates(
        pathOrLayoutRoute,
        mapRouteProperties2(pathOrLayoutRoute)
      );
      if (route.children) {
        pathOrLayoutRoute.children = convertRoutesToDataRoutes(
          route.children,
          mapRouteProperties2,
          treePath,
          manifest,
          allowInPlaceMutations
        );
      }
      return pathOrLayoutRoute;
    }
  });
}
function mergeRouteUpdates(route, updates) {
  return Object.assign(route, {
    ...updates,
    ...typeof updates.lazy === "object" && updates.lazy != null ? {
      lazy: {
        ...route.lazy,
        ...updates.lazy
      }
    } : {}
  });
}
function matchRoutes(routes, locationArg, basename = "/") {
  return matchRoutesImpl(routes, locationArg, basename, false);
}
function matchRoutesImpl(routes, locationArg, basename, allowPartial) {
  let location = typeof locationArg === "string" ? parsePath(locationArg) : locationArg;
  let pathname = stripBasename(location.pathname || "/", basename);
  if (pathname == null) {
    return null;
  }
  let branches = flattenRoutes(routes);
  rankRouteBranches(branches);
  let matches = null;
  for (let i = 0; matches == null && i < branches.length; ++i) {
    let decoded = decodePath(pathname);
    matches = matchRouteBranch(
      branches[i],
      decoded,
      allowPartial
    );
  }
  return matches;
}
function convertRouteMatchToUiMatch(match, loaderData) {
  let { route, pathname, params } = match;
  return {
    id: route.id,
    pathname,
    params,
    data: loaderData[route.id],
    loaderData: loaderData[route.id],
    handle: route.handle
  };
}
function flattenRoutes(routes, branches = [], parentsMeta = [], parentPath = "", _hasParentOptionalSegments = false) {
  let flattenRoute = (route, index, hasParentOptionalSegments = _hasParentOptionalSegments, relativePath) => {
    let meta = {
      relativePath: relativePath === void 0 ? route.path || "" : relativePath,
      caseSensitive: route.caseSensitive === true,
      childrenIndex: index,
      route
    };
    if (meta.relativePath.startsWith("/")) {
      if (!meta.relativePath.startsWith(parentPath) && hasParentOptionalSegments) {
        return;
      }
      invariant(
        meta.relativePath.startsWith(parentPath),
        `Absolute route path "${meta.relativePath}" nested under path "${parentPath}" is not valid. An absolute child route path must start with the combined path of all its parent routes.`
      );
      meta.relativePath = meta.relativePath.slice(parentPath.length);
    }
    let path = joinPaths([parentPath, meta.relativePath]);
    let routesMeta = parentsMeta.concat(meta);
    if (route.children && route.children.length > 0) {
      invariant(
        // Our types know better, but runtime JS may not!
        // @ts-expect-error
        route.index !== true,
        `Index routes must not have child routes. Please remove all child routes from route path "${path}".`
      );
      flattenRoutes(
        route.children,
        branches,
        routesMeta,
        path,
        hasParentOptionalSegments
      );
    }
    if (route.path == null && !route.index) {
      return;
    }
    branches.push({
      path,
      score: computeScore(path, route.index),
      routesMeta
    });
  };
  routes.forEach((route, index) => {
    if (route.path === "" || !route.path?.includes("?")) {
      flattenRoute(route, index);
    } else {
      for (let exploded of explodeOptionalSegments(route.path)) {
        flattenRoute(route, index, true, exploded);
      }
    }
  });
  return branches;
}
function explodeOptionalSegments(path) {
  let segments = path.split("/");
  if (segments.length === 0) return [];
  let [first, ...rest] = segments;
  let isOptional = first.endsWith("?");
  let required = first.replace(/\?$/, "");
  if (rest.length === 0) {
    return isOptional ? [required, ""] : [required];
  }
  let restExploded = explodeOptionalSegments(rest.join("/"));
  let result = [];
  result.push(
    ...restExploded.map(
      (subpath) => subpath === "" ? required : [required, subpath].join("/")
    )
  );
  if (isOptional) {
    result.push(...restExploded);
  }
  return result.map(
    (exploded) => path.startsWith("/") && exploded === "" ? "/" : exploded
  );
}
function rankRouteBranches(branches) {
  branches.sort(
    (a, b) => a.score !== b.score ? b.score - a.score : compareIndexes(
      a.routesMeta.map((meta) => meta.childrenIndex),
      b.routesMeta.map((meta) => meta.childrenIndex)
    )
  );
}
var paramRe = /^:[\w-]+$/;
var dynamicSegmentValue = 3;
var indexRouteValue = 2;
var emptySegmentValue = 1;
var staticSegmentValue = 10;
var splatPenalty = -2;
var isSplat = (s) => s === "*";
function computeScore(path, index) {
  let segments = path.split("/");
  let initialScore = segments.length;
  if (segments.some(isSplat)) {
    initialScore += splatPenalty;
  }
  if (index) {
    initialScore += indexRouteValue;
  }
  return segments.filter((s) => !isSplat(s)).reduce(
    (score, segment) => score + (paramRe.test(segment) ? dynamicSegmentValue : segment === "" ? emptySegmentValue : staticSegmentValue),
    initialScore
  );
}
function compareIndexes(a, b) {
  let siblings = a.length === b.length && a.slice(0, -1).every((n, i) => n === b[i]);
  return siblings ? (
    // If two routes are siblings, we should try to match the earlier sibling
    // first. This allows people to have fine-grained control over the matching
    // behavior by simply putting routes with identical paths in the order they
    // want them tried.
    a[a.length - 1] - b[b.length - 1]
  ) : (
    // Otherwise, it doesn't really make sense to rank non-siblings by index,
    // so they sort equally.
    0
  );
}
function matchRouteBranch(branch, pathname, allowPartial = false) {
  let { routesMeta } = branch;
  let matchedParams = {};
  let matchedPathname = "/";
  let matches = [];
  for (let i = 0; i < routesMeta.length; ++i) {
    let meta = routesMeta[i];
    let end = i === routesMeta.length - 1;
    let remainingPathname = matchedPathname === "/" ? pathname : pathname.slice(matchedPathname.length) || "/";
    let match = matchPath(
      { path: meta.relativePath, caseSensitive: meta.caseSensitive, end },
      remainingPathname
    );
    let route = meta.route;
    if (!match && end && allowPartial && !routesMeta[routesMeta.length - 1].route.index) {
      match = matchPath(
        {
          path: meta.relativePath,
          caseSensitive: meta.caseSensitive,
          end: false
        },
        remainingPathname
      );
    }
    if (!match) {
      return null;
    }
    Object.assign(matchedParams, match.params);
    matches.push({
      // TODO: Can this as be avoided?
      params: matchedParams,
      pathname: joinPaths([matchedPathname, match.pathname]),
      pathnameBase: normalizePathname(
        joinPaths([matchedPathname, match.pathnameBase])
      ),
      route
    });
    if (match.pathnameBase !== "/") {
      matchedPathname = joinPaths([matchedPathname, match.pathnameBase]);
    }
  }
  return matches;
}
function generatePath(originalPath, params = {}) {
  let path = originalPath;
  if (path.endsWith("*") && path !== "*" && !path.endsWith("/*")) {
    warning(
      false,
      `Route path "${path}" will be treated as if it were "${path.replace(/\*$/, "/*")}" because the \`*\` character must always follow a \`/\` in the pattern. To get rid of this warning, please change the route path to "${path.replace(/\*$/, "/*")}".`
    );
    path = path.replace(/\*$/, "/*");
  }
  const prefix = path.startsWith("/") ? "/" : "";
  const stringify2 = (p) => p == null ? "" : typeof p === "string" ? p : String(p);
  const segments = path.split(/\/+/).map((segment, index, array) => {
    const isLastSegment = index === array.length - 1;
    if (isLastSegment && segment === "*") {
      const star = "*";
      return stringify2(params[star]);
    }
    const keyMatch = segment.match(/^:([\w-]+)(\??)(.*)/);
    if (keyMatch) {
      const [, key, optional, suffix] = keyMatch;
      let param = params[key];
      invariant(optional === "?" || param != null, `Missing ":${key}" param`);
      return encodeURIComponent(stringify2(param)) + suffix;
    }
    return segment.replace(/\?$/g, "");
  }).filter((segment) => !!segment);
  return prefix + segments.join("/");
}
function matchPath(pattern, pathname) {
  if (typeof pattern === "string") {
    pattern = { path: pattern, caseSensitive: false, end: true };
  }
  let [matcher, compiledParams] = compilePath(
    pattern.path,
    pattern.caseSensitive,
    pattern.end
  );
  let match = pathname.match(matcher);
  if (!match) return null;
  let matchedPathname = match[0];
  let pathnameBase = matchedPathname.replace(/(.)\/+$/, "$1");
  let captureGroups = match.slice(1);
  let params = compiledParams.reduce(
    (memo2, { paramName, isOptional }, index) => {
      if (paramName === "*") {
        let splatValue = captureGroups[index] || "";
        pathnameBase = matchedPathname.slice(0, matchedPathname.length - splatValue.length).replace(/(.)\/+$/, "$1");
      }
      const value = captureGroups[index];
      if (isOptional && !value) {
        memo2[paramName] = void 0;
      } else {
        memo2[paramName] = (value || "").replace(/%2F/g, "/");
      }
      return memo2;
    },
    {}
  );
  return {
    params,
    pathname: matchedPathname,
    pathnameBase,
    pattern
  };
}
function compilePath(path, caseSensitive = false, end = true) {
  warning(
    path === "*" || !path.endsWith("*") || path.endsWith("/*"),
    `Route path "${path}" will be treated as if it were "${path.replace(/\*$/, "/*")}" because the \`*\` character must always follow a \`/\` in the pattern. To get rid of this warning, please change the route path to "${path.replace(/\*$/, "/*")}".`
  );
  let params = [];
  let regexpSource = "^" + path.replace(/\/*\*?$/, "").replace(/^\/*/, "/").replace(/[\\.*+^${}|()[\]]/g, "\\$&").replace(
    /\/:([\w-]+)(\?)?/g,
    (match, paramName, isOptional, index, str) => {
      params.push({ paramName, isOptional: isOptional != null });
      if (isOptional) {
        let nextChar = str.charAt(index + match.length);
        if (nextChar && nextChar !== "/") {
          return "/([^\\/]*)";
        }
        return "(?:/([^\\/]*))?";
      }
      return "/([^\\/]+)";
    }
  ).replace(/\/([\w-]+)\?(\/|$)/g, "(/$1)?$2");
  if (path.endsWith("*")) {
    params.push({ paramName: "*" });
    regexpSource += path === "*" || path === "/*" ? "(.*)$" : "(?:\\/(.+)|\\/*)$";
  } else if (end) {
    regexpSource += "\\/*$";
  } else if (path !== "" && path !== "/") {
    regexpSource += "(?:(?=\\/|$))";
  } else {
  }
  let matcher = new RegExp(regexpSource, caseSensitive ? void 0 : "i");
  return [matcher, params];
}
function decodePath(value) {
  try {
    return value.split("/").map((v) => decodeURIComponent(v).replace(/\//g, "%2F")).join("/");
  } catch (error) {
    warning(
      false,
      `The URL path "${value}" could not be decoded because it is a malformed URL segment. This is probably due to a bad percent encoding (${error}).`
    );
    return value;
  }
}
function stripBasename(pathname, basename) {
  if (basename === "/") return pathname;
  if (!pathname.toLowerCase().startsWith(basename.toLowerCase())) {
    return null;
  }
  let startIndex = basename.endsWith("/") ? basename.length - 1 : basename.length;
  let nextChar = pathname.charAt(startIndex);
  if (nextChar && nextChar !== "/") {
    return null;
  }
  return pathname.slice(startIndex) || "/";
}
function prependBasename({
  basename,
  pathname
}) {
  return pathname === "/" ? basename : joinPaths([basename, pathname]);
}
var ABSOLUTE_URL_REGEX = /^(?:[a-z][a-z0-9+.-]*:|\/\/)/i;
var isAbsoluteUrl = (url) => ABSOLUTE_URL_REGEX.test(url);
function resolvePath(to, fromPathname = "/") {
  let {
    pathname: toPathname,
    search = "",
    hash = ""
  } = typeof to === "string" ? parsePath(to) : to;
  let pathname;
  if (toPathname) {
    toPathname = toPathname.replace(/\/\/+/g, "/");
    if (toPathname.startsWith("/")) {
      pathname = resolvePathname(toPathname.substring(1), "/");
    } else {
      pathname = resolvePathname(toPathname, fromPathname);
    }
  } else {
    pathname = fromPathname;
  }
  return {
    pathname,
    search: normalizeSearch(search),
    hash: normalizeHash(hash)
  };
}
function resolvePathname(relativePath, fromPathname) {
  let segments = fromPathname.replace(/\/+$/, "").split("/");
  let relativeSegments = relativePath.split("/");
  relativeSegments.forEach((segment) => {
    if (segment === "..") {
      if (segments.length > 1) segments.pop();
    } else if (segment !== ".") {
      segments.push(segment);
    }
  });
  return segments.length > 1 ? segments.join("/") : "/";
}
function getInvalidPathError(char, field, dest, path) {
  return `Cannot include a '${char}' character in a manually specified \`to.${field}\` field [${JSON.stringify(
    path
  )}].  Please separate it out to the \`to.${dest}\` field. Alternatively you may provide the full path as a string in <Link to="..."> and the router will parse it for you.`;
}
function getPathContributingMatches(matches) {
  return matches.filter(
    (match, index) => index === 0 || match.route.path && match.route.path.length > 0
  );
}
function getResolveToMatches(matches) {
  let pathMatches = getPathContributingMatches(matches);
  return pathMatches.map(
    (match, idx) => idx === pathMatches.length - 1 ? match.pathname : match.pathnameBase
  );
}
function resolveTo(toArg, routePathnames, locationPathname, isPathRelative = false) {
  let to;
  if (typeof toArg === "string") {
    to = parsePath(toArg);
  } else {
    to = { ...toArg };
    invariant(
      !to.pathname || !to.pathname.includes("?"),
      getInvalidPathError("?", "pathname", "search", to)
    );
    invariant(
      !to.pathname || !to.pathname.includes("#"),
      getInvalidPathError("#", "pathname", "hash", to)
    );
    invariant(
      !to.search || !to.search.includes("#"),
      getInvalidPathError("#", "search", "hash", to)
    );
  }
  let isEmptyPath = toArg === "" || to.pathname === "";
  let toPathname = isEmptyPath ? "/" : to.pathname;
  let from;
  if (toPathname == null) {
    from = locationPathname;
  } else {
    let routePathnameIndex = routePathnames.length - 1;
    if (!isPathRelative && toPathname.startsWith("..")) {
      let toSegments = toPathname.split("/");
      while (toSegments[0] === "..") {
        toSegments.shift();
        routePathnameIndex -= 1;
      }
      to.pathname = toSegments.join("/");
    }
    from = routePathnameIndex >= 0 ? routePathnames[routePathnameIndex] : "/";
  }
  let path = resolvePath(to, from);
  let hasExplicitTrailingSlash = toPathname && toPathname !== "/" && toPathname.endsWith("/");
  let hasCurrentTrailingSlash = (isEmptyPath || toPathname === ".") && locationPathname.endsWith("/");
  if (!path.pathname.endsWith("/") && (hasExplicitTrailingSlash || hasCurrentTrailingSlash)) {
    path.pathname += "/";
  }
  return path;
}
var joinPaths = (paths) => paths.join("/").replace(/\/\/+/g, "/");
var normalizePathname = (pathname) => pathname.replace(/\/+$/, "").replace(/^\/*/, "/");
var normalizeSearch = (search) => !search || search === "?" ? "" : search.startsWith("?") ? search : "?" + search;
var normalizeHash = (hash) => !hash || hash === "#" ? "" : hash.startsWith("#") ? hash : "#" + hash;
var DataWithResponseInit = class {
  constructor(data2, init) {
    this.type = "DataWithResponseInit";
    this.data = data2;
    this.init = init || null;
  }
};
function data(data2, init) {
  return new DataWithResponseInit(
    data2,
    typeof init === "number" ? { status: init } : init
  );
}
var redirect = (url, init = 302) => {
  let responseInit = init;
  if (typeof responseInit === "number") {
    responseInit = { status: responseInit };
  } else if (typeof responseInit.status === "undefined") {
    responseInit.status = 302;
  }
  let headers = new Headers(responseInit.headers);
  headers.set("Location", url);
  return new Response(null, { ...responseInit, headers });
};
var redirectDocument = (url, init) => {
  let response = redirect(url, init);
  response.headers.set("X-Remix-Reload-Document", "true");
  return response;
};
var replace = (url, init) => {
  let response = redirect(url, init);
  response.headers.set("X-Remix-Replace", "true");
  return response;
};
var ErrorResponseImpl = class {
  constructor(status, statusText, data2, internal = false) {
    this.status = status;
    this.statusText = statusText || "";
    this.internal = internal;
    if (data2 instanceof Error) {
      this.data = data2.toString();
      this.error = data2;
    } else {
      this.data = data2;
    }
  }
};
function isRouteErrorResponse(error) {
  return error != null && typeof error.status === "number" && typeof error.statusText === "string" && typeof error.internal === "boolean" && "data" in error;
}
function getRoutePattern(matches) {
  return matches.map((m) => m.route.path).filter(Boolean).join("/").replace(/\/\/*/g, "/") || "/";
}
var isBrowser = typeof window !== "undefined" && typeof window.document !== "undefined" && typeof window.document.createElement !== "undefined";
function parseToInfo(_to, basename) {
  let to = _to;
  if (typeof to !== "string" || !ABSOLUTE_URL_REGEX.test(to)) {
    return {
      absoluteURL: void 0,
      isExternal: false,
      to
    };
  }
  let absoluteURL = to;
  let isExternal = false;
  if (isBrowser) {
    try {
      let currentUrl = new URL(window.location.href);
      let targetUrl = to.startsWith("//") ? new URL(currentUrl.protocol + to) : new URL(to);
      let path = stripBasename(targetUrl.pathname, basename);
      if (targetUrl.origin === currentUrl.origin && path != null) {
        to = path + targetUrl.search + targetUrl.hash;
      } else {
        isExternal = true;
      }
    } catch (e) {
      warning(
        false,
        `<Link to="${to}"> contains an invalid URL which will probably break when clicked - please update to a valid URL path.`
      );
    }
  }
  return {
    absoluteURL,
    isExternal,
    to
  };
}

// lib/router/instrumentation.ts
var UninstrumentedSymbol = Symbol("Uninstrumented");
function getRouteInstrumentationUpdates(fns, route) {
  let aggregated = {
    lazy: [],
    "lazy.loader": [],
    "lazy.action": [],
    "lazy.middleware": [],
    middleware: [],
    loader: [],
    action: []
  };
  fns.forEach(
    (fn) => fn({
      id: route.id,
      index: route.index,
      path: route.path,
      instrument(i) {
        let keys = Object.keys(aggregated);
        for (let key of keys) {
          if (i[key]) {
            aggregated[key].push(i[key]);
          }
        }
      }
    })
  );
  let updates = {};
  if (typeof route.lazy === "function" && aggregated.lazy.length > 0) {
    let instrumented = wrapImpl(aggregated.lazy, route.lazy, () => void 0);
    if (instrumented) {
      updates.lazy = instrumented;
    }
  }
  if (typeof route.lazy === "object") {
    let lazyObject = route.lazy;
    ["middleware", "loader", "action"].forEach((key) => {
      let lazyFn = lazyObject[key];
      let instrumentations = aggregated[`lazy.${key}`];
      if (typeof lazyFn === "function" && instrumentations.length > 0) {
        let instrumented = wrapImpl(instrumentations, lazyFn, () => void 0);
        if (instrumented) {
          updates.lazy = Object.assign(updates.lazy || {}, {
            [key]: instrumented
          });
        }
      }
    });
  }
  ["loader", "action"].forEach((key) => {
    let handler = route[key];
    if (typeof handler === "function" && aggregated[key].length > 0) {
      let original = handler[UninstrumentedSymbol] ?? handler;
      let instrumented = wrapImpl(
        aggregated[key],
        original,
        (...args) => getHandlerInfo(args[0])
      );
      if (instrumented) {
        if (key === "loader" && original.hydrate === true) {
          instrumented.hydrate = true;
        }
        instrumented[UninstrumentedSymbol] = original;
        updates[key] = instrumented;
      }
    }
  });
  if (route.middleware && route.middleware.length > 0 && aggregated.middleware.length > 0) {
    updates.middleware = route.middleware.map((middleware) => {
      let original = middleware[UninstrumentedSymbol] ?? middleware;
      let instrumented = wrapImpl(
        aggregated.middleware,
        original,
        (...args) => getHandlerInfo(args[0])
      );
      if (instrumented) {
        instrumented[UninstrumentedSymbol] = original;
        return instrumented;
      }
      return middleware;
    });
  }
  return updates;
}
function instrumentClientSideRouter(router, fns) {
  let aggregated = {
    navigate: [],
    fetch: []
  };
  fns.forEach(
    (fn) => fn({
      instrument(i) {
        let keys = Object.keys(i);
        for (let key of keys) {
          if (i[key]) {
            aggregated[key].push(i[key]);
          }
        }
      }
    })
  );
  if (aggregated.navigate.length > 0) {
    let navigate = router.navigate[UninstrumentedSymbol] ?? router.navigate;
    let instrumentedNavigate = wrapImpl(
      aggregated.navigate,
      navigate,
      (...args) => {
        let [to, opts] = args;
        return {
          to: typeof to === "number" || typeof to === "string" ? to : to ? createPath(to) : ".",
          ...getRouterInfo(router, opts ?? {})
        };
      }
    );
    if (instrumentedNavigate) {
      instrumentedNavigate[UninstrumentedSymbol] = navigate;
      router.navigate = instrumentedNavigate;
    }
  }
  if (aggregated.fetch.length > 0) {
    let fetch2 = router.fetch[UninstrumentedSymbol] ?? router.fetch;
    let instrumentedFetch = wrapImpl(aggregated.fetch, fetch2, (...args) => {
      let [key, , href, opts] = args;
      return {
        href: href ?? ".",
        fetcherKey: key,
        ...getRouterInfo(router, opts ?? {})
      };
    });
    if (instrumentedFetch) {
      instrumentedFetch[UninstrumentedSymbol] = fetch2;
      router.fetch = instrumentedFetch;
    }
  }
  return router;
}
function instrumentHandler(handler, fns) {
  let aggregated = {
    request: []
  };
  fns.forEach(
    (fn) => fn({
      instrument(i) {
        let keys = Object.keys(i);
        for (let key of keys) {
          if (i[key]) {
            aggregated[key].push(i[key]);
          }
        }
      }
    })
  );
  let instrumentedHandler = handler;
  if (aggregated.request.length > 0) {
    instrumentedHandler = wrapImpl(aggregated.request, handler, (...args) => {
      let [request, context] = args;
      return {
        request: getReadonlyRequest(request),
        context: context != null ? getReadonlyContext(context) : context
      };
    });
  }
  return instrumentedHandler;
}
function wrapImpl(impls, handler, getInfo) {
  if (impls.length === 0) {
    return null;
  }
  return async (...args) => {
    let result = await recurseRight(
      impls,
      getInfo(...args),
      () => handler(...args),
      impls.length - 1
    );
    if (result.type === "error") {
      throw result.value;
    }
    return result.value;
  };
}
async function recurseRight(impls, info, handler, index) {
  let impl = impls[index];
  let result;
  if (!impl) {
    try {
      let value = await handler();
      result = { type: "success", value };
    } catch (e) {
      result = { type: "error", value: e };
    }
  } else {
    let handlerPromise = void 0;
    let callHandler = async () => {
      if (handlerPromise) {
        console.error("You cannot call instrumented handlers more than once");
      } else {
        handlerPromise = recurseRight(impls, info, handler, index - 1);
      }
      result = await handlerPromise;
      invariant(result, "Expected a result");
      if (result.type === "error" && result.value instanceof Error) {
        return { status: "error", error: result.value };
      }
      return { status: "success", error: void 0 };
    };
    try {
      await impl(callHandler, info);
    } catch (e) {
      console.error("An instrumentation function threw an error:", e);
    }
    if (!handlerPromise) {
      await callHandler();
    }
    await handlerPromise;
  }
  if (result) {
    return result;
  }
  return {
    type: "error",
    value: new Error("No result assigned in instrumentation chain.")
  };
}
function getHandlerInfo(args) {
  let { request, context, params, unstable_pattern } = args;
  return {
    request: getReadonlyRequest(request),
    params: { ...params },
    unstable_pattern,
    context: getReadonlyContext(context)
  };
}
function getRouterInfo(router, opts) {
  return {
    currentUrl: createPath(router.state.location),
    ..."formMethod" in opts ? { formMethod: opts.formMethod } : {},
    ..."formEncType" in opts ? { formEncType: opts.formEncType } : {},
    ..."formData" in opts ? { formData: opts.formData } : {},
    ..."body" in opts ? { body: opts.body } : {}
  };
}
function getReadonlyRequest(request) {
  return {
    method: request.method,
    url: request.url,
    headers: {
      get: (...args) => request.headers.get(...args)
    }
  };
}
function getReadonlyContext(context) {
  if (isPlainObject(context)) {
    let frozen = { ...context };
    Object.freeze(frozen);
    return frozen;
  } else {
    return {
      get: (ctx) => context.get(ctx)
    };
  }
}
var objectProtoNames = Object.getOwnPropertyNames(Object.prototype).sort().join("\0");
function isPlainObject(thing) {
  if (thing === null || typeof thing !== "object") {
    return false;
  }
  const proto = Object.getPrototypeOf(thing);
  return proto === Object.prototype || proto === null || Object.getOwnPropertyNames(proto).sort().join("\0") === objectProtoNames;
}

// lib/router/router.ts
var validMutationMethodsArr = [
  "POST",
  "PUT",
  "PATCH",
  "DELETE"
];
var validMutationMethods = new Set(
  validMutationMethodsArr
);
var validRequestMethodsArr = [
  "GET",
  ...validMutationMethodsArr
];
var validRequestMethods = new Set(validRequestMethodsArr);
var redirectStatusCodes = /* @__PURE__ */ new Set([301, 302, 303, 307, 308]);
var redirectPreserveMethodStatusCodes = /* @__PURE__ */ new Set([307, 308]);
var IDLE_NAVIGATION = {
  state: "idle",
  location: void 0,
  formMethod: void 0,
  formAction: void 0,
  formEncType: void 0,
  formData: void 0,
  json: void 0,
  text: void 0
};
var IDLE_FETCHER = {
  state: "idle",
  data: void 0,
  formMethod: void 0,
  formAction: void 0,
  formEncType: void 0,
  formData: void 0,
  json: void 0,
  text: void 0
};
var IDLE_BLOCKER = {
  state: "unblocked",
  proceed: void 0,
  reset: void 0,
  location: void 0
};
var defaultMapRouteProperties = (route) => ({
  hasErrorBoundary: Boolean(route.hasErrorBoundary)
});
var TRANSITIONS_STORAGE_KEY = "remix-router-transitions";
var ResetLoaderDataSymbol = Symbol("ResetLoaderData");
function createRouter(init) {
  const routerWindow = init.window ? init.window : typeof window !== "undefined" ? window : void 0;
  const isBrowser3 = typeof routerWindow !== "undefined" && typeof routerWindow.document !== "undefined" && typeof routerWindow.document.createElement !== "undefined";
  invariant(
    init.routes.length > 0,
    "You must provide a non-empty routes array to createRouter"
  );
  let hydrationRouteProperties2 = init.hydrationRouteProperties || [];
  let _mapRouteProperties = init.mapRouteProperties || defaultMapRouteProperties;
  let mapRouteProperties2 = _mapRouteProperties;
  if (init.unstable_instrumentations) {
    let instrumentations = init.unstable_instrumentations;
    mapRouteProperties2 = (route) => {
      return {
        ..._mapRouteProperties(route),
        ...getRouteInstrumentationUpdates(
          instrumentations.map((i) => i.route).filter(Boolean),
          route
        )
      };
    };
  }
  let manifest = {};
  let dataRoutes = convertRoutesToDataRoutes(
    init.routes,
    mapRouteProperties2,
    void 0,
    manifest
  );
  let inFlightDataRoutes;
  let basename = init.basename || "/";
  if (!basename.startsWith("/")) {
    basename = `/${basename}`;
  }
  let dataStrategyImpl = init.dataStrategy || defaultDataStrategyWithMiddleware;
  let future = {
    unstable_passThroughRequests: false,
    ...init.future
  };
  let unlistenHistory = null;
  let subscribers = /* @__PURE__ */ new Set();
  let savedScrollPositions2 = null;
  let getScrollRestorationKey2 = null;
  let getScrollPosition = null;
  let initialScrollRestored = init.hydrationData != null;
  let initialMatches = matchRoutes(dataRoutes, init.history.location, basename);
  let initialMatchesIsFOW = false;
  let initialErrors = null;
  let initialized;
  let renderFallback;
  if (initialMatches == null && !init.patchRoutesOnNavigation) {
    let error = getInternalRouterError(404, {
      pathname: init.history.location.pathname
    });
    let { matches, route } = getShortCircuitMatches(dataRoutes);
    initialized = true;
    renderFallback = !initialized;
    initialMatches = matches;
    initialErrors = { [route.id]: error };
  } else {
    if (initialMatches && !init.hydrationData) {
      let fogOfWar = checkFogOfWar(
        initialMatches,
        dataRoutes,
        init.history.location.pathname
      );
      if (fogOfWar.active) {
        initialMatches = null;
      }
    }
    if (!initialMatches) {
      initialized = false;
      renderFallback = !initialized;
      initialMatches = [];
      let fogOfWar = checkFogOfWar(
        null,
        dataRoutes,
        init.history.location.pathname
      );
      if (fogOfWar.active && fogOfWar.matches) {
        initialMatchesIsFOW = true;
        initialMatches = fogOfWar.matches;
      }
    } else if (initialMatches.some((m) => m.route.lazy)) {
      initialized = false;
      renderFallback = !initialized;
    } else if (!initialMatches.some((m) => routeHasLoaderOrMiddleware(m.route))) {
      initialized = true;
      renderFallback = !initialized;
    } else {
      let loaderData = init.hydrationData ? init.hydrationData.loaderData : null;
      let errors = init.hydrationData ? init.hydrationData.errors : null;
      let relevantMatches = initialMatches;
      if (errors) {
        let idx = initialMatches.findIndex(
          (m) => errors[m.route.id] !== void 0
        );
        relevantMatches = relevantMatches.slice(0, idx + 1);
      }
      renderFallback = false;
      initialized = true;
      relevantMatches.forEach((m) => {
        let status = getRouteHydrationStatus(m.route, loaderData, errors);
        renderFallback = renderFallback || status.renderFallback;
        initialized = initialized && !status.shouldLoad;
      });
    }
  }
  let router;
  let state = {
    historyAction: init.history.action,
    location: init.history.location,
    matches: initialMatches,
    initialized,
    renderFallback,
    navigation: IDLE_NAVIGATION,
    // Don't restore on initial updateState() if we were SSR'd
    restoreScrollPosition: init.hydrationData != null ? false : null,
    preventScrollReset: false,
    revalidation: "idle",
    loaderData: init.hydrationData && init.hydrationData.loaderData || {},
    actionData: init.hydrationData && init.hydrationData.actionData || null,
    errors: init.hydrationData && init.hydrationData.errors || initialErrors,
    fetchers: /* @__PURE__ */ new Map(),
    blockers: /* @__PURE__ */ new Map()
  };
  let pendingAction = "POP" /* Pop */;
  let pendingPopstateNavigationDfd = null;
  let pendingPreventScrollReset = false;
  let pendingNavigationController;
  let pendingViewTransitionEnabled = false;
  let appliedViewTransitions = /* @__PURE__ */ new Map();
  let removePageHideEventListener = null;
  let isUninterruptedRevalidation = false;
  let isRevalidationRequired = false;
  let cancelledFetcherLoads = /* @__PURE__ */ new Set();
  let fetchControllers = /* @__PURE__ */ new Map();
  let incrementingLoadId = 0;
  let pendingNavigationLoadId = -1;
  let fetchReloadIds = /* @__PURE__ */ new Map();
  let fetchRedirectIds = /* @__PURE__ */ new Set();
  let fetchLoadMatches = /* @__PURE__ */ new Map();
  let activeFetchers = /* @__PURE__ */ new Map();
  let fetchersQueuedForDeletion = /* @__PURE__ */ new Set();
  let blockerFunctions = /* @__PURE__ */ new Map();
  let unblockBlockerHistoryUpdate = void 0;
  let pendingRevalidationDfd = null;
  function initialize() {
    unlistenHistory = init.history.listen(
      ({ action: historyAction, location, delta }) => {
        if (unblockBlockerHistoryUpdate) {
          unblockBlockerHistoryUpdate();
          unblockBlockerHistoryUpdate = void 0;
          return;
        }
        warning(
          blockerFunctions.size === 0 || delta != null,
          "You are trying to use a blocker on a POP navigation to a location that was not created by @remix-run/router. This will fail silently in production. This can happen if you are navigating outside the router via `window.history.pushState`/`window.location.hash` instead of using router navigation APIs.  This can also happen if you are using createHashRouter and the user manually changes the URL."
        );
        let blockerKey = shouldBlockNavigation({
          currentLocation: state.location,
          nextLocation: location,
          historyAction
        });
        if (blockerKey && delta != null) {
          let nextHistoryUpdatePromise = new Promise((resolve) => {
            unblockBlockerHistoryUpdate = resolve;
          });
          init.history.go(delta * -1);
          updateBlocker(blockerKey, {
            state: "blocked",
            location,
            proceed() {
              updateBlocker(blockerKey, {
                state: "proceeding",
                proceed: void 0,
                reset: void 0,
                location
              });
              nextHistoryUpdatePromise.then(() => init.history.go(delta));
            },
            reset() {
              let blockers = new Map(state.blockers);
              blockers.set(blockerKey, IDLE_BLOCKER);
              updateState({ blockers });
            }
          });
          pendingPopstateNavigationDfd?.resolve();
          pendingPopstateNavigationDfd = null;
          return;
        }
        return startNavigation(historyAction, location);
      }
    );
    if (isBrowser3) {
      restoreAppliedTransitions(routerWindow, appliedViewTransitions);
      let _saveAppliedTransitions = () => persistAppliedTransitions(routerWindow, appliedViewTransitions);
      routerWindow.addEventListener("pagehide", _saveAppliedTransitions);
      removePageHideEventListener = () => routerWindow.removeEventListener("pagehide", _saveAppliedTransitions);
    }
    if (!state.initialized) {
      startNavigation("POP" /* Pop */, state.location, {
        initialHydration: true
      });
    }
    return router;
  }
  function dispose() {
    if (unlistenHistory) {
      unlistenHistory();
    }
    if (removePageHideEventListener) {
      removePageHideEventListener();
    }
    subscribers.clear();
    pendingNavigationController && pendingNavigationController.abort();
    state.fetchers.forEach((_, key) => deleteFetcher(key));
    state.blockers.forEach((_, key) => deleteBlocker(key));
  }
  function subscribe(fn) {
    subscribers.add(fn);
    return () => subscribers.delete(fn);
  }
  function updateState(newState, opts = {}) {
    if (newState.matches) {
      newState.matches = newState.matches.map((m) => {
        let route = manifest[m.route.id];
        let matchRoute = m.route;
        if (matchRoute.element !== route.element || matchRoute.errorElement !== route.errorElement || matchRoute.hydrateFallbackElement !== route.hydrateFallbackElement) {
          return {
            ...m,
            route
          };
        }
        return m;
      });
    }
    state = {
      ...state,
      ...newState
    };
    let unmountedFetchers = [];
    let mountedFetchers = [];
    state.fetchers.forEach((fetcher, key) => {
      if (fetcher.state === "idle") {
        if (fetchersQueuedForDeletion.has(key)) {
          unmountedFetchers.push(key);
        } else {
          mountedFetchers.push(key);
        }
      }
    });
    fetchersQueuedForDeletion.forEach((key) => {
      if (!state.fetchers.has(key) && !fetchControllers.has(key)) {
        unmountedFetchers.push(key);
      }
    });
    [...subscribers].forEach(
      (subscriber) => subscriber(state, {
        deletedFetchers: unmountedFetchers,
        newErrors: newState.errors ?? null,
        viewTransitionOpts: opts.viewTransitionOpts,
        flushSync: opts.flushSync === true
      })
    );
    unmountedFetchers.forEach((key) => deleteFetcher(key));
    mountedFetchers.forEach((key) => state.fetchers.delete(key));
  }
  function completeNavigation(location, newState, { flushSync } = {}) {
    let isActionReload = state.actionData != null && state.navigation.formMethod != null && isMutationMethod(state.navigation.formMethod) && state.navigation.state === "loading" && location.state?._isRedirect !== true;
    let actionData;
    if (newState.actionData) {
      if (Object.keys(newState.actionData).length > 0) {
        actionData = newState.actionData;
      } else {
        actionData = null;
      }
    } else if (isActionReload) {
      actionData = state.actionData;
    } else {
      actionData = null;
    }
    let loaderData = newState.loaderData ? mergeLoaderData(
      state.loaderData,
      newState.loaderData,
      newState.matches || [],
      newState.errors
    ) : state.loaderData;
    let blockers = state.blockers;
    if (blockers.size > 0) {
      blockers = new Map(blockers);
      blockers.forEach((_, k) => blockers.set(k, IDLE_BLOCKER));
    }
    let restoreScrollPosition = isUninterruptedRevalidation ? false : getSavedScrollPosition(location, newState.matches || state.matches);
    let preventScrollReset = pendingPreventScrollReset === true || state.navigation.formMethod != null && isMutationMethod(state.navigation.formMethod) && location.state?._isRedirect !== true;
    if (inFlightDataRoutes) {
      dataRoutes = inFlightDataRoutes;
      inFlightDataRoutes = void 0;
    }
    if (isUninterruptedRevalidation) {
    } else if (pendingAction === "POP" /* Pop */) {
    } else if (pendingAction === "PUSH" /* Push */) {
      init.history.push(location, location.state);
    } else if (pendingAction === "REPLACE" /* Replace */) {
      init.history.replace(location, location.state);
    }
    let viewTransitionOpts;
    if (pendingAction === "POP" /* Pop */) {
      let priorPaths = appliedViewTransitions.get(state.location.pathname);
      if (priorPaths && priorPaths.has(location.pathname)) {
        viewTransitionOpts = {
          currentLocation: state.location,
          nextLocation: location
        };
      } else if (appliedViewTransitions.has(location.pathname)) {
        viewTransitionOpts = {
          currentLocation: location,
          nextLocation: state.location
        };
      }
    } else if (pendingViewTransitionEnabled) {
      let toPaths = appliedViewTransitions.get(state.location.pathname);
      if (toPaths) {
        toPaths.add(location.pathname);
      } else {
        toPaths = /* @__PURE__ */ new Set([location.pathname]);
        appliedViewTransitions.set(state.location.pathname, toPaths);
      }
      viewTransitionOpts = {
        currentLocation: state.location,
        nextLocation: location
      };
    }
    updateState(
      {
        ...newState,
        // matches, errors, fetchers go through as-is
        actionData,
        loaderData,
        historyAction: pendingAction,
        location,
        initialized: true,
        renderFallback: false,
        navigation: IDLE_NAVIGATION,
        revalidation: "idle",
        restoreScrollPosition,
        preventScrollReset,
        blockers
      },
      {
        viewTransitionOpts,
        flushSync: flushSync === true
      }
    );
    pendingAction = "POP" /* Pop */;
    pendingPreventScrollReset = false;
    pendingViewTransitionEnabled = false;
    isUninterruptedRevalidation = false;
    isRevalidationRequired = false;
    pendingPopstateNavigationDfd?.resolve();
    pendingPopstateNavigationDfd = null;
    pendingRevalidationDfd?.resolve();
    pendingRevalidationDfd = null;
  }
  async function navigate(to, opts) {
    pendingPopstateNavigationDfd?.resolve();
    pendingPopstateNavigationDfd = null;
    if (typeof to === "number") {
      if (!pendingPopstateNavigationDfd) {
        pendingPopstateNavigationDfd = createDeferred();
      }
      let promise = pendingPopstateNavigationDfd.promise;
      init.history.go(to);
      return promise;
    }
    let normalizedPath = normalizeTo(
      state.location,
      state.matches,
      basename,
      to,
      opts?.fromRouteId,
      opts?.relative
    );
    let { path, submission, error } = normalizeNavigateOptions(
      false,
      normalizedPath,
      opts
    );
    let maskPath;
    if (opts?.unstable_mask) {
      let partialPath = typeof opts.unstable_mask === "string" ? parsePath(opts.unstable_mask) : {
        ...state.location.unstable_mask,
        ...opts.unstable_mask
      };
      maskPath = {
        pathname: "",
        search: "",
        hash: "",
        ...partialPath
      };
    }
    let currentLocation = state.location;
    let nextLocation = createLocation(
      currentLocation,
      path,
      opts && opts.state,
      void 0,
      maskPath
    );
    nextLocation = {
      ...nextLocation,
      ...init.history.encodeLocation(nextLocation)
    };
    let userReplace = opts && opts.replace != null ? opts.replace : void 0;
    let historyAction = "PUSH" /* Push */;
    if (userReplace === true) {
      historyAction = "REPLACE" /* Replace */;
    } else if (userReplace === false) {
    } else if (submission != null && isMutationMethod(submission.formMethod) && submission.formAction === state.location.pathname + state.location.search) {
      historyAction = "REPLACE" /* Replace */;
    }
    let preventScrollReset = opts && "preventScrollReset" in opts ? opts.preventScrollReset === true : void 0;
    let flushSync = (opts && opts.flushSync) === true;
    let blockerKey = shouldBlockNavigation({
      currentLocation,
      nextLocation,
      historyAction
    });
    if (blockerKey) {
      updateBlocker(blockerKey, {
        state: "blocked",
        location: nextLocation,
        proceed() {
          updateBlocker(blockerKey, {
            state: "proceeding",
            proceed: void 0,
            reset: void 0,
            location: nextLocation
          });
          navigate(to, opts);
        },
        reset() {
          let blockers = new Map(state.blockers);
          blockers.set(blockerKey, IDLE_BLOCKER);
          updateState({ blockers });
        }
      });
      return;
    }
    await startNavigation(historyAction, nextLocation, {
      submission,
      // Send through the formData serialization error if we have one so we can
      // render at the right error boundary after we match routes
      pendingError: error,
      preventScrollReset,
      replace: opts && opts.replace,
      enableViewTransition: opts && opts.viewTransition,
      flushSync,
      callSiteDefaultShouldRevalidate: opts && opts.unstable_defaultShouldRevalidate
    });
  }
  function revalidate() {
    if (!pendingRevalidationDfd) {
      pendingRevalidationDfd = createDeferred();
    }
    interruptActiveLoads();
    updateState({ revalidation: "loading" });
    let promise = pendingRevalidationDfd.promise;
    if (state.navigation.state === "submitting") {
      return promise;
    }
    if (state.navigation.state === "idle") {
      startNavigation(state.historyAction, state.location, {
        startUninterruptedRevalidation: true
      });
      return promise;
    }
    startNavigation(
      pendingAction || state.historyAction,
      state.navigation.location,
      {
        overrideNavigation: state.navigation,
        // Proxy through any rending view transition
        enableViewTransition: pendingViewTransitionEnabled === true
      }
    );
    return promise;
  }
  async function startNavigation(historyAction, location, opts) {
    pendingNavigationController && pendingNavigationController.abort();
    pendingNavigationController = null;
    pendingAction = historyAction;
    isUninterruptedRevalidation = (opts && opts.startUninterruptedRevalidation) === true;
    saveScrollPosition(state.location, state.matches);
    pendingPreventScrollReset = (opts && opts.preventScrollReset) === true;
    pendingViewTransitionEnabled = (opts && opts.enableViewTransition) === true;
    let routesToUse = inFlightDataRoutes || dataRoutes;
    let loadingNavigation = opts && opts.overrideNavigation;
    let matches = opts?.initialHydration && state.matches && state.matches.length > 0 && !initialMatchesIsFOW ? (
      // `matchRoutes()` has already been called if we're in here via `router.initialize()`
      state.matches
    ) : matchRoutes(routesToUse, location, basename);
    let flushSync = (opts && opts.flushSync) === true;
    if (matches && state.initialized && !isRevalidationRequired && isHashChangeOnly(state.location, location) && !(opts && opts.submission && isMutationMethod(opts.submission.formMethod))) {
      completeNavigation(location, { matches }, { flushSync });
      return;
    }
    let fogOfWar = checkFogOfWar(matches, routesToUse, location.pathname);
    if (fogOfWar.active && fogOfWar.matches) {
      matches = fogOfWar.matches;
    }
    if (!matches) {
      let { error, notFoundMatches, route } = handleNavigational404(
        location.pathname
      );
      completeNavigation(
        location,
        {
          matches: notFoundMatches,
          loaderData: {},
          errors: {
            [route.id]: error
          }
        },
        { flushSync }
      );
      return;
    }
    pendingNavigationController = new AbortController();
    let request = createClientSideRequest(
      init.history,
      location,
      pendingNavigationController.signal,
      opts && opts.submission
    );
    let scopedContext = init.getContext ? await init.getContext() : new RouterContextProvider();
    let pendingActionResult;
    if (opts && opts.pendingError) {
      pendingActionResult = [
        findNearestBoundary(matches).route.id,
        { type: "error" /* error */, error: opts.pendingError }
      ];
    } else if (opts && opts.submission && isMutationMethod(opts.submission.formMethod)) {
      let actionResult = await handleAction(
        request,
        location,
        opts.submission,
        matches,
        scopedContext,
        fogOfWar.active,
        opts && opts.initialHydration === true,
        { replace: opts.replace, flushSync }
      );
      if (actionResult.shortCircuited) {
        return;
      }
      if (actionResult.pendingActionResult) {
        let [routeId, result] = actionResult.pendingActionResult;
        if (isErrorResult(result) && isRouteErrorResponse(result.error) && result.error.status === 404) {
          pendingNavigationController = null;
          completeNavigation(location, {
            matches: actionResult.matches,
            loaderData: {},
            errors: {
              [routeId]: result.error
            }
          });
          return;
        }
      }
      matches = actionResult.matches || matches;
      pendingActionResult = actionResult.pendingActionResult;
      loadingNavigation = getLoadingNavigation(location, opts.submission);
      flushSync = false;
      fogOfWar.active = false;
      request = createClientSideRequest(
        init.history,
        request.url,
        request.signal
      );
    }
    let {
      shortCircuited,
      matches: updatedMatches,
      loaderData,
      errors
    } = await handleLoaders(
      request,
      location,
      matches,
      scopedContext,
      fogOfWar.active,
      loadingNavigation,
      opts && opts.submission,
      opts && opts.fetcherSubmission,
      opts && opts.replace,
      opts && opts.initialHydration === true,
      flushSync,
      pendingActionResult,
      opts && opts.callSiteDefaultShouldRevalidate
    );
    if (shortCircuited) {
      return;
    }
    pendingNavigationController = null;
    completeNavigation(location, {
      matches: updatedMatches || matches,
      ...getActionDataForCommit(pendingActionResult),
      loaderData,
      errors
    });
  }
  async function handleAction(request, location, submission, matches, scopedContext, isFogOfWar, initialHydration, opts = {}) {
    interruptActiveLoads();
    let navigation = getSubmittingNavigation(location, submission);
    updateState({ navigation }, { flushSync: opts.flushSync === true });
    if (isFogOfWar) {
      let discoverResult = await discoverRoutes(
        matches,
        location.pathname,
        request.signal
      );
      if (discoverResult.type === "aborted") {
        return { shortCircuited: true };
      } else if (discoverResult.type === "error") {
        if (discoverResult.partialMatches.length === 0) {
          let { matches: matches2, route } = getShortCircuitMatches(dataRoutes);
          return {
            matches: matches2,
            pendingActionResult: [
              route.id,
              {
                type: "error" /* error */,
                error: discoverResult.error
              }
            ]
          };
        }
        let boundaryId = findNearestBoundary(discoverResult.partialMatches).route.id;
        return {
          matches: discoverResult.partialMatches,
          pendingActionResult: [
            boundaryId,
            {
              type: "error" /* error */,
              error: discoverResult.error
            }
          ]
        };
      } else if (!discoverResult.matches) {
        let { notFoundMatches, error, route } = handleNavigational404(
          location.pathname
        );
        return {
          matches: notFoundMatches,
          pendingActionResult: [
            route.id,
            {
              type: "error" /* error */,
              error
            }
          ]
        };
      } else {
        matches = discoverResult.matches;
      }
    }
    let result;
    let actionMatch = getTargetMatch(matches, location);
    if (!actionMatch.route.action && !actionMatch.route.lazy) {
      result = {
        type: "error" /* error */,
        error: getInternalRouterError(405, {
          method: request.method,
          pathname: location.pathname,
          routeId: actionMatch.route.id
        })
      };
    } else {
      let dsMatches = getTargetedDataStrategyMatches(
        mapRouteProperties2,
        manifest,
        request,
        location,
        matches,
        actionMatch,
        initialHydration ? [] : hydrationRouteProperties2,
        scopedContext
      );
      let results = await callDataStrategy(
        request,
        location,
        dsMatches,
        scopedContext,
        null
      );
      result = results[actionMatch.route.id];
      if (!result) {
        for (let match of matches) {
          if (results[match.route.id]) {
            result = results[match.route.id];
            break;
          }
        }
      }
      if (request.signal.aborted) {
        return { shortCircuited: true };
      }
    }
    if (isRedirectResult(result)) {
      let replace2;
      if (opts && opts.replace != null) {
        replace2 = opts.replace;
      } else {
        let location2 = normalizeRedirectLocation(
          result.response.headers.get("Location"),
          new URL(request.url),
          basename,
          init.history
        );
        replace2 = location2 === state.location.pathname + state.location.search;
      }
      await startRedirectNavigation(request, result, true, {
        submission,
        replace: replace2
      });
      return { shortCircuited: true };
    }
    if (isErrorResult(result)) {
      let boundaryMatch = findNearestBoundary(matches, actionMatch.route.id);
      if ((opts && opts.replace) !== true) {
        pendingAction = "PUSH" /* Push */;
      }
      return {
        matches,
        pendingActionResult: [
          boundaryMatch.route.id,
          result,
          actionMatch.route.id
        ]
      };
    }
    return {
      matches,
      pendingActionResult: [actionMatch.route.id, result]
    };
  }
  async function handleLoaders(request, location, matches, scopedContext, isFogOfWar, overrideNavigation, submission, fetcherSubmission, replace2, initialHydration, flushSync, pendingActionResult, callSiteDefaultShouldRevalidate) {
    let loadingNavigation = overrideNavigation || getLoadingNavigation(location, submission);
    let activeSubmission = submission || fetcherSubmission || getSubmissionFromNavigation(loadingNavigation);
    let shouldUpdateNavigationState = !isUninterruptedRevalidation && !initialHydration;
    if (isFogOfWar) {
      if (shouldUpdateNavigationState) {
        let actionData = getUpdatedActionData(pendingActionResult);
        updateState(
          {
            navigation: loadingNavigation,
            ...actionData !== void 0 ? { actionData } : {}
          },
          {
            flushSync
          }
        );
      }
      let discoverResult = await discoverRoutes(
        matches,
        location.pathname,
        request.signal
      );
      if (discoverResult.type === "aborted") {
        return { shortCircuited: true };
      } else if (discoverResult.type === "error") {
        if (discoverResult.partialMatches.length === 0) {
          let { matches: matches2, route } = getShortCircuitMatches(dataRoutes);
          return {
            matches: matches2,
            loaderData: {},
            errors: {
              [route.id]: discoverResult.error
            }
          };
        }
        let boundaryId = findNearestBoundary(discoverResult.partialMatches).route.id;
        return {
          matches: discoverResult.partialMatches,
          loaderData: {},
          errors: {
            [boundaryId]: discoverResult.error
          }
        };
      } else if (!discoverResult.matches) {
        let { error, notFoundMatches, route } = handleNavigational404(
          location.pathname
        );
        return {
          matches: notFoundMatches,
          loaderData: {},
          errors: {
            [route.id]: error
          }
        };
      } else {
        matches = discoverResult.matches;
      }
    }
    let routesToUse = inFlightDataRoutes || dataRoutes;
    let { dsMatches, revalidatingFetchers } = getMatchesToLoad(
      request,
      scopedContext,
      mapRouteProperties2,
      manifest,
      init.history,
      state,
      matches,
      activeSubmission,
      location,
      initialHydration ? [] : hydrationRouteProperties2,
      initialHydration === true,
      isRevalidationRequired,
      cancelledFetcherLoads,
      fetchersQueuedForDeletion,
      fetchLoadMatches,
      fetchRedirectIds,
      routesToUse,
      basename,
      init.patchRoutesOnNavigation != null,
      pendingActionResult,
      callSiteDefaultShouldRevalidate
    );
    pendingNavigationLoadId = ++incrementingLoadId;
    if (!init.dataStrategy && !dsMatches.some((m) => m.shouldLoad) && !dsMatches.some(
      (m) => m.route.middleware && m.route.middleware.length > 0
    ) && revalidatingFetchers.length === 0) {
      let updatedFetchers2 = markFetchRedirectsDone();
      completeNavigation(
        location,
        {
          matches,
          loaderData: {},
          // Commit pending error if we're short circuiting
          errors: pendingActionResult && isErrorResult(pendingActionResult[1]) ? { [pendingActionResult[0]]: pendingActionResult[1].error } : null,
          ...getActionDataForCommit(pendingActionResult),
          ...updatedFetchers2 ? { fetchers: new Map(state.fetchers) } : {}
        },
        { flushSync }
      );
      return { shortCircuited: true };
    }
    if (shouldUpdateNavigationState) {
      let updates = {};
      if (!isFogOfWar) {
        updates.navigation = loadingNavigation;
        let actionData = getUpdatedActionData(pendingActionResult);
        if (actionData !== void 0) {
          updates.actionData = actionData;
        }
      }
      if (revalidatingFetchers.length > 0) {
        updates.fetchers = getUpdatedRevalidatingFetchers(revalidatingFetchers);
      }
      updateState(updates, { flushSync });
    }
    revalidatingFetchers.forEach((rf) => {
      abortFetcher(rf.key);
      if (rf.controller) {
        fetchControllers.set(rf.key, rf.controller);
      }
    });
    let abortPendingFetchRevalidations = () => revalidatingFetchers.forEach((f) => abortFetcher(f.key));
    if (pendingNavigationController) {
      pendingNavigationController.signal.addEventListener(
        "abort",
        abortPendingFetchRevalidations
      );
    }
    let { loaderResults, fetcherResults } = await callLoadersAndMaybeResolveData(
      dsMatches,
      revalidatingFetchers,
      request,
      location,
      scopedContext
    );
    if (request.signal.aborted) {
      return { shortCircuited: true };
    }
    if (pendingNavigationController) {
      pendingNavigationController.signal.removeEventListener(
        "abort",
        abortPendingFetchRevalidations
      );
    }
    revalidatingFetchers.forEach((rf) => fetchControllers.delete(rf.key));
    let redirect2 = findRedirect(loaderResults);
    if (redirect2) {
      await startRedirectNavigation(request, redirect2.result, true, {
        replace: replace2
      });
      return { shortCircuited: true };
    }
    redirect2 = findRedirect(fetcherResults);
    if (redirect2) {
      fetchRedirectIds.add(redirect2.key);
      await startRedirectNavigation(request, redirect2.result, true, {
        replace: replace2
      });
      return { shortCircuited: true };
    }
    let { loaderData, errors } = processLoaderData(
      state,
      matches,
      loaderResults,
      pendingActionResult,
      revalidatingFetchers,
      fetcherResults
    );
    if (initialHydration && state.errors) {
      errors = { ...state.errors, ...errors };
    }
    let updatedFetchers = markFetchRedirectsDone();
    let didAbortFetchLoads = abortStaleFetchLoads(pendingNavigationLoadId);
    let shouldUpdateFetchers = updatedFetchers || didAbortFetchLoads || revalidatingFetchers.length > 0;
    return {
      matches,
      loaderData,
      errors,
      ...shouldUpdateFetchers ? { fetchers: new Map(state.fetchers) } : {}
    };
  }
  function getUpdatedActionData(pendingActionResult) {
    if (pendingActionResult && !isErrorResult(pendingActionResult[1])) {
      return {
        [pendingActionResult[0]]: pendingActionResult[1].data
      };
    } else if (state.actionData) {
      if (Object.keys(state.actionData).length === 0) {
        return null;
      } else {
        return state.actionData;
      }
    }
  }
  function getUpdatedRevalidatingFetchers(revalidatingFetchers) {
    revalidatingFetchers.forEach((rf) => {
      let fetcher = state.fetchers.get(rf.key);
      let revalidatingFetcher = getLoadingFetcher(
        void 0,
        fetcher ? fetcher.data : void 0
      );
      state.fetchers.set(rf.key, revalidatingFetcher);
    });
    return new Map(state.fetchers);
  }
  async function fetch2(key, routeId, href, opts) {
    abortFetcher(key);
    let flushSync = (opts && opts.flushSync) === true;
    let routesToUse = inFlightDataRoutes || dataRoutes;
    let normalizedPath = normalizeTo(
      state.location,
      state.matches,
      basename,
      href,
      routeId,
      opts?.relative
    );
    let matches = matchRoutes(routesToUse, normalizedPath, basename);
    let fogOfWar = checkFogOfWar(matches, routesToUse, normalizedPath);
    if (fogOfWar.active && fogOfWar.matches) {
      matches = fogOfWar.matches;
    }
    if (!matches) {
      setFetcherError(
        key,
        routeId,
        getInternalRouterError(404, { pathname: normalizedPath }),
        { flushSync }
      );
      return;
    }
    let { path, submission, error } = normalizeNavigateOptions(
      true,
      normalizedPath,
      opts
    );
    if (error) {
      setFetcherError(key, routeId, error, { flushSync });
      return;
    }
    let scopedContext = init.getContext ? await init.getContext() : new RouterContextProvider();
    let preventScrollReset = (opts && opts.preventScrollReset) === true;
    if (submission && isMutationMethod(submission.formMethod)) {
      await handleFetcherAction(
        key,
        routeId,
        path,
        matches,
        scopedContext,
        fogOfWar.active,
        flushSync,
        preventScrollReset,
        submission,
        opts && opts.unstable_defaultShouldRevalidate
      );
      return;
    }
    fetchLoadMatches.set(key, { routeId, path });
    await handleFetcherLoader(
      key,
      routeId,
      path,
      matches,
      scopedContext,
      fogOfWar.active,
      flushSync,
      preventScrollReset,
      submission
    );
  }
  async function handleFetcherAction(key, routeId, path, requestMatches, scopedContext, isFogOfWar, flushSync, preventScrollReset, submission, callSiteDefaultShouldRevalidate) {
    interruptActiveLoads();
    fetchLoadMatches.delete(key);
    let existingFetcher = state.fetchers.get(key);
    updateFetcherState(key, getSubmittingFetcher(submission, existingFetcher), {
      flushSync
    });
    let abortController = new AbortController();
    let fetchRequest = createClientSideRequest(
      init.history,
      path,
      abortController.signal,
      submission
    );
    if (isFogOfWar) {
      let discoverResult = await discoverRoutes(
        requestMatches,
        new URL(fetchRequest.url).pathname,
        fetchRequest.signal,
        key
      );
      if (discoverResult.type === "aborted") {
        return;
      } else if (discoverResult.type === "error") {
        setFetcherError(key, routeId, discoverResult.error, { flushSync });
        return;
      } else if (!discoverResult.matches) {
        setFetcherError(
          key,
          routeId,
          getInternalRouterError(404, { pathname: path }),
          { flushSync }
        );
        return;
      } else {
        requestMatches = discoverResult.matches;
      }
    }
    let match = getTargetMatch(requestMatches, path);
    if (!match.route.action && !match.route.lazy) {
      let error = getInternalRouterError(405, {
        method: submission.formMethod,
        pathname: path,
        routeId
      });
      setFetcherError(key, routeId, error, { flushSync });
      return;
    }
    fetchControllers.set(key, abortController);
    let originatingLoadId = incrementingLoadId;
    let fetchMatches = getTargetedDataStrategyMatches(
      mapRouteProperties2,
      manifest,
      fetchRequest,
      path,
      requestMatches,
      match,
      hydrationRouteProperties2,
      scopedContext
    );
    let actionResults = await callDataStrategy(
      fetchRequest,
      path,
      fetchMatches,
      scopedContext,
      key
    );
    let actionResult = actionResults[match.route.id];
    if (!actionResult) {
      for (let match2 of fetchMatches) {
        if (actionResults[match2.route.id]) {
          actionResult = actionResults[match2.route.id];
          break;
        }
      }
    }
    if (fetchRequest.signal.aborted) {
      if (fetchControllers.get(key) === abortController) {
        fetchControllers.delete(key);
      }
      return;
    }
    if (fetchersQueuedForDeletion.has(key)) {
      if (isRedirectResult(actionResult) || isErrorResult(actionResult)) {
        updateFetcherState(key, getDoneFetcher(void 0));
        return;
      }
    } else {
      if (isRedirectResult(actionResult)) {
        fetchControllers.delete(key);
        if (pendingNavigationLoadId > originatingLoadId) {
          updateFetcherState(key, getDoneFetcher(void 0));
          return;
        } else {
          fetchRedirectIds.add(key);
          updateFetcherState(key, getLoadingFetcher(submission));
          return startRedirectNavigation(fetchRequest, actionResult, false, {
            fetcherSubmission: submission,
            preventScrollReset
          });
        }
      }
      if (isErrorResult(actionResult)) {
        setFetcherError(key, routeId, actionResult.error);
        return;
      }
    }
    let nextLocation = state.navigation.location || state.location;
    let revalidationRequest = createClientSideRequest(
      init.history,
      nextLocation,
      abortController.signal
    );
    let routesToUse = inFlightDataRoutes || dataRoutes;
    let matches = state.navigation.state !== "idle" ? matchRoutes(routesToUse, state.navigation.location, basename) : state.matches;
    invariant(matches, "Didn't find any matches after fetcher action");
    let loadId = ++incrementingLoadId;
    fetchReloadIds.set(key, loadId);
    let loadFetcher = getLoadingFetcher(submission, actionResult.data);
    state.fetchers.set(key, loadFetcher);
    let { dsMatches, revalidatingFetchers } = getMatchesToLoad(
      revalidationRequest,
      scopedContext,
      mapRouteProperties2,
      manifest,
      init.history,
      state,
      matches,
      submission,
      nextLocation,
      hydrationRouteProperties2,
      false,
      isRevalidationRequired,
      cancelledFetcherLoads,
      fetchersQueuedForDeletion,
      fetchLoadMatches,
      fetchRedirectIds,
      routesToUse,
      basename,
      init.patchRoutesOnNavigation != null,
      [match.route.id, actionResult],
      callSiteDefaultShouldRevalidate
    );
    revalidatingFetchers.filter((rf) => rf.key !== key).forEach((rf) => {
      let staleKey = rf.key;
      let existingFetcher2 = state.fetchers.get(staleKey);
      let revalidatingFetcher = getLoadingFetcher(
        void 0,
        existingFetcher2 ? existingFetcher2.data : void 0
      );
      state.fetchers.set(staleKey, revalidatingFetcher);
      abortFetcher(staleKey);
      if (rf.controller) {
        fetchControllers.set(staleKey, rf.controller);
      }
    });
    updateState({ fetchers: new Map(state.fetchers) });
    let abortPendingFetchRevalidations = () => revalidatingFetchers.forEach((rf) => abortFetcher(rf.key));
    abortController.signal.addEventListener(
      "abort",
      abortPendingFetchRevalidations
    );
    let { loaderResults, fetcherResults } = await callLoadersAndMaybeResolveData(
      dsMatches,
      revalidatingFetchers,
      revalidationRequest,
      nextLocation,
      scopedContext
    );
    if (abortController.signal.aborted) {
      return;
    }
    abortController.signal.removeEventListener(
      "abort",
      abortPendingFetchRevalidations
    );
    fetchReloadIds.delete(key);
    fetchControllers.delete(key);
    revalidatingFetchers.forEach((r) => fetchControllers.delete(r.key));
    if (state.fetchers.has(key)) {
      let doneFetcher = getDoneFetcher(actionResult.data);
      state.fetchers.set(key, doneFetcher);
    }
    let redirect2 = findRedirect(loaderResults);
    if (redirect2) {
      return startRedirectNavigation(
        revalidationRequest,
        redirect2.result,
        false,
        { preventScrollReset }
      );
    }
    redirect2 = findRedirect(fetcherResults);
    if (redirect2) {
      fetchRedirectIds.add(redirect2.key);
      return startRedirectNavigation(
        revalidationRequest,
        redirect2.result,
        false,
        { preventScrollReset }
      );
    }
    let { loaderData, errors } = processLoaderData(
      state,
      matches,
      loaderResults,
      void 0,
      revalidatingFetchers,
      fetcherResults
    );
    abortStaleFetchLoads(loadId);
    if (state.navigation.state === "loading" && loadId > pendingNavigationLoadId) {
      invariant(pendingAction, "Expected pending action");
      pendingNavigationController && pendingNavigationController.abort();
      completeNavigation(state.navigation.location, {
        matches,
        loaderData,
        errors,
        fetchers: new Map(state.fetchers)
      });
    } else {
      updateState({
        errors,
        loaderData: mergeLoaderData(
          state.loaderData,
          loaderData,
          matches,
          errors
        ),
        fetchers: new Map(state.fetchers)
      });
      isRevalidationRequired = false;
    }
  }
  async function handleFetcherLoader(key, routeId, path, matches, scopedContext, isFogOfWar, flushSync, preventScrollReset, submission) {
    let existingFetcher = state.fetchers.get(key);
    updateFetcherState(
      key,
      getLoadingFetcher(
        submission,
        existingFetcher ? existingFetcher.data : void 0
      ),
      { flushSync }
    );
    let abortController = new AbortController();
    let fetchRequest = createClientSideRequest(
      init.history,
      path,
      abortController.signal
    );
    if (isFogOfWar) {
      let discoverResult = await discoverRoutes(
        matches,
        new URL(fetchRequest.url).pathname,
        fetchRequest.signal,
        key
      );
      if (discoverResult.type === "aborted") {
        return;
      } else if (discoverResult.type === "error") {
        setFetcherError(key, routeId, discoverResult.error, { flushSync });
        return;
      } else if (!discoverResult.matches) {
        setFetcherError(
          key,
          routeId,
          getInternalRouterError(404, { pathname: path }),
          { flushSync }
        );
        return;
      } else {
        matches = discoverResult.matches;
      }
    }
    let match = getTargetMatch(matches, path);
    fetchControllers.set(key, abortController);
    let originatingLoadId = incrementingLoadId;
    let dsMatches = getTargetedDataStrategyMatches(
      mapRouteProperties2,
      manifest,
      fetchRequest,
      path,
      matches,
      match,
      hydrationRouteProperties2,
      scopedContext
    );
    let results = await callDataStrategy(
      fetchRequest,
      path,
      dsMatches,
      scopedContext,
      key
    );
    let result = results[match.route.id];
    if (fetchControllers.get(key) === abortController) {
      fetchControllers.delete(key);
    }
    if (fetchRequest.signal.aborted) {
      return;
    }
    if (fetchersQueuedForDeletion.has(key)) {
      updateFetcherState(key, getDoneFetcher(void 0));
      return;
    }
    if (isRedirectResult(result)) {
      if (pendingNavigationLoadId > originatingLoadId) {
        updateFetcherState(key, getDoneFetcher(void 0));
        return;
      } else {
        fetchRedirectIds.add(key);
        await startRedirectNavigation(fetchRequest, result, false, {
          preventScrollReset
        });
        return;
      }
    }
    if (isErrorResult(result)) {
      setFetcherError(key, routeId, result.error);
      return;
    }
    updateFetcherState(key, getDoneFetcher(result.data));
  }
  async function startRedirectNavigation(request, redirect2, isNavigation, {
    submission,
    fetcherSubmission,
    preventScrollReset,
    replace: replace2
  } = {}) {
    if (!isNavigation) {
      pendingPopstateNavigationDfd?.resolve();
      pendingPopstateNavigationDfd = null;
    }
    if (redirect2.response.headers.has("X-Remix-Revalidate")) {
      isRevalidationRequired = true;
    }
    let location = redirect2.response.headers.get("Location");
    invariant(location, "Expected a Location header on the redirect Response");
    location = normalizeRedirectLocation(
      location,
      new URL(request.url),
      basename,
      init.history
    );
    let redirectLocation = createLocation(state.location, location, {
      _isRedirect: true
    });
    if (isBrowser3) {
      let isDocumentReload = false;
      if (redirect2.response.headers.has("X-Remix-Reload-Document")) {
        isDocumentReload = true;
      } else if (isAbsoluteUrl(location)) {
        const url = createBrowserURLImpl(location, true);
        isDocumentReload = // Hard reload if it's an absolute URL to a new origin
        url.origin !== routerWindow.location.origin || // Hard reload if it's an absolute URL that does not match our basename
        stripBasename(url.pathname, basename) == null;
      }
      if (isDocumentReload) {
        if (replace2) {
          routerWindow.location.replace(location);
        } else {
          routerWindow.location.assign(location);
        }
        return;
      }
    }
    pendingNavigationController = null;
    let redirectNavigationType = replace2 === true || redirect2.response.headers.has("X-Remix-Replace") ? "REPLACE" /* Replace */ : "PUSH" /* Push */;
    let { formMethod, formAction, formEncType } = state.navigation;
    if (!submission && !fetcherSubmission && formMethod && formAction && formEncType) {
      submission = getSubmissionFromNavigation(state.navigation);
    }
    let activeSubmission = submission || fetcherSubmission;
    if (redirectPreserveMethodStatusCodes.has(redirect2.response.status) && activeSubmission && isMutationMethod(activeSubmission.formMethod)) {
      await startNavigation(redirectNavigationType, redirectLocation, {
        submission: {
          ...activeSubmission,
          formAction: location
        },
        // Preserve these flags across redirects
        preventScrollReset: preventScrollReset || pendingPreventScrollReset,
        enableViewTransition: isNavigation ? pendingViewTransitionEnabled : void 0
      });
    } else {
      let overrideNavigation = getLoadingNavigation(
        redirectLocation,
        submission
      );
      await startNavigation(redirectNavigationType, redirectLocation, {
        overrideNavigation,
        // Send fetcher submissions through for shouldRevalidate
        fetcherSubmission,
        // Preserve these flags across redirects
        preventScrollReset: preventScrollReset || pendingPreventScrollReset,
        enableViewTransition: isNavigation ? pendingViewTransitionEnabled : void 0
      });
    }
  }
  async function callDataStrategy(request, path, matches, scopedContext, fetcherKey) {
    let results;
    let dataResults = {};
    try {
      results = await callDataStrategyImpl(
        dataStrategyImpl,
        request,
        path,
        matches,
        fetcherKey,
        scopedContext,
        false
      );
    } catch (e) {
      matches.filter((m) => m.shouldLoad).forEach((m) => {
        dataResults[m.route.id] = {
          type: "error" /* error */,
          error: e
        };
      });
      return dataResults;
    }
    if (request.signal.aborted) {
      return dataResults;
    }
    if (!isMutationMethod(request.method)) {
      for (let match of matches) {
        if (results[match.route.id]?.type === "error" /* error */) {
          break;
        }
        if (!results.hasOwnProperty(match.route.id) && !state.loaderData.hasOwnProperty(match.route.id) && (!state.errors || !state.errors.hasOwnProperty(match.route.id)) && match.shouldCallHandler()) {
          results[match.route.id] = {
            type: "error" /* error */,
            result: new Error(
              `No result returned from dataStrategy for route ${match.route.id}`
            )
          };
        }
      }
    }
    for (let [routeId, result] of Object.entries(results)) {
      if (isRedirectDataStrategyResult(result)) {
        let response = result.result;
        dataResults[routeId] = {
          type: "redirect" /* redirect */,
          response: normalizeRelativeRoutingRedirectResponse(
            response,
            request,
            routeId,
            matches,
            basename
          )
        };
      } else {
        dataResults[routeId] = await convertDataStrategyResultToDataResult(result);
      }
    }
    return dataResults;
  }
  async function callLoadersAndMaybeResolveData(matches, fetchersToLoad, request, location, scopedContext) {
    let loaderResultsPromise = callDataStrategy(
      request,
      location,
      matches,
      scopedContext,
      null
    );
    let fetcherResultsPromise = Promise.all(
      fetchersToLoad.map(async (f) => {
        if (f.matches && f.match && f.request && f.controller) {
          let results = await callDataStrategy(
            f.request,
            f.path,
            f.matches,
            scopedContext,
            f.key
          );
          let result = results[f.match.route.id];
          return { [f.key]: result };
        } else {
          return Promise.resolve({
            [f.key]: {
              type: "error" /* error */,
              error: getInternalRouterError(404, {
                pathname: f.path
              })
            }
          });
        }
      })
    );
    let loaderResults = await loaderResultsPromise;
    let fetcherResults = (await fetcherResultsPromise).reduce(
      (acc, r) => Object.assign(acc, r),
      {}
    );
    return {
      loaderResults,
      fetcherResults
    };
  }
  function interruptActiveLoads() {
    isRevalidationRequired = true;
    fetchLoadMatches.forEach((_, key) => {
      if (fetchControllers.has(key)) {
        cancelledFetcherLoads.add(key);
      }
      abortFetcher(key);
    });
  }
  function updateFetcherState(key, fetcher, opts = {}) {
    state.fetchers.set(key, fetcher);
    updateState(
      { fetchers: new Map(state.fetchers) },
      { flushSync: (opts && opts.flushSync) === true }
    );
  }
  function setFetcherError(key, routeId, error, opts = {}) {
    let boundaryMatch = findNearestBoundary(state.matches, routeId);
    deleteFetcher(key);
    updateState(
      {
        errors: {
          [boundaryMatch.route.id]: error
        },
        fetchers: new Map(state.fetchers)
      },
      { flushSync: (opts && opts.flushSync) === true }
    );
  }
  function getFetcher(key) {
    activeFetchers.set(key, (activeFetchers.get(key) || 0) + 1);
    if (fetchersQueuedForDeletion.has(key)) {
      fetchersQueuedForDeletion.delete(key);
    }
    return state.fetchers.get(key) || IDLE_FETCHER;
  }
  function resetFetcher(key, opts) {
    abortFetcher(key, opts?.reason);
    updateFetcherState(key, getDoneFetcher(null));
  }
  function deleteFetcher(key) {
    let fetcher = state.fetchers.get(key);
    if (fetchControllers.has(key) && !(fetcher && fetcher.state === "loading" && fetchReloadIds.has(key))) {
      abortFetcher(key);
    }
    fetchLoadMatches.delete(key);
    fetchReloadIds.delete(key);
    fetchRedirectIds.delete(key);
    fetchersQueuedForDeletion.delete(key);
    cancelledFetcherLoads.delete(key);
    state.fetchers.delete(key);
  }
  function queueFetcherForDeletion(key) {
    let count = (activeFetchers.get(key) || 0) - 1;
    if (count <= 0) {
      activeFetchers.delete(key);
      fetchersQueuedForDeletion.add(key);
    } else {
      activeFetchers.set(key, count);
    }
    updateState({ fetchers: new Map(state.fetchers) });
  }
  function abortFetcher(key, reason) {
    let controller = fetchControllers.get(key);
    if (controller) {
      controller.abort(reason);
      fetchControllers.delete(key);
    }
  }
  function markFetchersDone(keys) {
    for (let key of keys) {
      let fetcher = getFetcher(key);
      let doneFetcher = getDoneFetcher(fetcher.data);
      state.fetchers.set(key, doneFetcher);
    }
  }
  function markFetchRedirectsDone() {
    let doneKeys = [];
    let updatedFetchers = false;
    for (let key of fetchRedirectIds) {
      let fetcher = state.fetchers.get(key);
      invariant(fetcher, `Expected fetcher: ${key}`);
      if (fetcher.state === "loading") {
        fetchRedirectIds.delete(key);
        doneKeys.push(key);
        updatedFetchers = true;
      }
    }
    markFetchersDone(doneKeys);
    return updatedFetchers;
  }
  function abortStaleFetchLoads(landedId) {
    let yeetedKeys = [];
    for (let [key, id] of fetchReloadIds) {
      if (id < landedId) {
        let fetcher = state.fetchers.get(key);
        invariant(fetcher, `Expected fetcher: ${key}`);
        if (fetcher.state === "loading") {
          abortFetcher(key);
          fetchReloadIds.delete(key);
          yeetedKeys.push(key);
        }
      }
    }
    markFetchersDone(yeetedKeys);
    return yeetedKeys.length > 0;
  }
  function getBlocker(key, fn) {
    let blocker = state.blockers.get(key) || IDLE_BLOCKER;
    if (blockerFunctions.get(key) !== fn) {
      blockerFunctions.set(key, fn);
    }
    return blocker;
  }
  function deleteBlocker(key) {
    state.blockers.delete(key);
    blockerFunctions.delete(key);
  }
  function updateBlocker(key, newBlocker) {
    let blocker = state.blockers.get(key) || IDLE_BLOCKER;
    invariant(
      blocker.state === "unblocked" && newBlocker.state === "blocked" || blocker.state === "blocked" && newBlocker.state === "blocked" || blocker.state === "blocked" && newBlocker.state === "proceeding" || blocker.state === "blocked" && newBlocker.state === "unblocked" || blocker.state === "proceeding" && newBlocker.state === "unblocked",
      `Invalid blocker state transition: ${blocker.state} -> ${newBlocker.state}`
    );
    let blockers = new Map(state.blockers);
    blockers.set(key, newBlocker);
    updateState({ blockers });
  }
  function shouldBlockNavigation({
    currentLocation,
    nextLocation,
    historyAction
  }) {
    if (blockerFunctions.size === 0) {
      return;
    }
    if (blockerFunctions.size > 1) {
      warning(false, "A router only supports one blocker at a time");
    }
    let entries = Array.from(blockerFunctions.entries());
    let [blockerKey, blockerFunction] = entries[entries.length - 1];
    let blocker = state.blockers.get(blockerKey);
    if (blocker && blocker.state === "proceeding") {
      return;
    }
    if (blockerFunction({ currentLocation, nextLocation, historyAction })) {
      return blockerKey;
    }
  }
  function handleNavigational404(pathname) {
    let error = getInternalRouterError(404, { pathname });
    let routesToUse = inFlightDataRoutes || dataRoutes;
    let { matches, route } = getShortCircuitMatches(routesToUse);
    return { notFoundMatches: matches, route, error };
  }
  function enableScrollRestoration(positions, getPosition, getKey) {
    savedScrollPositions2 = positions;
    getScrollPosition = getPosition;
    getScrollRestorationKey2 = getKey || null;
    if (!initialScrollRestored && state.navigation === IDLE_NAVIGATION) {
      initialScrollRestored = true;
      let y = getSavedScrollPosition(state.location, state.matches);
      if (y != null) {
        updateState({ restoreScrollPosition: y });
      }
    }
    return () => {
      savedScrollPositions2 = null;
      getScrollPosition = null;
      getScrollRestorationKey2 = null;
    };
  }
  function getScrollKey(location, matches) {
    if (getScrollRestorationKey2) {
      let key = getScrollRestorationKey2(
        location,
        matches.map((m) => convertRouteMatchToUiMatch(m, state.loaderData))
      );
      return key || location.key;
    }
    return location.key;
  }
  function saveScrollPosition(location, matches) {
    if (savedScrollPositions2 && getScrollPosition) {
      let key = getScrollKey(location, matches);
      savedScrollPositions2[key] = getScrollPosition();
    }
  }
  function getSavedScrollPosition(location, matches) {
    if (savedScrollPositions2) {
      let key = getScrollKey(location, matches);
      let y = savedScrollPositions2[key];
      if (typeof y === "number") {
        return y;
      }
    }
    return null;
  }
  function checkFogOfWar(matches, routesToUse, pathname) {
    if (init.patchRoutesOnNavigation) {
      if (!matches) {
        let fogMatches = matchRoutesImpl(
          routesToUse,
          pathname,
          basename,
          true
        );
        return { active: true, matches: fogMatches || [] };
      } else {
        if (Object.keys(matches[0].params).length > 0) {
          let partialMatches = matchRoutesImpl(
            routesToUse,
            pathname,
            basename,
            true
          );
          return { active: true, matches: partialMatches };
        }
      }
    }
    return { active: false, matches: null };
  }
  async function discoverRoutes(matches, pathname, signal, fetcherKey) {
    if (!init.patchRoutesOnNavigation) {
      return { type: "success", matches };
    }
    let partialMatches = matches;
    while (true) {
      let isNonHMR = inFlightDataRoutes == null;
      let routesToUse = inFlightDataRoutes || dataRoutes;
      let localManifest = manifest;
      try {
        await init.patchRoutesOnNavigation({
          signal,
          path: pathname,
          matches: partialMatches,
          fetcherKey,
          patch: (routeId, children) => {
            if (signal.aborted) return;
            patchRoutesImpl(
              routeId,
              children,
              routesToUse,
              localManifest,
              mapRouteProperties2,
              false
            );
          }
        });
      } catch (e) {
        return { type: "error", error: e, partialMatches };
      } finally {
        if (isNonHMR && !signal.aborted) {
          dataRoutes = [...dataRoutes];
        }
      }
      if (signal.aborted) {
        return { type: "aborted" };
      }
      let newMatches = matchRoutes(routesToUse, pathname, basename);
      let newPartialMatches = null;
      if (newMatches) {
        if (Object.keys(newMatches[0].params).length === 0) {
          return { type: "success", matches: newMatches };
        } else {
          newPartialMatches = matchRoutesImpl(
            routesToUse,
            pathname,
            basename,
            true
          );
          let matchedDeeper = newPartialMatches && partialMatches.length < newPartialMatches.length && compareMatches(
            partialMatches,
            newPartialMatches.slice(0, partialMatches.length)
          );
          if (!matchedDeeper) {
            return { type: "success", matches: newMatches };
          }
        }
      }
      if (!newPartialMatches) {
        newPartialMatches = matchRoutesImpl(
          routesToUse,
          pathname,
          basename,
          true
        );
      }
      if (!newPartialMatches || compareMatches(partialMatches, newPartialMatches)) {
        return { type: "success", matches: null };
      }
      partialMatches = newPartialMatches;
    }
  }
  function compareMatches(a, b) {
    return a.length === b.length && a.every((m, i) => m.route.id === b[i].route.id);
  }
  function _internalSetRoutes(newRoutes) {
    manifest = {};
    inFlightDataRoutes = convertRoutesToDataRoutes(
      newRoutes,
      mapRouteProperties2,
      void 0,
      manifest
    );
  }
  function patchRoutes(routeId, children, unstable_allowElementMutations = false) {
    let isNonHMR = inFlightDataRoutes == null;
    let routesToUse = inFlightDataRoutes || dataRoutes;
    patchRoutesImpl(
      routeId,
      children,
      routesToUse,
      manifest,
      mapRouteProperties2,
      unstable_allowElementMutations
    );
    if (isNonHMR) {
      dataRoutes = [...dataRoutes];
      updateState({});
    }
  }
  router = {
    get basename() {
      return basename;
    },
    get future() {
      return future;
    },
    get state() {
      return state;
    },
    get routes() {
      return dataRoutes;
    },
    get window() {
      return routerWindow;
    },
    initialize,
    subscribe,
    enableScrollRestoration,
    navigate,
    fetch: fetch2,
    revalidate,
    // Passthrough to history-aware createHref used by useHref so we get proper
    // hash-aware URLs in DOM paths
    createHref: (to) => init.history.createHref(to),
    encodeLocation: (to) => init.history.encodeLocation(to),
    getFetcher,
    resetFetcher,
    deleteFetcher: queueFetcherForDeletion,
    dispose,
    getBlocker,
    deleteBlocker,
    patchRoutes,
    _internalFetchControllers: fetchControllers,
    // TODO: Remove setRoutes, it's temporary to avoid dealing with
    // updating the tree while validating the update algorithm.
    _internalSetRoutes,
    _internalSetStateDoNotUseOrYouWillBreakYourApp(newState) {
      updateState(newState);
    }
  };
  if (init.unstable_instrumentations) {
    router = instrumentClientSideRouter(
      router,
      init.unstable_instrumentations.map((i) => i.router).filter(Boolean)
    );
  }
  return router;
}
function createStaticHandler(routes, opts) {
  invariant(
    routes.length > 0,
    "You must provide a non-empty routes array to createStaticHandler"
  );
  let manifest = {};
  let basename = (opts ? opts.basename : null) || "/";
  let _mapRouteProperties = opts?.mapRouteProperties || defaultMapRouteProperties;
  let mapRouteProperties2 = _mapRouteProperties;
  let future = {
    unstable_passThroughRequests: false,
    // unused in static handler
    ...opts?.future
  };
  if (opts?.unstable_instrumentations) {
    let instrumentations = opts.unstable_instrumentations;
    mapRouteProperties2 = (route) => {
      return {
        ..._mapRouteProperties(route),
        ...getRouteInstrumentationUpdates(
          instrumentations.map((i) => i.route).filter(Boolean),
          route
        )
      };
    };
  }
  let dataRoutes = convertRoutesToDataRoutes(
    routes,
    mapRouteProperties2,
    void 0,
    manifest
  );
  async function query(request, {
    requestContext,
    filterMatchesToLoad,
    skipLoaderErrorBubbling,
    skipRevalidation,
    dataStrategy,
    generateMiddlewareResponse,
    unstable_normalizePath
  } = {}) {
    let normalizePath = unstable_normalizePath || defaultNormalizePath;
    let method = request.method;
    let location = createLocation("", normalizePath(request), null, "default");
    let matches = matchRoutes(dataRoutes, location, basename);
    requestContext = requestContext != null ? requestContext : new RouterContextProvider();
    if (!isValidMethod(method) && method !== "HEAD") {
      let error = getInternalRouterError(405, { method });
      let { matches: methodNotAllowedMatches, route } = getShortCircuitMatches(dataRoutes);
      let staticContext = {
        basename,
        location,
        matches: methodNotAllowedMatches,
        loaderData: {},
        actionData: null,
        errors: {
          [route.id]: error
        },
        statusCode: error.status,
        loaderHeaders: {},
        actionHeaders: {}
      };
      return generateMiddlewareResponse ? generateMiddlewareResponse(() => Promise.resolve(staticContext)) : staticContext;
    } else if (!matches) {
      let error = getInternalRouterError(404, { pathname: location.pathname });
      let { matches: notFoundMatches, route } = getShortCircuitMatches(dataRoutes);
      let staticContext = {
        basename,
        location,
        matches: notFoundMatches,
        loaderData: {},
        actionData: null,
        errors: {
          [route.id]: error
        },
        statusCode: error.status,
        loaderHeaders: {},
        actionHeaders: {}
      };
      return generateMiddlewareResponse ? generateMiddlewareResponse(() => Promise.resolve(staticContext)) : staticContext;
    }
    if (generateMiddlewareResponse) {
      invariant(
        requestContext instanceof RouterContextProvider,
        "When using middleware in `staticHandler.query()`, any provided `requestContext` must be an instance of `RouterContextProvider`"
      );
      try {
        await loadLazyMiddlewareForMatches(
          matches,
          manifest,
          mapRouteProperties2
        );
        let renderedStaticContext;
        let response = await runServerMiddlewarePipeline(
          {
            request,
            unstable_url: createDataFunctionUrl(request, location),
            unstable_pattern: getRoutePattern(matches),
            matches,
            params: matches[0].params,
            // If we're calling middleware then it must be enabled so we can cast
            // this to the proper type knowing it's not an `AppLoadContext`
            context: requestContext
          },
          async () => {
            let res = await generateMiddlewareResponse(
              async (revalidationRequest, opts2 = {}) => {
                let result2 = await queryImpl(
                  revalidationRequest,
                  location,
                  matches,
                  requestContext,
                  dataStrategy || null,
                  skipLoaderErrorBubbling === true,
                  null,
                  "filterMatchesToLoad" in opts2 ? opts2.filterMatchesToLoad ?? null : filterMatchesToLoad ?? null,
                  skipRevalidation === true
                );
                if (isResponse(result2)) {
                  return result2;
                }
                renderedStaticContext = { location, basename, ...result2 };
                return renderedStaticContext;
              }
            );
            return res;
          },
          async (error, routeId) => {
            if (isRedirectResponse(error)) {
              return error;
            }
            if (isResponse(error)) {
              try {
                error = new ErrorResponseImpl(
                  error.status,
                  error.statusText,
                  await parseResponseBody(error)
                );
              } catch (e) {
                error = e;
              }
            }
            if (isDataWithResponseInit(error)) {
              error = dataWithResponseInitToErrorResponse(error);
            }
            if (renderedStaticContext) {
              if (routeId in renderedStaticContext.loaderData) {
                renderedStaticContext.loaderData[routeId] = void 0;
              }
              let staticContext = getStaticContextFromError(
                dataRoutes,
                renderedStaticContext,
                error,
                skipLoaderErrorBubbling ? routeId : findNearestBoundary(matches, routeId).route.id
              );
              return generateMiddlewareResponse(
                () => Promise.resolve(staticContext)
              );
            } else {
              let boundaryRouteId = skipLoaderErrorBubbling ? routeId : findNearestBoundary(
                matches,
                matches.find(
                  (m) => m.route.id === routeId || m.route.loader
                )?.route.id || routeId
              ).route.id;
              let staticContext = {
                matches,
                location,
                basename,
                loaderData: {},
                actionData: null,
                errors: {
                  [boundaryRouteId]: error
                },
                statusCode: isRouteErrorResponse(error) ? error.status : 500,
                actionHeaders: {},
                loaderHeaders: {}
              };
              return generateMiddlewareResponse(
                () => Promise.resolve(staticContext)
              );
            }
          }
        );
        invariant(isResponse(response), "Expected a response in query()");
        return response;
      } catch (e) {
        if (isResponse(e)) {
          return e;
        }
        throw e;
      }
    }
    let result = await queryImpl(
      request,
      location,
      matches,
      requestContext,
      dataStrategy || null,
      skipLoaderErrorBubbling === true,
      null,
      filterMatchesToLoad || null,
      skipRevalidation === true
    );
    if (isResponse(result)) {
      return result;
    }
    return { location, basename, ...result };
  }
  async function queryRoute(request, {
    routeId,
    requestContext,
    dataStrategy,
    generateMiddlewareResponse,
    unstable_normalizePath
  } = {}) {
    let normalizePath = unstable_normalizePath || defaultNormalizePath;
    let method = request.method;
    let location = createLocation("", normalizePath(request), null, "default");
    let matches = matchRoutes(dataRoutes, location, basename);
    requestContext = requestContext != null ? requestContext : new RouterContextProvider();
    if (!isValidMethod(method) && method !== "HEAD" && method !== "OPTIONS") {
      throw getInternalRouterError(405, { method });
    } else if (!matches) {
      throw getInternalRouterError(404, { pathname: location.pathname });
    }
    let match = routeId ? matches.find((m) => m.route.id === routeId) : getTargetMatch(matches, location);
    if (routeId && !match) {
      throw getInternalRouterError(403, {
        pathname: location.pathname,
        routeId
      });
    } else if (!match) {
      throw getInternalRouterError(404, { pathname: location.pathname });
    }
    if (generateMiddlewareResponse) {
      invariant(
        requestContext instanceof RouterContextProvider,
        "When using middleware in `staticHandler.queryRoute()`, any provided `requestContext` must be an instance of `RouterContextProvider`"
      );
      await loadLazyMiddlewareForMatches(matches, manifest, mapRouteProperties2);
      let response = await runServerMiddlewarePipeline(
        {
          request,
          unstable_url: createDataFunctionUrl(request, location),
          unstable_pattern: getRoutePattern(matches),
          matches,
          params: matches[0].params,
          // If we're calling middleware then it must be enabled so we can cast
          // this to the proper type knowing it's not an `AppLoadContext`
          context: requestContext
        },
        async () => {
          let res = await generateMiddlewareResponse(
            async (innerRequest) => {
              let result2 = await queryImpl(
                innerRequest,
                location,
                matches,
                requestContext,
                dataStrategy || null,
                false,
                match,
                null,
                false
              );
              let processed = handleQueryResult(result2);
              return isResponse(processed) ? processed : typeof processed === "string" ? new Response(processed) : Response.json(processed);
            }
          );
          return res;
        },
        (error) => {
          if (isDataWithResponseInit(error)) {
            return Promise.resolve(dataWithResponseInitToResponse(error));
          }
          if (isResponse(error)) {
            return Promise.resolve(error);
          }
          throw error;
        }
      );
      return response;
    }
    let result = await queryImpl(
      request,
      location,
      matches,
      requestContext,
      dataStrategy || null,
      false,
      match,
      null,
      false
    );
    return handleQueryResult(result);
    function handleQueryResult(result2) {
      if (isResponse(result2)) {
        return result2;
      }
      let error = result2.errors ? Object.values(result2.errors)[0] : void 0;
      if (error !== void 0) {
        throw error;
      }
      if (result2.actionData) {
        return Object.values(result2.actionData)[0];
      }
      if (result2.loaderData) {
        return Object.values(result2.loaderData)[0];
      }
      return void 0;
    }
  }
  async function queryImpl(request, location, matches, requestContext, dataStrategy, skipLoaderErrorBubbling, routeMatch, filterMatchesToLoad, skipRevalidation) {
    invariant(
      request.signal,
      "query()/queryRoute() requests must contain an AbortController signal"
    );
    try {
      if (isMutationMethod(request.method)) {
        let result2 = await submit(
          request,
          location,
          matches,
          routeMatch || getTargetMatch(matches, location),
          requestContext,
          dataStrategy,
          skipLoaderErrorBubbling,
          routeMatch != null,
          filterMatchesToLoad,
          skipRevalidation
        );
        return result2;
      }
      let result = await loadRouteData(
        request,
        location,
        matches,
        requestContext,
        dataStrategy,
        skipLoaderErrorBubbling,
        routeMatch,
        filterMatchesToLoad
      );
      return isResponse(result) ? result : {
        ...result,
        actionData: null,
        actionHeaders: {}
      };
    } catch (e) {
      if (isDataStrategyResult(e) && isResponse(e.result)) {
        if (e.type === "error" /* error */) {
          throw e.result;
        }
        return e.result;
      }
      if (isRedirectResponse(e)) {
        return e;
      }
      throw e;
    }
  }
  async function submit(request, location, matches, actionMatch, requestContext, dataStrategy, skipLoaderErrorBubbling, isRouteRequest, filterMatchesToLoad, skipRevalidation) {
    let result;
    if (!actionMatch.route.action && !actionMatch.route.lazy) {
      let error = getInternalRouterError(405, {
        method: request.method,
        pathname: new URL(request.url).pathname,
        routeId: actionMatch.route.id
      });
      if (isRouteRequest) {
        throw error;
      }
      result = {
        type: "error" /* error */,
        error
      };
    } else {
      let dsMatches = getTargetedDataStrategyMatches(
        mapRouteProperties2,
        manifest,
        request,
        location,
        matches,
        actionMatch,
        [],
        requestContext
      );
      let results = await callDataStrategy(
        request,
        location,
        dsMatches,
        isRouteRequest,
        requestContext,
        dataStrategy
      );
      result = results[actionMatch.route.id];
      if (request.signal.aborted) {
        throwStaticHandlerAbortedError(request, isRouteRequest);
      }
    }
    if (isRedirectResult(result)) {
      throw new Response(null, {
        status: result.response.status,
        headers: {
          Location: result.response.headers.get("Location")
        }
      });
    }
    if (isRouteRequest) {
      if (isErrorResult(result)) {
        throw result.error;
      }
      return {
        matches: [actionMatch],
        loaderData: {},
        actionData: { [actionMatch.route.id]: result.data },
        errors: null,
        // Note: statusCode + headers are unused here since queryRoute will
        // return the raw Response or value
        statusCode: 200,
        loaderHeaders: {},
        actionHeaders: {}
      };
    }
    if (skipRevalidation) {
      if (isErrorResult(result)) {
        let boundaryMatch = skipLoaderErrorBubbling ? actionMatch : findNearestBoundary(matches, actionMatch.route.id);
        return {
          statusCode: isRouteErrorResponse(result.error) ? result.error.status : result.statusCode != null ? result.statusCode : 500,
          actionData: null,
          actionHeaders: {
            ...result.headers ? { [actionMatch.route.id]: result.headers } : {}
          },
          matches,
          loaderData: {},
          errors: {
            [boundaryMatch.route.id]: result.error
          },
          loaderHeaders: {}
        };
      } else {
        return {
          actionData: {
            [actionMatch.route.id]: result.data
          },
          actionHeaders: result.headers ? { [actionMatch.route.id]: result.headers } : {},
          matches,
          loaderData: {},
          errors: null,
          statusCode: result.statusCode || 200,
          loaderHeaders: {}
        };
      }
    }
    let loaderRequest = new Request(request.url, {
      headers: request.headers,
      redirect: request.redirect,
      signal: request.signal
    });
    if (isErrorResult(result)) {
      let boundaryMatch = skipLoaderErrorBubbling ? actionMatch : findNearestBoundary(matches, actionMatch.route.id);
      let handlerContext2 = await loadRouteData(
        loaderRequest,
        location,
        matches,
        requestContext,
        dataStrategy,
        skipLoaderErrorBubbling,
        null,
        filterMatchesToLoad,
        [boundaryMatch.route.id, result]
      );
      return {
        ...handlerContext2,
        statusCode: isRouteErrorResponse(result.error) ? result.error.status : result.statusCode != null ? result.statusCode : 500,
        actionData: null,
        actionHeaders: {
          ...result.headers ? { [actionMatch.route.id]: result.headers } : {}
        }
      };
    }
    let handlerContext = await loadRouteData(
      loaderRequest,
      location,
      matches,
      requestContext,
      dataStrategy,
      skipLoaderErrorBubbling,
      null,
      filterMatchesToLoad
    );
    return {
      ...handlerContext,
      actionData: {
        [actionMatch.route.id]: result.data
      },
      // action status codes take precedence over loader status codes
      ...result.statusCode ? { statusCode: result.statusCode } : {},
      actionHeaders: result.headers ? { [actionMatch.route.id]: result.headers } : {}
    };
  }
  async function loadRouteData(request, location, matches, requestContext, dataStrategy, skipLoaderErrorBubbling, routeMatch, filterMatchesToLoad, pendingActionResult) {
    let isRouteRequest = routeMatch != null;
    if (isRouteRequest && !routeMatch?.route.loader && !routeMatch?.route.lazy) {
      throw getInternalRouterError(400, {
        method: request.method,
        pathname: new URL(request.url).pathname,
        routeId: routeMatch?.route.id
      });
    }
    let dsMatches;
    if (routeMatch) {
      dsMatches = getTargetedDataStrategyMatches(
        mapRouteProperties2,
        manifest,
        request,
        location,
        matches,
        routeMatch,
        [],
        requestContext
      );
    } else {
      let maxIdx = pendingActionResult && isErrorResult(pendingActionResult[1]) ? (
        // Up to but not including the boundary
        matches.findIndex((m) => m.route.id === pendingActionResult[0]) - 1
      ) : void 0;
      let pattern = getRoutePattern(matches);
      dsMatches = matches.map((match, index) => {
        if (maxIdx != null && index > maxIdx) {
          return getDataStrategyMatch(
            mapRouteProperties2,
            manifest,
            request,
            location,
            pattern,
            match,
            [],
            requestContext,
            false
          );
        }
        return getDataStrategyMatch(
          mapRouteProperties2,
          manifest,
          request,
          location,
          pattern,
          match,
          [],
          requestContext,
          (match.route.loader || match.route.lazy) != null && (!filterMatchesToLoad || filterMatchesToLoad(match))
        );
      });
    }
    if (!dataStrategy && !dsMatches.some((m) => m.shouldLoad)) {
      return {
        matches,
        loaderData: {},
        errors: pendingActionResult && isErrorResult(pendingActionResult[1]) ? {
          [pendingActionResult[0]]: pendingActionResult[1].error
        } : null,
        statusCode: 200,
        loaderHeaders: {}
      };
    }
    let results = await callDataStrategy(
      request,
      location,
      dsMatches,
      isRouteRequest,
      requestContext,
      dataStrategy
    );
    if (request.signal.aborted) {
      throwStaticHandlerAbortedError(request, isRouteRequest);
    }
    let handlerContext = processRouteLoaderData(
      matches,
      results,
      pendingActionResult,
      true,
      skipLoaderErrorBubbling
    );
    return {
      ...handlerContext,
      matches
    };
  }
  async function callDataStrategy(request, location, matches, isRouteRequest, requestContext, dataStrategy) {
    let results = await callDataStrategyImpl(
      dataStrategy || defaultDataStrategy,
      request,
      location,
      matches,
      null,
      requestContext,
      true
    );
    let dataResults = {};
    await Promise.all(
      matches.map(async (match) => {
        if (!(match.route.id in results)) {
          return;
        }
        let result = results[match.route.id];
        if (isRedirectDataStrategyResult(result)) {
          let response = result.result;
          throw normalizeRelativeRoutingRedirectResponse(
            response,
            request,
            match.route.id,
            matches,
            basename
          );
        }
        if (isRouteRequest) {
          if (isResponse(result.result)) {
            throw result;
          } else if (isDataWithResponseInit(result.result)) {
            throw dataWithResponseInitToResponse(result.result);
          }
        }
        dataResults[match.route.id] = await convertDataStrategyResultToDataResult(result);
      })
    );
    return dataResults;
  }
  return {
    dataRoutes,
    query,
    queryRoute
  };
}
function getStaticContextFromError(routes, handlerContext, error, boundaryId) {
  let errorBoundaryId = boundaryId || handlerContext._deepestRenderedBoundaryId || routes[0].id;
  return {
    ...handlerContext,
    statusCode: isRouteErrorResponse(error) ? error.status : 500,
    errors: {
      [errorBoundaryId]: error
    }
  };
}
function throwStaticHandlerAbortedError(request, isRouteRequest) {
  if (request.signal.reason !== void 0) {
    throw request.signal.reason;
  }
  let method = isRouteRequest ? "queryRoute" : "query";
  throw new Error(
    `${method}() call aborted without an \`AbortSignal.reason\`: ${request.method} ${request.url}`
  );
}
function isSubmissionNavigation(opts) {
  return opts != null && ("formData" in opts && opts.formData != null || "body" in opts && opts.body !== void 0);
}
function defaultNormalizePath(request) {
  let url = new URL(request.url);
  return {
    pathname: url.pathname,
    search: url.search,
    hash: url.hash
  };
}
function normalizeTo(location, matches, basename, to, fromRouteId, relative) {
  let contextualMatches;
  let activeRouteMatch;
  if (fromRouteId) {
    contextualMatches = [];
    for (let match of matches) {
      contextualMatches.push(match);
      if (match.route.id === fromRouteId) {
        activeRouteMatch = match;
        break;
      }
    }
  } else {
    contextualMatches = matches;
    activeRouteMatch = matches[matches.length - 1];
  }
  let path = resolveTo(
    to ? to : ".",
    getResolveToMatches(contextualMatches),
    stripBasename(location.pathname, basename) || location.pathname,
    relative === "path"
  );
  if (to == null) {
    path.search = location.search;
    path.hash = location.hash;
  }
  if ((to == null || to === "" || to === ".") && activeRouteMatch) {
    let nakedIndex = hasNakedIndexQuery(path.search);
    if (activeRouteMatch.route.index && !nakedIndex) {
      path.search = path.search ? path.search.replace(/^\?/, "?index&") : "?index";
    } else if (!activeRouteMatch.route.index && nakedIndex) {
      let params = new URLSearchParams(path.search);
      let indexValues = params.getAll("index");
      params.delete("index");
      indexValues.filter((v) => v).forEach((v) => params.append("index", v));
      let qs = params.toString();
      path.search = qs ? `?${qs}` : "";
    }
  }
  if (basename !== "/") {
    path.pathname = prependBasename({ basename, pathname: path.pathname });
  }
  return createPath(path);
}
function normalizeNavigateOptions(isFetcher, path, opts) {
  if (!opts || !isSubmissionNavigation(opts)) {
    return { path };
  }
  if (opts.formMethod && !isValidMethod(opts.formMethod)) {
    return {
      path,
      error: getInternalRouterError(405, { method: opts.formMethod })
    };
  }
  let getInvalidBodyError = () => ({
    path,
    error: getInternalRouterError(400, { type: "invalid-body" })
  });
  let rawFormMethod = opts.formMethod || "get";
  let formMethod = rawFormMethod.toUpperCase();
  let formAction = stripHashFromPath(path);
  if (opts.body !== void 0) {
    if (opts.formEncType === "text/plain") {
      if (!isMutationMethod(formMethod)) {
        return getInvalidBodyError();
      }
      let text = typeof opts.body === "string" ? opts.body : opts.body instanceof FormData || opts.body instanceof URLSearchParams ? (
        // https://html.spec.whatwg.org/multipage/form-control-infrastructure.html#plain-text-form-data
        Array.from(opts.body.entries()).reduce(
          (acc, [name, value]) => `${acc}${name}=${value}
`,
          ""
        )
      ) : String(opts.body);
      return {
        path,
        submission: {
          formMethod,
          formAction,
          formEncType: opts.formEncType,
          formData: void 0,
          json: void 0,
          text
        }
      };
    } else if (opts.formEncType === "application/json") {
      if (!isMutationMethod(formMethod)) {
        return getInvalidBodyError();
      }
      try {
        let json = typeof opts.body === "string" ? JSON.parse(opts.body) : opts.body;
        return {
          path,
          submission: {
            formMethod,
            formAction,
            formEncType: opts.formEncType,
            formData: void 0,
            json,
            text: void 0
          }
        };
      } catch (e) {
        return getInvalidBodyError();
      }
    }
  }
  invariant(
    typeof FormData === "function",
    "FormData is not available in this environment"
  );
  let searchParams;
  let formData;
  if (opts.formData) {
    searchParams = convertFormDataToSearchParams(opts.formData);
    formData = opts.formData;
  } else if (opts.body instanceof FormData) {
    searchParams = convertFormDataToSearchParams(opts.body);
    formData = opts.body;
  } else if (opts.body instanceof URLSearchParams) {
    searchParams = opts.body;
    formData = convertSearchParamsToFormData(searchParams);
  } else if (opts.body == null) {
    searchParams = new URLSearchParams();
    formData = new FormData();
  } else {
    try {
      searchParams = new URLSearchParams(opts.body);
      formData = convertSearchParamsToFormData(searchParams);
    } catch (e) {
      return getInvalidBodyError();
    }
  }
  let submission = {
    formMethod,
    formAction,
    formEncType: opts && opts.formEncType || "application/x-www-form-urlencoded",
    formData,
    json: void 0,
    text: void 0
  };
  if (isMutationMethod(submission.formMethod)) {
    return { path, submission };
  }
  let parsedPath = parsePath(path);
  if (isFetcher && parsedPath.search && hasNakedIndexQuery(parsedPath.search)) {
    searchParams.append("index", "");
  }
  parsedPath.search = `?${searchParams}`;
  return { path: createPath(parsedPath), submission };
}
function getMatchesToLoad(request, scopedContext, mapRouteProperties2, manifest, history, state, matches, submission, location, lazyRoutePropertiesToSkip, initialHydration, isRevalidationRequired, cancelledFetcherLoads, fetchersQueuedForDeletion, fetchLoadMatches, fetchRedirectIds, routesToUse, basename, hasPatchRoutesOnNavigation, pendingActionResult, callSiteDefaultShouldRevalidate) {
  let actionResult = pendingActionResult ? isErrorResult(pendingActionResult[1]) ? pendingActionResult[1].error : pendingActionResult[1].data : void 0;
  let currentUrl = history.createURL(state.location);
  let nextUrl = history.createURL(location);
  let maxIdx;
  if (initialHydration && state.errors) {
    let boundaryId = Object.keys(state.errors)[0];
    maxIdx = matches.findIndex((m) => m.route.id === boundaryId);
  } else if (pendingActionResult && isErrorResult(pendingActionResult[1])) {
    let boundaryId = pendingActionResult[0];
    maxIdx = matches.findIndex((m) => m.route.id === boundaryId) - 1;
  }
  let actionStatus = pendingActionResult ? pendingActionResult[1].statusCode : void 0;
  let shouldSkipRevalidation = actionStatus && actionStatus >= 400;
  let baseShouldRevalidateArgs = {
    currentUrl,
    currentParams: state.matches[0]?.params || {},
    nextUrl,
    nextParams: matches[0].params,
    ...submission,
    actionResult,
    actionStatus
  };
  let pattern = getRoutePattern(matches);
  let dsMatches = matches.map((match, index) => {
    let { route } = match;
    let forceShouldLoad = null;
    if (maxIdx != null && index > maxIdx) {
      forceShouldLoad = false;
    } else if (route.lazy) {
      forceShouldLoad = true;
    } else if (!routeHasLoaderOrMiddleware(route)) {
      forceShouldLoad = false;
    } else if (initialHydration) {
      let { shouldLoad: shouldLoad2 } = getRouteHydrationStatus(
        route,
        state.loaderData,
        state.errors
      );
      forceShouldLoad = shouldLoad2;
    } else if (isNewLoader(state.loaderData, state.matches[index], match)) {
      forceShouldLoad = true;
    }
    if (forceShouldLoad !== null) {
      return getDataStrategyMatch(
        mapRouteProperties2,
        manifest,
        request,
        location,
        pattern,
        match,
        lazyRoutePropertiesToSkip,
        scopedContext,
        forceShouldLoad
      );
    }
    let defaultShouldRevalidate = false;
    if (typeof callSiteDefaultShouldRevalidate === "boolean") {
      defaultShouldRevalidate = callSiteDefaultShouldRevalidate;
    } else if (shouldSkipRevalidation) {
      defaultShouldRevalidate = false;
    } else if (isRevalidationRequired) {
      defaultShouldRevalidate = true;
    } else if (currentUrl.pathname + currentUrl.search === nextUrl.pathname + nextUrl.search) {
      defaultShouldRevalidate = true;
    } else if (currentUrl.search !== nextUrl.search) {
      defaultShouldRevalidate = true;
    } else if (isNewRouteInstance(state.matches[index], match)) {
      defaultShouldRevalidate = true;
    }
    let shouldRevalidateArgs = {
      ...baseShouldRevalidateArgs,
      defaultShouldRevalidate
    };
    let shouldLoad = shouldRevalidateLoader(match, shouldRevalidateArgs);
    return getDataStrategyMatch(
      mapRouteProperties2,
      manifest,
      request,
      location,
      pattern,
      match,
      lazyRoutePropertiesToSkip,
      scopedContext,
      shouldLoad,
      shouldRevalidateArgs,
      callSiteDefaultShouldRevalidate
    );
  });
  let revalidatingFetchers = [];
  fetchLoadMatches.forEach((f, key) => {
    if (initialHydration || !matches.some((m) => m.route.id === f.routeId) || fetchersQueuedForDeletion.has(key)) {
      return;
    }
    let fetcher = state.fetchers.get(key);
    let isMidInitialLoad = fetcher && fetcher.state !== "idle" && fetcher.data === void 0;
    let fetcherMatches = matchRoutes(routesToUse, f.path, basename);
    if (!fetcherMatches) {
      if (hasPatchRoutesOnNavigation && isMidInitialLoad) {
        return;
      }
      revalidatingFetchers.push({
        key,
        routeId: f.routeId,
        path: f.path,
        matches: null,
        match: null,
        request: null,
        controller: null
      });
      return;
    }
    if (fetchRedirectIds.has(key)) {
      return;
    }
    let fetcherMatch = getTargetMatch(fetcherMatches, f.path);
    let fetchController = new AbortController();
    let fetchRequest = createClientSideRequest(
      history,
      f.path,
      fetchController.signal
    );
    let fetcherDsMatches = null;
    if (cancelledFetcherLoads.has(key)) {
      cancelledFetcherLoads.delete(key);
      fetcherDsMatches = getTargetedDataStrategyMatches(
        mapRouteProperties2,
        manifest,
        fetchRequest,
        f.path,
        fetcherMatches,
        fetcherMatch,
        lazyRoutePropertiesToSkip,
        scopedContext
      );
    } else if (isMidInitialLoad) {
      if (isRevalidationRequired) {
        fetcherDsMatches = getTargetedDataStrategyMatches(
          mapRouteProperties2,
          manifest,
          fetchRequest,
          f.path,
          fetcherMatches,
          fetcherMatch,
          lazyRoutePropertiesToSkip,
          scopedContext
        );
      }
    } else {
      let defaultShouldRevalidate;
      if (typeof callSiteDefaultShouldRevalidate === "boolean") {
        defaultShouldRevalidate = callSiteDefaultShouldRevalidate;
      } else if (shouldSkipRevalidation) {
        defaultShouldRevalidate = false;
      } else {
        defaultShouldRevalidate = isRevalidationRequired;
      }
      let shouldRevalidateArgs = {
        ...baseShouldRevalidateArgs,
        defaultShouldRevalidate
      };
      if (shouldRevalidateLoader(fetcherMatch, shouldRevalidateArgs)) {
        fetcherDsMatches = getTargetedDataStrategyMatches(
          mapRouteProperties2,
          manifest,
          fetchRequest,
          f.path,
          fetcherMatches,
          fetcherMatch,
          lazyRoutePropertiesToSkip,
          scopedContext,
          shouldRevalidateArgs
        );
      }
    }
    if (fetcherDsMatches) {
      revalidatingFetchers.push({
        key,
        routeId: f.routeId,
        path: f.path,
        matches: fetcherDsMatches,
        match: fetcherMatch,
        request: fetchRequest,
        controller: fetchController
      });
    }
  });
  return { dsMatches, revalidatingFetchers };
}
function routeHasLoaderOrMiddleware(route) {
  return route.loader != null || route.middleware != null && route.middleware.length > 0;
}
function getRouteHydrationStatus(route, loaderData, errors) {
  if (route.lazy) {
    return { shouldLoad: true, renderFallback: true };
  }
  if (!routeHasLoaderOrMiddleware(route)) {
    return { shouldLoad: false, renderFallback: false };
  }
  let hasData = loaderData != null && route.id in loaderData;
  let hasError = errors != null && errors[route.id] !== void 0;
  if (!hasData && hasError) {
    return { shouldLoad: false, renderFallback: false };
  }
  if (typeof route.loader === "function" && route.loader.hydrate === true) {
    return { shouldLoad: true, renderFallback: !hasData };
  }
  let shouldLoad = !hasData && !hasError;
  return { shouldLoad, renderFallback: shouldLoad };
}
function isNewLoader(currentLoaderData, currentMatch, match) {
  let isNew = (
    // [a] -> [a, b]
    !currentMatch || // [a, b] -> [a, c]
    match.route.id !== currentMatch.route.id
  );
  let isMissingData = !currentLoaderData.hasOwnProperty(match.route.id);
  return isNew || isMissingData;
}
function isNewRouteInstance(currentMatch, match) {
  let currentPath = currentMatch.route.path;
  return (
    // param change for this match, /users/123 -> /users/456
    currentMatch.pathname !== match.pathname || // splat param changed, which is not present in match.path
    // e.g. /files/images/avatar.jpg -> files/finances.xls
    currentPath != null && currentPath.endsWith("*") && currentMatch.params["*"] !== match.params["*"]
  );
}
function shouldRevalidateLoader(loaderMatch, arg) {
  if (loaderMatch.route.shouldRevalidate) {
    let routeChoice = loaderMatch.route.shouldRevalidate(arg);
    if (typeof routeChoice === "boolean") {
      return routeChoice;
    }
  }
  return arg.defaultShouldRevalidate;
}
function patchRoutesImpl(routeId, children, routesToUse, manifest, mapRouteProperties2, allowElementMutations) {
  let childrenToPatch;
  if (routeId) {
    let route = manifest[routeId];
    invariant(
      route,
      `No route found to patch children into: routeId = ${routeId}`
    );
    if (!route.children) {
      route.children = [];
    }
    childrenToPatch = route.children;
  } else {
    childrenToPatch = routesToUse;
  }
  let uniqueChildren = [];
  let existingChildren = [];
  children.forEach((newRoute) => {
    let existingRoute = childrenToPatch.find(
      (existingRoute2) => isSameRoute(newRoute, existingRoute2)
    );
    if (existingRoute) {
      existingChildren.push({ existingRoute, newRoute });
    } else {
      uniqueChildren.push(newRoute);
    }
  });
  if (uniqueChildren.length > 0) {
    let newRoutes = convertRoutesToDataRoutes(
      uniqueChildren,
      mapRouteProperties2,
      [routeId || "_", "patch", String(childrenToPatch?.length || "0")],
      manifest
    );
    childrenToPatch.push(...newRoutes);
  }
  if (allowElementMutations && existingChildren.length > 0) {
    for (let i = 0; i < existingChildren.length; i++) {
      let { existingRoute, newRoute } = existingChildren[i];
      let existingRouteTyped = existingRoute;
      let [newRouteTyped] = convertRoutesToDataRoutes(
        [newRoute],
        mapRouteProperties2,
        [],
        // Doesn't matter for mutated routes since they already have an id
        {},
        // Don't touch the manifest here since we're updating in place
        true
      );
      Object.assign(existingRouteTyped, {
        element: newRouteTyped.element ? newRouteTyped.element : existingRouteTyped.element,
        errorElement: newRouteTyped.errorElement ? newRouteTyped.errorElement : existingRouteTyped.errorElement,
        hydrateFallbackElement: newRouteTyped.hydrateFallbackElement ? newRouteTyped.hydrateFallbackElement : existingRouteTyped.hydrateFallbackElement
      });
    }
  }
}
function isSameRoute(newRoute, existingRoute) {
  if ("id" in newRoute && "id" in existingRoute && newRoute.id === existingRoute.id) {
    return true;
  }
  if (!(newRoute.index === existingRoute.index && newRoute.path === existingRoute.path && newRoute.caseSensitive === existingRoute.caseSensitive)) {
    return false;
  }
  if ((!newRoute.children || newRoute.children.length === 0) && (!existingRoute.children || existingRoute.children.length === 0)) {
    return true;
  }
  return newRoute.children?.every(
    (aChild, i) => existingRoute.children?.some((bChild) => isSameRoute(aChild, bChild))
  ) ?? false;
}
var lazyRoutePropertyCache = /* @__PURE__ */ new WeakMap();
var loadLazyRouteProperty = ({
  key,
  route,
  manifest,
  mapRouteProperties: mapRouteProperties2
}) => {
  let routeToUpdate = manifest[route.id];
  invariant(routeToUpdate, "No route found in manifest");
  if (!routeToUpdate.lazy || typeof routeToUpdate.lazy !== "object") {
    return;
  }
  let lazyFn = routeToUpdate.lazy[key];
  if (!lazyFn) {
    return;
  }
  let cache = lazyRoutePropertyCache.get(routeToUpdate);
  if (!cache) {
    cache = {};
    lazyRoutePropertyCache.set(routeToUpdate, cache);
  }
  let cachedPromise = cache[key];
  if (cachedPromise) {
    return cachedPromise;
  }
  let propertyPromise = (async () => {
    let isUnsupported = isUnsupportedLazyRouteObjectKey(key);
    let staticRouteValue = routeToUpdate[key];
    let isStaticallyDefined = staticRouteValue !== void 0 && key !== "hasErrorBoundary";
    if (isUnsupported) {
      warning(
        !isUnsupported,
        "Route property " + key + " is not a supported lazy route property. This property will be ignored."
      );
      cache[key] = Promise.resolve();
    } else if (isStaticallyDefined) {
      warning(
        false,
        `Route "${routeToUpdate.id}" has a static property "${key}" defined. The lazy property will be ignored.`
      );
    } else {
      let value = await lazyFn();
      if (value != null) {
        Object.assign(routeToUpdate, { [key]: value });
        Object.assign(routeToUpdate, mapRouteProperties2(routeToUpdate));
      }
    }
    if (typeof routeToUpdate.lazy === "object") {
      routeToUpdate.lazy[key] = void 0;
      if (Object.values(routeToUpdate.lazy).every((value) => value === void 0)) {
        routeToUpdate.lazy = void 0;
      }
    }
  })();
  cache[key] = propertyPromise;
  return propertyPromise;
};
var lazyRouteFunctionCache = /* @__PURE__ */ new WeakMap();
function loadLazyRoute(route, type, manifest, mapRouteProperties2, lazyRoutePropertiesToSkip) {
  let routeToUpdate = manifest[route.id];
  invariant(routeToUpdate, "No route found in manifest");
  if (!route.lazy) {
    return {
      lazyRoutePromise: void 0,
      lazyHandlerPromise: void 0
    };
  }
  if (typeof route.lazy === "function") {
    let cachedPromise = lazyRouteFunctionCache.get(routeToUpdate);
    if (cachedPromise) {
      return {
        lazyRoutePromise: cachedPromise,
        lazyHandlerPromise: cachedPromise
      };
    }
    let lazyRoutePromise2 = (async () => {
      invariant(
        typeof route.lazy === "function",
        "No lazy route function found"
      );
      let lazyRoute = await route.lazy();
      let routeUpdates = {};
      for (let lazyRouteProperty in lazyRoute) {
        let lazyValue = lazyRoute[lazyRouteProperty];
        if (lazyValue === void 0) {
          continue;
        }
        let isUnsupported = isUnsupportedLazyRouteFunctionKey(lazyRouteProperty);
        let staticRouteValue = routeToUpdate[lazyRouteProperty];
        let isStaticallyDefined = staticRouteValue !== void 0 && // This property isn't static since it should always be updated based
        // on the route updates
        lazyRouteProperty !== "hasErrorBoundary";
        if (isUnsupported) {
          warning(
            !isUnsupported,
            "Route property " + lazyRouteProperty + " is not a supported property to be returned from a lazy route function. This property will be ignored."
          );
        } else if (isStaticallyDefined) {
          warning(
            !isStaticallyDefined,
            `Route "${routeToUpdate.id}" has a static property "${lazyRouteProperty}" defined but its lazy function is also returning a value for this property. The lazy route property "${lazyRouteProperty}" will be ignored.`
          );
        } else {
          routeUpdates[lazyRouteProperty] = lazyValue;
        }
      }
      Object.assign(routeToUpdate, routeUpdates);
      Object.assign(routeToUpdate, {
        // To keep things framework agnostic, we use the provided `mapRouteProperties`
        // function to set the framework-aware properties (`element`/`hasErrorBoundary`)
        // since the logic will differ between frameworks.
        ...mapRouteProperties2(routeToUpdate),
        lazy: void 0
      });
    })();
    lazyRouteFunctionCache.set(routeToUpdate, lazyRoutePromise2);
    lazyRoutePromise2.catch(() => {
    });
    return {
      lazyRoutePromise: lazyRoutePromise2,
      lazyHandlerPromise: lazyRoutePromise2
    };
  }
  let lazyKeys = Object.keys(route.lazy);
  let lazyPropertyPromises = [];
  let lazyHandlerPromise = void 0;
  for (let key of lazyKeys) {
    if (lazyRoutePropertiesToSkip && lazyRoutePropertiesToSkip.includes(key)) {
      continue;
    }
    let promise = loadLazyRouteProperty({
      key,
      route,
      manifest,
      mapRouteProperties: mapRouteProperties2
    });
    if (promise) {
      lazyPropertyPromises.push(promise);
      if (key === type) {
        lazyHandlerPromise = promise;
      }
    }
  }
  let lazyRoutePromise = lazyPropertyPromises.length > 0 ? Promise.all(lazyPropertyPromises).then(() => {
  }) : void 0;
  lazyRoutePromise?.catch(() => {
  });
  lazyHandlerPromise?.catch(() => {
  });
  return {
    lazyRoutePromise,
    lazyHandlerPromise
  };
}
function isNonNullable(value) {
  return value !== void 0;
}
function loadLazyMiddlewareForMatches(matches, manifest, mapRouteProperties2) {
  let promises = matches.map(({ route }) => {
    if (typeof route.lazy !== "object" || !route.lazy.middleware) {
      return void 0;
    }
    return loadLazyRouteProperty({
      key: "middleware",
      route,
      manifest,
      mapRouteProperties: mapRouteProperties2
    });
  }).filter(isNonNullable);
  return promises.length > 0 ? Promise.all(promises) : void 0;
}
async function defaultDataStrategy(args) {
  let matchesToLoad = args.matches.filter((m) => m.shouldLoad);
  let keyedResults = {};
  let results = await Promise.all(matchesToLoad.map((m) => m.resolve()));
  results.forEach((result, i) => {
    keyedResults[matchesToLoad[i].route.id] = result;
  });
  return keyedResults;
}
async function defaultDataStrategyWithMiddleware(args) {
  if (!args.matches.some((m) => m.route.middleware)) {
    return defaultDataStrategy(args);
  }
  return runClientMiddlewarePipeline(args, () => defaultDataStrategy(args));
}
function runServerMiddlewarePipeline(args, handler, errorHandler) {
  return runMiddlewarePipeline(
    args,
    handler,
    processResult,
    isResponse,
    errorHandler
  );
  function processResult(result) {
    return isDataWithResponseInit(result) ? dataWithResponseInitToResponse(result) : result;
  }
}
function runClientMiddlewarePipeline(args, handler) {
  return runMiddlewarePipeline(
    args,
    handler,
    (r) => {
      if (isRedirectResponse(r)) {
        throw r;
      }
      return r;
    },
    isDataStrategyResults,
    errorHandler
  );
  function errorHandler(error, routeId, nextResult) {
    if (nextResult) {
      return Promise.resolve(
        Object.assign(nextResult.value, {
          [routeId]: { type: "error", result: error }
        })
      );
    } else {
      let { matches } = args;
      let maxBoundaryIdx = Math.min(
        // Throwing route
        Math.max(
          matches.findIndex((m) => m.route.id === routeId),
          0
        ),
        // or the shallowest route that needs to load data
        Math.max(
          matches.findIndex((m) => m.shouldCallHandler()),
          0
        )
      );
      let boundaryRouteId = findNearestBoundary(
        matches,
        matches[maxBoundaryIdx].route.id
      ).route.id;
      return Promise.resolve({
        [boundaryRouteId]: { type: "error", result: error }
      });
    }
  }
}
async function runMiddlewarePipeline(args, handler, processResult, isResult, errorHandler) {
  let { matches, ...dataFnArgs } = args;
  let tuples = matches.flatMap(
    (m) => m.route.middleware ? m.route.middleware.map((fn) => [m.route.id, fn]) : []
  );
  let result = await callRouteMiddleware(
    dataFnArgs,
    tuples,
    handler,
    processResult,
    isResult,
    errorHandler
  );
  return result;
}
async function callRouteMiddleware(args, middlewares, handler, processResult, isResult, errorHandler, idx = 0) {
  let { request } = args;
  if (request.signal.aborted) {
    throw request.signal.reason ?? new Error(`Request aborted: ${request.method} ${request.url}`);
  }
  let tuple = middlewares[idx];
  if (!tuple) {
    let result = await handler();
    return result;
  }
  let [routeId, middleware] = tuple;
  let nextResult;
  let next = async () => {
    if (nextResult) {
      throw new Error("You may only call `next()` once per middleware");
    }
    try {
      let result = await callRouteMiddleware(
        args,
        middlewares,
        handler,
        processResult,
        isResult,
        errorHandler,
        idx + 1
      );
      nextResult = { value: result };
      return nextResult.value;
    } catch (error) {
      nextResult = { value: await errorHandler(error, routeId, nextResult) };
      return nextResult.value;
    }
  };
  try {
    let value = await middleware(args, next);
    let result = value != null ? processResult(value) : void 0;
    if (isResult(result)) {
      return result;
    } else if (nextResult) {
      return result ?? nextResult.value;
    } else {
      nextResult = { value: await next() };
      return nextResult.value;
    }
  } catch (error) {
    let response = await errorHandler(error, routeId, nextResult);
    return response;
  }
}
function getDataStrategyMatchLazyPromises(mapRouteProperties2, manifest, request, match, lazyRoutePropertiesToSkip) {
  let lazyMiddlewarePromise = loadLazyRouteProperty({
    key: "middleware",
    route: match.route,
    manifest,
    mapRouteProperties: mapRouteProperties2
  });
  let lazyRoutePromises = loadLazyRoute(
    match.route,
    isMutationMethod(request.method) ? "action" : "loader",
    manifest,
    mapRouteProperties2,
    lazyRoutePropertiesToSkip
  );
  return {
    middleware: lazyMiddlewarePromise,
    route: lazyRoutePromises.lazyRoutePromise,
    handler: lazyRoutePromises.lazyHandlerPromise
  };
}
function getDataStrategyMatch(mapRouteProperties2, manifest, request, path, unstable_pattern, match, lazyRoutePropertiesToSkip, scopedContext, shouldLoad, shouldRevalidateArgs = null, callSiteDefaultShouldRevalidate) {
  let isUsingNewApi = false;
  let _lazyPromises = getDataStrategyMatchLazyPromises(
    mapRouteProperties2,
    manifest,
    request,
    match,
    lazyRoutePropertiesToSkip
  );
  return {
    ...match,
    _lazyPromises,
    shouldLoad,
    shouldRevalidateArgs,
    shouldCallHandler(defaultShouldRevalidate) {
      isUsingNewApi = true;
      if (!shouldRevalidateArgs) {
        return shouldLoad;
      }
      if (typeof callSiteDefaultShouldRevalidate === "boolean") {
        return shouldRevalidateLoader(match, {
          ...shouldRevalidateArgs,
          defaultShouldRevalidate: callSiteDefaultShouldRevalidate
        });
      }
      if (typeof defaultShouldRevalidate === "boolean") {
        return shouldRevalidateLoader(match, {
          ...shouldRevalidateArgs,
          defaultShouldRevalidate
        });
      }
      return shouldRevalidateLoader(match, shouldRevalidateArgs);
    },
    resolve(handlerOverride) {
      let { lazy, loader, middleware } = match.route;
      let callHandler = isUsingNewApi || shouldLoad || handlerOverride && !isMutationMethod(request.method) && (lazy || loader);
      let isMiddlewareOnlyRoute = middleware && middleware.length > 0 && !loader && !lazy;
      if (callHandler && (isMutationMethod(request.method) || !isMiddlewareOnlyRoute)) {
        return callLoaderOrAction({
          request,
          path,
          unstable_pattern,
          match,
          lazyHandlerPromise: _lazyPromises?.handler,
          lazyRoutePromise: _lazyPromises?.route,
          handlerOverride,
          scopedContext
        });
      }
      return Promise.resolve({ type: "data" /* data */, result: void 0 });
    }
  };
}
function getTargetedDataStrategyMatches(mapRouteProperties2, manifest, request, path, matches, targetMatch, lazyRoutePropertiesToSkip, scopedContext, shouldRevalidateArgs = null) {
  return matches.map((match) => {
    if (match.route.id !== targetMatch.route.id) {
      return {
        ...match,
        shouldLoad: false,
        shouldRevalidateArgs,
        shouldCallHandler: () => false,
        _lazyPromises: getDataStrategyMatchLazyPromises(
          mapRouteProperties2,
          manifest,
          request,
          match,
          lazyRoutePropertiesToSkip
        ),
        resolve: () => Promise.resolve({ type: "data", result: void 0 })
      };
    }
    return getDataStrategyMatch(
      mapRouteProperties2,
      manifest,
      request,
      path,
      getRoutePattern(matches),
      match,
      lazyRoutePropertiesToSkip,
      scopedContext,
      true,
      shouldRevalidateArgs
    );
  });
}
async function callDataStrategyImpl(dataStrategyImpl, request, path, matches, fetcherKey, scopedContext, isStaticHandler) {
  if (matches.some((m) => m._lazyPromises?.middleware)) {
    await Promise.all(matches.map((m) => m._lazyPromises?.middleware));
  }
  let dataStrategyArgs = {
    request,
    unstable_url: createDataFunctionUrl(request, path),
    unstable_pattern: getRoutePattern(matches),
    params: matches[0].params,
    context: scopedContext,
    matches
  };
  let runClientMiddleware = isStaticHandler ? () => {
    throw new Error(
      "You cannot call `runClientMiddleware()` from a static handler `dataStrategy`. Middleware is run outside of `dataStrategy` during SSR in order to bubble up the Response.  You can enable middleware via the `respond` API in `query`/`queryRoute`"
    );
  } : (cb) => {
    let typedDataStrategyArgs = dataStrategyArgs;
    return runClientMiddlewarePipeline(typedDataStrategyArgs, () => {
      return cb({
        ...typedDataStrategyArgs,
        fetcherKey,
        runClientMiddleware: () => {
          throw new Error(
            "Cannot call `runClientMiddleware()` from within an `runClientMiddleware` handler"
          );
        }
      });
    });
  };
  let results = await dataStrategyImpl({
    ...dataStrategyArgs,
    fetcherKey,
    runClientMiddleware
  });
  try {
    await Promise.all(
      matches.flatMap((m) => [
        m._lazyPromises?.handler,
        m._lazyPromises?.route
      ])
    );
  } catch (e) {
  }
  return results;
}
async function callLoaderOrAction({
  request,
  path,
  unstable_pattern,
  match,
  lazyHandlerPromise,
  lazyRoutePromise,
  handlerOverride,
  scopedContext
}) {
  let result;
  let onReject;
  let isAction = isMutationMethod(request.method);
  let type = isAction ? "action" : "loader";
  let runHandler = (handler) => {
    let reject;
    let abortPromise = new Promise((_, r) => reject = r);
    onReject = () => reject();
    request.signal.addEventListener("abort", onReject);
    let actualHandler = (ctx) => {
      if (typeof handler !== "function") {
        return Promise.reject(
          new Error(
            `You cannot call the handler for a route which defines a boolean "${type}" [routeId: ${match.route.id}]`
          )
        );
      }
      return handler(
        {
          request,
          unstable_url: createDataFunctionUrl(request, path),
          unstable_pattern,
          params: match.params,
          context: scopedContext
        },
        ...ctx !== void 0 ? [ctx] : []
      );
    };
    let handlerPromise = (async () => {
      try {
        let val = await (handlerOverride ? handlerOverride((ctx) => actualHandler(ctx)) : actualHandler());
        return { type: "data", result: val };
      } catch (e) {
        return { type: "error", result: e };
      }
    })();
    return Promise.race([handlerPromise, abortPromise]);
  };
  try {
    let handler = isAction ? match.route.action : match.route.loader;
    if (lazyHandlerPromise || lazyRoutePromise) {
      if (handler) {
        let handlerError;
        let [value] = await Promise.all([
          // If the handler throws, don't let it immediately bubble out,
          // since we need to let the lazy() execution finish so we know if this
          // route has a boundary that can handle the error
          runHandler(handler).catch((e) => {
            handlerError = e;
          }),
          // Ensure all lazy route promises are resolved before continuing
          lazyHandlerPromise,
          lazyRoutePromise
        ]);
        if (handlerError !== void 0) {
          throw handlerError;
        }
        result = value;
      } else {
        await lazyHandlerPromise;
        let handler2 = isAction ? match.route.action : match.route.loader;
        if (handler2) {
          [result] = await Promise.all([runHandler(handler2), lazyRoutePromise]);
        } else if (type === "action") {
          let url = new URL(request.url);
          let pathname = url.pathname + url.search;
          throw getInternalRouterError(405, {
            method: request.method,
            pathname,
            routeId: match.route.id
          });
        } else {
          return { type: "data" /* data */, result: void 0 };
        }
      }
    } else if (!handler) {
      let url = new URL(request.url);
      let pathname = url.pathname + url.search;
      throw getInternalRouterError(404, {
        pathname
      });
    } else {
      result = await runHandler(handler);
    }
  } catch (e) {
    return { type: "error" /* error */, result: e };
  } finally {
    if (onReject) {
      request.signal.removeEventListener("abort", onReject);
    }
  }
  return result;
}
async function parseResponseBody(response) {
  let contentType = response.headers.get("Content-Type");
  if (contentType && /\bapplication\/json\b/.test(contentType)) {
    return response.body == null ? null : response.json();
  }
  return response.text();
}
async function convertDataStrategyResultToDataResult(dataStrategyResult) {
  let { result, type } = dataStrategyResult;
  if (isResponse(result)) {
    let data2;
    try {
      data2 = await parseResponseBody(result);
    } catch (e) {
      return { type: "error" /* error */, error: e };
    }
    if (type === "error" /* error */) {
      return {
        type: "error" /* error */,
        error: new ErrorResponseImpl(result.status, result.statusText, data2),
        statusCode: result.status,
        headers: result.headers
      };
    }
    return {
      type: "data" /* data */,
      data: data2,
      statusCode: result.status,
      headers: result.headers
    };
  }
  if (type === "error" /* error */) {
    if (isDataWithResponseInit(result)) {
      if (result.data instanceof Error) {
        return {
          type: "error" /* error */,
          error: result.data,
          statusCode: result.init?.status,
          headers: result.init?.headers ? new Headers(result.init.headers) : void 0
        };
      }
      return {
        type: "error" /* error */,
        error: dataWithResponseInitToErrorResponse(result),
        statusCode: isRouteErrorResponse(result) ? result.status : void 0,
        headers: result.init?.headers ? new Headers(result.init.headers) : void 0
      };
    }
    return {
      type: "error" /* error */,
      error: result,
      statusCode: isRouteErrorResponse(result) ? result.status : void 0
    };
  }
  if (isDataWithResponseInit(result)) {
    return {
      type: "data" /* data */,
      data: result.data,
      statusCode: result.init?.status,
      headers: result.init?.headers ? new Headers(result.init.headers) : void 0
    };
  }
  return { type: "data" /* data */, data: result };
}
function normalizeRelativeRoutingRedirectResponse(response, request, routeId, matches, basename) {
  let location = response.headers.get("Location");
  invariant(
    location,
    "Redirects returned/thrown from loaders/actions must have a Location header"
  );
  if (!isAbsoluteUrl(location)) {
    let trimmedMatches = matches.slice(
      0,
      matches.findIndex((m) => m.route.id === routeId) + 1
    );
    location = normalizeTo(
      new URL(request.url),
      trimmedMatches,
      basename,
      location
    );
    response.headers.set("Location", location);
  }
  return response;
}
var invalidProtocols = [
  "about:",
  "blob:",
  "chrome:",
  "chrome-untrusted:",
  "content:",
  "data:",
  "devtools:",
  "file:",
  "filesystem:",
  // eslint-disable-next-line no-script-url
  "javascript:"
];
function normalizeRedirectLocation(location, currentUrl, basename, historyInstance) {
  if (isAbsoluteUrl(location)) {
    let normalizedLocation = location;
    let url = normalizedLocation.startsWith("//") ? new URL(currentUrl.protocol + normalizedLocation) : new URL(normalizedLocation);
    if (invalidProtocols.includes(url.protocol)) {
      throw new Error("Invalid redirect location");
    }
    let isSameBasename = stripBasename(url.pathname, basename) != null;
    if (url.origin === currentUrl.origin && isSameBasename) {
      return url.pathname + url.search + url.hash;
    }
  }
  try {
    let url = historyInstance.createURL(location);
    if (invalidProtocols.includes(url.protocol)) {
      throw new Error("Invalid redirect location");
    }
  } catch (e) {
  }
  return location;
}
function createClientSideRequest(history, location, signal, submission) {
  let url = history.createURL(stripHashFromPath(location)).toString();
  let init = { signal };
  if (submission && isMutationMethod(submission.formMethod)) {
    let { formMethod, formEncType } = submission;
    init.method = formMethod.toUpperCase();
    if (formEncType === "application/json") {
      init.headers = new Headers({ "Content-Type": formEncType });
      init.body = JSON.stringify(submission.json);
    } else if (formEncType === "text/plain") {
      init.body = submission.text;
    } else if (formEncType === "application/x-www-form-urlencoded" && submission.formData) {
      init.body = convertFormDataToSearchParams(submission.formData);
    } else {
      init.body = submission.formData;
    }
  }
  return new Request(url, init);
}
function createDataFunctionUrl(request, path) {
  let url = new URL(request.url);
  let parsed = typeof path === "string" ? parsePath(path) : path;
  url.pathname = parsed.pathname || "/";
  if (parsed.search) {
    let searchParams = new URLSearchParams(parsed.search);
    let indexValues = searchParams.getAll("index");
    searchParams.delete("index");
    for (let value of indexValues.filter(Boolean)) {
      searchParams.append("index", value);
    }
    url.search = searchParams.size ? `?${searchParams.toString()}` : "";
  } else {
    url.search = "";
  }
  url.hash = parsed.hash || "";
  return url;
}
function convertFormDataToSearchParams(formData) {
  let searchParams = new URLSearchParams();
  for (let [key, value] of formData.entries()) {
    searchParams.append(key, typeof value === "string" ? value : value.name);
  }
  return searchParams;
}
function convertSearchParamsToFormData(searchParams) {
  let formData = new FormData();
  for (let [key, value] of searchParams.entries()) {
    formData.append(key, value);
  }
  return formData;
}
function processRouteLoaderData(matches, results, pendingActionResult, isStaticHandler = false, skipLoaderErrorBubbling = false) {
  let loaderData = {};
  let errors = null;
  let statusCode;
  let foundError = false;
  let loaderHeaders = {};
  let pendingError = pendingActionResult && isErrorResult(pendingActionResult[1]) ? pendingActionResult[1].error : void 0;
  matches.forEach((match) => {
    if (!(match.route.id in results)) {
      return;
    }
    let id = match.route.id;
    let result = results[id];
    invariant(
      !isRedirectResult(result),
      "Cannot handle redirect results in processLoaderData"
    );
    if (isErrorResult(result)) {
      let error = result.error;
      if (pendingError !== void 0) {
        error = pendingError;
        pendingError = void 0;
      }
      errors = errors || {};
      if (skipLoaderErrorBubbling) {
        errors[id] = error;
      } else {
        let boundaryMatch = findNearestBoundary(matches, id);
        if (errors[boundaryMatch.route.id] == null) {
          errors[boundaryMatch.route.id] = error;
        }
      }
      if (!isStaticHandler) {
        loaderData[id] = ResetLoaderDataSymbol;
      }
      if (!foundError) {
        foundError = true;
        statusCode = isRouteErrorResponse(result.error) ? result.error.status : 500;
      }
      if (result.headers) {
        loaderHeaders[id] = result.headers;
      }
    } else {
      loaderData[id] = result.data;
      if (result.statusCode && result.statusCode !== 200 && !foundError) {
        statusCode = result.statusCode;
      }
      if (result.headers) {
        loaderHeaders[id] = result.headers;
      }
    }
  });
  if (pendingError !== void 0 && pendingActionResult) {
    errors = { [pendingActionResult[0]]: pendingError };
    if (pendingActionResult[2]) {
      loaderData[pendingActionResult[2]] = void 0;
    }
  }
  return {
    loaderData,
    errors,
    statusCode: statusCode || 200,
    loaderHeaders
  };
}
function processLoaderData(state, matches, results, pendingActionResult, revalidatingFetchers, fetcherResults) {
  let { loaderData, errors } = processRouteLoaderData(
    matches,
    results,
    pendingActionResult
  );
  revalidatingFetchers.filter((f) => !f.matches || f.matches.some((m) => m.shouldLoad)).forEach((rf) => {
    let { key, match, controller } = rf;
    if (controller && controller.signal.aborted) {
      return;
    }
    let result = fetcherResults[key];
    invariant(result, "Did not find corresponding fetcher result");
    if (isErrorResult(result)) {
      let boundaryMatch = findNearestBoundary(state.matches, match?.route.id);
      if (!(errors && errors[boundaryMatch.route.id])) {
        errors = {
          ...errors,
          [boundaryMatch.route.id]: result.error
        };
      }
      state.fetchers.delete(key);
    } else if (isRedirectResult(result)) {
      invariant(false, "Unhandled fetcher revalidation redirect");
    } else {
      let doneFetcher = getDoneFetcher(result.data);
      state.fetchers.set(key, doneFetcher);
    }
  });
  return { loaderData, errors };
}
function mergeLoaderData(loaderData, newLoaderData, matches, errors) {
  let mergedLoaderData = Object.entries(newLoaderData).filter(([, v]) => v !== ResetLoaderDataSymbol).reduce((merged, [k, v]) => {
    merged[k] = v;
    return merged;
  }, {});
  for (let match of matches) {
    let id = match.route.id;
    if (!newLoaderData.hasOwnProperty(id) && loaderData.hasOwnProperty(id) && match.route.loader) {
      mergedLoaderData[id] = loaderData[id];
    }
    if (errors && errors.hasOwnProperty(id)) {
      break;
    }
  }
  return mergedLoaderData;
}
function getActionDataForCommit(pendingActionResult) {
  if (!pendingActionResult) {
    return {};
  }
  return isErrorResult(pendingActionResult[1]) ? {
    // Clear out prior actionData on errors
    actionData: {}
  } : {
    actionData: {
      [pendingActionResult[0]]: pendingActionResult[1].data
    }
  };
}
function findNearestBoundary(matches, routeId) {
  let eligibleMatches = routeId ? matches.slice(0, matches.findIndex((m) => m.route.id === routeId) + 1) : [...matches];
  return eligibleMatches.reverse().find((m) => m.route.hasErrorBoundary === true) || matches[0];
}
function getShortCircuitMatches(routes) {
  let route = routes.length === 1 ? routes[0] : routes.find((r) => r.index || !r.path || r.path === "/") || {
    id: `__shim-error-route__`
  };
  return {
    matches: [
      {
        params: {},
        pathname: "",
        pathnameBase: "",
        route
      }
    ],
    route
  };
}
function getInternalRouterError(status, {
  pathname,
  routeId,
  method,
  type,
  message
} = {}) {
  let statusText = "Unknown Server Error";
  let errorMessage = "Unknown @remix-run/router error";
  if (status === 400) {
    statusText = "Bad Request";
    if (method && pathname && routeId) {
      errorMessage = `You made a ${method} request to "${pathname}" but did not provide a \`loader\` for route "${routeId}", so there is no way to handle the request.`;
    } else if (type === "invalid-body") {
      errorMessage = "Unable to encode submission body";
    }
  } else if (status === 403) {
    statusText = "Forbidden";
    errorMessage = `Route "${routeId}" does not match URL "${pathname}"`;
  } else if (status === 404) {
    statusText = "Not Found";
    errorMessage = `No route matches URL "${pathname}"`;
  } else if (status === 405) {
    statusText = "Method Not Allowed";
    if (method && pathname && routeId) {
      errorMessage = `You made a ${method.toUpperCase()} request to "${pathname}" but did not provide an \`action\` for route "${routeId}", so there is no way to handle the request.`;
    } else if (method) {
      errorMessage = `Invalid request method "${method.toUpperCase()}"`;
    }
  }
  return new ErrorResponseImpl(
    status || 500,
    statusText,
    new Error(errorMessage),
    true
  );
}
function findRedirect(results) {
  let entries = Object.entries(results);
  for (let i = entries.length - 1; i >= 0; i--) {
    let [key, result] = entries[i];
    if (isRedirectResult(result)) {
      return { key, result };
    }
  }
}
function stripHashFromPath(path) {
  let parsedPath = typeof path === "string" ? parsePath(path) : path;
  return createPath({ ...parsedPath, hash: "" });
}
function isHashChangeOnly(a, b) {
  if (a.pathname !== b.pathname || a.search !== b.search) {
    return false;
  }
  if (a.hash === "") {
    return b.hash !== "";
  } else if (a.hash === b.hash) {
    return true;
  } else if (b.hash !== "") {
    return true;
  }
  return false;
}
function dataWithResponseInitToResponse(data2) {
  return Response.json(data2.data, data2.init ?? void 0);
}
function dataWithResponseInitToErrorResponse(data2) {
  return new ErrorResponseImpl(
    data2.init?.status ?? 500,
    data2.init?.statusText ?? "Internal Server Error",
    data2.data
  );
}
function isDataStrategyResults(result) {
  return result != null && typeof result === "object" && Object.entries(result).every(
    ([key, value]) => typeof key === "string" && isDataStrategyResult(value)
  );
}
function isDataStrategyResult(result) {
  return result != null && typeof result === "object" && "type" in result && "result" in result && (result.type === "data" /* data */ || result.type === "error" /* error */);
}
function isRedirectDataStrategyResult(result) {
  return isResponse(result.result) && redirectStatusCodes.has(result.result.status);
}
function isErrorResult(result) {
  return result.type === "error" /* error */;
}
function isRedirectResult(result) {
  return (result && result.type) === "redirect" /* redirect */;
}
function isDataWithResponseInit(value) {
  return typeof value === "object" && value != null && "type" in value && "data" in value && "init" in value && value.type === "DataWithResponseInit";
}
function isResponse(value) {
  return value != null && typeof value.status === "number" && typeof value.statusText === "string" && typeof value.headers === "object" && typeof value.body !== "undefined";
}
function isRedirectStatusCode(statusCode) {
  return redirectStatusCodes.has(statusCode);
}
function isRedirectResponse(result) {
  return isResponse(result) && isRedirectStatusCode(result.status) && result.headers.has("Location");
}
function isValidMethod(method) {
  return validRequestMethods.has(method.toUpperCase());
}
function isMutationMethod(method) {
  return validMutationMethods.has(method.toUpperCase());
}
function hasNakedIndexQuery(search) {
  return new URLSearchParams(search).getAll("index").some((v) => v === "");
}
function getTargetMatch(matches, location) {
  let search = typeof location === "string" ? parsePath(location).search : location.search;
  if (matches[matches.length - 1].route.index && hasNakedIndexQuery(search || "")) {
    return matches[matches.length - 1];
  }
  let pathMatches = getPathContributingMatches(matches);
  return pathMatches[pathMatches.length - 1];
}
function getSubmissionFromNavigation(navigation) {
  let { formMethod, formAction, formEncType, text, formData, json } = navigation;
  if (!formMethod || !formAction || !formEncType) {
    return;
  }
  if (text != null) {
    return {
      formMethod,
      formAction,
      formEncType,
      formData: void 0,
      json: void 0,
      text
    };
  } else if (formData != null) {
    return {
      formMethod,
      formAction,
      formEncType,
      formData,
      json: void 0,
      text: void 0
    };
  } else if (json !== void 0) {
    return {
      formMethod,
      formAction,
      formEncType,
      formData: void 0,
      json,
      text: void 0
    };
  }
}
function getLoadingNavigation(location, submission) {
  if (submission) {
    let navigation = {
      state: "loading",
      location,
      formMethod: submission.formMethod,
      formAction: submission.formAction,
      formEncType: submission.formEncType,
      formData: submission.formData,
      json: submission.json,
      text: submission.text
    };
    return navigation;
  } else {
    let navigation = {
      state: "loading",
      location,
      formMethod: void 0,
      formAction: void 0,
      formEncType: void 0,
      formData: void 0,
      json: void 0,
      text: void 0
    };
    return navigation;
  }
}
function getSubmittingNavigation(location, submission) {
  let navigation = {
    state: "submitting",
    location,
    formMethod: submission.formMethod,
    formAction: submission.formAction,
    formEncType: submission.formEncType,
    formData: submission.formData,
    json: submission.json,
    text: submission.text
  };
  return navigation;
}
function getLoadingFetcher(submission, data2) {
  if (submission) {
    let fetcher = {
      state: "loading",
      formMethod: submission.formMethod,
      formAction: submission.formAction,
      formEncType: submission.formEncType,
      formData: submission.formData,
      json: submission.json,
      text: submission.text,
      data: data2
    };
    return fetcher;
  } else {
    let fetcher = {
      state: "loading",
      formMethod: void 0,
      formAction: void 0,
      formEncType: void 0,
      formData: void 0,
      json: void 0,
      text: void 0,
      data: data2
    };
    return fetcher;
  }
}
function getSubmittingFetcher(submission, existingFetcher) {
  let fetcher = {
    state: "submitting",
    formMethod: submission.formMethod,
    formAction: submission.formAction,
    formEncType: submission.formEncType,
    formData: submission.formData,
    json: submission.json,
    text: submission.text,
    data: existingFetcher ? existingFetcher.data : void 0
  };
  return fetcher;
}
function getDoneFetcher(data2) {
  let fetcher = {
    state: "idle",
    formMethod: void 0,
    formAction: void 0,
    formEncType: void 0,
    formData: void 0,
    json: void 0,
    text: void 0,
    data: data2
  };
  return fetcher;
}
function restoreAppliedTransitions(_window, transitions) {
  try {
    let sessionPositions = _window.sessionStorage.getItem(
      TRANSITIONS_STORAGE_KEY
    );
    if (sessionPositions) {
      let json = JSON.parse(sessionPositions);
      for (let [k, v] of Object.entries(json || {})) {
        if (v && Array.isArray(v)) {
          transitions.set(k, new Set(v || []));
        }
      }
    }
  } catch (e) {
  }
}
function persistAppliedTransitions(_window, transitions) {
  if (transitions.size > 0) {
    let json = {};
    for (let [k, v] of transitions) {
      json[k] = [...v];
    }
    try {
      _window.sessionStorage.setItem(
        TRANSITIONS_STORAGE_KEY,
        JSON.stringify(json)
      );
    } catch (error) {
      warning(
        false,
        `Failed to save applied view transitions in sessionStorage (${error}).`
      );
    }
  }
}
function createDeferred() {
  let resolve;
  let reject;
  let promise = new Promise((res, rej) => {
    resolve = async (val) => {
      res(val);
      try {
        await promise;
      } catch (e) {
      }
    };
    reject = async (error) => {
      rej(error);
      try {
        await promise;
      } catch (e) {
      }
    };
  });
  return {
    promise,
    //@ts-ignore
    resolve,
    //@ts-ignore
    reject
  };
}

// lib/context.ts

var DataRouterContext = react__WEBPACK_IMPORTED_MODULE_0__.createContext(null);
DataRouterContext.displayName = "DataRouter";
var DataRouterStateContext = react__WEBPACK_IMPORTED_MODULE_0__.createContext(null);
DataRouterStateContext.displayName = "DataRouterState";
var RSCRouterContext = react__WEBPACK_IMPORTED_MODULE_0__.createContext(false);
function useIsRSCRouterContext() {
  return react__WEBPACK_IMPORTED_MODULE_0__.useContext(RSCRouterContext);
}
var ViewTransitionContext = react__WEBPACK_IMPORTED_MODULE_0__.createContext({
  isTransitioning: false
});
ViewTransitionContext.displayName = "ViewTransition";
var FetchersContext = react__WEBPACK_IMPORTED_MODULE_0__.createContext(
  /* @__PURE__ */ new Map()
);
FetchersContext.displayName = "Fetchers";
var AwaitContext = react__WEBPACK_IMPORTED_MODULE_0__.createContext(null);
AwaitContext.displayName = "Await";
var AwaitContextProvider = (props) => react__WEBPACK_IMPORTED_MODULE_0__.createElement(AwaitContext.Provider, props);
var NavigationContext = react__WEBPACK_IMPORTED_MODULE_0__.createContext(
  null
);
NavigationContext.displayName = "Navigation";
var LocationContext = react__WEBPACK_IMPORTED_MODULE_0__.createContext(
  null
);
LocationContext.displayName = "Location";
var RouteContext = react__WEBPACK_IMPORTED_MODULE_0__.createContext({
  outlet: null,
  matches: [],
  isDataRoute: false
});
RouteContext.displayName = "Route";
var RouteErrorContext = react__WEBPACK_IMPORTED_MODULE_0__.createContext(null);
RouteErrorContext.displayName = "RouteError";
var ENABLE_DEV_WARNINGS = true;

// lib/hooks.tsx

// lib/errors.ts
var ERROR_DIGEST_BASE = "REACT_ROUTER_ERROR";
var ERROR_DIGEST_REDIRECT = "REDIRECT";
var ERROR_DIGEST_ROUTE_ERROR_RESPONSE = "ROUTE_ERROR_RESPONSE";
function decodeRedirectErrorDigest(digest) {
  if (digest.startsWith(`${ERROR_DIGEST_BASE}:${ERROR_DIGEST_REDIRECT}:{`)) {
    try {
      let parsed = JSON.parse(digest.slice(28));
      if (typeof parsed === "object" && parsed && typeof parsed.status === "number" && typeof parsed.statusText === "string" && typeof parsed.location === "string" && typeof parsed.reloadDocument === "boolean" && typeof parsed.replace === "boolean") {
        return parsed;
      }
    } catch {
    }
  }
}
function decodeRouteErrorResponseDigest(digest) {
  if (digest.startsWith(
    `${ERROR_DIGEST_BASE}:${ERROR_DIGEST_ROUTE_ERROR_RESPONSE}:{`
  )) {
    try {
      let parsed = JSON.parse(digest.slice(40));
      if (typeof parsed === "object" && parsed && typeof parsed.status === "number" && typeof parsed.statusText === "string") {
        return new ErrorResponseImpl(
          parsed.status,
          parsed.statusText,
          parsed.data
        );
      }
    } catch {
    }
  }
}

// lib/hooks.tsx
function useHref(to, { relative } = {}) {
  invariant(
    useInRouterContext(),
    // TODO: This error is probably because they somehow have 2 versions of the
    // router loaded. We can help them understand how to avoid that.
    `useHref() may be used only in the context of a <Router> component.`
  );
  let { basename, navigator } = react__WEBPACK_IMPORTED_MODULE_0__.useContext(NavigationContext);
  let { hash, pathname, search } = useResolvedPath(to, { relative });
  let joinedPathname = pathname;
  if (basename !== "/") {
    joinedPathname = pathname === "/" ? basename : joinPaths([basename, pathname]);
  }
  return navigator.createHref({ pathname: joinedPathname, search, hash });
}
function useInRouterContext() {
  return react__WEBPACK_IMPORTED_MODULE_0__.useContext(LocationContext) != null;
}
function useLocation() {
  invariant(
    useInRouterContext(),
    // TODO: This error is probably because they somehow have 2 versions of the
    // router loaded. We can help them understand how to avoid that.
    `useLocation() may be used only in the context of a <Router> component.`
  );
  return react__WEBPACK_IMPORTED_MODULE_0__.useContext(LocationContext).location;
}
function useNavigationType() {
  return react__WEBPACK_IMPORTED_MODULE_0__.useContext(LocationContext).navigationType;
}
function useMatch(pattern) {
  invariant(
    useInRouterContext(),
    // TODO: This error is probably because they somehow have 2 versions of the
    // router loaded. We can help them understand how to avoid that.
    `useMatch() may be used only in the context of a <Router> component.`
  );
  let { pathname } = useLocation();
  return react__WEBPACK_IMPORTED_MODULE_0__.useMemo(
    () => matchPath(pattern, decodePath(pathname)),
    [pathname, pattern]
  );
}
var navigateEffectWarning = `You should call navigate() in a React.useEffect(), not when your component is first rendered.`;
function useIsomorphicLayoutEffect(cb) {
  let isStatic = react__WEBPACK_IMPORTED_MODULE_0__.useContext(NavigationContext).static;
  if (!isStatic) {
    react__WEBPACK_IMPORTED_MODULE_0__.useLayoutEffect(cb);
  }
}
function useNavigate() {
  let { isDataRoute } = react__WEBPACK_IMPORTED_MODULE_0__.useContext(RouteContext);
  return isDataRoute ? useNavigateStable() : useNavigateUnstable();
}
function useNavigateUnstable() {
  invariant(
    useInRouterContext(),
    // TODO: This error is probably because they somehow have 2 versions of the
    // router loaded. We can help them understand how to avoid that.
    `useNavigate() may be used only in the context of a <Router> component.`
  );
  let dataRouterContext = react__WEBPACK_IMPORTED_MODULE_0__.useContext(DataRouterContext);
  let { basename, navigator } = react__WEBPACK_IMPORTED_MODULE_0__.useContext(NavigationContext);
  let { matches } = react__WEBPACK_IMPORTED_MODULE_0__.useContext(RouteContext);
  let { pathname: locationPathname } = useLocation();
  let routePathnamesJson = JSON.stringify(getResolveToMatches(matches));
  let activeRef = react__WEBPACK_IMPORTED_MODULE_0__.useRef(false);
  useIsomorphicLayoutEffect(() => {
    activeRef.current = true;
  });
  let navigate = react__WEBPACK_IMPORTED_MODULE_0__.useCallback(
    (to, options = {}) => {
      warning(activeRef.current, navigateEffectWarning);
      if (!activeRef.current) return;
      if (typeof to === "number") {
        navigator.go(to);
        return;
      }
      let path = resolveTo(
        to,
        JSON.parse(routePathnamesJson),
        locationPathname,
        options.relative === "path"
      );
      if (dataRouterContext == null && basename !== "/") {
        path.pathname = path.pathname === "/" ? basename : joinPaths([basename, path.pathname]);
      }
      (!!options.replace ? navigator.replace : navigator.push)(
        path,
        options.state,
        options
      );
    },
    [
      basename,
      navigator,
      routePathnamesJson,
      locationPathname,
      dataRouterContext
    ]
  );
  return navigate;
}
var OutletContext = react__WEBPACK_IMPORTED_MODULE_0__.createContext(null);
function useOutletContext() {
  return react__WEBPACK_IMPORTED_MODULE_0__.useContext(OutletContext);
}
function useOutlet(context) {
  let outlet = react__WEBPACK_IMPORTED_MODULE_0__.useContext(RouteContext).outlet;
  return react__WEBPACK_IMPORTED_MODULE_0__.useMemo(
    () => outlet && /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement(OutletContext.Provider, { value: context }, outlet),
    [outlet, context]
  );
}
function useParams() {
  let { matches } = react__WEBPACK_IMPORTED_MODULE_0__.useContext(RouteContext);
  let routeMatch = matches[matches.length - 1];
  return routeMatch ? routeMatch.params : {};
}
function useResolvedPath(to, { relative } = {}) {
  let { matches } = react__WEBPACK_IMPORTED_MODULE_0__.useContext(RouteContext);
  let { pathname: locationPathname } = useLocation();
  let routePathnamesJson = JSON.stringify(getResolveToMatches(matches));
  return react__WEBPACK_IMPORTED_MODULE_0__.useMemo(
    () => resolveTo(
      to,
      JSON.parse(routePathnamesJson),
      locationPathname,
      relative === "path"
    ),
    [to, routePathnamesJson, locationPathname, relative]
  );
}
function useRoutes(routes, locationArg) {
  return useRoutesImpl(routes, locationArg);
}
function useRoutesImpl(routes, locationArg, dataRouterOpts) {
  invariant(
    useInRouterContext(),
    // TODO: This error is probably because they somehow have 2 versions of the
    // router loaded. We can help them understand how to avoid that.
    `useRoutes() may be used only in the context of a <Router> component.`
  );
  let { navigator } = react__WEBPACK_IMPORTED_MODULE_0__.useContext(NavigationContext);
  let { matches: parentMatches } = react__WEBPACK_IMPORTED_MODULE_0__.useContext(RouteContext);
  let routeMatch = parentMatches[parentMatches.length - 1];
  let parentParams = routeMatch ? routeMatch.params : {};
  let parentPathname = routeMatch ? routeMatch.pathname : "/";
  let parentPathnameBase = routeMatch ? routeMatch.pathnameBase : "/";
  let parentRoute = routeMatch && routeMatch.route;
  if (ENABLE_DEV_WARNINGS) {
    let parentPath = parentRoute && parentRoute.path || "";
    warningOnce(
      parentPathname,
      !parentRoute || parentPath.endsWith("*") || parentPath.endsWith("*?"),
      `You rendered descendant <Routes> (or called \`useRoutes()\`) at "${parentPathname}" (under <Route path="${parentPath}">) but the parent route path has no trailing "*". This means if you navigate deeper, the parent won't match anymore and therefore the child routes will never render.

Please change the parent <Route path="${parentPath}"> to <Route path="${parentPath === "/" ? "*" : `${parentPath}/*`}">.`
    );
  }
  let locationFromContext = useLocation();
  let location;
  if (locationArg) {
    let parsedLocationArg = typeof locationArg === "string" ? parsePath(locationArg) : locationArg;
    invariant(
      parentPathnameBase === "/" || parsedLocationArg.pathname?.startsWith(parentPathnameBase),
      `When overriding the location using \`<Routes location>\` or \`useRoutes(routes, location)\`, the location pathname must begin with the portion of the URL pathname that was matched by all parent routes. The current pathname base is "${parentPathnameBase}" but pathname "${parsedLocationArg.pathname}" was given in the \`location\` prop.`
    );
    location = parsedLocationArg;
  } else {
    location = locationFromContext;
  }
  let pathname = location.pathname || "/";
  let remainingPathname = pathname;
  if (parentPathnameBase !== "/") {
    let parentSegments = parentPathnameBase.replace(/^\//, "").split("/");
    let segments = pathname.replace(/^\//, "").split("/");
    remainingPathname = "/" + segments.slice(parentSegments.length).join("/");
  }
  let matches = matchRoutes(routes, { pathname: remainingPathname });
  if (ENABLE_DEV_WARNINGS) {
    warning(
      parentRoute || matches != null,
      `No routes matched location "${location.pathname}${location.search}${location.hash}" `
    );
    warning(
      matches == null || matches[matches.length - 1].route.element !== void 0 || matches[matches.length - 1].route.Component !== void 0 || matches[matches.length - 1].route.lazy !== void 0,
      `Matched leaf route at location "${location.pathname}${location.search}${location.hash}" does not have an element or Component. This means it will render an <Outlet /> with a null value by default resulting in an "empty" page.`
    );
  }
  let renderedMatches = _renderMatches(
    matches && matches.map(
      (match) => Object.assign({}, match, {
        params: Object.assign({}, parentParams, match.params),
        pathname: joinPaths([
          parentPathnameBase,
          // Re-encode pathnames that were decoded inside matchRoutes.
          // Pre-encode `%`, `?` and `#` ahead of `encodeLocation` because it uses
          // `new URL()` internally and we need to prevent it from treating
          // them as separators
          navigator.encodeLocation ? navigator.encodeLocation(
            match.pathname.replace(/%/g, "%25").replace(/\?/g, "%3F").replace(/#/g, "%23")
          ).pathname : match.pathname
        ]),
        pathnameBase: match.pathnameBase === "/" ? parentPathnameBase : joinPaths([
          parentPathnameBase,
          // Re-encode pathnames that were decoded inside matchRoutes
          // Pre-encode `%`, `?` and `#` ahead of `encodeLocation` because it uses
          // `new URL()` internally and we need to prevent it from treating
          // them as separators
          navigator.encodeLocation ? navigator.encodeLocation(
            match.pathnameBase.replace(/%/g, "%25").replace(/\?/g, "%3F").replace(/#/g, "%23")
          ).pathname : match.pathnameBase
        ])
      })
    ),
    parentMatches,
    dataRouterOpts
  );
  if (locationArg && renderedMatches) {
    return /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement(
      LocationContext.Provider,
      {
        value: {
          location: {
            pathname: "/",
            search: "",
            hash: "",
            state: null,
            key: "default",
            unstable_mask: void 0,
            ...location
          },
          navigationType: "POP" /* Pop */
        }
      },
      renderedMatches
    );
  }
  return renderedMatches;
}
function DefaultErrorComponent() {
  let error = useRouteError();
  let message = isRouteErrorResponse(error) ? `${error.status} ${error.statusText}` : error instanceof Error ? error.message : JSON.stringify(error);
  let stack = error instanceof Error ? error.stack : null;
  let lightgrey = "rgba(200,200,200, 0.5)";
  let preStyles = { padding: "0.5rem", backgroundColor: lightgrey };
  let codeStyles = { padding: "2px 4px", backgroundColor: lightgrey };
  let devInfo = null;
  if (ENABLE_DEV_WARNINGS) {
    console.error(
      "Error handled by React Router default ErrorBoundary:",
      error
    );
    devInfo = /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement(react__WEBPACK_IMPORTED_MODULE_0__.Fragment, null, /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement("p", null, "\u{1F4BF} Hey developer \u{1F44B}"), /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement("p", null, "You can provide a way better UX than this when your app throws errors by providing your own ", /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement("code", { style: codeStyles }, "ErrorBoundary"), " or", " ", /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement("code", { style: codeStyles }, "errorElement"), " prop on your route."));
  }
  return /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement(react__WEBPACK_IMPORTED_MODULE_0__.Fragment, null, /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement("h2", null, "Unexpected Application Error!"), /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement("h3", { style: { fontStyle: "italic" } }, message), stack ? /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement("pre", { style: preStyles }, stack) : null, devInfo);
}
var defaultErrorElement = /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement(DefaultErrorComponent, null);
var RenderErrorBoundary = class extends react__WEBPACK_IMPORTED_MODULE_0__.Component {
  constructor(props) {
    super(props);
    this.state = {
      location: props.location,
      revalidation: props.revalidation,
      error: props.error
    };
  }
  static getDerivedStateFromError(error) {
    return { error };
  }
  static getDerivedStateFromProps(props, state) {
    if (state.location !== props.location || state.revalidation !== "idle" && props.revalidation === "idle") {
      return {
        error: props.error,
        location: props.location,
        revalidation: props.revalidation
      };
    }
    return {
      error: props.error !== void 0 ? props.error : state.error,
      location: state.location,
      revalidation: props.revalidation || state.revalidation
    };
  }
  componentDidCatch(error, errorInfo) {
    if (this.props.onError) {
      this.props.onError(error, errorInfo);
    } else {
      console.error(
        "React Router caught the following error during render",
        error
      );
    }
  }
  render() {
    let error = this.state.error;
    if (this.context && typeof error === "object" && error && "digest" in error && typeof error.digest === "string") {
      const decoded = decodeRouteErrorResponseDigest(error.digest);
      if (decoded) error = decoded;
    }
    let result = error !== void 0 ? /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement(RouteContext.Provider, { value: this.props.routeContext }, /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement(
      RouteErrorContext.Provider,
      {
        value: error,
        children: this.props.component
      }
    )) : this.props.children;
    if (this.context) {
      return /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement(RSCErrorHandler, { error }, result);
    }
    return result;
  }
};
RenderErrorBoundary.contextType = RSCRouterContext;
var errorRedirectHandledMap = /* @__PURE__ */ new WeakMap();
function RSCErrorHandler({
  children,
  error
}) {
  let { basename } = react__WEBPACK_IMPORTED_MODULE_0__.useContext(NavigationContext);
  if (typeof error === "object" && error && "digest" in error && typeof error.digest === "string") {
    let redirect2 = decodeRedirectErrorDigest(error.digest);
    if (redirect2) {
      let existingRedirect = errorRedirectHandledMap.get(error);
      if (existingRedirect) throw existingRedirect;
      let parsed = parseToInfo(redirect2.location, basename);
      if (isBrowser && !errorRedirectHandledMap.get(error)) {
        if (parsed.isExternal || redirect2.reloadDocument) {
          window.location.href = parsed.absoluteURL || parsed.to;
        } else {
          const redirectPromise = Promise.resolve().then(
            () => window.__reactRouterDataRouter.navigate(parsed.to, {
              replace: redirect2.replace
            })
          );
          errorRedirectHandledMap.set(error, redirectPromise);
          throw redirectPromise;
        }
      }
      return /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement(
        "meta",
        {
          httpEquiv: "refresh",
          content: `0;url=${parsed.absoluteURL || parsed.to}`
        }
      );
    }
  }
  return children;
}
function RenderedRoute({ routeContext, match, children }) {
  let dataRouterContext = react__WEBPACK_IMPORTED_MODULE_0__.useContext(DataRouterContext);
  if (dataRouterContext && dataRouterContext.static && dataRouterContext.staticContext && (match.route.errorElement || match.route.ErrorBoundary)) {
    dataRouterContext.staticContext._deepestRenderedBoundaryId = match.route.id;
  }
  return /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement(RouteContext.Provider, { value: routeContext }, children);
}
function _renderMatches(matches, parentMatches = [], dataRouterOpts) {
  let dataRouterState = dataRouterOpts?.state;
  if (matches == null) {
    if (!dataRouterState) {
      return null;
    }
    if (dataRouterState.errors) {
      matches = dataRouterState.matches;
    } else if (parentMatches.length === 0 && !dataRouterState.initialized && dataRouterState.matches.length > 0) {
      matches = dataRouterState.matches;
    } else {
      return null;
    }
  }
  let renderedMatches = matches;
  let errors = dataRouterState?.errors;
  if (errors != null) {
    let errorIndex = renderedMatches.findIndex(
      (m) => m.route.id && errors?.[m.route.id] !== void 0
    );
    invariant(
      errorIndex >= 0,
      `Could not find a matching route for errors on route IDs: ${Object.keys(
        errors
      ).join(",")}`
    );
    renderedMatches = renderedMatches.slice(
      0,
      Math.min(renderedMatches.length, errorIndex + 1)
    );
  }
  let renderFallback = false;
  let fallbackIndex = -1;
  if (dataRouterOpts && dataRouterState) {
    renderFallback = dataRouterState.renderFallback;
    for (let i = 0; i < renderedMatches.length; i++) {
      let match = renderedMatches[i];
      if (match.route.HydrateFallback || match.route.hydrateFallbackElement) {
        fallbackIndex = i;
      }
      if (match.route.id) {
        let { loaderData, errors: errors2 } = dataRouterState;
        let needsToRunLoader = match.route.loader && !loaderData.hasOwnProperty(match.route.id) && (!errors2 || errors2[match.route.id] === void 0);
        if (match.route.lazy || needsToRunLoader) {
          if (dataRouterOpts.isStatic) {
            renderFallback = true;
          }
          if (fallbackIndex >= 0) {
            renderedMatches = renderedMatches.slice(0, fallbackIndex + 1);
          } else {
            renderedMatches = [renderedMatches[0]];
          }
          break;
        }
      }
    }
  }
  let onErrorHandler = dataRouterOpts?.onError;
  let onError = dataRouterState && onErrorHandler ? (error, errorInfo) => {
    onErrorHandler(error, {
      location: dataRouterState.location,
      params: dataRouterState.matches?.[0]?.params ?? {},
      unstable_pattern: getRoutePattern(dataRouterState.matches),
      errorInfo
    });
  } : void 0;
  return renderedMatches.reduceRight(
    (outlet, match, index) => {
      let error;
      let shouldRenderHydrateFallback = false;
      let errorElement = null;
      let hydrateFallbackElement = null;
      if (dataRouterState) {
        error = errors && match.route.id ? errors[match.route.id] : void 0;
        errorElement = match.route.errorElement || defaultErrorElement;
        if (renderFallback) {
          if (fallbackIndex < 0 && index === 0) {
            warningOnce(
              "route-fallback",
              false,
              "No `HydrateFallback` element provided to render during initial hydration"
            );
            shouldRenderHydrateFallback = true;
            hydrateFallbackElement = null;
          } else if (fallbackIndex === index) {
            shouldRenderHydrateFallback = true;
            hydrateFallbackElement = match.route.hydrateFallbackElement || null;
          }
        }
      }
      let matches2 = parentMatches.concat(renderedMatches.slice(0, index + 1));
      let getChildren = () => {
        let children;
        if (error) {
          children = errorElement;
        } else if (shouldRenderHydrateFallback) {
          children = hydrateFallbackElement;
        } else if (match.route.Component) {
          children = /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement(match.route.Component, null);
        } else if (match.route.element) {
          children = match.route.element;
        } else {
          children = outlet;
        }
        return /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement(
          RenderedRoute,
          {
            match,
            routeContext: {
              outlet,
              matches: matches2,
              isDataRoute: dataRouterState != null
            },
            children
          }
        );
      };
      return dataRouterState && (match.route.ErrorBoundary || match.route.errorElement || index === 0) ? /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement(
        RenderErrorBoundary,
        {
          location: dataRouterState.location,
          revalidation: dataRouterState.revalidation,
          component: errorElement,
          error,
          children: getChildren(),
          routeContext: { outlet: null, matches: matches2, isDataRoute: true },
          onError
        }
      ) : getChildren();
    },
    null
  );
}
function getDataRouterConsoleError(hookName) {
  return `${hookName} must be used within a data router.  See https://reactrouter.com/en/main/routers/picking-a-router.`;
}
function useDataRouterContext(hookName) {
  let ctx = react__WEBPACK_IMPORTED_MODULE_0__.useContext(DataRouterContext);
  invariant(ctx, getDataRouterConsoleError(hookName));
  return ctx;
}
function useDataRouterState(hookName) {
  let state = react__WEBPACK_IMPORTED_MODULE_0__.useContext(DataRouterStateContext);
  invariant(state, getDataRouterConsoleError(hookName));
  return state;
}
function useRouteContext(hookName) {
  let route = react__WEBPACK_IMPORTED_MODULE_0__.useContext(RouteContext);
  invariant(route, getDataRouterConsoleError(hookName));
  return route;
}
function useCurrentRouteId(hookName) {
  let route = useRouteContext(hookName);
  let thisRoute = route.matches[route.matches.length - 1];
  invariant(
    thisRoute.route.id,
    `${hookName} can only be used on routes that contain a unique "id"`
  );
  return thisRoute.route.id;
}
function useRouteId() {
  return useCurrentRouteId("useRouteId" /* UseRouteId */);
}
function useNavigation() {
  let state = useDataRouterState("useNavigation" /* UseNavigation */);
  return state.navigation;
}
function useRevalidator() {
  let dataRouterContext = useDataRouterContext("useRevalidator" /* UseRevalidator */);
  let state = useDataRouterState("useRevalidator" /* UseRevalidator */);
  let revalidate = react__WEBPACK_IMPORTED_MODULE_0__.useCallback(async () => {
    await dataRouterContext.router.revalidate();
  }, [dataRouterContext.router]);
  return react__WEBPACK_IMPORTED_MODULE_0__.useMemo(
    () => ({ revalidate, state: state.revalidation }),
    [revalidate, state.revalidation]
  );
}
function useMatches() {
  let { matches, loaderData } = useDataRouterState(
    "useMatches" /* UseMatches */
  );
  return react__WEBPACK_IMPORTED_MODULE_0__.useMemo(
    () => matches.map((m) => convertRouteMatchToUiMatch(m, loaderData)),
    [matches, loaderData]
  );
}
function useLoaderData() {
  let state = useDataRouterState("useLoaderData" /* UseLoaderData */);
  let routeId = useCurrentRouteId("useLoaderData" /* UseLoaderData */);
  return state.loaderData[routeId];
}
function useRouteLoaderData(routeId) {
  let state = useDataRouterState("useRouteLoaderData" /* UseRouteLoaderData */);
  return state.loaderData[routeId];
}
function useActionData() {
  let state = useDataRouterState("useActionData" /* UseActionData */);
  let routeId = useCurrentRouteId("useLoaderData" /* UseLoaderData */);
  return state.actionData ? state.actionData[routeId] : void 0;
}
function useRouteError() {
  let error = react__WEBPACK_IMPORTED_MODULE_0__.useContext(RouteErrorContext);
  let state = useDataRouterState("useRouteError" /* UseRouteError */);
  let routeId = useCurrentRouteId("useRouteError" /* UseRouteError */);
  if (error !== void 0) {
    return error;
  }
  return state.errors?.[routeId];
}
function useAsyncValue() {
  let value = react__WEBPACK_IMPORTED_MODULE_0__.useContext(AwaitContext);
  return value?._data;
}
function useAsyncError() {
  let value = react__WEBPACK_IMPORTED_MODULE_0__.useContext(AwaitContext);
  return value?._error;
}
var blockerId = 0;
function useBlocker(shouldBlock) {
  let { router, basename } = useDataRouterContext("useBlocker" /* UseBlocker */);
  let state = useDataRouterState("useBlocker" /* UseBlocker */);
  let [blockerKey, setBlockerKey] = react__WEBPACK_IMPORTED_MODULE_0__.useState("");
  let blockerFunction = react__WEBPACK_IMPORTED_MODULE_0__.useCallback(
    (arg) => {
      if (typeof shouldBlock !== "function") {
        return !!shouldBlock;
      }
      if (basename === "/") {
        return shouldBlock(arg);
      }
      let { currentLocation, nextLocation, historyAction } = arg;
      return shouldBlock({
        currentLocation: {
          ...currentLocation,
          pathname: stripBasename(currentLocation.pathname, basename) || currentLocation.pathname
        },
        nextLocation: {
          ...nextLocation,
          pathname: stripBasename(nextLocation.pathname, basename) || nextLocation.pathname
        },
        historyAction
      });
    },
    [basename, shouldBlock]
  );
  react__WEBPACK_IMPORTED_MODULE_0__.useEffect(() => {
    let key = String(++blockerId);
    setBlockerKey(key);
    return () => router.deleteBlocker(key);
  }, [router]);
  react__WEBPACK_IMPORTED_MODULE_0__.useEffect(() => {
    if (blockerKey !== "") {
      router.getBlocker(blockerKey, blockerFunction);
    }
  }, [router, blockerKey, blockerFunction]);
  return blockerKey && state.blockers.has(blockerKey) ? state.blockers.get(blockerKey) : IDLE_BLOCKER;
}
function useNavigateStable() {
  let { router } = useDataRouterContext("useNavigate" /* UseNavigateStable */);
  let id = useCurrentRouteId("useNavigate" /* UseNavigateStable */);
  let activeRef = react__WEBPACK_IMPORTED_MODULE_0__.useRef(false);
  useIsomorphicLayoutEffect(() => {
    activeRef.current = true;
  });
  let navigate = react__WEBPACK_IMPORTED_MODULE_0__.useCallback(
    async (to, options = {}) => {
      warning(activeRef.current, navigateEffectWarning);
      if (!activeRef.current) return;
      if (typeof to === "number") {
        await router.navigate(to);
      } else {
        await router.navigate(to, { fromRouteId: id, ...options });
      }
    },
    [router, id]
  );
  return navigate;
}
var alreadyWarned = {};
function warningOnce(key, cond, message) {
  if (!cond && !alreadyWarned[key]) {
    alreadyWarned[key] = true;
    warning(false, message);
  }
}
function useRoute(...args) {
  const currentRouteId = useCurrentRouteId(
    "useRoute" /* UseRoute */
  );
  const id = args[0] ?? currentRouteId;
  const state = useDataRouterState("useRoute" /* UseRoute */);
  const route = state.matches.find(({ route: route2 }) => route2.id === id);
  if (route === void 0) return void 0;
  return {
    handle: route.route.handle,
    loaderData: state.loaderData[id],
    actionData: state.actionData?.[id]
  };
}

// lib/components.tsx

// lib/server-runtime/warnings.ts
var alreadyWarned2 = {};
function warnOnce(condition, message) {
  if (!condition && !alreadyWarned2[message]) {
    alreadyWarned2[message] = true;
    console.warn(message);
  }
}

// lib/components.tsx
var USE_OPTIMISTIC = "useOptimistic";
var useOptimisticImpl = /*#__PURE__*/ (react__WEBPACK_IMPORTED_MODULE_0___namespace_cache || (react__WEBPACK_IMPORTED_MODULE_0___namespace_cache = __webpack_require__.t(react__WEBPACK_IMPORTED_MODULE_0__, 2)))[USE_OPTIMISTIC];
var stableUseOptimisticSetter = () => void 0;
function useOptimisticSafe(val) {
  if (useOptimisticImpl) {
    return useOptimisticImpl(val);
  } else {
    return [val, stableUseOptimisticSetter];
  }
}
function mapRouteProperties(route) {
  let updates = {
    // Note: this check also occurs in createRoutesFromChildren so update
    // there if you change this -- please and thank you!
    hasErrorBoundary: route.hasErrorBoundary || route.ErrorBoundary != null || route.errorElement != null
  };
  if (route.Component) {
    if (ENABLE_DEV_WARNINGS) {
      if (route.element) {
        warning(
          false,
          "You should not include both `Component` and `element` on your route - `Component` will be used."
        );
      }
    }
    Object.assign(updates, {
      element: react__WEBPACK_IMPORTED_MODULE_0__.createElement(route.Component),
      Component: void 0
    });
  }
  if (route.HydrateFallback) {
    if (ENABLE_DEV_WARNINGS) {
      if (route.hydrateFallbackElement) {
        warning(
          false,
          "You should not include both `HydrateFallback` and `hydrateFallbackElement` on your route - `HydrateFallback` will be used."
        );
      }
    }
    Object.assign(updates, {
      hydrateFallbackElement: react__WEBPACK_IMPORTED_MODULE_0__.createElement(route.HydrateFallback),
      HydrateFallback: void 0
    });
  }
  if (route.ErrorBoundary) {
    if (ENABLE_DEV_WARNINGS) {
      if (route.errorElement) {
        warning(
          false,
          "You should not include both `ErrorBoundary` and `errorElement` on your route - `ErrorBoundary` will be used."
        );
      }
    }
    Object.assign(updates, {
      errorElement: react__WEBPACK_IMPORTED_MODULE_0__.createElement(route.ErrorBoundary),
      ErrorBoundary: void 0
    });
  }
  return updates;
}
var hydrationRouteProperties = [
  "HydrateFallback",
  "hydrateFallbackElement"
];
function createMemoryRouter(routes, opts) {
  return createRouter({
    basename: opts?.basename,
    getContext: opts?.getContext,
    future: opts?.future,
    history: createMemoryHistory({
      initialEntries: opts?.initialEntries,
      initialIndex: opts?.initialIndex
    }),
    hydrationData: opts?.hydrationData,
    routes,
    hydrationRouteProperties,
    mapRouteProperties,
    dataStrategy: opts?.dataStrategy,
    patchRoutesOnNavigation: opts?.patchRoutesOnNavigation,
    unstable_instrumentations: opts?.unstable_instrumentations
  }).initialize();
}
var Deferred = class {
  constructor() {
    this.status = "pending";
    this.promise = new Promise((resolve, reject) => {
      this.resolve = (value) => {
        if (this.status === "pending") {
          this.status = "resolved";
          resolve(value);
        }
      };
      this.reject = (reason) => {
        if (this.status === "pending") {
          this.status = "rejected";
          reject(reason);
        }
      };
    });
  }
};
function RouterProvider({
  router,
  flushSync: reactDomFlushSyncImpl,
  onError,
  unstable_useTransitions
}) {
  let unstable_rsc = useIsRSCRouterContext();
  unstable_useTransitions = unstable_rsc || unstable_useTransitions;
  let [_state, setStateImpl] = react__WEBPACK_IMPORTED_MODULE_0__.useState(router.state);
  let [state, setOptimisticState] = useOptimisticSafe(_state);
  let [pendingState, setPendingState] = react__WEBPACK_IMPORTED_MODULE_0__.useState();
  let [vtContext, setVtContext] = react__WEBPACK_IMPORTED_MODULE_0__.useState({
    isTransitioning: false
  });
  let [renderDfd, setRenderDfd] = react__WEBPACK_IMPORTED_MODULE_0__.useState();
  let [transition, setTransition] = react__WEBPACK_IMPORTED_MODULE_0__.useState();
  let [interruption, setInterruption] = react__WEBPACK_IMPORTED_MODULE_0__.useState();
  let fetcherData = react__WEBPACK_IMPORTED_MODULE_0__.useRef(/* @__PURE__ */ new Map());
  let setState = react__WEBPACK_IMPORTED_MODULE_0__.useCallback(
    (newState, { deletedFetchers, newErrors, flushSync, viewTransitionOpts }) => {
      if (newErrors && onError) {
        Object.values(newErrors).forEach(
          (error) => onError(error, {
            location: newState.location,
            params: newState.matches[0]?.params ?? {},
            unstable_pattern: getRoutePattern(newState.matches)
          })
        );
      }
      newState.fetchers.forEach((fetcher, key) => {
        if (fetcher.data !== void 0) {
          fetcherData.current.set(key, fetcher.data);
        }
      });
      deletedFetchers.forEach((key) => fetcherData.current.delete(key));
      warnOnce(
        flushSync === false || reactDomFlushSyncImpl != null,
        'You provided the `flushSync` option to a router update, but you are not using the `<RouterProvider>` from `react-router/dom` so `ReactDOM.flushSync()` is unavailable.  Please update your app to `import { RouterProvider } from "react-router/dom"` and ensure you have `react-dom` installed as a dependency to use the `flushSync` option.'
      );
      let isViewTransitionAvailable = router.window != null && router.window.document != null && typeof router.window.document.startViewTransition === "function";
      warnOnce(
        viewTransitionOpts == null || isViewTransitionAvailable,
        "You provided the `viewTransition` option to a router update, but you do not appear to be running in a DOM environment as `window.startViewTransition` is not available."
      );
      if (!viewTransitionOpts || !isViewTransitionAvailable) {
        if (reactDomFlushSyncImpl && flushSync) {
          reactDomFlushSyncImpl(() => setStateImpl(newState));
        } else if (unstable_useTransitions === false) {
          setStateImpl(newState);
        } else {
          react__WEBPACK_IMPORTED_MODULE_0__.startTransition(() => {
            if (unstable_useTransitions === true) {
              setOptimisticState((s) => getOptimisticRouterState(s, newState));
            }
            setStateImpl(newState);
          });
        }
        return;
      }
      if (reactDomFlushSyncImpl && flushSync) {
        reactDomFlushSyncImpl(() => {
          if (transition) {
            renderDfd?.resolve();
            transition.skipTransition();
          }
          setVtContext({
            isTransitioning: true,
            flushSync: true,
            currentLocation: viewTransitionOpts.currentLocation,
            nextLocation: viewTransitionOpts.nextLocation
          });
        });
        let t = router.window.document.startViewTransition(() => {
          reactDomFlushSyncImpl(() => setStateImpl(newState));
        });
        t.finished.finally(() => {
          reactDomFlushSyncImpl(() => {
            setRenderDfd(void 0);
            setTransition(void 0);
            setPendingState(void 0);
            setVtContext({ isTransitioning: false });
          });
        });
        reactDomFlushSyncImpl(() => setTransition(t));
        return;
      }
      if (transition) {
        renderDfd?.resolve();
        transition.skipTransition();
        setInterruption({
          state: newState,
          currentLocation: viewTransitionOpts.currentLocation,
          nextLocation: viewTransitionOpts.nextLocation
        });
      } else {
        setPendingState(newState);
        setVtContext({
          isTransitioning: true,
          flushSync: false,
          currentLocation: viewTransitionOpts.currentLocation,
          nextLocation: viewTransitionOpts.nextLocation
        });
      }
    },
    [
      router.window,
      reactDomFlushSyncImpl,
      transition,
      renderDfd,
      unstable_useTransitions,
      setOptimisticState,
      onError
    ]
  );
  react__WEBPACK_IMPORTED_MODULE_0__.useLayoutEffect(() => router.subscribe(setState), [router, setState]);
  react__WEBPACK_IMPORTED_MODULE_0__.useEffect(() => {
    if (vtContext.isTransitioning && !vtContext.flushSync) {
      setRenderDfd(new Deferred());
    }
  }, [vtContext]);
  react__WEBPACK_IMPORTED_MODULE_0__.useEffect(() => {
    if (renderDfd && pendingState && router.window) {
      let newState = pendingState;
      let renderPromise = renderDfd.promise;
      let transition2 = router.window.document.startViewTransition(async () => {
        if (unstable_useTransitions === false) {
          setStateImpl(newState);
        } else {
          react__WEBPACK_IMPORTED_MODULE_0__.startTransition(() => {
            if (unstable_useTransitions === true) {
              setOptimisticState((s) => getOptimisticRouterState(s, newState));
            }
            setStateImpl(newState);
          });
        }
        await renderPromise;
      });
      transition2.finished.finally(() => {
        setRenderDfd(void 0);
        setTransition(void 0);
        setPendingState(void 0);
        setVtContext({ isTransitioning: false });
      });
      setTransition(transition2);
    }
  }, [
    pendingState,
    renderDfd,
    router.window,
    unstable_useTransitions,
    setOptimisticState
  ]);
  react__WEBPACK_IMPORTED_MODULE_0__.useEffect(() => {
    if (renderDfd && pendingState && state.location.key === pendingState.location.key) {
      renderDfd.resolve();
    }
  }, [renderDfd, transition, state.location, pendingState]);
  react__WEBPACK_IMPORTED_MODULE_0__.useEffect(() => {
    if (!vtContext.isTransitioning && interruption) {
      setPendingState(interruption.state);
      setVtContext({
        isTransitioning: true,
        flushSync: false,
        currentLocation: interruption.currentLocation,
        nextLocation: interruption.nextLocation
      });
      setInterruption(void 0);
    }
  }, [vtContext.isTransitioning, interruption]);
  let navigator = react__WEBPACK_IMPORTED_MODULE_0__.useMemo(() => {
    return {
      createHref: router.createHref,
      encodeLocation: router.encodeLocation,
      go: (n) => router.navigate(n),
      push: (to, state2, opts) => router.navigate(to, {
        state: state2,
        preventScrollReset: opts?.preventScrollReset
      }),
      replace: (to, state2, opts) => router.navigate(to, {
        replace: true,
        state: state2,
        preventScrollReset: opts?.preventScrollReset
      })
    };
  }, [router]);
  let basename = router.basename || "/";
  let dataRouterContext = react__WEBPACK_IMPORTED_MODULE_0__.useMemo(
    () => ({
      router,
      navigator,
      static: false,
      basename,
      onError
    }),
    [router, navigator, basename, onError]
  );
  return /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement(react__WEBPACK_IMPORTED_MODULE_0__.Fragment, null, /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement(DataRouterContext.Provider, { value: dataRouterContext }, /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement(DataRouterStateContext.Provider, { value: state }, /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement(FetchersContext.Provider, { value: fetcherData.current }, /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement(ViewTransitionContext.Provider, { value: vtContext }, /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement(
    Router,
    {
      basename,
      location: state.location,
      navigationType: state.historyAction,
      navigator,
      unstable_useTransitions
    },
    /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement(
      MemoizedDataRoutes,
      {
        routes: router.routes,
        future: router.future,
        state,
        isStatic: false,
        onError
      }
    )
  ))))), null);
}
function getOptimisticRouterState(currentState, newState) {
  return {
    // Don't surface "current location specific" stuff mid-navigation
    // (historyAction, location, matches, loaderData, errors, initialized,
    // restoreScroll, preventScrollReset, blockers, etc.)
    ...currentState,
    // Only surface "pending/in-flight stuff"
    // (navigation, revalidation, actionData, fetchers, )
    navigation: newState.navigation.state !== "idle" ? newState.navigation : currentState.navigation,
    revalidation: newState.revalidation !== "idle" ? newState.revalidation : currentState.revalidation,
    actionData: newState.navigation.state !== "submitting" ? newState.actionData : currentState.actionData,
    fetchers: newState.fetchers
  };
}
var MemoizedDataRoutes = react__WEBPACK_IMPORTED_MODULE_0__.memo(DataRoutes);
function DataRoutes({
  routes,
  future,
  state,
  isStatic,
  onError
}) {
  return useRoutesImpl(routes, void 0, { state, isStatic, onError, future });
}
function MemoryRouter({
  basename,
  children,
  initialEntries,
  initialIndex,
  unstable_useTransitions
}) {
  let historyRef = react__WEBPACK_IMPORTED_MODULE_0__.useRef();
  if (historyRef.current == null) {
    historyRef.current = createMemoryHistory({
      initialEntries,
      initialIndex,
      v5Compat: true
    });
  }
  let history = historyRef.current;
  let [state, setStateImpl] = react__WEBPACK_IMPORTED_MODULE_0__.useState({
    action: history.action,
    location: history.location
  });
  let setState = react__WEBPACK_IMPORTED_MODULE_0__.useCallback(
    (newState) => {
      if (unstable_useTransitions === false) {
        setStateImpl(newState);
      } else {
        react__WEBPACK_IMPORTED_MODULE_0__.startTransition(() => setStateImpl(newState));
      }
    },
    [unstable_useTransitions]
  );
  react__WEBPACK_IMPORTED_MODULE_0__.useLayoutEffect(() => history.listen(setState), [history, setState]);
  return /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement(
    Router,
    {
      basename,
      children,
      location: state.location,
      navigationType: state.action,
      navigator: history,
      unstable_useTransitions
    }
  );
}
function Navigate({
  to,
  replace: replace2,
  state,
  relative
}) {
  invariant(
    useInRouterContext(),
    // TODO: This error is probably because they somehow have 2 versions of
    // the router loaded. We can help them understand how to avoid that.
    `<Navigate> may be used only in the context of a <Router> component.`
  );
  let { static: isStatic } = react__WEBPACK_IMPORTED_MODULE_0__.useContext(NavigationContext);
  warning(
    !isStatic,
    `<Navigate> must not be used on the initial render in a <StaticRouter>. This is a no-op, but you should modify your code so the <Navigate> is only ever rendered in response to some user interaction or state change.`
  );
  let { matches } = react__WEBPACK_IMPORTED_MODULE_0__.useContext(RouteContext);
  let { pathname: locationPathname } = useLocation();
  let navigate = useNavigate();
  let path = resolveTo(
    to,
    getResolveToMatches(matches),
    locationPathname,
    relative === "path"
  );
  let jsonPath = JSON.stringify(path);
  react__WEBPACK_IMPORTED_MODULE_0__.useEffect(() => {
    navigate(JSON.parse(jsonPath), { replace: replace2, state, relative });
  }, [navigate, jsonPath, relative, replace2, state]);
  return null;
}
function Outlet(props) {
  return useOutlet(props.context);
}
function Route(props) {
  invariant(
    false,
    `A <Route> is only ever to be used as the child of <Routes> element, never rendered directly. Please wrap your <Route> in a <Routes>.`
  );
}
function Router({
  basename: basenameProp = "/",
  children = null,
  location: locationProp,
  navigationType = "POP" /* Pop */,
  navigator,
  static: staticProp = false,
  unstable_useTransitions
}) {
  invariant(
    !useInRouterContext(),
    `You cannot render a <Router> inside another <Router>. You should never have more than one in your app.`
  );
  let basename = basenameProp.replace(/^\/*/, "/");
  let navigationContext = react__WEBPACK_IMPORTED_MODULE_0__.useMemo(
    () => ({
      basename,
      navigator,
      static: staticProp,
      unstable_useTransitions,
      future: {}
    }),
    [basename, navigator, staticProp, unstable_useTransitions]
  );
  if (typeof locationProp === "string") {
    locationProp = parsePath(locationProp);
  }
  let {
    pathname = "/",
    search = "",
    hash = "",
    state = null,
    key = "default",
    unstable_mask
  } = locationProp;
  let locationContext = react__WEBPACK_IMPORTED_MODULE_0__.useMemo(() => {
    let trailingPathname = stripBasename(pathname, basename);
    if (trailingPathname == null) {
      return null;
    }
    return {
      location: {
        pathname: trailingPathname,
        search,
        hash,
        state,
        key,
        unstable_mask
      },
      navigationType
    };
  }, [
    basename,
    pathname,
    search,
    hash,
    state,
    key,
    navigationType,
    unstable_mask
  ]);
  warning(
    locationContext != null,
    `<Router basename="${basename}"> is not able to match the URL "${pathname}${search}${hash}" because it does not start with the basename, so the <Router> won't render anything.`
  );
  if (locationContext == null) {
    return null;
  }
  return /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement(NavigationContext.Provider, { value: navigationContext }, /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement(LocationContext.Provider, { children, value: locationContext }));
}
function Routes({
  children,
  location
}) {
  return useRoutes(createRoutesFromChildren(children), location);
}
function Await({
  children,
  errorElement,
  resolve
}) {
  let dataRouterContext = react__WEBPACK_IMPORTED_MODULE_0__.useContext(DataRouterContext);
  let dataRouterStateContext = react__WEBPACK_IMPORTED_MODULE_0__.useContext(DataRouterStateContext);
  let onError = react__WEBPACK_IMPORTED_MODULE_0__.useCallback(
    (error, errorInfo) => {
      if (dataRouterContext && dataRouterContext.onError && dataRouterStateContext) {
        dataRouterContext.onError(error, {
          location: dataRouterStateContext.location,
          params: dataRouterStateContext.matches[0]?.params || {},
          unstable_pattern: getRoutePattern(dataRouterStateContext.matches),
          errorInfo
        });
      }
    },
    [dataRouterContext, dataRouterStateContext]
  );
  return /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement(
    AwaitErrorBoundary,
    {
      resolve,
      errorElement,
      onError
    },
    /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement(ResolveAwait, null, children)
  );
}
var AwaitErrorBoundary = class extends react__WEBPACK_IMPORTED_MODULE_0__.Component {
  constructor(props) {
    super(props);
    this.state = { error: null };
  }
  static getDerivedStateFromError(error) {
    return { error };
  }
  componentDidCatch(error, errorInfo) {
    if (this.props.onError) {
      this.props.onError(error, errorInfo);
    } else {
      console.error(
        "<Await> caught the following error during render",
        error,
        errorInfo
      );
    }
  }
  render() {
    let { children, errorElement, resolve } = this.props;
    let promise = null;
    let status = 0 /* pending */;
    if (!(resolve instanceof Promise)) {
      status = 1 /* success */;
      promise = Promise.resolve();
      Object.defineProperty(promise, "_tracked", { get: () => true });
      Object.defineProperty(promise, "_data", { get: () => resolve });
    } else if (this.state.error) {
      status = 2 /* error */;
      let renderError = this.state.error;
      promise = Promise.reject().catch(() => {
      });
      Object.defineProperty(promise, "_tracked", { get: () => true });
      Object.defineProperty(promise, "_error", { get: () => renderError });
    } else if (resolve._tracked) {
      promise = resolve;
      status = "_error" in promise ? 2 /* error */ : "_data" in promise ? 1 /* success */ : 0 /* pending */;
    } else {
      status = 0 /* pending */;
      Object.defineProperty(resolve, "_tracked", { get: () => true });
      promise = resolve.then(
        (data2) => Object.defineProperty(resolve, "_data", { get: () => data2 }),
        (error) => {
          this.props.onError?.(error);
          Object.defineProperty(resolve, "_error", { get: () => error });
        }
      );
    }
    if (status === 2 /* error */ && !errorElement) {
      throw promise._error;
    }
    if (status === 2 /* error */) {
      return /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement(AwaitContext.Provider, { value: promise, children: errorElement });
    }
    if (status === 1 /* success */) {
      return /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement(AwaitContext.Provider, { value: promise, children });
    }
    throw promise;
  }
};
function ResolveAwait({
  children
}) {
  let data2 = useAsyncValue();
  let toRender = typeof children === "function" ? children(data2) : children;
  return /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement(react__WEBPACK_IMPORTED_MODULE_0__.Fragment, null, toRender);
}
function createRoutesFromChildren(children, parentPath = []) {
  let routes = [];
  react__WEBPACK_IMPORTED_MODULE_0__.Children.forEach(children, (element, index) => {
    if (!react__WEBPACK_IMPORTED_MODULE_0__.isValidElement(element)) {
      return;
    }
    let treePath = [...parentPath, index];
    if (element.type === react__WEBPACK_IMPORTED_MODULE_0__.Fragment) {
      routes.push.apply(
        routes,
        createRoutesFromChildren(element.props.children, treePath)
      );
      return;
    }
    invariant(
      element.type === Route,
      `[${typeof element.type === "string" ? element.type : element.type.name}] is not a <Route> component. All component children of <Routes> must be a <Route> or <React.Fragment>`
    );
    invariant(
      !element.props.index || !element.props.children,
      "An index route cannot have child routes."
    );
    let route = {
      id: element.props.id || treePath.join("-"),
      caseSensitive: element.props.caseSensitive,
      element: element.props.element,
      Component: element.props.Component,
      index: element.props.index,
      path: element.props.path,
      middleware: element.props.middleware,
      loader: element.props.loader,
      action: element.props.action,
      hydrateFallbackElement: element.props.hydrateFallbackElement,
      HydrateFallback: element.props.HydrateFallback,
      errorElement: element.props.errorElement,
      ErrorBoundary: element.props.ErrorBoundary,
      hasErrorBoundary: element.props.hasErrorBoundary === true || element.props.ErrorBoundary != null || element.props.errorElement != null,
      shouldRevalidate: element.props.shouldRevalidate,
      handle: element.props.handle,
      lazy: element.props.lazy
    };
    if (element.props.children) {
      route.children = createRoutesFromChildren(
        element.props.children,
        treePath
      );
    }
    routes.push(route);
  });
  return routes;
}
var createRoutesFromElements = createRoutesFromChildren;
function renderMatches(matches) {
  return _renderMatches(matches);
}
function useRouteComponentProps() {
  return {
    params: useParams(),
    loaderData: useLoaderData(),
    actionData: useActionData(),
    matches: useMatches()
  };
}
function WithComponentProps({
  children
}) {
  const props = useRouteComponentProps();
  return react__WEBPACK_IMPORTED_MODULE_0__.cloneElement(children, props);
}
function withComponentProps(Component4) {
  return function WithComponentProps2() {
    const props = useRouteComponentProps();
    return react__WEBPACK_IMPORTED_MODULE_0__.createElement(Component4, props);
  };
}
function useHydrateFallbackProps() {
  return {
    params: useParams(),
    loaderData: useLoaderData(),
    actionData: useActionData()
  };
}
function WithHydrateFallbackProps({
  children
}) {
  const props = useHydrateFallbackProps();
  return react__WEBPACK_IMPORTED_MODULE_0__.cloneElement(children, props);
}
function withHydrateFallbackProps(HydrateFallback) {
  return function WithHydrateFallbackProps2() {
    const props = useHydrateFallbackProps();
    return react__WEBPACK_IMPORTED_MODULE_0__.createElement(HydrateFallback, props);
  };
}
function useErrorBoundaryProps() {
  return {
    params: useParams(),
    loaderData: useLoaderData(),
    actionData: useActionData(),
    error: useRouteError()
  };
}
function WithErrorBoundaryProps({
  children
}) {
  const props = useErrorBoundaryProps();
  return react__WEBPACK_IMPORTED_MODULE_0__.cloneElement(children, props);
}
function withErrorBoundaryProps(ErrorBoundary) {
  return function WithErrorBoundaryProps2() {
    const props = useErrorBoundaryProps();
    return react__WEBPACK_IMPORTED_MODULE_0__.createElement(ErrorBoundary, props);
  };
}

// lib/dom/dom.ts
var defaultMethod = "get";
var defaultEncType = "application/x-www-form-urlencoded";
function isHtmlElement(object) {
  return typeof HTMLElement !== "undefined" && object instanceof HTMLElement;
}
function isButtonElement(object) {
  return isHtmlElement(object) && object.tagName.toLowerCase() === "button";
}
function isFormElement(object) {
  return isHtmlElement(object) && object.tagName.toLowerCase() === "form";
}
function isInputElement(object) {
  return isHtmlElement(object) && object.tagName.toLowerCase() === "input";
}
function isModifiedEvent(event) {
  return !!(event.metaKey || event.altKey || event.ctrlKey || event.shiftKey);
}
function shouldProcessLinkClick(event, target) {
  return event.button === 0 && // Ignore everything but left clicks
  (!target || target === "_self") && // Let browser handle "target=_blank" etc.
  !isModifiedEvent(event);
}
function createSearchParams(init = "") {
  return new URLSearchParams(
    typeof init === "string" || Array.isArray(init) || init instanceof URLSearchParams ? init : Object.keys(init).reduce((memo2, key) => {
      let value = init[key];
      return memo2.concat(
        Array.isArray(value) ? value.map((v) => [key, v]) : [[key, value]]
      );
    }, [])
  );
}
function getSearchParamsForLocation(locationSearch, defaultSearchParams) {
  let searchParams = createSearchParams(locationSearch);
  if (defaultSearchParams) {
    defaultSearchParams.forEach((_, key) => {
      if (!searchParams.has(key)) {
        defaultSearchParams.getAll(key).forEach((value) => {
          searchParams.append(key, value);
        });
      }
    });
  }
  return searchParams;
}
var _formDataSupportsSubmitter = null;
function isFormDataSubmitterSupported() {
  if (_formDataSupportsSubmitter === null) {
    try {
      new FormData(
        document.createElement("form"),
        // @ts-expect-error if FormData supports the submitter parameter, this will throw
        0
      );
      _formDataSupportsSubmitter = false;
    } catch (e) {
      _formDataSupportsSubmitter = true;
    }
  }
  return _formDataSupportsSubmitter;
}
var supportedFormEncTypes = /* @__PURE__ */ new Set([
  "application/x-www-form-urlencoded",
  "multipart/form-data",
  "text/plain"
]);
function getFormEncType(encType) {
  if (encType != null && !supportedFormEncTypes.has(encType)) {
    warning(
      false,
      `"${encType}" is not a valid \`encType\` for \`<Form>\`/\`<fetcher.Form>\` and will default to "${defaultEncType}"`
    );
    return null;
  }
  return encType;
}
function getFormSubmissionInfo(target, basename) {
  let method;
  let action;
  let encType;
  let formData;
  let body;
  if (isFormElement(target)) {
    let attr = target.getAttribute("action");
    action = attr ? stripBasename(attr, basename) : null;
    method = target.getAttribute("method") || defaultMethod;
    encType = getFormEncType(target.getAttribute("enctype")) || defaultEncType;
    formData = new FormData(target);
  } else if (isButtonElement(target) || isInputElement(target) && (target.type === "submit" || target.type === "image")) {
    let form = target.form;
    if (form == null) {
      throw new Error(
        `Cannot submit a <button> or <input type="submit"> without a <form>`
      );
    }
    let attr = target.getAttribute("formaction") || form.getAttribute("action");
    action = attr ? stripBasename(attr, basename) : null;
    method = target.getAttribute("formmethod") || form.getAttribute("method") || defaultMethod;
    encType = getFormEncType(target.getAttribute("formenctype")) || getFormEncType(form.getAttribute("enctype")) || defaultEncType;
    formData = new FormData(form, target);
    if (!isFormDataSubmitterSupported()) {
      let { name, type, value } = target;
      if (type === "image") {
        let prefix = name ? `${name}.` : "";
        formData.append(`${prefix}x`, "0");
        formData.append(`${prefix}y`, "0");
      } else if (name) {
        formData.append(name, value);
      }
    }
  } else if (isHtmlElement(target)) {
    throw new Error(
      `Cannot submit element that is not <form>, <button>, or <input type="submit|image">`
    );
  } else {
    method = defaultMethod;
    action = null;
    encType = defaultEncType;
    body = target;
  }
  if (formData && encType === "text/plain") {
    body = formData;
    formData = void 0;
  }
  return { action, method: method.toLowerCase(), encType, formData, body };
}

// lib/dom/ssr/single-fetch.tsx

// vendor/turbo-stream-v2/utils.ts
var HOLE = -1;
var NAN = -2;
var NEGATIVE_INFINITY = -3;
var NEGATIVE_ZERO = -4;
var NULL = -5;
var POSITIVE_INFINITY = -6;
var UNDEFINED = -7;
var TYPE_BIGINT = "B";
var TYPE_DATE = "D";
var TYPE_ERROR = "E";
var TYPE_MAP = "M";
var TYPE_NULL_OBJECT = "N";
var TYPE_PROMISE = "P";
var TYPE_REGEXP = "R";
var TYPE_SET = "S";
var TYPE_SYMBOL = "Y";
var TYPE_URL = "U";
var TYPE_PREVIOUS_RESOLVED = "Z";
var Deferred2 = class {
  constructor() {
    this.promise = new Promise((resolve, reject) => {
      this.resolve = resolve;
      this.reject = reject;
    });
  }
};
function createLineSplittingTransform() {
  const decoder = new TextDecoder();
  let leftover = "";
  return new TransformStream({
    transform(chunk, controller) {
      const str = decoder.decode(chunk, { stream: true });
      const parts = (leftover + str).split("\n");
      leftover = parts.pop() || "";
      for (const part of parts) {
        controller.enqueue(part);
      }
    },
    flush(controller) {
      if (leftover) {
        controller.enqueue(leftover);
      }
    }
  });
}

// vendor/turbo-stream-v2/flatten.ts
function flatten(input) {
  const { indices } = this;
  const existing = indices.get(input);
  if (existing) return [existing];
  if (input === void 0) return UNDEFINED;
  if (input === null) return NULL;
  if (Number.isNaN(input)) return NAN;
  if (input === Number.POSITIVE_INFINITY) return POSITIVE_INFINITY;
  if (input === Number.NEGATIVE_INFINITY) return NEGATIVE_INFINITY;
  if (input === 0 && 1 / input < 0) return NEGATIVE_ZERO;
  const index = this.index++;
  indices.set(input, index);
  stringify.call(this, input, index);
  return index;
}
function stringify(input, index) {
  const { deferred, plugins, postPlugins } = this;
  const str = this.stringified;
  const stack = [[input, index]];
  while (stack.length > 0) {
    const [input2, index2] = stack.pop();
    const partsForObj = (obj) => Object.keys(obj).map((k) => `"_${flatten.call(this, k)}":${flatten.call(this, obj[k])}`).join(",");
    let error = null;
    switch (typeof input2) {
      case "boolean":
      case "number":
      case "string":
        str[index2] = JSON.stringify(input2);
        break;
      case "bigint":
        str[index2] = `["${TYPE_BIGINT}","${input2}"]`;
        break;
      case "symbol": {
        const keyFor = Symbol.keyFor(input2);
        if (!keyFor) {
          error = new Error(
            "Cannot encode symbol unless created with Symbol.for()"
          );
        } else {
          str[index2] = `["${TYPE_SYMBOL}",${JSON.stringify(keyFor)}]`;
        }
        break;
      }
      case "object": {
        if (!input2) {
          str[index2] = `${NULL}`;
          break;
        }
        const isArray = Array.isArray(input2);
        let pluginHandled = false;
        if (!isArray && plugins) {
          for (const plugin of plugins) {
            const pluginResult = plugin(input2);
            if (Array.isArray(pluginResult)) {
              pluginHandled = true;
              const [pluginIdentifier, ...rest] = pluginResult;
              str[index2] = `[${JSON.stringify(pluginIdentifier)}`;
              if (rest.length > 0) {
                str[index2] += `,${rest.map((v) => flatten.call(this, v)).join(",")}`;
              }
              str[index2] += "]";
              break;
            }
          }
        }
        if (!pluginHandled) {
          let result = isArray ? "[" : "{";
          if (isArray) {
            for (let i = 0; i < input2.length; i++)
              result += (i ? "," : "") + (i in input2 ? flatten.call(this, input2[i]) : HOLE);
            str[index2] = `${result}]`;
          } else if (input2 instanceof Date) {
            const dateTime = input2.getTime();
            str[index2] = `["${TYPE_DATE}",${Number.isNaN(dateTime) ? JSON.stringify("invalid") : dateTime}]`;
          } else if (input2 instanceof URL) {
            str[index2] = `["${TYPE_URL}",${JSON.stringify(input2.href)}]`;
          } else if (input2 instanceof RegExp) {
            str[index2] = `["${TYPE_REGEXP}",${JSON.stringify(
              input2.source
            )},${JSON.stringify(input2.flags)}]`;
          } else if (input2 instanceof Set) {
            if (input2.size > 0) {
              str[index2] = `["${TYPE_SET}",${[...input2].map((val) => flatten.call(this, val)).join(",")}]`;
            } else {
              str[index2] = `["${TYPE_SET}"]`;
            }
          } else if (input2 instanceof Map) {
            if (input2.size > 0) {
              str[index2] = `["${TYPE_MAP}",${[...input2].flatMap(([k, v]) => [
                flatten.call(this, k),
                flatten.call(this, v)
              ]).join(",")}]`;
            } else {
              str[index2] = `["${TYPE_MAP}"]`;
            }
          } else if (input2 instanceof Promise) {
            str[index2] = `["${TYPE_PROMISE}",${index2}]`;
            deferred[index2] = input2;
          } else if (input2 instanceof Error) {
            str[index2] = `["${TYPE_ERROR}",${JSON.stringify(input2.message)}`;
            if (input2.name !== "Error") {
              str[index2] += `,${JSON.stringify(input2.name)}`;
            }
            str[index2] += "]";
          } else if (Object.getPrototypeOf(input2) === null) {
            str[index2] = `["${TYPE_NULL_OBJECT}",{${partsForObj(input2)}}]`;
          } else if (isPlainObject2(input2)) {
            str[index2] = `{${partsForObj(input2)}}`;
          } else {
            error = new Error("Cannot encode object with prototype");
          }
        }
        break;
      }
      default: {
        const isArray = Array.isArray(input2);
        let pluginHandled = false;
        if (!isArray && plugins) {
          for (const plugin of plugins) {
            const pluginResult = plugin(input2);
            if (Array.isArray(pluginResult)) {
              pluginHandled = true;
              const [pluginIdentifier, ...rest] = pluginResult;
              str[index2] = `[${JSON.stringify(pluginIdentifier)}`;
              if (rest.length > 0) {
                str[index2] += `,${rest.map((v) => flatten.call(this, v)).join(",")}`;
              }
              str[index2] += "]";
              break;
            }
          }
        }
        if (!pluginHandled) {
          error = new Error("Cannot encode function or unexpected type");
        }
      }
    }
    if (error) {
      let pluginHandled = false;
      if (postPlugins) {
        for (const plugin of postPlugins) {
          const pluginResult = plugin(input2);
          if (Array.isArray(pluginResult)) {
            pluginHandled = true;
            const [pluginIdentifier, ...rest] = pluginResult;
            str[index2] = `[${JSON.stringify(pluginIdentifier)}`;
            if (rest.length > 0) {
              str[index2] += `,${rest.map((v) => flatten.call(this, v)).join(",")}`;
            }
            str[index2] += "]";
            break;
          }
        }
      }
      if (!pluginHandled) {
        throw error;
      }
    }
  }
}
var objectProtoNames2 = Object.getOwnPropertyNames(Object.prototype).sort().join("\0");
function isPlainObject2(thing) {
  const proto = Object.getPrototypeOf(thing);
  return proto === Object.prototype || proto === null || Object.getOwnPropertyNames(proto).sort().join("\0") === objectProtoNames2;
}

// vendor/turbo-stream-v2/unflatten.ts
var globalObj = typeof window !== "undefined" ? window : typeof globalThis !== "undefined" ? globalThis : void 0;
function unflatten(parsed) {
  const { hydrated, values } = this;
  if (typeof parsed === "number") return hydrate.call(this, parsed);
  if (!Array.isArray(parsed) || !parsed.length) throw new SyntaxError();
  const startIndex = values.length;
  for (const value of parsed) {
    values.push(value);
  }
  hydrated.length = values.length;
  return hydrate.call(this, startIndex);
}
function hydrate(index) {
  const { hydrated, values, deferred, plugins } = this;
  let result;
  const stack = [
    [
      index,
      (v) => {
        result = v;
      }
    ]
  ];
  let postRun = [];
  while (stack.length > 0) {
    const [index2, set] = stack.pop();
    switch (index2) {
      case UNDEFINED:
        set(void 0);
        continue;
      case NULL:
        set(null);
        continue;
      case NAN:
        set(NaN);
        continue;
      case POSITIVE_INFINITY:
        set(Infinity);
        continue;
      case NEGATIVE_INFINITY:
        set(-Infinity);
        continue;
      case NEGATIVE_ZERO:
        set(-0);
        continue;
    }
    if (hydrated[index2]) {
      set(hydrated[index2]);
      continue;
    }
    const value = values[index2];
    if (!value || typeof value !== "object") {
      hydrated[index2] = value;
      set(value);
      continue;
    }
    if (Array.isArray(value)) {
      if (typeof value[0] === "string") {
        const [type, b, c] = value;
        switch (type) {
          case TYPE_DATE:
            set(hydrated[index2] = new Date(b));
            continue;
          case TYPE_URL:
            set(hydrated[index2] = new URL(b));
            continue;
          case TYPE_BIGINT:
            set(hydrated[index2] = BigInt(b));
            continue;
          case TYPE_REGEXP:
            set(hydrated[index2] = new RegExp(b, c));
            continue;
          case TYPE_SYMBOL:
            set(hydrated[index2] = Symbol.for(b));
            continue;
          case TYPE_SET:
            const newSet = /* @__PURE__ */ new Set();
            hydrated[index2] = newSet;
            for (let i = value.length - 1; i > 0; i--)
              stack.push([
                value[i],
                (v) => {
                  newSet.add(v);
                }
              ]);
            set(newSet);
            continue;
          case TYPE_MAP:
            const map = /* @__PURE__ */ new Map();
            hydrated[index2] = map;
            for (let i = value.length - 2; i > 0; i -= 2) {
              const r = [];
              stack.push([
                value[i + 1],
                (v) => {
                  r[1] = v;
                }
              ]);
              stack.push([
                value[i],
                (k) => {
                  r[0] = k;
                }
              ]);
              postRun.push(() => {
                map.set(r[0], r[1]);
              });
            }
            set(map);
            continue;
          case TYPE_NULL_OBJECT:
            const obj = /* @__PURE__ */ Object.create(null);
            hydrated[index2] = obj;
            for (const key of Object.keys(b).reverse()) {
              const r = [];
              stack.push([
                b[key],
                (v) => {
                  r[1] = v;
                }
              ]);
              stack.push([
                Number(key.slice(1)),
                (k) => {
                  r[0] = k;
                }
              ]);
              postRun.push(() => {
                obj[r[0]] = r[1];
              });
            }
            set(obj);
            continue;
          case TYPE_PROMISE:
            if (hydrated[b]) {
              set(hydrated[index2] = hydrated[b]);
            } else {
              const d = new Deferred2();
              deferred[b] = d;
              set(hydrated[index2] = d.promise);
            }
            continue;
          case TYPE_ERROR:
            const [, message, errorType] = value;
            let error = errorType && globalObj && globalObj[errorType] ? new globalObj[errorType](message) : new Error(message);
            hydrated[index2] = error;
            set(error);
            continue;
          case TYPE_PREVIOUS_RESOLVED:
            set(hydrated[index2] = hydrated[b]);
            continue;
          default:
            if (Array.isArray(plugins)) {
              const r = [];
              const vals = value.slice(1);
              for (let i = 0; i < vals.length; i++) {
                const v = vals[i];
                stack.push([
                  v,
                  (v2) => {
                    r[i] = v2;
                  }
                ]);
              }
              postRun.push(() => {
                for (const plugin of plugins) {
                  const result2 = plugin(value[0], ...r);
                  if (result2) {
                    set(hydrated[index2] = result2.value);
                    return;
                  }
                }
                throw new SyntaxError();
              });
              continue;
            }
            throw new SyntaxError();
        }
      } else {
        const array = [];
        hydrated[index2] = array;
        for (let i = 0; i < value.length; i++) {
          const n = value[i];
          if (n !== HOLE) {
            stack.push([
              n,
              (v) => {
                array[i] = v;
              }
            ]);
          }
        }
        set(array);
        continue;
      }
    } else {
      const object = {};
      hydrated[index2] = object;
      for (const key of Object.keys(value).reverse()) {
        const r = [];
        stack.push([
          value[key],
          (v) => {
            r[1] = v;
          }
        ]);
        stack.push([
          Number(key.slice(1)),
          (k) => {
            r[0] = k;
          }
        ]);
        postRun.push(() => {
          object[r[0]] = r[1];
        });
      }
      set(object);
      continue;
    }
  }
  while (postRun.length > 0) {
    postRun.pop()();
  }
  return result;
}

// vendor/turbo-stream-v2/turbo-stream.ts
async function decode(readable, options) {
  const { plugins } = options ?? {};
  const done = new Deferred2();
  const reader = readable.pipeThrough(createLineSplittingTransform()).getReader();
  const decoder = {
    values: [],
    hydrated: [],
    deferred: {},
    plugins
  };
  const decoded = await decodeInitial.call(decoder, reader);
  let donePromise = done.promise;
  if (decoded.done) {
    done.resolve();
  } else {
    donePromise = decodeDeferred.call(decoder, reader).then(done.resolve).catch((reason) => {
      for (const deferred of Object.values(decoder.deferred)) {
        deferred.reject(reason);
      }
      done.reject(reason);
    });
  }
  return {
    done: donePromise.then(() => reader.closed),
    value: decoded.value
  };
}
async function decodeInitial(reader) {
  const read = await reader.read();
  if (!read.value) {
    throw new SyntaxError();
  }
  let line;
  try {
    line = JSON.parse(read.value);
  } catch (reason) {
    throw new SyntaxError();
  }
  return {
    done: read.done,
    value: unflatten.call(this, line)
  };
}
async function decodeDeferred(reader) {
  let read = await reader.read();
  while (!read.done) {
    if (!read.value) continue;
    const line = read.value;
    switch (line[0]) {
      case TYPE_PROMISE: {
        const colonIndex = line.indexOf(":");
        const deferredId = Number(line.slice(1, colonIndex));
        const deferred = this.deferred[deferredId];
        if (!deferred) {
          throw new Error(`Deferred ID ${deferredId} not found in stream`);
        }
        const lineData = line.slice(colonIndex + 1);
        let jsonLine;
        try {
          jsonLine = JSON.parse(lineData);
        } catch (reason) {
          throw new SyntaxError();
        }
        const value = unflatten.call(this, jsonLine);
        deferred.resolve(value);
        break;
      }
      case TYPE_ERROR: {
        const colonIndex = line.indexOf(":");
        const deferredId = Number(line.slice(1, colonIndex));
        const deferred = this.deferred[deferredId];
        if (!deferred) {
          throw new Error(`Deferred ID ${deferredId} not found in stream`);
        }
        const lineData = line.slice(colonIndex + 1);
        let jsonLine;
        try {
          jsonLine = JSON.parse(lineData);
        } catch (reason) {
          throw new SyntaxError();
        }
        const value = unflatten.call(this, jsonLine);
        deferred.reject(value);
        break;
      }
      default:
        throw new SyntaxError();
    }
    read = await reader.read();
  }
}
function encode(input, options) {
  const { plugins, postPlugins, signal, onComplete } = options ?? {};
  const encoder = {
    deferred: {},
    index: 0,
    indices: /* @__PURE__ */ new Map(),
    stringified: [],
    plugins,
    postPlugins,
    signal
  };
  const textEncoder = new TextEncoder();
  let lastSentIndex = 0;
  const readable = new ReadableStream({
    async start(controller) {
      const id = flatten.call(encoder, input);
      if (Array.isArray(id)) {
        throw new Error("This should never happen");
      }
      if (id < 0) {
        controller.enqueue(textEncoder.encode(`${id}
`));
      } else {
        controller.enqueue(
          textEncoder.encode(`[${encoder.stringified.join(",")}]
`)
        );
        lastSentIndex = encoder.stringified.length - 1;
      }
      const seenPromises = /* @__PURE__ */ new WeakSet();
      if (Object.keys(encoder.deferred).length) {
        let raceDone;
        const racePromise = new Promise((resolve, reject) => {
          raceDone = resolve;
          if (signal) {
            const rejectPromise = () => reject(signal.reason || new Error("Signal was aborted."));
            if (signal.aborted) {
              rejectPromise();
            } else {
              signal.addEventListener("abort", (event) => {
                rejectPromise();
              });
            }
          }
        });
        while (Object.keys(encoder.deferred).length > 0) {
          for (const [deferredId, deferred] of Object.entries(
            encoder.deferred
          )) {
            if (seenPromises.has(deferred)) continue;
            seenPromises.add(
              // biome-ignore lint/suspicious/noAssignInExpressions: <explanation>
              encoder.deferred[Number(deferredId)] = Promise.race([
                racePromise,
                deferred
              ]).then(
                (resolved) => {
                  const id2 = flatten.call(encoder, resolved);
                  if (Array.isArray(id2)) {
                    controller.enqueue(
                      textEncoder.encode(
                        `${TYPE_PROMISE}${deferredId}:[["${TYPE_PREVIOUS_RESOLVED}",${id2[0]}]]
`
                      )
                    );
                    encoder.index++;
                    lastSentIndex++;
                  } else if (id2 < 0) {
                    controller.enqueue(
                      textEncoder.encode(
                        `${TYPE_PROMISE}${deferredId}:${id2}
`
                      )
                    );
                  } else {
                    const values = encoder.stringified.slice(lastSentIndex + 1).join(",");
                    controller.enqueue(
                      textEncoder.encode(
                        `${TYPE_PROMISE}${deferredId}:[${values}]
`
                      )
                    );
                    lastSentIndex = encoder.stringified.length - 1;
                  }
                },
                (reason) => {
                  if (!reason || typeof reason !== "object" || !(reason instanceof Error)) {
                    reason = new Error("An unknown error occurred");
                  }
                  const id2 = flatten.call(encoder, reason);
                  if (Array.isArray(id2)) {
                    controller.enqueue(
                      textEncoder.encode(
                        `${TYPE_ERROR}${deferredId}:[["${TYPE_PREVIOUS_RESOLVED}",${id2[0]}]]
`
                      )
                    );
                    encoder.index++;
                    lastSentIndex++;
                  } else if (id2 < 0) {
                    controller.enqueue(
                      textEncoder.encode(
                        `${TYPE_ERROR}${deferredId}:${id2}
`
                      )
                    );
                  } else {
                    const values = encoder.stringified.slice(lastSentIndex + 1).join(",");
                    controller.enqueue(
                      textEncoder.encode(
                        `${TYPE_ERROR}${deferredId}:[${values}]
`
                      )
                    );
                    lastSentIndex = encoder.stringified.length - 1;
                  }
                }
              ).finally(() => {
                delete encoder.deferred[Number(deferredId)];
              })
            );
          }
          await Promise.race(Object.values(encoder.deferred));
        }
        raceDone();
      }
      await Promise.all(Object.values(encoder.deferred));
      onComplete?.();
      controller.close();
    }
  });
  return readable;
}

// lib/dom/ssr/data.ts
async function createRequestInit(request) {
  let init = { signal: request.signal };
  if (request.method !== "GET") {
    init.method = request.method;
    let contentType = request.headers.get("Content-Type");
    if (contentType && /\bapplication\/json\b/.test(contentType)) {
      init.headers = { "Content-Type": contentType };
      init.body = JSON.stringify(await request.json());
    } else if (contentType && /\btext\/plain\b/.test(contentType)) {
      init.headers = { "Content-Type": contentType };
      init.body = await request.text();
    } else if (contentType && /\bapplication\/x-www-form-urlencoded\b/.test(contentType)) {
      init.body = new URLSearchParams(await request.text());
    } else {
      init.body = await request.formData();
    }
  }
  return init;
}

// lib/dom/ssr/markup.ts
var ESCAPE_LOOKUP = {
  "&": "\\u0026",
  ">": "\\u003e",
  "<": "\\u003c",
  "\u2028": "\\u2028",
  "\u2029": "\\u2029"
};
var ESCAPE_REGEX = /[&><\u2028\u2029]/g;
function escapeHtml(html) {
  return html.replace(ESCAPE_REGEX, (match) => ESCAPE_LOOKUP[match]);
}

// lib/dom/ssr/invariant.ts
function invariant2(value, message) {
  if (value === false || value === null || typeof value === "undefined") {
    throw new Error(message);
  }
}

// lib/dom/ssr/single-fetch.tsx
var SingleFetchRedirectSymbol = Symbol("SingleFetchRedirect");
var SingleFetchNoResultError = class extends Error {
};
var SINGLE_FETCH_REDIRECT_STATUS = 202;
var NO_BODY_STATUS_CODES = /* @__PURE__ */ new Set([100, 101, 204, 205]);
function StreamTransfer({
  context,
  identifier,
  reader,
  textDecoder,
  nonce
}) {
  if (!context.renderMeta || !context.renderMeta.didRenderScripts) {
    return null;
  }
  if (!context.renderMeta.streamCache) {
    context.renderMeta.streamCache = {};
  }
  let { streamCache } = context.renderMeta;
  let promise = streamCache[identifier];
  if (!promise) {
    promise = streamCache[identifier] = reader.read().then((result) => {
      streamCache[identifier].result = {
        done: result.done,
        value: textDecoder.decode(result.value, { stream: true })
      };
    }).catch((e) => {
      streamCache[identifier].error = e;
    });
  }
  if (promise.error) {
    throw promise.error;
  }
  if (promise.result === void 0) {
    throw promise;
  }
  let { done, value } = promise.result;
  let scriptTag = value ? /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement(
    "script",
    {
      nonce,
      dangerouslySetInnerHTML: {
        __html: `window.__reactRouterContext.streamController.enqueue(${escapeHtml(
          JSON.stringify(value)
        )});`
      }
    }
  ) : null;
  if (done) {
    return /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement(react__WEBPACK_IMPORTED_MODULE_0__.Fragment, null, scriptTag, /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement(
      "script",
      {
        nonce,
        dangerouslySetInnerHTML: {
          __html: `window.__reactRouterContext.streamController.close();`
        }
      }
    ));
  } else {
    return /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement(react__WEBPACK_IMPORTED_MODULE_0__.Fragment, null, scriptTag, /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement(react__WEBPACK_IMPORTED_MODULE_0__.Suspense, null, /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement(
      StreamTransfer,
      {
        context,
        identifier: identifier + 1,
        reader,
        textDecoder,
        nonce
      }
    )));
  }
}
function getTurboStreamSingleFetchDataStrategy(getRouter, manifest, routeModules, ssr, basename, trailingSlashAware) {
  let dataStrategy = getSingleFetchDataStrategyImpl(
    getRouter,
    (match) => {
      let manifestRoute = manifest.routes[match.route.id];
      invariant2(manifestRoute, "Route not found in manifest");
      let routeModule = routeModules[match.route.id];
      return {
        hasLoader: manifestRoute.hasLoader,
        hasClientLoader: manifestRoute.hasClientLoader,
        hasShouldRevalidate: Boolean(routeModule?.shouldRevalidate)
      };
    },
    fetchAndDecodeViaTurboStream,
    ssr,
    basename,
    trailingSlashAware
  );
  return async (args) => args.runClientMiddleware(dataStrategy);
}
function getSingleFetchDataStrategyImpl(getRouter, getRouteInfo, fetchAndDecode, ssr, basename, trailingSlashAware, shouldAllowOptOut = () => true) {
  return async (args) => {
    let { request, matches, fetcherKey } = args;
    let router = getRouter();
    if (request.method !== "GET") {
      return singleFetchActionStrategy(
        args,
        fetchAndDecode,
        basename,
        trailingSlashAware
      );
    }
    let foundRevalidatingServerLoader = matches.some((m) => {
      let { hasLoader, hasClientLoader } = getRouteInfo(m);
      return m.shouldCallHandler() && hasLoader && !hasClientLoader;
    });
    if (!ssr && !foundRevalidatingServerLoader) {
      return nonSsrStrategy(
        args,
        getRouteInfo,
        fetchAndDecode,
        basename,
        trailingSlashAware
      );
    }
    if (fetcherKey) {
      return singleFetchLoaderFetcherStrategy(
        args,
        fetchAndDecode,
        basename,
        trailingSlashAware
      );
    }
    return singleFetchLoaderNavigationStrategy(
      args,
      router,
      getRouteInfo,
      fetchAndDecode,
      ssr,
      basename,
      trailingSlashAware,
      shouldAllowOptOut
    );
  };
}
async function singleFetchActionStrategy(args, fetchAndDecode, basename, trailingSlashAware) {
  let actionMatch = args.matches.find((m) => m.shouldCallHandler());
  invariant2(actionMatch, "No action match found");
  let actionStatus = void 0;
  let result = await actionMatch.resolve(async (handler) => {
    let result2 = await handler(async () => {
      let { data: data2, status } = await fetchAndDecode(
        args,
        basename,
        trailingSlashAware,
        [actionMatch.route.id]
      );
      actionStatus = status;
      return unwrapSingleFetchResult(data2, actionMatch.route.id);
    });
    return result2;
  });
  if (isResponse(result.result) || isRouteErrorResponse(result.result) || isDataWithResponseInit(result.result)) {
    return { [actionMatch.route.id]: result };
  }
  return {
    [actionMatch.route.id]: {
      type: result.type,
      result: data(result.result, actionStatus)
    }
  };
}
async function nonSsrStrategy(args, getRouteInfo, fetchAndDecode, basename, trailingSlashAware) {
  let matchesToLoad = args.matches.filter((m) => m.shouldCallHandler());
  let results = {};
  await Promise.all(
    matchesToLoad.map(
      (m) => m.resolve(async (handler) => {
        try {
          let { hasClientLoader } = getRouteInfo(m);
          let routeId = m.route.id;
          let result = hasClientLoader ? await handler(async () => {
            let { data: data2 } = await fetchAndDecode(
              args,
              basename,
              trailingSlashAware,
              [routeId]
            );
            return unwrapSingleFetchResult(data2, routeId);
          }) : await handler();
          results[m.route.id] = { type: "data", result };
        } catch (e) {
          results[m.route.id] = { type: "error", result: e };
        }
      })
    )
  );
  return results;
}
async function singleFetchLoaderNavigationStrategy(args, router, getRouteInfo, fetchAndDecode, ssr, basename, trailingSlashAware, shouldAllowOptOut = () => true) {
  let routesParams = /* @__PURE__ */ new Set();
  let foundOptOutRoute = false;
  let routeDfds = args.matches.map(() => createDeferred2());
  let singleFetchDfd = createDeferred2();
  let results = {};
  let resolvePromise = Promise.all(
    args.matches.map(
      async (m, i) => m.resolve(async (handler) => {
        routeDfds[i].resolve();
        let routeId = m.route.id;
        let { hasLoader, hasClientLoader, hasShouldRevalidate } = getRouteInfo(m);
        let defaultShouldRevalidate = !m.shouldRevalidateArgs || m.shouldRevalidateArgs.actionStatus == null || m.shouldRevalidateArgs.actionStatus < 400;
        let shouldCall = m.shouldCallHandler(defaultShouldRevalidate);
        if (!shouldCall) {
          foundOptOutRoute || (foundOptOutRoute = m.shouldRevalidateArgs != null && // This is a revalidation,
          hasLoader && // for a route with a server loader,
          hasShouldRevalidate === true);
          return;
        }
        if (shouldAllowOptOut(m) && hasClientLoader) {
          if (hasLoader) {
            foundOptOutRoute = true;
          }
          try {
            let result = await handler(async () => {
              let { data: data2 } = await fetchAndDecode(
                args,
                basename,
                trailingSlashAware,
                [routeId]
              );
              return unwrapSingleFetchResult(data2, routeId);
            });
            results[routeId] = { type: "data", result };
          } catch (e) {
            results[routeId] = { type: "error", result: e };
          }
          return;
        }
        if (hasLoader) {
          routesParams.add(routeId);
        }
        try {
          let result = await handler(async () => {
            let data2 = await singleFetchDfd.promise;
            return unwrapSingleFetchResult(data2, routeId);
          });
          results[routeId] = { type: "data", result };
        } catch (e) {
          results[routeId] = { type: "error", result: e };
        }
      })
    )
  );
  await Promise.all(routeDfds.map((d) => d.promise));
  let isInitialLoad = !router.state.initialized && router.state.navigation.state === "idle";
  if ((isInitialLoad || routesParams.size === 0) && !window.__reactRouterHdrActive) {
    singleFetchDfd.resolve({ routes: {} });
  } else {
    let targetRoutes = ssr && foundOptOutRoute && routesParams.size > 0 ? [...routesParams.keys()] : void 0;
    try {
      let data2 = await fetchAndDecode(
        args,
        basename,
        trailingSlashAware,
        targetRoutes
      );
      singleFetchDfd.resolve(data2.data);
    } catch (e) {
      singleFetchDfd.reject(e);
    }
  }
  await resolvePromise;
  await bubbleMiddlewareErrors(
    singleFetchDfd.promise,
    args.matches,
    routesParams,
    results
  );
  return results;
}
async function bubbleMiddlewareErrors(singleFetchPromise, matches, routesParams, results) {
  try {
    let middlewareError;
    let fetchedData = await singleFetchPromise;
    if ("routes" in fetchedData) {
      for (let match of matches) {
        if (match.route.id in fetchedData.routes) {
          let routeResult = fetchedData.routes[match.route.id];
          if ("error" in routeResult) {
            middlewareError = routeResult.error;
            if (results[match.route.id]?.result == null) {
              results[match.route.id] = {
                type: "error",
                result: middlewareError
              };
            }
            break;
          }
        }
      }
    }
    if (middlewareError !== void 0) {
      Array.from(routesParams.values()).forEach((routeId) => {
        if (results[routeId].result instanceof SingleFetchNoResultError) {
          results[routeId].result = middlewareError;
        }
      });
    }
  } catch (e) {
  }
}
async function singleFetchLoaderFetcherStrategy(args, fetchAndDecode, basename, trailingSlashAware) {
  let fetcherMatch = args.matches.find((m) => m.shouldCallHandler());
  invariant2(fetcherMatch, "No fetcher match found");
  let routeId = fetcherMatch.route.id;
  let result = await fetcherMatch.resolve(
    async (handler) => handler(async () => {
      let { data: data2 } = await fetchAndDecode(args, basename, trailingSlashAware, [
        routeId
      ]);
      return unwrapSingleFetchResult(data2, routeId);
    })
  );
  return { [fetcherMatch.route.id]: result };
}
function stripIndexParam(url) {
  let indexValues = url.searchParams.getAll("index");
  url.searchParams.delete("index");
  let indexValuesToKeep = [];
  for (let indexValue of indexValues) {
    if (indexValue) {
      indexValuesToKeep.push(indexValue);
    }
  }
  for (let toKeep of indexValuesToKeep) {
    url.searchParams.append("index", toKeep);
  }
  return url;
}
function singleFetchUrl(reqUrl, basename, trailingSlashAware, extension) {
  let url = typeof reqUrl === "string" ? new URL(
    reqUrl,
    // This can be called during the SSR flow via PrefetchPageLinksImpl so
    // don't assume window is available
    typeof window === "undefined" ? "server://singlefetch/" : window.location.origin
  ) : reqUrl;
  if (trailingSlashAware) {
    if (url.pathname.endsWith("/")) {
      url.pathname = `${url.pathname}_.${extension}`;
    } else {
      url.pathname = `${url.pathname}.${extension}`;
    }
  } else {
    if (url.pathname === "/") {
      url.pathname = `_root.${extension}`;
    } else if (basename && stripBasename(url.pathname, basename) === "/") {
      url.pathname = `${basename.replace(/\/$/, "")}/_root.${extension}`;
    } else {
      url.pathname = `${url.pathname.replace(/\/$/, "")}.${extension}`;
    }
  }
  return url;
}
async function fetchAndDecodeViaTurboStream(args, basename, trailingSlashAware, targetRoutes) {
  let { request } = args;
  let url = singleFetchUrl(request.url, basename, trailingSlashAware, "data");
  if (request.method === "GET") {
    url = stripIndexParam(url);
    if (targetRoutes) {
      url.searchParams.set("_routes", targetRoutes.join(","));
    }
  }
  let res = await fetch(url, await createRequestInit(request));
  if (res.status >= 400 && !res.headers.has("X-Remix-Response")) {
    throw new ErrorResponseImpl(res.status, res.statusText, await res.text());
  }
  if (res.status === 204 && res.headers.has("X-Remix-Redirect")) {
    return {
      status: SINGLE_FETCH_REDIRECT_STATUS,
      data: {
        redirect: {
          redirect: res.headers.get("X-Remix-Redirect"),
          status: Number(res.headers.get("X-Remix-Status") || "302"),
          revalidate: res.headers.get("X-Remix-Revalidate") === "true",
          reload: res.headers.get("X-Remix-Reload-Document") === "true",
          replace: res.headers.get("X-Remix-Replace") === "true"
        }
      }
    };
  }
  if (NO_BODY_STATUS_CODES.has(res.status)) {
    let routes = {};
    if (targetRoutes && request.method !== "GET") {
      routes[targetRoutes[0]] = { data: void 0 };
    }
    return {
      status: res.status,
      data: { routes }
    };
  }
  invariant2(res.body, "No response body to decode");
  try {
    let decoded = await decodeViaTurboStream(res.body, window);
    let data2;
    if (request.method === "GET") {
      let typed = decoded.value;
      if (SingleFetchRedirectSymbol in typed) {
        data2 = { redirect: typed[SingleFetchRedirectSymbol] };
      } else {
        data2 = { routes: typed };
      }
    } else {
      let typed = decoded.value;
      let routeId = targetRoutes?.[0];
      invariant2(routeId, "No routeId found for single fetch call decoding");
      if ("redirect" in typed) {
        data2 = { redirect: typed };
      } else {
        data2 = { routes: { [routeId]: typed } };
      }
    }
    return { status: res.status, data: data2 };
  } catch (e) {
    throw new Error("Unable to decode turbo-stream response");
  }
}
function decodeViaTurboStream(body, global) {
  return decode(body, {
    plugins: [
      (type, ...rest) => {
        if (type === "SanitizedError") {
          let [name, message, stack] = rest;
          let Constructor = Error;
          if (name && name in global && typeof global[name] === "function") {
            Constructor = global[name];
          }
          let error = new Constructor(message);
          error.stack = stack;
          return { value: error };
        }
        if (type === "ErrorResponse") {
          let [data2, status, statusText] = rest;
          return {
            value: new ErrorResponseImpl(status, statusText, data2)
          };
        }
        if (type === "SingleFetchRedirect") {
          return { value: { [SingleFetchRedirectSymbol]: rest[0] } };
        }
        if (type === "SingleFetchClassInstance") {
          return { value: rest[0] };
        }
        if (type === "SingleFetchFallback") {
          return { value: void 0 };
        }
      }
    ]
  });
}
function unwrapSingleFetchResult(result, routeId) {
  if ("redirect" in result) {
    let {
      redirect: location,
      revalidate,
      reload,
      replace: replace2,
      status
    } = result.redirect;
    throw redirect(location, {
      status,
      headers: {
        // Three R's of redirecting (lol Veep)
        ...revalidate ? { "X-Remix-Revalidate": "yes" } : null,
        ...reload ? { "X-Remix-Reload-Document": "yes" } : null,
        ...replace2 ? { "X-Remix-Replace": "yes" } : null
      }
    });
  }
  let routeResult = result.routes[routeId];
  if (routeResult == null) {
    throw new SingleFetchNoResultError(
      `No result found for routeId "${routeId}"`
    );
  } else if ("error" in routeResult) {
    throw routeResult.error;
  } else if ("data" in routeResult) {
    return routeResult.data;
  } else {
    throw new Error(`Invalid response found for routeId "${routeId}"`);
  }
}
function createDeferred2() {
  let resolve;
  let reject;
  let promise = new Promise((res, rej) => {
    resolve = async (val) => {
      res(val);
      try {
        await promise;
      } catch (e) {
      }
    };
    reject = async (error) => {
      rej(error);
      try {
        await promise;
      } catch (e) {
      }
    };
  });
  return {
    promise,
    //@ts-ignore
    resolve,
    //@ts-ignore
    reject
  };
}

// lib/dom/ssr/errorBoundaries.tsx

// lib/dom/ssr/components.tsx

// lib/dom/ssr/routeModules.ts
async function loadRouteModule(route, routeModulesCache) {
  if (route.id in routeModulesCache) {
    return routeModulesCache[route.id];
  }
  try {
    let routeModule = await import(
      /* @vite-ignore */
      /* webpackIgnore: true */
      route.module
    );
    routeModulesCache[route.id] = routeModule;
    return routeModule;
  } catch (error) {
    console.error(
      `Error loading route module \`${route.module}\`, reloading page...`
    );
    console.error(error);
    if (window.__reactRouterContext && window.__reactRouterContext.isSpaMode && // @ts-expect-error
    /* unsupported import.meta.hot */ undefined) // removed by dead control flow
{}
    window.location.reload();
    return new Promise(() => {
    });
  }
}

// lib/dom/ssr/links.ts
function getKeyedLinksForMatches(matches, routeModules, manifest) {
  let descriptors = matches.map((match) => {
    let module = routeModules[match.route.id];
    let route = manifest.routes[match.route.id];
    return [
      route && route.css ? route.css.map((href) => ({ rel: "stylesheet", href })) : [],
      module?.links?.() || []
    ];
  }).flat(2);
  let preloads = getModuleLinkHrefs(matches, manifest);
  return dedupeLinkDescriptors(descriptors, preloads);
}
function getRouteCssDescriptors(route) {
  if (!route.css) return [];
  return route.css.map((href) => ({ rel: "stylesheet", href }));
}
async function prefetchRouteCss(route) {
  if (!route.css) return;
  let descriptors = getRouteCssDescriptors(route);
  await Promise.all(descriptors.map(prefetchStyleLink));
}
async function prefetchStyleLinks(route, routeModule) {
  if (!route.css && !routeModule.links || !isPreloadSupported()) return;
  let descriptors = [];
  if (route.css) {
    descriptors.push(...getRouteCssDescriptors(route));
  }
  if (routeModule.links) {
    descriptors.push(...routeModule.links());
  }
  if (descriptors.length === 0) return;
  let styleLinks = [];
  for (let descriptor of descriptors) {
    if (!isPageLinkDescriptor(descriptor) && descriptor.rel === "stylesheet") {
      styleLinks.push({
        ...descriptor,
        rel: "preload",
        as: "style"
      });
    }
  }
  await Promise.all(styleLinks.map(prefetchStyleLink));
}
async function prefetchStyleLink(descriptor) {
  return new Promise((resolve) => {
    if (descriptor.media && !window.matchMedia(descriptor.media).matches || document.querySelector(
      `link[rel="stylesheet"][href="${descriptor.href}"]`
    )) {
      return resolve();
    }
    let link = document.createElement("link");
    Object.assign(link, descriptor);
    function removeLink() {
      if (document.head.contains(link)) {
        document.head.removeChild(link);
      }
    }
    link.onload = () => {
      removeLink();
      resolve();
    };
    link.onerror = () => {
      removeLink();
      resolve();
    };
    document.head.appendChild(link);
  });
}
function isPageLinkDescriptor(object) {
  return object != null && typeof object.page === "string";
}
function isHtmlLinkDescriptor(object) {
  if (object == null) {
    return false;
  }
  if (object.href == null) {
    return object.rel === "preload" && typeof object.imageSrcSet === "string" && typeof object.imageSizes === "string";
  }
  return typeof object.rel === "string" && typeof object.href === "string";
}
async function getKeyedPrefetchLinks(matches, manifest, routeModules) {
  let links = await Promise.all(
    matches.map(async (match) => {
      let route = manifest.routes[match.route.id];
      if (route) {
        let mod = await loadRouteModule(route, routeModules);
        return mod.links ? mod.links() : [];
      }
      return [];
    })
  );
  return dedupeLinkDescriptors(
    links.flat(1).filter(isHtmlLinkDescriptor).filter((link) => link.rel === "stylesheet" || link.rel === "preload").map(
      (link) => link.rel === "stylesheet" ? { ...link, rel: "prefetch", as: "style" } : { ...link, rel: "prefetch" }
    )
  );
}
function getNewMatchesForLinks(page, nextMatches, currentMatches, manifest, location, mode) {
  let isNew = (match, index) => {
    if (!currentMatches[index]) return true;
    return match.route.id !== currentMatches[index].route.id;
  };
  let matchPathChanged = (match, index) => {
    return (
      // param change, /users/123 -> /users/456
      currentMatches[index].pathname !== match.pathname || // splat param changed, which is not present in match.path
      // e.g. /files/images/avatar.jpg -> files/finances.xls
      currentMatches[index].route.path?.endsWith("*") && currentMatches[index].params["*"] !== match.params["*"]
    );
  };
  if (mode === "assets") {
    return nextMatches.filter(
      (match, index) => isNew(match, index) || matchPathChanged(match, index)
    );
  }
  if (mode === "data") {
    return nextMatches.filter((match, index) => {
      let manifestRoute = manifest.routes[match.route.id];
      if (!manifestRoute || !manifestRoute.hasLoader) {
        return false;
      }
      if (isNew(match, index) || matchPathChanged(match, index)) {
        return true;
      }
      if (match.route.shouldRevalidate) {
        let routeChoice = match.route.shouldRevalidate({
          currentUrl: new URL(
            location.pathname + location.search + location.hash,
            window.origin
          ),
          currentParams: currentMatches[0]?.params || {},
          nextUrl: new URL(page, window.origin),
          nextParams: match.params,
          defaultShouldRevalidate: true
        });
        if (typeof routeChoice === "boolean") {
          return routeChoice;
        }
      }
      return true;
    });
  }
  return [];
}
function getModuleLinkHrefs(matches, manifest, { includeHydrateFallback } = {}) {
  return dedupeHrefs(
    matches.map((match) => {
      let route = manifest.routes[match.route.id];
      if (!route) return [];
      let hrefs = [route.module];
      if (route.clientActionModule) {
        hrefs = hrefs.concat(route.clientActionModule);
      }
      if (route.clientLoaderModule) {
        hrefs = hrefs.concat(route.clientLoaderModule);
      }
      if (includeHydrateFallback && route.hydrateFallbackModule) {
        hrefs = hrefs.concat(route.hydrateFallbackModule);
      }
      if (route.imports) {
        hrefs = hrefs.concat(route.imports);
      }
      return hrefs;
    }).flat(1)
  );
}
function dedupeHrefs(hrefs) {
  return [...new Set(hrefs)];
}
function sortKeys(obj) {
  let sorted = {};
  let keys = Object.keys(obj).sort();
  for (let key of keys) {
    sorted[key] = obj[key];
  }
  return sorted;
}
function dedupeLinkDescriptors(descriptors, preloads) {
  let set = /* @__PURE__ */ new Set();
  let preloadsSet = new Set(preloads);
  return descriptors.reduce((deduped, descriptor) => {
    let alreadyModulePreload = preloads && !isPageLinkDescriptor(descriptor) && descriptor.as === "script" && descriptor.href && preloadsSet.has(descriptor.href);
    if (alreadyModulePreload) {
      return deduped;
    }
    let key = JSON.stringify(sortKeys(descriptor));
    if (!set.has(key)) {
      set.add(key);
      deduped.push({ key, link: descriptor });
    }
    return deduped;
  }, []);
}
var _isPreloadSupported;
function isPreloadSupported() {
  if (_isPreloadSupported !== void 0) {
    return _isPreloadSupported;
  }
  let el = document.createElement("link");
  _isPreloadSupported = el.relList.supports("preload");
  el = null;
  return _isPreloadSupported;
}

// lib/dom/ssr/fog-of-war.ts

// lib/dom/ssr/routes.tsx

// lib/dom/ssr/fallback.tsx

function RemixRootDefaultHydrateFallback() {
  return /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement(BoundaryShell, { title: "Loading...", renderScripts: true }, ENABLE_DEV_WARNINGS ? /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement(
    "script",
    {
      dangerouslySetInnerHTML: {
        __html: `
              console.log(
                "\u{1F4BF} Hey developer \u{1F44B}. You can provide a way better UX than this " +
                "when your app is loading JS modules and/or running \`clientLoader\` " +
                "functions. Check out https://reactrouter.com/start/framework/route-module#hydratefallback " +
                "for more information."
              );
            `
      }
    }
  ) : null);
}

// lib/dom/ssr/routes.tsx
function groupRoutesByParentId(manifest) {
  let routes = {};
  Object.values(manifest).forEach((route) => {
    if (route) {
      let parentId = route.parentId || "";
      if (!routes[parentId]) {
        routes[parentId] = [];
      }
      routes[parentId].push(route);
    }
  });
  return routes;
}
function getRouteComponents(route, routeModule, isSpaMode) {
  let Component4 = getRouteModuleComponent(routeModule);
  let HydrateFallback = routeModule.HydrateFallback && (!isSpaMode || route.id === "root") ? routeModule.HydrateFallback : route.id === "root" ? RemixRootDefaultHydrateFallback : void 0;
  let ErrorBoundary = routeModule.ErrorBoundary ? routeModule.ErrorBoundary : route.id === "root" ? () => /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement(RemixRootDefaultErrorBoundary, { error: useRouteError() }) : void 0;
  if (route.id === "root" && routeModule.Layout) {
    return {
      ...Component4 ? {
        element: /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement(routeModule.Layout, null, /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement(Component4, null))
      } : { Component: Component4 },
      ...ErrorBoundary ? {
        errorElement: /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement(routeModule.Layout, null, /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement(ErrorBoundary, null))
      } : { ErrorBoundary },
      ...HydrateFallback ? {
        hydrateFallbackElement: /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement(routeModule.Layout, null, /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement(HydrateFallback, null))
      } : { HydrateFallback }
    };
  }
  return { Component: Component4, ErrorBoundary, HydrateFallback };
}
function createServerRoutes(manifest, routeModules, future, isSpaMode, parentId = "", routesByParentId = groupRoutesByParentId(manifest), spaModeLazyPromise = Promise.resolve({ Component: () => null })) {
  return (routesByParentId[parentId] || []).map((route) => {
    let routeModule = routeModules[route.id];
    invariant2(
      routeModule,
      "No `routeModule` available to create server routes"
    );
    let dataRoute = {
      ...getRouteComponents(route, routeModule, isSpaMode),
      caseSensitive: route.caseSensitive,
      id: route.id,
      index: route.index,
      path: route.path,
      handle: routeModule.handle,
      // For SPA Mode, all routes are lazy except root.  However we tell the
      // router root is also lazy here too since we don't need a full
      // implementation - we just need a `lazy` prop to tell the RR rendering
      // where to stop which is always at the root route in SPA mode
      lazy: isSpaMode ? () => spaModeLazyPromise : void 0,
      // For partial hydration rendering, we need to indicate when the route
      // has a loader/clientLoader, but it won't ever be called during the static
      // render, so just give it a no-op function so we can render down to the
      // proper fallback
      loader: route.hasLoader || route.hasClientLoader ? () => null : void 0
      // We don't need middleware/action/shouldRevalidate on these routes since
      // they're for a static render
    };
    let children = createServerRoutes(
      manifest,
      routeModules,
      future,
      isSpaMode,
      route.id,
      routesByParentId,
      spaModeLazyPromise
    );
    if (children.length > 0) dataRoute.children = children;
    return dataRoute;
  });
}
function createClientRoutesWithHMRRevalidationOptOut(needsRevalidation, manifest, routeModulesCache, initialState, ssr, isSpaMode) {
  return createClientRoutes(
    manifest,
    routeModulesCache,
    initialState,
    ssr,
    isSpaMode,
    "",
    groupRoutesByParentId(manifest),
    needsRevalidation
  );
}
function preventInvalidServerHandlerCall(type, route) {
  if (type === "loader" && !route.hasLoader || type === "action" && !route.hasAction) {
    let fn = type === "action" ? "serverAction()" : "serverLoader()";
    let msg = `You are trying to call ${fn} on a route that does not have a server ${type} (routeId: "${route.id}")`;
    console.error(msg);
    throw new ErrorResponseImpl(400, "Bad Request", new Error(msg), true);
  }
}
function noActionDefinedError(type, routeId) {
  let article = type === "clientAction" ? "a" : "an";
  let msg = `Route "${routeId}" does not have ${article} ${type}, but you are trying to submit to it. To fix this, please add ${article} \`${type}\` function to the route`;
  console.error(msg);
  throw new ErrorResponseImpl(405, "Method Not Allowed", new Error(msg), true);
}
function createClientRoutes(manifest, routeModulesCache, initialState, ssr, isSpaMode, parentId = "", routesByParentId = groupRoutesByParentId(manifest), needsRevalidation) {
  return (routesByParentId[parentId] || []).map((route) => {
    let routeModule = routeModulesCache[route.id];
    function fetchServerHandler(singleFetch) {
      invariant2(
        typeof singleFetch === "function",
        "No single fetch function available for route handler"
      );
      return singleFetch();
    }
    function fetchServerLoader(singleFetch) {
      if (!route.hasLoader) return Promise.resolve(null);
      return fetchServerHandler(singleFetch);
    }
    function fetchServerAction(singleFetch) {
      if (!route.hasAction) {
        throw noActionDefinedError("action", route.id);
      }
      return fetchServerHandler(singleFetch);
    }
    function prefetchModule(modulePath) {
      import(
        /* @vite-ignore */
        /* webpackIgnore: true */
        modulePath
      );
    }
    function prefetchRouteModuleChunks(route2) {
      if (route2.clientActionModule) {
        prefetchModule(route2.clientActionModule);
      }
      if (route2.clientLoaderModule) {
        prefetchModule(route2.clientLoaderModule);
      }
    }
    async function prefetchStylesAndCallHandler(handler) {
      let cachedModule = routeModulesCache[route.id];
      let linkPrefetchPromise = cachedModule ? prefetchStyleLinks(route, cachedModule) : Promise.resolve();
      try {
        return handler();
      } finally {
        await linkPrefetchPromise;
      }
    }
    let dataRoute = {
      id: route.id,
      index: route.index,
      path: route.path
    };
    if (routeModule) {
      Object.assign(dataRoute, {
        ...dataRoute,
        ...getRouteComponents(route, routeModule, isSpaMode),
        middleware: routeModule.clientMiddleware,
        handle: routeModule.handle,
        shouldRevalidate: getShouldRevalidateFunction(
          dataRoute.path,
          routeModule,
          route,
          ssr,
          needsRevalidation
        )
      });
      let hasInitialData = initialState && initialState.loaderData && route.id in initialState.loaderData;
      let initialData = hasInitialData ? initialState?.loaderData?.[route.id] : void 0;
      let hasInitialError = initialState && initialState.errors && route.id in initialState.errors;
      let initialError = hasInitialError ? initialState?.errors?.[route.id] : void 0;
      let isHydrationRequest = needsRevalidation == null && (routeModule.clientLoader?.hydrate === true || !route.hasLoader);
      dataRoute.loader = async ({
        request,
        params,
        context,
        unstable_pattern,
        unstable_url
      }, singleFetch) => {
        try {
          let result = await prefetchStylesAndCallHandler(async () => {
            invariant2(
              routeModule,
              "No `routeModule` available for critical-route loader"
            );
            if (!routeModule.clientLoader) {
              return fetchServerLoader(singleFetch);
            }
            return routeModule.clientLoader({
              request,
              params,
              context,
              unstable_pattern,
              unstable_url,
              async serverLoader() {
                preventInvalidServerHandlerCall("loader", route);
                if (isHydrationRequest) {
                  if (hasInitialData) {
                    return initialData;
                  }
                  if (hasInitialError) {
                    throw initialError;
                  }
                }
                return fetchServerLoader(singleFetch);
              }
            });
          });
          return result;
        } finally {
          isHydrationRequest = false;
        }
      };
      dataRoute.loader.hydrate = shouldHydrateRouteLoader(
        route.id,
        routeModule.clientLoader,
        route.hasLoader,
        isSpaMode
      );
      dataRoute.action = ({
        request,
        params,
        context,
        unstable_pattern,
        unstable_url
      }, singleFetch) => {
        return prefetchStylesAndCallHandler(async () => {
          invariant2(
            routeModule,
            "No `routeModule` available for critical-route action"
          );
          if (!routeModule.clientAction) {
            if (isSpaMode) {
              throw noActionDefinedError("clientAction", route.id);
            }
            return fetchServerAction(singleFetch);
          }
          return routeModule.clientAction({
            request,
            params,
            context,
            unstable_pattern,
            unstable_url,
            async serverAction() {
              preventInvalidServerHandlerCall("action", route);
              return fetchServerAction(singleFetch);
            }
          });
        });
      };
    } else {
      if (!route.hasClientLoader) {
        dataRoute.loader = (_, singleFetch) => prefetchStylesAndCallHandler(() => {
          return fetchServerLoader(singleFetch);
        });
      }
      if (!route.hasClientAction) {
        dataRoute.action = (_, singleFetch) => prefetchStylesAndCallHandler(() => {
          if (isSpaMode) {
            throw noActionDefinedError("clientAction", route.id);
          }
          return fetchServerAction(singleFetch);
        });
      }
      let lazyRoutePromise;
      async function getLazyRoute() {
        if (lazyRoutePromise) {
          return await lazyRoutePromise;
        }
        lazyRoutePromise = (async () => {
          if (route.clientLoaderModule || route.clientActionModule) {
            await new Promise((resolve) => setTimeout(resolve, 0));
          }
          let routeModulePromise = loadRouteModuleWithBlockingLinks(
            route,
            routeModulesCache
          );
          prefetchRouteModuleChunks(route);
          return await routeModulePromise;
        })();
        return await lazyRoutePromise;
      }
      dataRoute.lazy = {
        loader: route.hasClientLoader ? async () => {
          let { clientLoader } = route.clientLoaderModule ? await import(
            /* @vite-ignore */
            /* webpackIgnore: true */
            route.clientLoaderModule
          ) : await getLazyRoute();
          invariant2(clientLoader, "No `clientLoader` export found");
          return (args, singleFetch) => clientLoader({
            ...args,
            async serverLoader() {
              preventInvalidServerHandlerCall("loader", route);
              return fetchServerLoader(singleFetch);
            }
          });
        } : void 0,
        action: route.hasClientAction ? async () => {
          let clientActionPromise = route.clientActionModule ? import(
            /* @vite-ignore */
            /* webpackIgnore: true */
            route.clientActionModule
          ) : getLazyRoute();
          prefetchRouteModuleChunks(route);
          let { clientAction } = await clientActionPromise;
          invariant2(clientAction, "No `clientAction` export found");
          return (args, singleFetch) => clientAction({
            ...args,
            async serverAction() {
              preventInvalidServerHandlerCall("action", route);
              return fetchServerAction(singleFetch);
            }
          });
        } : void 0,
        middleware: route.hasClientMiddleware ? async () => {
          let { clientMiddleware } = route.clientMiddlewareModule ? await import(
            /* @vite-ignore */
            /* webpackIgnore: true */
            route.clientMiddlewareModule
          ) : await getLazyRoute();
          invariant2(clientMiddleware, "No `clientMiddleware` export found");
          return clientMiddleware;
        } : void 0,
        shouldRevalidate: async () => {
          let lazyRoute = await getLazyRoute();
          return getShouldRevalidateFunction(
            dataRoute.path,
            lazyRoute,
            route,
            ssr,
            needsRevalidation
          );
        },
        handle: async () => (await getLazyRoute()).handle,
        // No need to wrap these in layout since the root route is never
        // loaded via route.lazy()
        Component: async () => (await getLazyRoute()).Component,
        ErrorBoundary: route.hasErrorBoundary ? async () => (await getLazyRoute()).ErrorBoundary : void 0
      };
    }
    let children = createClientRoutes(
      manifest,
      routeModulesCache,
      initialState,
      ssr,
      isSpaMode,
      route.id,
      routesByParentId,
      needsRevalidation
    );
    if (children.length > 0) dataRoute.children = children;
    return dataRoute;
  });
}
function getShouldRevalidateFunction(path, route, manifestRoute, ssr, needsRevalidation) {
  if (needsRevalidation) {
    return wrapShouldRevalidateForHdr(
      manifestRoute.id,
      route.shouldRevalidate,
      needsRevalidation
    );
  }
  if (!ssr && manifestRoute.hasLoader && !manifestRoute.hasClientLoader) {
    let myParams = path ? compilePath(path)[1].map((p) => p.paramName) : [];
    const didParamsChange = (opts) => myParams.some((p) => opts.currentParams[p] !== opts.nextParams[p]);
    if (route.shouldRevalidate) {
      let fn = route.shouldRevalidate;
      return (opts) => fn({
        ...opts,
        defaultShouldRevalidate: didParamsChange(opts)
      });
    } else {
      return (opts) => didParamsChange(opts);
    }
  }
  return route.shouldRevalidate;
}
function wrapShouldRevalidateForHdr(routeId, routeShouldRevalidate, needsRevalidation) {
  let handledRevalidation = false;
  return (arg) => {
    if (!handledRevalidation) {
      handledRevalidation = true;
      return needsRevalidation.has(routeId);
    }
    return routeShouldRevalidate ? routeShouldRevalidate(arg) : arg.defaultShouldRevalidate;
  };
}
async function loadRouteModuleWithBlockingLinks(route, routeModules) {
  let routeModulePromise = loadRouteModule(route, routeModules);
  let prefetchRouteCssPromise = prefetchRouteCss(route);
  let routeModule = await routeModulePromise;
  await Promise.all([
    prefetchRouteCssPromise,
    prefetchStyleLinks(route, routeModule)
  ]);
  return {
    Component: getRouteModuleComponent(routeModule),
    ErrorBoundary: routeModule.ErrorBoundary,
    clientMiddleware: routeModule.clientMiddleware,
    clientAction: routeModule.clientAction,
    clientLoader: routeModule.clientLoader,
    handle: routeModule.handle,
    links: routeModule.links,
    meta: routeModule.meta,
    shouldRevalidate: routeModule.shouldRevalidate
  };
}
function getRouteModuleComponent(routeModule) {
  if (routeModule.default == null) return void 0;
  let isEmptyObject = typeof routeModule.default === "object" && Object.keys(routeModule.default).length === 0;
  if (!isEmptyObject) {
    return routeModule.default;
  }
}
function shouldHydrateRouteLoader(routeId, clientLoader, hasLoader, isSpaMode) {
  return isSpaMode && routeId !== "root" || clientLoader != null && (clientLoader.hydrate === true || hasLoader !== true);
}

// lib/dom/ssr/fog-of-war.ts
var nextPaths = /* @__PURE__ */ new Set();
var discoveredPathsMaxSize = 1e3;
var discoveredPaths = /* @__PURE__ */ new Set();
var URL_LIMIT = 7680;
function isFogOfWarEnabled(routeDiscovery, ssr) {
  return routeDiscovery.mode === "lazy" && ssr === true;
}
function getPartialManifest({ sri, ...manifest }, router) {
  let routeIds = new Set(router.state.matches.map((m) => m.route.id));
  let segments = router.state.location.pathname.split("/").filter(Boolean);
  let paths = ["/"];
  segments.pop();
  while (segments.length > 0) {
    paths.push(`/${segments.join("/")}`);
    segments.pop();
  }
  paths.forEach((path) => {
    let matches = matchRoutes(router.routes, path, router.basename);
    if (matches) {
      matches.forEach((m) => routeIds.add(m.route.id));
    }
  });
  let initialRoutes = [...routeIds].reduce(
    (acc, id) => Object.assign(acc, { [id]: manifest.routes[id] }),
    {}
  );
  return {
    ...manifest,
    routes: initialRoutes,
    sri: sri ? true : void 0
  };
}
function getPatchRoutesOnNavigationFunction(getRouter, manifest, routeModules, ssr, routeDiscovery, isSpaMode, basename) {
  if (!isFogOfWarEnabled(routeDiscovery, ssr)) {
    return void 0;
  }
  return async ({ path, patch, signal, fetcherKey }) => {
    if (discoveredPaths.has(path)) {
      return;
    }
    let { state } = getRouter();
    await fetchAndApplyManifestPatches(
      [path],
      // If we're patching for a fetcher call, reload the current location
      // Otherwise prefer any ongoing navigation location
      fetcherKey ? window.location.href : createPath(state.navigation.location || state.location),
      manifest,
      routeModules,
      ssr,
      isSpaMode,
      basename,
      routeDiscovery.manifestPath,
      patch,
      signal
    );
  };
}
function useFogOFWarDiscovery(router, manifest, routeModules, ssr, routeDiscovery, isSpaMode) {
  react__WEBPACK_IMPORTED_MODULE_0__.useEffect(() => {
    if (!isFogOfWarEnabled(routeDiscovery, ssr) || // @ts-expect-error - TS doesn't know about this yet
    window.navigator?.connection?.saveData === true) {
      return;
    }
    function registerElement(el) {
      let path = el.tagName === "FORM" ? el.getAttribute("action") : el.getAttribute("href");
      if (!path) {
        return;
      }
      let pathname = el.tagName === "A" ? el.pathname : new URL(path, window.location.origin).pathname;
      if (!discoveredPaths.has(pathname)) {
        nextPaths.add(pathname);
      }
    }
    async function fetchPatches() {
      document.querySelectorAll("a[data-discover], form[data-discover]").forEach(registerElement);
      let lazyPaths = Array.from(nextPaths.keys()).filter((path) => {
        if (discoveredPaths.has(path)) {
          nextPaths.delete(path);
          return false;
        }
        return true;
      });
      if (lazyPaths.length === 0) {
        return;
      }
      try {
        await fetchAndApplyManifestPatches(
          lazyPaths,
          null,
          manifest,
          routeModules,
          ssr,
          isSpaMode,
          router.basename,
          routeDiscovery.manifestPath,
          router.patchRoutes
        );
      } catch (e) {
        console.error("Failed to fetch manifest patches", e);
      }
    }
    let debouncedFetchPatches = debounce(fetchPatches, 100);
    fetchPatches();
    let observer = new MutationObserver(() => debouncedFetchPatches());
    observer.observe(document.documentElement, {
      subtree: true,
      childList: true,
      attributes: true,
      attributeFilter: ["data-discover", "href", "action"]
    });
    return () => observer.disconnect();
  }, [ssr, isSpaMode, manifest, routeModules, router, routeDiscovery]);
}
function getManifestPath(_manifestPath, basename) {
  let manifestPath = _manifestPath || "/__manifest";
  if (basename == null) {
    return manifestPath;
  }
  return `${basename}${manifestPath}`.replace(/\/+/g, "/");
}
var MANIFEST_VERSION_STORAGE_KEY = "react-router-manifest-version";
async function fetchAndApplyManifestPatches(paths, errorReloadPath, manifest, routeModules, ssr, isSpaMode, basename, manifestPath, patchRoutes, signal) {
  const searchParams = new URLSearchParams();
  searchParams.set("paths", paths.sort().join(","));
  searchParams.set("version", manifest.version);
  let url = new URL(
    getManifestPath(manifestPath, basename),
    window.location.origin
  );
  url.search = searchParams.toString();
  if (url.toString().length > URL_LIMIT) {
    nextPaths.clear();
    return;
  }
  let serverPatches;
  try {
    let res = await fetch(url, { signal });
    if (!res.ok) {
      throw new Error(`${res.status} ${res.statusText}`);
    } else if (res.status === 204 && res.headers.has("X-Remix-Reload-Document")) {
      if (!errorReloadPath) {
        console.warn(
          "Detected a manifest version mismatch during eager route discovery. The next navigation/fetch to an undiscovered route will result in a new document navigation to sync up with the latest manifest."
        );
        return;
      }
      try {
        if (sessionStorage.getItem(MANIFEST_VERSION_STORAGE_KEY) === manifest.version) {
          console.error(
            "Unable to discover routes due to manifest version mismatch."
          );
          return;
        }
        sessionStorage.setItem(MANIFEST_VERSION_STORAGE_KEY, manifest.version);
      } catch {
      }
      window.location.href = errorReloadPath;
      console.warn("Detected manifest version mismatch, reloading...");
      await new Promise(() => {
      });
    } else if (res.status >= 400) {
      throw new Error(await res.text());
    }
    try {
      sessionStorage.removeItem(MANIFEST_VERSION_STORAGE_KEY);
    } catch {
    }
    serverPatches = await res.json();
  } catch (e) {
    if (signal?.aborted) return;
    throw e;
  }
  let knownRoutes = new Set(Object.keys(manifest.routes));
  let patches = Object.values(serverPatches).reduce((acc, route) => {
    if (route && !knownRoutes.has(route.id)) {
      acc[route.id] = route;
    }
    return acc;
  }, {});
  Object.assign(manifest.routes, patches);
  paths.forEach((p) => addToFifoQueue(p, discoveredPaths));
  let parentIds = /* @__PURE__ */ new Set();
  Object.values(patches).forEach((patch) => {
    if (patch && (!patch.parentId || !patches[patch.parentId])) {
      parentIds.add(patch.parentId);
    }
  });
  parentIds.forEach(
    (parentId) => patchRoutes(
      parentId || null,
      createClientRoutes(patches, routeModules, null, ssr, isSpaMode, parentId)
    )
  );
}
function addToFifoQueue(path, queue) {
  if (queue.size >= discoveredPathsMaxSize) {
    let first = queue.values().next().value;
    queue.delete(first);
  }
  queue.add(path);
}
function debounce(callback, wait) {
  let timeoutId;
  return (...args) => {
    window.clearTimeout(timeoutId);
    timeoutId = window.setTimeout(() => callback(...args), wait);
  };
}

// lib/dom/ssr/components.tsx
function useDataRouterContext2() {
  let context = react__WEBPACK_IMPORTED_MODULE_0__.useContext(DataRouterContext);
  invariant2(
    context,
    "You must render this element inside a <DataRouterContext.Provider> element"
  );
  return context;
}
function useDataRouterStateContext() {
  let context = react__WEBPACK_IMPORTED_MODULE_0__.useContext(DataRouterStateContext);
  invariant2(
    context,
    "You must render this element inside a <DataRouterStateContext.Provider> element"
  );
  return context;
}
var FrameworkContext = react__WEBPACK_IMPORTED_MODULE_0__.createContext(void 0);
FrameworkContext.displayName = "FrameworkContext";
function useFrameworkContext() {
  let context = react__WEBPACK_IMPORTED_MODULE_0__.useContext(FrameworkContext);
  invariant2(
    context,
    "You must render this element inside a <HydratedRouter> element"
  );
  return context;
}
function usePrefetchBehavior(prefetch, theirElementProps) {
  let frameworkContext = react__WEBPACK_IMPORTED_MODULE_0__.useContext(FrameworkContext);
  let [maybePrefetch, setMaybePrefetch] = react__WEBPACK_IMPORTED_MODULE_0__.useState(false);
  let [shouldPrefetch, setShouldPrefetch] = react__WEBPACK_IMPORTED_MODULE_0__.useState(false);
  let { onFocus, onBlur, onMouseEnter, onMouseLeave, onTouchStart } = theirElementProps;
  let ref = react__WEBPACK_IMPORTED_MODULE_0__.useRef(null);
  react__WEBPACK_IMPORTED_MODULE_0__.useEffect(() => {
    if (prefetch === "render") {
      setShouldPrefetch(true);
    }
    if (prefetch === "viewport") {
      let callback = (entries) => {
        entries.forEach((entry) => {
          setShouldPrefetch(entry.isIntersecting);
        });
      };
      let observer = new IntersectionObserver(callback, { threshold: 0.5 });
      if (ref.current) observer.observe(ref.current);
      return () => {
        observer.disconnect();
      };
    }
  }, [prefetch]);
  react__WEBPACK_IMPORTED_MODULE_0__.useEffect(() => {
    if (maybePrefetch) {
      let id = setTimeout(() => {
        setShouldPrefetch(true);
      }, 100);
      return () => {
        clearTimeout(id);
      };
    }
  }, [maybePrefetch]);
  let setIntent = () => {
    setMaybePrefetch(true);
  };
  let cancelIntent = () => {
    setMaybePrefetch(false);
    setShouldPrefetch(false);
  };
  if (!frameworkContext) {
    return [false, ref, {}];
  }
  if (prefetch !== "intent") {
    return [shouldPrefetch, ref, {}];
  }
  return [
    shouldPrefetch,
    ref,
    {
      onFocus: composeEventHandlers(onFocus, setIntent),
      onBlur: composeEventHandlers(onBlur, cancelIntent),
      onMouseEnter: composeEventHandlers(onMouseEnter, setIntent),
      onMouseLeave: composeEventHandlers(onMouseLeave, cancelIntent),
      onTouchStart: composeEventHandlers(onTouchStart, setIntent)
    }
  ];
}
function composeEventHandlers(theirHandler, ourHandler) {
  return (event) => {
    theirHandler && theirHandler(event);
    if (!event.defaultPrevented) {
      ourHandler(event);
    }
  };
}
function getActiveMatches(matches, errors, isSpaMode) {
  if (isSpaMode && !isHydrated) {
    return [matches[0]];
  }
  if (errors) {
    let errorIdx = matches.findIndex((m) => errors[m.route.id] !== void 0);
    return matches.slice(0, errorIdx + 1);
  }
  return matches;
}
var CRITICAL_CSS_DATA_ATTRIBUTE = "data-react-router-critical-css";
function Links({ nonce, crossOrigin }) {
  let { isSpaMode, manifest, routeModules, criticalCss } = useFrameworkContext();
  let { errors, matches: routerMatches } = useDataRouterStateContext();
  let matches = getActiveMatches(routerMatches, errors, isSpaMode);
  let keyedLinks = react__WEBPACK_IMPORTED_MODULE_0__.useMemo(
    () => getKeyedLinksForMatches(matches, routeModules, manifest),
    [matches, routeModules, manifest]
  );
  return /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement(react__WEBPACK_IMPORTED_MODULE_0__.Fragment, null, typeof criticalCss === "string" ? /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement(
    "style",
    {
      ...{ [CRITICAL_CSS_DATA_ATTRIBUTE]: "" },
      nonce,
      dangerouslySetInnerHTML: { __html: criticalCss }
    }
  ) : null, typeof criticalCss === "object" ? /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement(
    "link",
    {
      ...{ [CRITICAL_CSS_DATA_ATTRIBUTE]: "" },
      rel: "stylesheet",
      href: criticalCss.href,
      nonce,
      crossOrigin
    }
  ) : null, keyedLinks.map(
    ({ key, link }) => isPageLinkDescriptor(link) ? /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement(
      PrefetchPageLinks,
      {
        key,
        nonce,
        ...link,
        crossOrigin: link.crossOrigin ?? crossOrigin
      }
    ) : /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement(
      "link",
      {
        key,
        nonce,
        ...link,
        crossOrigin: link.crossOrigin ?? crossOrigin
      }
    )
  ));
}
function PrefetchPageLinks({ page, ...linkProps }) {
  let { router } = useDataRouterContext2();
  let matches = react__WEBPACK_IMPORTED_MODULE_0__.useMemo(
    () => matchRoutes(router.routes, page, router.basename),
    [router.routes, page, router.basename]
  );
  if (!matches) {
    return null;
  }
  return /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement(PrefetchPageLinksImpl, { page, matches, ...linkProps });
}
function useKeyedPrefetchLinks(matches) {
  let { manifest, routeModules } = useFrameworkContext();
  let [keyedPrefetchLinks, setKeyedPrefetchLinks] = react__WEBPACK_IMPORTED_MODULE_0__.useState([]);
  react__WEBPACK_IMPORTED_MODULE_0__.useEffect(() => {
    let interrupted = false;
    void getKeyedPrefetchLinks(matches, manifest, routeModules).then(
      (links) => {
        if (!interrupted) {
          setKeyedPrefetchLinks(links);
        }
      }
    );
    return () => {
      interrupted = true;
    };
  }, [matches, manifest, routeModules]);
  return keyedPrefetchLinks;
}
function PrefetchPageLinksImpl({
  page,
  matches: nextMatches,
  ...linkProps
}) {
  let location = useLocation();
  let { future, manifest, routeModules } = useFrameworkContext();
  let { basename } = useDataRouterContext2();
  let { loaderData, matches } = useDataRouterStateContext();
  let newMatchesForData = react__WEBPACK_IMPORTED_MODULE_0__.useMemo(
    () => getNewMatchesForLinks(
      page,
      nextMatches,
      matches,
      manifest,
      location,
      "data"
    ),
    [page, nextMatches, matches, manifest, location]
  );
  let newMatchesForAssets = react__WEBPACK_IMPORTED_MODULE_0__.useMemo(
    () => getNewMatchesForLinks(
      page,
      nextMatches,
      matches,
      manifest,
      location,
      "assets"
    ),
    [page, nextMatches, matches, manifest, location]
  );
  let dataHrefs = react__WEBPACK_IMPORTED_MODULE_0__.useMemo(() => {
    if (page === location.pathname + location.search + location.hash) {
      return [];
    }
    let routesParams = /* @__PURE__ */ new Set();
    let foundOptOutRoute = false;
    nextMatches.forEach((m) => {
      let manifestRoute = manifest.routes[m.route.id];
      if (!manifestRoute || !manifestRoute.hasLoader) {
        return;
      }
      if (!newMatchesForData.some((m2) => m2.route.id === m.route.id) && m.route.id in loaderData && routeModules[m.route.id]?.shouldRevalidate) {
        foundOptOutRoute = true;
      } else if (manifestRoute.hasClientLoader) {
        foundOptOutRoute = true;
      } else {
        routesParams.add(m.route.id);
      }
    });
    if (routesParams.size === 0) {
      return [];
    }
    let url = singleFetchUrl(
      page,
      basename,
      future.unstable_trailingSlashAwareDataRequests,
      "data"
    );
    if (foundOptOutRoute && routesParams.size > 0) {
      url.searchParams.set(
        "_routes",
        nextMatches.filter((m) => routesParams.has(m.route.id)).map((m) => m.route.id).join(",")
      );
    }
    return [url.pathname + url.search];
  }, [
    basename,
    future.unstable_trailingSlashAwareDataRequests,
    loaderData,
    location,
    manifest,
    newMatchesForData,
    nextMatches,
    page,
    routeModules
  ]);
  let moduleHrefs = react__WEBPACK_IMPORTED_MODULE_0__.useMemo(
    () => getModuleLinkHrefs(newMatchesForAssets, manifest),
    [newMatchesForAssets, manifest]
  );
  let keyedPrefetchLinks = useKeyedPrefetchLinks(newMatchesForAssets);
  return /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement(react__WEBPACK_IMPORTED_MODULE_0__.Fragment, null, dataHrefs.map((href) => /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement("link", { key: href, rel: "prefetch", as: "fetch", href, ...linkProps })), moduleHrefs.map((href) => /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement("link", { key: href, rel: "modulepreload", href, ...linkProps })), keyedPrefetchLinks.map(({ key, link }) => (
    // these don't spread `linkProps` because they are full link descriptors
    // already with their own props
    /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement(
      "link",
      {
        key,
        nonce: linkProps.nonce,
        ...link,
        crossOrigin: link.crossOrigin ?? linkProps.crossOrigin
      }
    )
  )));
}
function Meta() {
  let { isSpaMode, routeModules } = useFrameworkContext();
  let {
    errors,
    matches: routerMatches,
    loaderData
  } = useDataRouterStateContext();
  let location = useLocation();
  let _matches = getActiveMatches(routerMatches, errors, isSpaMode);
  let error = null;
  if (errors) {
    error = errors[_matches[_matches.length - 1].route.id];
  }
  let meta = [];
  let leafMeta = null;
  let matches = [];
  for (let i = 0; i < _matches.length; i++) {
    let _match = _matches[i];
    let routeId = _match.route.id;
    let data2 = loaderData[routeId];
    let params = _match.params;
    let routeModule = routeModules[routeId];
    let routeMeta = [];
    let match = {
      id: routeId,
      data: data2,
      loaderData: data2,
      meta: [],
      params: _match.params,
      pathname: _match.pathname,
      handle: _match.route.handle,
      error
    };
    matches[i] = match;
    if (routeModule?.meta) {
      routeMeta = typeof routeModule.meta === "function" ? routeModule.meta({
        data: data2,
        loaderData: data2,
        params,
        location,
        matches,
        error
      }) : Array.isArray(routeModule.meta) ? [...routeModule.meta] : routeModule.meta;
    } else if (leafMeta) {
      routeMeta = [...leafMeta];
    }
    routeMeta = routeMeta || [];
    if (!Array.isArray(routeMeta)) {
      throw new Error(
        "The route at " + _match.route.path + " returns an invalid value. All route meta functions must return an array of meta objects.\n\nTo reference the meta function API, see https://reactrouter.com/start/framework/route-module#meta"
      );
    }
    match.meta = routeMeta;
    matches[i] = match;
    meta = [...routeMeta];
    leafMeta = meta;
  }
  return /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement(react__WEBPACK_IMPORTED_MODULE_0__.Fragment, null, meta.flat().map((metaProps) => {
    if (!metaProps) {
      return null;
    }
    if ("tagName" in metaProps) {
      let { tagName, ...rest } = metaProps;
      if (!isValidMetaTag(tagName)) {
        console.warn(
          `A meta object uses an invalid tagName: ${tagName}. Expected either 'link' or 'meta'`
        );
        return null;
      }
      let Comp = tagName;
      return /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement(Comp, { key: JSON.stringify(rest), ...rest });
    }
    if ("title" in metaProps) {
      return /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement("title", { key: "title" }, String(metaProps.title));
    }
    if ("charset" in metaProps) {
      metaProps.charSet ?? (metaProps.charSet = metaProps.charset);
      delete metaProps.charset;
    }
    if ("charSet" in metaProps && metaProps.charSet != null) {
      return typeof metaProps.charSet === "string" ? /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement("meta", { key: "charSet", charSet: metaProps.charSet }) : null;
    }
    if ("script:ld+json" in metaProps) {
      try {
        let json = JSON.stringify(metaProps["script:ld+json"]);
        return /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement(
          "script",
          {
            key: `script:ld+json:${json}`,
            type: "application/ld+json",
            dangerouslySetInnerHTML: { __html: escapeHtml(json) }
          }
        );
      } catch (err) {
        return null;
      }
    }
    return /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement("meta", { key: JSON.stringify(metaProps), ...metaProps });
  }));
}
function isValidMetaTag(tagName) {
  return typeof tagName === "string" && /^(meta|link)$/.test(tagName);
}
var isHydrated = false;
function setIsHydrated() {
  isHydrated = true;
}
function Scripts(scriptProps) {
  let {
    manifest,
    serverHandoffString,
    isSpaMode,
    renderMeta,
    routeDiscovery,
    ssr
  } = useFrameworkContext();
  let { router, static: isStatic, staticContext } = useDataRouterContext2();
  let { matches: routerMatches } = useDataRouterStateContext();
  let isRSCRouterContext = useIsRSCRouterContext();
  let enableFogOfWar = isFogOfWarEnabled(routeDiscovery, ssr);
  if (renderMeta) {
    renderMeta.didRenderScripts = true;
  }
  let matches = getActiveMatches(routerMatches, null, isSpaMode);
  react__WEBPACK_IMPORTED_MODULE_0__.useEffect(() => {
    setIsHydrated();
  }, []);
  let initialScripts = react__WEBPACK_IMPORTED_MODULE_0__.useMemo(() => {
    if (isRSCRouterContext) {
      return null;
    }
    let streamScript = "window.__reactRouterContext.stream = new ReadableStream({start(controller){window.__reactRouterContext.streamController = controller;}}).pipeThrough(new TextEncoderStream());";
    let contextScript = staticContext ? `window.__reactRouterContext = ${serverHandoffString};${streamScript}` : " ";
    let routeModulesScript = !isStatic ? " " : `${manifest.hmr?.runtime ? `import ${JSON.stringify(manifest.hmr.runtime)};` : ""}${!enableFogOfWar ? `import ${JSON.stringify(manifest.url)}` : ""};
${matches.map((match, routeIndex) => {
      let routeVarName = `route${routeIndex}`;
      let manifestEntry = manifest.routes[match.route.id];
      invariant2(manifestEntry, `Route ${match.route.id} not found in manifest`);
      let {
        clientActionModule,
        clientLoaderModule,
        clientMiddlewareModule,
        hydrateFallbackModule,
        module
      } = manifestEntry;
      let chunks = [
        ...clientActionModule ? [
          {
            module: clientActionModule,
            varName: `${routeVarName}_clientAction`
          }
        ] : [],
        ...clientLoaderModule ? [
          {
            module: clientLoaderModule,
            varName: `${routeVarName}_clientLoader`
          }
        ] : [],
        ...clientMiddlewareModule ? [
          {
            module: clientMiddlewareModule,
            varName: `${routeVarName}_clientMiddleware`
          }
        ] : [],
        ...hydrateFallbackModule ? [
          {
            module: hydrateFallbackModule,
            varName: `${routeVarName}_HydrateFallback`
          }
        ] : [],
        { module, varName: `${routeVarName}_main` }
      ];
      if (chunks.length === 1) {
        return `import * as ${routeVarName} from ${JSON.stringify(module)};`;
      }
      let chunkImportsSnippet = chunks.map((chunk) => `import * as ${chunk.varName} from "${chunk.module}";`).join("\n");
      let mergedChunksSnippet = `const ${routeVarName} = {${chunks.map((chunk) => `...${chunk.varName}`).join(",")}};`;
      return [chunkImportsSnippet, mergedChunksSnippet].join("\n");
    }).join("\n")}
  ${enableFogOfWar ? (
      // Inline a minimal manifest with the SSR matches
      `window.__reactRouterManifest = ${JSON.stringify(
        getPartialManifest(manifest, router),
        null,
        2
      )};`
    ) : ""}
  window.__reactRouterRouteModules = {${matches.map((match, index) => `${JSON.stringify(match.route.id)}:route${index}`).join(",")}};

import(${JSON.stringify(manifest.entry.module)});`;
    return /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement(react__WEBPACK_IMPORTED_MODULE_0__.Fragment, null, /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement(
      "script",
      {
        ...scriptProps,
        suppressHydrationWarning: true,
        dangerouslySetInnerHTML: { __html: contextScript },
        type: void 0
      }
    ), /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement(
      "script",
      {
        ...scriptProps,
        suppressHydrationWarning: true,
        dangerouslySetInnerHTML: { __html: routeModulesScript },
        type: "module",
        async: true
      }
    ));
  }, []);
  let preloads = isHydrated || isRSCRouterContext ? [] : dedupe(
    manifest.entry.imports.concat(
      getModuleLinkHrefs(matches, manifest, {
        includeHydrateFallback: true
      })
    )
  );
  let sri = typeof manifest.sri === "object" ? manifest.sri : {};
  warnOnce(
    !isRSCRouterContext,
    "The <Scripts /> element is a no-op when using RSC and can be safely removed."
  );
  return isHydrated || isRSCRouterContext ? null : /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement(react__WEBPACK_IMPORTED_MODULE_0__.Fragment, null, typeof manifest.sri === "object" ? /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement(
    "script",
    {
      ...scriptProps,
      "rr-importmap": "",
      type: "importmap",
      suppressHydrationWarning: true,
      dangerouslySetInnerHTML: {
        __html: JSON.stringify({
          integrity: sri
        })
      }
    }
  ) : null, !enableFogOfWar ? /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement(
    "link",
    {
      rel: "modulepreload",
      href: manifest.url,
      crossOrigin: scriptProps.crossOrigin,
      integrity: sri[manifest.url],
      suppressHydrationWarning: true
    }
  ) : null, /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement(
    "link",
    {
      rel: "modulepreload",
      href: manifest.entry.module,
      crossOrigin: scriptProps.crossOrigin,
      integrity: sri[manifest.entry.module],
      suppressHydrationWarning: true
    }
  ), preloads.map((path) => /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement(
    "link",
    {
      key: path,
      rel: "modulepreload",
      href: path,
      crossOrigin: scriptProps.crossOrigin,
      integrity: sri[path],
      suppressHydrationWarning: true
    }
  )), initialScripts);
}
function dedupe(array) {
  return [...new Set(array)];
}
function mergeRefs(...refs) {
  return (value) => {
    refs.forEach((ref) => {
      if (typeof ref === "function") {
        ref(value);
      } else if (ref != null) {
        ref.current = value;
      }
    });
  };
}

// lib/dom/ssr/errorBoundaries.tsx
var RemixErrorBoundary = class extends react__WEBPACK_IMPORTED_MODULE_0__.Component {
  constructor(props) {
    super(props);
    this.state = { error: props.error || null, location: props.location };
  }
  static getDerivedStateFromError(error) {
    return { error };
  }
  static getDerivedStateFromProps(props, state) {
    if (state.location !== props.location) {
      return { error: props.error || null, location: props.location };
    }
    return { error: props.error || state.error, location: state.location };
  }
  render() {
    if (this.state.error) {
      return /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement(
        RemixRootDefaultErrorBoundary,
        {
          error: this.state.error,
          isOutsideRemixApp: true
        }
      );
    } else {
      return this.props.children;
    }
  }
};
function RemixRootDefaultErrorBoundary({
  error,
  isOutsideRemixApp
}) {
  console.error(error);
  let heyDeveloper = /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement(
    "script",
    {
      dangerouslySetInnerHTML: {
        __html: `
        console.log(
          "\u{1F4BF} Hey developer \u{1F44B}. You can provide a way better UX than this when your app throws errors. Check out https://reactrouter.com/how-to/error-boundary for more information."
        );
      `
      }
    }
  );
  if (isRouteErrorResponse(error)) {
    return /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement(BoundaryShell, { title: "Unhandled Thrown Response!" }, /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement("h1", { style: { fontSize: "24px" } }, error.status, " ", error.statusText), ENABLE_DEV_WARNINGS ? heyDeveloper : null);
  }
  let errorInstance;
  if (error instanceof Error) {
    errorInstance = error;
  } else {
    let errorString = error == null ? "Unknown Error" : typeof error === "object" && "toString" in error ? error.toString() : JSON.stringify(error);
    errorInstance = new Error(errorString);
  }
  return /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement(
    BoundaryShell,
    {
      title: "Application Error!",
      isOutsideRemixApp
    },
    /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement("h1", { style: { fontSize: "24px" } }, "Application Error"),
    /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement(
      "pre",
      {
        style: {
          padding: "2rem",
          background: "hsla(10, 50%, 50%, 0.1)",
          color: "red",
          overflow: "auto"
        }
      },
      errorInstance.stack
    ),
    heyDeveloper
  );
}
function BoundaryShell({
  title,
  renderScripts,
  isOutsideRemixApp,
  children
}) {
  let { routeModules } = useFrameworkContext();
  if (routeModules.root?.Layout && !isOutsideRemixApp) {
    return children;
  }
  return /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement("html", { lang: "en" }, /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement("head", null, /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement("meta", { charSet: "utf-8" }), /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement(
    "meta",
    {
      name: "viewport",
      content: "width=device-width,initial-scale=1,viewport-fit=cover"
    }
  ), /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement("title", null, title)), /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement("body", null, /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement("main", { style: { fontFamily: "system-ui, sans-serif", padding: "2rem" } }, children, renderScripts ? /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement(Scripts, null) : null)));
}

// lib/dom/lib.tsx

var isBrowser2 = typeof window !== "undefined" && typeof window.document !== "undefined" && typeof window.document.createElement !== "undefined";
try {
  if (isBrowser2) {
    window.__reactRouterVersion = // @ts-expect-error
    "7.13.2";
  }
} catch (e) {
}
function createBrowserRouter(routes, opts) {
  return createRouter({
    basename: opts?.basename,
    getContext: opts?.getContext,
    future: opts?.future,
    history: createBrowserHistory({ window: opts?.window }),
    hydrationData: opts?.hydrationData || parseHydrationData(),
    routes,
    mapRouteProperties,
    hydrationRouteProperties,
    dataStrategy: opts?.dataStrategy,
    patchRoutesOnNavigation: opts?.patchRoutesOnNavigation,
    window: opts?.window,
    unstable_instrumentations: opts?.unstable_instrumentations
  }).initialize();
}
function createHashRouter(routes, opts) {
  return createRouter({
    basename: opts?.basename,
    getContext: opts?.getContext,
    future: opts?.future,
    history: createHashHistory({ window: opts?.window }),
    hydrationData: opts?.hydrationData || parseHydrationData(),
    routes,
    mapRouteProperties,
    hydrationRouteProperties,
    dataStrategy: opts?.dataStrategy,
    patchRoutesOnNavigation: opts?.patchRoutesOnNavigation,
    window: opts?.window,
    unstable_instrumentations: opts?.unstable_instrumentations
  }).initialize();
}
function parseHydrationData() {
  let state = window?.__staticRouterHydrationData;
  if (state && state.errors) {
    state = {
      ...state,
      errors: deserializeErrors(state.errors)
    };
  }
  return state;
}
function deserializeErrors(errors) {
  if (!errors) return null;
  let entries = Object.entries(errors);
  let serialized = {};
  for (let [key, val] of entries) {
    if (val && val.__type === "RouteErrorResponse") {
      serialized[key] = new ErrorResponseImpl(
        val.status,
        val.statusText,
        val.data,
        val.internal === true
      );
    } else if (val && val.__type === "Error") {
      if (val.__subType) {
        let ErrorConstructor = window[val.__subType];
        if (typeof ErrorConstructor === "function") {
          try {
            let error = new ErrorConstructor(val.message);
            error.stack = "";
            serialized[key] = error;
          } catch (e) {
          }
        }
      }
      if (serialized[key] == null) {
        let error = new Error(val.message);
        error.stack = "";
        serialized[key] = error;
      }
    } else {
      serialized[key] = val;
    }
  }
  return serialized;
}
function BrowserRouter({
  basename,
  children,
  unstable_useTransitions,
  window: window2
}) {
  let historyRef = react__WEBPACK_IMPORTED_MODULE_0__.useRef();
  if (historyRef.current == null) {
    historyRef.current = createBrowserHistory({ window: window2, v5Compat: true });
  }
  let history = historyRef.current;
  let [state, setStateImpl] = react__WEBPACK_IMPORTED_MODULE_0__.useState({
    action: history.action,
    location: history.location
  });
  let setState = react__WEBPACK_IMPORTED_MODULE_0__.useCallback(
    (newState) => {
      if (unstable_useTransitions === false) {
        setStateImpl(newState);
      } else {
        react__WEBPACK_IMPORTED_MODULE_0__.startTransition(() => setStateImpl(newState));
      }
    },
    [unstable_useTransitions]
  );
  react__WEBPACK_IMPORTED_MODULE_0__.useLayoutEffect(() => history.listen(setState), [history, setState]);
  return /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement(
    Router,
    {
      basename,
      children,
      location: state.location,
      navigationType: state.action,
      navigator: history,
      unstable_useTransitions
    }
  );
}
function HashRouter({
  basename,
  children,
  unstable_useTransitions,
  window: window2
}) {
  let historyRef = react__WEBPACK_IMPORTED_MODULE_0__.useRef();
  if (historyRef.current == null) {
    historyRef.current = createHashHistory({ window: window2, v5Compat: true });
  }
  let history = historyRef.current;
  let [state, setStateImpl] = react__WEBPACK_IMPORTED_MODULE_0__.useState({
    action: history.action,
    location: history.location
  });
  let setState = react__WEBPACK_IMPORTED_MODULE_0__.useCallback(
    (newState) => {
      if (unstable_useTransitions === false) {
        setStateImpl(newState);
      } else {
        react__WEBPACK_IMPORTED_MODULE_0__.startTransition(() => setStateImpl(newState));
      }
    },
    [unstable_useTransitions]
  );
  react__WEBPACK_IMPORTED_MODULE_0__.useLayoutEffect(() => history.listen(setState), [history, setState]);
  return /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement(
    Router,
    {
      basename,
      children,
      location: state.location,
      navigationType: state.action,
      navigator: history,
      unstable_useTransitions
    }
  );
}
function HistoryRouter({
  basename,
  children,
  history,
  unstable_useTransitions
}) {
  let [state, setStateImpl] = react__WEBPACK_IMPORTED_MODULE_0__.useState({
    action: history.action,
    location: history.location
  });
  let setState = react__WEBPACK_IMPORTED_MODULE_0__.useCallback(
    (newState) => {
      if (unstable_useTransitions === false) {
        setStateImpl(newState);
      } else {
        react__WEBPACK_IMPORTED_MODULE_0__.startTransition(() => setStateImpl(newState));
      }
    },
    [unstable_useTransitions]
  );
  react__WEBPACK_IMPORTED_MODULE_0__.useLayoutEffect(() => history.listen(setState), [history, setState]);
  return /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement(
    Router,
    {
      basename,
      children,
      location: state.location,
      navigationType: state.action,
      navigator: history,
      unstable_useTransitions
    }
  );
}
HistoryRouter.displayName = "unstable_HistoryRouter";
var ABSOLUTE_URL_REGEX2 = /^(?:[a-z][a-z0-9+.-]*:|\/\/)/i;
var Link = react__WEBPACK_IMPORTED_MODULE_0__.forwardRef(
  function LinkWithRef({
    onClick,
    discover = "render",
    prefetch = "none",
    relative,
    reloadDocument,
    replace: replace2,
    unstable_mask,
    state,
    target,
    to,
    preventScrollReset,
    viewTransition,
    unstable_defaultShouldRevalidate,
    ...rest
  }, forwardedRef) {
    let { basename, navigator, unstable_useTransitions } = react__WEBPACK_IMPORTED_MODULE_0__.useContext(NavigationContext);
    let isAbsolute = typeof to === "string" && ABSOLUTE_URL_REGEX2.test(to);
    let parsed = parseToInfo(to, basename);
    to = parsed.to;
    let href = useHref(to, { relative });
    let location = useLocation();
    let maskedHref = null;
    if (unstable_mask) {
      let resolved = resolveTo(
        unstable_mask,
        [],
        location.unstable_mask ? location.unstable_mask.pathname : "/",
        true
      );
      if (basename !== "/") {
        resolved.pathname = resolved.pathname === "/" ? basename : joinPaths([basename, resolved.pathname]);
      }
      maskedHref = navigator.createHref(resolved);
    }
    let [shouldPrefetch, prefetchRef, prefetchHandlers] = usePrefetchBehavior(
      prefetch,
      rest
    );
    let internalOnClick = useLinkClickHandler(to, {
      replace: replace2,
      unstable_mask,
      state,
      target,
      preventScrollReset,
      relative,
      viewTransition,
      unstable_defaultShouldRevalidate,
      unstable_useTransitions
    });
    function handleClick(event) {
      if (onClick) onClick(event);
      if (!event.defaultPrevented) {
        internalOnClick(event);
      }
    }
    let isSpaLink = !(parsed.isExternal || reloadDocument);
    let link = (
      // eslint-disable-next-line jsx-a11y/anchor-has-content
      /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement(
        "a",
        {
          ...rest,
          ...prefetchHandlers,
          href: (isSpaLink ? maskedHref : void 0) || parsed.absoluteURL || href,
          onClick: isSpaLink ? handleClick : onClick,
          ref: mergeRefs(forwardedRef, prefetchRef),
          target,
          "data-discover": !isAbsolute && discover === "render" ? "true" : void 0
        }
      )
    );
    return shouldPrefetch && !isAbsolute ? /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement(react__WEBPACK_IMPORTED_MODULE_0__.Fragment, null, link, /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement(PrefetchPageLinks, { page: href })) : link;
  }
);
Link.displayName = "Link";
var NavLink = react__WEBPACK_IMPORTED_MODULE_0__.forwardRef(
  function NavLinkWithRef({
    "aria-current": ariaCurrentProp = "page",
    caseSensitive = false,
    className: classNameProp = "",
    end = false,
    style: styleProp,
    to,
    viewTransition,
    children,
    ...rest
  }, ref) {
    let path = useResolvedPath(to, { relative: rest.relative });
    let location = useLocation();
    let routerState = react__WEBPACK_IMPORTED_MODULE_0__.useContext(DataRouterStateContext);
    let { navigator, basename } = react__WEBPACK_IMPORTED_MODULE_0__.useContext(NavigationContext);
    let isTransitioning = routerState != null && // Conditional usage is OK here because the usage of a data router is static
    // eslint-disable-next-line react-hooks/rules-of-hooks
    useViewTransitionState(path) && viewTransition === true;
    let toPathname = navigator.encodeLocation ? navigator.encodeLocation(path).pathname : path.pathname;
    let locationPathname = location.pathname;
    let nextLocationPathname = routerState && routerState.navigation && routerState.navigation.location ? routerState.navigation.location.pathname : null;
    if (!caseSensitive) {
      locationPathname = locationPathname.toLowerCase();
      nextLocationPathname = nextLocationPathname ? nextLocationPathname.toLowerCase() : null;
      toPathname = toPathname.toLowerCase();
    }
    if (nextLocationPathname && basename) {
      nextLocationPathname = stripBasename(nextLocationPathname, basename) || nextLocationPathname;
    }
    const endSlashPosition = toPathname !== "/" && toPathname.endsWith("/") ? toPathname.length - 1 : toPathname.length;
    let isActive = locationPathname === toPathname || !end && locationPathname.startsWith(toPathname) && locationPathname.charAt(endSlashPosition) === "/";
    let isPending = nextLocationPathname != null && (nextLocationPathname === toPathname || !end && nextLocationPathname.startsWith(toPathname) && nextLocationPathname.charAt(toPathname.length) === "/");
    let renderProps = {
      isActive,
      isPending,
      isTransitioning
    };
    let ariaCurrent = isActive ? ariaCurrentProp : void 0;
    let className;
    if (typeof classNameProp === "function") {
      className = classNameProp(renderProps);
    } else {
      className = [
        classNameProp,
        isActive ? "active" : null,
        isPending ? "pending" : null,
        isTransitioning ? "transitioning" : null
      ].filter(Boolean).join(" ");
    }
    let style = typeof styleProp === "function" ? styleProp(renderProps) : styleProp;
    return /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement(
      Link,
      {
        ...rest,
        "aria-current": ariaCurrent,
        className,
        ref,
        style,
        to,
        viewTransition
      },
      typeof children === "function" ? children(renderProps) : children
    );
  }
);
NavLink.displayName = "NavLink";
var Form = react__WEBPACK_IMPORTED_MODULE_0__.forwardRef(
  ({
    discover = "render",
    fetcherKey,
    navigate,
    reloadDocument,
    replace: replace2,
    state,
    method = defaultMethod,
    action,
    onSubmit,
    relative,
    preventScrollReset,
    viewTransition,
    unstable_defaultShouldRevalidate,
    ...props
  }, forwardedRef) => {
    let { unstable_useTransitions } = react__WEBPACK_IMPORTED_MODULE_0__.useContext(NavigationContext);
    let submit = useSubmit();
    let formAction = useFormAction(action, { relative });
    let formMethod = method.toLowerCase() === "get" ? "get" : "post";
    let isAbsolute = typeof action === "string" && ABSOLUTE_URL_REGEX2.test(action);
    let submitHandler = (event) => {
      onSubmit && onSubmit(event);
      if (event.defaultPrevented) return;
      event.preventDefault();
      let submitter = event.nativeEvent.submitter;
      let submitMethod = submitter?.getAttribute("formmethod") || method;
      let doSubmit = () => submit(submitter || event.currentTarget, {
        fetcherKey,
        method: submitMethod,
        navigate,
        replace: replace2,
        state,
        relative,
        preventScrollReset,
        viewTransition,
        unstable_defaultShouldRevalidate
      });
      if (unstable_useTransitions && navigate !== false) {
        react__WEBPACK_IMPORTED_MODULE_0__.startTransition(() => doSubmit());
      } else {
        doSubmit();
      }
    };
    return /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement(
      "form",
      {
        ref: forwardedRef,
        method: formMethod,
        action: formAction,
        onSubmit: reloadDocument ? onSubmit : submitHandler,
        ...props,
        "data-discover": !isAbsolute && discover === "render" ? "true" : void 0
      }
    );
  }
);
Form.displayName = "Form";
function ScrollRestoration({
  getKey,
  storageKey,
  ...props
}) {
  let remixContext = react__WEBPACK_IMPORTED_MODULE_0__.useContext(FrameworkContext);
  let { basename } = react__WEBPACK_IMPORTED_MODULE_0__.useContext(NavigationContext);
  let location = useLocation();
  let matches = useMatches();
  useScrollRestoration({ getKey, storageKey });
  let ssrKey = react__WEBPACK_IMPORTED_MODULE_0__.useMemo(
    () => {
      if (!remixContext || !getKey) return null;
      let userKey = getScrollRestorationKey(
        location,
        matches,
        basename,
        getKey
      );
      return userKey !== location.key ? userKey : null;
    },
    // Nah, we only need this the first time for the SSR render
    // eslint-disable-next-line react-hooks/exhaustive-deps
    []
  );
  if (!remixContext || remixContext.isSpaMode) {
    return null;
  }
  let restoreScroll = ((storageKey2, restoreKey) => {
    if (!window.history.state || !window.history.state.key) {
      let key = Math.random().toString(32).slice(2);
      window.history.replaceState({ key }, "");
    }
    try {
      let positions = JSON.parse(sessionStorage.getItem(storageKey2) || "{}");
      let storedY = positions[restoreKey || window.history.state.key];
      if (typeof storedY === "number") {
        window.scrollTo(0, storedY);
      }
    } catch (error) {
      console.error(error);
      sessionStorage.removeItem(storageKey2);
    }
  }).toString();
  return /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement(
    "script",
    {
      ...props,
      suppressHydrationWarning: true,
      dangerouslySetInnerHTML: {
        __html: `(${restoreScroll})(${escapeHtml(
          JSON.stringify(storageKey || SCROLL_RESTORATION_STORAGE_KEY)
        )}, ${escapeHtml(JSON.stringify(ssrKey))})`
      }
    }
  );
}
ScrollRestoration.displayName = "ScrollRestoration";
function getDataRouterConsoleError2(hookName) {
  return `${hookName} must be used within a data router.  See https://reactrouter.com/en/main/routers/picking-a-router.`;
}
function useDataRouterContext3(hookName) {
  let ctx = react__WEBPACK_IMPORTED_MODULE_0__.useContext(DataRouterContext);
  invariant(ctx, getDataRouterConsoleError2(hookName));
  return ctx;
}
function useDataRouterState2(hookName) {
  let state = react__WEBPACK_IMPORTED_MODULE_0__.useContext(DataRouterStateContext);
  invariant(state, getDataRouterConsoleError2(hookName));
  return state;
}
function useLinkClickHandler(to, {
  target,
  replace: replaceProp,
  unstable_mask,
  state,
  preventScrollReset,
  relative,
  viewTransition,
  unstable_defaultShouldRevalidate,
  unstable_useTransitions
} = {}) {
  let navigate = useNavigate();
  let location = useLocation();
  let path = useResolvedPath(to, { relative });
  return react__WEBPACK_IMPORTED_MODULE_0__.useCallback(
    (event) => {
      if (shouldProcessLinkClick(event, target)) {
        event.preventDefault();
        let replace2 = replaceProp !== void 0 ? replaceProp : createPath(location) === createPath(path);
        let doNavigate = () => navigate(to, {
          replace: replace2,
          unstable_mask,
          state,
          preventScrollReset,
          relative,
          viewTransition,
          unstable_defaultShouldRevalidate
        });
        if (unstable_useTransitions) {
          react__WEBPACK_IMPORTED_MODULE_0__.startTransition(() => doNavigate());
        } else {
          doNavigate();
        }
      }
    },
    [
      location,
      navigate,
      path,
      replaceProp,
      unstable_mask,
      state,
      target,
      to,
      preventScrollReset,
      relative,
      viewTransition,
      unstable_defaultShouldRevalidate,
      unstable_useTransitions
    ]
  );
}
function useSearchParams(defaultInit) {
  warning(
    typeof URLSearchParams !== "undefined",
    `You cannot use the \`useSearchParams\` hook in a browser that does not support the URLSearchParams API. If you need to support Internet Explorer 11, we recommend you load a polyfill such as https://github.com/ungap/url-search-params.`
  );
  let defaultSearchParamsRef = react__WEBPACK_IMPORTED_MODULE_0__.useRef(createSearchParams(defaultInit));
  let hasSetSearchParamsRef = react__WEBPACK_IMPORTED_MODULE_0__.useRef(false);
  let location = useLocation();
  let searchParams = react__WEBPACK_IMPORTED_MODULE_0__.useMemo(
    () => (
      // Only merge in the defaults if we haven't yet called setSearchParams.
      // Once we call that we want those to take precedence, otherwise you can't
      // remove a param with setSearchParams({}) if it has an initial value
      getSearchParamsForLocation(
        location.search,
        hasSetSearchParamsRef.current ? null : defaultSearchParamsRef.current
      )
    ),
    [location.search]
  );
  let navigate = useNavigate();
  let setSearchParams = react__WEBPACK_IMPORTED_MODULE_0__.useCallback(
    (nextInit, navigateOptions) => {
      const newSearchParams = createSearchParams(
        typeof nextInit === "function" ? nextInit(new URLSearchParams(searchParams)) : nextInit
      );
      hasSetSearchParamsRef.current = true;
      navigate("?" + newSearchParams, navigateOptions);
    },
    [navigate, searchParams]
  );
  return [searchParams, setSearchParams];
}
var fetcherId = 0;
var getUniqueFetcherId = () => `__${String(++fetcherId)}__`;
function useSubmit() {
  let { router } = useDataRouterContext3("useSubmit" /* UseSubmit */);
  let { basename } = react__WEBPACK_IMPORTED_MODULE_0__.useContext(NavigationContext);
  let currentRouteId = useRouteId();
  let routerFetch = router.fetch;
  let routerNavigate = router.navigate;
  return react__WEBPACK_IMPORTED_MODULE_0__.useCallback(
    async (target, options = {}) => {
      let { action, method, encType, formData, body } = getFormSubmissionInfo(
        target,
        basename
      );
      if (options.navigate === false) {
        let key = options.fetcherKey || getUniqueFetcherId();
        await routerFetch(key, currentRouteId, options.action || action, {
          unstable_defaultShouldRevalidate: options.unstable_defaultShouldRevalidate,
          preventScrollReset: options.preventScrollReset,
          formData,
          body,
          formMethod: options.method || method,
          formEncType: options.encType || encType,
          flushSync: options.flushSync
        });
      } else {
        await routerNavigate(options.action || action, {
          unstable_defaultShouldRevalidate: options.unstable_defaultShouldRevalidate,
          preventScrollReset: options.preventScrollReset,
          formData,
          body,
          formMethod: options.method || method,
          formEncType: options.encType || encType,
          replace: options.replace,
          state: options.state,
          fromRouteId: currentRouteId,
          flushSync: options.flushSync,
          viewTransition: options.viewTransition
        });
      }
    },
    [routerFetch, routerNavigate, basename, currentRouteId]
  );
}
function useFormAction(action, { relative } = {}) {
  let { basename } = react__WEBPACK_IMPORTED_MODULE_0__.useContext(NavigationContext);
  let routeContext = react__WEBPACK_IMPORTED_MODULE_0__.useContext(RouteContext);
  invariant(routeContext, "useFormAction must be used inside a RouteContext");
  let [match] = routeContext.matches.slice(-1);
  let path = { ...useResolvedPath(action ? action : ".", { relative }) };
  let location = useLocation();
  if (action == null) {
    path.search = location.search;
    let params = new URLSearchParams(path.search);
    let indexValues = params.getAll("index");
    let hasNakedIndexParam = indexValues.some((v) => v === "");
    if (hasNakedIndexParam) {
      params.delete("index");
      indexValues.filter((v) => v).forEach((v) => params.append("index", v));
      let qs = params.toString();
      path.search = qs ? `?${qs}` : "";
    }
  }
  if ((!action || action === ".") && match.route.index) {
    path.search = path.search ? path.search.replace(/^\?/, "?index&") : "?index";
  }
  if (basename !== "/") {
    path.pathname = path.pathname === "/" ? basename : joinPaths([basename, path.pathname]);
  }
  return createPath(path);
}
function useFetcher({
  key
} = {}) {
  let { router } = useDataRouterContext3("useFetcher" /* UseFetcher */);
  let state = useDataRouterState2("useFetcher" /* UseFetcher */);
  let fetcherData = react__WEBPACK_IMPORTED_MODULE_0__.useContext(FetchersContext);
  let route = react__WEBPACK_IMPORTED_MODULE_0__.useContext(RouteContext);
  let routeId = route.matches[route.matches.length - 1]?.route.id;
  invariant(fetcherData, `useFetcher must be used inside a FetchersContext`);
  invariant(route, `useFetcher must be used inside a RouteContext`);
  invariant(
    routeId != null,
    `useFetcher can only be used on routes that contain a unique "id"`
  );
  let defaultKey = react__WEBPACK_IMPORTED_MODULE_0__.useId();
  let [fetcherKey, setFetcherKey] = react__WEBPACK_IMPORTED_MODULE_0__.useState(key || defaultKey);
  if (key && key !== fetcherKey) {
    setFetcherKey(key);
  }
  let { deleteFetcher, getFetcher, resetFetcher, fetch: routerFetch } = router;
  react__WEBPACK_IMPORTED_MODULE_0__.useEffect(() => {
    getFetcher(fetcherKey);
    return () => deleteFetcher(fetcherKey);
  }, [deleteFetcher, getFetcher, fetcherKey]);
  let load = react__WEBPACK_IMPORTED_MODULE_0__.useCallback(
    async (href, opts) => {
      invariant(routeId, "No routeId available for fetcher.load()");
      await routerFetch(fetcherKey, routeId, href, opts);
    },
    [fetcherKey, routeId, routerFetch]
  );
  let submitImpl = useSubmit();
  let submit = react__WEBPACK_IMPORTED_MODULE_0__.useCallback(
    async (target, opts) => {
      await submitImpl(target, {
        ...opts,
        navigate: false,
        fetcherKey
      });
    },
    [fetcherKey, submitImpl]
  );
  let reset = react__WEBPACK_IMPORTED_MODULE_0__.useCallback(
    (opts) => resetFetcher(fetcherKey, opts),
    [resetFetcher, fetcherKey]
  );
  let FetcherForm = react__WEBPACK_IMPORTED_MODULE_0__.useMemo(() => {
    let FetcherForm2 = react__WEBPACK_IMPORTED_MODULE_0__.forwardRef(
      (props, ref) => {
        return /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement(Form, { ...props, navigate: false, fetcherKey, ref });
      }
    );
    FetcherForm2.displayName = "fetcher.Form";
    return FetcherForm2;
  }, [fetcherKey]);
  let fetcher = state.fetchers.get(fetcherKey) || IDLE_FETCHER;
  let data2 = fetcherData.get(fetcherKey);
  let fetcherWithComponents = react__WEBPACK_IMPORTED_MODULE_0__.useMemo(
    () => ({
      Form: FetcherForm,
      submit,
      load,
      reset,
      ...fetcher,
      data: data2
    }),
    [FetcherForm, submit, load, reset, fetcher, data2]
  );
  return fetcherWithComponents;
}
function useFetchers() {
  let state = useDataRouterState2("useFetchers" /* UseFetchers */);
  return Array.from(state.fetchers.entries()).map(([key, fetcher]) => ({
    ...fetcher,
    key
  }));
}
var SCROLL_RESTORATION_STORAGE_KEY = "react-router-scroll-positions";
var savedScrollPositions = {};
function getScrollRestorationKey(location, matches, basename, getKey) {
  let key = null;
  if (getKey) {
    if (basename !== "/") {
      key = getKey(
        {
          ...location,
          pathname: stripBasename(location.pathname, basename) || location.pathname
        },
        matches
      );
    } else {
      key = getKey(location, matches);
    }
  }
  if (key == null) {
    key = location.key;
  }
  return key;
}
function useScrollRestoration({
  getKey,
  storageKey
} = {}) {
  let { router } = useDataRouterContext3("useScrollRestoration" /* UseScrollRestoration */);
  let { restoreScrollPosition, preventScrollReset } = useDataRouterState2(
    "useScrollRestoration" /* UseScrollRestoration */
  );
  let { basename } = react__WEBPACK_IMPORTED_MODULE_0__.useContext(NavigationContext);
  let location = useLocation();
  let matches = useMatches();
  let navigation = useNavigation();
  react__WEBPACK_IMPORTED_MODULE_0__.useEffect(() => {
    window.history.scrollRestoration = "manual";
    return () => {
      window.history.scrollRestoration = "auto";
    };
  }, []);
  usePageHide(
    react__WEBPACK_IMPORTED_MODULE_0__.useCallback(() => {
      if (navigation.state === "idle") {
        let key = getScrollRestorationKey(location, matches, basename, getKey);
        savedScrollPositions[key] = window.scrollY;
      }
      try {
        sessionStorage.setItem(
          storageKey || SCROLL_RESTORATION_STORAGE_KEY,
          JSON.stringify(savedScrollPositions)
        );
      } catch (error) {
        warning(
          false,
          `Failed to save scroll positions in sessionStorage, <ScrollRestoration /> will not work properly (${error}).`
        );
      }
      window.history.scrollRestoration = "auto";
    }, [navigation.state, getKey, basename, location, matches, storageKey])
  );
  if (typeof document !== "undefined") {
    react__WEBPACK_IMPORTED_MODULE_0__.useLayoutEffect(() => {
      try {
        let sessionPositions = sessionStorage.getItem(
          storageKey || SCROLL_RESTORATION_STORAGE_KEY
        );
        if (sessionPositions) {
          savedScrollPositions = JSON.parse(sessionPositions);
        }
      } catch (e) {
      }
    }, [storageKey]);
    react__WEBPACK_IMPORTED_MODULE_0__.useLayoutEffect(() => {
      let disableScrollRestoration = router?.enableScrollRestoration(
        savedScrollPositions,
        () => window.scrollY,
        getKey ? (location2, matches2) => getScrollRestorationKey(location2, matches2, basename, getKey) : void 0
      );
      return () => disableScrollRestoration && disableScrollRestoration();
    }, [router, basename, getKey]);
    react__WEBPACK_IMPORTED_MODULE_0__.useLayoutEffect(() => {
      if (restoreScrollPosition === false) {
        return;
      }
      if (typeof restoreScrollPosition === "number") {
        window.scrollTo(0, restoreScrollPosition);
        return;
      }
      try {
        if (location.hash) {
          let el = document.getElementById(
            decodeURIComponent(location.hash.slice(1))
          );
          if (el) {
            el.scrollIntoView();
            return;
          }
        }
      } catch {
        warning(
          false,
          `"${location.hash.slice(
            1
          )}" is not a decodable element ID. The view will not scroll to it.`
        );
      }
      if (preventScrollReset === true) {
        return;
      }
      window.scrollTo(0, 0);
    }, [location, restoreScrollPosition, preventScrollReset]);
  }
}
function useBeforeUnload(callback, options) {
  let { capture } = options || {};
  react__WEBPACK_IMPORTED_MODULE_0__.useEffect(() => {
    let opts = capture != null ? { capture } : void 0;
    window.addEventListener("beforeunload", callback, opts);
    return () => {
      window.removeEventListener("beforeunload", callback, opts);
    };
  }, [callback, capture]);
}
function usePageHide(callback, options) {
  let { capture } = options || {};
  react__WEBPACK_IMPORTED_MODULE_0__.useEffect(() => {
    let opts = capture != null ? { capture } : void 0;
    window.addEventListener("pagehide", callback, opts);
    return () => {
      window.removeEventListener("pagehide", callback, opts);
    };
  }, [callback, capture]);
}
function usePrompt({
  when,
  message
}) {
  let blocker = useBlocker(when);
  react__WEBPACK_IMPORTED_MODULE_0__.useEffect(() => {
    if (blocker.state === "blocked") {
      let proceed = window.confirm(message);
      if (proceed) {
        setTimeout(blocker.proceed, 0);
      } else {
        blocker.reset();
      }
    }
  }, [blocker, message]);
  react__WEBPACK_IMPORTED_MODULE_0__.useEffect(() => {
    if (blocker.state === "blocked" && !when) {
      blocker.reset();
    }
  }, [blocker, when]);
}
function useViewTransitionState(to, { relative } = {}) {
  let vtContext = react__WEBPACK_IMPORTED_MODULE_0__.useContext(ViewTransitionContext);
  invariant(
    vtContext != null,
    "`useViewTransitionState` must be used within `react-router-dom`'s `RouterProvider`.  Did you accidentally import `RouterProvider` from `react-router`?"
  );
  let { basename } = useDataRouterContext3(
    "useViewTransitionState" /* useViewTransitionState */
  );
  let path = useResolvedPath(to, { relative });
  if (!vtContext.isTransitioning) {
    return false;
  }
  let currentPath = stripBasename(vtContext.currentLocation.pathname, basename) || vtContext.currentLocation.pathname;
  let nextPath = stripBasename(vtContext.nextLocation.pathname, basename) || vtContext.nextLocation.pathname;
  return matchPath(path.pathname, nextPath) != null || matchPath(path.pathname, currentPath) != null;
}

// lib/dom/server.tsx

function StaticRouter({
  basename,
  children,
  location: locationProp = "/"
}) {
  if (typeof locationProp === "string") {
    locationProp = parsePath(locationProp);
  }
  let action = "POP" /* Pop */;
  let location = {
    pathname: locationProp.pathname || "/",
    search: locationProp.search || "",
    hash: locationProp.hash || "",
    state: locationProp.state != null ? locationProp.state : null,
    key: locationProp.key || "default",
    unstable_mask: void 0
  };
  let staticNavigator = getStatelessNavigator();
  return /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement(
    Router,
    {
      basename,
      children,
      location,
      navigationType: action,
      navigator: staticNavigator,
      static: true,
      unstable_useTransitions: false
    }
  );
}
function StaticRouterProvider({
  context,
  router,
  hydrate: hydrate2 = true,
  nonce
}) {
  invariant(
    router && context,
    "You must provide `router` and `context` to <StaticRouterProvider>"
  );
  let dataRouterContext = {
    router,
    navigator: getStatelessNavigator(),
    static: true,
    staticContext: context,
    basename: context.basename || "/"
  };
  let fetchersContext = /* @__PURE__ */ new Map();
  let hydrateScript = "";
  if (hydrate2 !== false) {
    let data2 = {
      loaderData: context.loaderData,
      actionData: context.actionData,
      errors: serializeErrors(context.errors)
    };
    let json = escapeHtml(JSON.stringify(JSON.stringify(data2)));
    hydrateScript = `window.__staticRouterHydrationData = JSON.parse(${json});`;
  }
  let { state } = dataRouterContext.router;
  return /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement(react__WEBPACK_IMPORTED_MODULE_0__.Fragment, null, /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement(DataRouterContext.Provider, { value: dataRouterContext }, /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement(DataRouterStateContext.Provider, { value: state }, /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement(FetchersContext.Provider, { value: fetchersContext }, /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement(ViewTransitionContext.Provider, { value: { isTransitioning: false } }, /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement(
    Router,
    {
      basename: dataRouterContext.basename,
      location: state.location,
      navigationType: state.historyAction,
      navigator: dataRouterContext.navigator,
      static: dataRouterContext.static,
      unstable_useTransitions: false
    },
    /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement(
      DataRoutes,
      {
        routes: router.routes,
        future: router.future,
        state,
        isStatic: true
      }
    )
  ))))), hydrateScript ? /* @__PURE__ */ react__WEBPACK_IMPORTED_MODULE_0__.createElement(
    "script",
    {
      suppressHydrationWarning: true,
      nonce,
      dangerouslySetInnerHTML: { __html: hydrateScript }
    }
  ) : null);
}
function serializeErrors(errors) {
  if (!errors) return null;
  let entries = Object.entries(errors);
  let serialized = {};
  for (let [key, val] of entries) {
    if (isRouteErrorResponse(val)) {
      serialized[key] = { ...val, __type: "RouteErrorResponse" };
    } else if (val instanceof Error) {
      serialized[key] = {
        message: val.message,
        __type: "Error",
        // If this is a subclass (i.e., ReferenceError), send up the type so we
        // can re-create the same type during hydration.
        ...val.name !== "Error" ? {
          __subType: val.name
        } : {}
      };
    } else {
      serialized[key] = val;
    }
  }
  return serialized;
}
function getStatelessNavigator() {
  return {
    createHref,
    encodeLocation,
    push(to) {
      throw new Error(
        `You cannot use navigator.push() on the server because it is a stateless environment. This error was probably triggered when you did a \`navigate(${JSON.stringify(to)})\` somewhere in your app.`
      );
    },
    replace(to) {
      throw new Error(
        `You cannot use navigator.replace() on the server because it is a stateless environment. This error was probably triggered when you did a \`navigate(${JSON.stringify(to)}, { replace: true })\` somewhere in your app.`
      );
    },
    go(delta) {
      throw new Error(
        `You cannot use navigator.go() on the server because it is a stateless environment. This error was probably triggered when you did a \`navigate(${delta})\` somewhere in your app.`
      );
    },
    back() {
      throw new Error(
        `You cannot use navigator.back() on the server because it is a stateless environment.`
      );
    },
    forward() {
      throw new Error(
        `You cannot use navigator.forward() on the server because it is a stateless environment.`
      );
    }
  };
}
function createStaticHandler2(routes, opts) {
  return createStaticHandler(routes, {
    ...opts,
    mapRouteProperties
  });
}
function createStaticRouter(routes, context, opts = {}) {
  let manifest = {};
  let dataRoutes = convertRoutesToDataRoutes(
    routes,
    mapRouteProperties,
    void 0,
    manifest
  );
  let matches = context.matches.map((match) => {
    let route = manifest[match.route.id] || match.route;
    return {
      ...match,
      route
    };
  });
  let msg = (method) => `You cannot use router.${method}() on the server because it is a stateless environment`;
  return {
    get basename() {
      return context.basename;
    },
    get future() {
      return {
        v8_middleware: false,
        unstable_passThroughRequests: false,
        ...opts?.future
      };
    },
    get state() {
      return {
        historyAction: "POP" /* Pop */,
        location: context.location,
        matches,
        loaderData: context.loaderData,
        actionData: context.actionData,
        errors: context.errors,
        initialized: true,
        renderFallback: false,
        navigation: IDLE_NAVIGATION,
        restoreScrollPosition: null,
        preventScrollReset: false,
        revalidation: "idle",
        fetchers: /* @__PURE__ */ new Map(),
        blockers: /* @__PURE__ */ new Map()
      };
    },
    get routes() {
      return dataRoutes;
    },
    get window() {
      return void 0;
    },
    initialize() {
      throw msg("initialize");
    },
    subscribe() {
      throw msg("subscribe");
    },
    enableScrollRestoration() {
      throw msg("enableScrollRestoration");
    },
    navigate() {
      throw msg("navigate");
    },
    fetch() {
      throw msg("fetch");
    },
    revalidate() {
      throw msg("revalidate");
    },
    createHref,
    encodeLocation,
    getFetcher() {
      return IDLE_FETCHER;
    },
    deleteFetcher() {
      throw msg("deleteFetcher");
    },
    resetFetcher() {
      throw msg("resetFetcher");
    },
    dispose() {
      throw msg("dispose");
    },
    getBlocker() {
      return IDLE_BLOCKER;
    },
    deleteBlocker() {
      throw msg("deleteBlocker");
    },
    patchRoutes() {
      throw msg("patchRoutes");
    },
    _internalFetchControllers: /* @__PURE__ */ new Map(),
    _internalSetRoutes() {
      throw msg("_internalSetRoutes");
    },
    _internalSetStateDoNotUseOrYouWillBreakYourApp() {
      throw msg("_internalSetStateDoNotUseOrYouWillBreakYourApp");
    }
  };
}
function createHref(to) {
  return typeof to === "string" ? to : createPath(to);
}
function encodeLocation(to) {
  let href = typeof to === "string" ? to : createPath(to);
  href = href.replace(/ $/, "%20");
  let encoded = ABSOLUTE_URL_REGEX3.test(href) ? new URL(href) : new URL(href, "http://localhost");
  return {
    pathname: encoded.pathname,
    search: encoded.search,
    hash: encoded.hash
  };
}
var ABSOLUTE_URL_REGEX3 = /^(?:[a-z][a-z0-9+.-]*:|\/\/)/i;

/***/ },

/***/ "./node_modules/redux-thunk/dist/redux-thunk.mjs"
/*!*******************************************************!*\
  !*** ./node_modules/redux-thunk/dist/redux-thunk.mjs ***!
  \*******************************************************/
(__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   thunk: () => (/* binding */ thunk),
/* harmony export */   withExtraArgument: () => (/* binding */ withExtraArgument)
/* harmony export */ });
// src/index.ts
function createThunkMiddleware(extraArgument) {
  const middleware = ({ dispatch, getState }) => (next) => (action) => {
    if (typeof action === "function") {
      return action(dispatch, getState, extraArgument);
    }
    return next(action);
  };
  return middleware;
}
var thunk = createThunkMiddleware();
var withExtraArgument = createThunkMiddleware;

/***/ },

/***/ "./node_modules/redux/dist/redux.mjs"
/*!*******************************************!*\
  !*** ./node_modules/redux/dist/redux.mjs ***!
  \*******************************************/
(__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   __DO_NOT_USE__ActionTypes: () => (/* binding */ actionTypes_default),
/* harmony export */   applyMiddleware: () => (/* binding */ applyMiddleware),
/* harmony export */   bindActionCreators: () => (/* binding */ bindActionCreators),
/* harmony export */   combineReducers: () => (/* binding */ combineReducers),
/* harmony export */   compose: () => (/* binding */ compose),
/* harmony export */   createStore: () => (/* binding */ createStore),
/* harmony export */   isAction: () => (/* binding */ isAction),
/* harmony export */   isPlainObject: () => (/* binding */ isPlainObject),
/* harmony export */   legacy_createStore: () => (/* binding */ legacy_createStore)
/* harmony export */ });
// src/utils/formatProdErrorMessage.ts
function formatProdErrorMessage(code) {
  return `Minified Redux error #${code}; visit https://redux.js.org/Errors?code=${code} for the full message or use the non-minified dev environment for full errors. `;
}

// src/utils/symbol-observable.ts
var $$observable = /* @__PURE__ */ (() => typeof Symbol === "function" && Symbol.observable || "@@observable")();
var symbol_observable_default = $$observable;

// src/utils/actionTypes.ts
var randomString = () => Math.random().toString(36).substring(7).split("").join(".");
var ActionTypes = {
  INIT: `@@redux/INIT${/* @__PURE__ */ randomString()}`,
  REPLACE: `@@redux/REPLACE${/* @__PURE__ */ randomString()}`,
  PROBE_UNKNOWN_ACTION: () => `@@redux/PROBE_UNKNOWN_ACTION${randomString()}`
};
var actionTypes_default = ActionTypes;

// src/utils/isPlainObject.ts
function isPlainObject(obj) {
  if (typeof obj !== "object" || obj === null)
    return false;
  let proto = obj;
  while (Object.getPrototypeOf(proto) !== null) {
    proto = Object.getPrototypeOf(proto);
  }
  return Object.getPrototypeOf(obj) === proto || Object.getPrototypeOf(obj) === null;
}

// src/utils/kindOf.ts
function miniKindOf(val) {
  if (val === void 0)
    return "undefined";
  if (val === null)
    return "null";
  const type = typeof val;
  switch (type) {
    case "boolean":
    case "string":
    case "number":
    case "symbol":
    case "function": {
      return type;
    }
  }
  if (Array.isArray(val))
    return "array";
  if (isDate(val))
    return "date";
  if (isError(val))
    return "error";
  const constructorName = ctorName(val);
  switch (constructorName) {
    case "Symbol":
    case "Promise":
    case "WeakMap":
    case "WeakSet":
    case "Map":
    case "Set":
      return constructorName;
  }
  return Object.prototype.toString.call(val).slice(8, -1).toLowerCase().replace(/\s/g, "");
}
function ctorName(val) {
  return typeof val.constructor === "function" ? val.constructor.name : null;
}
function isError(val) {
  return val instanceof Error || typeof val.message === "string" && val.constructor && typeof val.constructor.stackTraceLimit === "number";
}
function isDate(val) {
  if (val instanceof Date)
    return true;
  return typeof val.toDateString === "function" && typeof val.getDate === "function" && typeof val.setDate === "function";
}
function kindOf(val) {
  let typeOfVal = typeof val;
  if (true) {
    typeOfVal = miniKindOf(val);
  }
  return typeOfVal;
}

// src/createStore.ts
function createStore(reducer, preloadedState, enhancer) {
  if (typeof reducer !== "function") {
    throw new Error( false ? 0 : `Expected the root reducer to be a function. Instead, received: '${kindOf(reducer)}'`);
  }
  if (typeof preloadedState === "function" && typeof enhancer === "function" || typeof enhancer === "function" && typeof arguments[3] === "function") {
    throw new Error( false ? 0 : "It looks like you are passing several store enhancers to createStore(). This is not supported. Instead, compose them together to a single function. See https://redux.js.org/tutorials/fundamentals/part-4-store#creating-a-store-with-enhancers for an example.");
  }
  if (typeof preloadedState === "function" && typeof enhancer === "undefined") {
    enhancer = preloadedState;
    preloadedState = void 0;
  }
  if (typeof enhancer !== "undefined") {
    if (typeof enhancer !== "function") {
      throw new Error( false ? 0 : `Expected the enhancer to be a function. Instead, received: '${kindOf(enhancer)}'`);
    }
    return enhancer(createStore)(reducer, preloadedState);
  }
  let currentReducer = reducer;
  let currentState = preloadedState;
  let currentListeners = /* @__PURE__ */ new Map();
  let nextListeners = currentListeners;
  let listenerIdCounter = 0;
  let isDispatching = false;
  function ensureCanMutateNextListeners() {
    if (nextListeners === currentListeners) {
      nextListeners = /* @__PURE__ */ new Map();
      currentListeners.forEach((listener, key) => {
        nextListeners.set(key, listener);
      });
    }
  }
  function getState() {
    if (isDispatching) {
      throw new Error( false ? 0 : "You may not call store.getState() while the reducer is executing. The reducer has already received the state as an argument. Pass it down from the top reducer instead of reading it from the store.");
    }
    return currentState;
  }
  function subscribe(listener) {
    if (typeof listener !== "function") {
      throw new Error( false ? 0 : `Expected the listener to be a function. Instead, received: '${kindOf(listener)}'`);
    }
    if (isDispatching) {
      throw new Error( false ? 0 : "You may not call store.subscribe() while the reducer is executing. If you would like to be notified after the store has been updated, subscribe from a component and invoke store.getState() in the callback to access the latest state. See https://redux.js.org/api/store#subscribelistener for more details.");
    }
    let isSubscribed = true;
    ensureCanMutateNextListeners();
    const listenerId = listenerIdCounter++;
    nextListeners.set(listenerId, listener);
    return function unsubscribe() {
      if (!isSubscribed) {
        return;
      }
      if (isDispatching) {
        throw new Error( false ? 0 : "You may not unsubscribe from a store listener while the reducer is executing. See https://redux.js.org/api/store#subscribelistener for more details.");
      }
      isSubscribed = false;
      ensureCanMutateNextListeners();
      nextListeners.delete(listenerId);
      currentListeners = null;
    };
  }
  function dispatch(action) {
    if (!isPlainObject(action)) {
      throw new Error( false ? 0 : `Actions must be plain objects. Instead, the actual type was: '${kindOf(action)}'. You may need to add middleware to your store setup to handle dispatching other values, such as 'redux-thunk' to handle dispatching functions. See https://redux.js.org/tutorials/fundamentals/part-4-store#middleware and https://redux.js.org/tutorials/fundamentals/part-6-async-logic#using-the-redux-thunk-middleware for examples.`);
    }
    if (typeof action.type === "undefined") {
      throw new Error( false ? 0 : 'Actions may not have an undefined "type" property. You may have misspelled an action type string constant.');
    }
    if (typeof action.type !== "string") {
      throw new Error( false ? 0 : `Action "type" property must be a string. Instead, the actual type was: '${kindOf(action.type)}'. Value was: '${action.type}' (stringified)`);
    }
    if (isDispatching) {
      throw new Error( false ? 0 : "Reducers may not dispatch actions.");
    }
    try {
      isDispatching = true;
      currentState = currentReducer(currentState, action);
    } finally {
      isDispatching = false;
    }
    const listeners = currentListeners = nextListeners;
    listeners.forEach((listener) => {
      listener();
    });
    return action;
  }
  function replaceReducer(nextReducer) {
    if (typeof nextReducer !== "function") {
      throw new Error( false ? 0 : `Expected the nextReducer to be a function. Instead, received: '${kindOf(nextReducer)}`);
    }
    currentReducer = nextReducer;
    dispatch({
      type: actionTypes_default.REPLACE
    });
  }
  function observable() {
    const outerSubscribe = subscribe;
    return {
      /**
       * The minimal observable subscription method.
       * @param observer Any object that can be used as an observer.
       * The observer object should have a `next` method.
       * @returns An object with an `unsubscribe` method that can
       * be used to unsubscribe the observable from the store, and prevent further
       * emission of values from the observable.
       */
      subscribe(observer) {
        if (typeof observer !== "object" || observer === null) {
          throw new Error( false ? 0 : `Expected the observer to be an object. Instead, received: '${kindOf(observer)}'`);
        }
        function observeState() {
          const observerAsObserver = observer;
          if (observerAsObserver.next) {
            observerAsObserver.next(getState());
          }
        }
        observeState();
        const unsubscribe = outerSubscribe(observeState);
        return {
          unsubscribe
        };
      },
      [symbol_observable_default]() {
        return this;
      }
    };
  }
  dispatch({
    type: actionTypes_default.INIT
  });
  const store = {
    dispatch,
    subscribe,
    getState,
    replaceReducer,
    [symbol_observable_default]: observable
  };
  return store;
}
function legacy_createStore(reducer, preloadedState, enhancer) {
  return createStore(reducer, preloadedState, enhancer);
}

// src/utils/warning.ts
function warning(message) {
  if (typeof console !== "undefined" && typeof console.error === "function") {
    console.error(message);
  }
  try {
    throw new Error(message);
  } catch (e) {
  }
}

// src/combineReducers.ts
function getUnexpectedStateShapeWarningMessage(inputState, reducers, action, unexpectedKeyCache) {
  const reducerKeys = Object.keys(reducers);
  const argumentName = action && action.type === actionTypes_default.INIT ? "preloadedState argument passed to createStore" : "previous state received by the reducer";
  if (reducerKeys.length === 0) {
    return "Store does not have a valid reducer. Make sure the argument passed to combineReducers is an object whose values are reducers.";
  }
  if (!isPlainObject(inputState)) {
    return `The ${argumentName} has unexpected type of "${kindOf(inputState)}". Expected argument to be an object with the following keys: "${reducerKeys.join('", "')}"`;
  }
  const unexpectedKeys = Object.keys(inputState).filter((key) => !reducers.hasOwnProperty(key) && !unexpectedKeyCache[key]);
  unexpectedKeys.forEach((key) => {
    unexpectedKeyCache[key] = true;
  });
  if (action && action.type === actionTypes_default.REPLACE)
    return;
  if (unexpectedKeys.length > 0) {
    return `Unexpected ${unexpectedKeys.length > 1 ? "keys" : "key"} "${unexpectedKeys.join('", "')}" found in ${argumentName}. Expected to find one of the known reducer keys instead: "${reducerKeys.join('", "')}". Unexpected keys will be ignored.`;
  }
}
function assertReducerShape(reducers) {
  Object.keys(reducers).forEach((key) => {
    const reducer = reducers[key];
    const initialState = reducer(void 0, {
      type: actionTypes_default.INIT
    });
    if (typeof initialState === "undefined") {
      throw new Error( false ? 0 : `The slice reducer for key "${key}" returned undefined during initialization. If the state passed to the reducer is undefined, you must explicitly return the initial state. The initial state may not be undefined. If you don't want to set a value for this reducer, you can use null instead of undefined.`);
    }
    if (typeof reducer(void 0, {
      type: actionTypes_default.PROBE_UNKNOWN_ACTION()
    }) === "undefined") {
      throw new Error( false ? 0 : `The slice reducer for key "${key}" returned undefined when probed with a random type. Don't try to handle '${actionTypes_default.INIT}' or other actions in "redux/*" namespace. They are considered private. Instead, you must return the current state for any unknown actions, unless it is undefined, in which case you must return the initial state, regardless of the action type. The initial state may not be undefined, but can be null.`);
    }
  });
}
function combineReducers(reducers) {
  const reducerKeys = Object.keys(reducers);
  const finalReducers = {};
  for (let i = 0; i < reducerKeys.length; i++) {
    const key = reducerKeys[i];
    if (true) {
      if (typeof reducers[key] === "undefined") {
        warning(`No reducer provided for key "${key}"`);
      }
    }
    if (typeof reducers[key] === "function") {
      finalReducers[key] = reducers[key];
    }
  }
  const finalReducerKeys = Object.keys(finalReducers);
  let unexpectedKeyCache;
  if (true) {
    unexpectedKeyCache = {};
  }
  let shapeAssertionError;
  try {
    assertReducerShape(finalReducers);
  } catch (e) {
    shapeAssertionError = e;
  }
  return function combination(state = {}, action) {
    if (shapeAssertionError) {
      throw shapeAssertionError;
    }
    if (true) {
      const warningMessage = getUnexpectedStateShapeWarningMessage(state, finalReducers, action, unexpectedKeyCache);
      if (warningMessage) {
        warning(warningMessage);
      }
    }
    let hasChanged = false;
    const nextState = {};
    for (let i = 0; i < finalReducerKeys.length; i++) {
      const key = finalReducerKeys[i];
      const reducer = finalReducers[key];
      const previousStateForKey = state[key];
      const nextStateForKey = reducer(previousStateForKey, action);
      if (typeof nextStateForKey === "undefined") {
        const actionType = action && action.type;
        throw new Error( false ? 0 : `When called with an action of type ${actionType ? `"${String(actionType)}"` : "(unknown type)"}, the slice reducer for key "${key}" returned undefined. To ignore an action, you must explicitly return the previous state. If you want this reducer to hold no value, you can return null instead of undefined.`);
      }
      nextState[key] = nextStateForKey;
      hasChanged = hasChanged || nextStateForKey !== previousStateForKey;
    }
    hasChanged = hasChanged || finalReducerKeys.length !== Object.keys(state).length;
    return hasChanged ? nextState : state;
  };
}

// src/bindActionCreators.ts
function bindActionCreator(actionCreator, dispatch) {
  return function(...args) {
    return dispatch(actionCreator.apply(this, args));
  };
}
function bindActionCreators(actionCreators, dispatch) {
  if (typeof actionCreators === "function") {
    return bindActionCreator(actionCreators, dispatch);
  }
  if (typeof actionCreators !== "object" || actionCreators === null) {
    throw new Error( false ? 0 : `bindActionCreators expected an object or a function, but instead received: '${kindOf(actionCreators)}'. Did you write "import ActionCreators from" instead of "import * as ActionCreators from"?`);
  }
  const boundActionCreators = {};
  for (const key in actionCreators) {
    const actionCreator = actionCreators[key];
    if (typeof actionCreator === "function") {
      boundActionCreators[key] = bindActionCreator(actionCreator, dispatch);
    }
  }
  return boundActionCreators;
}

// src/compose.ts
function compose(...funcs) {
  if (funcs.length === 0) {
    return (arg) => arg;
  }
  if (funcs.length === 1) {
    return funcs[0];
  }
  return funcs.reduce((a, b) => (...args) => a(b(...args)));
}

// src/applyMiddleware.ts
function applyMiddleware(...middlewares) {
  return (createStore2) => (reducer, preloadedState) => {
    const store = createStore2(reducer, preloadedState);
    let dispatch = () => {
      throw new Error( false ? 0 : "Dispatching while constructing your middleware is not allowed. Other middleware would not be applied to this dispatch.");
    };
    const middlewareAPI = {
      getState: store.getState,
      dispatch: (action, ...args) => dispatch(action, ...args)
    };
    const chain = middlewares.map((middleware) => middleware(middlewareAPI));
    dispatch = compose(...chain)(store.dispatch);
    return {
      ...store,
      dispatch
    };
  };
}

// src/utils/isAction.ts
function isAction(action) {
  return isPlainObject(action) && "type" in action && typeof action.type === "string";
}

//# sourceMappingURL=redux.mjs.map

/***/ },

/***/ "./node_modules/reselect/dist/reselect.mjs"
/*!*************************************************!*\
  !*** ./node_modules/reselect/dist/reselect.mjs ***!
  \*************************************************/
(__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   createSelector: () => (/* binding */ createSelector),
/* harmony export */   createSelectorCreator: () => (/* binding */ createSelectorCreator),
/* harmony export */   createStructuredSelector: () => (/* binding */ createStructuredSelector),
/* harmony export */   lruMemoize: () => (/* binding */ lruMemoize),
/* harmony export */   referenceEqualityCheck: () => (/* binding */ referenceEqualityCheck),
/* harmony export */   setGlobalDevModeChecks: () => (/* binding */ setGlobalDevModeChecks),
/* harmony export */   unstable_autotrackMemoize: () => (/* binding */ autotrackMemoize),
/* harmony export */   weakMapMemoize: () => (/* binding */ weakMapMemoize)
/* harmony export */ });
// src/devModeChecks/identityFunctionCheck.ts
var runIdentityFunctionCheck = (resultFunc, inputSelectorsResults, outputSelectorResult) => {
  if (inputSelectorsResults.length === 1 && inputSelectorsResults[0] === outputSelectorResult) {
    let isInputSameAsOutput = false;
    try {
      const emptyObject = {};
      if (resultFunc(emptyObject) === emptyObject)
        isInputSameAsOutput = true;
    } catch {
    }
    if (isInputSameAsOutput) {
      let stack = void 0;
      try {
        throw new Error();
      } catch (e) {
        ;
        ({ stack } = e);
      }
      console.warn(
        "The result function returned its own inputs without modification. e.g\n`createSelector([state => state.todos], todos => todos)`\nThis could lead to inefficient memoization and unnecessary re-renders.\nEnsure transformation logic is in the result function, and extraction logic is in the input selectors.",
        { stack }
      );
    }
  }
};

// src/devModeChecks/inputStabilityCheck.ts
var runInputStabilityCheck = (inputSelectorResultsObject, options, inputSelectorArgs) => {
  const { memoize, memoizeOptions } = options;
  const { inputSelectorResults, inputSelectorResultsCopy } = inputSelectorResultsObject;
  const createAnEmptyObject = memoize(() => ({}), ...memoizeOptions);
  const areInputSelectorResultsEqual = createAnEmptyObject.apply(null, inputSelectorResults) === createAnEmptyObject.apply(null, inputSelectorResultsCopy);
  if (!areInputSelectorResultsEqual) {
    let stack = void 0;
    try {
      throw new Error();
    } catch (e) {
      ;
      ({ stack } = e);
    }
    console.warn(
      "An input selector returned a different result when passed same arguments.\nThis means your output selector will likely run more frequently than intended.\nAvoid returning a new reference inside your input selector, e.g.\n`createSelector([state => state.todos.map(todo => todo.id)], todoIds => todoIds.length)`",
      {
        arguments: inputSelectorArgs,
        firstInputs: inputSelectorResults,
        secondInputs: inputSelectorResultsCopy,
        stack
      }
    );
  }
};

// src/devModeChecks/setGlobalDevModeChecks.ts
var globalDevModeChecks = {
  inputStabilityCheck: "once",
  identityFunctionCheck: "once"
};
var setGlobalDevModeChecks = (devModeChecks) => {
  Object.assign(globalDevModeChecks, devModeChecks);
};

// src/utils.ts
var NOT_FOUND = /* @__PURE__ */ Symbol("NOT_FOUND");
function assertIsFunction(func, errorMessage = `expected a function, instead received ${typeof func}`) {
  if (typeof func !== "function") {
    throw new TypeError(errorMessage);
  }
}
function assertIsObject(object, errorMessage = `expected an object, instead received ${typeof object}`) {
  if (typeof object !== "object") {
    throw new TypeError(errorMessage);
  }
}
function assertIsArrayOfFunctions(array, errorMessage = `expected all items to be functions, instead received the following types: `) {
  if (!array.every((item) => typeof item === "function")) {
    const itemTypes = array.map(
      (item) => typeof item === "function" ? `function ${item.name || "unnamed"}()` : typeof item
    ).join(", ");
    throw new TypeError(`${errorMessage}[${itemTypes}]`);
  }
}
var ensureIsArray = (item) => {
  return Array.isArray(item) ? item : [item];
};
function getDependencies(createSelectorArgs) {
  const dependencies = Array.isArray(createSelectorArgs[0]) ? createSelectorArgs[0] : createSelectorArgs;
  assertIsArrayOfFunctions(
    dependencies,
    `createSelector expects all input-selectors to be functions, but received the following types: `
  );
  return dependencies;
}
function collectInputSelectorResults(dependencies, inputSelectorArgs) {
  const inputSelectorResults = [];
  const { length } = dependencies;
  for (let i = 0; i < length; i++) {
    inputSelectorResults.push(dependencies[i].apply(null, inputSelectorArgs));
  }
  return inputSelectorResults;
}
var getDevModeChecksExecutionInfo = (firstRun, devModeChecks) => {
  const { identityFunctionCheck, inputStabilityCheck } = {
    ...globalDevModeChecks,
    ...devModeChecks
  };
  return {
    identityFunctionCheck: {
      shouldRun: identityFunctionCheck === "always" || identityFunctionCheck === "once" && firstRun,
      run: runIdentityFunctionCheck
    },
    inputStabilityCheck: {
      shouldRun: inputStabilityCheck === "always" || inputStabilityCheck === "once" && firstRun,
      run: runInputStabilityCheck
    }
  };
};

// src/autotrackMemoize/autotracking.ts
var $REVISION = 0;
var CURRENT_TRACKER = null;
var Cell = class {
  revision = $REVISION;
  _value;
  _lastValue;
  _isEqual = tripleEq;
  constructor(initialValue, isEqual = tripleEq) {
    this._value = this._lastValue = initialValue;
    this._isEqual = isEqual;
  }
  // Whenever a storage value is read, it'll add itself to the current tracker if
  // one exists, entangling its state with that cache.
  get value() {
    CURRENT_TRACKER?.add(this);
    return this._value;
  }
  // Whenever a storage value is updated, we bump the global revision clock,
  // assign the revision for this storage to the new value, _and_ we schedule a
  // rerender. This is important, and it's what makes autotracking  _pull_
  // based. We don't actively tell the caches which depend on the storage that
  // anything has happened. Instead, we recompute the caches when needed.
  set value(newValue) {
    if (this.value === newValue)
      return;
    this._value = newValue;
    this.revision = ++$REVISION;
  }
};
function tripleEq(a, b) {
  return a === b;
}
var TrackingCache = class {
  _cachedValue;
  _cachedRevision = -1;
  _deps = [];
  hits = 0;
  fn;
  constructor(fn) {
    this.fn = fn;
  }
  clear() {
    this._cachedValue = void 0;
    this._cachedRevision = -1;
    this._deps = [];
    this.hits = 0;
  }
  get value() {
    if (this.revision > this._cachedRevision) {
      const { fn } = this;
      const currentTracker = /* @__PURE__ */ new Set();
      const prevTracker = CURRENT_TRACKER;
      CURRENT_TRACKER = currentTracker;
      this._cachedValue = fn();
      CURRENT_TRACKER = prevTracker;
      this.hits++;
      this._deps = Array.from(currentTracker);
      this._cachedRevision = this.revision;
    }
    CURRENT_TRACKER?.add(this);
    return this._cachedValue;
  }
  get revision() {
    return Math.max(...this._deps.map((d) => d.revision), 0);
  }
};
function getValue(cell) {
  if (!(cell instanceof Cell)) {
    console.warn("Not a valid cell! ", cell);
  }
  return cell.value;
}
function setValue(storage, value) {
  if (!(storage instanceof Cell)) {
    throw new TypeError(
      "setValue must be passed a tracked store created with `createStorage`."
    );
  }
  storage.value = storage._lastValue = value;
}
function createCell(initialValue, isEqual = tripleEq) {
  return new Cell(initialValue, isEqual);
}
function createCache(fn) {
  assertIsFunction(
    fn,
    "the first parameter to `createCache` must be a function"
  );
  return new TrackingCache(fn);
}

// src/autotrackMemoize/tracking.ts
var neverEq = (a, b) => false;
function createTag() {
  return createCell(null, neverEq);
}
function dirtyTag(tag, value) {
  setValue(tag, value);
}
var consumeCollection = (node) => {
  let tag = node.collectionTag;
  if (tag === null) {
    tag = node.collectionTag = createTag();
  }
  getValue(tag);
};
var dirtyCollection = (node) => {
  const tag = node.collectionTag;
  if (tag !== null) {
    dirtyTag(tag, null);
  }
};

// src/autotrackMemoize/proxy.ts
var REDUX_PROXY_LABEL = Symbol();
var nextId = 0;
var proto = Object.getPrototypeOf({});
var ObjectTreeNode = class {
  constructor(value) {
    this.value = value;
    this.value = value;
    this.tag.value = value;
  }
  proxy = new Proxy(this, objectProxyHandler);
  tag = createTag();
  tags = {};
  children = {};
  collectionTag = null;
  id = nextId++;
};
var objectProxyHandler = {
  get(node, key) {
    function calculateResult() {
      const { value } = node;
      const childValue = Reflect.get(value, key);
      if (typeof key === "symbol") {
        return childValue;
      }
      if (key in proto) {
        return childValue;
      }
      if (typeof childValue === "object" && childValue !== null) {
        let childNode = node.children[key];
        if (childNode === void 0) {
          childNode = node.children[key] = createNode(childValue);
        }
        if (childNode.tag) {
          getValue(childNode.tag);
        }
        return childNode.proxy;
      } else {
        let tag = node.tags[key];
        if (tag === void 0) {
          tag = node.tags[key] = createTag();
          tag.value = childValue;
        }
        getValue(tag);
        return childValue;
      }
    }
    const res = calculateResult();
    return res;
  },
  ownKeys(node) {
    consumeCollection(node);
    return Reflect.ownKeys(node.value);
  },
  getOwnPropertyDescriptor(node, prop) {
    return Reflect.getOwnPropertyDescriptor(node.value, prop);
  },
  has(node, prop) {
    return Reflect.has(node.value, prop);
  }
};
var ArrayTreeNode = class {
  constructor(value) {
    this.value = value;
    this.value = value;
    this.tag.value = value;
  }
  proxy = new Proxy([this], arrayProxyHandler);
  tag = createTag();
  tags = {};
  children = {};
  collectionTag = null;
  id = nextId++;
};
var arrayProxyHandler = {
  get([node], key) {
    if (key === "length") {
      consumeCollection(node);
    }
    return objectProxyHandler.get(node, key);
  },
  ownKeys([node]) {
    return objectProxyHandler.ownKeys(node);
  },
  getOwnPropertyDescriptor([node], prop) {
    return objectProxyHandler.getOwnPropertyDescriptor(node, prop);
  },
  has([node], prop) {
    return objectProxyHandler.has(node, prop);
  }
};
function createNode(value) {
  if (Array.isArray(value)) {
    return new ArrayTreeNode(value);
  }
  return new ObjectTreeNode(value);
}
function updateNode(node, newValue) {
  const { value, tags, children } = node;
  node.value = newValue;
  if (Array.isArray(value) && Array.isArray(newValue) && value.length !== newValue.length) {
    dirtyCollection(node);
  } else {
    if (value !== newValue) {
      let oldKeysSize = 0;
      let newKeysSize = 0;
      let anyKeysAdded = false;
      for (const _key in value) {
        oldKeysSize++;
      }
      for (const key in newValue) {
        newKeysSize++;
        if (!(key in value)) {
          anyKeysAdded = true;
          break;
        }
      }
      const isDifferent = anyKeysAdded || oldKeysSize !== newKeysSize;
      if (isDifferent) {
        dirtyCollection(node);
      }
    }
  }
  for (const key in tags) {
    const childValue = value[key];
    const newChildValue = newValue[key];
    if (childValue !== newChildValue) {
      dirtyCollection(node);
      dirtyTag(tags[key], newChildValue);
    }
    if (typeof newChildValue === "object" && newChildValue !== null) {
      delete tags[key];
    }
  }
  for (const key in children) {
    const childNode = children[key];
    const newChildValue = newValue[key];
    const childValue = childNode.value;
    if (childValue === newChildValue) {
      continue;
    } else if (typeof newChildValue === "object" && newChildValue !== null) {
      updateNode(childNode, newChildValue);
    } else {
      deleteNode(childNode);
      delete children[key];
    }
  }
}
function deleteNode(node) {
  if (node.tag) {
    dirtyTag(node.tag, null);
  }
  dirtyCollection(node);
  for (const key in node.tags) {
    dirtyTag(node.tags[key], null);
  }
  for (const key in node.children) {
    deleteNode(node.children[key]);
  }
}

// src/lruMemoize.ts
function createSingletonCache(equals) {
  let entry;
  return {
    get(key) {
      if (entry && equals(entry.key, key)) {
        return entry.value;
      }
      return NOT_FOUND;
    },
    put(key, value) {
      entry = { key, value };
    },
    getEntries() {
      return entry ? [entry] : [];
    },
    clear() {
      entry = void 0;
    }
  };
}
function createLruCache(maxSize, equals) {
  let entries = [];
  function get(key) {
    const cacheIndex = entries.findIndex((entry) => equals(key, entry.key));
    if (cacheIndex > -1) {
      const entry = entries[cacheIndex];
      if (cacheIndex > 0) {
        entries.splice(cacheIndex, 1);
        entries.unshift(entry);
      }
      return entry.value;
    }
    return NOT_FOUND;
  }
  function put(key, value) {
    if (get(key) === NOT_FOUND) {
      entries.unshift({ key, value });
      if (entries.length > maxSize) {
        entries.pop();
      }
    }
  }
  function getEntries() {
    return entries;
  }
  function clear() {
    entries = [];
  }
  return { get, put, getEntries, clear };
}
var referenceEqualityCheck = (a, b) => a === b;
function createCacheKeyComparator(equalityCheck) {
  return function areArgumentsShallowlyEqual(prev, next) {
    if (prev === null || next === null || prev.length !== next.length) {
      return false;
    }
    const { length } = prev;
    for (let i = 0; i < length; i++) {
      if (!equalityCheck(prev[i], next[i])) {
        return false;
      }
    }
    return true;
  };
}
function lruMemoize(func, equalityCheckOrOptions) {
  const providedOptions = typeof equalityCheckOrOptions === "object" ? equalityCheckOrOptions : { equalityCheck: equalityCheckOrOptions };
  const {
    equalityCheck = referenceEqualityCheck,
    maxSize = 1,
    resultEqualityCheck
  } = providedOptions;
  const comparator = createCacheKeyComparator(equalityCheck);
  let resultsCount = 0;
  const cache = maxSize <= 1 ? createSingletonCache(comparator) : createLruCache(maxSize, comparator);
  function memoized() {
    let value = cache.get(arguments);
    if (value === NOT_FOUND) {
      value = func.apply(null, arguments);
      resultsCount++;
      if (resultEqualityCheck) {
        const entries = cache.getEntries();
        const matchingEntry = entries.find(
          (entry) => resultEqualityCheck(entry.value, value)
        );
        if (matchingEntry) {
          value = matchingEntry.value;
          resultsCount !== 0 && resultsCount--;
        }
      }
      cache.put(arguments, value);
    }
    return value;
  }
  memoized.clearCache = () => {
    cache.clear();
    memoized.resetResultsCount();
  };
  memoized.resultsCount = () => resultsCount;
  memoized.resetResultsCount = () => {
    resultsCount = 0;
  };
  return memoized;
}

// src/autotrackMemoize/autotrackMemoize.ts
function autotrackMemoize(func) {
  const node = createNode(
    []
  );
  let lastArgs = null;
  const shallowEqual = createCacheKeyComparator(referenceEqualityCheck);
  const cache = createCache(() => {
    const res = func.apply(null, node.proxy);
    return res;
  });
  function memoized() {
    if (!shallowEqual(lastArgs, arguments)) {
      updateNode(node, arguments);
      lastArgs = arguments;
    }
    return cache.value;
  }
  memoized.clearCache = () => {
    return cache.clear();
  };
  return memoized;
}

// src/weakMapMemoize.ts
var StrongRef = class {
  constructor(value) {
    this.value = value;
  }
  deref() {
    return this.value;
  }
};
var Ref = typeof WeakRef !== "undefined" ? WeakRef : StrongRef;
var UNTERMINATED = 0;
var TERMINATED = 1;
function createCacheNode() {
  return {
    s: UNTERMINATED,
    v: void 0,
    o: null,
    p: null
  };
}
function weakMapMemoize(func, options = {}) {
  let fnNode = createCacheNode();
  const { resultEqualityCheck } = options;
  let lastResult;
  let resultsCount = 0;
  function memoized() {
    let cacheNode = fnNode;
    const { length } = arguments;
    for (let i = 0, l = length; i < l; i++) {
      const arg = arguments[i];
      if (typeof arg === "function" || typeof arg === "object" && arg !== null) {
        let objectCache = cacheNode.o;
        if (objectCache === null) {
          cacheNode.o = objectCache = /* @__PURE__ */ new WeakMap();
        }
        const objectNode = objectCache.get(arg);
        if (objectNode === void 0) {
          cacheNode = createCacheNode();
          objectCache.set(arg, cacheNode);
        } else {
          cacheNode = objectNode;
        }
      } else {
        let primitiveCache = cacheNode.p;
        if (primitiveCache === null) {
          cacheNode.p = primitiveCache = /* @__PURE__ */ new Map();
        }
        const primitiveNode = primitiveCache.get(arg);
        if (primitiveNode === void 0) {
          cacheNode = createCacheNode();
          primitiveCache.set(arg, cacheNode);
        } else {
          cacheNode = primitiveNode;
        }
      }
    }
    const terminatedNode = cacheNode;
    let result;
    if (cacheNode.s === TERMINATED) {
      result = cacheNode.v;
    } else {
      result = func.apply(null, arguments);
      resultsCount++;
      if (resultEqualityCheck) {
        const lastResultValue = lastResult?.deref?.() ?? lastResult;
        if (lastResultValue != null && resultEqualityCheck(lastResultValue, result)) {
          result = lastResultValue;
          resultsCount !== 0 && resultsCount--;
        }
        const needsWeakRef = typeof result === "object" && result !== null || typeof result === "function";
        lastResult = needsWeakRef ? new Ref(result) : result;
      }
    }
    terminatedNode.s = TERMINATED;
    terminatedNode.v = result;
    return result;
  }
  memoized.clearCache = () => {
    fnNode = createCacheNode();
    memoized.resetResultsCount();
  };
  memoized.resultsCount = () => resultsCount;
  memoized.resetResultsCount = () => {
    resultsCount = 0;
  };
  return memoized;
}

// src/createSelectorCreator.ts
function createSelectorCreator(memoizeOrOptions, ...memoizeOptionsFromArgs) {
  const createSelectorCreatorOptions = typeof memoizeOrOptions === "function" ? {
    memoize: memoizeOrOptions,
    memoizeOptions: memoizeOptionsFromArgs
  } : memoizeOrOptions;
  const createSelector2 = (...createSelectorArgs) => {
    let recomputations = 0;
    let dependencyRecomputations = 0;
    let lastResult;
    let directlyPassedOptions = {};
    let resultFunc = createSelectorArgs.pop();
    if (typeof resultFunc === "object") {
      directlyPassedOptions = resultFunc;
      resultFunc = createSelectorArgs.pop();
    }
    assertIsFunction(
      resultFunc,
      `createSelector expects an output function after the inputs, but received: [${typeof resultFunc}]`
    );
    const combinedOptions = {
      ...createSelectorCreatorOptions,
      ...directlyPassedOptions
    };
    const {
      memoize,
      memoizeOptions = [],
      argsMemoize = weakMapMemoize,
      argsMemoizeOptions = [],
      devModeChecks = {}
    } = combinedOptions;
    const finalMemoizeOptions = ensureIsArray(memoizeOptions);
    const finalArgsMemoizeOptions = ensureIsArray(argsMemoizeOptions);
    const dependencies = getDependencies(createSelectorArgs);
    const memoizedResultFunc = memoize(function recomputationWrapper() {
      recomputations++;
      return resultFunc.apply(
        null,
        arguments
      );
    }, ...finalMemoizeOptions);
    let firstRun = true;
    const selector = argsMemoize(function dependenciesChecker() {
      dependencyRecomputations++;
      const inputSelectorResults = collectInputSelectorResults(
        dependencies,
        arguments
      );
      lastResult = memoizedResultFunc.apply(null, inputSelectorResults);
      if (true) {
        const { identityFunctionCheck, inputStabilityCheck } = getDevModeChecksExecutionInfo(firstRun, devModeChecks);
        if (identityFunctionCheck.shouldRun) {
          identityFunctionCheck.run(
            resultFunc,
            inputSelectorResults,
            lastResult
          );
        }
        if (inputStabilityCheck.shouldRun) {
          const inputSelectorResultsCopy = collectInputSelectorResults(
            dependencies,
            arguments
          );
          inputStabilityCheck.run(
            { inputSelectorResults, inputSelectorResultsCopy },
            { memoize, memoizeOptions: finalMemoizeOptions },
            arguments
          );
        }
        if (firstRun)
          firstRun = false;
      }
      return lastResult;
    }, ...finalArgsMemoizeOptions);
    return Object.assign(selector, {
      resultFunc,
      memoizedResultFunc,
      dependencies,
      dependencyRecomputations: () => dependencyRecomputations,
      resetDependencyRecomputations: () => {
        dependencyRecomputations = 0;
      },
      lastResult: () => lastResult,
      recomputations: () => recomputations,
      resetRecomputations: () => {
        recomputations = 0;
      },
      memoize,
      argsMemoize
    });
  };
  Object.assign(createSelector2, {
    withTypes: () => createSelector2
  });
  return createSelector2;
}
var createSelector = /* @__PURE__ */ createSelectorCreator(weakMapMemoize);

// src/createStructuredSelector.ts
var createStructuredSelector = Object.assign(
  (inputSelectorsObject, selectorCreator = createSelector) => {
    assertIsObject(
      inputSelectorsObject,
      `createStructuredSelector expects first argument to be an object where each property is a selector, instead received a ${typeof inputSelectorsObject}`
    );
    const inputSelectorKeys = Object.keys(inputSelectorsObject);
    const dependencies = inputSelectorKeys.map(
      (key) => inputSelectorsObject[key]
    );
    const structuredSelector = selectorCreator(
      dependencies,
      (...inputSelectorResults) => {
        return inputSelectorResults.reduce((composition, value, index) => {
          composition[inputSelectorKeys[index]] = value;
          return composition;
        }, {});
      }
    );
    return structuredSelector;
  },
  { withTypes: () => createStructuredSelector }
);

//# sourceMappingURL=reselect.mjs.map

/***/ }

/******/ 	});
/************************************************************************/
/******/ 	// The module cache
/******/ 	var __webpack_module_cache__ = {};
/******/ 	
/******/ 	// The require function
/******/ 	function __webpack_require__(moduleId) {
/******/ 		// Check if module is in cache
/******/ 		var cachedModule = __webpack_module_cache__[moduleId];
/******/ 		if (cachedModule !== undefined) {
/******/ 			return cachedModule.exports;
/******/ 		}
/******/ 		// Create a new module (and put it into the cache)
/******/ 		var module = __webpack_module_cache__[moduleId] = {
/******/ 			// no module.id needed
/******/ 			// no module.loaded needed
/******/ 			exports: {}
/******/ 		};
/******/ 	
/******/ 		// Execute the module function
/******/ 		if (!(moduleId in __webpack_modules__)) {
/******/ 			delete __webpack_module_cache__[moduleId];
/******/ 			var e = new Error("Cannot find module '" + moduleId + "'");
/******/ 			e.code = 'MODULE_NOT_FOUND';
/******/ 			throw e;
/******/ 		}
/******/ 		__webpack_modules__[moduleId](module, module.exports, __webpack_require__);
/******/ 	
/******/ 		// Return the exports of the module
/******/ 		return module.exports;
/******/ 	}
/******/ 	
/************************************************************************/
/******/ 	/* webpack/runtime/compat get default export */
/******/ 	(() => {
/******/ 		// getDefaultExport function for compatibility with non-harmony modules
/******/ 		__webpack_require__.n = (module) => {
/******/ 			var getter = module && module.__esModule ?
/******/ 				() => (module['default']) :
/******/ 				() => (module);
/******/ 			__webpack_require__.d(getter, { a: getter });
/******/ 			return getter;
/******/ 		};
/******/ 	})();
/******/ 	
/******/ 	/* webpack/runtime/create fake namespace object */
/******/ 	(() => {
/******/ 		var getProto = Object.getPrototypeOf ? (obj) => (Object.getPrototypeOf(obj)) : (obj) => (obj.__proto__);
/******/ 		var leafPrototypes;
/******/ 		// create a fake namespace object
/******/ 		// mode & 1: value is a module id, require it
/******/ 		// mode & 2: merge all properties of value into the ns
/******/ 		// mode & 4: return value when already ns object
/******/ 		// mode & 16: return value when it's Promise-like
/******/ 		// mode & 8|1: behave like require
/******/ 		__webpack_require__.t = function(value, mode) {
/******/ 			if(mode & 1) value = this(value);
/******/ 			if(mode & 8) return value;
/******/ 			if(typeof value === 'object' && value) {
/******/ 				if((mode & 4) && value.__esModule) return value;
/******/ 				if((mode & 16) && typeof value.then === 'function') return value;
/******/ 			}
/******/ 			var ns = Object.create(null);
/******/ 			__webpack_require__.r(ns);
/******/ 			var def = {};
/******/ 			leafPrototypes = leafPrototypes || [null, getProto({}), getProto([]), getProto(getProto)];
/******/ 			for(var current = mode & 2 && value; (typeof current == 'object' || typeof current == 'function') && !~leafPrototypes.indexOf(current); current = getProto(current)) {
/******/ 				Object.getOwnPropertyNames(current).forEach((key) => (def[key] = () => (value[key])));
/******/ 			}
/******/ 			def['default'] = () => (value);
/******/ 			__webpack_require__.d(ns, def);
/******/ 			return ns;
/******/ 		};
/******/ 	})();
/******/ 	
/******/ 	/* webpack/runtime/define property getters */
/******/ 	(() => {
/******/ 		// define getter functions for harmony exports
/******/ 		__webpack_require__.d = (exports, definition) => {
/******/ 			for(var key in definition) {
/******/ 				if(__webpack_require__.o(definition, key) && !__webpack_require__.o(exports, key)) {
/******/ 					Object.defineProperty(exports, key, { enumerable: true, get: definition[key] });
/******/ 				}
/******/ 			}
/******/ 		};
/******/ 	})();
/******/ 	
/******/ 	/* webpack/runtime/hasOwnProperty shorthand */
/******/ 	(() => {
/******/ 		__webpack_require__.o = (obj, prop) => (Object.prototype.hasOwnProperty.call(obj, prop))
/******/ 	})();
/******/ 	
/******/ 	/* webpack/runtime/make namespace object */
/******/ 	(() => {
/******/ 		// define __esModule on exports
/******/ 		__webpack_require__.r = (exports) => {
/******/ 			if(typeof Symbol !== 'undefined' && Symbol.toStringTag) {
/******/ 				Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });
/******/ 			}
/******/ 			Object.defineProperty(exports, '__esModule', { value: true });
/******/ 		};
/******/ 	})();
/******/ 	
/************************************************************************/
var __webpack_exports__ = {};
// This entry needs to be wrapped in an IIFE because it needs to be isolated against other modules in the chunk.
(() => {
/*!*******************************!*\
  !*** ./source/admin/admin.js ***!
  \*******************************/
__webpack_require__.r(__webpack_exports__);
/* harmony import */ var _wordpress_element__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @wordpress/element */ "@wordpress/element");
/* harmony import */ var _Main_tsx__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./Main.tsx */ "./source/admin/Main.tsx");
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! react/jsx-runtime */ "react/jsx-runtime");

function render() {
  const container = document.getElementById("pnpnm-admin");
  const color = pnpnm?.settings?.display?.theme?.color ?? "#4D49FC";
  const root = document?.documentElement;
  root?.style.setProperty("--pnpnm-primary", color);
  if (container === null) return;
  const component = /*#__PURE__*/(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_2__.jsx)(_Main_tsx__WEBPACK_IMPORTED_MODULE_1__["default"], {});
  if (_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.createRoot) {
    _wordpress_element__WEBPACK_IMPORTED_MODULE_0__.createRoot(container).render(component);
  } else {
    _wordpress_element__WEBPACK_IMPORTED_MODULE_0__.render(component, container);
  }
  if (false) // removed by dead control flow
{}
}
render();
})();

/******/ })()
;
//# sourceMappingURL=admin.js.map